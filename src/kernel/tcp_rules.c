// Auto-generated eBPF code for TCP rule detection
// Generated from Snort rules

#include <uapi/linux/ptrace.h>
#include <net/sock.h>
#include <bcc/proto.h>


// eBPF Rule Implementation: SID 105
// Rule Name: MALWARE-BACKDOOR - Dagger_1.4.0
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 105
static inline int check_rule_105(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2589
    if (tcph->source != htons(2589))
        return 0;
    // Content matching (simplified)
    // Full pattern: 2|00 00 00 06 00 00 00|Drives|24 00|",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 105
static inline void alert_rule_105(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 105;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR - Dagger_1.4.0", 
                     sizeof("MALWARE-BACKDOOR - Dagger_1.4.0"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 108
// Rule Name: MALWARE-BACKDOOR QAZ Worm Client Login access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 108
static inline int check_rule_108(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7597
    if (tcph->dest != htons(7597))
        return 0;
    // Content matching (simplified)
    // Full pattern: qazwsx.hsq
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 108
static inline void alert_rule_108(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 108;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR QAZ Worm Client Login access", 
                     sizeof("MALWARE-BACKDOOR QAZ Worm Client Login access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 110
// Rule Name: MALWARE-BACKDOOR netbus getinfo
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 110
static inline int check_rule_110(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 12345-12346
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 12345 || dst_port > 12346)
        return 0;
    // Content matching (simplified)
    // Full pattern: GetInfo|0D|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 110
static inline void alert_rule_110(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 110;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR netbus getinfo", 
                     sizeof("MALWARE-BACKDOOR netbus getinfo"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 115
// Rule Name: MALWARE-BACKDOOR NetBus Pro 2.0 connection established
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 115
static inline int check_rule_115(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20034
    if (tcph->source != htons(20034))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['BN|10 00 02 00|",depth 6', '|05 00|",depth 2,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 115
static inline void alert_rule_115(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 115;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR NetBus Pro 2.0 connection established", 
                     sizeof("MALWARE-BACKDOOR NetBus Pro 2.0 connection established"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 117
// Rule Name: MALWARE-BACKDOOR Infector.1.x
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 117
static inline int check_rule_117(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: WHATISIT",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 117
static inline void alert_rule_117(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 117;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Infector.1.x", 
                     sizeof("MALWARE-BACKDOOR Infector.1.x"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 118
// Rule Name: MALWARE-BACKDOOR SatansBackdoor.2.0.Beta
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 118
static inline int check_rule_118(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 666
    if (tcph->source != htons(666))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Remote|3A| ",depth 11,nocase', 'You are connected to me.|0D 0A|Remote|3A| Ready for commands",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 118
static inline void alert_rule_118(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 118;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR SatansBackdoor.2.0.Beta", 
                     sizeof("MALWARE-BACKDOOR SatansBackdoor.2.0.Beta"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 119
// Rule Name: MALWARE-BACKDOOR Doly 2.0 access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 119
static inline int check_rule_119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 6789
    if (tcph->source != htons(6789))
        return 0;
    // Content matching (simplified)
    // Full pattern: Wtzup Use",depth 32
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 119
static inline void alert_rule_119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Doly 2.0 access", 
                     sizeof("MALWARE-BACKDOOR Doly 2.0 access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 121
// Rule Name: MALWARE-BACKDOOR Infector 1.6 Client to Server Connection Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 121
static inline int check_rule_121(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 1000-1300
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1000 || src_port > 1300)
        return 0;
    // Check dst port: 146
    if (tcph->dest != htons(146))
        return 0;
    // Content matching (simplified)
    // Full pattern: FC 
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 121
static inline void alert_rule_121(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 121;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Infector 1.6 Client to Server Connection Request", 
                     sizeof("MALWARE-BACKDOOR Infector 1.6 Client to Server Connection Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 141
// Rule Name: MALWARE-BACKDOOR HackAttack 1.20 Connect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 141
static inline int check_rule_141(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 31785
    if (tcph->source != htons(31785))
        return 0;
    // Content matching (simplified)
    // Full pattern: host
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 141
static inline void alert_rule_141(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 141;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR HackAttack 1.20 Connect", 
                     sizeof("MALWARE-BACKDOOR HackAttack 1.20 Connect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 144
// Rule Name: PROTOCOL-FTP ADMw0rm ftp login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 144
static inline int check_rule_144(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['USER",nocase', 'w0rm",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 144
static inline void alert_rule_144(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 144;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP ADMw0rm ftp login attempt", 
                     sizeof("PROTOCOL-FTP ADMw0rm ftp login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 146
// Rule Name: MALWARE-BACKDOOR NetSphere access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 146
static inline int check_rule_146(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 30100-30102
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 30100 || src_port > 30102)
        return 0;
    // Content matching (simplified)
    // Full pattern: NetSphere
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 146
static inline void alert_rule_146(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 146;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR NetSphere access", 
                     sizeof("MALWARE-BACKDOOR NetSphere access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 147
// Rule Name: MALWARE-BACKDOOR GateCrasher
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 147
static inline int check_rule_147(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 6969
    if (tcph->source != htons(6969))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['GateCrasher",depth 11,nocase', 'Server",distance 0,nocase', 'On-Line...",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 147
static inline void alert_rule_147(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 147;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR GateCrasher", 
                     sizeof("MALWARE-BACKDOOR GateCrasher"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 152
// Rule Name: MALWARE-BACKDOOR BackConstruction 2.1 Connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 152
static inline int check_rule_152(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 5401-5402
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 5401 || src_port > 5402)
        return 0;
    // Content matching (simplified)
    // Full pattern: c|3A 5C|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 152
static inline void alert_rule_152(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 152;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR BackConstruction 2.1 Connection", 
                     sizeof("MALWARE-BACKDOOR BackConstruction 2.1 Connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 157
// Rule Name: MALWARE-BACKDOOR BackConstruction 2.1 Client FTP Open Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 157
static inline int check_rule_157(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 666
    if (tcph->dest != htons(666))
        return 0;
    // Content matching (simplified)
    // Full pattern: FTPON
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 157
static inline void alert_rule_157(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 157;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR BackConstruction 2.1 Client FTP Open Request", 
                     sizeof("MALWARE-BACKDOOR BackConstruction 2.1 Client FTP Open Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 158
// Rule Name: MALWARE-BACKDOOR BackConstruction 2.1 Server FTP Open Reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 158
static inline int check_rule_158(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 666
    if (tcph->source != htons(666))
        return 0;
    // Content matching (simplified)
    // Full pattern: FTP Port open
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 158
static inline void alert_rule_158(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 158;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR BackConstruction 2.1 Server FTP Open Reply", 
                     sizeof("MALWARE-BACKDOOR BackConstruction 2.1 Server FTP Open Reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 163
// Rule Name: MALWARE-BACKDOOR WinCrash 1.0 Server Active
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 163
static inline int check_rule_163(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 5714
    if (tcph->source != htons(5714))
        return 0;
    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->ack == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: |B4 B4|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 163
static inline void alert_rule_163(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 163;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR WinCrash 1.0 Server Active", 
                     sizeof("MALWARE-BACKDOOR WinCrash 1.0 Server Active"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 185
// Rule Name: MALWARE-BACKDOOR CDK
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 185
static inline int check_rule_185(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: ypi0ca",depth 15,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 185
static inline void alert_rule_185(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 185;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR CDK", 
                     sizeof("MALWARE-BACKDOOR CDK"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 208
// Rule Name: MALWARE-BACKDOOR PhaseZero Server Active on Network
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 208
static inline int check_rule_208(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 555
    if (tcph->source != htons(555))
        return 0;
    // Content matching (simplified)
    // Full pattern: phAse zero server",depth 17,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 208
static inline void alert_rule_208(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 208;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR PhaseZero Server Active on Network", 
                     sizeof("MALWARE-BACKDOOR PhaseZero Server Active on Network"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 209
// Rule Name: MALWARE-BACKDOOR w00w00 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 209
static inline int check_rule_209(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: w00w00
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 209
static inline void alert_rule_209(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 209;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR w00w00 attempt", 
                     sizeof("MALWARE-BACKDOOR w00w00 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 210
// Rule Name: MALWARE-BACKDOOR attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 210
static inline int check_rule_210(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: backdoor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 210
static inline void alert_rule_210(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 210;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR attempt", 
                     sizeof("MALWARE-BACKDOOR attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 211
// Rule Name: MALWARE-BACKDOOR MISC r00t attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 211
static inline int check_rule_211(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: r00t
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 211
static inline void alert_rule_211(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 211;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC r00t attempt", 
                     sizeof("MALWARE-BACKDOOR MISC r00t attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 212
// Rule Name: MALWARE-BACKDOOR MISC rewt attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 212
static inline int check_rule_212(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: rewt
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 212
static inline void alert_rule_212(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 212;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC rewt attempt", 
                     sizeof("MALWARE-BACKDOOR MISC rewt attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 213
// Rule Name: MALWARE-BACKDOOR MISC Linux rootkit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 213
static inline int check_rule_213(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: wh00t!
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 213
static inline void alert_rule_213(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 213;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC Linux rootkit attempt", 
                     sizeof("MALWARE-BACKDOOR MISC Linux rootkit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 214
// Rule Name: MALWARE-BACKDOOR MISC Linux rootkit attempt lrkr0x
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 214
static inline int check_rule_214(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: lrkr0x
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 214
static inline void alert_rule_214(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 214;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC Linux rootkit attempt lrkr0x", 
                     sizeof("MALWARE-BACKDOOR MISC Linux rootkit attempt lrkr0x"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 215
// Rule Name: MALWARE-BACKDOOR MISC Linux rootkit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 215
static inline int check_rule_215(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: d13hh[",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 215
static inline void alert_rule_215(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 215;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC Linux rootkit attempt", 
                     sizeof("MALWARE-BACKDOOR MISC Linux rootkit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 216
// Rule Name: MALWARE-BACKDOOR MISC Linux rootkit satori attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 216
static inline int check_rule_216(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: satori
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 216
static inline void alert_rule_216(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 216;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC Linux rootkit satori attempt", 
                     sizeof("MALWARE-BACKDOOR MISC Linux rootkit satori attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 217
// Rule Name: MALWARE-BACKDOOR MISC sm4ck attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 217
static inline int check_rule_217(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: hax0r
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 217
static inline void alert_rule_217(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 217;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC sm4ck attempt", 
                     sizeof("MALWARE-BACKDOOR MISC sm4ck attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 218
// Rule Name: MALWARE-BACKDOOR MISC Solaris 2.5 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 218
static inline int check_rule_218(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: friday
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 218
static inline void alert_rule_218(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 218;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MISC Solaris 2.5 attempt", 
                     sizeof("MALWARE-BACKDOOR MISC Solaris 2.5 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 219
// Rule Name: MALWARE-BACKDOOR HidePak backdoor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 219
static inline int check_rule_219(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: StoogR
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 219
static inline void alert_rule_219(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 219;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR HidePak backdoor attempt", 
                     sizeof("MALWARE-BACKDOOR HidePak backdoor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 220
// Rule Name: MALWARE-BACKDOOR HideSource backdoor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 220
static inline int check_rule_220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: wank
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 220
static inline void alert_rule_220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR HideSource backdoor attempt", 
                     sizeof("MALWARE-BACKDOOR HideSource backdoor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 230
// Rule Name: MALWARE-OTHER shaft client login to handler
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 230
static inline int check_rule_230(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20432
    if (tcph->source != htons(20432))
        return 0;
    // Content matching (simplified)
    // Full pattern: login|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 230
static inline void alert_rule_230(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 230;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER shaft client login to handler", 
                     sizeof("MALWARE-OTHER shaft client login to handler"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 233
// Rule Name: MALWARE-OTHER Trin00 Attacker to Master default startup password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 233
static inline int check_rule_233(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 27665
    if (tcph->dest != htons(27665))
        return 0;
    // Content matching (simplified)
    // Full pattern: betaalmostdone
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 233
static inline void alert_rule_233(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 233;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Trin00 Attacker to Master default startup password", 
                     sizeof("MALWARE-OTHER Trin00 Attacker to Master default startup password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 234
// Rule Name: MALWARE-OTHER Trin00 Attacker to Master default password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 234
static inline int check_rule_234(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 27665
    if (tcph->dest != htons(27665))
        return 0;
    // Content matching (simplified)
    // Full pattern: gOrave
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 234
static inline void alert_rule_234(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 234;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Trin00 Attacker to Master default password", 
                     sizeof("MALWARE-OTHER Trin00 Attacker to Master default password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 235
// Rule Name: MALWARE-OTHER Trin00 Attacker to Master default mdie password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 235
static inline int check_rule_235(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 27665
    if (tcph->dest != htons(27665))
        return 0;
    // Content matching (simplified)
    // Full pattern: killme
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 235
static inline void alert_rule_235(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 235;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Trin00 Attacker to Master default mdie password", 
                     sizeof("MALWARE-OTHER Trin00 Attacker to Master default mdie password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 247
// Rule Name: MALWARE-OTHER mstream client to handler
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 247
static inline int check_rule_247(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 12754
    if (tcph->dest != htons(12754))
        return 0;
    // Content matching (simplified)
    // Full pattern: >
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 247
static inline void alert_rule_247(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 247;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER mstream client to handler", 
                     sizeof("MALWARE-OTHER mstream client to handler"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 248
// Rule Name: MALWARE-OTHER mstream handler to client
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 248
static inline int check_rule_248(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 12754
    if (tcph->source != htons(12754))
        return 0;
    // Content matching (simplified)
    // Full pattern: >
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 248
static inline void alert_rule_248(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 248;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER mstream handler to client", 
                     sizeof("MALWARE-OTHER mstream handler to client"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 250
// Rule Name: MALWARE-OTHER mstream handler to client
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 250
static inline int check_rule_250(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 15104
    if (tcph->source != htons(15104))
        return 0;
    // Content matching (simplified)
    // Full pattern: >
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 250
static inline void alert_rule_250(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 250;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER mstream handler to client", 
                     sizeof("MALWARE-OTHER mstream handler to client"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 255
// Rule Name: PROTOCOL-DNS dns zone transfer via TCP detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 255
static inline int check_rule_255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 00 00 00 00 00|",depth 8,offset 6', '|00 00 FC 00 01|",fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 255
static inline void alert_rule_255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-DNS dns zone transfer via TCP detected", 
                     sizeof("PROTOCOL-DNS dns zone transfer via TCP detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 257
// Rule Name: PROTOCOL-DNS named version attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 257
static inline int check_rule_257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|07|version",offset 12,nocase', '|04|bind|00|",offset 12,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 257
static inline void alert_rule_257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-DNS named version attempt", 
                     sizeof("PROTOCOL-DNS named version attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 258
// Rule Name: SERVER-OTHER Bind Buffer Overflow via NXT records
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 258
static inline int check_rule_258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: ../../../",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 258
static inline void alert_rule_258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bind Buffer Overflow via NXT records", 
                     sizeof("SERVER-OTHER Bind Buffer Overflow via NXT records"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 259
// Rule Name: SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADM
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 259
static inline int check_rule_259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: thisissometempspaceforthesockinaddrinyeahyeahiknowthisislamebutanywaywhocareshorizongotitworkingsoalliscool",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 259
static inline void alert_rule_259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADM", 
                     sizeof("SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADM"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 260
// Rule Name: SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADMROCKS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 260
static inline int check_rule_260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: ADMROCKS
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 260
static inline void alert_rule_260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADMROCKS", 
                     sizeof("SERVER-OTHER Bind Buffer Overflow via NXT records named overflow ADMROCKS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 261
// Rule Name: SERVER-OTHER Bind named overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 261
static inline int check_rule_261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: |CD 80 E8 D7 FF FF FF|/bin/sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 261
static inline void alert_rule_261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bind named overflow attempt", 
                     sizeof("SERVER-OTHER Bind named overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 262
// Rule Name: OS-LINUX x86 Linux overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 262
static inline int check_rule_262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: 1|C0 B0|?1|DB B3 FF|1|C9 CD 80|1|C0|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 262
static inline void alert_rule_262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX x86 Linux overflow attempt", 
                     sizeof("OS-LINUX x86 Linux overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 264
// Rule Name: OS-LINUX x86 Linux overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 264
static inline int check_rule_264(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: 1|C0 B0 02 CD 80 85 C0|uL|EB|L^|B0|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 264
static inline void alert_rule_264(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 264;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX x86 Linux overflow attempt", 
                     sizeof("OS-LINUX x86 Linux overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 265
// Rule Name: OS-LINUX x86 Linux overflow attempt ADMv2
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 265
static inline int check_rule_265(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: |89 F7 29 C7 89 F3 89 F9 89 F2 AC|<|FE|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 265
static inline void alert_rule_265(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 265;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX x86 Linux overflow attempt ADMv2", 
                     sizeof("OS-LINUX x86 Linux overflow attempt ADMv2"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 266
// Rule Name: OS-OTHER x86 FreeBSD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 266
static inline int check_rule_266(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB|n^|C6 06 9A|1|C9 89|N|01 C6|F|05|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 266
static inline void alert_rule_266(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 266;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER x86 FreeBSD overflow attempt", 
                     sizeof("OS-OTHER x86 FreeBSD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 267
// Rule Name: OS-SOLARIS EXPLOIT sparc overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 267
static inline int check_rule_267(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: |90 1A C0 0F 90 02| |08 92 02| |0F D0 23 BF F8|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 267
static inline void alert_rule_267(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 267;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-SOLARIS EXPLOIT sparc overflow attempt", 
                     sizeof("OS-SOLARIS EXPLOIT sparc overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 276
// Rule Name: SERVER-OTHER RealNetworks Audio Server denial of service attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 276
static inline int check_rule_276(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7070
    if (tcph->dest != htons(7070))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF F4 FF FD 06|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 276
static inline void alert_rule_276(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 276;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER RealNetworks Audio Server denial of service attempt", 
                     sizeof("SERVER-OTHER RealNetworks Audio Server denial of service attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 277
// Rule Name: SERVER-OTHER RealNetworks Server template.html
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 277
static inline int check_rule_277(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7070
    if (tcph->dest != htons(7070))
        return 0;
    // Content matching (simplified)
    // Full pattern: /viewsource/template.html?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 277
static inline void alert_rule_277(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 277;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER RealNetworks Server template.html", 
                     sizeof("SERVER-OTHER RealNetworks Server template.html"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 278
// Rule Name: SERVER-OTHER RealNetworks Server template.html
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 278
static inline int check_rule_278(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: /viewsource/template.html?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 278
static inline void alert_rule_278(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 278;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER RealNetworks Server template.html", 
                     sizeof("SERVER-OTHER RealNetworks Server template.html"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 283
// Rule Name: BROWSER-OTHER Netscape 4.7 client overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 283
static inline int check_rule_283(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 80
    if (tcph->source != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: 3|C9 B1 10|?|E9 06|Q<|FA|G3|C0|P|F7 D0|P
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 283
static inline void alert_rule_283(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 283;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-OTHER Netscape 4.7 client overflow", 
                     sizeof("BROWSER-OTHER Netscape 4.7 client overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 286
// Rule Name: PROTOCOL-POP EXPLOIT x86 BSD overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 286
static inline int check_rule_286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: ^|0E|1|C0 B0 3B 8D|~|0E 89 FA 89 F9|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 286
static inline void alert_rule_286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP EXPLOIT x86 BSD overflow", 
                     sizeof("PROTOCOL-POP EXPLOIT x86 BSD overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 287
// Rule Name: PROTOCOL-POP EXPLOIT x86 BSD overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 287
static inline int check_rule_287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: h]^|FF D5 FF D4 FF F5 8B F5 90|f1",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 287
static inline void alert_rule_287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP EXPLOIT x86 BSD overflow", 
                     sizeof("PROTOCOL-POP EXPLOIT x86 BSD overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 288
// Rule Name: PROTOCOL-POP EXPLOIT x86 Linux overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 288
static inline int check_rule_288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: |D8|@|CD 80 E8 D9 FF FF FF|/bin/sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 288
static inline void alert_rule_288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP EXPLOIT x86 Linux overflow", 
                     sizeof("PROTOCOL-POP EXPLOIT x86 Linux overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 289
// Rule Name: PROTOCOL-POP EXPLOIT x86 SCO overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 289
static inline int check_rule_289(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: V|0E|1|C0 B0 3B 8D|~|12 89 F9 89 F9|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 289
static inline void alert_rule_289(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 289;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP EXPLOIT x86 SCO overflow", 
                     sizeof("PROTOCOL-POP EXPLOIT x86 SCO overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 290
// Rule Name: PROTOCOL-POP EXPLOIT qpopper overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 290
static inline int check_rule_290(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: |E8 D9 FF FF FF|/bin/sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 290
static inline void alert_rule_290(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 290;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP EXPLOIT qpopper overflow", 
                     sizeof("PROTOCOL-POP EXPLOIT qpopper overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 292
// Rule Name: OS-LINUX x86 Linux samba overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 292
static inline int check_rule_292(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB|/_|EB|J^|89 FB 89|>|89 F2|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 292
static inline void alert_rule_292(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 292;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX x86 Linux samba overflow", 
                     sizeof("OS-LINUX x86 Linux samba overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300
// Rule Name: OS-SOLARIS Oracle Solaris npls x86 overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 300
static inline int check_rule_300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2766
    if (tcph->dest != htons(2766))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB 23|^3|C0 88|F|FA 89|F|F5 89|6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300
static inline void alert_rule_300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-SOLARIS Oracle Solaris npls x86 overflow", 
                     sizeof("OS-SOLARIS Oracle Solaris npls x86 overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 301
// Rule Name: SERVER-OTHER LPRng overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 301
static inline int check_rule_301(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 515
    if (tcph->dest != htons(515))
        return 0;
    // Content matching (simplified)
    // Full pattern: C|07 89|[|08 8D|K|08 89|C|0C B0 0B CD 80|1|C0 FE C0 CD 80 E8 94 FF FF FF|/bin/sh|0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 301
static inline void alert_rule_301(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 301;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER LPRng overflow", 
                     sizeof("SERVER-OTHER LPRng overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 302
// Rule Name: OS-LINUX Redhat 7.0 lprd overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 302
static inline int check_rule_302(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 515
    if (tcph->dest != htons(515))
        return 0;
    // Content matching (simplified)
    // Full pattern: XXXX%.172u%300|24|n
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 302
static inline void alert_rule_302(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 302;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX Redhat 7.0 lprd overflow", 
                     sizeof("OS-LINUX Redhat 7.0 lprd overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 303
// Rule Name: SERVER-OTHER Bind Buffer Overflow named tsig overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 303
static inline int check_rule_303(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: |AB CD 09 80 00 00 00 01 00 00 00 00 00 00 01 00 01|    |02|a
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 303
static inline void alert_rule_303(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 303;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bind Buffer Overflow named tsig overflow attempt", 
                     sizeof("SERVER-OTHER Bind Buffer Overflow named tsig overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 304
// Rule Name: SERVER-OTHER SCO calserver overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 304
static inline int check_rule_304(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6373
    if (tcph->dest != htons(6373))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB 7F|]U|FE|M|98 FE|M|9B|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 304
static inline void alert_rule_304(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 304;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER SCO calserver overflow", 
                     sizeof("SERVER-OTHER SCO calserver overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 305
// Rule Name: SERVER-OTHER delegate proxy overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 305
static inline int check_rule_305(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: whois|3A|//",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 305
static inline void alert_rule_305(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 305;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER delegate proxy overflow", 
                     sizeof("SERVER-OTHER delegate proxy overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 306
// Rule Name: SERVER-OTHER VQServer admin
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 306
static inline int check_rule_306(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9090
    if (tcph->dest != htons(9090))
        return 0;
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 306
static inline void alert_rule_306(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 306;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER VQServer admin", 
                     sizeof("SERVER-OTHER VQServer admin"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 307
// Rule Name: SERVER-OTHER CHAT IRC topic overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 307
static inline int check_rule_307(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB|K[S2|E4 83 C3 0B|K|88 23 B8|Pw
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 307
static inline void alert_rule_307(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 307;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER CHAT IRC topic overflow", 
                     sizeof("SERVER-OTHER CHAT IRC topic overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 308
// Rule Name: SERVER-OTHER NextFTP client overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 308
static inline int check_rule_308(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 21
    if (tcph->source != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: |B4| |B4|!|8B CC 83 E9 04 8B 19|3|C9|f|B9 10|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 308
static inline void alert_rule_308(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 308;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER NextFTP client overflow", 
                     sizeof("SERVER-OTHER NextFTP client overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 309
// Rule Name: SERVER-MAIL sniffit overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 309
static inline int check_rule_309(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Check TCP flags
    if (!((tcph->ack == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: from|3A 90 90 90 90 90 90 90 90 90 90 90|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 309
static inline void alert_rule_309(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 309;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL sniffit overflow", 
                     sizeof("SERVER-MAIL sniffit overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 310
// Rule Name: SERVER-MAIL x86 windows MailMax overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 310
static inline int check_rule_310(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB|E|EB| [|FC|3|C9 B1 82 8B F3 80|+",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 310
static inline void alert_rule_310(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 310;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL x86 windows MailMax overflow", 
                     sizeof("SERVER-MAIL x86 windows MailMax overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 311
// Rule Name: BROWSER-OTHER Netscape 4.7 unsucessful overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 311
static inline int check_rule_311(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: 3|C9 B1 10|?|E9 06|Q<|FA|G3|C0|P|F7 D0|P
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 311
static inline void alert_rule_311(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 311;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-OTHER Netscape 4.7 unsucessful overflow", 
                     sizeof("BROWSER-OTHER Netscape 4.7 unsucessful overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 320
// Rule Name: PROTOCOL-FINGER cmd_rootsh backdoor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 320
static inline int check_rule_320(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: cmd_rootsh
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 320
static inline void alert_rule_320(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 320;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER cmd_rootsh backdoor attempt", 
                     sizeof("PROTOCOL-FINGER cmd_rootsh backdoor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 321
// Rule Name: PROTOCOL-FINGER account enumeration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 321
static inline int check_rule_321(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: a b c d e f",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 321
static inline void alert_rule_321(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 321;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER account enumeration attempt", 
                     sizeof("PROTOCOL-FINGER account enumeration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 322
// Rule Name: PROTOCOL-FINGER search query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 322
static inline int check_rule_322(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: search
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 322
static inline void alert_rule_322(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 322;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER search query", 
                     sizeof("PROTOCOL-FINGER search query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 323
// Rule Name: PROTOCOL-FINGER root query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 323
static inline int check_rule_323(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: root
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 323
static inline void alert_rule_323(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 323;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER root query", 
                     sizeof("PROTOCOL-FINGER root query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 324
// Rule Name: PROTOCOL-FINGER null request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 324
static inline int check_rule_324(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 324
static inline void alert_rule_324(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 324;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER null request", 
                     sizeof("PROTOCOL-FINGER null request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 326
// Rule Name: PROTOCOL-FINGER remote command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 326
static inline int check_rule_326(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: |3B|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 326
static inline void alert_rule_326(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 326;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER remote command execution attempt", 
                     sizeof("PROTOCOL-FINGER remote command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 327
// Rule Name: PROTOCOL-FINGER remote command pipe execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 327
static inline int check_rule_327(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: |7C|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 327
static inline void alert_rule_327(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 327;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER remote command pipe execution attempt", 
                     sizeof("PROTOCOL-FINGER remote command pipe execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 328
// Rule Name: PROTOCOL-FINGER bomb attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 328
static inline int check_rule_328(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: @@
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 328
static inline void alert_rule_328(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 328;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER bomb attempt", 
                     sizeof("PROTOCOL-FINGER bomb attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 330
// Rule Name: PROTOCOL-FINGER redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 330
static inline int check_rule_330(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: @
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 330
static inline void alert_rule_330(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 330;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER redirection attempt", 
                     sizeof("PROTOCOL-FINGER redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 331
// Rule Name: PROTOCOL-FINGER cybercop query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 331
static inline int check_rule_331(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A|     ",depth 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 331
static inline void alert_rule_331(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 331;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER cybercop query", 
                     sizeof("PROTOCOL-FINGER cybercop query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 332
// Rule Name: PROTOCOL-FINGER 0 query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 332
static inline int check_rule_332(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: 0
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 332
static inline void alert_rule_332(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 332;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER 0 query", 
                     sizeof("PROTOCOL-FINGER 0 query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 333
// Rule Name: PROTOCOL-FINGER . query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 333
static inline int check_rule_333(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: .
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 333
static inline void alert_rule_333(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 333;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER . query", 
                     sizeof("PROTOCOL-FINGER . query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 334
// Rule Name: PROTOCOL-FTP .forward
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 334
static inline int check_rule_334(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: .forward
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 334
static inline void alert_rule_334(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 334;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP .forward", 
                     sizeof("PROTOCOL-FTP .forward"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 335
// Rule Name: PROTOCOL-FTP .rhosts
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 335
static inline int check_rule_335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: .rhosts
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 335
static inline void alert_rule_335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP .rhosts", 
                     sizeof("PROTOCOL-FTP .rhosts"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 336
// Rule Name: PROTOCOL-FTP CWD ~root attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 336
static inline int check_rule_336(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['CWD",nocase', '~root",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 336
static inline void alert_rule_336(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 336;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CWD ~root attempt", 
                     sizeof("PROTOCOL-FTP CWD ~root attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 337
// Rule Name: PROTOCOL-FTP CEL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 337
static inline int check_rule_337(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: CEL",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 337
static inline void alert_rule_337(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 337;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CEL overflow attempt", 
                     sizeof("PROTOCOL-FTP CEL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 353
// Rule Name: PROTOCOL-FTP adm scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 353
static inline int check_rule_353(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS ddd@|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 353
static inline void alert_rule_353(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 353;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP adm scan", 
                     sizeof("PROTOCOL-FTP adm scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 354
// Rule Name: PROTOCOL-FTP iss scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 354
static inline int check_rule_354(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: pass -iss@iss",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 354
static inline void alert_rule_354(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 354;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP iss scan", 
                     sizeof("PROTOCOL-FTP iss scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 355
// Rule Name: PROTOCOL-FTP pass wh00t
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 355
static inline int check_rule_355(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: pass wh00t",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 355
static inline void alert_rule_355(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 355;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP pass wh00t", 
                     sizeof("PROTOCOL-FTP pass wh00t"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 356
// Rule Name: PROTOCOL-FTP passwd retrieval attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 356
static inline int check_rule_356(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['RETR",nocase', 'passwd']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 356
static inline void alert_rule_356(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 356;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP passwd retrieval attempt", 
                     sizeof("PROTOCOL-FTP passwd retrieval attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 357
// Rule Name: PROTOCOL-FTP piss scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 357
static inline int check_rule_357(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: pass -cklaus",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 357
static inline void alert_rule_357(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 357;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP piss scan", 
                     sizeof("PROTOCOL-FTP piss scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 358
// Rule Name: PROTOCOL-FTP saint scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 358
static inline int check_rule_358(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: pass -saint",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 358
static inline void alert_rule_358(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 358;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP saint scan", 
                     sizeof("PROTOCOL-FTP saint scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 359
// Rule Name: PROTOCOL-FTP satan scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 359
static inline int check_rule_359(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: pass -satan",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 359
static inline void alert_rule_359(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 359;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP satan scan", 
                     sizeof("PROTOCOL-FTP satan scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 360
// Rule Name: PROTOCOL-FTP serv-u directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 360
static inline int check_rule_360(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: .%20.",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 360
static inline void alert_rule_360(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 360;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP serv-u directory traversal", 
                     sizeof("PROTOCOL-FTP serv-u directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 361
// Rule Name: PROTOCOL-FTP SITE EXEC attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 361
static inline int check_rule_361(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'EXEC",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 361
static inline void alert_rule_361(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 361;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE EXEC attempt", 
                     sizeof("PROTOCOL-FTP SITE EXEC attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 362
// Rule Name: PROTOCOL-FTP tar parameters
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 362
static inline int check_rule_362(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern:  --use-compress-program ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 362
static inline void alert_rule_362(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 362;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP tar parameters", 
                     sizeof("PROTOCOL-FTP tar parameters"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 489
// Rule Name: PROTOCOL-FTP no password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unknown
// Generated from Snort rule parser output

// Check function for rule 489
static inline int check_rule_489(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 489
static inline void alert_rule_489(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 489;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP no password", 
                     sizeof("PROTOCOL-FTP no password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 490
// Rule Name: SERVER-MAIL battle-mail traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 490
static inline int check_rule_490(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: BattleMail
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 490
static inline void alert_rule_490(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 490;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL battle-mail traffic", 
                     sizeof("SERVER-MAIL battle-mail traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 491
// Rule Name: PROTOCOL-FTP Bad login
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 491
static inline int check_rule_491(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 21
    if (tcph->source != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: 530 ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 491
static inline void alert_rule_491(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 491;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP Bad login", 
                     sizeof("PROTOCOL-FTP Bad login"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 492
// Rule Name: PROTOCOL-TELNET login failed
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 492
static inline int check_rule_492(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: Login failed",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 492
static inline void alert_rule_492(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 492;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET login failed", 
                     sizeof("PROTOCOL-TELNET login failed"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 493
// Rule Name: APP-DETECT psyBNC access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 493
static inline int check_rule_493(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: Welcome!psyBNC@lam3rz.de",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 493
static inline void alert_rule_493(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 493;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT psyBNC access", 
                     sizeof("APP-DETECT psyBNC access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 494
// Rule Name: INDICATOR-COMPROMISE command completed
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 494
static inline int check_rule_494(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Command completed",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 494
static inline void alert_rule_494(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 494;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE command completed", 
                     sizeof("INDICATOR-COMPROMISE command completed"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 495
// Rule Name: INDICATOR-COMPROMISE command error
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 495
static inline int check_rule_495(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Bad command or filename",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 495
static inline void alert_rule_495(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 495;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE command error", 
                     sizeof("INDICATOR-COMPROMISE command error"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 497
// Rule Name: INDICATOR-COMPROMISE file copied ok
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 497
static inline int check_rule_497(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: 1 file|28|s|29| copied",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 497
static inline void alert_rule_497(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 497;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE file copied ok", 
                     sizeof("INDICATOR-COMPROMISE file copied ok"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 505
// Rule Name: SERVER-OTHER Insecure TIMBUKTU Password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 505
static inline int check_rule_505(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1417
    if (tcph->dest != htons(1417))
        return 0;
    // Content matching (simplified)
    // Full pattern: |05 00|>",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 505
static inline void alert_rule_505(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 505;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Insecure TIMBUKTU Password", 
                     sizeof("SERVER-OTHER Insecure TIMBUKTU Password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 507
// Rule Name: PUA-OTHER PCAnywhere Attempted Administrator Login
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 507
static inline int check_rule_507(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 5631
    if (tcph->dest != htons(5631))
        return 0;
    // Content matching (simplified)
    // Full pattern: ADMINISTRATOR
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 507
static inline void alert_rule_507(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 507;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER PCAnywhere Attempted Administrator Login", 
                     sizeof("PUA-OTHER PCAnywhere Attempted Administrator Login"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 508
// Rule Name: SERVER-OTHER gopher proxy
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 508
static inline int check_rule_508(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 70
    if (tcph->dest != htons(70))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['ftp|3A|",fast_pattern,nocase', '@/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 508
static inline void alert_rule_508(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 508;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER gopher proxy", 
                     sizeof("SERVER-OTHER gopher proxy"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 509
// Rule Name: SERVER-WEBAPP PCCS mysql database admin tool access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 509
static inline int check_rule_509(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: pccsmysqladm/incs/dbconnect.inc",depth 36,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 509
static inline void alert_rule_509(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 509;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PCCS mysql database admin tool access", 
                     sizeof("SERVER-WEBAPP PCCS mysql database admin tool access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 510
// Rule Name: POLICY-OTHER HP JetDirect LCD modification attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 510
static inline int check_rule_510(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 9000-9002
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 9000 || dst_port > 9002)
        return 0;
    // Content matching (simplified)
    // Full pattern: @PJL RDYMSG DISPLAY =
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 510
static inline void alert_rule_510(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 510;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER HP JetDirect LCD modification attempt", 
                     sizeof("POLICY-OTHER HP JetDirect LCD modification attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 512
// Rule Name: PUA-OTHER PCAnywhere Failed Login
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 512
static inline int check_rule_512(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 5631-5632
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 5631 || src_port > 5632)
        return 0;
    // Content matching (simplified)
    // Full pattern: Invalid login",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 512
static inline void alert_rule_512(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 512;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER PCAnywhere Failed Login", 
                     sizeof("PUA-OTHER PCAnywhere Failed Login"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 514
// Rule Name: SERVER-OTHER ramen worm
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 514
static inline int check_rule_514(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 27374
    if (tcph->dest != htons(27374))
        return 0;
    // Content matching (simplified)
    // Full pattern: GET ",depth 8,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 514
static inline void alert_rule_514(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 514;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER ramen worm", 
                     sizeof("SERVER-OTHER ramen worm"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 529
// Rule Name: NETBIOS DCERPC NCACN-IP-TCP srvsvc NetrShareEnum null policy handle attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 529
static inline int check_rule_529(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 00 00 00|",within 4,distance 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 529
static inline void alert_rule_529(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 529;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS DCERPC NCACN-IP-TCP srvsvc NetrShareEnum null policy handle attempt", 
                     sizeof("NETBIOS DCERPC NCACN-IP-TCP srvsvc NetrShareEnum null policy handle attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 530
// Rule Name: OS-WINDOWS NT NULL session
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 530
static inline int check_rule_530(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 00 00 00|W|00|i|00|n|00|d|00|o|00|w|00|s|00| |00|N|00|T|00| |00|1|00|3|00|8|00|1
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 530
static inline void alert_rule_530(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 530;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS NT NULL session", 
                     sizeof("OS-WINDOWS NT NULL session"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 534
// Rule Name: NETBIOS SMB CD..
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 534
static inline int check_rule_534(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: |5C|../|00 00 00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 534
static inline void alert_rule_534(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 534;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB CD..", 
                     sizeof("NETBIOS SMB CD.."));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 535
// Rule Name: NETBIOS SMB CD...
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 535
static inline int check_rule_535(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: |5C|...|00 00 00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 535
static inline void alert_rule_535(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 535;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB CD...", 
                     sizeof("NETBIOS SMB CD..."));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 540
// Rule Name: POLICY-SOCIAL Microsoft MSN message
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 540
static inline int check_rule_540(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 1863
    if (tcph->dest != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MSG ",depth 4', 'Content-Type|3A|",nocase', 'text/plain",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 540
static inline void alert_rule_540(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 540;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN message", 
                     sizeof("POLICY-SOCIAL Microsoft MSN message"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 541
// Rule Name: POLICY-SOCIAL ICQ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 541
static inline int check_rule_541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: User-Agent|3A|ICQ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 541
static inline void alert_rule_541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL ICQ access", 
                     sizeof("POLICY-SOCIAL ICQ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 542
// Rule Name: POLICY-SOCIAL IRC nick change
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 542
static inline int check_rule_542(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: NICK ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 542
static inline void alert_rule_542(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 542;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC nick change", 
                     sizeof("POLICY-SOCIAL IRC nick change"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 543
// Rule Name: INDICATOR-COMPROMISE FTP 'STOR 1MB' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 543
static inline int check_rule_543(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['STOR",nocase', '1MB",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 543
static inline void alert_rule_543(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 543;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'STOR 1MB' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'STOR 1MB' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 544
// Rule Name: INDICATOR-COMPROMISE FTP 'RETR 1MB' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 544
static inline int check_rule_544(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['RETR",nocase', '1MB",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 544
static inline void alert_rule_544(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 544;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'RETR 1MB' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'RETR 1MB' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 545
// Rule Name: INDICATOR-COMPROMISE FTP 'CWD / ' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 545
static inline int check_rule_545(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['CWD",nocase', '/ ",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 545
static inline void alert_rule_545(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 545;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'CWD / ' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'CWD / ' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 546
// Rule Name: INDICATOR-COMPROMISE FTP 'CWD  ' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 546
static inline int check_rule_546(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: CWD  ",depth 5,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 546
static inline void alert_rule_546(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 546;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'CWD  ' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'CWD  ' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 547
// Rule Name: INDICATOR-COMPROMISE FTP 'MKD  ' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 547
static inline int check_rule_547(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MKD  ",depth 5,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 547
static inline void alert_rule_547(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 547;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'MKD  ' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'MKD  ' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 548
// Rule Name: INDICATOR-COMPROMISE FTP 'MKD .' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 548
static inline int check_rule_548(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MKD .",depth 5,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 548
static inline void alert_rule_548(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 548;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'MKD .' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'MKD .' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 553
// Rule Name: POLICY-OTHER FTP anonymous login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 553
static inline int check_rule_553(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 553
static inline void alert_rule_553(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 553;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER FTP anonymous login attempt", 
                     sizeof("POLICY-OTHER FTP anonymous login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 554
// Rule Name: INDICATOR-COMPROMISE FTP 'MKD / ' possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 554
static inline int check_rule_554(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MKD",nocase', '/ ",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 554
static inline void alert_rule_554(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 554;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP 'MKD / ' possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP 'MKD / ' possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 555
// Rule Name: POLICY-OTHER WinGate telnet server response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 555
static inline int check_rule_555(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: WinGate>
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 555
static inline void alert_rule_555(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 555;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER WinGate telnet server response", 
                     sizeof("POLICY-OTHER WinGate telnet server response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 556
// Rule Name: PUA-P2P Outbound GNUTella client request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 556
static inline int check_rule_556(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: GNUTELLA CONNECT",depth 40
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 556
static inline void alert_rule_556(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 556;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P Outbound GNUTella client request", 
                     sizeof("PUA-P2P Outbound GNUTella client request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 557
// Rule Name: PUA-P2P GNUTella client request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 557
static inline int check_rule_557(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: GNUTELLA OK",depth 40
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 557
static inline void alert_rule_557(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 557;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P GNUTella client request", 
                     sizeof("PUA-P2P GNUTella client request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 560
// Rule Name: APP-DETECT VNC server response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 560
static inline int check_rule_560(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['RFB 0",depth 5', '.0",depth 2,offset 7']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 560
static inline void alert_rule_560(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 560;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT VNC server response", 
                     sizeof("APP-DETECT VNC server response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 567
// Rule Name: SERVER-MAIL SMTP relaying denied
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 567
static inline int check_rule_567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SMTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 25
    if (tcph->source != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: 550 5.7.1",depth 70
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 567
static inline void alert_rule_567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL SMTP relaying denied", 
                     sizeof("SERVER-MAIL SMTP relaying denied"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 568
// Rule Name: POLICY-OTHER HP JetDirect LCD modification attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 568
static inline int check_rule_568(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9100
    if (tcph->dest != htons(9100))
        return 0;
    // Content matching (simplified)
    // Full pattern: @PJL RDYMSG DISPLAY =
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 568
static inline void alert_rule_568(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 568;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER HP JetDirect LCD modification attempt", 
                     sizeof("POLICY-OTHER HP JetDirect LCD modification attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 569
// Rule Name: PROTOCOL-RPC snmpXdmi overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 569
static inline int check_rule_569(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 87 99|",depth 4,offset 16', '|00 00 01 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 569
static inline void alert_rule_569(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 569;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC snmpXdmi overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC snmpXdmi overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 572
// Rule Name: PROTOCOL-RPC DOS ttdbserv Solaris
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 572
static inline int check_rule_572(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 32771-34000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 32771 || dst_port > 34000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 00|",depth 4,offset 8', '|00 01 86 F3 00 00 00 01 00 00 00 0F 00 00 00 01|",depth 32,offset 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 572
static inline void alert_rule_572(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 572;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC DOS ttdbserv Solaris", 
                     sizeof("PROTOCOL-RPC DOS ttdbserv Solaris"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 574
// Rule Name: PROTOCOL-RPC mountd TCP export request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 574
static inline int check_rule_574(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 05|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 574
static inline void alert_rule_574(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 574;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP export request", 
                     sizeof("PROTOCOL-RPC mountd TCP export request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 591
// Rule Name: PROTOCOL-RPC portmap ypupdated request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 591
static inline int check_rule_591(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 BC|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 591
static inline void alert_rule_591(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 591;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap ypupdated request TCP", 
                     sizeof("PROTOCOL-RPC portmap ypupdated request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 593
// Rule Name: PROTOCOL-RPC portmap snmpXdmi request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 593
static inline int check_rule_593(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87 99|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 593
static inline void alert_rule_593(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 593;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap snmpXdmi request TCP", 
                     sizeof("PROTOCOL-RPC portmap snmpXdmi request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 595
// Rule Name: PROTOCOL-RPC portmap espd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 595
static inline int check_rule_595(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 05 F7|u",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 595
static inline void alert_rule_595(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 595;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap espd request TCP", 
                     sizeof("PROTOCOL-RPC portmap espd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 598
// Rule Name: PROTOCOL-RPC portmap listing TCP 111
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 598
static inline int check_rule_598(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 04|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 598
static inline void alert_rule_598(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 598;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap listing TCP 111", 
                     sizeof("PROTOCOL-RPC portmap listing TCP 111"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 599
// Rule Name: PROTOCOL-RPC portmap listing TCP 32771
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 599
static inline int check_rule_599(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 32771
    if (tcph->dest != htons(32771))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 04|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 599
static inline void alert_rule_599(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 599;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap listing TCP 32771", 
                     sizeof("PROTOCOL-RPC portmap listing TCP 32771"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 601
// Rule Name: PROTOCOL-SERVICES rlogin LinuxNIS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 601
static inline int check_rule_601(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 513
    if (tcph->dest != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: |3A 3A 3A 3A 3A 3A 3A 3A 00 3A 3A 3A 3A 3A 3A 3A 3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 601
static inline void alert_rule_601(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 601;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin LinuxNIS", 
                     sizeof("PROTOCOL-SERVICES rlogin LinuxNIS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 602
// Rule Name: PROTOCOL-SERVICES rlogin bin
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 602
static inline int check_rule_602(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 513
    if (tcph->dest != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: bin|00|bin|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 602
static inline void alert_rule_602(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 602;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin bin", 
                     sizeof("PROTOCOL-SERVICES rlogin bin"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 603
// Rule Name: PROTOCOL-SERVICES rlogin echo++
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 603
static inline int check_rule_603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 513
    if (tcph->dest != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: echo |22| + + |22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 603
static inline void alert_rule_603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin echo++", 
                     sizeof("PROTOCOL-SERVICES rlogin echo++"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 604
// Rule Name: PROTOCOL-SERVICES Unix rlogin froot parameter root access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 604
static inline int check_rule_604(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 513
    if (tcph->dest != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: -froot|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 604
static inline void alert_rule_604(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 604;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES Unix rlogin froot parameter root access attempt", 
                     sizeof("PROTOCOL-SERVICES Unix rlogin froot parameter root access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 605
// Rule Name: PROTOCOL-SERVICES rlogin login failure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 605
static inline int check_rule_605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 513
    if (tcph->source != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: login incorrect",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 605
static inline void alert_rule_605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin login failure", 
                     sizeof("PROTOCOL-SERVICES rlogin login failure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 606
// Rule Name: PROTOCOL-SERVICES rlogin root
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 606
static inline int check_rule_606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 513
    if (tcph->dest != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: root|00|root|00|",depth 11
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 606
static inline void alert_rule_606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin root", 
                     sizeof("PROTOCOL-SERVICES rlogin root"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 607
// Rule Name: PROTOCOL-SERVICES rsh bin
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 607
static inline int check_rule_607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 514
    if (tcph->dest != htons(514))
        return 0;
    // Content matching (simplified)
    // Full pattern: bin|00|bin|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 607
static inline void alert_rule_607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rsh bin", 
                     sizeof("PROTOCOL-SERVICES rsh bin"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 608
// Rule Name: PROTOCOL-SERVICES rsh echo + +
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 608
static inline int check_rule_608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 514
    if (tcph->dest != htons(514))
        return 0;
    // Content matching (simplified)
    // Full pattern: echo |22|+ +|22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 608
static inline void alert_rule_608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rsh echo + +", 
                     sizeof("PROTOCOL-SERVICES rsh echo + +"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 609
// Rule Name: PROTOCOL-SERVICES rsh froot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 609
static inline int check_rule_609(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 514
    if (tcph->dest != htons(514))
        return 0;
    // Content matching (simplified)
    // Full pattern: -froot|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 609
static inline void alert_rule_609(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 609;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rsh froot", 
                     sizeof("PROTOCOL-SERVICES rsh froot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 610
// Rule Name: PROTOCOL-SERVICES rsh root
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 610
static inline int check_rule_610(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 514
    if (tcph->dest != htons(514))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00|root|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 610
static inline void alert_rule_610(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 610;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rsh root", 
                     sizeof("PROTOCOL-SERVICES rsh root"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 611
// Rule Name: PROTOCOL-SERVICES rlogin login failure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 611
static inline int check_rule_611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 513
    if (tcph->source != htons(513))
        return 0;
    // Content matching (simplified)
    // Full pattern: |01|rlogind|3A| Permission denied.",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 611
static inline void alert_rule_611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rlogin login failure", 
                     sizeof("PROTOCOL-SERVICES rlogin login failure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 613
// Rule Name: INDICATOR-SCAN myscan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 613
static inline int check_rule_613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 10101
    if (tcph->source != htons(10101))
        return 0;
    // Check TCP flags
    if (!((tcph->syn == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 613
static inline void alert_rule_613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN myscan", 
                     sizeof("INDICATOR-SCAN myscan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 614
// Rule Name: MALWARE-BACKDOOR hack-a-tack attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 614
static inline int check_rule_614(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 31790
    if (tcph->source != htons(31790))
        return 0;
    // Check dst port: 31789
    if (tcph->dest != htons(31789))
        return 0;
    // Check TCP flags
    if (!((tcph->ack == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: A",depth 1
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 614
static inline void alert_rule_614(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 614;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR hack-a-tack attempt", 
                     sizeof("MALWARE-BACKDOOR hack-a-tack attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 616
// Rule Name: INDICATOR-SCAN ident version request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 616
static inline int check_rule_616(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 113
    if (tcph->dest != htons(113))
        return 0;
    // Content matching (simplified)
    // Full pattern: VERSION|0A|",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 616
static inline void alert_rule_616(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 616;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN ident version request", 
                     sizeof("INDICATOR-SCAN ident version request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 619
// Rule Name: INDICATOR-SCAN cybercop os probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 619
static inline int check_rule_619(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->fin == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 619
static inline void alert_rule_619(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 619;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN cybercop os probe", 
                     sizeof("INDICATOR-SCAN cybercop os probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 622
// Rule Name: INDICATOR-SCAN ipEye SYN scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 622
static inline int check_rule_622(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Check TCP flags
    if (!((tcph->syn == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 622
static inline void alert_rule_622(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 622;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN ipEye SYN scan", 
                     sizeof("INDICATOR-SCAN ipEye SYN scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 626
// Rule Name: INDICATOR-SCAN cybercop os PA12 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 626
static inline int check_rule_626(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Check TCP flags
    if (!((tcph->ack == 1) && (tcph->psh == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: AAAAAAAAAAAAAAAA",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 626
static inline void alert_rule_626(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 626;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN cybercop os PA12 attempt", 
                     sizeof("INDICATOR-SCAN cybercop os PA12 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 627
// Rule Name: INDICATOR-SCAN cybercop os SFU12 probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 627
static inline int check_rule_627(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->fin == 1) && (tcph->urg == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: AAAAAAAAAAAAAAAA",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 627
static inline void alert_rule_627(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 627;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN cybercop os SFU12 probe", 
                     sizeof("INDICATOR-SCAN cybercop os SFU12 probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 630
// Rule Name: INDICATOR-SCAN synscan portscan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 630
static inline int check_rule_630(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->fin == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 630
static inline void alert_rule_630(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 630;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN synscan portscan", 
                     sizeof("INDICATOR-SCAN synscan portscan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 631
// Rule Name: SERVER-MAIL ehlo cybercop attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 631
static inline int check_rule_631(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ehlo cybercop|0A|quit|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 631
static inline void alert_rule_631(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 631;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL ehlo cybercop attempt", 
                     sizeof("SERVER-MAIL ehlo cybercop attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 632
// Rule Name: SERVER-MAIL expn cybercop attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 632
static inline int check_rule_632(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: expn cybercop",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 632
static inline void alert_rule_632(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 632;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL expn cybercop attempt", 
                     sizeof("SERVER-MAIL expn cybercop attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 654
// Rule Name: SERVER-MAIL RCPT TO overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 654
static inline int check_rule_654(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: rcpt to|3A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 654
static inline void alert_rule_654(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 654;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL RCPT TO overflow", 
                     sizeof("SERVER-MAIL RCPT TO overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 655
// Rule Name: SERVER-MAIL Sendmail 8.6.9 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 655
static inline int check_rule_655(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check src port: 113
    if (tcph->source != htons(113))
        return 0;
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A|D/
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 655
static inline void alert_rule_655(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 655;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.9 exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.9 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 657
// Rule Name: SERVER-MAIL Netmanager chameleon SMTPd buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 657
static inline int check_rule_657(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: HELP",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 657
static inline void alert_rule_657(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 657;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Netmanager chameleon SMTPd buffer overflow attempt", 
                     sizeof("SERVER-MAIL Netmanager chameleon SMTPd buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 658
// Rule Name: SERVER-MAIL Microsoft Windows Exchange Server 5.5 mime DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 658
static inline int check_rule_658(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: charset = |22 22|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 658
static inline void alert_rule_658(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 658;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Microsoft Windows Exchange Server 5.5 mime DOS", 
                     sizeof("SERVER-MAIL Microsoft Windows Exchange Server 5.5 mime DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 659
// Rule Name: SERVER-MAIL Sendmail expn decode
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 659
static inline int check_rule_659(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['expn",nocase', 'decode",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 659
static inline void alert_rule_659(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 659;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail expn decode", 
                     sizeof("SERVER-MAIL Sendmail expn decode"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 660
// Rule Name: SERVER-MAIL expn root
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 660
static inline int check_rule_660(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['expn",nocase', 'root",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 660
static inline void alert_rule_660(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 660;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL expn root", 
                     sizeof("SERVER-MAIL expn root"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 661
// Rule Name: SERVER-MAIL Majordomo ifs
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 661
static inline int check_rule_661(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: eply-to|3A| a~.`/bin/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 661
static inline void alert_rule_661(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 661;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Majordomo ifs", 
                     sizeof("SERVER-MAIL Majordomo ifs"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 662
// Rule Name: SERVER-MAIL Sendmail 5.5.5 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 662
static inline int check_rule_662(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: mail from|3A| |22 7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 662
static inline void alert_rule_662(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 662;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 5.5.5 exploit", 
                     sizeof("SERVER-MAIL Sendmail 5.5.5 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 663
// Rule Name: SERVER-MAIL Sendmail rcpt to command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 663
static inline int check_rule_663(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: rcpt to|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 663
static inline void alert_rule_663(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 663;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail rcpt to command attempt", 
                     sizeof("SERVER-MAIL Sendmail rcpt to command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 664
// Rule Name: SERVER-MAIL Sendmail RCPT TO decode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 664
static inline int check_rule_664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['rcpt to|3A|",nocase', 'decode",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 664
static inline void alert_rule_664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail RCPT TO decode attempt", 
                     sizeof("SERVER-MAIL Sendmail RCPT TO decode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 665
// Rule Name: SERVER-MAIL Sendmail 5.6.5 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 665
static inline int check_rule_665(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: MAIL FROM|3A| |7C|/usr/ucb/tail",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 665
static inline void alert_rule_665(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 665;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 5.6.5 exploit", 
                     sizeof("SERVER-MAIL Sendmail 5.6.5 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 667
// Rule Name: SERVER-MAIL Sendmail 8.6.10 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 667
static inline int check_rule_667(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Croot|0D 0A|Mprog, P=/bin/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 667
static inline void alert_rule_667(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 667;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.10 exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.10 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 668
// Rule Name: SERVER-MAIL Sendmail 8.6.10 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 668
static inline int check_rule_668(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Croot|09 09 09 09 09 09 09|Mprog,P=/bin",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 668
static inline void alert_rule_668(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 668;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.10 exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.10 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 669
// Rule Name: SERVER-MAIL Sendmail 8.6.9 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 669
static inline int check_rule_669(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A|Croot|0A|Mprog",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 669
static inline void alert_rule_669(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 669;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.9 exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.9 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 670
// Rule Name: SERVER-MAIL Sendmail 8.6.9 exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 670
static inline int check_rule_670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A|C|3A|daemon|0A|R",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 670
static inline void alert_rule_670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.9 exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.9 exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 671
// Rule Name: SERVER-MAIL Sendmail 8.6.9c exploit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 671
static inline int check_rule_671(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A|Croot|0D 0A|Mprog",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 671
static inline void alert_rule_671(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 671;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail 8.6.9c exploit", 
                     sizeof("SERVER-MAIL Sendmail 8.6.9c exploit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 672
// Rule Name: SERVER-MAIL vrfy decode
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 672
static inline int check_rule_672(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['vrfy",nocase', 'decode",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 672
static inline void alert_rule_672(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 672;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL vrfy decode", 
                     sizeof("SERVER-MAIL vrfy decode"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 673
// Rule Name: SQL sp_start_job - program execution
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 673
static inline int check_rule_673(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|s|00|t|00|a|00|r|00|t|00|_|00|j|00|o|00|b|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 673
static inline void alert_rule_673(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 673;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_start_job - program execution", 
                     sizeof("SQL sp_start_job - program execution"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 676
// Rule Name: SQL sp_start_job - program execution
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 676
static inline int check_rule_676(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|s|00|t|00|a|00|r|00|t|00|_|00|j|00|o|00|b|00|",depth 32,offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 676
static inline void alert_rule_676(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 676;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_start_job - program execution", 
                     sizeof("SQL sp_start_job - program execution"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 677
// Rule Name: SQL sp_password password change
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 677
static inline int check_rule_677(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|p|00|a|00|s|00|s|00|w|00|o|00|r|00|d|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 677
static inline void alert_rule_677(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 677;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_password password change", 
                     sizeof("SQL sp_password password change"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 678
// Rule Name: SQL sp_delete_alert log file deletion
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 678
static inline int check_rule_678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|d|00|e|00|l|00|e|00|t|00|e|00|_|00|a|00|l|00|e|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 678
static inline void alert_rule_678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_delete_alert log file deletion", 
                     sizeof("SQL sp_delete_alert log file deletion"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 679
// Rule Name: SQL sp_adduser database user creation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 679
static inline int check_rule_679(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|a|00|d|00|d|00|u|00|s|00|e|00|r|00|",depth 32,offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 679
static inline void alert_rule_679(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 679;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_adduser database user creation", 
                     sizeof("SQL sp_adduser database user creation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 681
// Rule Name: SQL xp_cmdshell program execution
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 681
static inline int check_rule_681(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|c|00|m|00|d|00|s|00|h|00|e|00|l|00|l|00|",offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 681
static inline void alert_rule_681(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 681;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_cmdshell program execution", 
                     sizeof("SQL xp_cmdshell program execution"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 683
// Rule Name: SQL sp_password - password change
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 683
static inline int check_rule_683(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|p|00|a|00|s|00|s|00|w|00|o|00|r|00|d|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 683
static inline void alert_rule_683(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 683;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_password - password change", 
                     sizeof("SQL sp_password - password change"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 684
// Rule Name: SQL sp_delete_alert log file deletion
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 684
static inline int check_rule_684(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|d|00|e|00|l|00|e|00|t|00|e|00|_|00|a|00|l|00|e|00|r|00|t|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 684
static inline void alert_rule_684(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 684;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_delete_alert log file deletion", 
                     sizeof("SQL sp_delete_alert log file deletion"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 685
// Rule Name: SQL sp_adduser - database user creation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 685
static inline int check_rule_685(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|p|00|_|00|a|00|d|00|d|00|u|00|s|00|e|00|r|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 685
static inline void alert_rule_685(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 685;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sp_adduser - database user creation", 
                     sizeof("SQL sp_adduser - database user creation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 686
// Rule Name: SERVER-MSSQL xp_reg* - registry access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 686
static inline int check_rule_686(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|r|00|e|00|g|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 686
static inline void alert_rule_686(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 686;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MSSQL xp_reg* - registry access", 
                     sizeof("SERVER-MSSQL xp_reg* - registry access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 687
// Rule Name: SQL xp_cmdshell - program execution
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 687
static inline int check_rule_687(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|c|00|m|00|d|00|s|00|h|00|e|00|l|00|l|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 687
static inline void alert_rule_687(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 687;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_cmdshell - program execution", 
                     sizeof("SQL xp_cmdshell - program execution"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 688
// Rule Name: SQL sa login failed
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 688
static inline int check_rule_688(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SQL_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 1433
    if (tcph->source != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: Login failed for user 'sa'",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 688
static inline void alert_rule_688(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 688;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sa login failed", 
                     sizeof("SQL sa login failed"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 689
// Rule Name: SERVER-MSSQL xp_reg* registry access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 689
static inline int check_rule_689(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|r|00|e|00|g|00|",depth 32,offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 689
static inline void alert_rule_689(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 689;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MSSQL xp_reg* registry access", 
                     sizeof("SERVER-MSSQL xp_reg* registry access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 691
// Rule Name: INDICATOR-SHELLCODE shellcode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 691
static inline int check_rule_691(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: 9 |D0 00 92 01 C2 00|R|00|U|00|9 |EC 00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 691
static inline void alert_rule_691(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 691;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE shellcode attempt", 
                     sizeof("INDICATOR-SHELLCODE shellcode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 692
// Rule Name: INDICATOR-SHELLCODE shellcode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 692
static inline int check_rule_692(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: 9 |D0 00 92 01 C2 00|R|00|U|00|9 |EC 00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 692
static inline void alert_rule_692(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 692;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE shellcode attempt", 
                     sizeof("INDICATOR-SHELLCODE shellcode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 693
// Rule Name: INDICATOR-SHELLCODE shellcode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 693
static inline int check_rule_693(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: H|00|%|00|x|00|w|00 90 00 90 00 90 00 90 00 90 00|3|00 C0 00|P|00|h|00|.|00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 693
static inline void alert_rule_693(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 693;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE shellcode attempt", 
                     sizeof("INDICATOR-SHELLCODE shellcode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 694
// Rule Name: INDICATOR-SHELLCODE shellcode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 694
static inline int check_rule_694(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: H|00|%|00|x|00|w|00 90 00 90 00 90 00 90 00 90 00|3|00 C0 00|P|00|h|00|.|00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 694
static inline void alert_rule_694(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 694;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE shellcode attempt", 
                     sizeof("INDICATOR-SHELLCODE shellcode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 695
// Rule Name: SERVER-MSSQL xp_sprintf possible buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 695
static inline int check_rule_695(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|s|00|p|00|r|00|i|00|n|00|t|00|f|00|",offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 695
static inline void alert_rule_695(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 695;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MSSQL xp_sprintf possible buffer overflow", 
                     sizeof("SERVER-MSSQL xp_sprintf possible buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 704
// Rule Name: SERVER-MSSQL xp_sprintf possible buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 704
static inline int check_rule_704(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|s|00|p|00|r|00|i|00|n|00|t|00|f|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 704
static inline void alert_rule_704(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 704;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MSSQL xp_sprintf possible buffer overflow", 
                     sizeof("SERVER-MSSQL xp_sprintf possible buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 709
// Rule Name: PROTOCOL-TELNET 4Dgifts SGI account attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 709
static inline int check_rule_709(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: 4Dgifts
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 709
static inline void alert_rule_709(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 709;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET 4Dgifts SGI account attempt", 
                     sizeof("PROTOCOL-TELNET 4Dgifts SGI account attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 710
// Rule Name: PROTOCOL-TELNET EZsetup account attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 710
static inline int check_rule_710(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: OutOfBox
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 710
static inline void alert_rule_710(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 710;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET EZsetup account attempt", 
                     sizeof("PROTOCOL-TELNET EZsetup account attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 711
// Rule Name: PROTOCOL-TELNET SGI telnetd format bug
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 711
static inline int check_rule_711(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['_RLD",fast_pattern,nocase', 'bin/sh']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 711
static inline void alert_rule_711(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 711;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET SGI telnetd format bug", 
                     sizeof("PROTOCOL-TELNET SGI telnetd format bug"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 712
// Rule Name: PROTOCOL-TELNET ld_library_path
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 712
static inline int check_rule_712(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: ld_library_path",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 712
static inline void alert_rule_712(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 712;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET ld_library_path", 
                     sizeof("PROTOCOL-TELNET ld_library_path"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 713
// Rule Name: PROTOCOL-TELNET livingston DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 713
static inline int check_rule_713(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF F3 FF F3 FF F3 FF F3 FF F3|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 713
static inline void alert_rule_713(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 713;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET livingston DOS", 
                     sizeof("PROTOCOL-TELNET livingston DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 714
// Rule Name: PROTOCOL-TELNET resolv_host_conf
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 714
static inline int check_rule_714(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: resolv_host_conf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 714
static inline void alert_rule_714(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 714;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET resolv_host_conf", 
                     sizeof("PROTOCOL-TELNET resolv_host_conf"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 715
// Rule Name: PROTOCOL-TELNET Attempted SU from wrong group
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 715
static inline int check_rule_715(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: to su root",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 715
static inline void alert_rule_715(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 715;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET Attempted SU from wrong group", 
                     sizeof("PROTOCOL-TELNET Attempted SU from wrong group"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 717
// Rule Name: PROTOCOL-TELNET not on console
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 717
static inline int check_rule_717(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: not on system console",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 717
static inline void alert_rule_717(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 717;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET not on console", 
                     sizeof("PROTOCOL-TELNET not on console"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 718
// Rule Name: PROTOCOL-TELNET login incorrect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 718
static inline int check_rule_718(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: Login incorrect
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 718
static inline void alert_rule_718(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 718;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET login incorrect", 
                     sizeof("PROTOCOL-TELNET login incorrect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 719
// Rule Name: PROTOCOL-TELNET root login
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 719
static inline int check_rule_719(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: login|3A| root",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 719
static inline void alert_rule_719(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 719;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET root login", 
                     sizeof("PROTOCOL-TELNET root login"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 803
// Rule Name: SERVER-WEBAPP HyperSeek hsx.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 803
static inline int check_rule_803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/hsx.cgi', '../../', '%00",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 803
static inline void alert_rule_803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP HyperSeek hsx.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP HyperSeek hsx.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 804
// Rule Name: SERVER-WEBAPP SWSoft ASPSeek Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 804
static inline int check_rule_804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/s.cgi",fast_pattern,nocase', 'tmpl=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 804
static inline void alert_rule_804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SWSoft ASPSeek Overflow attempt", 
                     sizeof("SERVER-WEBAPP SWSoft ASPSeek Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 805
// Rule Name: SERVER-WEBAPP Progress webspeed access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 805
static inline int check_rule_805(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wsisa.dll/WService=",fast_pattern,nocase', 'WSMadmin",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 805
static inline void alert_rule_805(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 805;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Progress webspeed access", 
                     sizeof("SERVER-WEBAPP Progress webspeed access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 806
// Rule Name: SERVER-WEBAPP yabb directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 806
static inline int check_rule_806(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/YaBB",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 806
static inline void alert_rule_806(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 806;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP yabb directory traversal attempt", 
                     sizeof("SERVER-WEBAPP yabb directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 807
// Rule Name: SERVER-WEBAPP /wwwboard/passwd.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 807
static inline int check_rule_807(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wwwboard/passwd.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 807
static inline void alert_rule_807(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 807;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /wwwboard/passwd.txt access", 
                     sizeof("SERVER-WEBAPP /wwwboard/passwd.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 808
// Rule Name: SERVER-WEBAPP webdriver access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 808
static inline int check_rule_808(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webdriver",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 808
static inline void alert_rule_808(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 808;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webdriver access", 
                     sizeof("SERVER-WEBAPP webdriver access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 809
// Rule Name: SERVER-WEBAPP whois_raw.cgi arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 809
static inline int check_rule_809(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/whois_raw.cgi?', '|0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 809
static inline void alert_rule_809(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 809;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP whois_raw.cgi arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP whois_raw.cgi arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 810
// Rule Name: SERVER-WEBAPP whois_raw.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 810
static inline int check_rule_810(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /whois_raw.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 810
static inline void alert_rule_810(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 810;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP whois_raw.cgi access", 
                     sizeof("SERVER-WEBAPP whois_raw.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 811
// Rule Name: SERVER-WEBAPP websitepro path access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 811
static inline int check_rule_811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  /HTTP/1.",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 811
static inline void alert_rule_811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP websitepro path access", 
                     sizeof("SERVER-WEBAPP websitepro path access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 812
// Rule Name: SERVER-WEBAPP webplus version access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 812
static inline int check_rule_812(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webplus?about",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 812
static inline void alert_rule_812(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 812;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webplus version access", 
                     sizeof("SERVER-WEBAPP webplus version access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 813
// Rule Name: SERVER-WEBAPP webplus directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 813
static inline int check_rule_813(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webplus?script",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 813
static inline void alert_rule_813(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 813;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webplus directory traversal", 
                     sizeof("SERVER-WEBAPP webplus directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 815
// Rule Name: SERVER-WEBAPP websendmail access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 815
static inline int check_rule_815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /websendmail",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 815
static inline void alert_rule_815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP websendmail access", 
                     sizeof("SERVER-WEBAPP websendmail access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 817
// Rule Name: SERVER-WEBAPP dcboard.cgi invalid user addition attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 817
static inline int check_rule_817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dcboard.cgi', 'command=register', '%7cadmin']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 817
static inline void alert_rule_817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dcboard.cgi invalid user addition attempt", 
                     sizeof("SERVER-WEBAPP dcboard.cgi invalid user addition attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 818
// Rule Name: SERVER-WEBAPP dcforum.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 818
static inline int check_rule_818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dcforum.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 818
static inline void alert_rule_818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dcforum.cgi access", 
                     sizeof("SERVER-WEBAPP dcforum.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 819
// Rule Name: SERVER-WEBAPP mmstdod.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 819
static inline int check_rule_819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mmstdod.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 819
static inline void alert_rule_819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mmstdod.cgi access", 
                     sizeof("SERVER-WEBAPP mmstdod.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 820
// Rule Name: SERVER-WEBAPP anaconda directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 820
static inline int check_rule_820(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/apexec.pl', 'template=../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 820
static inline void alert_rule_820(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 820;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP anaconda directory traversal attempt", 
                     sizeof("SERVER-WEBAPP anaconda directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 821
// Rule Name: SERVER-WEBAPP imagemap.exe overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 821
static inline int check_rule_821(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /imagemap.exe?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 821
static inline void alert_rule_821(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 821;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP imagemap.exe overflow attempt", 
                     sizeof("SERVER-WEBAPP imagemap.exe overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 823
// Rule Name: SERVER-WEBAPP cvsweb.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 823
static inline int check_rule_823(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cvsweb.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 823
static inline void alert_rule_823(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 823;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cvsweb.cgi access", 
                     sizeof("SERVER-WEBAPP cvsweb.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 824
// Rule Name: SERVER-WEBAPP php.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 824
static inline int check_rule_824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /php.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 824
static inline void alert_rule_824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP php.cgi access", 
                     sizeof("SERVER-WEBAPP php.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 825
// Rule Name: SERVER-WEBAPP glimpse access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 825
static inline int check_rule_825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /glimpse",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 825
static inline void alert_rule_825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP glimpse access", 
                     sizeof("SERVER-WEBAPP glimpse access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 826
// Rule Name: SERVER-WEBAPP htmlscript access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 826
static inline int check_rule_826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htmlscript",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 826
static inline void alert_rule_826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htmlscript access", 
                     sizeof("SERVER-WEBAPP htmlscript access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 827
// Rule Name: SERVER-WEBAPP info2www access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 827
static inline int check_rule_827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /info2www",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 827
static inline void alert_rule_827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP info2www access", 
                     sizeof("SERVER-WEBAPP info2www access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 828
// Rule Name: SERVER-WEBAPP maillist.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 828
static inline int check_rule_828(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /maillist.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 828
static inline void alert_rule_828(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 828;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP maillist.pl access", 
                     sizeof("SERVER-WEBAPP maillist.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 829
// Rule Name: SERVER-WEBAPP nph-test-cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 829
static inline int check_rule_829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nph-test-cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 829
static inline void alert_rule_829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nph-test-cgi access", 
                     sizeof("SERVER-WEBAPP nph-test-cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 832
// Rule Name: SERVER-WEBAPP perl.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 832
static inline int check_rule_832(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /perl.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 832
static inline void alert_rule_832(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 832;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP perl.exe access", 
                     sizeof("SERVER-WEBAPP perl.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 833
// Rule Name: SERVER-WEBAPP rguest.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 833
static inline int check_rule_833(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rguest.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 833
static inline void alert_rule_833(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 833;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rguest.exe access", 
                     sizeof("SERVER-WEBAPP rguest.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 834
// Rule Name: SERVER-WEBAPP rwwwshell.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 834
static inline int check_rule_834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rwwwshell.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 834
static inline void alert_rule_834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rwwwshell.pl access", 
                     sizeof("SERVER-WEBAPP rwwwshell.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 835
// Rule Name: SERVER-WEBAPP test-cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 835
static inline int check_rule_835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test-cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 835
static inline void alert_rule_835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test-cgi access", 
                     sizeof("SERVER-WEBAPP test-cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 836
// Rule Name: SERVER-WEBAPP textcounter.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 836
static inline int check_rule_836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /textcounter.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 836
static inline void alert_rule_836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP textcounter.pl access", 
                     sizeof("SERVER-WEBAPP textcounter.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 837
// Rule Name: SERVER-WEBAPP uploader.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 837
static inline int check_rule_837(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /uploader.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 837
static inline void alert_rule_837(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 837;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP uploader.exe access", 
                     sizeof("SERVER-WEBAPP uploader.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 838
// Rule Name: SERVER-WEBAPP webgais access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 838
static inline int check_rule_838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webgais",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 838
static inline void alert_rule_838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webgais access", 
                     sizeof("SERVER-WEBAPP webgais access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 839
// Rule Name: SERVER-WEBAPP finger access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 839
static inline int check_rule_839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /finger",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 839
static inline void alert_rule_839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP finger access", 
                     sizeof("SERVER-WEBAPP finger access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 840
// Rule Name: SERVER-WEBAPP perlshop.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 840
static inline int check_rule_840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /perlshop.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 840
static inline void alert_rule_840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP perlshop.cgi access", 
                     sizeof("SERVER-WEBAPP perlshop.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 842
// Rule Name: SERVER-WEBAPP aglimpse access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 842
static inline int check_rule_842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /aglimpse",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 842
static inline void alert_rule_842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP aglimpse access", 
                     sizeof("SERVER-WEBAPP aglimpse access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 843
// Rule Name: SERVER-WEBAPP anform2 access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 843
static inline int check_rule_843(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /AnForm2",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 843
static inline void alert_rule_843(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 843;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP anform2 access", 
                     sizeof("SERVER-WEBAPP anform2 access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 844
// Rule Name: SERVER-WEBAPP args.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 844
static inline int check_rule_844(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /args.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 844
static inline void alert_rule_844(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 844;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP args.bat access", 
                     sizeof("SERVER-WEBAPP args.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 845
// Rule Name: SERVER-WEBAPP AT-admin.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 845
static inline int check_rule_845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /AT-admin.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 845
static inline void alert_rule_845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AT-admin.cgi access", 
                     sizeof("SERVER-WEBAPP AT-admin.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 846
// Rule Name: SERVER-WEBAPP bnbform.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 846
static inline int check_rule_846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bnbform.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 846
static inline void alert_rule_846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bnbform.cgi access", 
                     sizeof("SERVER-WEBAPP bnbform.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 847
// Rule Name: SERVER-WEBAPP campas access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 847
static inline int check_rule_847(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /campas",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 847
static inline void alert_rule_847(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 847;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP campas access", 
                     sizeof("SERVER-WEBAPP campas access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 848
// Rule Name: SERVER-WEBAPP view-source directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 848
static inline int check_rule_848(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/view-source",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 848
static inline void alert_rule_848(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 848;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP view-source directory traversal", 
                     sizeof("SERVER-WEBAPP view-source directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 849
// Rule Name: SERVER-WEBAPP view-source access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 849
static inline int check_rule_849(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /view-source",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 849
static inline void alert_rule_849(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 849;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP view-source access", 
                     sizeof("SERVER-WEBAPP view-source access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 850
// Rule Name: SERVER-WEBAPP wais.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 850
static inline int check_rule_850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wais.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 850
static inline void alert_rule_850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wais.pl access", 
                     sizeof("SERVER-WEBAPP wais.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 851
// Rule Name: SERVER-WEBAPP files.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 851
static inline int check_rule_851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /files.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 851
static inline void alert_rule_851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP files.pl access", 
                     sizeof("SERVER-WEBAPP files.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 852
// Rule Name: SERVER-WEBAPP wguest.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 852
static inline int check_rule_852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wguest.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 852
static inline void alert_rule_852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wguest.exe access", 
                     sizeof("SERVER-WEBAPP wguest.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 853
// Rule Name: SERVER-WEBAPP wrap access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 853
static inline int check_rule_853(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wrap
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 853
static inline void alert_rule_853(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 853;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wrap access", 
                     sizeof("SERVER-WEBAPP wrap access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 854
// Rule Name: SERVER-WEBAPP classifieds.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 854
static inline int check_rule_854(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /classifieds.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 854
static inline void alert_rule_854(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 854;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP classifieds.cgi access", 
                     sizeof("SERVER-WEBAPP classifieds.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 856
// Rule Name: SERVER-WEBAPP environ.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 856
static inline int check_rule_856(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /environ.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 856
static inline void alert_rule_856(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 856;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP environ.cgi access", 
                     sizeof("SERVER-WEBAPP environ.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 857
// Rule Name: SERVER-WEBAPP faxsurvey access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 857
static inline int check_rule_857(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /faxsurvey",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 857
static inline void alert_rule_857(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 857;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP faxsurvey access", 
                     sizeof("SERVER-WEBAPP faxsurvey access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 858
// Rule Name: SERVER-WEBAPP filemail access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 858
static inline int check_rule_858(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /filemail.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 858
static inline void alert_rule_858(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 858;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP filemail access", 
                     sizeof("SERVER-WEBAPP filemail access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 859
// Rule Name: SERVER-WEBAPP man.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 859
static inline int check_rule_859(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /man.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 859
static inline void alert_rule_859(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 859;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP man.sh access", 
                     sizeof("SERVER-WEBAPP man.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 860
// Rule Name: SERVER-WEBAPP snork.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 860
static inline int check_rule_860(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /snork.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 860
static inline void alert_rule_860(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 860;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP snork.bat access", 
                     sizeof("SERVER-WEBAPP snork.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 861
// Rule Name: SERVER-WEBAPP w3-msql access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 861
static inline int check_rule_861(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /w3-msql/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 861
static inline void alert_rule_861(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 861;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP w3-msql access", 
                     sizeof("SERVER-WEBAPP w3-msql access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 862
// Rule Name: SERVER-WEBAPP csh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 862
static inline int check_rule_862(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /csh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 862
static inline void alert_rule_862(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 862;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP csh access", 
                     sizeof("SERVER-WEBAPP csh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 863
// Rule Name: SERVER-WEBAPP day5datacopier.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 863
static inline int check_rule_863(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /day5datacopier.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 863
static inline void alert_rule_863(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 863;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP day5datacopier.cgi access", 
                     sizeof("SERVER-WEBAPP day5datacopier.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 864
// Rule Name: SERVER-WEBAPP day5datanotifier.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 864
static inline int check_rule_864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /day5datanotifier.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 864
static inline void alert_rule_864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP day5datanotifier.cgi access", 
                     sizeof("SERVER-WEBAPP day5datanotifier.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 865
// Rule Name: SERVER-WEBAPP ksh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 865
static inline int check_rule_865(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ksh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 865
static inline void alert_rule_865(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 865;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ksh access", 
                     sizeof("SERVER-WEBAPP ksh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 866
// Rule Name: SERVER-WEBAPP post-query access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 866
static inline int check_rule_866(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /post-query",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 866
static inline void alert_rule_866(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 866;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP post-query access", 
                     sizeof("SERVER-WEBAPP post-query access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 867
// Rule Name: SERVER-WEBAPP visadmin.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 867
static inline int check_rule_867(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /visadmin.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 867
static inline void alert_rule_867(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 867;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP visadmin.exe access", 
                     sizeof("SERVER-WEBAPP visadmin.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 868
// Rule Name: SERVER-WEBAPP rsh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 868
static inline int check_rule_868(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rsh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 868
static inline void alert_rule_868(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 868;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rsh access", 
                     sizeof("SERVER-WEBAPP rsh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 869
// Rule Name: SERVER-WEBAPP dumpenv.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 869
static inline int check_rule_869(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dumpenv.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 869
static inline void alert_rule_869(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 869;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dumpenv.pl access", 
                     sizeof("SERVER-WEBAPP dumpenv.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 870
// Rule Name: SERVER-WEBAPP snorkerz.cmd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 870
static inline int check_rule_870(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /snorkerz.cmd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 870
static inline void alert_rule_870(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 870;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP snorkerz.cmd access", 
                     sizeof("SERVER-WEBAPP snorkerz.cmd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 871
// Rule Name: SERVER-WEBAPP survey.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 871
static inline int check_rule_871(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /survey.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 871
static inline void alert_rule_871(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 871;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP survey.cgi access", 
                     sizeof("SERVER-WEBAPP survey.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 872
// Rule Name: SERVER-WEBAPP tcsh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 872
static inline int check_rule_872(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /tcsh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 872
static inline void alert_rule_872(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 872;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP tcsh access", 
                     sizeof("SERVER-WEBAPP tcsh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 875
// Rule Name: SERVER-WEBAPP win-c-sample.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 875
static inline int check_rule_875(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /win-c-sample.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 875
static inline void alert_rule_875(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 875;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP win-c-sample.exe access", 
                     sizeof("SERVER-WEBAPP win-c-sample.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 877
// Rule Name: SERVER-WEBAPP rksh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 877
static inline int check_rule_877(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rksh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 877
static inline void alert_rule_877(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 877;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rksh access", 
                     sizeof("SERVER-WEBAPP rksh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 878
// Rule Name: SERVER-WEBAPP w3tvars.pm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 878
static inline int check_rule_878(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /w3tvars.pm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 878
static inline void alert_rule_878(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 878;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP w3tvars.pm access", 
                     sizeof("SERVER-WEBAPP w3tvars.pm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 879
// Rule Name: SERVER-WEBAPP admin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 879
static inline int check_rule_879(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 879
static inline void alert_rule_879(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 879;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP admin.pl access", 
                     sizeof("SERVER-WEBAPP admin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 880
// Rule Name: SERVER-WEBAPP LWGate access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 880
static inline int check_rule_880(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /LWGate",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 880
static inline void alert_rule_880(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 880;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP LWGate access", 
                     sizeof("SERVER-WEBAPP LWGate access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 881
// Rule Name: SERVER-WEBAPP archie access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 881
static inline int check_rule_881(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /archie",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 881
static inline void alert_rule_881(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 881;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP archie access", 
                     sizeof("SERVER-WEBAPP archie access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 882
// Rule Name: SERVER-WEBAPP calendar access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 882
static inline int check_rule_882(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calendar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 882
static inline void alert_rule_882(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 882;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar access", 
                     sizeof("SERVER-WEBAPP calendar access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 883
// Rule Name: SERVER-WEBAPP flexform access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 883
static inline int check_rule_883(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /flexform",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 883
static inline void alert_rule_883(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 883;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP flexform access", 
                     sizeof("SERVER-WEBAPP flexform access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 885
// Rule Name: SERVER-WEBAPP bash access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 885
static inline int check_rule_885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bash",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 885
static inline void alert_rule_885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bash access", 
                     sizeof("SERVER-WEBAPP bash access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 886
// Rule Name: SERVER-WEBAPP phf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 886
static inline int check_rule_886(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /phf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 886
static inline void alert_rule_886(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 886;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phf access", 
                     sizeof("SERVER-WEBAPP phf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 887
// Rule Name: SERVER-WEBAPP www-sql access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 887
static inline int check_rule_887(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /www-sql",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 887
static inline void alert_rule_887(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 887;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP www-sql access", 
                     sizeof("SERVER-WEBAPP www-sql access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 888
// Rule Name: SERVER-WEBAPP wwwadmin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 888
static inline int check_rule_888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wwwadmin.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 888
static inline void alert_rule_888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wwwadmin.pl access", 
                     sizeof("SERVER-WEBAPP wwwadmin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 889
// Rule Name: SERVER-WEBAPP ppdscgi.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 889
static inline int check_rule_889(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ppdscgi.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 889
static inline void alert_rule_889(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 889;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ppdscgi.exe access", 
                     sizeof("SERVER-WEBAPP ppdscgi.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 890
// Rule Name: SERVER-WEBAPP sendform.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 890
static inline int check_rule_890(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sendform.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 890
static inline void alert_rule_890(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 890;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sendform.cgi access", 
                     sizeof("SERVER-WEBAPP sendform.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 891
// Rule Name: SERVER-WEBAPP upload.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 891
static inline int check_rule_891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /upload.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 891
static inline void alert_rule_891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP upload.pl access", 
                     sizeof("SERVER-WEBAPP upload.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 892
// Rule Name: SERVER-WEBAPP AnyForm2 access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 892
static inline int check_rule_892(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /AnyForm2",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 892
static inline void alert_rule_892(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 892;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AnyForm2 access", 
                     sizeof("SERVER-WEBAPP AnyForm2 access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 894
// Rule Name: SERVER-WEBAPP bb-hist.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 894
static inline int check_rule_894(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-hist.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 894
static inline void alert_rule_894(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 894;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-hist.sh access", 
                     sizeof("SERVER-WEBAPP bb-hist.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 895
// Rule Name: SERVER-WEBAPP redirect access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 895
static inline int check_rule_895(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /redirect",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 895
static inline void alert_rule_895(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 895;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP redirect access", 
                     sizeof("SERVER-WEBAPP redirect access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 896
// Rule Name: SERVER-WEBAPP way-board access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 896
static inline int check_rule_896(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /way-board",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 896
static inline void alert_rule_896(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 896;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP way-board access", 
                     sizeof("SERVER-WEBAPP way-board access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 897
// Rule Name: SERVER-WEBAPP pals-cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 897
static inline int check_rule_897(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pals-cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 897
static inline void alert_rule_897(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 897;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pals-cgi access", 
                     sizeof("SERVER-WEBAPP pals-cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 898
// Rule Name: SERVER-WEBAPP commerce.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 898
static inline int check_rule_898(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /commerce.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 898
static inline void alert_rule_898(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 898;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP commerce.cgi access", 
                     sizeof("SERVER-WEBAPP commerce.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 899
// Rule Name: SERVER-WEBAPP Amaya templates sendtemp.pl directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 899
static inline int check_rule_899(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/sendtemp.pl",fast_pattern,nocase', 'templ=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 899
static inline void alert_rule_899(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 899;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Amaya templates sendtemp.pl directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Amaya templates sendtemp.pl directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 900
// Rule Name: SERVER-WEBAPP webspirs.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 900
static inline int check_rule_900(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webspirs.cgi",fast_pattern,nocase', '../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 900
static inline void alert_rule_900(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 900;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webspirs.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP webspirs.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 901
// Rule Name: SERVER-WEBAPP webspirs.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 901
static inline int check_rule_901(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webspirs.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 901
static inline void alert_rule_901(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 901;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webspirs.cgi access", 
                     sizeof("SERVER-WEBAPP webspirs.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 902
// Rule Name: SERVER-WEBAPP tstisapi.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 902
static inline int check_rule_902(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: tstisapi.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 902
static inline void alert_rule_902(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 902;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP tstisapi.dll access", 
                     sizeof("SERVER-WEBAPP tstisapi.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 903
// Rule Name: SERVER-OTHER Adobe Coldfusion cfcache.map access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 903
static inline int check_rule_903(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfcache.map",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 903
static inline void alert_rule_903(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 903;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion cfcache.map access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion cfcache.map access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 904
// Rule Name: SERVER-OTHER Adobe Coldfusion exampleapp application.cfm
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 904
static inline int check_rule_904(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/exampleapp/email/application.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 904
static inline void alert_rule_904(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 904;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion exampleapp application.cfm", 
                     sizeof("SERVER-OTHER Adobe Coldfusion exampleapp application.cfm"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 905
// Rule Name: SERVER-OTHER Adobe Coldfusion application.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 905
static inline int check_rule_905(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/exampleapp/publish/admin/application.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 905
static inline void alert_rule_905(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 905;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion application.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion application.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 906
// Rule Name: SERVER-OTHER Adobe Coldfusion getfile.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 906
static inline int check_rule_906(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/exampleapp/email/getfile.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 906
static inline void alert_rule_906(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 906;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion getfile.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion getfile.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 907
// Rule Name: SERVER-OTHER Adobe Coldfusion addcontent.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 907
static inline int check_rule_907(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/exampleapp/publish/admin/addcontent.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 907
static inline void alert_rule_907(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 907;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion addcontent.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion addcontent.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 908
// Rule Name: SERVER-OTHER Adobe Coldfusion administrator access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 908
static inline int check_rule_908(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfide/administrator/index.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 908
static inline void alert_rule_908(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 908;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion administrator access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion administrator access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 909
// Rule Name: SERVER-OTHER Adobe Coldfusion datasource username attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 909
static inline int check_rule_909(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CF_SETDATASOURCEUSERNAME|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 909
static inline void alert_rule_909(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 909;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion datasource username attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion datasource username attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 910
// Rule Name: SERVER-OTHER Adobe Coldfusion fileexists.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 910
static inline int check_rule_910(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/snippets/fileexists.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 910
static inline void alert_rule_910(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 910;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion fileexists.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion fileexists.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 911
// Rule Name: SERVER-OTHER Adobe Coldfusion exprcalc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 911
static inline int check_rule_911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/expeval/exprcalc.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 911
static inline void alert_rule_911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion exprcalc access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion exprcalc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 912
// Rule Name: SERVER-OTHER Adobe Coldfusion parks access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 912
static inline int check_rule_912(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/examples/parks/detail.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 912
static inline void alert_rule_912(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 912;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion parks access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion parks access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 913
// Rule Name: SERVER-OTHER Adobe Coldfusion cfappman access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 913
static inline int check_rule_913(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfappman/index.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 913
static inline void alert_rule_913(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 913;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion cfappman access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion cfappman access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 914
// Rule Name: SERVER-OTHER Adobe Coldfusion beaninfo access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 914
static inline int check_rule_914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/examples/cvbeans/beaninfo.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 914
static inline void alert_rule_914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion beaninfo access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion beaninfo access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 915
// Rule Name: SERVER-OTHER Adobe Coldfusion evaluate.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 915
static inline int check_rule_915(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/snippets/evaluate.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 915
static inline void alert_rule_915(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 915;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion evaluate.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion evaluate.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 916
// Rule Name: SERVER-OTHER Adobe Coldfusion getodbcdsn access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 916
static inline int check_rule_916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_GETODBCDSN|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 916
static inline void alert_rule_916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion getodbcdsn access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion getodbcdsn access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 917
// Rule Name: SERVER-OTHER Adobe Coldfusion db connections flush attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 917
static inline int check_rule_917(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_DBCONNECTIONS_FLUSH|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 917
static inline void alert_rule_917(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 917;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion db connections flush attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion db connections flush attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 918
// Rule Name: SERVER-OTHER Adobe Coldfusion expeval access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 918
static inline int check_rule_918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/expeval/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 918
static inline void alert_rule_918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion expeval access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion expeval access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 919
// Rule Name: SERVER-OTHER Adobe Coldfusion datasource passwordattempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 919
static inline int check_rule_919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CF_SETDATASOURCEPASSWORD|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 919
static inline void alert_rule_919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion datasource passwordattempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion datasource passwordattempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 920
// Rule Name: SERVER-OTHER Adobe Coldfusion datasource attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 920
static inline int check_rule_920(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CF_ISCOLDFUSIONDATASOURCE|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 920
static inline void alert_rule_920(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 920;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion datasource attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion datasource attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 921
// Rule Name: SERVER-OTHER Adobe Coldfusion admin encrypt attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 921
static inline int check_rule_921(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_ENCRYPT|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 921
static inline void alert_rule_921(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 921;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion admin encrypt attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion admin encrypt attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 922
// Rule Name: SERVER-OTHER Adobe Coldfusion displayfile access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 922
static inline int check_rule_922(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/expeval/displayopenedfile.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 922
static inline void alert_rule_922(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 922;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion displayfile access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion displayfile access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 923
// Rule Name: SERVER-OTHER Adobe Coldfusion getodbcin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 923
static inline int check_rule_923(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_GETODBCINI|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 923
static inline void alert_rule_923(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 923;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion getodbcin attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion getodbcin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 924
// Rule Name: SERVER-OTHER Adobe Coldfusion admin decrypt attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 924
static inline int check_rule_924(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_DECRYPT|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 924
static inline void alert_rule_924(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 924;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion admin decrypt attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion admin decrypt attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 925
// Rule Name: SERVER-OTHER Adobe Coldfusion mainframeset access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 925
static inline int check_rule_925(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/examples/mainframeset.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 925
static inline void alert_rule_925(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 925;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion mainframeset access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion mainframeset access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 926
// Rule Name: SERVER-OTHER Adobe Coldfusion set odbc ini attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 926
static inline int check_rule_926(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_SETODBCINI|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 926
static inline void alert_rule_926(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 926;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion set odbc ini attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion set odbc ini attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 927
// Rule Name: SERVER-OTHER Adobe Coldfusion settings refresh attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 927
static inline int check_rule_927(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_SETTINGS_REFRESH|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 927
static inline void alert_rule_927(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 927;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion settings refresh attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion settings refresh attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 928
// Rule Name: SERVER-OTHER Adobe Coldfusion exampleapp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 928
static inline int check_rule_928(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/exampleapp/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 928
static inline void alert_rule_928(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 928;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion exampleapp access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion exampleapp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 929
// Rule Name: SERVER-OTHER Adobe Coldfusion CFUSION_VERIFYMAIL access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 929
static inline int check_rule_929(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: CFUSION_VERIFYMAIL|28 29|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 929
static inline void alert_rule_929(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 929;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion CFUSION_VERIFYMAIL access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion CFUSION_VERIFYMAIL access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 930
// Rule Name: SERVER-OTHER Adobe Coldfusion snippets attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 930
static inline int check_rule_930(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/snippets/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 930
static inline void alert_rule_930(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 930;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion snippets attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion snippets attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 931
// Rule Name: SERVER-OTHER Adobe Coldfusion cfmlsyntaxcheck.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 931
static inline int check_rule_931(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/cfmlsyntaxcheck.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 931
static inline void alert_rule_931(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 931;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion cfmlsyntaxcheck.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion cfmlsyntaxcheck.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 932
// Rule Name: SERVER-OTHER Adobe Coldfusion application.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 932
static inline int check_rule_932(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /application.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 932
static inline void alert_rule_932(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 932;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion application.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion application.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 933
// Rule Name: SERVER-OTHER Adobe Coldfusion onrequestend.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 933
static inline int check_rule_933(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /onrequestend.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 933
static inline void alert_rule_933(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 933;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion onrequestend.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion onrequestend.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 935
// Rule Name: SERVER-OTHER Adobe Coldfusion startstop DOS access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 935
static inline int check_rule_935(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfide/administrator/startstop.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 935
static inline void alert_rule_935(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 935;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion startstop DOS access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion startstop DOS access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 936
// Rule Name: SERVER-OTHER Adobe Coldfusion gettempdirectory.cfm access 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 936
static inline int check_rule_936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfdocs/snippets/gettempdirectory.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 936
static inline void alert_rule_936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion gettempdirectory.cfm access ", 
                     sizeof("SERVER-OTHER Adobe Coldfusion gettempdirectory.cfm access "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 937
// Rule Name: SERVER-OTHER Microsoft Frontpage _vti_rpc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 937
static inline int check_rule_937(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_rpc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 937
static inline void alert_rule_937(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 937;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage _vti_rpc access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage _vti_rpc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 939
// Rule Name: SERVER-OTHER Microsoft Frontpage posting
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 939
static inline int check_rule_939(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/author.dll",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 939
static inline void alert_rule_939(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 939;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage posting", 
                     sizeof("SERVER-OTHER Microsoft Frontpage posting"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 940
// Rule Name: SERVER-OTHER Microsoft Frontpage shtml.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 940
static inline int check_rule_940(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_bin/shtml.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 940
static inline void alert_rule_940(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 940;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage shtml.dll access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage shtml.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 941
// Rule Name: SERVER-OTHER Microsoft Frontpage contents.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 941
static inline int check_rule_941(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admcgi/contents.htm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 941
static inline void alert_rule_941(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 941;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage contents.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage contents.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 942
// Rule Name: SERVER-OTHER Microsoft Frontpage orders.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 942
static inline int check_rule_942(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/orders.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 942
static inline void alert_rule_942(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 942;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage orders.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage orders.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 943
// Rule Name: SERVER-OTHER Microsoft Frontpage fpsrvadm.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 943
static inline int check_rule_943(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fpsrvadm.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 943
static inline void alert_rule_943(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 943;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage fpsrvadm.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage fpsrvadm.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 944
// Rule Name: SERVER-OTHER Microsoft Frontpage fpremadm.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 944
static inline int check_rule_944(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fpremadm.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 944
static inline void alert_rule_944(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 944;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage fpremadm.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage fpremadm.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 945
// Rule Name: SERVER-OTHER Microsoft Frontpage fpadmin.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 945
static inline int check_rule_945(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admisapi/fpadmin.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 945
static inline void alert_rule_945(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 945;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage fpadmin.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage fpadmin.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 946
// Rule Name: SERVER-OTHER Microsoft Frontpage fpadmcgi.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 946
static inline int check_rule_946(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/Fpadmcgi.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 946
static inline void alert_rule_946(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 946;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage fpadmcgi.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage fpadmcgi.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 947
// Rule Name: SERVER-OTHER Microsoft Frontpage orders.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 947
static inline int check_rule_947(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/orders.txt",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 947
static inline void alert_rule_947(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 947;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage orders.txt access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage orders.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 948
// Rule Name: SERVER-OTHER Microsoft Frontpage form_results access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 948
static inline int check_rule_948(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/form_results.txt",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 948
static inline void alert_rule_948(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 948;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage form_results access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage form_results access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 949
// Rule Name: SERVER-OTHER Microsoft Frontpage registrations.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 949
static inline int check_rule_949(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/registrations.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 949
static inline void alert_rule_949(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 949;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage registrations.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage registrations.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 950
// Rule Name: SERVER-OTHER Microsoft Frontpage cfgwiz.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 950
static inline int check_rule_950(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cfgwiz.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 950
static inline void alert_rule_950(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 950;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage cfgwiz.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage cfgwiz.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 951
// Rule Name: SERVER-OTHER Microsoft Frontpage authors.pwd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 951
static inline int check_rule_951(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /authors.pwd",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 951
static inline void alert_rule_951(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 951;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage authors.pwd access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage authors.pwd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 952
// Rule Name: SERVER-OTHER Microsoft Frontpage author.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 952
static inline int check_rule_952(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_bin/_vti_aut/author.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 952
static inline void alert_rule_952(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 952;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage author.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage author.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 953
// Rule Name: SERVER-OTHER Microsoft Frontpage administrators.pwd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 953
static inline int check_rule_953(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /administrators.pwd",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 953
static inline void alert_rule_953(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 953;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage administrators.pwd access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage administrators.pwd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 954
// Rule Name: SERVER-OTHER Microsoft Frontpage form_results.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 954
static inline int check_rule_954(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/form_results.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 954
static inline void alert_rule_954(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 954;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage form_results.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage form_results.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 955
// Rule Name: SERVER-OTHER Microsoft Frontpage access.cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 955
static inline int check_rule_955(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/access.cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 955
static inline void alert_rule_955(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 955;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage access.cnf access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage access.cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 956
// Rule Name: SERVER-OTHER Microsoft Frontpage register.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 956
static inline int check_rule_956(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/register.txt",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 956
static inline void alert_rule_956(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 956;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage register.txt access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage register.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 957
// Rule Name: SERVER-OTHER Microsoft Frontpage registrations.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 957
static inline int check_rule_957(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/registrations.txt",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 957
static inline void alert_rule_957(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 957;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage registrations.txt access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage registrations.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 958
// Rule Name: SERVER-OTHER Microsoft Frontpage service.cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 958
static inline int check_rule_958(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/service.cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 958
static inline void alert_rule_958(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 958;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage service.cnf access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage service.cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 959
// Rule Name: SERVER-OTHER Microsoft Frontpage service.pwd
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 959
static inline int check_rule_959(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /service.pwd",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 959
static inline void alert_rule_959(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 959;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage service.pwd", 
                     sizeof("SERVER-OTHER Microsoft Frontpage service.pwd"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 960
// Rule Name: SERVER-OTHER Microsoft Frontpage service.stp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 960
static inline int check_rule_960(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/service.stp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 960
static inline void alert_rule_960(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 960;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage service.stp access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage service.stp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 961
// Rule Name: SERVER-OTHER Microsoft Frontpage services.cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 961
static inline int check_rule_961(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/services.cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 961
static inline void alert_rule_961(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 961;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage services.cnf access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage services.cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 962
// Rule Name: SERVER-OTHER Microsoft Frontpage shtml.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 962
static inline int check_rule_962(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_bin/shtml.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 962
static inline void alert_rule_962(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 962;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage shtml.exe access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage shtml.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 963
// Rule Name: SERVER-OTHER Microsoft Frontpage svcacl.cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 963
static inline int check_rule_963(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/svcacl.cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 963
static inline void alert_rule_963(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 963;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage svcacl.cnf access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage svcacl.cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 964
// Rule Name: SERVER-OTHER Microsoft Frontpage users.pwd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 964
static inline int check_rule_964(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /users.pwd",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 964
static inline void alert_rule_964(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 964;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage users.pwd access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage users.pwd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 965
// Rule Name: SERVER-OTHER Microsoft Frontpage writeto.cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 965
static inline int check_rule_965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_pvt/writeto.cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 965
static inline void alert_rule_965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage writeto.cnf access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage writeto.cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 966
// Rule Name: SERVER-OTHER Microsoft Frontpage .... request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 966
static inline int check_rule_966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ..../
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 966
static inline void alert_rule_966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage .... request", 
                     sizeof("SERVER-OTHER Microsoft Frontpage .... request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 967
// Rule Name: SERVER-OTHER Microsoft Frontpage dvwssr.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 967
static inline int check_rule_967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dvwssr.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 967
static inline void alert_rule_967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage dvwssr.dll access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage dvwssr.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 968
// Rule Name: SERVER-OTHER Microsoft Frontpage register.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 968
static inline int check_rule_968(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_private/register.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 968
static inline void alert_rule_968(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 968;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage register.htm access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage register.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 969
// Rule Name: SERVER-IIS WebDAV file lock attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 969
static inline int check_rule_969(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: LOCK ",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 969
static inline void alert_rule_969(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 969;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS WebDAV file lock attempt", 
                     sizeof("SERVER-IIS WebDAV file lock attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 971
// Rule Name: SERVER-IIS ISAPI .printer access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 971
static inline int check_rule_971(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .printer",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 971
static inline void alert_rule_971(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 971;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ISAPI .printer access", 
                     sizeof("SERVER-IIS ISAPI .printer access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 973
// Rule Name: SERVER-IIS *.idc attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 973
static inline int check_rule_973(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /*.idc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 973
static inline void alert_rule_973(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 973;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS *.idc attempt", 
                     sizeof("SERVER-IIS *.idc attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 974
// Rule Name: SERVER-IIS Microsoft Windows IIS directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 974
static inline int check_rule_974(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ..|5C|..",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 974
static inline void alert_rule_974(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 974;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Microsoft Windows IIS directory traversal attempt", 
                     sizeof("SERVER-IIS Microsoft Windows IIS directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 975
// Rule Name: SERVER-IIS Alternate Data streams ASP file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 975
static inline int check_rule_975(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .asp|3A 3A 24|DATA",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 975
static inline void alert_rule_975(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 975;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Alternate Data streams ASP file access attempt", 
                     sizeof("SERVER-IIS Alternate Data streams ASP file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 976
// Rule Name: SERVER-WEBAPP .bat? access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 976
static inline int check_rule_976(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.bat?",fast_pattern,nocase', '/cgi-bin/",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 976
static inline void alert_rule_976(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 976;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .bat? access", 
                     sizeof("SERVER-WEBAPP .bat? access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 977
// Rule Name: SERVER-IIS .cnf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 977
static inline int check_rule_977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .cnf",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 977
static inline void alert_rule_977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .cnf access", 
                     sizeof("SERVER-IIS .cnf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 978
// Rule Name: SERVER-IIS ASP contents view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 978
static inline int check_rule_978(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['%20', '&CiRestriction=none",nocase', '&CiHiliteType=Full",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 978
static inline void alert_rule_978(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 978;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ASP contents view", 
                     sizeof("SERVER-IIS ASP contents view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 979
// Rule Name: SERVER-IIS ASP contents view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 979
static inline int check_rule_979(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htw?CiWebHitsFile",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 979
static inline void alert_rule_979(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 979;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ASP contents view", 
                     sizeof("SERVER-IIS ASP contents view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 980
// Rule Name: SERVER-IIS CGImail.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 980
static inline int check_rule_980(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/CGImail.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 980
static inline void alert_rule_980(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 980;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS CGImail.exe access", 
                     sizeof("SERVER-IIS CGImail.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 984
// Rule Name: SERVER-IIS JET VBA access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 984
static inline int check_rule_984(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/samples/ctguestb.idc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 984
static inline void alert_rule_984(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 984;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS JET VBA access", 
                     sizeof("SERVER-IIS JET VBA access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 985
// Rule Name: SERVER-IIS JET VBA access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 985
static inline int check_rule_985(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/samples/details.idc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 985
static inline void alert_rule_985(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 985;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS JET VBA access", 
                     sizeof("SERVER-IIS JET VBA access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 986
// Rule Name: SERVER-IIS MSProxy access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 986
static inline int check_rule_986(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/proxy/w3proxy.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 986
static inline void alert_rule_986(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 986;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS MSProxy access", 
                     sizeof("SERVER-IIS MSProxy access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 987
// Rule Name: FILE-IDENTIFY .htr access file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 987
static inline int check_rule_987(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htr",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 987
static inline void alert_rule_987(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 987;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY .htr access file download request", 
                     sizeof("FILE-IDENTIFY .htr access file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 989
// Rule Name: MALWARE-CNC sensepost.exe command shell
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 989
static inline int check_rule_989(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sensepost.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 989
static inline void alert_rule_989(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 989;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC sensepost.exe command shell", 
                     sizeof("MALWARE-CNC sensepost.exe command shell"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 990
// Rule Name: SERVER-OTHER Microsoft Frontpage _vti_inf.html access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 990
static inline int check_rule_990(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_inf.html",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 990
static inline void alert_rule_990(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 990;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage _vti_inf.html access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage _vti_inf.html access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 991
// Rule Name: SERVER-IIS achg.htr access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 991
static inline int check_rule_991(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisadmpwd/achg.htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 991
static inline void alert_rule_991(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 991;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS achg.htr access", 
                     sizeof("SERVER-IIS achg.htr access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 992
// Rule Name: SERVER-IIS adctest.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 992
static inline int check_rule_992(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /msadc/samples/adctest.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 992
static inline void alert_rule_992(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 992;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS adctest.asp access", 
                     sizeof("SERVER-IIS adctest.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 993
// Rule Name: SERVER-IIS iisadmin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 993
static inline int check_rule_993(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisadmin",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 993
static inline void alert_rule_993(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 993;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS iisadmin access", 
                     sizeof("SERVER-IIS iisadmin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 994
// Rule Name: SERVER-IIS /scripts/iisadmin/default.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 994
static inline int check_rule_994(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/iisadmin/default.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 994
static inline void alert_rule_994(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 994;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /scripts/iisadmin/default.htm access", 
                     sizeof("SERVER-IIS /scripts/iisadmin/default.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 995
// Rule Name: SERVER-IIS ism.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 995
static inline int check_rule_995(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/iisadmin/ism.dll?http/dir",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 995
static inline void alert_rule_995(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 995;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ism.dll access", 
                     sizeof("SERVER-IIS ism.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 996
// Rule Name: SERVER-IIS anot.htr access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 996
static inline int check_rule_996(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisadmpwd/anot",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 996
static inline void alert_rule_996(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 996;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS anot.htr access", 
                     sizeof("SERVER-IIS anot.htr access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 997
// Rule Name: SERVER-IIS asp-dot attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 997
static inline int check_rule_997(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .asp.",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 997
static inline void alert_rule_997(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 997;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS asp-dot attempt", 
                     sizeof("SERVER-IIS asp-dot attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 998
// Rule Name: SERVER-IIS asp-srch attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 998
static inline int check_rule_998(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |23|filename=*.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 998
static inline void alert_rule_998(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 998;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS asp-srch attempt", 
                     sizeof("SERVER-IIS asp-srch attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 999
// Rule Name: SERVER-IIS bdir access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 999
static inline int check_rule_999(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/iisadmin/bdir.htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 999
static inline void alert_rule_999(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 999;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS bdir access", 
                     sizeof("SERVER-IIS bdir access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1000
// Rule Name: SERVER-IIS bdir.htr access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1000
static inline int check_rule_1000(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bdir.htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1000
static inline void alert_rule_1000(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1000;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS bdir.htr access", 
                     sizeof("SERVER-IIS bdir.htr access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1001
// Rule Name: SERVER-WEBAPP carbo.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1001
static inline int check_rule_1001(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/carbo.dll', 'icatcommand=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1001
static inline void alert_rule_1001(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1001;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP carbo.dll access", 
                     sizeof("SERVER-WEBAPP carbo.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1002
// Rule Name: SERVER-IIS cmd.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1002
static inline int check_rule_1002(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: cmd.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1002
static inline void alert_rule_1002(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1002;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS cmd.exe access", 
                     sizeof("SERVER-IIS cmd.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1003
// Rule Name: SERVER-IIS cmd? access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1003
static inline int check_rule_1003(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .cmd?&",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1003
static inline void alert_rule_1003(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1003;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS cmd? access", 
                     sizeof("SERVER-IIS cmd? access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1004
// Rule Name: SERVER-IIS codebrowser Exair access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1004
static inline int check_rule_1004(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iissamples/exair/howitworks/codebrws.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1004
static inline void alert_rule_1004(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1004;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS codebrowser Exair access", 
                     sizeof("SERVER-IIS codebrowser Exair access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1005
// Rule Name: SERVER-IIS codebrowser SDK access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1005
static inline int check_rule_1005(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iissamples/sdk/asp/docs/codebrws.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1005
static inline void alert_rule_1005(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1005;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS codebrowser SDK access", 
                     sizeof("SERVER-IIS codebrowser SDK access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1007
// Rule Name: SERVER-IIS Form_JScript.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1007
static inline int check_rule_1007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Form_JScript.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1007
static inline void alert_rule_1007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Form_JScript.asp access", 
                     sizeof("SERVER-IIS Form_JScript.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1008
// Rule Name: SERVER-IIS del attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1008
static inline int check_rule_1008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: &del+/s+c|3A 5C|*.*",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1008
static inline void alert_rule_1008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS del attempt", 
                     sizeof("SERVER-IIS del attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1009
// Rule Name: SERVER-IIS directory listing
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1009
static inline int check_rule_1009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ServerVariables_Jscript.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1009
static inline void alert_rule_1009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS directory listing", 
                     sizeof("SERVER-IIS directory listing"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1010
// Rule Name: SERVER-IIS encoding access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1010
static inline int check_rule_1010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %1u",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1010
static inline void alert_rule_1010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS encoding access", 
                     sizeof("SERVER-IIS encoding access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1011
// Rule Name: SERVER-IIS exec-src access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1011
static inline int check_rule_1011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |23|filename=*.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1011
static inline void alert_rule_1011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS exec-src access", 
                     sizeof("SERVER-IIS exec-src access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1012
// Rule Name: SERVER-IIS fpcount attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1012
static inline int check_rule_1012(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/fpcount.exe",fast_pattern,nocase', 'Digits=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1012
static inline void alert_rule_1012(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1012;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS fpcount attempt", 
                     sizeof("SERVER-IIS fpcount attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1013
// Rule Name: SERVER-IIS fpcount access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1013
static inline int check_rule_1013(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fpcount.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1013
static inline void alert_rule_1013(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1013;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS fpcount access", 
                     sizeof("SERVER-IIS fpcount access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1015
// Rule Name: SERVER-IIS getdrvs.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1015
static inline int check_rule_1015(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/tools/getdrvs.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1015
static inline void alert_rule_1015(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1015;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS getdrvs.exe access", 
                     sizeof("SERVER-IIS getdrvs.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1016
// Rule Name: SERVER-IIS global.asa access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1016
static inline int check_rule_1016(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /global.asa",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1016
static inline void alert_rule_1016(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1016;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS global.asa access", 
                     sizeof("SERVER-IIS global.asa access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1017
// Rule Name: SERVER-IIS idc-srch attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1017
static inline int check_rule_1017(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |23|filename=*.idc",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1017
static inline void alert_rule_1017(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1017;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS idc-srch attempt", 
                     sizeof("SERVER-IIS idc-srch attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1018
// Rule Name: SERVER-IIS iisadmpwd attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1018
static inline int check_rule_1018(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisadmpwd/aexp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1018
static inline void alert_rule_1018(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1018;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS iisadmpwd attempt", 
                     sizeof("SERVER-IIS iisadmpwd attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1019
// Rule Name: SERVER-IIS Malformed Hit-Highlighting Argument File Access Attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1019
static inline int check_rule_1019(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['CiWebHitsFile=",nocase', 'CiRestriction=none",fast_pattern,nocase', 'ciHiliteType=Full",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1019
static inline void alert_rule_1019(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1019;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Malformed Hit-Highlighting Argument File Access Attempt", 
                     sizeof("SERVER-IIS Malformed Hit-Highlighting Argument File Access Attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1020
// Rule Name: SERVER-IIS isc$data attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1020
static inline int check_rule_1020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .idc|3A 3A 24|data",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1020
static inline void alert_rule_1020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS isc$data attempt", 
                     sizeof("SERVER-IIS isc$data attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1021
// Rule Name: SERVER-IIS ism.dll attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1021
static inline int check_rule_1021(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  .htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1021
static inline void alert_rule_1021(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1021;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ism.dll attempt", 
                     sizeof("SERVER-IIS ism.dll attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1022
// Rule Name: SERVER-IIS jet vba access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1022
static inline int check_rule_1022(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /advworks/equipment/catalog_type.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1022
static inline void alert_rule_1022(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1022;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS jet vba access", 
                     sizeof("SERVER-IIS jet vba access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1023
// Rule Name: SERVER-IIS msadcs.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1023
static inline int check_rule_1023(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /msadcs.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1023
static inline void alert_rule_1023(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1023;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS msadcs.dll access", 
                     sizeof("SERVER-IIS msadcs.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1024
// Rule Name: SERVER-IIS newdsn.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1024
static inline int check_rule_1024(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/tools/newdsn.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1024
static inline void alert_rule_1024(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1024;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS newdsn.exe access", 
                     sizeof("SERVER-IIS newdsn.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1025
// Rule Name: SERVER-IIS perl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1025
static inline int check_rule_1025(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/perl",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1025
static inline void alert_rule_1025(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1025;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS perl access", 
                     sizeof("SERVER-IIS perl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1026
// Rule Name: SERVER-IIS perl-browse newline attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1026
static inline int check_rule_1026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |0A|.pl",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1026
static inline void alert_rule_1026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS perl-browse newline attempt", 
                     sizeof("SERVER-IIS perl-browse newline attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1027
// Rule Name: SERVER-IIS perl-browse space attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1027
static inline int check_rule_1027(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  .pl",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1027
static inline void alert_rule_1027(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1027;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS perl-browse space attempt", 
                     sizeof("SERVER-IIS perl-browse space attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1028
// Rule Name: SERVER-IIS query.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1028
static inline int check_rule_1028(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /issamples/query.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1028
static inline void alert_rule_1028(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1028;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS query.asp access", 
                     sizeof("SERVER-IIS query.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1029
// Rule Name: SERVER-IIS scripts-browse access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1029
static inline int check_rule_1029(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/ ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1029
static inline void alert_rule_1029(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1029;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS scripts-browse access", 
                     sizeof("SERVER-IIS scripts-browse access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1030
// Rule Name: SERVER-IIS search97.vts access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1030
static inline int check_rule_1030(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /search97.vts
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1030
static inline void alert_rule_1030(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1030;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS search97.vts access", 
                     sizeof("SERVER-IIS search97.vts access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1031
// Rule Name: SERVER-IIS /SiteServer/Publishing/viewcode.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1031
static inline int check_rule_1031(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /SiteServer/Publishing/viewcode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1031
static inline void alert_rule_1031(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1031;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /SiteServer/Publishing/viewcode.asp access", 
                     sizeof("SERVER-IIS /SiteServer/Publishing/viewcode.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1032
// Rule Name: SERVER-IIS showcode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1032
static inline int check_rule_1032(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Sites/Knowledge/Membership/Inspired/ViewCode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1032
static inline void alert_rule_1032(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1032;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS showcode access", 
                     sizeof("SERVER-IIS showcode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1033
// Rule Name: SERVER-IIS viewcode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1033
static inline int check_rule_1033(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Sites/Knowledge/Membership/Inspiredtutorial/ViewCode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1033
static inline void alert_rule_1033(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1033;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS viewcode access", 
                     sizeof("SERVER-IIS viewcode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1034
// Rule Name: SERVER-IIS viewcode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1034
static inline int check_rule_1034(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Sites/Samples/Knowledge/Membership/Inspiredtutorial/ViewCode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1034
static inline void alert_rule_1034(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1034;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS viewcode access", 
                     sizeof("SERVER-IIS viewcode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1035
// Rule Name: SERVER-IIS viewcode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1035
static inline int check_rule_1035(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Sites/Samples/Knowledge/Push/ViewCode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1035
static inline void alert_rule_1035(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1035;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS viewcode access", 
                     sizeof("SERVER-IIS viewcode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1036
// Rule Name: SERVER-IIS viewcode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1036
static inline int check_rule_1036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Sites/Samples/Knowledge/Search/ViewCode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1036
static inline void alert_rule_1036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS viewcode access", 
                     sizeof("SERVER-IIS viewcode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1037
// Rule Name: SERVER-IIS showcode.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1037
static inline int check_rule_1037(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /showcode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1037
static inline void alert_rule_1037(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1037;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS showcode.asp access", 
                     sizeof("SERVER-IIS showcode.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1038
// Rule Name: SERVER-IIS site server config access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1038
static inline int check_rule_1038(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /adsamples/config/site.csc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1038
static inline void alert_rule_1038(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1038;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS site server config access", 
                     sizeof("SERVER-IIS site server config access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1039
// Rule Name: SERVER-IIS srch.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1039
static inline int check_rule_1039(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /samples/isapi/srch.htm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1039
static inline void alert_rule_1039(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1039;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS srch.htm access", 
                     sizeof("SERVER-IIS srch.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1040
// Rule Name: SERVER-IIS srchadm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1040
static inline int check_rule_1040(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /srchadm",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1040
static inline void alert_rule_1040(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1040;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS srchadm access", 
                     sizeof("SERVER-IIS srchadm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1041
// Rule Name: SERVER-IIS uploadn.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1041
static inline int check_rule_1041(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/uploadn.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1041
static inline void alert_rule_1041(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1041;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS uploadn.asp access", 
                     sizeof("SERVER-IIS uploadn.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1042
// Rule Name: SERVER-IIS view source via translate header
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1042
static inline int check_rule_1042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Translate|3A| F",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1042
static inline void alert_rule_1042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS view source via translate header", 
                     sizeof("SERVER-IIS view source via translate header"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1043
// Rule Name: SERVER-IIS viewcode.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1043
static inline int check_rule_1043(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /viewcode.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1043
static inline void alert_rule_1043(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1043;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS viewcode.asp access", 
                     sizeof("SERVER-IIS viewcode.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1044
// Rule Name: SERVER-IIS webhits access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1044
static inline int check_rule_1044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htw
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1044
static inline void alert_rule_1044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS webhits access", 
                     sizeof("SERVER-IIS webhits access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1045
// Rule Name: SERVER-IIS Unauthorized IP Access Attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1045
static inline int check_rule_1045(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['403', 'Forbidden|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1045
static inline void alert_rule_1045(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1045;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Unauthorized IP Access Attempt", 
                     sizeof("SERVER-IIS Unauthorized IP Access Attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1046
// Rule Name: SERVER-IIS site/iisamples access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1046
static inline int check_rule_1046(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /site/iisamples",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1046
static inline void alert_rule_1046(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1046;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS site/iisamples access", 
                     sizeof("SERVER-IIS site/iisamples access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1047
// Rule Name: SERVER-WEBAPP Netscape Enterprise DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1047
static inline int check_rule_1047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: REVLOG / ",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1047
static inline void alert_rule_1047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise DOS", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1048
// Rule Name: SERVER-WEBAPP Netscape Enterprise directory listing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1048
static inline int check_rule_1048(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: INDEX ",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1048
static inline void alert_rule_1048(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1048;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise directory listing attempt", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise directory listing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1050
// Rule Name: SERVER-WEBAPP iPlanet GETPROPERTIES attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1050
static inline int check_rule_1050(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GETPROPERTIES",depth 13
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1050
static inline void alert_rule_1050(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1050;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP iPlanet GETPROPERTIES attempt", 
                     sizeof("SERVER-WEBAPP iPlanet GETPROPERTIES attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1051
// Rule Name: FILE-OTHER technote main.cgi file directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1051
static inline int check_rule_1051(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/technote/main.cgi",fast_pattern,nocase', 'filename=",nocase', '../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1051
static inline void alert_rule_1051(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1051;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER technote main.cgi file directory traversal attempt", 
                     sizeof("FILE-OTHER technote main.cgi file directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1052
// Rule Name: SERVER-WEBAPP technote print.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1052
static inline int check_rule_1052(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/technote/print.cgi",fast_pattern,nocase', 'board=",nocase', '../../', '%00']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1052
static inline void alert_rule_1052(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1052;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP technote print.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP technote print.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1053
// Rule Name: SERVER-WEBAPP ads.cgi command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1053
static inline int check_rule_1053(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ads.cgi",fast_pattern,nocase', 'file=",nocase', '../../', '|7C|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1053
static inline void alert_rule_1053(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1053;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ads.cgi command execution attempt", 
                     sizeof("SERVER-WEBAPP ads.cgi command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1054
// Rule Name: SERVER-WEBAPP weblogic/tomcat .jsp view source attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1054
static inline int check_rule_1054(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jsp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1054
static inline void alert_rule_1054(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1054;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP weblogic/tomcat .jsp view source attempt", 
                     sizeof("SERVER-WEBAPP weblogic/tomcat .jsp view source attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1056
// Rule Name: SERVER-APACHE Apache Tomcat view source attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1056
static inline int check_rule_1056(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %252ejsp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1056
static inline void alert_rule_1056(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1056;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat view source attempt", 
                     sizeof("SERVER-APACHE Apache Tomcat view source attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1057
// Rule Name: SQL ftp attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1057
static inline int check_rule_1057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ftp.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1057
static inline void alert_rule_1057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL ftp attempt", 
                     sizeof("SQL ftp attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1058
// Rule Name: SQL xp_enumdsn attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1058
static inline int check_rule_1058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_enumdsn",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1058
static inline void alert_rule_1058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_enumdsn attempt", 
                     sizeof("SQL xp_enumdsn attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1059
// Rule Name: SQL xp_filelist attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1059
static inline int check_rule_1059(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_filelist",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1059
static inline void alert_rule_1059(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1059;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_filelist attempt", 
                     sizeof("SQL xp_filelist attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1060
// Rule Name: SQL xp_availablemedia attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1060
static inline int check_rule_1060(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_availablemedia",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1060
static inline void alert_rule_1060(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1060;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_availablemedia attempt", 
                     sizeof("SQL xp_availablemedia attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1061
// Rule Name: SQL xp_cmdshell attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1061
static inline int check_rule_1061(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_cmdshell",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1061
static inline void alert_rule_1061(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1061;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_cmdshell attempt", 
                     sizeof("SQL xp_cmdshell attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1062
// Rule Name: SERVER-WEBAPP nc.exe attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1062
static inline int check_rule_1062(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: nc.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1062
static inline void alert_rule_1062(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1062;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nc.exe attempt", 
                     sizeof("SERVER-WEBAPP nc.exe attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1064
// Rule Name: SERVER-WEBAPP wsh attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1064
static inline int check_rule_1064(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: wsh.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1064
static inline void alert_rule_1064(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1064;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wsh attempt", 
                     sizeof("SERVER-WEBAPP wsh attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1065
// Rule Name: SERVER-WEBAPP rcmd attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1065
static inline int check_rule_1065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: rcmd.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1065
static inline void alert_rule_1065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rcmd attempt", 
                     sizeof("SERVER-WEBAPP rcmd attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1066
// Rule Name: SERVER-WEBAPP telnet attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1066
static inline int check_rule_1066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: telnet.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1066
static inline void alert_rule_1066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP telnet attempt", 
                     sizeof("SERVER-WEBAPP telnet attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1067
// Rule Name: SERVER-WEBAPP net attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1067
static inline int check_rule_1067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: net.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1067
static inline void alert_rule_1067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP net attempt", 
                     sizeof("SERVER-WEBAPP net attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1068
// Rule Name: SERVER-WEBAPP tftp attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1068
static inline int check_rule_1068(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: tftp.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1068
static inline void alert_rule_1068(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1068;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP tftp attempt", 
                     sizeof("SERVER-WEBAPP tftp attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1069
// Rule Name: SQL xp_regread attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1069
static inline int check_rule_1069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_regread",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1069
static inline void alert_rule_1069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_regread attempt", 
                     sizeof("SQL xp_regread attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1070
// Rule Name: SERVER-WEBAPP WebDAV search access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1070
static inline int check_rule_1070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: SEARCH ",depth 8,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1070
static inline void alert_rule_1070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WebDAV search access", 
                     sizeof("SERVER-WEBAPP WebDAV search access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1071
// Rule Name: SERVER-WEBAPP .htpasswd access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1071
static inline int check_rule_1071(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htpasswd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1071
static inline void alert_rule_1071(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1071;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .htpasswd access attempt", 
                     sizeof("SERVER-WEBAPP .htpasswd access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1072
// Rule Name: SERVER-WEBAPP Lotus Domino directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1072
static inline int check_rule_1072(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.nsf/', '../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1072
static inline void alert_rule_1072(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1072;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus Domino directory traversal", 
                     sizeof("SERVER-WEBAPP Lotus Domino directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1073
// Rule Name: SERVER-WEBAPP webhits.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1073
static inline int check_rule_1073(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/samples/search/webhits.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1073
static inline void alert_rule_1073(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1073;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webhits.exe access", 
                     sizeof("SERVER-WEBAPP webhits.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1075
// Rule Name: SERVER-IIS postinfo.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1075
static inline int check_rule_1075(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/postinfo.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1075
static inline void alert_rule_1075(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1075;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS postinfo.asp access", 
                     sizeof("SERVER-IIS postinfo.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1076
// Rule Name: SERVER-IIS repost.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1076
static inline int check_rule_1076(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/repost.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1076
static inline void alert_rule_1076(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1076;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS repost.asp access", 
                     sizeof("SERVER-IIS repost.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1077
// Rule Name: SQL queryhit.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1077
static inline int check_rule_1077(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /samples/search/queryhit.htm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1077
static inline void alert_rule_1077(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1077;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL queryhit.htm access", 
                     sizeof("SQL queryhit.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1078
// Rule Name: SQL counter.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1078
static inline int check_rule_1078(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /counter.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1078
static inline void alert_rule_1078(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1078;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL counter.exe access", 
                     sizeof("SQL counter.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1079
// Rule Name: OS-WINDOWS Microsoft Windows WebDAV propfind access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1079
static inline int check_rule_1079(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: propfind",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1079
static inline void alert_rule_1079(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1079;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows WebDAV propfind access", 
                     sizeof("OS-WINDOWS Microsoft Windows WebDAV propfind access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1080
// Rule Name: SERVER-WEBAPP unify eWave ServletExec upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1080
static inline int check_rule_1080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /servlet/com.unify.servletexec.UploadServlet",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1080
static inline void alert_rule_1080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP unify eWave ServletExec upload", 
                     sizeof("SERVER-WEBAPP unify eWave ServletExec upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1081
// Rule Name: SERVER-WEBAPP Netscape Servers suite DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1081
static inline int check_rule_1081(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dsgw/bin/search?context=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1081
static inline void alert_rule_1081(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1081;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Servers suite DOS", 
                     sizeof("SERVER-WEBAPP Netscape Servers suite DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1082
// Rule Name: SERVER-WEBAPP amazon 1-click cookie theft
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1082
static inline int check_rule_1082(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ref%3Cscript%20language%3D%22Javascript",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1082
static inline void alert_rule_1082(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1082;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP amazon 1-click cookie theft", 
                     sizeof("SERVER-WEBAPP amazon 1-click cookie theft"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1083
// Rule Name: SERVER-WEBAPP unify eWave ServletExec DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1083
static inline int check_rule_1083(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /servlet/ServletExec",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1083
static inline void alert_rule_1083(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1083;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP unify eWave ServletExec DOS", 
                     sizeof("SERVER-WEBAPP unify eWave ServletExec DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1084
// Rule Name: SERVER-WEBAPP Allaire JRUN DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1084
static inline int check_rule_1084(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: servlet/.......",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1084
static inline void alert_rule_1084(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1084;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Allaire JRUN DOS attempt", 
                     sizeof("SERVER-WEBAPP Allaire JRUN DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1085
// Rule Name: SERVER-WEBAPP strings overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1085
static inline int check_rule_1085(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |BA|I|FE FF FF F7 D2 B9 BF FF FF FF F7 D1|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1085
static inline void alert_rule_1085(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1085;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP strings overflow", 
                     sizeof("SERVER-WEBAPP strings overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1086
// Rule Name: SERVER-WEBAPP strings overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1086
static inline int check_rule_1086(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?STRENGUR",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1086
static inline void alert_rule_1086(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1086;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP strings overflow", 
                     sizeof("SERVER-WEBAPP strings overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1088
// Rule Name: SERVER-WEBAPP eXtropia webstore directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1088
static inline int check_rule_1088(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/web_store.cgi', 'page=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1088
static inline void alert_rule_1088(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1088;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP eXtropia webstore directory traversal", 
                     sizeof("SERVER-WEBAPP eXtropia webstore directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1089
// Rule Name: SERVER-WEBAPP shopping cart directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1089
static inline int check_rule_1089(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/shop.cgi', 'page=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1089
static inline void alert_rule_1089(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1089;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP shopping cart directory traversal", 
                     sizeof("SERVER-WEBAPP shopping cart directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1090
// Rule Name: SERVER-WEBAPP Allaire Pro Web Shell attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1090
static inline int check_rule_1090(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/authenticate.cgi?PASSWORD",fast_pattern,nocase', 'config.ini']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1090
static inline void alert_rule_1090(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1090;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Allaire Pro Web Shell attempt", 
                     sizeof("SERVER-WEBAPP Allaire Pro Web Shell attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1091
// Rule Name: SERVER-WEBAPP ICQ Webfront HTTP DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1091
static inline int check_rule_1091(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ??????????",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1091
static inline void alert_rule_1091(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1091;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ICQ Webfront HTTP DOS", 
                     sizeof("SERVER-WEBAPP ICQ Webfront HTTP DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1092
// Rule Name: SERVER-WEBAPP Armada Style Master Index directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1092
static inline int check_rule_1092(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.cgi?",nocase', 'keys",distance 0,nocase', 'catigory=../",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1092
static inline void alert_rule_1092(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1092;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Armada Style Master Index directory traversal", 
                     sizeof("SERVER-WEBAPP Armada Style Master Index directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1093
// Rule Name: SERVER-WEBAPP cached_feed.cgi moreover shopping cart directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1093
static inline int check_rule_1093(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cached_feed.cgi', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1093
static inline void alert_rule_1093(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1093;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cached_feed.cgi moreover shopping cart directory traversal", 
                     sizeof("SERVER-WEBAPP cached_feed.cgi moreover shopping cart directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1095
// Rule Name: SERVER-WEBAPP Talentsoft Web+ Source Code view access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1095
static inline int check_rule_1095(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webplus.exe?",nocase', 'script=test.wml",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1095
static inline void alert_rule_1095(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1095;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Talentsoft Web+ Source Code view access", 
                     sizeof("SERVER-WEBAPP Talentsoft Web+ Source Code view access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1096
// Rule Name: SERVER-WEBAPP Talentsoft Web+ internal IP Address access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1096
static inline int check_rule_1096(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webplus.exe?",nocase', 'about",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1096
static inline void alert_rule_1096(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1096;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Talentsoft Web+ internal IP Address access", 
                     sizeof("SERVER-WEBAPP Talentsoft Web+ internal IP Address access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1097
// Rule Name: SERVER-WEBAPP Talentsoft Web+ exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1097
static inline int check_rule_1097(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webplus.cgi?",nocase', 'Script=/webplus/webping/webping.wml",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1097
static inline void alert_rule_1097(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1097;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Talentsoft Web+ exploit attempt", 
                     sizeof("SERVER-WEBAPP Talentsoft Web+ exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1098
// Rule Name: SERVER-WEBAPP SmartWin CyberOffice Shopping Cart access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1098
static inline int check_rule_1098(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: _private/shopping_cart.mdb",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1098
static inline void alert_rule_1098(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1098;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SmartWin CyberOffice Shopping Cart access", 
                     sizeof("SERVER-WEBAPP SmartWin CyberOffice Shopping Cart access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1099
// Rule Name: SERVER-WEBAPP cybercop scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1099
static inline int check_rule_1099(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cybercop",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1099
static inline void alert_rule_1099(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1099;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cybercop scan", 
                     sizeof("SERVER-WEBAPP cybercop scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1100
// Rule Name: INDICATOR-SCAN L3retriever HTTP Probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1100
static inline int check_rule_1100(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Java1.2.1|0D 0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1100
static inline void alert_rule_1100(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1100;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN L3retriever HTTP Probe", 
                     sizeof("INDICATOR-SCAN L3retriever HTTP Probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1101
// Rule Name: INDICATOR-SCAN Webtrends HTTP probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1101
static inline int check_rule_1101(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Webtrends Security Analyzer|0D 0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1101
static inline void alert_rule_1101(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1101;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN Webtrends HTTP probe", 
                     sizeof("INDICATOR-SCAN Webtrends HTTP probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1102
// Rule Name: SERVER-WEBAPP nessus 1.X 404 probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1102
static inline int check_rule_1102(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nessus_is_probing_you_",depth 32
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1102
static inline void alert_rule_1102(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1102;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nessus 1.X 404 probe", 
                     sizeof("SERVER-WEBAPP nessus 1.X 404 probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1103
// Rule Name: SERVER-WEBAPP Netscape admin passwd
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1103
static inline int check_rule_1103(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin-serv/config/admpw",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1103
static inline void alert_rule_1103(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1103;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape admin passwd", 
                     sizeof("SERVER-WEBAPP Netscape admin passwd"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1105
// Rule Name: SERVER-WEBAPP BigBrother access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1105
static inline int check_rule_1105(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bb-hostsvc.sh?",nocase', 'HOSTSVC",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1105
static inline void alert_rule_1105(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1105;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BigBrother access", 
                     sizeof("SERVER-WEBAPP BigBrother access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1106
// Rule Name: SERVER-WEBAPP Poll-it access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1106
static inline int check_rule_1106(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pollit/Poll_It_SSI_v2.0.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1106
static inline void alert_rule_1106(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1106;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Poll-it access", 
                     sizeof("SERVER-WEBAPP Poll-it access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1107
// Rule Name: SERVER-WEBAPP ftp.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1107
static inline int check_rule_1107(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ftp.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1107
static inline void alert_rule_1107(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1107;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ftp.pl access", 
                     sizeof("SERVER-WEBAPP ftp.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1108
// Rule Name: SERVER-APACHE Apache Tomcat server snoop access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1108
static inline int check_rule_1108(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/jsp/snp/', '.snp']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1108
static inline void alert_rule_1108(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1108;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat server snoop access", 
                     sizeof("SERVER-APACHE Apache Tomcat server snoop access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1109
// Rule Name: SERVER-WEBAPP ROXEN directory list attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1109
static inline int check_rule_1109(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /%00
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1109
static inline void alert_rule_1109(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1109;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ROXEN directory list attempt", 
                     sizeof("SERVER-WEBAPP ROXEN directory list attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1110
// Rule Name: SERVER-WEBAPP apache source.asp file access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1110
static inline int check_rule_1110(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /site/eg/source.asp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1110
static inline void alert_rule_1110(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1110;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP apache source.asp file access", 
                     sizeof("SERVER-WEBAPP apache source.asp file access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1111
// Rule Name: SERVER-APACHE Apache Tomcat server exploit access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1111
static inline int check_rule_1111(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /contextAdmin/contextAdmin.html",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1111
static inline void alert_rule_1111(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1111;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat server exploit access", 
                     sizeof("SERVER-APACHE Apache Tomcat server exploit access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1115
// Rule Name: SERVER-WEBAPP ICQ webserver DOS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1115
static inline int check_rule_1115(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .html/......",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1115
static inline void alert_rule_1115(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1115;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ICQ webserver DOS", 
                     sizeof("SERVER-WEBAPP ICQ webserver DOS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1116
// Rule Name: SERVER-WEBAPP Lotus DelDoc attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1116
static inline int check_rule_1116(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?DeleteDocument",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1116
static inline void alert_rule_1116(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1116;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus DelDoc attempt", 
                     sizeof("SERVER-WEBAPP Lotus DelDoc attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1117
// Rule Name: SERVER-WEBAPP Lotus EditDoc attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1117
static inline int check_rule_1117(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?EditDocument",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1117
static inline void alert_rule_1117(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1117;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus EditDoc attempt", 
                     sizeof("SERVER-WEBAPP Lotus EditDoc attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1118
// Rule Name: SERVER-WEBAPP ls 20-l
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1118
static inline int check_rule_1118(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ls%20-l",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1118
static inline void alert_rule_1118(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1118;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ls 20-l", 
                     sizeof("SERVER-WEBAPP ls 20-l"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1119
// Rule Name: SERVER-WEBAPP mlog.phtml access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1119
static inline int check_rule_1119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mlog.phtml",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1119
static inline void alert_rule_1119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mlog.phtml access", 
                     sizeof("SERVER-WEBAPP mlog.phtml access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1120
// Rule Name: SERVER-WEBAPP mylog.phtml access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1120
static inline int check_rule_1120(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mylog.phtml",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1120
static inline void alert_rule_1120(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1120;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mylog.phtml access", 
                     sizeof("SERVER-WEBAPP mylog.phtml access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1122
// Rule Name: SERVER-WEBAPP /etc/passwd file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1122
static inline int check_rule_1122(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /etc/passwd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1122
static inline void alert_rule_1122(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1122;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /etc/passwd file access attempt", 
                     sizeof("SERVER-WEBAPP /etc/passwd file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1123
// Rule Name: SERVER-WEBAPP ?PageServices access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1123
static inline int check_rule_1123(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?PageServices",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1123
static inline void alert_rule_1123(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1123;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ?PageServices access", 
                     sizeof("SERVER-WEBAPP ?PageServices access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1124
// Rule Name: SERVER-WEBAPP Ecommerce check.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1124
static inline int check_rule_1124(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /config/check.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1124
static inline void alert_rule_1124(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1124;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ecommerce check.txt access", 
                     sizeof("SERVER-WEBAPP Ecommerce check.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1125
// Rule Name: SERVER-WEBAPP webcart access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1125
static inline int check_rule_1125(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webcart/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1125
static inline void alert_rule_1125(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1125;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webcart access", 
                     sizeof("SERVER-WEBAPP webcart access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1126
// Rule Name: SERVER-WEBAPP AuthChangeUrl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1126
static inline int check_rule_1126(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: _AuthChangeUrl?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1126
static inline void alert_rule_1126(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1126;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AuthChangeUrl access", 
                     sizeof("SERVER-WEBAPP AuthChangeUrl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1127
// Rule Name: SERVER-WEBAPP convert.bas access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1127
static inline int check_rule_1127(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/convert.bas",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1127
static inline void alert_rule_1127(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1127;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP convert.bas access", 
                     sizeof("SERVER-WEBAPP convert.bas access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1128
// Rule Name: SERVER-WEBAPP cpshost.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1128
static inline int check_rule_1128(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/cpshost.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1128
static inline void alert_rule_1128(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1128;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cpshost.dll access", 
                     sizeof("SERVER-WEBAPP cpshost.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1129
// Rule Name: SERVER-WEBAPP .htaccess access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1129
static inline int check_rule_1129(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htaccess",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1129
static inline void alert_rule_1129(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1129;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .htaccess access", 
                     sizeof("SERVER-WEBAPP .htaccess access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1130
// Rule Name: SERVER-WEBAPP .wwwacl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1130
static inline int check_rule_1130(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .wwwacl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1130
static inline void alert_rule_1130(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1130;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .wwwacl access", 
                     sizeof("SERVER-WEBAPP .wwwacl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1131
// Rule Name: SERVER-WEBAPP .wwwacl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1131
static inline int check_rule_1131(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .www_acl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1131
static inline void alert_rule_1131(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1131;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .wwwacl access", 
                     sizeof("SERVER-WEBAPP .wwwacl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1132
// Rule Name: SERVER-WEBAPP Netscape Unixware overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1132
static inline int check_rule_1132(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 457
    if (tcph->dest != htons(457))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EB|_|9A FF FF FF FF 07 FF C3|^1|C0 89|F|9D|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1132
static inline void alert_rule_1132(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1132;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Unixware overflow", 
                     sizeof("SERVER-WEBAPP Netscape Unixware overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1133
// Rule Name: INDICATOR-SCAN cybercop os probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1133
static inline int check_rule_1133(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->fin == 1) && (tcph->psh == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: AAAAAAAAAAAAAAAA",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1133
static inline void alert_rule_1133(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1133;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN cybercop os probe", 
                     sizeof("INDICATOR-SCAN cybercop os probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1134
// Rule Name: SERVER-WEBAPP Phorum admin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1134
static inline int check_rule_1134(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin.php3",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1134
static inline void alert_rule_1134(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1134;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum admin access", 
                     sizeof("SERVER-WEBAPP Phorum admin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1136
// Rule Name: SERVER-WEBAPP cd..
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1136
static inline int check_rule_1136(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: cd..",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1136
static inline void alert_rule_1136(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1136;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cd..", 
                     sizeof("SERVER-WEBAPP cd.."));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1137
// Rule Name: SERVER-WEBAPP Phorum authentication access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1137
static inline int check_rule_1137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PHP_AUTH_USER=boogieman",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1137
static inline void alert_rule_1137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum authentication access", 
                     sizeof("SERVER-WEBAPP Phorum authentication access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1139
// Rule Name: SERVER-WEBAPP whisker HEAD/./
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1139
static inline int check_rule_1139(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: HEAD/./
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1139
static inline void alert_rule_1139(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1139;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP whisker HEAD/./", 
                     sizeof("SERVER-WEBAPP whisker HEAD/./"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1140
// Rule Name: SERVER-WEBAPP guestbook.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1140
static inline int check_rule_1140(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /guestbook.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1140
static inline void alert_rule_1140(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1140;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP guestbook.pl access", 
                     sizeof("SERVER-WEBAPP guestbook.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1141
// Rule Name: SERVER-WEBAPP handler access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1141
static inline int check_rule_1141(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /handler",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1141
static inline void alert_rule_1141(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1141;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP handler access", 
                     sizeof("SERVER-WEBAPP handler access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1142
// Rule Name: SERVER-WEBAPP /.... access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1142
static inline int check_rule_1142(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /....
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1142
static inline void alert_rule_1142(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1142;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /.... access", 
                     sizeof("SERVER-WEBAPP /.... access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1145
// Rule Name: SERVER-WEBAPP root access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1145
static inline int check_rule_1145(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /~root",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1145
static inline void alert_rule_1145(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1145;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP root access", 
                     sizeof("SERVER-WEBAPP root access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1146
// Rule Name: SERVER-WEBAPP Ecommerce import.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1146
static inline int check_rule_1146(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /config/import.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1146
static inline void alert_rule_1146(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1146;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ecommerce import.txt access", 
                     sizeof("SERVER-WEBAPP Ecommerce import.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1147
// Rule Name: SERVER-WEBAPP cat_ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1147
static inline int check_rule_1147(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: cat ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1147
static inline void alert_rule_1147(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1147;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cat_ access", 
                     sizeof("SERVER-WEBAPP cat_ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1148
// Rule Name: SERVER-WEBAPP Ecommerce import.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1148
static inline int check_rule_1148(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /orders/import.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1148
static inline void alert_rule_1148(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1148;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ecommerce import.txt access", 
                     sizeof("SERVER-WEBAPP Ecommerce import.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1149
// Rule Name: SERVER-WEBAPP count.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1149
static inline int check_rule_1149(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /count.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1149
static inline void alert_rule_1149(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1149;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP count.cgi access", 
                     sizeof("SERVER-WEBAPP count.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1150
// Rule Name: SERVER-WEBAPP Domino catalog.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1150
static inline int check_rule_1150(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /catalog.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1150
static inline void alert_rule_1150(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1150;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino catalog.nsf access", 
                     sizeof("SERVER-WEBAPP Domino catalog.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1151
// Rule Name: SERVER-WEBAPP Domino domcfg.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1151
static inline int check_rule_1151(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /domcfg.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1151
static inline void alert_rule_1151(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1151;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino domcfg.nsf access", 
                     sizeof("SERVER-WEBAPP Domino domcfg.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1152
// Rule Name: SERVER-WEBAPP Domino domlog.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1152
static inline int check_rule_1152(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /domlog.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1152
static inline void alert_rule_1152(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1152;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino domlog.nsf access", 
                     sizeof("SERVER-WEBAPP Domino domlog.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1153
// Rule Name: SERVER-WEBAPP Domino log.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1153
static inline int check_rule_1153(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /log.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1153
static inline void alert_rule_1153(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1153;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino log.nsf access", 
                     sizeof("SERVER-WEBAPP Domino log.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1154
// Rule Name: SERVER-WEBAPP Domino names.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1154
static inline int check_rule_1154(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /names.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1154
static inline void alert_rule_1154(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1154;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino names.nsf access", 
                     sizeof("SERVER-WEBAPP Domino names.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1155
// Rule Name: SERVER-WEBAPP Ecommerce checks.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1155
static inline int check_rule_1155(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /orders/checks.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1155
static inline void alert_rule_1155(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1155;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ecommerce checks.txt access", 
                     sizeof("SERVER-WEBAPP Ecommerce checks.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1156
// Rule Name: SERVER-WEBAPP apache directory disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1156
static inline int check_rule_1156(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['////////",fast_pattern,nocase', '////////']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1156
static inline void alert_rule_1156(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1156;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP apache directory disclosure attempt", 
                     sizeof("SERVER-WEBAPP apache directory disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1157
// Rule Name: SERVER-WEBAPP Netscape PublishingXpert access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1157
static inline int check_rule_1157(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /PSUser/PSCOErrPage.htm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1157
static inline void alert_rule_1157(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1157;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape PublishingXpert access", 
                     sizeof("SERVER-WEBAPP Netscape PublishingXpert access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1158
// Rule Name: SERVER-WEBAPP windmail.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1158
static inline int check_rule_1158(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /windmail.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1158
static inline void alert_rule_1158(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1158;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP windmail.exe access", 
                     sizeof("SERVER-WEBAPP windmail.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1159
// Rule Name: SERVER-WEBAPP webplus access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1159
static inline int check_rule_1159(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webplus?script",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1159
static inline void alert_rule_1159(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1159;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webplus access", 
                     sizeof("SERVER-WEBAPP webplus access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1160
// Rule Name: SERVER-WEBAPP Netscape dir index wp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1160
static inline int check_rule_1160(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1160
static inline void alert_rule_1160(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1160;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape dir index wp", 
                     sizeof("SERVER-WEBAPP Netscape dir index wp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1161
// Rule Name: SERVER-WEBAPP piranha passwd.php3 access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1161
static inline int check_rule_1161(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /passwd.php3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1161
static inline void alert_rule_1161(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1161;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP piranha passwd.php3 access", 
                     sizeof("SERVER-WEBAPP piranha passwd.php3 access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1162
// Rule Name: SERVER-WEBAPP cart 32 AdminPwd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1162
static inline int check_rule_1162(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /c32web.exe/ChangeAdminPassword",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1162
static inline void alert_rule_1162(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1162;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cart 32 AdminPwd access", 
                     sizeof("SERVER-WEBAPP cart 32 AdminPwd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1163
// Rule Name: SERVER-WEBAPP webdist.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1163
static inline int check_rule_1163(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webdist.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1163
static inline void alert_rule_1163(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1163;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webdist.cgi access", 
                     sizeof("SERVER-WEBAPP webdist.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1164
// Rule Name: SERVER-WEBAPP shopping cart access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1164
static inline int check_rule_1164(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /quikstore.cfg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1164
static inline void alert_rule_1164(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1164;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP shopping cart access", 
                     sizeof("SERVER-WEBAPP shopping cart access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1165
// Rule Name: SERVER-WEBAPP Novell Groupwise gwweb.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1165
static inline int check_rule_1165(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /GWWEB.EXE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1165
static inline void alert_rule_1165(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1165;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Novell Groupwise gwweb.exe access", 
                     sizeof("SERVER-WEBAPP Novell Groupwise gwweb.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1166
// Rule Name: SERVER-WEBAPP ws_ftp.ini access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1166
static inline int check_rule_1166(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ws_ftp.ini",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1166
static inline void alert_rule_1166(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1166;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ws_ftp.ini access", 
                     sizeof("SERVER-WEBAPP ws_ftp.ini access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1167
// Rule Name: SERVER-WEBAPP rpm_query access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1167
static inline int check_rule_1167(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rpm_query",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1167
static inline void alert_rule_1167(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1167;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rpm_query access", 
                     sizeof("SERVER-WEBAPP rpm_query access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1168
// Rule Name: SERVER-WEBAPP mall log order access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1168
static inline int check_rule_1168(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mall_log_files/order.log",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1168
static inline void alert_rule_1168(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1168;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mall log order access", 
                     sizeof("SERVER-WEBAPP mall log order access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1172
// Rule Name: SERVER-WEBAPP bigconf.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1172
static inline int check_rule_1172(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bigconf.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1172
static inline void alert_rule_1172(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1172;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bigconf.cgi access", 
                     sizeof("SERVER-WEBAPP bigconf.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1173
// Rule Name: SERVER-WEBAPP architext_query.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1173
static inline int check_rule_1173(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ews/architext_query.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1173
static inline void alert_rule_1173(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1173;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP architext_query.pl access", 
                     sizeof("SERVER-WEBAPP architext_query.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1174
// Rule Name: SERVER-WEBAPP /cgi-bin/jj access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1174
static inline int check_rule_1174(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/jj",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1174
static inline void alert_rule_1174(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1174;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /cgi-bin/jj access", 
                     sizeof("SERVER-WEBAPP /cgi-bin/jj access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1175
// Rule Name: SERVER-WEBAPP wwwboard.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1175
static inline int check_rule_1175(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wwwboard.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1175
static inline void alert_rule_1175(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1175;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wwwboard.pl access", 
                     sizeof("SERVER-WEBAPP wwwboard.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1177
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1177
static inline int check_rule_1177(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-verify-link",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1177
static inline void alert_rule_1177(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1177;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1178
// Rule Name: SERVER-WEBAPP Phorum read access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1178
static inline int check_rule_1178(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /read.php3",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1178
static inline void alert_rule_1178(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1178;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum read access", 
                     sizeof("SERVER-WEBAPP Phorum read access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1179
// Rule Name: SERVER-WEBAPP Phorum violation access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1179
static inline int check_rule_1179(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /violation.php3",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1179
static inline void alert_rule_1179(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1179;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum violation access", 
                     sizeof("SERVER-WEBAPP Phorum violation access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1180
// Rule Name: SERVER-WEBAPP get32.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1180
static inline int check_rule_1180(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /get32.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1180
static inline void alert_rule_1180(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1180;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP get32.exe access", 
                     sizeof("SERVER-WEBAPP get32.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1181
// Rule Name: SERVER-WEBAPP Annex Terminal DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1181
static inline int check_rule_1181(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ping?query=
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1181
static inline void alert_rule_1181(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1181;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Annex Terminal DOS attempt", 
                     sizeof("SERVER-WEBAPP Annex Terminal DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1183
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1183
static inline int check_rule_1183(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-cs-dump",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1183
static inline void alert_rule_1183(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1183;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1184
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1184
static inline int check_rule_1184(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-ver-info",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1184
static inline void alert_rule_1184(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1184;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1185
// Rule Name: SERVER-WEBAPP bizdbsearch attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1185
static inline int check_rule_1185(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bizdb1-search.cgi",fast_pattern,nocase', 'mail",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1185
static inline void alert_rule_1185(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1185;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bizdbsearch attempt", 
                     sizeof("SERVER-WEBAPP bizdbsearch attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1186
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1186
static inline int check_rule_1186(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-ver-diff",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1186
static inline void alert_rule_1186(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1186;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1187
// Rule Name: SERVER-WEBAPP SalesLogix Eviewer web command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1187
static inline int check_rule_1187(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /slxweb.dll/admin?command=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1187
static inline void alert_rule_1187(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1187;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SalesLogix Eviewer web command attempt", 
                     sizeof("SERVER-WEBAPP SalesLogix Eviewer web command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1188
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1188
static inline int check_rule_1188(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-start-ver",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1188
static inline void alert_rule_1188(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1188;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1189
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1189
static inline int check_rule_1189(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-stop-ver",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1189
static inline void alert_rule_1189(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1189;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1190
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1190
static inline int check_rule_1190(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-uncheckout",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1190
static inline void alert_rule_1190(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1190;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1191
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1191
static inline int check_rule_1191(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-html-rend",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1191
static inline void alert_rule_1191(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1191;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1192
// Rule Name: SERVER-WEBAPP Trend Micro OfficeScan access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1192
static inline int check_rule_1192(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /officescan/cgi/jdkRqNotify.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1192
static inline void alert_rule_1192(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1192;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Trend Micro OfficeScan access", 
                     sizeof("SERVER-WEBAPP Trend Micro OfficeScan access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1193
// Rule Name: SERVER-WEBAPP oracle web arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1193
static inline int check_rule_1193(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ows-bin/",nocase', '?&']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1193
static inline void alert_rule_1193(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1193;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP oracle web arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP oracle web arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1194
// Rule Name: SERVER-WEBAPP sojourn.cgi File attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1194
static inline int check_rule_1194(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/sojourn.cgi?",nocase', 'cat=",distance 0,nocase', '%00",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1194
static inline void alert_rule_1194(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1194;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sojourn.cgi File attempt", 
                     sizeof("SERVER-WEBAPP sojourn.cgi File attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1195
// Rule Name: SERVER-WEBAPP sojourn.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1195
static inline int check_rule_1195(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sojourn.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1195
static inline void alert_rule_1195(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1195;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sojourn.cgi access", 
                     sizeof("SERVER-WEBAPP sojourn.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1196
// Rule Name: SERVER-WEBAPP SGI InfoSearch fname attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1196
static inline int check_rule_1196(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/infosrch.cgi?",fast_pattern,nocase', 'fname=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1196
static inline void alert_rule_1196(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1196;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SGI InfoSearch fname attempt", 
                     sizeof("SERVER-WEBAPP SGI InfoSearch fname attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1197
// Rule Name: SERVER-WEBAPP Phorum code access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1197
static inline int check_rule_1197(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /code.php3",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1197
static inline void alert_rule_1197(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1197;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum code access", 
                     sizeof("SERVER-WEBAPP Phorum code access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1198
// Rule Name: SERVER-WEBAPP Netscape Enterprise Server directory view
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1198
static inline int check_rule_1198(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?wp-usr-prop",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1198
static inline void alert_rule_1198(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1198;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netscape Enterprise Server directory view", 
                     sizeof("SERVER-WEBAPP Netscape Enterprise Server directory view"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1199
// Rule Name: SERVER-WEBAPP Compaq Insight directory traversal
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1199
static inline int check_rule_1199(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2301
    if (tcph->dest != htons(2301))
        return 0;
    // Content matching (simplified)
    // Full pattern: ../
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1199
static inline void alert_rule_1199(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1199;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Compaq Insight directory traversal", 
                     sizeof("SERVER-WEBAPP Compaq Insight directory traversal"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1200
// Rule Name: INDICATOR-COMPROMISE Invalid URL
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1200
static inline int check_rule_1200(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Invalid URL",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1200
static inline void alert_rule_1200(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1200;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Invalid URL", 
                     sizeof("INDICATOR-COMPROMISE Invalid URL"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1201
// Rule Name: INDICATOR-COMPROMISE 403 Forbidden
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1201
static inline int check_rule_1201(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: 403
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1201
static inline void alert_rule_1201(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1201;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE 403 Forbidden", 
                     sizeof("INDICATOR-COMPROMISE 403 Forbidden"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1202
// Rule Name: SERVER-WEBAPP search.vts access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1202
static inline int check_rule_1202(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /search.vts
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1202
static inline void alert_rule_1202(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1202;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP search.vts access", 
                     sizeof("SERVER-WEBAPP search.vts access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1204
// Rule Name: SERVER-WEBAPP ax-admin.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1204
static inline int check_rule_1204(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ax-admin.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1204
static inline void alert_rule_1204(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1204;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ax-admin.cgi access", 
                     sizeof("SERVER-WEBAPP ax-admin.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1205
// Rule Name: SERVER-WEBAPP axs.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1205
static inline int check_rule_1205(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /axs.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1205
static inline void alert_rule_1205(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1205;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP axs.cgi access", 
                     sizeof("SERVER-WEBAPP axs.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1206
// Rule Name: SERVER-WEBAPP cachemgr.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1206
static inline int check_rule_1206(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cachemgr.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1206
static inline void alert_rule_1206(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1206;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cachemgr.cgi access", 
                     sizeof("SERVER-WEBAPP cachemgr.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1207
// Rule Name: SERVER-WEBAPP htgrep access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1207
static inline int check_rule_1207(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htgrep
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1207
static inline void alert_rule_1207(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1207;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htgrep access", 
                     sizeof("SERVER-WEBAPP htgrep access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1208
// Rule Name: SERVER-WEBAPP responder.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1208
static inline int check_rule_1208(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /responder.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1208
static inline void alert_rule_1208(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1208;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP responder.cgi access", 
                     sizeof("SERVER-WEBAPP responder.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1209
// Rule Name: SERVER-WEBAPP .nsconfig access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1209
static inline int check_rule_1209(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.nsconfig
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1209
static inline void alert_rule_1209(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1209;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .nsconfig access", 
                     sizeof("SERVER-WEBAPP .nsconfig access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1211
// Rule Name: SERVER-WEBAPP web-map.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1211
static inline int check_rule_1211(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /web-map.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1211
static inline void alert_rule_1211(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1211;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP web-map.cgi access", 
                     sizeof("SERVER-WEBAPP web-map.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1212
// Rule Name: SERVER-WEBAPP Admin_files access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1212
static inline int check_rule_1212(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_files",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1212
static inline void alert_rule_1212(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1212;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Admin_files access", 
                     sizeof("SERVER-WEBAPP Admin_files access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1213
// Rule Name: SERVER-WEBAPP backup access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1213
static inline int check_rule_1213(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /backup",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1213
static inline void alert_rule_1213(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1213;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP backup access", 
                     sizeof("SERVER-WEBAPP backup access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1214
// Rule Name: SERVER-WEBAPP intranet access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1214
static inline int check_rule_1214(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /intranet/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1214
static inline void alert_rule_1214(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1214;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP intranet access", 
                     sizeof("SERVER-WEBAPP intranet access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1215
// Rule Name: SERVER-WEBAPP ministats admin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1215
static inline int check_rule_1215(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ministats/admin.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1215
static inline void alert_rule_1215(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1215;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ministats admin access", 
                     sizeof("SERVER-WEBAPP ministats admin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1216
// Rule Name: SERVER-WEBAPP filemail access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1216
static inline int check_rule_1216(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /filemail",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1216
static inline void alert_rule_1216(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1216;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP filemail access", 
                     sizeof("SERVER-WEBAPP filemail access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1217
// Rule Name: SERVER-WEBAPP plusmail access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1217
static inline int check_rule_1217(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /plusmail",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1217
static inline void alert_rule_1217(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1217;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP plusmail access", 
                     sizeof("SERVER-WEBAPP plusmail access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1218
// Rule Name: SERVER-WEBAPP adminlogin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1218
static inline int check_rule_1218(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /adminlogin",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1218
static inline void alert_rule_1218(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1218;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP adminlogin access", 
                     sizeof("SERVER-WEBAPP adminlogin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1219
// Rule Name: SERVER-WEBAPP dfire.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1219
static inline int check_rule_1219(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dfire.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1219
static inline void alert_rule_1219(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1219;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dfire.cgi access", 
                     sizeof("SERVER-WEBAPP dfire.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1220
// Rule Name: SERVER-WEBAPP ultraboard access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1220
static inline int check_rule_1220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ultraboard",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1220
static inline void alert_rule_1220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ultraboard access", 
                     sizeof("SERVER-WEBAPP ultraboard access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1221
// Rule Name: SERVER-WEBAPP Muscat Empower cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1221
static inline int check_rule_1221(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /empower?DB",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1221
static inline void alert_rule_1221(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1221;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Muscat Empower cgi access", 
                     sizeof("SERVER-WEBAPP Muscat Empower cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1222
// Rule Name: SERVER-WEBAPP pals-cgi arbitrary file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1222
static inline int check_rule_1222(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/pals-cgi",fast_pattern,nocase', 'documentName=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1222
static inline void alert_rule_1222(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1222;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pals-cgi arbitrary file access attempt", 
                     sizeof("SERVER-WEBAPP pals-cgi arbitrary file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1224
// Rule Name: SERVER-WEBAPP ROADS search.pl attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1224
static inline int check_rule_1224(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ROADS/cgi-bin/search.pl', 'form=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1224
static inline void alert_rule_1224(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1224;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ROADS search.pl attempt", 
                     sizeof("SERVER-WEBAPP ROADS search.pl attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1225
// Rule Name: X11 MIT Magic Cookie detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1225
static inline int check_rule_1225(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6000
    if (tcph->dest != htons(6000))
        return 0;
    // Content matching (simplified)
    // Full pattern: MIT-MAGIC-COOKIE-1",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1225
static inline void alert_rule_1225(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1225;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "X11 MIT Magic Cookie detected", 
                     sizeof("X11 MIT Magic Cookie detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1226
// Rule Name: X11 xopen
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unknown
// Generated from Snort rule parser output

// Check function for rule 1226
static inline int check_rule_1226(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6000
    if (tcph->dest != htons(6000))
        return 0;
    // Content matching (simplified)
    // Full pattern: l|00 0B 00 00 00 00 00 00 00 00 00|",fast_pattern,fast_pattern_offset 0,fast_pattern_length 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1226
static inline void alert_rule_1226(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1226;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "X11 xopen", 
                     sizeof("X11 xopen"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1229
// Rule Name: PROTOCOL-FTP CWD ...
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1229
static inline int check_rule_1229(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['CWD",nocase', '...",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1229
static inline void alert_rule_1229(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1229;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CWD ...", 
                     sizeof("PROTOCOL-FTP CWD ..."));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1230
// Rule Name: SERVER-WEBAPP VirusWall FtpSave access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1230
static inline int check_rule_1230(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /FtpSave.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1230
static inline void alert_rule_1230(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1230;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VirusWall FtpSave access", 
                     sizeof("SERVER-WEBAPP VirusWall FtpSave access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1231
// Rule Name: SERVER-WEBAPP VirusWall catinfo access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1231
static inline int check_rule_1231(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /catinfo",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1231
static inline void alert_rule_1231(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1231;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VirusWall catinfo access", 
                     sizeof("SERVER-WEBAPP VirusWall catinfo access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1232
// Rule Name: SERVER-WEBAPP VirusWall catinfo access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1232
static inline int check_rule_1232(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1812
    if (tcph->dest != htons(1812))
        return 0;
    // Content matching (simplified)
    // Full pattern: /catinfo",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1232
static inline void alert_rule_1232(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1232;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VirusWall catinfo access", 
                     sizeof("SERVER-WEBAPP VirusWall catinfo access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1234
// Rule Name: SERVER-WEBAPP VirusWall FtpSaveCSP access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1234
static inline int check_rule_1234(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /FtpSaveCSP.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1234
static inline void alert_rule_1234(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1234;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VirusWall FtpSaveCSP access", 
                     sizeof("SERVER-WEBAPP VirusWall FtpSaveCSP access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1235
// Rule Name: SERVER-WEBAPP VirusWall FtpSaveCVP access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1235
static inline int check_rule_1235(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /FtpSaveCVP.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1235
static inline void alert_rule_1235(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1235;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VirusWall FtpSaveCVP access", 
                     sizeof("SERVER-WEBAPP VirusWall FtpSaveCVP access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1239
// Rule Name: OS-WINDOWS RFParalyze Attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1239
static inline int check_rule_1239(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['BEAVIS', 'yep yep']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1239
static inline void alert_rule_1239(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1239;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS RFParalyze Attempt", 
                     sizeof("OS-WINDOWS RFParalyze Attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1240
// Rule Name: SERVER-OTHER MDBMS overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1240
static inline int check_rule_1240(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2224
    if (tcph->dest != htons(2224))
        return 0;
    // Content matching (simplified)
    // Full pattern: |01|1|DB CD 80 E8|[|FF FF FF|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1240
static inline void alert_rule_1240(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1240;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER MDBMS overflow", 
                     sizeof("SERVER-OTHER MDBMS overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1241
// Rule Name: SERVER-WEBAPP SWEditServlet directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1241
static inline int check_rule_1241(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/SWEditServlet', 'template=../../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1241
static inline void alert_rule_1241(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1241;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SWEditServlet directory traversal attempt", 
                     sizeof("SERVER-WEBAPP SWEditServlet directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1242
// Rule Name: SERVER-IIS ISAPI .ida access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1242
static inline int check_rule_1242(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .ida",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1242
static inline void alert_rule_1242(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1242;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ISAPI .ida access", 
                     sizeof("SERVER-IIS ISAPI .ida access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1243
// Rule Name: SERVER-IIS ISAPI .ida attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1243
static inline int check_rule_1243(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .ida?",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1243
static inline void alert_rule_1243(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1243;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ISAPI .ida attempt", 
                     sizeof("SERVER-IIS ISAPI .ida attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1244
// Rule Name: SERVER-IIS ISAPI .idq attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1244
static inline int check_rule_1244(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .idq?",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1244
static inline void alert_rule_1244(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1244;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ISAPI .idq attempt", 
                     sizeof("SERVER-IIS ISAPI .idq attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1245
// Rule Name: SERVER-IIS ISAPI .idq access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1245
static inline int check_rule_1245(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .idq",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1245
static inline void alert_rule_1245(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1245;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ISAPI .idq access", 
                     sizeof("SERVER-IIS ISAPI .idq access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1248
// Rule Name: SERVER-OTHER Microsoft Frontpage rad fp30reg.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1248
static inline int check_rule_1248(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fp30reg.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1248
static inline void alert_rule_1248(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1248;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage rad fp30reg.dll access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage rad fp30reg.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1249
// Rule Name: SERVER-OTHER Microsoft Frontpage rad fp4areg.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1249
static inline int check_rule_1249(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fp4areg.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1249
static inline void alert_rule_1249(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1249;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage rad fp4areg.dll access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage rad fp4areg.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1250
// Rule Name: OS-OTHER Cisco IOS HTTP configuration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1250
static inline int check_rule_1250(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /level/
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1250
static inline void alert_rule_1250(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1250;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Cisco IOS HTTP configuration attempt", 
                     sizeof("OS-OTHER Cisco IOS HTTP configuration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1252
// Rule Name: PROTOCOL-TELNET bsd telnet exploit response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1252
static inline int check_rule_1252(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $TELNET_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23
    if (tcph->source != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0D 0A|[Yes]|0D 0A FF FE 08 FF FD|&",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1252
static inline void alert_rule_1252(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1252;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET bsd telnet exploit response", 
                     sizeof("PROTOCOL-TELNET bsd telnet exploit response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1253
// Rule Name: PROTOCOL-TELNET bsd exploit client finishing
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 1253
static inline int check_rule_1253(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF F6 FF F6 FF FB 08 FF F6|",depth 50,offset 200
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1253
static inline void alert_rule_1253(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1253;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET bsd exploit client finishing", 
                     sizeof("PROTOCOL-TELNET bsd exploit client finishing"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1254
// Rule Name: SERVER-WEBAPP PHPLIB remote command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1254
static inline int check_rule_1254(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: _PHPLIB[libdir]",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1254
static inline void alert_rule_1254(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1254;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHPLIB remote command attempt", 
                     sizeof("SERVER-WEBAPP PHPLIB remote command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1255
// Rule Name: SERVER-WEBAPP PHPLIB remote command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1255
static inline int check_rule_1255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /db_mysql.inc
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1255
static inline void alert_rule_1255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHPLIB remote command attempt", 
                     sizeof("SERVER-WEBAPP PHPLIB remote command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1256
// Rule Name: SERVER-IIS CodeRed v2 root.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1256
static inline int check_rule_1256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /root.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1256
static inline void alert_rule_1256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS CodeRed v2 root.exe access", 
                     sizeof("SERVER-IIS CodeRed v2 root.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1257
// Rule Name: SERVER-OTHER Winnuke attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1257
static inline int check_rule_1257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 135-139
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 135 || dst_port > 139)
        return 0;
    // Check TCP flags
    if (!((tcph->urg == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1257
static inline void alert_rule_1257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Winnuke attack", 
                     sizeof("SERVER-OTHER Winnuke attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1259
// Rule Name: SERVER-WEBAPP SWEditServlet access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1259
static inline int check_rule_1259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /SWEditServlet
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1259
static inline void alert_rule_1259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SWEditServlet access", 
                     sizeof("SERVER-WEBAPP SWEditServlet access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1261
// Rule Name: SERVER-OTHER AIX pdnsd overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1261
static inline int check_rule_1261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4242
    if (tcph->dest != htons(4242))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|7F FF FB|x|7F FF FB|x|7F FF FB|x|7F FF FB|x', '@|8A FF C8|@|82 FF D8 3B|6|FE 03 3B|v|FE 02|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1261
static inline void alert_rule_1261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER AIX pdnsd overflow", 
                     sizeof("SERVER-OTHER AIX pdnsd overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1262
// Rule Name: PROTOCOL-RPC portmap admind request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1262
static inline int check_rule_1262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 F7|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1262
static inline void alert_rule_1262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap admind request TCP", 
                     sizeof("PROTOCOL-RPC portmap admind request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1263
// Rule Name: PROTOCOL-RPC portmap amountd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1263
static inline int check_rule_1263(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87 03|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1263
static inline void alert_rule_1263(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1263;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap amountd request TCP", 
                     sizeof("PROTOCOL-RPC portmap amountd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1264
// Rule Name: PROTOCOL-RPC portmap bootparam request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1264
static inline int check_rule_1264(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 BA|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1264
static inline void alert_rule_1264(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1264;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap bootparam request TCP", 
                     sizeof("PROTOCOL-RPC portmap bootparam request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1265
// Rule Name: PROTOCOL-RPC portmap cmsd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1265
static inline int check_rule_1265(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 E4|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1265
static inline void alert_rule_1265(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1265;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap cmsd request TCP", 
                     sizeof("PROTOCOL-RPC portmap cmsd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1267
// Rule Name: PROTOCOL-RPC portmap nisd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1267
static inline int check_rule_1267(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87 CC|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1267
static inline void alert_rule_1267(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1267;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap nisd request TCP", 
                     sizeof("PROTOCOL-RPC portmap nisd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1268
// Rule Name: PROTOCOL-RPC portmap pcnfsd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1268
static inline int check_rule_1268(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 02|I|F1|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1268
static inline void alert_rule_1268(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1268;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap pcnfsd request TCP", 
                     sizeof("PROTOCOL-RPC portmap pcnfsd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1269
// Rule Name: PROTOCOL-RPC portmap rexd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1269
static inline int check_rule_1269(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 B1|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1269
static inline void alert_rule_1269(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1269;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap rexd request TCP", 
                     sizeof("PROTOCOL-RPC portmap rexd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1270
// Rule Name: PROTOCOL-RPC portmap rstatd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1270
static inline int check_rule_1270(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A1|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1270
static inline void alert_rule_1270(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1270;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap rstatd request TCP", 
                     sizeof("PROTOCOL-RPC portmap rstatd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1271
// Rule Name: PROTOCOL-RPC portmap rusers request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1271
static inline int check_rule_1271(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A2|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1271
static inline void alert_rule_1271(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1271;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap rusers request TCP", 
                     sizeof("PROTOCOL-RPC portmap rusers request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1272
// Rule Name: PROTOCOL-RPC portmap sadmind request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1272
static inline int check_rule_1272(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87 88|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1272
static inline void alert_rule_1272(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1272;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap sadmind request TCP", 
                     sizeof("PROTOCOL-RPC portmap sadmind request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1273
// Rule Name: PROTOCOL-RPC portmap selection_svc request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1273
static inline int check_rule_1273(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 AF|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1273
static inline void alert_rule_1273(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1273;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap selection_svc request TCP", 
                     sizeof("PROTOCOL-RPC portmap selection_svc request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1274
// Rule Name: PROTOCOL-RPC portmap ttdbserv request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1274
static inline int check_rule_1274(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 F3|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1274
static inline void alert_rule_1274(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1274;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap ttdbserv request TCP", 
                     sizeof("PROTOCOL-RPC portmap ttdbserv request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1275
// Rule Name: PROTOCOL-RPC portmap yppasswd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1275
static inline int check_rule_1275(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A9|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1275
static inline void alert_rule_1275(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1275;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap yppasswd request TCP", 
                     sizeof("PROTOCOL-RPC portmap yppasswd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1276
// Rule Name: PROTOCOL-RPC portmap ypserv request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1276
static inline int check_rule_1276(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A4|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1276
static inline void alert_rule_1276(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1276;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap ypserv request TCP", 
                     sizeof("PROTOCOL-RPC portmap ypserv request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1283
// Rule Name: SERVER-IIS Microsoft Office Outlook web dos
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1283
static inline int check_rule_1283(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/exchange/LogonFrm.asp?",fast_pattern,nocase', 'mailbox=",nocase', '%%%']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1283
static inline void alert_rule_1283(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1283;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Microsoft Office Outlook web dos", 
                     sizeof("SERVER-IIS Microsoft Office Outlook web dos"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1284
// Rule Name: SERVER-OTHER readme.eml download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1284
static inline int check_rule_1284(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /readme.eml",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1284
static inline void alert_rule_1284(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1284;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER readme.eml download attempt", 
                     sizeof("SERVER-OTHER readme.eml download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1285
// Rule Name: SERVER-IIS msdac access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1285
static inline int check_rule_1285(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /msdac/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1285
static inline void alert_rule_1285(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1285;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS msdac access", 
                     sizeof("SERVER-IIS msdac access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1286
// Rule Name: SERVER-IIS _mem_bin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1286
static inline int check_rule_1286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_mem_bin/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1286
static inline void alert_rule_1286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS _mem_bin access", 
                     sizeof("SERVER-IIS _mem_bin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1288
// Rule Name: SERVER-OTHER Microsoft Frontpage /_vti_bin/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1288
static inline int check_rule_1288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_vti_bin/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1288
static inline void alert_rule_1288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Microsoft Frontpage /_vti_bin/ access", 
                     sizeof("SERVER-OTHER Microsoft Frontpage /_vti_bin/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1290
// Rule Name: FILE-OTHER readme.eml autoload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1290
static inline int check_rule_1290(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: window.open|28 22|readme.eml|22|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1290
static inline void alert_rule_1290(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1290;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER readme.eml autoload attempt", 
                     sizeof("FILE-OTHER readme.eml autoload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1291
// Rule Name: SERVER-WEBAPP sml3com access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1291
static inline int check_rule_1291(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /graphics/sml3com
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1291
static inline void alert_rule_1291(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1291;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sml3com access", 
                     sizeof("SERVER-WEBAPP sml3com access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1292
// Rule Name: INDICATOR-COMPROMISE directory listing
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1292
static inline int check_rule_1292(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: Volume Serial Number
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1292
static inline void alert_rule_1292(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1292;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE directory listing", 
                     sizeof("INDICATOR-COMPROMISE directory listing"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1295
// Rule Name: INDICATOR-COMPROMISE nimda RICHED20.DLL
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1295
static inline int check_rule_1295(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: R|00|I|00|C|00|H|00|E|00|D|00|2|00|0|00|.|00|D|00|L|00|L",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1295
static inline void alert_rule_1295(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1295;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE nimda RICHED20.DLL", 
                     sizeof("INDICATOR-COMPROMISE nimda RICHED20.DLL"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1300
// Rule Name: SERVER-WEBAPP admin.php file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1300
static inline int check_rule_1300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/admin.php",fast_pattern,nocase', 'file_name=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1300
static inline void alert_rule_1300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP admin.php file upload attempt", 
                     sizeof("SERVER-WEBAPP admin.php file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1301
// Rule Name: SERVER-WEBAPP admin.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1301
static inline int check_rule_1301(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1301
static inline void alert_rule_1301(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1301;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP admin.php access", 
                     sizeof("SERVER-WEBAPP admin.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1302
// Rule Name: SERVER-WEBAPP console.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1302
static inline int check_rule_1302(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/console.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1302
static inline void alert_rule_1302(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1302;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP console.exe access", 
                     sizeof("SERVER-WEBAPP console.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1303
// Rule Name: SERVER-WEBAPP cs.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1303
static inline int check_rule_1303(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/cs.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1303
static inline void alert_rule_1303(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1303;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cs.exe access", 
                     sizeof("SERVER-WEBAPP cs.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1304
// Rule Name: SERVER-WEBAPP txt2html.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1304
static inline int check_rule_1304(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /txt2html.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1304
static inline void alert_rule_1304(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1304;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP txt2html.cgi access", 
                     sizeof("SERVER-WEBAPP txt2html.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1305
// Rule Name: SERVER-WEBAPP txt2html.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1305
static inline int check_rule_1305(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/txt2html.cgi",fast_pattern,nocase', '/../../../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1305
static inline void alert_rule_1305(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1305;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP txt2html.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP txt2html.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1307
// Rule Name: SERVER-WEBAPP store.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1307
static inline int check_rule_1307(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /store.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1307
static inline void alert_rule_1307(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1307;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP store.cgi access", 
                     sizeof("SERVER-WEBAPP store.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1308
// Rule Name: SERVER-WEBAPP sendmessage.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1308
static inline int check_rule_1308(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sendmessage.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1308
static inline void alert_rule_1308(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1308;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sendmessage.cgi access", 
                     sizeof("SERVER-WEBAPP sendmessage.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1309
// Rule Name: SERVER-WEBAPP zsh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1309
static inline int check_rule_1309(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /zsh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1309
static inline void alert_rule_1309(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1309;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP zsh access", 
                     sizeof("SERVER-WEBAPP zsh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1323
// Rule Name: SERVER-OTHER rwhoisd format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1323
static inline int check_rule_1323(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4321
    if (tcph->dest != htons(4321))
        return 0;
    // Content matching (simplified)
    // Full pattern: -soa %p
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1323
static inline void alert_rule_1323(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1323;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER rwhoisd format string attempt", 
                     sizeof("SERVER-OTHER rwhoisd format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1324
// Rule Name: INDICATOR-SHELLCODE ssh CRC32 overflow /bin/sh
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1324
static inline int check_rule_1324(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $SSH_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bin/sh
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1324
static inline void alert_rule_1324(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1324;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE ssh CRC32 overflow /bin/sh", 
                     sizeof("INDICATOR-SHELLCODE ssh CRC32 overflow /bin/sh"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1325
// Rule Name: INDICATOR-SHELLCODE ssh CRC32 overflow filler
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1325
static inline int check_rule_1325(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $SSH_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1325
static inline void alert_rule_1325(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1325;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE ssh CRC32 overflow filler", 
                     sizeof("INDICATOR-SHELLCODE ssh CRC32 overflow filler"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1326
// Rule Name: INDICATOR-SHELLCODE ssh CRC32 overflow NOOP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1326
static inline int check_rule_1326(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $SSH_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |90 90 90 90 90 90 90 90 90 90 90 90 90 90 90 90|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1326
static inline void alert_rule_1326(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1326;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE ssh CRC32 overflow NOOP", 
                     sizeof("INDICATOR-SHELLCODE ssh CRC32 overflow NOOP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1327
// Rule Name: INDICATOR-SHELLCODE ssh CRC32 overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1327
static inline int check_rule_1327(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $SSH_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|00 01|W|00 00 00 18|",depth 7', '|FF FF FF FF 00 00|",depth 14,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1327
static inline void alert_rule_1327(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1327;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE ssh CRC32 overflow", 
                     sizeof("INDICATOR-SHELLCODE ssh CRC32 overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1374
// Rule Name: SERVER-WEBAPP .htgroup access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1374
static inline int check_rule_1374(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .htgroup",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1374
static inline void alert_rule_1374(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1374;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .htgroup access", 
                     sizeof("SERVER-WEBAPP .htgroup access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1375
// Rule Name: SERVER-WEBAPP sadmind worm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1375
static inline int check_rule_1375(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GET x HTTP/1.0",depth 15
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1375
static inline void alert_rule_1375(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1375;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sadmind worm access", 
                     sizeof("SERVER-WEBAPP sadmind worm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1376
// Rule Name: SERVER-WEBAPP jrun directory browse attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1376
static inline int check_rule_1376(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /?.jsp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1376
static inline void alert_rule_1376(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1376;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP jrun directory browse attempt", 
                     sizeof("SERVER-WEBAPP jrun directory browse attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1377
// Rule Name: PROTOCOL-FTP wu-ftp bad file completion attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1377
static inline int check_rule_1377(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['~', '[",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1377
static inline void alert_rule_1377(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1377;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP wu-ftp bad file completion attempt", 
                     sizeof("PROTOCOL-FTP wu-ftp bad file completion attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1378
// Rule Name: PROTOCOL-FTP wu-ftp bad file completion attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1378
static inline int check_rule_1378(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['~', '{",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1378
static inline void alert_rule_1378(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1378;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP wu-ftp bad file completion attempt", 
                     sizeof("PROTOCOL-FTP wu-ftp bad file completion attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1379
// Rule Name: PROTOCOL-FTP STAT overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1379
static inline int check_rule_1379(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: STAT",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1379
static inline void alert_rule_1379(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1379;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP STAT overflow attempt", 
                     sizeof("PROTOCOL-FTP STAT overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1380
// Rule Name: SERVER-IIS Form_VBScript.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1380
static inline int check_rule_1380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Form_VBScript.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1380
static inline void alert_rule_1380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Form_VBScript.asp access", 
                     sizeof("SERVER-IIS Form_VBScript.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1381
// Rule Name: SERVER-WEBAPP Trend Micro OfficeScan attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1381
static inline int check_rule_1381(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/officescan/cgi/jdkRqNotify.exe?",nocase', 'domain=",nocase', 'event=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1381
static inline void alert_rule_1381(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1381;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Trend Micro OfficeScan attempt", 
                     sizeof("SERVER-WEBAPP Trend Micro OfficeScan attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1382
// Rule Name: SERVER-OTHER CHAT IRC Ettercap parse overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1382
static inline int check_rule_1382(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PRIVMSG",fast_pattern,nocase', 'nickserv",nocase', 'IDENTIFY",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1382
static inline void alert_rule_1382(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1382;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER CHAT IRC Ettercap parse overflow attempt", 
                     sizeof("SERVER-OTHER CHAT IRC Ettercap parse overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1385
// Rule Name: SERVER-WEBAPP mod-plsql administration access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1385
static inline int check_rule_1385(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_/
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1385
static inline void alert_rule_1385(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1385;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mod-plsql administration access", 
                     sizeof("SERVER-WEBAPP mod-plsql administration access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1386
// Rule Name: SERVER-MSSQL raiserror possible buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1386
static inline int check_rule_1386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: r|00|a|00|i|00|s|00|e|00|r|00|r|00|o|00|r|00|",offset 32,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1386
static inline void alert_rule_1386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MSSQL raiserror possible buffer overflow", 
                     sizeof("SERVER-MSSQL raiserror possible buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1387
// Rule Name: SQL raiserror possible buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1387
static inline int check_rule_1387(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 1433
    if (tcph->dest != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: r|00|a|00|i|00|s|00|e|00|r|00|r|00|o|00|r|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1387
static inline void alert_rule_1387(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1387;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL raiserror possible buffer overflow", 
                     sizeof("SQL raiserror possible buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1392
// Rule Name: SERVER-WEBAPP lastlines.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1392
static inline int check_rule_1392(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /lastlines.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1392
static inline void alert_rule_1392(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1392;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP lastlines.cgi access", 
                     sizeof("SERVER-WEBAPP lastlines.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1395
// Rule Name: SERVER-WEBAPP zml.cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1395
static inline int check_rule_1395(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/zml.cgi', 'file=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1395
static inline void alert_rule_1395(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1395;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP zml.cgi attempt", 
                     sizeof("SERVER-WEBAPP zml.cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1396
// Rule Name: SERVER-WEBAPP zml.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1396
static inline int check_rule_1396(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /zml.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1396
static inline void alert_rule_1396(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1396;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP zml.cgi access", 
                     sizeof("SERVER-WEBAPP zml.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1397
// Rule Name: SERVER-WEBAPP wayboard attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1397
static inline int check_rule_1397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/way-board/way-board.cgi', 'db=', '../..']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1397
static inline void alert_rule_1397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wayboard attempt", 
                     sizeof("SERVER-WEBAPP wayboard attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1398
// Rule Name: SERVER-OTHER CDE dtspcd exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1398
static inline int check_rule_1398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6112
    if (tcph->dest != htons(6112))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['1",depth 1,offset 10', '!"000",depth 3,offset 11']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1398
static inline void alert_rule_1398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER CDE dtspcd exploit attempt", 
                     sizeof("SERVER-OTHER CDE dtspcd exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1399
// Rule Name: SERVER-WEBAPP PHP-Nuke remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1399
static inline int check_rule_1399(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php",fast_pattern,nocase', 'file=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1399
static inline void alert_rule_1399(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1399;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHP-Nuke remote file include attempt", 
                     sizeof("SERVER-WEBAPP PHP-Nuke remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1400
// Rule Name: SERVER-IIS /scripts/samples/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1400
static inline int check_rule_1400(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /scripts/samples/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1400
static inline void alert_rule_1400(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1400;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /scripts/samples/ access", 
                     sizeof("SERVER-IIS /scripts/samples/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1401
// Rule Name: SERVER-IIS /msadc/samples/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1401
static inline int check_rule_1401(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /msadc/samples/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1401
static inline void alert_rule_1401(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1401;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /msadc/samples/ access", 
                     sizeof("SERVER-IIS /msadc/samples/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1402
// Rule Name: SERVER-IIS iissamples access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1402
static inline int check_rule_1402(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iissamples/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1402
static inline void alert_rule_1402(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1402;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS iissamples access", 
                     sizeof("SERVER-IIS iissamples access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1405
// Rule Name: SERVER-WEBAPP AHG search.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1405
static inline int check_rule_1405(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/publisher/search.cgi",fast_pattern,nocase', 'template=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1405
static inline void alert_rule_1405(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1405;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AHG search.cgi access", 
                     sizeof("SERVER-WEBAPP AHG search.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1406
// Rule Name: SERVER-WEBAPP agora.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1406
static inline int check_rule_1406(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /store/agora.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1406
static inline void alert_rule_1406(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1406;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP agora.cgi access", 
                     sizeof("SERVER-WEBAPP agora.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1407
// Rule Name: SERVER-WEBAPP smssend.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1407
static inline int check_rule_1407(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /smssend.php
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1407
static inline void alert_rule_1407(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1407;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP smssend.php access", 
                     sizeof("SERVER-WEBAPP smssend.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1408
// Rule Name: SERVER-OTHER MSDTC attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1408
static inline int check_rule_1408(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3372
    if (tcph->dest != htons(3372))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1408
static inline void alert_rule_1408(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1408;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER MSDTC attempt", 
                     sizeof("SERVER-OTHER MSDTC attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1410
// Rule Name: SERVER-WEBAPP dcboard.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1410
static inline int check_rule_1410(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dcboard.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1410
static inline void alert_rule_1410(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1410;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dcboard.cgi access", 
                     sizeof("SERVER-WEBAPP dcboard.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1412
// Rule Name: PROTOCOL-SNMP public access tcp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1412
static inline int check_rule_1412(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 161
    if (tcph->dest != htons(161))
        return 0;
    // Content matching (simplified)
    // Full pattern: public
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1412
static inline void alert_rule_1412(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1412;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SNMP public access tcp", 
                     sizeof("PROTOCOL-SNMP public access tcp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1414
// Rule Name: PROTOCOL-SNMP private access tcp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1414
static inline int check_rule_1414(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 161
    if (tcph->dest != htons(161))
        return 0;
    // Content matching (simplified)
    // Full pattern: private
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1414
static inline void alert_rule_1414(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1414;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SNMP private access tcp", 
                     sizeof("PROTOCOL-SNMP private access tcp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1418
// Rule Name: PROTOCOL-SNMP request tcp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1418
static inline int check_rule_1418(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 161
    if (tcph->dest != htons(161))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1418
static inline void alert_rule_1418(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1418;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SNMP request tcp", 
                     sizeof("PROTOCOL-SNMP request tcp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1420
// Rule Name: PROTOCOL-SNMP trap tcp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1420
static inline int check_rule_1420(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 162
    if (tcph->dest != htons(162))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1420
static inline void alert_rule_1420(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1420;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SNMP trap tcp", 
                     sizeof("PROTOCOL-SNMP trap tcp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1421
// Rule Name: PROTOCOL-SNMP AgentX/tcp request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1421
static inline int check_rule_1421(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 705
    if (tcph->dest != htons(705))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1421
static inline void alert_rule_1421(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1421;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SNMP AgentX/tcp request", 
                     sizeof("PROTOCOL-SNMP AgentX/tcp request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1423
// Rule Name: SERVER-WEBAPP content-disposition memchr overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1423
static inline int check_rule_1423(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Disposition|3A|",nocase', 'name=|22 CC CC CC CC CC|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1423
static inline void alert_rule_1423(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1423;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP content-disposition memchr overflow", 
                     sizeof("SERVER-WEBAPP content-disposition memchr overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1425
// Rule Name: SERVER-WEBAPP content-disposition file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1425
static inline int check_rule_1425(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Disposition|3A|",nocase', 'form-data|3B|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1425
static inline void alert_rule_1425(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1425;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP content-disposition file upload attempt", 
                     sizeof("SERVER-WEBAPP content-disposition file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1428
// Rule Name: POLICY-MULTIMEDIA audio galaxy keepalive
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 1428
static inline int check_rule_1428(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Check destination IP: 64.245.58.0/23
    __u32 destination_net = htonl(0x40f53a00);
    __u32 destination_mask = htonl(0xfffffe00);
    if ((iph->daddr & destination_mask) != (destination_net & destination_mask))
        return 0;

    // Content matching (simplified)
    // Full pattern: E_|00 03 05|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1428
static inline void alert_rule_1428(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1428;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-MULTIMEDIA audio galaxy keepalive", 
                     sizeof("POLICY-MULTIMEDIA audio galaxy keepalive"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1432
// Rule Name: PUA-P2P GNUTella client request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1432
static inline int check_rule_1432(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: GNUTELLA",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1432
static inline void alert_rule_1432(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1432;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P GNUTella client request", 
                     sizeof("PUA-P2P GNUTella client request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1433
// Rule Name: SERVER-WEBAPP .history access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1433
static inline int check_rule_1433(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.history
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1433
static inline void alert_rule_1433(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1433;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .history access", 
                     sizeof("SERVER-WEBAPP .history access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1434
// Rule Name: SERVER-WEBAPP .bash_history access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1434
static inline int check_rule_1434(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.bash_history
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1434
static inline void alert_rule_1434(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1434;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .bash_history access", 
                     sizeof("SERVER-WEBAPP .bash_history access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1435
// Rule Name: PROTOCOL-DNS named authors attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1435
static inline int check_rule_1435(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|07|authors",offset 12,nocase', '|04|bind|00|",offset 12,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1435
static inline void alert_rule_1435(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1435;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-DNS named authors attempt", 
                     sizeof("PROTOCOL-DNS named authors attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1436
// Rule Name: POLICY-MULTIMEDIA Apple Quicktime User Agent access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1436
static inline int check_rule_1436(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Quicktime",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1436
static inline void alert_rule_1436(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1436;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-MULTIMEDIA Apple Quicktime User Agent access", 
                     sizeof("POLICY-MULTIMEDIA Apple Quicktime User Agent access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1437
// Rule Name: FILE-IDENTIFY Microsoft Windows Media download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 1437
static inline int check_rule_1437(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Content-Type|3A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1437
static inline void alert_rule_1437(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1437;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Windows Media download detected", 
                     sizeof("FILE-IDENTIFY Microsoft Windows Media download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1439
// Rule Name: POLICY-MULTIMEDIA Shoutcast playlist redirection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1439
static inline int check_rule_1439(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-type|3A|",nocase', 'audio/x-scpls",within 50,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1439
static inline void alert_rule_1439(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1439;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-MULTIMEDIA Shoutcast playlist redirection", 
                     sizeof("POLICY-MULTIMEDIA Shoutcast playlist redirection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1440
// Rule Name: POLICY-MULTIMEDIA Icecast playlist redirection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1440
static inline int check_rule_1440(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-type|3A|",nocase', 'audio/x-mpegurl",within 50,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1440
static inline void alert_rule_1440(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1440;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-MULTIMEDIA Icecast playlist redirection", 
                     sizeof("POLICY-MULTIMEDIA Icecast playlist redirection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1445
// Rule Name: INDICATOR-COMPROMISE FTP file_id.diz access possible warez site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 1445
static inline int check_rule_1445(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['RETR",nocase', 'file_id.diz",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1445
static inline void alert_rule_1445(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1445;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE FTP file_id.diz access possible warez site", 
                     sizeof("INDICATOR-COMPROMISE FTP file_id.diz access possible warez site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1446
// Rule Name: SERVER-MAIL vrfy root
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1446
static inline int check_rule_1446(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['vrfy",nocase', 'root",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1446
static inline void alert_rule_1446(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1446;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL vrfy root", 
                     sizeof("SERVER-MAIL vrfy root"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1447
// Rule Name: POLICY-OTHER Microsoft Windows Terminal server RDP attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1447
static inline int check_rule_1447(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: |03 00 00 0B 06 E0 00 00 00 00 00|",depth 11
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1447
static inline void alert_rule_1447(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1447;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Microsoft Windows Terminal server RDP attempt", 
                     sizeof("POLICY-OTHER Microsoft Windows Terminal server RDP attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1448
// Rule Name: POLICY-OTHER Microsoft Windows Terminal server request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1448
static inline int check_rule_1448(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|03 00 00|",depth 3', '|E0 00 00 00 00 00|",depth 6,offset 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1448
static inline void alert_rule_1448(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1448;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Microsoft Windows Terminal server request attempt", 
                     sizeof("POLICY-OTHER Microsoft Windows Terminal server request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1450
// Rule Name: SERVER-MAIL Vintra Mailserver expn *@
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1450
static inline int check_rule_1450(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['expn",fast_pattern,nocase', '*@']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1450
static inline void alert_rule_1450(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1450;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Vintra Mailserver expn *@", 
                     sizeof("SERVER-MAIL Vintra Mailserver expn *@"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1451
// Rule Name: SERVER-WEBAPP NPH-maillist access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1451
static inline int check_rule_1451(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nph-maillist.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1451
static inline void alert_rule_1451(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1451;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP NPH-maillist access", 
                     sizeof("SERVER-WEBAPP NPH-maillist access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1452
// Rule Name: SERVER-WEBAPP args.cmd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1452
static inline int check_rule_1452(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /args.cmd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1452
static inline void alert_rule_1452(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1452;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP args.cmd access", 
                     sizeof("SERVER-WEBAPP args.cmd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1453
// Rule Name: SERVER-WEBAPP AT-generated.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1453
static inline int check_rule_1453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /AT-generated.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1453
static inline void alert_rule_1453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AT-generated.cgi access", 
                     sizeof("SERVER-WEBAPP AT-generated.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1454
// Rule Name: SERVER-WEBAPP wwwwais access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1454
static inline int check_rule_1454(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wwwwais",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1454
static inline void alert_rule_1454(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1454;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP wwwwais access", 
                     sizeof("SERVER-WEBAPP wwwwais access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1455
// Rule Name: SERVER-WEBAPP calendar.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1455
static inline int check_rule_1455(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: calendar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1455
static inline void alert_rule_1455(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1455;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar.pl access", 
                     sizeof("SERVER-WEBAPP calendar.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1456
// Rule Name: SERVER-WEBAPP calender_admin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1456
static inline int check_rule_1456(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calender_admin.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1456
static inline void alert_rule_1456(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1456;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calender_admin.pl access", 
                     sizeof("SERVER-WEBAPP calender_admin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1457
// Rule Name: SERVER-WEBAPP user_update_admin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1457
static inline int check_rule_1457(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /user_update_admin.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1457
static inline void alert_rule_1457(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1457;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP user_update_admin.pl access", 
                     sizeof("SERVER-WEBAPP user_update_admin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1458
// Rule Name: SERVER-WEBAPP user_update_passwd.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1458
static inline int check_rule_1458(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /user_update_passwd.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1458
static inline void alert_rule_1458(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1458;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP user_update_passwd.pl access", 
                     sizeof("SERVER-WEBAPP user_update_passwd.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1459
// Rule Name: SERVER-WEBAPP bb-histlog.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1459
static inline int check_rule_1459(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-histlog.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1459
static inline void alert_rule_1459(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1459;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-histlog.sh access", 
                     sizeof("SERVER-WEBAPP bb-histlog.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1460
// Rule Name: SERVER-WEBAPP bb-histsvc.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1460
static inline int check_rule_1460(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-histsvc.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1460
static inline void alert_rule_1460(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1460;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-histsvc.sh access", 
                     sizeof("SERVER-WEBAPP bb-histsvc.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1461
// Rule Name: SERVER-WEBAPP bb-rep.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1461
static inline int check_rule_1461(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-rep.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1461
static inline void alert_rule_1461(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1461;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-rep.sh access", 
                     sizeof("SERVER-WEBAPP bb-rep.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1462
// Rule Name: SERVER-WEBAPP bb-replog.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1462
static inline int check_rule_1462(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-replog.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1462
static inline void alert_rule_1462(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1462;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-replog.sh access", 
                     sizeof("SERVER-WEBAPP bb-replog.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1463
// Rule Name: POLICY-SOCIAL IRC message
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1463
static inline int check_rule_1463(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: PRIVMSG 
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1463
static inline void alert_rule_1463(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1463;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC message", 
                     sizeof("POLICY-SOCIAL IRC message"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1464
// Rule Name: INDICATOR-COMPROMISE oracle one hour install
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1464
static inline int check_rule_1464(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 8002
    if (tcph->source != htons(8002))
        return 0;
    // Content matching (simplified)
    // Full pattern: Oracle Applications One-Hour Install
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1464
static inline void alert_rule_1464(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1464;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE oracle one hour install", 
                     sizeof("INDICATOR-COMPROMISE oracle one hour install"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1465
// Rule Name: SERVER-WEBAPP auktion.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1465
static inline int check_rule_1465(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /auktion.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1465
static inline void alert_rule_1465(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1465;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP auktion.cgi access", 
                     sizeof("SERVER-WEBAPP auktion.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1466
// Rule Name: SERVER-WEBAPP cgiforum.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1466
static inline int check_rule_1466(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiforum.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1466
static inline void alert_rule_1466(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1466;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiforum.pl access", 
                     sizeof("SERVER-WEBAPP cgiforum.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1467
// Rule Name: SERVER-WEBAPP directorypro.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1467
static inline int check_rule_1467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /directorypro.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1467
static inline void alert_rule_1467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP directorypro.cgi access", 
                     sizeof("SERVER-WEBAPP directorypro.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1468
// Rule Name: SERVER-WEBAPP Web Shopper shopper.cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1468
static inline int check_rule_1468(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/shopper.cgi",fast_pattern,nocase', 'newpage=../",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1468
static inline void alert_rule_1468(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1468;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Web Shopper shopper.cgi attempt", 
                     sizeof("SERVER-WEBAPP Web Shopper shopper.cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1469
// Rule Name: SERVER-WEBAPP Web Shopper shopper.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1469
static inline int check_rule_1469(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /shopper.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1469
static inline void alert_rule_1469(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1469;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Web Shopper shopper.cgi access", 
                     sizeof("SERVER-WEBAPP Web Shopper shopper.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1470
// Rule Name: SERVER-WEBAPP listrec.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1470
static inline int check_rule_1470(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /listrec.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1470
static inline void alert_rule_1470(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1470;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP listrec.pl access", 
                     sizeof("SERVER-WEBAPP listrec.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1471
// Rule Name: SERVER-WEBAPP mailnews.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1471
static inline int check_rule_1471(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mailnews.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1471
static inline void alert_rule_1471(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1471;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mailnews.cgi access", 
                     sizeof("SERVER-WEBAPP mailnews.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1472
// Rule Name: SERVER-WEBAPP book.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1472
static inline int check_rule_1472(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /book.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1472
static inline void alert_rule_1472(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1472;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP book.cgi access", 
                     sizeof("SERVER-WEBAPP book.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1473
// Rule Name: SERVER-WEBAPP newsdesk.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1473
static inline int check_rule_1473(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/newsdesk.cgi",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1473
static inline void alert_rule_1473(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1473;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP newsdesk.cgi access", 
                     sizeof("SERVER-WEBAPP newsdesk.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1474
// Rule Name: SERVER-WEBAPP cal_make.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1474
static inline int check_rule_1474(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cal_make.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1474
static inline void alert_rule_1474(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1474;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cal_make.pl access", 
                     sizeof("SERVER-WEBAPP cal_make.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1475
// Rule Name: SERVER-WEBAPP mailit.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1475
static inline int check_rule_1475(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mailit.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1475
static inline void alert_rule_1475(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1475;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mailit.pl access", 
                     sizeof("SERVER-WEBAPP mailit.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1476
// Rule Name: SERVER-WEBAPP sdbsearch.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1476
static inline int check_rule_1476(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sdbsearch.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1476
static inline void alert_rule_1476(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1476;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sdbsearch.cgi access", 
                     sizeof("SERVER-WEBAPP sdbsearch.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1478
// Rule Name: SERVER-WEBAPP Simple Web Counter URI Parameter Buffer Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1478
static inline int check_rule_1478(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/swc",nocase', 'ctr=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1478
static inline void alert_rule_1478(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1478;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Simple Web Counter URI Parameter Buffer Overflow attempt", 
                     sizeof("SERVER-WEBAPP Simple Web Counter URI Parameter Buffer Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1479
// Rule Name: SERVER-WEBAPP ttawebtop.cgi arbitrary file attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1479
static inline int check_rule_1479(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ttawebtop.cgi",nocase', 'pg=../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1479
static inline void alert_rule_1479(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1479;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ttawebtop.cgi arbitrary file attempt", 
                     sizeof("SERVER-WEBAPP ttawebtop.cgi arbitrary file attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1480
// Rule Name: SERVER-WEBAPP ttawebtop.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1480
static inline int check_rule_1480(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ttawebtop.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1480
static inline void alert_rule_1480(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1480;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ttawebtop.cgi access", 
                     sizeof("SERVER-WEBAPP ttawebtop.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1481
// Rule Name: SERVER-WEBAPP upload.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1481
static inline int check_rule_1481(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /upload.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1481
static inline void alert_rule_1481(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1481;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP upload.cgi access", 
                     sizeof("SERVER-WEBAPP upload.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1482
// Rule Name: SERVER-WEBAPP view_source access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1482
static inline int check_rule_1482(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /view_source",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1482
static inline void alert_rule_1482(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1482;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP view_source access", 
                     sizeof("SERVER-WEBAPP view_source access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1483
// Rule Name: SERVER-WEBAPP ustorekeeper.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1483
static inline int check_rule_1483(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ustorekeeper.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1483
static inline void alert_rule_1483(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1483;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ustorekeeper.pl access", 
                     sizeof("SERVER-WEBAPP ustorekeeper.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1485
// Rule Name: SERVER-IIS mkilog.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1485
static inline int check_rule_1485(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mkilog.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1485
static inline void alert_rule_1485(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1485;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS mkilog.exe access", 
                     sizeof("SERVER-IIS mkilog.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1486
// Rule Name: SERVER-IIS ctss.idc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1486
static inline int check_rule_1486(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ctss.idc",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1486
static inline void alert_rule_1486(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1486;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ctss.idc access", 
                     sizeof("SERVER-IIS ctss.idc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1487
// Rule Name: SERVER-IIS /iisadmpwd/aexp2.htr access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1487
static inline int check_rule_1487(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisadmpwd/aexp2.htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1487
static inline void alert_rule_1487(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1487;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /iisadmpwd/aexp2.htr access", 
                     sizeof("SERVER-IIS /iisadmpwd/aexp2.htr access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1488
// Rule Name: SERVER-WEBAPP store.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1488
static inline int check_rule_1488(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/store.cgi",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1488
static inline void alert_rule_1488(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1488;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP store.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP store.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1489
// Rule Name: SERVER-WEBAPP nobody access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1489
static inline int check_rule_1489(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /~nobody
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1489
static inline void alert_rule_1489(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1489;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nobody access", 
                     sizeof("SERVER-WEBAPP nobody access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1490
// Rule Name: SERVER-WEBAPP Phorum /support/common.php attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1490
static inline int check_rule_1490(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/support/common.php', 'ForumLang=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1490
static inline void alert_rule_1490(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1490;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum /support/common.php attempt", 
                     sizeof("SERVER-WEBAPP Phorum /support/common.php attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1491
// Rule Name: SERVER-WEBAPP Phorum /support/common.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1491
static inline int check_rule_1491(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /support/common.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1491
static inline void alert_rule_1491(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1491;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Phorum /support/common.php access", 
                     sizeof("SERVER-WEBAPP Phorum /support/common.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1492
// Rule Name: SERVER-WEBAPP RBS ISP /newuser  directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1492
static inline int check_rule_1492(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /newuser?Image=../..
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1492
static inline void alert_rule_1492(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1492;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP RBS ISP /newuser  directory traversal attempt", 
                     sizeof("SERVER-WEBAPP RBS ISP /newuser  directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1493
// Rule Name: SERVER-WEBAPP RBS ISP /newuser access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1493
static inline int check_rule_1493(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /newuser
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1493
static inline void alert_rule_1493(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1493;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP RBS ISP /newuser access", 
                     sizeof("SERVER-WEBAPP RBS ISP /newuser access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1494
// Rule Name: SERVER-WEBAPP SIX webboard generate.cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1494
static inline int check_rule_1494(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/generate.cgi', 'content=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1494
static inline void alert_rule_1494(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1494;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SIX webboard generate.cgi attempt", 
                     sizeof("SERVER-WEBAPP SIX webboard generate.cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1495
// Rule Name: SERVER-WEBAPP SIX webboard generate.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1495
static inline int check_rule_1495(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /generate.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1495
static inline void alert_rule_1495(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1495;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SIX webboard generate.cgi access", 
                     sizeof("SERVER-WEBAPP SIX webboard generate.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1496
// Rule Name: SERVER-WEBAPP spin_client.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1496
static inline int check_rule_1496(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /spin_client.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1496
static inline void alert_rule_1496(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1496;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP spin_client.cgi access", 
                     sizeof("SERVER-WEBAPP spin_client.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1499
// Rule Name: SERVER-WEBAPP SiteScope Service access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1499
static inline int check_rule_1499(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8888
    if (tcph->dest != htons(8888))
        return 0;
    // Content matching (simplified)
    // Full pattern: /SiteScope/cgi/go.exe/SiteScope
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1499
static inline void alert_rule_1499(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1499;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SiteScope Service access", 
                     sizeof("SERVER-WEBAPP SiteScope Service access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1500
// Rule Name: SERVER-WEBAPP ExAir access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1500
static inline int check_rule_1500(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /exair/search/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1500
static inline void alert_rule_1500(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1500;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ExAir access", 
                     sizeof("SERVER-WEBAPP ExAir access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1501
// Rule Name: SERVER-WEBAPP a1stats a1disp3.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1501
static inline int check_rule_1501(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/a1disp3.cgi?",fast_pattern,nocase', '/../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1501
static inline void alert_rule_1501(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1501;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP a1stats a1disp3.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP a1stats a1disp3.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1502
// Rule Name: SERVER-WEBAPP a1stats a1disp3.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1502
static inline int check_rule_1502(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /a1disp3.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1502
static inline void alert_rule_1502(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1502;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP a1stats a1disp3.cgi access", 
                     sizeof("SERVER-WEBAPP a1stats a1disp3.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1503
// Rule Name: SERVER-WEBAPP admentor admin.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1503
static inline int check_rule_1503(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admentor/admin/admin.asp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1503
static inline void alert_rule_1503(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1503;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP admentor admin.asp access", 
                     sizeof("SERVER-WEBAPP admentor admin.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1505
// Rule Name: SERVER-WEBAPP alchemy http server PRN arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1505
static inline int check_rule_1505(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/PRN/",fast_pattern', '../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1505
static inline void alert_rule_1505(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1505;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alchemy http server PRN arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP alchemy http server PRN arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1506
// Rule Name: SERVER-WEBAPP alchemy http server NUL arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1506
static inline int check_rule_1506(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/NUL/",fast_pattern', '../../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1506
static inline void alert_rule_1506(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1506;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alchemy http server NUL arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP alchemy http server NUL arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1507
// Rule Name: SERVER-WEBAPP alibaba.pl arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1507
static inline int check_rule_1507(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /alibaba.pl|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1507
static inline void alert_rule_1507(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1507;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alibaba.pl arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP alibaba.pl arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1508
// Rule Name: SERVER-WEBAPP alibaba.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1508
static inline int check_rule_1508(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /alibaba.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1508
static inline void alert_rule_1508(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1508;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alibaba.pl access", 
                     sizeof("SERVER-WEBAPP alibaba.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1509
// Rule Name: SERVER-WEBAPP AltaVista Intranet Search directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1509
static inline int check_rule_1509(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /query?mss=..",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1509
static inline void alert_rule_1509(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1509;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AltaVista Intranet Search directory traversal attempt", 
                     sizeof("SERVER-WEBAPP AltaVista Intranet Search directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1510
// Rule Name: SERVER-WEBAPP test.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1510
static inline int check_rule_1510(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test.bat|7C|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1510
static inline void alert_rule_1510(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1510;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP test.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1511
// Rule Name: SERVER-WEBAPP test.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1511
static inline int check_rule_1511(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1511
static inline void alert_rule_1511(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1511;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test.bat access", 
                     sizeof("SERVER-WEBAPP test.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1512
// Rule Name: SERVER-WEBAPP input.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1512
static inline int check_rule_1512(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /input.bat|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1512
static inline void alert_rule_1512(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1512;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP input.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP input.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1513
// Rule Name: SERVER-WEBAPP input.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1513
static inline int check_rule_1513(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /input.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1513
static inline void alert_rule_1513(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1513;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP input.bat access", 
                     sizeof("SERVER-WEBAPP input.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1514
// Rule Name: SERVER-WEBAPP input2.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1514
static inline int check_rule_1514(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /input2.bat|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1514
static inline void alert_rule_1514(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1514;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP input2.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP input2.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1515
// Rule Name: SERVER-WEBAPP input2.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1515
static inline int check_rule_1515(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /input2.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1515
static inline void alert_rule_1515(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1515;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP input2.bat access", 
                     sizeof("SERVER-WEBAPP input2.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1516
// Rule Name: SERVER-WEBAPP envout.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1516
static inline int check_rule_1516(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /envout.bat|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1516
static inline void alert_rule_1516(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1516;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP envout.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP envout.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1517
// Rule Name: SERVER-WEBAPP envout.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1517
static inline int check_rule_1517(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /envout.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1517
static inline void alert_rule_1517(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1517;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP envout.bat access", 
                     sizeof("SERVER-WEBAPP envout.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1518
// Rule Name: SERVER-WEBAPP nstelemetry.adp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1518
static inline int check_rule_1518(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8000
    if (tcph->dest != htons(8000))
        return 0;
    // Content matching (simplified)
    // Full pattern: /nstelemetry.adp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1518
static inline void alert_rule_1518(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1518;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nstelemetry.adp access", 
                     sizeof("SERVER-WEBAPP nstelemetry.adp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1519
// Rule Name: SERVER-WEBAPP apache ?M=D directory list attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1519
static inline int check_rule_1519(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /?M=D
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1519
static inline void alert_rule_1519(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1519;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP apache ?M=D directory list attempt", 
                     sizeof("SERVER-WEBAPP apache ?M=D directory list attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1520
// Rule Name: SERVER-WEBAPP server-info access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1520
static inline int check_rule_1520(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /server-info",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1520
static inline void alert_rule_1520(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1520;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP server-info access", 
                     sizeof("SERVER-WEBAPP server-info access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1521
// Rule Name: SERVER-WEBAPP server-status access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1521
static inline int check_rule_1521(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /server-status
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1521
static inline void alert_rule_1521(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1521;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP server-status access", 
                     sizeof("SERVER-WEBAPP server-status access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1522
// Rule Name: SERVER-WEBAPP ans.pl attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1522
static inline int check_rule_1522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ans.pl?",nocase', 'p=../../",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1522
static inline void alert_rule_1522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ans.pl attempt", 
                     sizeof("SERVER-WEBAPP ans.pl attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1523
// Rule Name: SERVER-WEBAPP ans.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1523
static inline int check_rule_1523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ans.pl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1523
static inline void alert_rule_1523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ans.pl access", 
                     sizeof("SERVER-WEBAPP ans.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1524
// Rule Name: SERVER-WEBAPP Axis Storpoint CD attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1524
static inline int check_rule_1524(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cd/../config/html/cnf_gi.htm
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1524
static inline void alert_rule_1524(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1524;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Axis Storpoint CD attempt", 
                     sizeof("SERVER-WEBAPP Axis Storpoint CD attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1525
// Rule Name: SERVER-WEBAPP Axis Storpoint CD access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1525
static inline int check_rule_1525(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /config/html/cnf_gi.htm
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1525
static inline void alert_rule_1525(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1525;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Axis Storpoint CD access", 
                     sizeof("SERVER-WEBAPP Axis Storpoint CD access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1526
// Rule Name: SERVER-WEBAPP basilix sendmail.inc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1526
static inline int check_rule_1526(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /inc/sendmail.inc
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1526
static inline void alert_rule_1526(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1526;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP basilix sendmail.inc access", 
                     sizeof("SERVER-WEBAPP basilix sendmail.inc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1527
// Rule Name: SERVER-WEBAPP basilix mysql.class access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1527
static inline int check_rule_1527(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /class/mysql.class
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1527
static inline void alert_rule_1527(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1527;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP basilix mysql.class access", 
                     sizeof("SERVER-WEBAPP basilix mysql.class access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1528
// Rule Name: SERVER-WEBAPP BBoard access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1528
static inline int check_rule_1528(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /servlet/sunexamples.BBoardServlet
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1528
static inline void alert_rule_1528(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1528;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BBoard access", 
                     sizeof("SERVER-WEBAPP BBoard access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1529
// Rule Name: PROTOCOL-FTP SITE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1529
static inline int check_rule_1529(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: SITE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1529
static inline void alert_rule_1529(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1529;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1531
// Rule Name: SERVER-WEBAPP bb-hist.sh attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1531
static inline int check_rule_1531(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bb-hist.sh?",nocase', 'HISTFILE=../..",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1531
static inline void alert_rule_1531(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1531;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-hist.sh attempt", 
                     sizeof("SERVER-WEBAPP bb-hist.sh attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1532
// Rule Name: SERVER-WEBAPP bb-hostscv.sh attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1532
static inline int check_rule_1532(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bb-hostsvc.sh?",fast_pattern,nocase', 'HOSTSVC",nocase', '../..",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1532
static inline void alert_rule_1532(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1532;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-hostscv.sh attempt", 
                     sizeof("SERVER-WEBAPP bb-hostscv.sh attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1533
// Rule Name: SERVER-WEBAPP bb-hostscv.sh access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1533
static inline int check_rule_1533(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb-hostsvc.sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1533
static inline void alert_rule_1533(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1533;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb-hostscv.sh access", 
                     sizeof("SERVER-WEBAPP bb-hostscv.sh access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1534
// Rule Name: SERVER-WEBAPP agora.cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1534
static inline int check_rule_1534(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/store/agora.cgi?",nocase', 'cart_id=<SCRIPT>",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1534
static inline void alert_rule_1534(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1534;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP agora.cgi attempt", 
                     sizeof("SERVER-WEBAPP agora.cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1535
// Rule Name: SERVER-WEBAPP bizdbsearch access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1535
static inline int check_rule_1535(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bizdb1-search.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1535
static inline void alert_rule_1535(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1535;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bizdbsearch access", 
                     sizeof("SERVER-WEBAPP bizdbsearch access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1536
// Rule Name: SERVER-WEBAPP calendar_admin.pl arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1536
static inline int check_rule_1536(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/calendar_admin.pl?",nocase', 'config=|7C|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1536
static inline void alert_rule_1536(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1536;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar_admin.pl arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP calendar_admin.pl arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1537
// Rule Name: SERVER-WEBAPP calendar_admin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1537
static inline int check_rule_1537(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calendar_admin.pl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1537
static inline void alert_rule_1537(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1537;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar_admin.pl access", 
                     sizeof("SERVER-WEBAPP calendar_admin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1538
// Rule Name: PROTOCOL-NNTP AUTHINFO USER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1538
static inline int check_rule_1538(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['AUTHINFO",nocase', 'USER",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1538
static inline void alert_rule_1538(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1538;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP AUTHINFO USER overflow attempt", 
                     sizeof("PROTOCOL-NNTP AUTHINFO USER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1539
// Rule Name: SERVER-WEBAPP /cgi-bin/ls access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1539
static inline int check_rule_1539(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/ls",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1539
static inline void alert_rule_1539(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1539;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /cgi-bin/ls access", 
                     sizeof("SERVER-WEBAPP /cgi-bin/ls access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1540
// Rule Name: SERVER-OTHER Adobe Coldfusion ?Mode=debug attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1540
static inline int check_rule_1540(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Mode=debug",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1540
static inline void alert_rule_1540(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1540;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion ?Mode=debug attempt", 
                     sizeof("SERVER-OTHER Adobe Coldfusion ?Mode=debug attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1541
// Rule Name: PROTOCOL-FINGER version query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1541
static inline int check_rule_1541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: version
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1541
static inline void alert_rule_1541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER version query", 
                     sizeof("PROTOCOL-FINGER version query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1542
// Rule Name: SERVER-WEBAPP cgimail access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1542
static inline int check_rule_1542(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgimail",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1542
static inline void alert_rule_1542(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1542;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgimail access", 
                     sizeof("SERVER-WEBAPP cgimail access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1543
// Rule Name: SERVER-WEBAPP cgiwrap access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1543
static inline int check_rule_1543(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiwrap",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1543
static inline void alert_rule_1543(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1543;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiwrap access", 
                     sizeof("SERVER-WEBAPP cgiwrap access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1544
// Rule Name: SERVER-WEBAPP Cisco Catalyst command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1544
static inline int check_rule_1544(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /exec/show/config/cr",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1544
static inline void alert_rule_1544(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1544;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco Catalyst command execution attempt", 
                     sizeof("SERVER-WEBAPP Cisco Catalyst command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1545
// Rule Name: SERVER-OTHER Cisco denial of service attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1545
static inline int check_rule_1545(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |13|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1545
static inline void alert_rule_1545(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1545;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Cisco denial of service attempt", 
                     sizeof("SERVER-OTHER Cisco denial of service attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1546
// Rule Name: SERVER-WEBAPP Cisco HTTP double-percent DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1546
static inline int check_rule_1546(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /%%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1546
static inline void alert_rule_1546(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1546;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco HTTP double-percent DOS attempt", 
                     sizeof("SERVER-WEBAPP Cisco HTTP double-percent DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1547
// Rule Name: SERVER-WEBAPP csSearch.cgi arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1547
static inline int check_rule_1547(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/csSearch.cgi', 'setup=', '`', '`",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1547
static inline void alert_rule_1547(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1547;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP csSearch.cgi arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP csSearch.cgi arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1548
// Rule Name: SERVER-WEBAPP csSearch.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1548
static inline int check_rule_1548(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /csSearch.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1548
static inline void alert_rule_1548(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1548;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP csSearch.cgi access", 
                     sizeof("SERVER-WEBAPP csSearch.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1549
// Rule Name: SERVER-MAIL HELO overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1549
static inline int check_rule_1549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: HELO",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1549
static inline void alert_rule_1549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL HELO overflow attempt", 
                     sizeof("SERVER-MAIL HELO overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1550
// Rule Name: SERVER-MAIL ETRN overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1550
static inline int check_rule_1550(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ETRN",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1550
static inline void alert_rule_1550(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1550;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL ETRN overflow attempt", 
                     sizeof("SERVER-MAIL ETRN overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1551
// Rule Name: SERVER-WEBAPP /CVS/Entries access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1551
static inline int check_rule_1551(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /CVS/Entries",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1551
static inline void alert_rule_1551(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1551;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /CVS/Entries access", 
                     sizeof("SERVER-WEBAPP /CVS/Entries access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1552
// Rule Name: SERVER-WEBAPP cvsweb version access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1552
static inline int check_rule_1552(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cvsweb/version
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1552
static inline void alert_rule_1552(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1552;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cvsweb version access", 
                     sizeof("SERVER-WEBAPP cvsweb version access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1554
// Rule Name: SERVER-WEBAPP dbman db.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1554
static inline int check_rule_1554(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dbman/db.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1554
static inline void alert_rule_1554(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1554;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dbman db.cgi access", 
                     sizeof("SERVER-WEBAPP dbman db.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1555
// Rule Name: SERVER-WEBAPP DCShop access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1555
static inline int check_rule_1555(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dcshop",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1555
static inline void alert_rule_1555(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1555;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DCShop access", 
                     sizeof("SERVER-WEBAPP DCShop access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1556
// Rule Name: SERVER-WEBAPP DCShop orders.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1556
static inline int check_rule_1556(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /orders/orders.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1556
static inline void alert_rule_1556(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1556;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DCShop orders.txt access", 
                     sizeof("SERVER-WEBAPP DCShop orders.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1557
// Rule Name: SERVER-WEBAPP DCShop auth_user_file.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1557
static inline int check_rule_1557(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /auth_data/auth_user_file.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1557
static inline void alert_rule_1557(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1557;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DCShop auth_user_file.txt access", 
                     sizeof("SERVER-WEBAPP DCShop auth_user_file.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1558
// Rule Name: SERVER-WEBAPP Delegate whois overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1558
static inline int check_rule_1558(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: whois|3A|//",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1558
static inline void alert_rule_1558(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1558;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Delegate whois overflow attempt", 
                     sizeof("SERVER-WEBAPP Delegate whois overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1559
// Rule Name: SERVER-WEBAPP /doc/packages access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1559
static inline int check_rule_1559(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /doc/packages",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1559
static inline void alert_rule_1559(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1559;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /doc/packages access", 
                     sizeof("SERVER-WEBAPP /doc/packages access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1560
// Rule Name: SERVER-WEBAPP /doc/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1560
static inline int check_rule_1560(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /doc/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1560
static inline void alert_rule_1560(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1560;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /doc/ access", 
                     sizeof("SERVER-WEBAPP /doc/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1562
// Rule Name: PROTOCOL-FTP SITE CHOWN overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1562
static inline int check_rule_1562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'CHOWN",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1562
static inline void alert_rule_1562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE CHOWN overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE CHOWN overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1563
// Rule Name: SERVER-WEBAPP login.htm attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1563
static inline int check_rule_1563(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/login.htm?",nocase', 'password=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1563
static inline void alert_rule_1563(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1563;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP login.htm attempt", 
                     sizeof("SERVER-WEBAPP login.htm attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1564
// Rule Name: SERVER-WEBAPP login.htm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1564
static inline int check_rule_1564(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /login.htm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1564
static inline void alert_rule_1564(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1564;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP login.htm access", 
                     sizeof("SERVER-WEBAPP login.htm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1565
// Rule Name: SERVER-WEBAPP eshop.pl arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1565
static inline int check_rule_1565(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/eshop.pl?",nocase', 'seite=|3B|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1565
static inline void alert_rule_1565(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1565;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP eshop.pl arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP eshop.pl arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1566
// Rule Name: SERVER-WEBAPP eshop.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1566
static inline int check_rule_1566(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /eshop.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1566
static inline void alert_rule_1566(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1566;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP eshop.pl access", 
                     sizeof("SERVER-WEBAPP eshop.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1567
// Rule Name: SERVER-IIS /exchange/root.asp attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1567
static inline int check_rule_1567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/exchange/root.asp?",nocase', 'acs=anon",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1567
static inline void alert_rule_1567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /exchange/root.asp attempt", 
                     sizeof("SERVER-IIS /exchange/root.asp attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1568
// Rule Name: SERVER-IIS /exchange/root.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1568
static inline int check_rule_1568(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /exchange/root.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1568
static inline void alert_rule_1568(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1568;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /exchange/root.asp access", 
                     sizeof("SERVER-IIS /exchange/root.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1569
// Rule Name: SERVER-WEBAPP loadpage.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1569
static inline int check_rule_1569(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/loadpage.cgi', 'file=../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1569
static inline void alert_rule_1569(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1569;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP loadpage.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP loadpage.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1570
// Rule Name: SERVER-WEBAPP loadpage.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1570
static inline int check_rule_1570(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /loadpage.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1570
static inline void alert_rule_1570(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1570;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP loadpage.cgi access", 
                     sizeof("SERVER-WEBAPP loadpage.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1571
// Rule Name: SERVER-WEBAPP dcforum.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1571
static inline int check_rule_1571(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dcforum.cgi', 'forum=../..']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1571
static inline void alert_rule_1571(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1571;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dcforum.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP dcforum.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1572
// Rule Name: SERVER-WEBAPP commerce.cgi arbitrary file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1572
static inline int check_rule_1572(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/commerce.cgi', 'page=', '/../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1572
static inline void alert_rule_1572(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1572;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP commerce.cgi arbitrary file access attempt", 
                     sizeof("SERVER-WEBAPP commerce.cgi arbitrary file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1573
// Rule Name: SERVER-WEBAPP cgiforum.pl attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1573
static inline int check_rule_1573(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgiforum.pl?",nocase', 'thesection=../..",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1573
static inline void alert_rule_1573(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1573;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiforum.pl attempt", 
                     sizeof("SERVER-WEBAPP cgiforum.pl attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1574
// Rule Name: SERVER-WEBAPP directorypro.cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1574
static inline int check_rule_1574(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/directorypro.cgi', 'show=', '../..",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1574
static inline void alert_rule_1574(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1574;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP directorypro.cgi attempt", 
                     sizeof("SERVER-WEBAPP directorypro.cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1575
// Rule Name: SERVER-WEBAPP Domino mab.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1575
static inline int check_rule_1575(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mab.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1575
static inline void alert_rule_1575(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1575;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino mab.nsf access", 
                     sizeof("SERVER-WEBAPP Domino mab.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1576
// Rule Name: SERVER-WEBAPP Domino cersvr.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1576
static inline int check_rule_1576(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cersvr.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1576
static inline void alert_rule_1576(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1576;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino cersvr.nsf access", 
                     sizeof("SERVER-WEBAPP Domino cersvr.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1577
// Rule Name: SERVER-WEBAPP Domino setup.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1577
static inline int check_rule_1577(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /setup.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1577
static inline void alert_rule_1577(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1577;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino setup.nsf access", 
                     sizeof("SERVER-WEBAPP Domino setup.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1578
// Rule Name: SERVER-WEBAPP Domino statrep.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1578
static inline int check_rule_1578(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /statrep.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1578
static inline void alert_rule_1578(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1578;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino statrep.nsf access", 
                     sizeof("SERVER-WEBAPP Domino statrep.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1579
// Rule Name: SERVER-WEBAPP Domino webadmin.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1579
static inline int check_rule_1579(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webadmin.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1579
static inline void alert_rule_1579(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1579;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino webadmin.nsf access", 
                     sizeof("SERVER-WEBAPP Domino webadmin.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1580
// Rule Name: SERVER-WEBAPP Domino events4.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1580
static inline int check_rule_1580(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /events4.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1580
static inline void alert_rule_1580(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1580;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino events4.nsf access", 
                     sizeof("SERVER-WEBAPP Domino events4.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1581
// Rule Name: SERVER-WEBAPP Domino ntsync4.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1581
static inline int check_rule_1581(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ntsync4.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1581
static inline void alert_rule_1581(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1581;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino ntsync4.nsf access", 
                     sizeof("SERVER-WEBAPP Domino ntsync4.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1582
// Rule Name: SERVER-WEBAPP Domino collect4.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1582
static inline int check_rule_1582(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /collect4.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1582
static inline void alert_rule_1582(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1582;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino collect4.nsf access", 
                     sizeof("SERVER-WEBAPP Domino collect4.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1583
// Rule Name: SERVER-WEBAPP Domino mailw46.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1583
static inline int check_rule_1583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mailw46.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1583
static inline void alert_rule_1583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino mailw46.nsf access", 
                     sizeof("SERVER-WEBAPP Domino mailw46.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1584
// Rule Name: SERVER-WEBAPP Domino bookmark.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1584
static inline int check_rule_1584(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bookmark.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1584
static inline void alert_rule_1584(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1584;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino bookmark.nsf access", 
                     sizeof("SERVER-WEBAPP Domino bookmark.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1585
// Rule Name: SERVER-WEBAPP Domino agentrunner.nsf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1585
static inline int check_rule_1585(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /agentrunner.nsf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1585
static inline void alert_rule_1585(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1585;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino agentrunner.nsf access", 
                     sizeof("SERVER-WEBAPP Domino agentrunner.nsf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1586
// Rule Name: SERVER-WEBAPP Domino mail.box access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1586
static inline int check_rule_1586(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mail.box",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1586
static inline void alert_rule_1586(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1586;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Domino mail.box access", 
                     sizeof("SERVER-WEBAPP Domino mail.box access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1587
// Rule Name: SERVER-WEBAPP cgitest.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1587
static inline int check_rule_1587(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgitest.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1587
static inline void alert_rule_1587(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1587;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgitest.exe access", 
                     sizeof("SERVER-WEBAPP cgitest.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1588
// Rule Name: SERVER-WEBAPP SalesLogix Eviewer access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1588
static inline int check_rule_1588(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /slxweb.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1588
static inline void alert_rule_1588(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1588;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SalesLogix Eviewer access", 
                     sizeof("SERVER-WEBAPP SalesLogix Eviewer access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1589
// Rule Name: SERVER-WEBAPP musicat empower attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1589
static inline int check_rule_1589(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /empower?DB=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1589
static inline void alert_rule_1589(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1589;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP musicat empower attempt", 
                     sizeof("SERVER-WEBAPP musicat empower attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1590
// Rule Name: SERVER-WEBAPP faqmanager.cgi arbitrary file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1590
static inline int check_rule_1590(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/faqmanager.cgi?",nocase', 'toc=",distance 0,nocase', '|00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1590
static inline void alert_rule_1590(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1590;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP faqmanager.cgi arbitrary file access attempt", 
                     sizeof("SERVER-WEBAPP faqmanager.cgi arbitrary file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1591
// Rule Name: SERVER-WEBAPP faqmanager.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1591
static inline int check_rule_1591(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /faqmanager.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1591
static inline void alert_rule_1591(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1591;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP faqmanager.cgi access", 
                     sizeof("SERVER-WEBAPP faqmanager.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1592
// Rule Name: SERVER-WEBAPP /fcgi-bin/echo.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1592
static inline int check_rule_1592(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fcgi-bin/echo.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1592
static inline void alert_rule_1592(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1592;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /fcgi-bin/echo.exe access", 
                     sizeof("SERVER-WEBAPP /fcgi-bin/echo.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1593
// Rule Name: SERVER-WEBAPP FormHandler.cgi external site redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1593
static inline int check_rule_1593(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/FormHandler.cgi",fast_pattern,nocase', 'redirect=http']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1593
static inline void alert_rule_1593(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1593;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP FormHandler.cgi external site redirection attempt", 
                     sizeof("SERVER-WEBAPP FormHandler.cgi external site redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1594
// Rule Name: SERVER-WEBAPP FormHandler.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1594
static inline int check_rule_1594(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /FormHandler.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1594
static inline void alert_rule_1594(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1594;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP FormHandler.cgi access", 
                     sizeof("SERVER-WEBAPP FormHandler.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1595
// Rule Name: SERVER-IIS htimage.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1595
static inline int check_rule_1595(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htimage.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1595
static inline void alert_rule_1595(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1595;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS htimage.exe access", 
                     sizeof("SERVER-IIS htimage.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1597
// Rule Name: SERVER-WEBAPP guestbook.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1597
static inline int check_rule_1597(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /guestbook.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1597
static inline void alert_rule_1597(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1597;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP guestbook.cgi access", 
                     sizeof("SERVER-WEBAPP guestbook.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1598
// Rule Name: SERVER-WEBAPP Home Free search.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1598
static inline int check_rule_1598(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.cgi', 'letter=../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1598
static inline void alert_rule_1598(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1598;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Home Free search.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Home Free search.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1599
// Rule Name: SERVER-WEBAPP search.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1599
static inline int check_rule_1599(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /search.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1599
static inline void alert_rule_1599(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1599;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP search.cgi access", 
                     sizeof("SERVER-WEBAPP search.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1600
// Rule Name: SERVER-WEBAPP htsearch arbitrary configuration file attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1600
static inline int check_rule_1600(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htsearch?-c",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1600
static inline void alert_rule_1600(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1600;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htsearch arbitrary configuration file attempt", 
                     sizeof("SERVER-WEBAPP htsearch arbitrary configuration file attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1601
// Rule Name: SERVER-WEBAPP htsearch arbitrary file read attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1601
static inline int check_rule_1601(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htsearch?exclude=`",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1601
static inline void alert_rule_1601(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1601;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htsearch arbitrary file read attempt", 
                     sizeof("SERVER-WEBAPP htsearch arbitrary file read attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1602
// Rule Name: SERVER-WEBAPP htsearch access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1602
static inline int check_rule_1602(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htsearch",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1602
static inline void alert_rule_1602(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1602;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htsearch access", 
                     sizeof("SERVER-WEBAPP htsearch access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1603
// Rule Name: SERVER-WEBAPP DELETE attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1603
static inline int check_rule_1603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: DELETE ",depth 7,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1603
static inline void alert_rule_1603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DELETE attempt", 
                     sizeof("SERVER-WEBAPP DELETE attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1604
// Rule Name: SERVER-WEBAPP iChat directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1604
static inline int check_rule_1604(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4080
    if (tcph->dest != htons(4080))
        return 0;
    // Content matching (simplified)
    // Full pattern: /../../
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1604
static inline void alert_rule_1604(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1604;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP iChat directory traversal attempt", 
                     sizeof("SERVER-WEBAPP iChat directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1605
// Rule Name: SERVER-OTHER iParty DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1605
static inline int check_rule_1605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6004
    if (tcph->dest != htons(6004))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FF FF FF FF FF|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1605
static inline void alert_rule_1605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER iParty DOS attempt", 
                     sizeof("SERVER-OTHER iParty DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1606
// Rule Name: SERVER-WEBAPP icat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1606
static inline int check_rule_1606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /icat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1606
static inline void alert_rule_1606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP icat access", 
                     sizeof("SERVER-WEBAPP icat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1607
// Rule Name: SERVER-WEBAPP HyperSeek hsx.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1607
static inline int check_rule_1607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /hsx.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1607
static inline void alert_rule_1607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP HyperSeek hsx.cgi access", 
                     sizeof("SERVER-WEBAPP HyperSeek hsx.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1608
// Rule Name: SERVER-WEBAPP htmlscript attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1608
static inline int check_rule_1608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /htmlscript?../..",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1608
static inline void alert_rule_1608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htmlscript attempt", 
                     sizeof("SERVER-WEBAPP htmlscript attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1610
// Rule Name: SERVER-WEBAPP formmail arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1610
static inline int check_rule_1610(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/formmail",fast_pattern,nocase', '%0a",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1610
static inline void alert_rule_1610(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1610;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP formmail arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP formmail arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1611
// Rule Name: SERVER-WEBAPP eXtropia webstore access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1611
static inline int check_rule_1611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /web_store.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1611
static inline void alert_rule_1611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP eXtropia webstore access", 
                     sizeof("SERVER-WEBAPP eXtropia webstore access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1612
// Rule Name: SERVER-WEBAPP ftp.pl attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1612
static inline int check_rule_1612(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ftp.pl?",nocase', 'dir=../..",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1612
static inline void alert_rule_1612(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1612;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ftp.pl attempt", 
                     sizeof("SERVER-WEBAPP ftp.pl attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1613
// Rule Name: SERVER-WEBAPP handler attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1613
static inline int check_rule_1613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/handler', '|7C|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1613
static inline void alert_rule_1613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP handler attempt", 
                     sizeof("SERVER-WEBAPP handler attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1614
// Rule Name: SERVER-WEBAPP Novell Groupwise gwweb.exe attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1614
static inline int check_rule_1614(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/GWWEB.EXE?",nocase', 'HELP=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1614
static inline void alert_rule_1614(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1614;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Novell Groupwise gwweb.exe attempt", 
                     sizeof("SERVER-WEBAPP Novell Groupwise gwweb.exe attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1615
// Rule Name: SERVER-WEBAPP htgrep attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1615
static inline int check_rule_1615(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/htgrep', 'hdr=/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1615
static inline void alert_rule_1615(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1615;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP htgrep attempt", 
                     sizeof("SERVER-WEBAPP htgrep attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1617
// Rule Name: SERVER-WEBAPP Bugzilla doeditvotes.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1617
static inline int check_rule_1617(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /doeditvotes.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1617
static inline void alert_rule_1617(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1617;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Bugzilla doeditvotes.cgi access", 
                     sizeof("SERVER-WEBAPP Bugzilla doeditvotes.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1618
// Rule Name: SERVER-IIS .asp chunked Transfer-Encoding
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1618
static inline int check_rule_1618(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.asp",nocase', 'Transfer-Encoding|3A|",nocase', 'chunked",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1618
static inline void alert_rule_1618(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1618;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .asp chunked Transfer-Encoding", 
                     sizeof("SERVER-IIS .asp chunked Transfer-Encoding"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1621
// Rule Name: PROTOCOL-FTP CMD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1621
static inline int check_rule_1621(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: CMD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1621
static inline void alert_rule_1621(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1621;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CMD overflow attempt", 
                     sizeof("PROTOCOL-FTP CMD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1622
// Rule Name: PROTOCOL-FTP RNFR ././ attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1622
static inline int check_rule_1622(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['RNFR ",fast_pattern,nocase', ' ././']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1622
static inline void alert_rule_1622(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1622;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RNFR ././ attempt", 
                     sizeof("PROTOCOL-FTP RNFR ././ attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1623
// Rule Name: PROTOCOL-FTP invalid MODE
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1623
static inline int check_rule_1623(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MODE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1623
static inline void alert_rule_1623(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1623;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP invalid MODE", 
                     sizeof("PROTOCOL-FTP invalid MODE"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1624
// Rule Name: PROTOCOL-FTP PWD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1624
static inline int check_rule_1624(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PWD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1624
static inline void alert_rule_1624(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1624;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP PWD overflow attempt", 
                     sizeof("PROTOCOL-FTP PWD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1625
// Rule Name: PROTOCOL-FTP SYST overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1625
static inline int check_rule_1625(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: SYST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1625
static inline void alert_rule_1625(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1625;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SYST overflow attempt", 
                     sizeof("PROTOCOL-FTP SYST overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1626
// Rule Name: SERVER-IIS /StoreCSVS/InstantOrder.asmx request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1626
static inline int check_rule_1626(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /StoreCSVS/InstantOrder.asmx",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1626
static inline void alert_rule_1626(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1626;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /StoreCSVS/InstantOrder.asmx request", 
                     sizeof("SERVER-IIS /StoreCSVS/InstantOrder.asmx request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1628
// Rule Name: SERVER-WEBAPP FormHandler.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1628
static inline int check_rule_1628(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/FormHandler.cgi",nocase', 'reply_message_attach=",fast_pattern,nocase', '/../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1628
static inline void alert_rule_1628(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1628;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP FormHandler.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP FormHandler.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1634
// Rule Name: PROTOCOL-POP PASS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1634
static inline int check_rule_1634(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1634
static inline void alert_rule_1634(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1634;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP PASS overflow attempt", 
                     sizeof("PROTOCOL-POP PASS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1635
// Rule Name: PROTOCOL-POP APOP overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1635
static inline int check_rule_1635(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: APOP",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1635
static inline void alert_rule_1635(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1635;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP APOP overflow attempt", 
                     sizeof("PROTOCOL-POP APOP overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1636
// Rule Name: SERVER-OTHER Xtramail Username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1636
static inline int check_rule_1636(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 32000
    if (tcph->dest != htons(32000))
        return 0;
    // Content matching (simplified)
    // Full pattern: Username|3A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1636
static inline void alert_rule_1636(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1636;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Xtramail Username overflow attempt", 
                     sizeof("SERVER-OTHER Xtramail Username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1637
// Rule Name: SERVER-WEBAPP yabb access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1637
static inline int check_rule_1637(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /YaBB",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1637
static inline void alert_rule_1637(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1637;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP yabb access", 
                     sizeof("SERVER-WEBAPP yabb access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1638
// Rule Name: INDICATOR-SCAN SSH Version map attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: network-scan
// Generated from Snort rule parser output

// Check function for rule 1638
static inline int check_rule_1638(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 22
    if (tcph->dest != htons(22))
        return 0;
    // Content matching (simplified)
    // Full pattern: Version_Mapper",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1638
static inline void alert_rule_1638(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1638;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SCAN SSH Version map attempt", 
                     sizeof("INDICATOR-SCAN SSH Version map attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1639
// Rule Name: POLICY-SOCIAL IRC DCC file transfer request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1639
static inline int check_rule_1639(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PRIVMSG ",nocase', ' |3A|.DCC SEND",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1639
static inline void alert_rule_1639(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1639;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC DCC file transfer request", 
                     sizeof("POLICY-SOCIAL IRC DCC file transfer request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1640
// Rule Name: POLICY-SOCIAL IRC DCC chat request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1640
static inline int check_rule_1640(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PRIVMSG ",nocase', ' |3A|.DCC CHAT chat",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1640
static inline void alert_rule_1640(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1640;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC DCC chat request", 
                     sizeof("POLICY-SOCIAL IRC DCC chat request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1641
// Rule Name: SERVER-OTHER DB2 dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: denial-of-service
// Generated from Snort rule parser output

// Check function for rule 1641
static inline int check_rule_1641(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 6789-6790
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6789 || dst_port > 6790)
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 1641
static inline void alert_rule_1641(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1641;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER DB2 dos attempt", 
                     sizeof("SERVER-OTHER DB2 dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1642
// Rule Name: SERVER-WEBAPP document.d2w access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1642
static inline int check_rule_1642(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /document.d2w",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1642
static inline void alert_rule_1642(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1642;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP document.d2w access", 
                     sizeof("SERVER-WEBAPP document.d2w access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1643
// Rule Name: SERVER-WEBAPP db2www access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1643
static inline int check_rule_1643(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /db2www",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1643
static inline void alert_rule_1643(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1643;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP db2www access", 
                     sizeof("SERVER-WEBAPP db2www access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1644
// Rule Name: SERVER-WEBAPP test-cgi attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1644
static inline int check_rule_1644(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test-cgi/*?*",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1644
static inline void alert_rule_1644(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1644;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test-cgi attempt", 
                     sizeof("SERVER-WEBAPP test-cgi attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1645
// Rule Name: SERVER-WEBAPP testcgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1645
static inline int check_rule_1645(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /testcgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1645
static inline void alert_rule_1645(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1645;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP testcgi access", 
                     sizeof("SERVER-WEBAPP testcgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1646
// Rule Name: SERVER-WEBAPP test.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1646
static inline int check_rule_1646(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1646
static inline void alert_rule_1646(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1646;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test.cgi access", 
                     sizeof("SERVER-WEBAPP test.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1648
// Rule Name: SERVER-WEBAPP perl.exe command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1648
static inline int check_rule_1648(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /perl.exe?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1648
static inline void alert_rule_1648(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1648;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP perl.exe command attempt", 
                     sizeof("SERVER-WEBAPP perl.exe command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1649
// Rule Name: SERVER-WEBAPP perl command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1649
static inline int check_rule_1649(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /perl?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1649
static inline void alert_rule_1649(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1649;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP perl command attempt", 
                     sizeof("SERVER-WEBAPP perl command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1650
// Rule Name: SERVER-WEBAPP tst.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1650
static inline int check_rule_1650(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /tst.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1650
static inline void alert_rule_1650(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1650;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP tst.bat access", 
                     sizeof("SERVER-WEBAPP tst.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1651
// Rule Name: SERVER-WEBAPP environ.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1651
static inline int check_rule_1651(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /environ.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1651
static inline void alert_rule_1651(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1651;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP environ.pl access", 
                     sizeof("SERVER-WEBAPP environ.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1652
// Rule Name: SERVER-WEBAPP campas attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1652
static inline int check_rule_1652(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /campas?|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1652
static inline void alert_rule_1652(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1652;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP campas attempt", 
                     sizeof("SERVER-WEBAPP campas attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1654
// Rule Name: SERVER-WEBAPP cart32.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1654
static inline int check_rule_1654(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cart32.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1654
static inline void alert_rule_1654(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1654;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cart32.exe access", 
                     sizeof("SERVER-WEBAPP cart32.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1655
// Rule Name: SERVER-WEBAPP pfdispaly.cgi arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1655
static inline int check_rule_1655(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/pfdispaly.cgi?",nocase', '\'",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1655
static inline void alert_rule_1655(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1655;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pfdispaly.cgi arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP pfdispaly.cgi arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1656
// Rule Name: SERVER-WEBAPP pfdispaly.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1656
static inline int check_rule_1656(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pfdispaly.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1656
static inline void alert_rule_1656(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1656;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pfdispaly.cgi access", 
                     sizeof("SERVER-WEBAPP pfdispaly.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1657
// Rule Name: SERVER-WEBAPP pagelog.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1657
static inline int check_rule_1657(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/pagelog.cgi",nocase', 'name=../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1657
static inline void alert_rule_1657(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1657;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pagelog.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP pagelog.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1658
// Rule Name: SERVER-WEBAPP pagelog.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1658
static inline int check_rule_1658(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pagelog.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1658
static inline void alert_rule_1658(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1658;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pagelog.cgi access", 
                     sizeof("SERVER-WEBAPP pagelog.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1659
// Rule Name: SERVER-OTHER Adobe Coldfusion sendmail.cfm access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1659
static inline int check_rule_1659(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sendmail.cfm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1659
static inline void alert_rule_1659(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1659;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Adobe Coldfusion sendmail.cfm access", 
                     sizeof("SERVER-OTHER Adobe Coldfusion sendmail.cfm access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1660
// Rule Name: SERVER-IIS trace.axd access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1660
static inline int check_rule_1660(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /trace.axd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1660
static inline void alert_rule_1660(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1660;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS trace.axd access", 
                     sizeof("SERVER-IIS trace.axd access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1661
// Rule Name: SERVER-IIS cmd32.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1661
static inline int check_rule_1661(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: cmd32.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1661
static inline void alert_rule_1661(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1661;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS cmd32.exe access", 
                     sizeof("SERVER-IIS cmd32.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1662
// Rule Name: SERVER-WEBAPP /~ftp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1662
static inline int check_rule_1662(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /~ftp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1662
static inline void alert_rule_1662(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1662;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /~ftp access", 
                     sizeof("SERVER-WEBAPP /~ftp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1663
// Rule Name: SERVER-WEBAPP *%20.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1663
static inline int check_rule_1663(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  .pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1663
static inline void alert_rule_1663(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1663;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP *%20.pl access", 
                     sizeof("SERVER-WEBAPP *%20.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1664
// Rule Name: SERVER-WEBAPP mkplog.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1664
static inline int check_rule_1664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mkplog.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1664
static inline void alert_rule_1664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mkplog.exe access", 
                     sizeof("SERVER-WEBAPP mkplog.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1666
// Rule Name: INDICATOR-COMPROMISE index of /cgi-bin/ response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1666
static inline int check_rule_1666(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HTTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Index of /cgi-bin/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1666
static inline void alert_rule_1666(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1666;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE index of /cgi-bin/ response", 
                     sizeof("INDICATOR-COMPROMISE index of /cgi-bin/ response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1667
// Rule Name: SERVER-WEBAPP cross site scripting HTML Image tag set to javascript attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1667
static inline int check_rule_1667(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: img src=javascript",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1667
static inline void alert_rule_1667(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1667;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cross site scripting HTML Image tag set to javascript attempt", 
                     sizeof("SERVER-WEBAPP cross site scripting HTML Image tag set to javascript attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1668
// Rule Name: SERVER-WEBAPP /cgi-bin/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1668
static inline int check_rule_1668(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/', '/cgi-bin/ HTTP",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1668
static inline void alert_rule_1668(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1668;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /cgi-bin/ access", 
                     sizeof("SERVER-WEBAPP /cgi-bin/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1669
// Rule Name: SERVER-WEBAPP /cgi-dos/ access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1669
static inline int check_rule_1669(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-dos/', '/cgi-dos/ HTTP",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1669
static inline void alert_rule_1669(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1669;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /cgi-dos/ access", 
                     sizeof("SERVER-WEBAPP /cgi-dos/ access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1670
// Rule Name: SERVER-WEBAPP /home/ftp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1670
static inline int check_rule_1670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /home/ftp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1670
static inline void alert_rule_1670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /home/ftp access", 
                     sizeof("SERVER-WEBAPP /home/ftp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1671
// Rule Name: SERVER-WEBAPP /home/www access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1671
static inline int check_rule_1671(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /home/www",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1671
static inline void alert_rule_1671(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1671;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /home/www access", 
                     sizeof("SERVER-WEBAPP /home/www access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1672
// Rule Name: PROTOCOL-FTP CWD ~ attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: denial-of-service
// Generated from Snort rule parser output

// Check function for rule 1672
static inline int check_rule_1672(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: CWD",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1672
static inline void alert_rule_1672(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1672;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CWD ~ attempt", 
                     sizeof("PROTOCOL-FTP CWD ~ attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1673
// Rule Name: SERVER-ORACLE EXECUTE_SYSTEM attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: system-call-detect
// Generated from Snort rule parser output

// Check function for rule 1673
static inline int check_rule_1673(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: EXECUTE_SYSTEM",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1673
static inline void alert_rule_1673(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1673;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE EXECUTE_SYSTEM attempt", 
                     sizeof("SERVER-ORACLE EXECUTE_SYSTEM attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1674
// Rule Name: SERVER-ORACLE connect_data remote version detection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1674
static inline int check_rule_1674(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: connect_data|28|command=version|29|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1674
static inline void alert_rule_1674(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1674;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE connect_data remote version detection attempt", 
                     sizeof("SERVER-ORACLE connect_data remote version detection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1675
// Rule Name: SERVER-ORACLE misparsed login response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 1675
static inline int check_rule_1675(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['description=|28|",nocase', '!"connect_data=|28|sid=",nocase', '!"address=|28|protocol=tcp",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1675
static inline void alert_rule_1675(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1675;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE misparsed login response", 
                     sizeof("SERVER-ORACLE misparsed login response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1676
// Rule Name: SERVER-ORACLE select union attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1676
static inline int check_rule_1676(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['select ",nocase', ' union ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1676
static inline void alert_rule_1676(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1676;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE select union attempt", 
                     sizeof("SERVER-ORACLE select union attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1677
// Rule Name: SERVER-ORACLE select like '%' attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1677
static inline int check_rule_1677(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' where ",nocase', ' like \'%\'",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1677
static inline void alert_rule_1677(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1677;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE select like '%' attempt", 
                     sizeof("SERVER-ORACLE select like '%' attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1678
// Rule Name: SERVER-ORACLE select like '%' attempt backslash escaped
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1678
static inline int check_rule_1678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' where ",nocase', ' like |22|%|22|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1678
static inline void alert_rule_1678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE select like '%' attempt backslash escaped", 
                     sizeof("SERVER-ORACLE select like '%' attempt backslash escaped"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1679
// Rule Name: SERVER-ORACLE describe attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1679
static inline int check_rule_1679(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: describe ",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1679
static inline void alert_rule_1679(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1679;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE describe attempt", 
                     sizeof("SERVER-ORACLE describe attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1680
// Rule Name: SERVER-ORACLE all_constraints access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1680
static inline int check_rule_1680(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_constraints",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1680
static inline void alert_rule_1680(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1680;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_constraints access", 
                     sizeof("SERVER-ORACLE all_constraints access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1681
// Rule Name: SERVER-ORACLE all_views access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1681
static inline int check_rule_1681(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_views",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1681
static inline void alert_rule_1681(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1681;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_views access", 
                     sizeof("SERVER-ORACLE all_views access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1682
// Rule Name: SERVER-ORACLE all_source access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1682
static inline int check_rule_1682(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_source",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1682
static inline void alert_rule_1682(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1682;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_source access", 
                     sizeof("SERVER-ORACLE all_source access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1683
// Rule Name: SERVER-ORACLE all_tables access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1683
static inline int check_rule_1683(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_tables",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1683
static inline void alert_rule_1683(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1683;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_tables access", 
                     sizeof("SERVER-ORACLE all_tables access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1684
// Rule Name: SERVER-ORACLE all_tab_columns access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1684
static inline int check_rule_1684(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_tab_columns",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1684
static inline void alert_rule_1684(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1684;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_tab_columns access", 
                     sizeof("SERVER-ORACLE all_tab_columns access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1685
// Rule Name: SERVER-ORACLE all_tab_privs access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1685
static inline int check_rule_1685(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: all_tab_privs",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1685
static inline void alert_rule_1685(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1685;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE all_tab_privs access", 
                     sizeof("SERVER-ORACLE all_tab_privs access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1686
// Rule Name: SERVER-ORACLE dba_tablespace access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1686
static inline int check_rule_1686(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dba_tablespace",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1686
static inline void alert_rule_1686(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1686;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dba_tablespace access", 
                     sizeof("SERVER-ORACLE dba_tablespace access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1687
// Rule Name: SERVER-ORACLE dba_tables access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1687
static inline int check_rule_1687(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dba_tables",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1687
static inline void alert_rule_1687(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1687;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dba_tables access", 
                     sizeof("SERVER-ORACLE dba_tables access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1688
// Rule Name: SERVER-ORACLE user_tablespace access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1688
static inline int check_rule_1688(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: user_tablespace",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1688
static inline void alert_rule_1688(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1688;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE user_tablespace access", 
                     sizeof("SERVER-ORACLE user_tablespace access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1689
// Rule Name: SERVER-ORACLE sys.all_users access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1689
static inline int check_rule_1689(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.all_users",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1689
static inline void alert_rule_1689(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1689;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.all_users access", 
                     sizeof("SERVER-ORACLE sys.all_users access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1690
// Rule Name: SERVER-ORACLE grant attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1690
static inline int check_rule_1690(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['grant ",nocase', ' to ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1690
static inline void alert_rule_1690(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1690;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE grant attempt", 
                     sizeof("SERVER-ORACLE grant attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1691
// Rule Name: SERVER-ORACLE ALTER USER attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1691
static inline int check_rule_1691(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['alter user",nocase', ' identified by ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1691
static inline void alert_rule_1691(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1691;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE ALTER USER attempt", 
                     sizeof("SERVER-ORACLE ALTER USER attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1692
// Rule Name: SERVER-ORACLE drop table attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1692
static inline int check_rule_1692(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: drop table",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1692
static inline void alert_rule_1692(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1692;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE drop table attempt", 
                     sizeof("SERVER-ORACLE drop table attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1693
// Rule Name: SERVER-ORACLE create table attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1693
static inline int check_rule_1693(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: create table",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1693
static inline void alert_rule_1693(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1693;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE create table attempt", 
                     sizeof("SERVER-ORACLE create table attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1694
// Rule Name: SERVER-ORACLE alter table attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1694
static inline int check_rule_1694(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: alter table",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1694
static inline void alert_rule_1694(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1694;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE alter table attempt", 
                     sizeof("SERVER-ORACLE alter table attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1695
// Rule Name: SERVER-ORACLE truncate table attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1695
static inline int check_rule_1695(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: truncate table",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1695
static inline void alert_rule_1695(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1695;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE truncate table attempt", 
                     sizeof("SERVER-ORACLE truncate table attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1696
// Rule Name: SERVER-ORACLE create database attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1696
static inline int check_rule_1696(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: create database",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1696
static inline void alert_rule_1696(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1696;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE create database attempt", 
                     sizeof("SERVER-ORACLE create database attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1697
// Rule Name: SERVER-ORACLE alter database attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1697
static inline int check_rule_1697(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: alter database",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1697
static inline void alert_rule_1697(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1697;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE alter database attempt", 
                     sizeof("SERVER-ORACLE alter database attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1700
// Rule Name: SERVER-WEBAPP imagemap.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1700
static inline int check_rule_1700(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /imagemap.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1700
static inline void alert_rule_1700(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1700;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP imagemap.exe access", 
                     sizeof("SERVER-WEBAPP imagemap.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1701
// Rule Name: SERVER-WEBAPP calendar-admin.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1701
static inline int check_rule_1701(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calendar-admin.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1701
static inline void alert_rule_1701(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1701;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar-admin.pl access", 
                     sizeof("SERVER-WEBAPP calendar-admin.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1702
// Rule Name: SERVER-WEBAPP Amaya templates sendtemp.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1702
static inline int check_rule_1702(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sendtemp.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1702
static inline void alert_rule_1702(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1702;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Amaya templates sendtemp.pl access", 
                     sizeof("SERVER-WEBAPP Amaya templates sendtemp.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1703
// Rule Name: SERVER-WEBAPP auktion.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1703
static inline int check_rule_1703(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/auktion.cgi",fast_pattern,nocase', 'menue=../../",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1703
static inline void alert_rule_1703(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1703;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP auktion.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP auktion.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1704
// Rule Name: SERVER-WEBAPP cal_make.pl directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1704
static inline int check_rule_1704(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cal_make.pl",nocase', 'p0=../../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1704
static inline void alert_rule_1704(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1704;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cal_make.pl directory traversal attempt", 
                     sizeof("SERVER-WEBAPP cal_make.pl directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1705
// Rule Name: SERVER-WEBAPP echo.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1705
static inline int check_rule_1705(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/echo.bat', '&']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1705
static inline void alert_rule_1705(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1705;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP echo.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP echo.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1706
// Rule Name: SERVER-WEBAPP echo.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1706
static inline int check_rule_1706(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /echo.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1706
static inline void alert_rule_1706(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1706;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP echo.bat access", 
                     sizeof("SERVER-WEBAPP echo.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1707
// Rule Name: SERVER-WEBAPP hello.bat arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1707
static inline int check_rule_1707(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/hello.bat', '&']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1707
static inline void alert_rule_1707(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1707;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP hello.bat arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP hello.bat arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1708
// Rule Name: SERVER-WEBAPP hello.bat access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1708
static inline int check_rule_1708(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /hello.bat",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1708
static inline void alert_rule_1708(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1708;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP hello.bat access", 
                     sizeof("SERVER-WEBAPP hello.bat access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1709
// Rule Name: SERVER-WEBAPP ad.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1709
static inline int check_rule_1709(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ad.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1709
static inline void alert_rule_1709(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1709;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ad.cgi access", 
                     sizeof("SERVER-WEBAPP ad.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1710
// Rule Name: SERVER-WEBAPP bbs_forum.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1710
static inline int check_rule_1710(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bbs_forum.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1710
static inline void alert_rule_1710(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1710;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bbs_forum.cgi access", 
                     sizeof("SERVER-WEBAPP bbs_forum.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1711
// Rule Name: SERVER-WEBAPP bsguest.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1711
static inline int check_rule_1711(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bsguest.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1711
static inline void alert_rule_1711(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1711;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bsguest.cgi access", 
                     sizeof("SERVER-WEBAPP bsguest.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1712
// Rule Name: SERVER-WEBAPP bslist.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1712
static inline int check_rule_1712(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bslist.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1712
static inline void alert_rule_1712(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1712;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bslist.cgi access", 
                     sizeof("SERVER-WEBAPP bslist.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1713
// Rule Name: SERVER-WEBAPP cgforum.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1713
static inline int check_rule_1713(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgforum.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1713
static inline void alert_rule_1713(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1713;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgforum.cgi access", 
                     sizeof("SERVER-WEBAPP cgforum.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1714
// Rule Name: SERVER-WEBAPP newdesk access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1714
static inline int check_rule_1714(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /newdesk",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1714
static inline void alert_rule_1714(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1714;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP newdesk access", 
                     sizeof("SERVER-WEBAPP newdesk access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1715
// Rule Name: SERVER-WEBAPP register.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1715
static inline int check_rule_1715(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /register.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1715
static inline void alert_rule_1715(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1715;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP register.cgi access", 
                     sizeof("SERVER-WEBAPP register.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1716
// Rule Name: SERVER-WEBAPP gbook.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1716
static inline int check_rule_1716(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /gbook.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1716
static inline void alert_rule_1716(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1716;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP gbook.cgi access", 
                     sizeof("SERVER-WEBAPP gbook.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1717
// Rule Name: SERVER-WEBAPP simplestguest.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1717
static inline int check_rule_1717(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /simplestguest.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1717
static inline void alert_rule_1717(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1717;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP simplestguest.cgi access", 
                     sizeof("SERVER-WEBAPP simplestguest.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1718
// Rule Name: SERVER-WEBAPP statsconfig.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1718
static inline int check_rule_1718(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /statsconfig.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1718
static inline void alert_rule_1718(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1718;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP statsconfig.pl access", 
                     sizeof("SERVER-WEBAPP statsconfig.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1719
// Rule Name: SERVER-WEBAPP talkback.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1719
static inline int check_rule_1719(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/talkbalk.cgi",nocase', 'article=../../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1719
static inline void alert_rule_1719(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1719;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP talkback.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP talkback.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1720
// Rule Name: SERVER-WEBAPP talkback.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1720
static inline int check_rule_1720(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /talkbalk.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1720
static inline void alert_rule_1720(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1720;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP talkback.cgi access", 
                     sizeof("SERVER-WEBAPP talkback.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1721
// Rule Name: SERVER-WEBAPP adcycle access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1721
static inline int check_rule_1721(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /adcycle",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1721
static inline void alert_rule_1721(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1721;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP adcycle access", 
                     sizeof("SERVER-WEBAPP adcycle access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1722
// Rule Name: SERVER-WEBAPP MachineInfo access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1722
static inline int check_rule_1722(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /MachineInfo",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1722
static inline void alert_rule_1722(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1722;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MachineInfo access", 
                     sizeof("SERVER-WEBAPP MachineInfo access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1723
// Rule Name: SERVER-WEBAPP emumail.cgi NULL attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1723
static inline int check_rule_1723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/emumail.cgi', 'type=",nocase', '%00']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1723
static inline void alert_rule_1723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP emumail.cgi NULL attempt", 
                     sizeof("SERVER-WEBAPP emumail.cgi NULL attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1724
// Rule Name: SERVER-WEBAPP emumail.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1724
static inline int check_rule_1724(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /emumail.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1724
static inline void alert_rule_1724(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1724;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP emumail.cgi access", 
                     sizeof("SERVER-WEBAPP emumail.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1725
// Rule Name: SERVER-IIS +.htr code fragment attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1725
static inline int check_rule_1725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  .htr",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1725
static inline void alert_rule_1725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS +.htr code fragment attempt", 
                     sizeof("SERVER-IIS +.htr code fragment attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1726
// Rule Name: SERVER-IIS doctodep.btr access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1726
static inline int check_rule_1726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: doctodep.btr
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1726
static inline void alert_rule_1726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS doctodep.btr access", 
                     sizeof("SERVER-IIS doctodep.btr access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1727
// Rule Name: SERVER-WEBAPP SGI InfoSearch fname access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1727
static inline int check_rule_1727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /infosrch.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1727
static inline void alert_rule_1727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SGI InfoSearch fname access", 
                     sizeof("SERVER-WEBAPP SGI InfoSearch fname access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1729
// Rule Name: POLICY-SOCIAL IRC channel join
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1729
static inline int check_rule_1729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: JOIN 
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1729
static inline void alert_rule_1729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC channel join", 
                     sizeof("POLICY-SOCIAL IRC channel join"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1730
// Rule Name: SERVER-WEBAPP ustorekeeper.pl directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1730
static inline int check_rule_1730(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ustorekeeper.pl",nocase', 'file=../../",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1730
static inline void alert_rule_1730(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1730;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ustorekeeper.pl directory traversal attempt", 
                     sizeof("SERVER-WEBAPP ustorekeeper.pl directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1731
// Rule Name: SERVER-WEBAPP a1stats access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1731
static inline int check_rule_1731(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /a1stats/
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1731
static inline void alert_rule_1731(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1731;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP a1stats access", 
                     sizeof("SERVER-WEBAPP a1stats access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1733
// Rule Name: PROTOCOL-RPC portmap rwalld request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1733
static inline int check_rule_1733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A8|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1733
static inline void alert_rule_1733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap rwalld request TCP", 
                     sizeof("PROTOCOL-RPC portmap rwalld request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1734
// Rule Name: PROTOCOL-FTP USER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1734
static inline int check_rule_1734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1734
static inline void alert_rule_1734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP USER overflow attempt", 
                     sizeof("PROTOCOL-FTP USER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1735
// Rule Name: BROWSER-OTHER Mozilla Netscape XMLHttpRequest local file read attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1735
static inline int check_rule_1735(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['new XMLHttpRequest|28|', 'file|3A|//",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1735
static inline void alert_rule_1735(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1735;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-OTHER Mozilla Netscape XMLHttpRequest local file read attempt", 
                     sizeof("BROWSER-OTHER Mozilla Netscape XMLHttpRequest local file read attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1736
// Rule Name: SERVER-WEBAPP squirrel mail spell-check arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1736
static inline int check_rule_1736(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/squirrelspell/modules/check_me.mod.php",fast_pattern,nocase', 'SQSPELL_APP[",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1736
static inline void alert_rule_1736(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1736;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP squirrel mail spell-check arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP squirrel mail spell-check arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1737
// Rule Name: SERVER-WEBAPP squirrel mail theme arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1737
static inline int check_rule_1737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/left_main.php",nocase', 'cmdd=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1737
static inline void alert_rule_1737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP squirrel mail theme arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP squirrel mail theme arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1738
// Rule Name: SERVER-WEBAPP global.inc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1738
static inline int check_rule_1738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /global.inc",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1738
static inline void alert_rule_1738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP global.inc access", 
                     sizeof("SERVER-WEBAPP global.inc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1739
// Rule Name: SERVER-WEBAPP DNSTools administrator authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1739
static inline int check_rule_1739(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnstools.php",nocase', 'user_logged_in=true",nocase', 'user_dnstools_administrator=true",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1739
static inline void alert_rule_1739(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1739;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DNSTools administrator authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP DNSTools administrator authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1740
// Rule Name: SERVER-WEBAPP DNSTools authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1740
static inline int check_rule_1740(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnstools.php",fast_pattern,nocase', 'user_logged_in=true']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1740
static inline void alert_rule_1740(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1740;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DNSTools authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP DNSTools authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1741
// Rule Name: SERVER-WEBAPP DNSTools access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1741
static inline int check_rule_1741(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dnstools.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1741
static inline void alert_rule_1741(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1741;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DNSTools access", 
                     sizeof("SERVER-WEBAPP DNSTools access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1742
// Rule Name: SERVER-WEBAPP Blahz-DNS dostuff.php modify user attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1742
static inline int check_rule_1742(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dostuff.php?",nocase', 'action=modify_user",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1742
static inline void alert_rule_1742(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1742;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Blahz-DNS dostuff.php modify user attempt", 
                     sizeof("SERVER-WEBAPP Blahz-DNS dostuff.php modify user attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1743
// Rule Name: SERVER-WEBAPP Blahz-DNS dostuff.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1743
static inline int check_rule_1743(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dostuff.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1743
static inline void alert_rule_1743(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1743;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Blahz-DNS dostuff.php access", 
                     sizeof("SERVER-WEBAPP Blahz-DNS dostuff.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1744
// Rule Name: SERVER-WEBAPP SecureSite authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1744
static inline int check_rule_1744(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: secure_site, ok",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1744
static inline void alert_rule_1744(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1744;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SecureSite authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP SecureSite authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1745
// Rule Name: SERVER-WEBAPP Messagerie supp_membre.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1745
static inline int check_rule_1745(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /supp_membre.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1745
static inline void alert_rule_1745(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1745;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Messagerie supp_membre.php access", 
                     sizeof("SERVER-WEBAPP Messagerie supp_membre.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1747
// Rule Name: PROTOCOL-RPC portmap cachefsd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1747
static inline int check_rule_1747(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87 8B|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1747
static inline void alert_rule_1747(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1747;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap cachefsd request TCP", 
                     sizeof("PROTOCOL-RPC portmap cachefsd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1750
// Rule Name: SERVER-IIS users.xml access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1750
static inline int check_rule_1750(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /users.xml",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1750
static inline void alert_rule_1750(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1750;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS users.xml access", 
                     sizeof("SERVER-IIS users.xml access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1751
// Rule Name: SERVER-OTHER cachefsd buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1751
static inline int check_rule_1751(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 32772-34000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 32772 || dst_port > 34000)
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 01 87 86 00 00 00 01 00 00 00 05|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1751
static inline void alert_rule_1751(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1751;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER cachefsd buffer overflow attempt", 
                     sizeof("SERVER-OTHER cachefsd buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1753
// Rule Name: SERVER-IIS as_web.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1753
static inline int check_rule_1753(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /as_web.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1753
static inline void alert_rule_1753(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1753;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS as_web.exe access", 
                     sizeof("SERVER-IIS as_web.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1754
// Rule Name: SERVER-IIS as_web4.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1754
static inline int check_rule_1754(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /as_web4.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1754
static inline void alert_rule_1754(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1754;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS as_web4.exe access", 
                     sizeof("SERVER-IIS as_web4.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1755
// Rule Name: PROTOCOL-IMAP partial body buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1755
static inline int check_rule_1755(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PARTIAL",nocase', 'BODY[",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1755
static inline void alert_rule_1755(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1755;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP partial body buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP partial body buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1756
// Rule Name: SERVER-IIS NewsPro administration authentication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1756
static inline int check_rule_1756(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: logged,true
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1756
static inline void alert_rule_1756(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1756;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS NewsPro administration authentication attempt", 
                     sizeof("SERVER-IIS NewsPro administration authentication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1757
// Rule Name: SERVER-WEBAPP b2 arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1757
static inline int check_rule_1757(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/b2/b2-include/', 'b2inc', 'http|3A|//']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1757
static inline void alert_rule_1757(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1757;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP b2 arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP b2 arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1759
// Rule Name: SQL xp_cmdshell program execution 445
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1759
static inline int check_rule_1759(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: x|00|p|00|_|00|c|00|m|00|d|00|s|00|h|00|e|00|l|00|l|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1759
static inline void alert_rule_1759(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1759;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL xp_cmdshell program execution 445", 
                     sizeof("SQL xp_cmdshell program execution 445"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1762
// Rule Name: SERVER-WEBAPP phf arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1762
static inline int check_rule_1762(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/phf?Q",fast_pattern,nocase', '%0a",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1762
static inline void alert_rule_1762(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1762;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phf arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP phf arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1763
// Rule Name: SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1763
static inline int check_rule_1763(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiproc?Nocfile=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1763
static inline void alert_rule_1763(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1763;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt", 
                     sizeof("SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1764
// Rule Name: SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1764
static inline int check_rule_1764(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiproc?|24|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1764
static inline void alert_rule_1764(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1764;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt", 
                     sizeof("SERVER-WEBAPP Nortel Contivity cgiproc DOS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1765
// Rule Name: SERVER-WEBAPP Nortel Contivity cgiproc access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1765
static inline int check_rule_1765(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiproc",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1765
static inline void alert_rule_1765(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1765;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nortel Contivity cgiproc access", 
                     sizeof("SERVER-WEBAPP Nortel Contivity cgiproc access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1766
// Rule Name: SERVER-WEBAPP search.dll directory listing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1766
static inline int check_rule_1766(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.dll', 'query=%00']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1766
static inline void alert_rule_1766(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1766;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP search.dll directory listing attempt", 
                     sizeof("SERVER-WEBAPP search.dll directory listing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1767
// Rule Name: SERVER-WEBAPP search.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1767
static inline int check_rule_1767(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /search.dll
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1767
static inline void alert_rule_1767(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1767;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP search.dll access", 
                     sizeof("SERVER-WEBAPP search.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1769
// Rule Name: SERVER-WEBAPP .DS_Store access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1769
static inline int check_rule_1769(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.DS_Store
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1769
static inline void alert_rule_1769(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1769;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .DS_Store access", 
                     sizeof("SERVER-WEBAPP .DS_Store access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1770
// Rule Name: SERVER-WEBAPP .FBCIndex access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1770
static inline int check_rule_1770(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.FBCIndex
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1770
static inline void alert_rule_1770(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1770;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP .FBCIndex access", 
                     sizeof("SERVER-WEBAPP .FBCIndex access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1772
// Rule Name: SERVER-IIS pbserver access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1772
static inline int check_rule_1772(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pbserver/pbserver.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1772
static inline void alert_rule_1772(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1772;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS pbserver access", 
                     sizeof("SERVER-IIS pbserver access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1773
// Rule Name: SERVER-WEBAPP php.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1773
static inline int check_rule_1773(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /php.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1773
static inline void alert_rule_1773(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1773;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP php.exe access", 
                     sizeof("SERVER-WEBAPP php.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1774
// Rule Name: SERVER-WEBAPP bb_smilies.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1774
static inline int check_rule_1774(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bb_smilies.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1774
static inline void alert_rule_1774(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1774;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bb_smilies.php access", 
                     sizeof("SERVER-WEBAPP bb_smilies.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1775
// Rule Name: SERVER-MYSQL root login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1775
static inline int check_rule_1775(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 3306
    if (tcph->dest != htons(3306))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0A 00 00 01 85 04 00 00 80|root|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1775
static inline void alert_rule_1775(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1775;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MYSQL root login attempt", 
                     sizeof("SERVER-MYSQL root login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1776
// Rule Name: SERVER-MYSQL show databases attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1776
static inline int check_rule_1776(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 3306
    if (tcph->dest != htons(3306))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0F 00 00 00 03|show databases",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1776
static inline void alert_rule_1776(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1776;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MYSQL show databases attempt", 
                     sizeof("SERVER-MYSQL show databases attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1777
// Rule Name: PROTOCOL-FTP EXPLOIT STAT asterisk dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1777
static inline int check_rule_1777(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: STAT",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1777
static inline void alert_rule_1777(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1777;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP EXPLOIT STAT asterisk dos attempt", 
                     sizeof("PROTOCOL-FTP EXPLOIT STAT asterisk dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1778
// Rule Name: PROTOCOL-FTP EXPLOIT STAT ? dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1778
static inline int check_rule_1778(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: STAT",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1778
static inline void alert_rule_1778(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1778;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP EXPLOIT STAT ? dos attempt", 
                     sizeof("PROTOCOL-FTP EXPLOIT STAT ? dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1787
// Rule Name: SERVER-WEBAPP csPassword.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1787
static inline int check_rule_1787(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /csPassword.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1787
static inline void alert_rule_1787(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1787;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP csPassword.cgi access", 
                     sizeof("SERVER-WEBAPP csPassword.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1788
// Rule Name: SERVER-WEBAPP csPassword password.cgi.tmp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1788
static inline int check_rule_1788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /password.cgi.tmp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1788
static inline void alert_rule_1788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP csPassword password.cgi.tmp access", 
                     sizeof("SERVER-WEBAPP csPassword password.cgi.tmp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1789
// Rule Name: POLICY-SOCIAL IRC dns request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1789
static inline int check_rule_1789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 6666-7000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 6666 || dst_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: USERHOST 
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1789
static inline void alert_rule_1789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC dns request", 
                     sizeof("POLICY-SOCIAL IRC dns request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1790
// Rule Name: POLICY-SOCIAL IRC dns response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1790
static inline int check_rule_1790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 6666-7000
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 6666 || src_port > 7000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|3A|', ' 302 ', '=+",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1790
static inline void alert_rule_1790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL IRC dns response", 
                     sizeof("POLICY-SOCIAL IRC dns response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1792
// Rule Name: PROTOCOL-NNTP return code buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1792
static inline int check_rule_1792(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 119
    if (tcph->source != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: 200
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1792
static inline void alert_rule_1792(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1792;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP return code buffer overflow attempt", 
                     sizeof("PROTOCOL-NNTP return code buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1802
// Rule Name: SERVER-IIS .asa HTTP header buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1802
static inline int check_rule_1802(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HTTP/",nocase', '.asa",fast_pattern,nocase', '|3A|', '|0A|', '|00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1802
static inline void alert_rule_1802(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1802;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .asa HTTP header buffer overflow attempt", 
                     sizeof("SERVER-IIS .asa HTTP header buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1803
// Rule Name: SERVER-IIS .cer HTTP header buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1803
static inline int check_rule_1803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HTTP/",nocase', '.cer",fast_pattern,nocase', '|3A|', '|0A|', '|00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1803
static inline void alert_rule_1803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .cer HTTP header buffer overflow attempt", 
                     sizeof("SERVER-IIS .cer HTTP header buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1804
// Rule Name: SERVER-IIS .cdx HTTP header buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1804
static inline int check_rule_1804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HTTP/",nocase', '.cdx",fast_pattern,nocase', '|3A|', '|0A|', '|00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1804
static inline void alert_rule_1804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .cdx HTTP header buffer overflow attempt", 
                     sizeof("SERVER-IIS .cdx HTTP header buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1805
// Rule Name: SERVER-WEBAPP Oracle Reports CGI access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1805
static inline int check_rule_1805(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/rwcgi60",fast_pattern,nocase', 'setauth=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1805
static inline void alert_rule_1805(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1805;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle Reports CGI access", 
                     sizeof("SERVER-WEBAPP Oracle Reports CGI access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1806
// Rule Name: SERVER-IIS .htr chunked Transfer-Encoding
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1806
static inline int check_rule_1806(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.htr",nocase', 'Transfer-Encoding|3A|",nocase', 'chunked",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1806
static inline void alert_rule_1806(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1806;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .htr chunked Transfer-Encoding", 
                     sizeof("SERVER-IIS .htr chunked Transfer-Encoding"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1807
// Rule Name: POLICY-OTHER Chunked-Encoding transfer with no data attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1807
static inline int check_rule_1807(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Transfer-Encoding: chunked|0D 0A 0D 0A 0D 0A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1807
static inline void alert_rule_1807(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1807;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Chunked-Encoding transfer with no data attempt", 
                     sizeof("POLICY-OTHER Chunked-Encoding transfer with no data attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1808
// Rule Name: SERVER-WEBAPP Apache chunked-encoding memory corruption exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1808
static inline int check_rule_1808(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |C0|PR|89 E1|PQRP|B8 3B 00 00 00 CD 80|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1808
static inline void alert_rule_1808(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1808;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Apache chunked-encoding memory corruption exploit attempt", 
                     sizeof("SERVER-WEBAPP Apache chunked-encoding memory corruption exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1809
// Rule Name: SERVER-APACHE Apache chunked-encoding worm attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1809
static inline int check_rule_1809(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: X-CCCCCCC|3A 20|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1809
static inline void alert_rule_1809(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1809;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache chunked-encoding worm attempt", 
                     sizeof("SERVER-APACHE Apache chunked-encoding worm attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1810
// Rule Name: SERVER-OTHER successful gobbles ssh exploit GOBBLE
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 1810
static inline int check_rule_1810(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 22
    if (tcph->source != htons(22))
        return 0;
    // Content matching (simplified)
    // Full pattern: *GOBBLE*
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1810
static inline void alert_rule_1810(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1810;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER successful gobbles ssh exploit GOBBLE", 
                     sizeof("SERVER-OTHER successful gobbles ssh exploit GOBBLE"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1811
// Rule Name: SERVER-OTHER successful gobbles ssh exploit uname
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1811
static inline int check_rule_1811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 22
    if (tcph->source != htons(22))
        return 0;
    // Content matching (simplified)
    // Full pattern: uname
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1811
static inline void alert_rule_1811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER successful gobbles ssh exploit uname", 
                     sizeof("SERVER-OTHER successful gobbles ssh exploit uname"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1812
// Rule Name: SERVER-OTHER gobbles SSH exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1812
static inline int check_rule_1812(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 22
    if (tcph->dest != htons(22))
        return 0;
    // Content matching (simplified)
    // Full pattern: GOBBLES
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1812
static inline void alert_rule_1812(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1812;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER gobbles SSH exploit attempt", 
                     sizeof("SERVER-OTHER gobbles SSH exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1814
// Rule Name: SERVER-WEBAPP CISCO VoIP DOS ATTEMPT
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1814
static inline int check_rule_1814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /StreamingStatistics
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1814
static inline void alert_rule_1814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CISCO VoIP DOS ATTEMPT", 
                     sizeof("SERVER-WEBAPP CISCO VoIP DOS ATTEMPT"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1815
// Rule Name: SERVER-WEBAPP directory.php arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1815
static inline int check_rule_1815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/directory.php', 'dir=', '|3B|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1815
static inline void alert_rule_1815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP directory.php arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP directory.php arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1816
// Rule Name: SERVER-WEBAPP directory.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1816
static inline int check_rule_1816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /directory.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1816
static inline void alert_rule_1816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP directory.php access", 
                     sizeof("SERVER-WEBAPP directory.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1817
// Rule Name: SERVER-IIS MS Site Server default login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1817
static inline int check_rule_1817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /SiteServer/Admin/knowledge/persmbr/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1817
static inline void alert_rule_1817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS MS Site Server default login attempt", 
                     sizeof("SERVER-IIS MS Site Server default login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1818
// Rule Name: SERVER-IIS MS Site Server admin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1818
static inline int check_rule_1818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Site Server/Admin/knowledge/persmbr/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1818
static inline void alert_rule_1818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS MS Site Server admin attempt", 
                     sizeof("SERVER-IIS MS Site Server admin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1819
// Rule Name: SERVER-OTHER Alcatel PABX 4400 connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 1819
static inline int check_rule_1819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2533
    if (tcph->dest != htons(2533))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 01|C",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1819
static inline void alert_rule_1819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Alcatel PABX 4400 connection attempt", 
                     sizeof("SERVER-OTHER Alcatel PABX 4400 connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1820
// Rule Name: SERVER-WEBAPP IBM Net.Commerce orderdspc.d2w access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1820
static inline int check_rule_1820(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ncommerce3/ExecMacro/orderdspc.d2w
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1820
static inline void alert_rule_1820(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1820;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP IBM Net.Commerce orderdspc.d2w access", 
                     sizeof("SERVER-WEBAPP IBM Net.Commerce orderdspc.d2w access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1821
// Rule Name: SERVER-OTHER LPD dvips remote command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: system-call-detect
// Generated from Snort rule parser output

// Check function for rule 1821
static inline int check_rule_1821(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 515
    if (tcph->dest != htons(515))
        return 0;
    // Content matching (simplified)
    // Full pattern: psfile=|22|`
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1821
static inline void alert_rule_1821(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1821;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER LPD dvips remote command execution attempt", 
                     sizeof("SERVER-OTHER LPD dvips remote command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1822
// Rule Name: SERVER-WEBAPP AlienForm alienform.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1822
static inline int check_rule_1822(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/alienform.cgi', '.|7C|./.|7C|.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1822
static inline void alert_rule_1822(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1822;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AlienForm alienform.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP AlienForm alienform.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1823
// Rule Name: SERVER-WEBAPP AlienForm af.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1823
static inline int check_rule_1823(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/af.cgi', '.|7C|./.|7C|.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1823
static inline void alert_rule_1823(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1823;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AlienForm af.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP AlienForm af.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1824
// Rule Name: SERVER-WEBAPP AlienForm alienform.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1824
static inline int check_rule_1824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /alienform.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1824
static inline void alert_rule_1824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AlienForm alienform.cgi access", 
                     sizeof("SERVER-WEBAPP AlienForm alienform.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1825
// Rule Name: SERVER-WEBAPP AlienForm af.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1825
static inline int check_rule_1825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /af.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1825
static inline void alert_rule_1825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP AlienForm af.cgi access", 
                     sizeof("SERVER-WEBAPP AlienForm af.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1826
// Rule Name: SERVER-WEBAPP WEB-INF access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1826
static inline int check_rule_1826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /WEB-INF",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1826
static inline void alert_rule_1826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WEB-INF access", 
                     sizeof("SERVER-WEBAPP WEB-INF access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1827
// Rule Name: SERVER-APACHE Apache Tomcat servlet mapping cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1827
static inline int check_rule_1827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/servlet/', '/org.apache.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1827
static inline void alert_rule_1827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat servlet mapping cross site scripting attempt", 
                     sizeof("SERVER-APACHE Apache Tomcat servlet mapping cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1828
// Rule Name: SERVER-WEBAPP iPlanet Search directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1828
static inline int check_rule_1828(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search",nocase', 'NS-query-pat=",fast_pattern,nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1828
static inline void alert_rule_1828(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1828;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP iPlanet Search directory traversal attempt", 
                     sizeof("SERVER-WEBAPP iPlanet Search directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1829
// Rule Name: SERVER-APACHE Apache Tomcat TroubleShooter servlet access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1829
static inline int check_rule_1829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /examples/servlet/TroubleShooter
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1829
static inline void alert_rule_1829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat TroubleShooter servlet access", 
                     sizeof("SERVER-APACHE Apache Tomcat TroubleShooter servlet access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1830
// Rule Name: SERVER-APACHE Apache Tomcat SnoopServlet servlet access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1830
static inline int check_rule_1830(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /examples/servlet/SnoopServlet
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1830
static inline void alert_rule_1830(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1830;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat SnoopServlet servlet access", 
                     sizeof("SERVER-APACHE Apache Tomcat SnoopServlet servlet access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1831
// Rule Name: SERVER-WEBAPP jigsaw dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1831
static inline int check_rule_1831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /servlet/con
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1831
static inline void alert_rule_1831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP jigsaw dos attempt", 
                     sizeof("SERVER-WEBAPP jigsaw dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1832
// Rule Name: POLICY-SOCIAL ICQ forced user addition
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1832
static inline int check_rule_1832(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 80
    if (tcph->source != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A| application/x-icq",fast_pattern,nocase', '[ICQ User]']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1832
static inline void alert_rule_1832(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1832;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL ICQ forced user addition", 
                     sizeof("POLICY-SOCIAL ICQ forced user addition"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1834
// Rule Name: SERVER-WEBAPP PHP-Wiki cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1834
static inline int check_rule_1834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/modules.php?', 'name=Wiki",fast_pattern,nocase', '<script",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1834
static inline void alert_rule_1834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHP-Wiki cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP PHP-Wiki cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1835
// Rule Name: SERVER-WEBAPP Macromedia SiteSpring cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1835
static inline int check_rule_1835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/error/500error.jsp",nocase', 'et=', '<script",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1835
static inline void alert_rule_1835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Macromedia SiteSpring cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP Macromedia SiteSpring cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1838
// Rule Name: SERVER-OTHER SSH server banner overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1838
static inline int check_rule_1838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 22
    if (tcph->source != htons(22))
        return 0;
    // Content matching (simplified)
    // Full pattern: SSH-",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1838
static inline void alert_rule_1838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER SSH server banner overflow", 
                     sizeof("SERVER-OTHER SSH server banner overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1839
// Rule Name: SERVER-WEBAPP mailman cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1839
static inline int check_rule_1839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mailman/",nocase', '?', 'info=', '<script",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1839
static inline void alert_rule_1839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mailman cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP mailman cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1840
// Rule Name: FILE-JAVA Oracle Javascript document.domain attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1840
static inline int check_rule_1840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: document.domain|28|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1840
static inline void alert_rule_1840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-JAVA Oracle Javascript document.domain attempt", 
                     sizeof("FILE-JAVA Oracle Javascript document.domain attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1841
// Rule Name: BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1841
static inline int check_rule_1841(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['javascript|3A|//",fast_pattern,nocase', 'document.cookie",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1841
static inline void alert_rule_1841(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1841;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt", 
                     sizeof("BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1842
// Rule Name: PROTOCOL-IMAP login buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 1842
static inline int check_rule_1842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1842
static inline void alert_rule_1842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP login buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP login buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1843
// Rule Name: MALWARE-BACKDOOR trinity connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1843
static inline int check_rule_1843(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 33270
    if (tcph->dest != htons(33270))
        return 0;
    // Content matching (simplified)
    // Full pattern: !@|23|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1843
static inline void alert_rule_1843(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1843;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR trinity connection attempt", 
                     sizeof("MALWARE-BACKDOOR trinity connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1844
// Rule Name: PROTOCOL-IMAP authenticate overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1844
static inline int check_rule_1844(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: AUTHENTICATE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1844
static inline void alert_rule_1844(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1844;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP authenticate overflow attempt", 
                     sizeof("PROTOCOL-IMAP authenticate overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1845
// Rule Name: PROTOCOL-IMAP list literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1845
static inline int check_rule_1845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LIST",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1845
static inline void alert_rule_1845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP list literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP list literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1846
// Rule Name: POLICY-MULTIMEDIA vncviewer Java applet download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 1846
static inline int check_rule_1846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 5800-5802
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 5800 || dst_port > 5802)
        return 0;
    // Content matching (simplified)
    // Full pattern: /vncviewer.jar
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1846
static inline void alert_rule_1846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-MULTIMEDIA vncviewer Java applet download attempt", 
                     sizeof("POLICY-MULTIMEDIA vncviewer Java applet download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1847
// Rule Name: SERVER-WEBAPP webalizer access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1847
static inline int check_rule_1847(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webalizer/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1847
static inline void alert_rule_1847(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1847;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webalizer access", 
                     sizeof("SERVER-WEBAPP webalizer access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1848
// Rule Name: SERVER-WEBAPP webcart-lite access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1848
static inline int check_rule_1848(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webcart-lite/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1848
static inline void alert_rule_1848(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1848;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webcart-lite access", 
                     sizeof("SERVER-WEBAPP webcart-lite access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1849
// Rule Name: SERVER-WEBAPP webfind.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1849
static inline int check_rule_1849(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webfind.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1849
static inline void alert_rule_1849(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1849;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webfind.exe access", 
                     sizeof("SERVER-WEBAPP webfind.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1850
// Rule Name: SERVER-WEBAPP way-board.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1850
static inline int check_rule_1850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /way-board.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1850
static inline void alert_rule_1850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP way-board.cgi access", 
                     sizeof("SERVER-WEBAPP way-board.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1851
// Rule Name: SERVER-WEBAPP active.log access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1851
static inline int check_rule_1851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /active.log",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1851
static inline void alert_rule_1851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP active.log access", 
                     sizeof("SERVER-WEBAPP active.log access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1852
// Rule Name: SERVER-WEBAPP robots.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1852
static inline int check_rule_1852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /robots.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1852
static inline void alert_rule_1852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP robots.txt access", 
                     sizeof("SERVER-WEBAPP robots.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1857
// Rule Name: SERVER-WEBAPP robot.txt access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1857
static inline int check_rule_1857(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /robot.txt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1857
static inline void alert_rule_1857(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1857;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP robot.txt access", 
                     sizeof("SERVER-WEBAPP robot.txt access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1858
// Rule Name: SERVER-WEBAPP CISCO PIX Firewall Manager directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1858
static inline int check_rule_1858(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pixfir~1/how_to_login.html
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1858
static inline void alert_rule_1858(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1858;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CISCO PIX Firewall Manager directory traversal attempt", 
                     sizeof("SERVER-WEBAPP CISCO PIX Firewall Manager directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1859
// Rule Name: SERVER-WEBAPP Oracle JavaServer default password login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 1859
static inline int check_rule_1859(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9090
    if (tcph->dest != htons(9090))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/servlet/admin', 'ae9f86d6beaa3f9ecb9a5b7e072a4138']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1859
static inline void alert_rule_1859(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1859;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle JavaServer default password login attempt", 
                     sizeof("SERVER-WEBAPP Oracle JavaServer default password login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1860
// Rule Name: SERVER-WEBAPP Linksys router default password login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 1860
static inline int check_rule_1860(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: Authorization|3A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1860
static inline void alert_rule_1860(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1860;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys router default password login attempt", 
                     sizeof("SERVER-WEBAPP Linksys router default password login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1861
// Rule Name: SERVER-WEBAPP Linksys router default username and password login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 1861
static inline int check_rule_1861(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: YWRtaW46YWRtaW4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1861
static inline void alert_rule_1861(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1861;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys router default username and password login attempt", 
                     sizeof("SERVER-WEBAPP Linksys router default username and password login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1862
// Rule Name: SERVER-WEBAPP mrtg.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1862
static inline int check_rule_1862(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mrtg.cgi', 'cfg=/../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1862
static inline void alert_rule_1862(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1862;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mrtg.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP mrtg.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1864
// Rule Name: PROTOCOL-FTP SITE NEWER attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 1864
static inline int check_rule_1864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'NEWER",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1864
static inline void alert_rule_1864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE NEWER attempt", 
                     sizeof("PROTOCOL-FTP SITE NEWER attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1865
// Rule Name: SERVER-WEBAPP webdist.cgi arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1865
static inline int check_rule_1865(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webdist.cgi",nocase', 'distloc=|3B|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1865
static inline void alert_rule_1865(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1865;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webdist.cgi arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP webdist.cgi arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1866
// Rule Name: PROTOCOL-POP USER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1866
static inline int check_rule_1866(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1866
static inline void alert_rule_1866(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1866;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP USER overflow attempt", 
                     sizeof("PROTOCOL-POP USER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1868
// Rule Name: SERVER-WEBAPP Interactive Story story.pl arbitrary file read attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 1868
static inline int check_rule_1868(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/story.pl', 'next=../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1868
static inline void alert_rule_1868(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1868;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Interactive Story story.pl arbitrary file read attempt", 
                     sizeof("SERVER-WEBAPP Interactive Story story.pl arbitrary file read attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1869
// Rule Name: SERVER-WEBAPP Interactive Story story.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 1869
static inline int check_rule_1869(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8080
    if (tcph->dest != htons(8080))
        return 0;
    // Content matching (simplified)
    // Full pattern: /story.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1869
static inline void alert_rule_1869(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1869;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Interactive Story story.pl access", 
                     sizeof("SERVER-WEBAPP Interactive Story story.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1870
// Rule Name: SERVER-WEBAPP siteUserMod.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1870
static inline int check_rule_1870(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.cobalt/siteUserMod/siteUserMod.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1870
static inline void alert_rule_1870(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1870;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP siteUserMod.cgi access", 
                     sizeof("SERVER-WEBAPP siteUserMod.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1871
// Rule Name: SERVER-WEBAPP Oracle XSQLConfig.xml access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1871
static inline int check_rule_1871(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /XSQLConfig.xml
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1871
static inline void alert_rule_1871(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1871;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle XSQLConfig.xml access", 
                     sizeof("SERVER-WEBAPP Oracle XSQLConfig.xml access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1872
// Rule Name: SERVER-WEBAPP Oracle Dynamic Monitoring Services dms access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1872
static inline int check_rule_1872(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dms0
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1872
static inline void alert_rule_1872(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1872;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle Dynamic Monitoring Services dms access", 
                     sizeof("SERVER-WEBAPP Oracle Dynamic Monitoring Services dms access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1873
// Rule Name: SERVER-WEBAPP globals.jsa access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1873
static inline int check_rule_1873(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /globals.jsa
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1873
static inline void alert_rule_1873(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1873;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP globals.jsa access", 
                     sizeof("SERVER-WEBAPP globals.jsa access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1874
// Rule Name: SERVER-WEBAPP Oracle Java Process Manager access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1874
static inline int check_rule_1874(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /oprocmgr-status
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1874
static inline void alert_rule_1874(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1874;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle Java Process Manager access", 
                     sizeof("SERVER-WEBAPP Oracle Java Process Manager access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1875
// Rule Name: SERVER-WEBAPP cgicso access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1875
static inline int check_rule_1875(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgicso",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1875
static inline void alert_rule_1875(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1875;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgicso access", 
                     sizeof("SERVER-WEBAPP cgicso access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1876
// Rule Name: SERVER-WEBAPP nph-publish.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1876
static inline int check_rule_1876(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nph-publish.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1876
static inline void alert_rule_1876(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1876;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nph-publish.cgi access", 
                     sizeof("SERVER-WEBAPP nph-publish.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1877
// Rule Name: SERVER-WEBAPP printenv access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1877
static inline int check_rule_1877(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /printenv",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1877
static inline void alert_rule_1877(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1877;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP printenv access", 
                     sizeof("SERVER-WEBAPP printenv access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1878
// Rule Name: SERVER-WEBAPP sdbsearch.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1878
static inline int check_rule_1878(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sdbsearch.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1878
static inline void alert_rule_1878(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1878;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sdbsearch.cgi access", 
                     sizeof("SERVER-WEBAPP sdbsearch.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1879
// Rule Name: SERVER-WEBAPP book.cgi arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1879
static inline int check_rule_1879(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/book.cgi",fast_pattern,nocase', 'current=|7C|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1879
static inline void alert_rule_1879(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1879;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP book.cgi arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP book.cgi arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1880
// Rule Name: SERVER-WEBAPP oracle web application server access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1880
static inline int check_rule_1880(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ows-bin/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1880
static inline void alert_rule_1880(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1880;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP oracle web application server access", 
                     sizeof("SERVER-WEBAPP oracle web application server access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1881
// Rule Name: SERVER-WEBAPP bad HTTP 1.1 request - potential worm attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1881
static inline int check_rule_1881(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1|0D 0A 0D 0A|",depth 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1881
static inline void alert_rule_1881(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1881;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bad HTTP 1.1 request - potential worm attack", 
                     sizeof("SERVER-WEBAPP bad HTTP 1.1 request - potential worm attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1887
// Rule Name: SERVER-OTHER OpenSSL Worm traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1887
static inline int check_rule_1887(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: TERM=xterm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1887
static inline void alert_rule_1887(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1887;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL Worm traffic", 
                     sizeof("SERVER-OTHER OpenSSL Worm traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1888
// Rule Name: PROTOCOL-FTP SITE CPWD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1888
static inline int check_rule_1888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'CPWD",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1888
static inline void alert_rule_1888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE CPWD overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE CPWD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1891
// Rule Name: PROTOCOL-RPC status GHBN format string attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1891
static inline int check_rule_1891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 B8|",depth 4,offset 16', '|00 00 00 02|",within 4,distance 4', '%x %x",within 256', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1891
static inline void alert_rule_1891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC status GHBN format string attack", 
                     sizeof("PROTOCOL-RPC status GHBN format string attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1894
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1894
static inline int check_rule_1894(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 749
    if (tcph->dest != htons(749))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 C0 05 08 00 C0 05 08 00 C0 05 08 00 C0 05 08|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1894
static inline void alert_rule_1894(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1894;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1895
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1895
static inline int check_rule_1895(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 751
    if (tcph->dest != htons(751))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 C0 05 08 00 C0 05 08 00 C0 05 08 00 C0 05 08|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1895
static inline void alert_rule_1895(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1895;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1896
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1896
static inline int check_rule_1896(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 749
    if (tcph->dest != htons(749))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FF|KADM0.0A|00 00 FB 03|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1896
static inline void alert_rule_1896(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1896;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1897
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1897
static inline int check_rule_1897(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 751
    if (tcph->dest != htons(751))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FF|KADM0.0A|00 00 FB 03|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1897
static inline void alert_rule_1897(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1897;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1898
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1898
static inline int check_rule_1898(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 749
    if (tcph->dest != htons(749))
        return 0;
    // Content matching (simplified)
    // Full pattern: /shh//bi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1898
static inline void alert_rule_1898(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1898;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1899
// Rule Name: INDICATOR-SHELLCODE kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: shellcode-detect
// Generated from Snort rule parser output

// Check function for rule 1899
static inline int check_rule_1899(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 751
    if (tcph->dest != htons(751))
        return 0;
    // Content matching (simplified)
    // Full pattern: /shh//bi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1899
static inline void alert_rule_1899(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1899;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-SHELLCODE kadmind buffer overflow attempt", 
                     sizeof("INDICATOR-SHELLCODE kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1900
// Rule Name: SERVER-OTHER successful kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 1900
static inline int check_rule_1900(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 749
    if (tcph->source != htons(749))
        return 0;
    // Content matching (simplified)
    // Full pattern: *GOBBLE*",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1900
static inline void alert_rule_1900(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1900;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER successful kadmind buffer overflow attempt", 
                     sizeof("SERVER-OTHER successful kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1901
// Rule Name: SERVER-OTHER successful kadmind buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 1901
static inline int check_rule_1901(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 751
    if (tcph->source != htons(751))
        return 0;
    // Content matching (simplified)
    // Full pattern: *GOBBLE*",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1901
static inline void alert_rule_1901(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1901;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER successful kadmind buffer overflow attempt", 
                     sizeof("SERVER-OTHER successful kadmind buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1902
// Rule Name: PROTOCOL-IMAP lsub literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1902
static inline int check_rule_1902(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LSUB",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1902
static inline void alert_rule_1902(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1902;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP lsub literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP lsub literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1903
// Rule Name: PROTOCOL-IMAP rename overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1903
static inline int check_rule_1903(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: RENAME",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1903
static inline void alert_rule_1903(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1903;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP rename overflow attempt", 
                     sizeof("PROTOCOL-IMAP rename overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1904
// Rule Name: PROTOCOL-IMAP find overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1904
static inline int check_rule_1904(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: FIND",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1904
static inline void alert_rule_1904(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1904;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP find overflow attempt", 
                     sizeof("PROTOCOL-IMAP find overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1906
// Rule Name: PROTOCOL-RPC AMD TCP amqproc_mount plog overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1906
static inline int check_rule_1906(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 500-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 500 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 04 93 F3|",depth 4,offset 16', '|00 00 00 07|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1906
static inline void alert_rule_1906(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1906;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC AMD TCP amqproc_mount plog overflow attempt", 
                     sizeof("PROTOCOL-RPC AMD TCP amqproc_mount plog overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1908
// Rule Name: PROTOCOL-RPC CMSD TCP CMSD_CREATE buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1908
static inline int check_rule_1908(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 E4|",depth 4,offset 16', '|00 00 00 15|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1908
static inline void alert_rule_1908(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1908;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC CMSD TCP CMSD_CREATE buffer overflow attempt", 
                     sizeof("PROTOCOL-RPC CMSD TCP CMSD_CREATE buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1909
// Rule Name: PROTOCOL-RPC CMSD TCP CMSD_INSERT buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1909
static inline int check_rule_1909(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 E4|",depth 4,offset 16', '|00 00 00 06|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1909
static inline void alert_rule_1909(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1909;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC CMSD TCP CMSD_INSERT buffer overflow attempt", 
                     sizeof("PROTOCOL-RPC CMSD TCP CMSD_INSERT buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1912
// Rule Name: PROTOCOL-RPC sadmind TCP NETMGT_PROC_SERVICE CLIENT_DOMAIN overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1912
static inline int check_rule_1912(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 87 88|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1912
static inline void alert_rule_1912(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1912;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC sadmind TCP NETMGT_PROC_SERVICE CLIENT_DOMAIN overflow attempt", 
                     sizeof("PROTOCOL-RPC sadmind TCP NETMGT_PROC_SERVICE CLIENT_DOMAIN overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1914
// Rule Name: PROTOCOL-RPC STATD TCP stat mon_name format string exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1914
static inline int check_rule_1914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 B8|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1914
static inline void alert_rule_1914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC STATD TCP stat mon_name format string exploit attempt", 
                     sizeof("PROTOCOL-RPC STATD TCP stat mon_name format string exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1916
// Rule Name: PROTOCOL-RPC STATD TCP monitor mon_name format string exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1916
static inline int check_rule_1916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 B8|",depth 4,offset 16', '|00 00 00 02|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1916
static inline void alert_rule_1916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC STATD TCP monitor mon_name format string exploit attempt", 
                     sizeof("PROTOCOL-RPC STATD TCP monitor mon_name format string exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1919
// Rule Name: PROTOCOL-FTP CWD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1919
static inline int check_rule_1919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: CWD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1919
static inline void alert_rule_1919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CWD overflow attempt", 
                     sizeof("PROTOCOL-FTP CWD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1920
// Rule Name: PROTOCOL-FTP SITE NEWER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1920
static inline int check_rule_1920(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'NEWER",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1920
static inline void alert_rule_1920(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1920;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE NEWER overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE NEWER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1921
// Rule Name: PROTOCOL-FTP SITE ZIPCHK overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1921
static inline int check_rule_1921(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'ZIPCHK",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1921
static inline void alert_rule_1921(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1921;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE ZIPCHK overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE ZIPCHK overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1922
// Rule Name: PROTOCOL-RPC portmap proxy attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1922
static inline int check_rule_1922(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 05|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1922
static inline void alert_rule_1922(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1922;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap proxy attempt TCP", 
                     sizeof("PROTOCOL-RPC portmap proxy attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1925
// Rule Name: PROTOCOL-RPC mountd TCP exportall request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1925
static inline int check_rule_1925(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 06|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1925
static inline void alert_rule_1925(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1925;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP exportall request", 
                     sizeof("PROTOCOL-RPC mountd TCP exportall request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1927
// Rule Name: PROTOCOL-FTP authorized_keys
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 1927
static inline int check_rule_1927(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: authorized_keys",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1927
static inline void alert_rule_1927(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1927;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP authorized_keys", 
                     sizeof("PROTOCOL-FTP authorized_keys"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1928
// Rule Name: PROTOCOL-FTP shadow retrieval attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-filename-detect
// Generated from Snort rule parser output

// Check function for rule 1928
static inline int check_rule_1928(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['RETR",nocase', 'shadow']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1928
static inline void alert_rule_1928(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1928;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP shadow retrieval attempt", 
                     sizeof("PROTOCOL-FTP shadow retrieval attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1930
// Rule Name: PROTOCOL-IMAP auth literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1930
static inline int check_rule_1930(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: AUTH",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1930
static inline void alert_rule_1930(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1930;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP auth literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP auth literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1931
// Rule Name: SERVER-WEBAPP rpc-nlog.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1931
static inline int check_rule_1931(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rpc-nlog.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1931
static inline void alert_rule_1931(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1931;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rpc-nlog.pl access", 
                     sizeof("SERVER-WEBAPP rpc-nlog.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1932
// Rule Name: SERVER-WEBAPP rpc-smb.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1932
static inline int check_rule_1932(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rpc-smb.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1932
static inline void alert_rule_1932(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1932;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rpc-smb.pl access", 
                     sizeof("SERVER-WEBAPP rpc-smb.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1933
// Rule Name: SERVER-WEBAPP cart.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1933
static inline int check_rule_1933(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cart.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1933
static inline void alert_rule_1933(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1933;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cart.cgi access", 
                     sizeof("SERVER-WEBAPP cart.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1936
// Rule Name: PROTOCOL-POP AUTH overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1936
static inline int check_rule_1936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: AUTH",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1936
static inline void alert_rule_1936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP AUTH overflow attempt", 
                     sizeof("PROTOCOL-POP AUTH overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1937
// Rule Name: PROTOCOL-POP LIST overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1937
static inline int check_rule_1937(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: LIST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1937
static inline void alert_rule_1937(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1937;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP LIST overflow attempt", 
                     sizeof("PROTOCOL-POP LIST overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1938
// Rule Name: PROTOCOL-POP XTND overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1938
static inline int check_rule_1938(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: XTND",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1938
static inline void alert_rule_1938(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1938;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP XTND overflow attempt", 
                     sizeof("PROTOCOL-POP XTND overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1942
// Rule Name: PROTOCOL-FTP RMDIR overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1942
static inline int check_rule_1942(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RMDIR",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1942
static inline void alert_rule_1942(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1942;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RMDIR overflow attempt", 
                     sizeof("PROTOCOL-FTP RMDIR overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1943
// Rule Name: SERVER-WEBAPP /Carello/add.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1943
static inline int check_rule_1943(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Carello/add.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1943
static inline void alert_rule_1943(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1943;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /Carello/add.exe access", 
                     sizeof("SERVER-WEBAPP /Carello/add.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1944
// Rule Name: SERVER-WEBAPP /ecscripts/ecware.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1944
static inline int check_rule_1944(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ecscripts/ecware.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1944
static inline void alert_rule_1944(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1944;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /ecscripts/ecware.exe access", 
                     sizeof("SERVER-WEBAPP /ecscripts/ecware.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1946
// Rule Name: SERVER-WEBAPP answerbook2 admin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1946
static inline int check_rule_1946(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8888
    if (tcph->dest != htons(8888))
        return 0;
    // Content matching (simplified)
    // Full pattern: /cgi-bin/admin/admin
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1946
static inline void alert_rule_1946(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1946;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP answerbook2 admin attempt", 
                     sizeof("SERVER-WEBAPP answerbook2 admin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1947
// Rule Name: SERVER-WEBAPP answerbook2 arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1947
static inline int check_rule_1947(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8888
    if (tcph->dest != htons(8888))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/ab2/', '|3B|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1947
static inline void alert_rule_1947(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1947;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP answerbook2 arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP answerbook2 arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1949
// Rule Name: PROTOCOL-RPC portmap SET attempt TCP 111
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1949
static inline int check_rule_1949(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1949
static inline void alert_rule_1949(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1949;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap SET attempt TCP 111", 
                     sizeof("PROTOCOL-RPC portmap SET attempt TCP 111"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1951
// Rule Name: PROTOCOL-RPC mountd TCP mount request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 1951
static inline int check_rule_1951(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1951
static inline void alert_rule_1951(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1951;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP mount request", 
                     sizeof("PROTOCOL-RPC mountd TCP mount request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1953
// Rule Name: PROTOCOL-RPC AMD TCP pid request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1953
static inline int check_rule_1953(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 500-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 500 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 04 93 F3|",depth 4,offset 16', '|00 00 00 09|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1953
static inline void alert_rule_1953(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1953;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC AMD TCP pid request", 
                     sizeof("PROTOCOL-RPC AMD TCP pid request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1955
// Rule Name: PROTOCOL-RPC AMD TCP version request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1955
static inline int check_rule_1955(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 500-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 500 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 04 93 F3|",depth 4,offset 16', '|00 00 00 08|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1955
static inline void alert_rule_1955(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1955;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC AMD TCP version request", 
                     sizeof("PROTOCOL-RPC AMD TCP version request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1958
// Rule Name: PROTOCOL-RPC sadmind TCP PING
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1958
static inline int check_rule_1958(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 87 88|",depth 4,offset 16', '|00 00 00 00|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1958
static inline void alert_rule_1958(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1958;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC sadmind TCP PING", 
                     sizeof("PROTOCOL-RPC sadmind TCP PING"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1960
// Rule Name: PROTOCOL-RPC portmap NFS request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1960
static inline int check_rule_1960(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 A3|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1960
static inline void alert_rule_1960(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1960;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap NFS request TCP", 
                     sizeof("PROTOCOL-RPC portmap NFS request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1962
// Rule Name: PROTOCOL-RPC portmap RQUOTA request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 1962
static inline int check_rule_1962(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 AB|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1962
static inline void alert_rule_1962(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1962;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap RQUOTA request TCP", 
                     sizeof("PROTOCOL-RPC portmap RQUOTA request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1965
// Rule Name: PROTOCOL-RPC tooltalk TCP overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1965
static inline int check_rule_1965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 F3|",depth 4,offset 16', '|00 00 00 07|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1965
static inline void alert_rule_1965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC tooltalk TCP overflow attempt", 
                     sizeof("PROTOCOL-RPC tooltalk TCP overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1967
// Rule Name: SERVER-WEBAPP phpbb quick-reply.php arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1967
static inline int check_rule_1967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/quick-reply.php', 'phpbb_root_path=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1967
static inline void alert_rule_1967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phpbb quick-reply.php arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP phpbb quick-reply.php arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1968
// Rule Name: SERVER-WEBAPP phpbb quick-reply.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1968
static inline int check_rule_1968(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /quick-reply.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1968
static inline void alert_rule_1968(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1968;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phpbb quick-reply.php access", 
                     sizeof("SERVER-WEBAPP phpbb quick-reply.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1969
// Rule Name: SERVER-WEBAPP ion-p access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1969
static inline int check_rule_1969(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ion-p",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1969
static inline void alert_rule_1969(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1969;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ion-p access", 
                     sizeof("SERVER-WEBAPP ion-p access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1970
// Rule Name: SERVER-IIS MDAC Content-Type overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1970
static inline int check_rule_1970(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/msadcs.dll",nocase', 'Content-Type|3A|",nocase', '!"|0A|",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1970
static inline void alert_rule_1970(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1970;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS MDAC Content-Type overflow attempt", 
                     sizeof("SERVER-IIS MDAC Content-Type overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1971
// Rule Name: PROTOCOL-FTP SITE EXEC format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 1971
static inline int check_rule_1971(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'EXEC",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1971
static inline void alert_rule_1971(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1971;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE EXEC format string attempt", 
                     sizeof("PROTOCOL-FTP SITE EXEC format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1972
// Rule Name: PROTOCOL-FTP PASS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1972
static inline int check_rule_1972(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1972
static inline void alert_rule_1972(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1972;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP PASS overflow attempt", 
                     sizeof("PROTOCOL-FTP PASS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1973
// Rule Name: PROTOCOL-FTP MKD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1973
static inline int check_rule_1973(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MKD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1973
static inline void alert_rule_1973(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1973;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP MKD overflow attempt", 
                     sizeof("PROTOCOL-FTP MKD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1974
// Rule Name: PROTOCOL-FTP REST overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1974
static inline int check_rule_1974(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: REST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1974
static inline void alert_rule_1974(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1974;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP REST overflow attempt", 
                     sizeof("PROTOCOL-FTP REST overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1975
// Rule Name: PROTOCOL-FTP DELE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1975
static inline int check_rule_1975(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: DELE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1975
static inline void alert_rule_1975(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1975;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP DELE overflow attempt", 
                     sizeof("PROTOCOL-FTP DELE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1976
// Rule Name: PROTOCOL-FTP RMD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 1976
static inline int check_rule_1976(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RMD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1976
static inline void alert_rule_1976(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1976;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RMD overflow attempt", 
                     sizeof("PROTOCOL-FTP RMD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1977
// Rule Name: SERVER-WEBAPP xp_regwrite attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1977
static inline int check_rule_1977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_regwrite",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1977
static inline void alert_rule_1977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP xp_regwrite attempt", 
                     sizeof("SERVER-WEBAPP xp_regwrite attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1978
// Rule Name: SERVER-WEBAPP xp_regdeletekey attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1978
static inline int check_rule_1978(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: xp_regdeletekey",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1978
static inline void alert_rule_1978(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1978;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP xp_regdeletekey attempt", 
                     sizeof("SERVER-WEBAPP xp_regdeletekey attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1979
// Rule Name: SERVER-WEBAPP perl post attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 1979
static inline int check_rule_1979(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST",depth 4', '/perl/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1979
static inline void alert_rule_1979(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1979;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP perl post attempt", 
                     sizeof("SERVER-WEBAPP perl post attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1985
// Rule Name: MALWARE-BACKDOOR Doly variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 1985
static inline int check_rule_1985(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: * Doly trojan v1.5 - Connected.",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1985
static inline void alert_rule_1985(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1985;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Doly variant outbound connection attempt", 
                     sizeof("MALWARE-BACKDOOR Doly variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1986
// Rule Name: POLICY-SOCIAL Microsoft MSN outbound file transfer request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1986
static inline int check_rule_1986(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 1863
    if (tcph->dest != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MSG ",depth 4', 'Content-Type|3A| application/x-msnmsgrp2p",nocase', 'INVITE",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1986
static inline void alert_rule_1986(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1986;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN outbound file transfer request", 
                     sizeof("POLICY-SOCIAL Microsoft MSN outbound file transfer request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1987
// Rule Name: SERVER-OTHER xfs overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 1987
static inline int check_rule_1987(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7100
    if (tcph->dest != htons(7100))
        return 0;
    // Content matching (simplified)
    // Full pattern: B|00 02|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1987
static inline void alert_rule_1987(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1987;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER xfs overflow attempt", 
                     sizeof("SERVER-OTHER xfs overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1988
// Rule Name: POLICY-SOCIAL Microsoft MSN outbound file transfer accept
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1988
static inline int check_rule_1988(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 1863
    if (tcph->source != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MSG ",depth 4', 'Content-Type|3A| application/x-msnmsgrp2p",distance 0,nocase', 'MSNSLP/1.0 200 OK",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1988
static inline void alert_rule_1988(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1988;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN outbound file transfer accept", 
                     sizeof("POLICY-SOCIAL Microsoft MSN outbound file transfer accept"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1989
// Rule Name: POLICY-SOCIAL Microsoft MSN outbound file transfer rejected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1989
static inline int check_rule_1989(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 1863
    if (tcph->source != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MSG ",depth 4', 'Content-Type|3A| application/x-msnmsgrp2p",distance 0,nocase', 'MSNSLP/1.0 603 Decline",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1989
static inline void alert_rule_1989(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1989;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN outbound file transfer rejected", 
                     sizeof("POLICY-SOCIAL Microsoft MSN outbound file transfer rejected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1990
// Rule Name: POLICY-SOCIAL Microsoft MSN user search
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1990
static inline int check_rule_1990(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 1863
    if (tcph->dest != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: CAL ",depth 4,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1990
static inline void alert_rule_1990(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1990;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN user search", 
                     sizeof("POLICY-SOCIAL Microsoft MSN user search"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1991
// Rule Name: POLICY-SOCIAL Microsoft MSN login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 1991
static inline int check_rule_1991(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 1863
    if (tcph->dest != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['USR ",depth 4,nocase', ' TWN ",distance 1,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1991
static inline void alert_rule_1991(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1991;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Microsoft MSN login attempt", 
                     sizeof("POLICY-SOCIAL Microsoft MSN login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1992
// Rule Name: PROTOCOL-FTP LIST directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 1992
static inline int check_rule_1992(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['LIST",nocase', '..",distance 1', '..",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1992
static inline void alert_rule_1992(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1992;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP LIST directory traversal attempt", 
                     sizeof("PROTOCOL-FTP LIST directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1993
// Rule Name: PROTOCOL-IMAP login literal buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 1993
static inline int check_rule_1993(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1993
static inline void alert_rule_1993(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1993;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP login literal buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP login literal buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1994
// Rule Name: SERVER-WEBAPP vpasswd.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1994
static inline int check_rule_1994(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /vpasswd.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1994
static inline void alert_rule_1994(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1994;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP vpasswd.cgi access", 
                     sizeof("SERVER-WEBAPP vpasswd.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1995
// Rule Name: SERVER-WEBAPP alya.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1995
static inline int check_rule_1995(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /alya.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1995
static inline void alert_rule_1995(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1995;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alya.cgi access", 
                     sizeof("SERVER-WEBAPP alya.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1996
// Rule Name: SERVER-WEBAPP viralator.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1996
static inline int check_rule_1996(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /viralator.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1996
static inline void alert_rule_1996(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1996;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP viralator.cgi access", 
                     sizeof("SERVER-WEBAPP viralator.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1997
// Rule Name: SERVER-WEBAPP read_body.php access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1997
static inline int check_rule_1997(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /read_body.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1997
static inline void alert_rule_1997(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1997;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP read_body.php access attempt", 
                     sizeof("SERVER-WEBAPP read_body.php access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1998
// Rule Name: SERVER-WEBAPP calendar.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1998
static inline int check_rule_1998(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calendar.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1998
static inline void alert_rule_1998(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1998;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP calendar.php access", 
                     sizeof("SERVER-WEBAPP calendar.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 1999
// Rule Name: SERVER-WEBAPP edit_image.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 1999
static inline int check_rule_1999(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /edit_image.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 1999
static inline void alert_rule_1999(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 1999;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP edit_image.php access", 
                     sizeof("SERVER-WEBAPP edit_image.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2000
// Rule Name: SERVER-WEBAPP readmsg.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2000
static inline int check_rule_2000(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /readmsg.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2000
static inline void alert_rule_2000(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2000;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP readmsg.php access", 
                     sizeof("SERVER-WEBAPP readmsg.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2001
// Rule Name: SERVER-WEBAPP smartsearch.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2001
static inline int check_rule_2001(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /smartsearch.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2001
static inline void alert_rule_2001(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2001;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP smartsearch.cgi access", 
                     sizeof("SERVER-WEBAPP smartsearch.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2002
// Rule Name: SERVER-WEBAPP remote include path attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2002
static inline int check_rule_2002(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php",nocase', 'path=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2002
static inline void alert_rule_2002(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2002;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP remote include path attempt", 
                     sizeof("SERVER-WEBAPP remote include path attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2006
// Rule Name: PROTOCOL-RPC portmap kcms_server request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2006
static inline int check_rule_2006(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 87|}",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2006
static inline void alert_rule_2006(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2006;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap kcms_server request TCP", 
                     sizeof("PROTOCOL-RPC portmap kcms_server request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2007
// Rule Name: PROTOCOL-RPC kcms_server directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2007
static inline int check_rule_2007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 32771-34000
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 32771 || dst_port > 34000)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 87|}",depth 4,offset 16', '/../",distance 0', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2007
static inline void alert_rule_2007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC kcms_server directory traversal attempt", 
                     sizeof("PROTOCOL-RPC kcms_server directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2008
// Rule Name: INDICATOR-COMPROMISE CVS invalid user authentication response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2008
static inline int check_rule_2008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['E Fatal error, aborting.",fast_pattern,nocase', '|3A| no such user']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2008
static inline void alert_rule_2008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS invalid user authentication response", 
                     sizeof("INDICATOR-COMPROMISE CVS invalid user authentication response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2009
// Rule Name: INDICATOR-COMPROMISE CVS invalid repository response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2009
static inline int check_rule_2009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['error ', '|3A| no such repository', 'I HATE YOU",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2009
static inline void alert_rule_2009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS invalid repository response", 
                     sizeof("INDICATOR-COMPROMISE CVS invalid repository response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2010
// Rule Name: INDICATOR-COMPROMISE CVS double free exploit attempt response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2010
static inline int check_rule_2010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: free|28 29 3A| warning|3A| chunk is already free",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2010
static inline void alert_rule_2010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS double free exploit attempt response", 
                     sizeof("INDICATOR-COMPROMISE CVS double free exploit attempt response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2011
// Rule Name: INDICATOR-COMPROMISE CVS invalid directory response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2011
static inline int check_rule_2011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: E protocol error|3A| invalid directory syntax in",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2011
static inline void alert_rule_2011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS invalid directory response", 
                     sizeof("INDICATOR-COMPROMISE CVS invalid directory response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2012
// Rule Name: INDICATOR-COMPROMISE CVS missing cvsroot response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2012
static inline int check_rule_2012(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: E protocol error|3A| Root request missing",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2012
static inline void alert_rule_2012(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2012;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS missing cvsroot response", 
                     sizeof("INDICATOR-COMPROMISE CVS missing cvsroot response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2013
// Rule Name: INDICATOR-COMPROMISE CVS invalid module response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2013
static inline int check_rule_2013(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['cvs server|3A| cannot find module",fast_pattern,nocase', 'error']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2013
static inline void alert_rule_2013(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2013;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS invalid module response", 
                     sizeof("INDICATOR-COMPROMISE CVS invalid module response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2014
// Rule Name: PROTOCOL-RPC portmap UNSET attempt TCP 111
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2014
static inline int check_rule_2014(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 02|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2014
static inline void alert_rule_2014(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2014;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap UNSET attempt TCP 111", 
                     sizeof("PROTOCOL-RPC portmap UNSET attempt TCP 111"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2016
// Rule Name: PROTOCOL-RPC portmap status request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2016
static inline int check_rule_2016(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 B8|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2016
static inline void alert_rule_2016(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2016;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap status request TCP", 
                     sizeof("PROTOCOL-RPC portmap status request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2018
// Rule Name: PROTOCOL-RPC mountd TCP dump request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2018
static inline int check_rule_2018(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 02|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2018
static inline void alert_rule_2018(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2018;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP dump request", 
                     sizeof("PROTOCOL-RPC mountd TCP dump request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2020
// Rule Name: PROTOCOL-RPC mountd TCP unmount request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2020
static inline int check_rule_2020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2020
static inline void alert_rule_2020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP unmount request", 
                     sizeof("PROTOCOL-RPC mountd TCP unmount request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2022
// Rule Name: PROTOCOL-RPC mountd TCP unmountall request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2022
static inline int check_rule_2022(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5|",depth 4,offset 16', '|00 00 00 04|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2022
static inline void alert_rule_2022(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2022;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP unmountall request", 
                     sizeof("PROTOCOL-RPC mountd TCP unmountall request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2024
// Rule Name: PROTOCOL-RPC RQUOTA getquota overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2024
static inline int check_rule_2024(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 AB|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2024
static inline void alert_rule_2024(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2024;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC RQUOTA getquota overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC RQUOTA getquota overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2026
// Rule Name: PROTOCOL-RPC yppasswd username overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2026
static inline int check_rule_2026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A9|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2026
static inline void alert_rule_2026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC yppasswd username overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC yppasswd username overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2028
// Rule Name: PROTOCOL-RPC yppasswd old password overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2028
static inline int check_rule_2028(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A9|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2028
static inline void alert_rule_2028(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2028;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC yppasswd old password overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC yppasswd old password overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2030
// Rule Name: PROTOCOL-RPC yppasswd new password overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2030
static inline int check_rule_2030(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A9|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2030
static inline void alert_rule_2030(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2030;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC yppasswd new password overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC yppasswd new password overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2032
// Rule Name: PROTOCOL-RPC yppasswd user update TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2032
static inline int check_rule_2032(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A9|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2032
static inline void alert_rule_2032(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2032;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC yppasswd user update TCP", 
                     sizeof("PROTOCOL-RPC yppasswd user update TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2034
// Rule Name: PROTOCOL-RPC ypserv maplist request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2034
static inline int check_rule_2034(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A4|",depth 4,offset 16', '|00 00 00 0B|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2034
static inline void alert_rule_2034(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2034;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC ypserv maplist request TCP", 
                     sizeof("PROTOCOL-RPC ypserv maplist request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2036
// Rule Name: PROTOCOL-RPC portmap network-status-monitor request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2036
static inline int check_rule_2036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 03 0D|p",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2036
static inline void alert_rule_2036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap network-status-monitor request TCP", 
                     sizeof("PROTOCOL-RPC portmap network-status-monitor request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2038
// Rule Name: PROTOCOL-RPC network-status-monitor mon-callback request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2038
static inline int check_rule_2038(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 03 0D|p",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2038
static inline void alert_rule_2038(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2038;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC network-status-monitor mon-callback request TCP", 
                     sizeof("PROTOCOL-RPC network-status-monitor mon-callback request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2044
// Rule Name: POLICY-OTHER PPTP Start Control Request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2044
static inline int check_rule_2044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1723
    if (tcph->dest != htons(1723))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01|",depth 2,offset 2', '|00 01|",depth 2,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2044
static inline void alert_rule_2044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER PPTP Start Control Request attempt", 
                     sizeof("POLICY-OTHER PPTP Start Control Request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2046
// Rule Name: PROTOCOL-IMAP partial body.peek buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2046
static inline int check_rule_2046(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PARTIAL",nocase', 'BODY.PEEK[",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2046
static inline void alert_rule_2046(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2046;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP partial body.peek buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP partial body.peek buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2047
// Rule Name: SERVER-OTHER rsyncd module list access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2047
static inline int check_rule_2047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 873
    if (tcph->dest != htons(873))
        return 0;
    // Content matching (simplified)
    // Full pattern: |23|list",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2047
static inline void alert_rule_2047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER rsyncd module list access", 
                     sizeof("SERVER-OTHER rsyncd module list access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2051
// Rule Name: SERVER-WEBAPP cached_feed.cgi moreover shopping cart access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2051
static inline int check_rule_2051(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cached_feed.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2051
static inline void alert_rule_2051(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2051;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cached_feed.cgi moreover shopping cart access", 
                     sizeof("SERVER-WEBAPP cached_feed.cgi moreover shopping cart access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2052
// Rule Name: SERVER-WEBAPP overflow.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2052
static inline int check_rule_2052(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /overflow.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2052
static inline void alert_rule_2052(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2052;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP overflow.cgi access", 
                     sizeof("SERVER-WEBAPP overflow.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2053
// Rule Name: SERVER-WEBAPP Bugtraq process_bug.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2053
static inline int check_rule_2053(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /process_bug.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2053
static inline void alert_rule_2053(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2053;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Bugtraq process_bug.cgi access", 
                     sizeof("SERVER-WEBAPP Bugtraq process_bug.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2054
// Rule Name: SERVER-WEBAPP Bugtraq enter_bug.cgi arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2054
static inline int check_rule_2054(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/enter_bug.cgi",fast_pattern,nocase', 'who=', '|3B|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2054
static inline void alert_rule_2054(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2054;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Bugtraq enter_bug.cgi arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP Bugtraq enter_bug.cgi arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2055
// Rule Name: SERVER-WEBAPP Bugtraq enter_bug.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2055
static inline int check_rule_2055(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /enter_bug.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2055
static inline void alert_rule_2055(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2055;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Bugtraq enter_bug.cgi access", 
                     sizeof("SERVER-WEBAPP Bugtraq enter_bug.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2056
// Rule Name: SERVER-WEBAPP TRACE attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2056
static inline int check_rule_2056(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: TRACE",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2056
static inline void alert_rule_2056(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2056;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TRACE attempt", 
                     sizeof("SERVER-WEBAPP TRACE attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2057
// Rule Name: SERVER-WEBAPP helpout.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2057
static inline int check_rule_2057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /helpout.exe
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2057
static inline void alert_rule_2057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP helpout.exe access", 
                     sizeof("SERVER-WEBAPP helpout.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2058
// Rule Name: SERVER-WEBAPP MsmMask.exe attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2058
static inline int check_rule_2058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/MsmMask.exe', 'mask=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2058
static inline void alert_rule_2058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MsmMask.exe attempt", 
                     sizeof("SERVER-WEBAPP MsmMask.exe attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2059
// Rule Name: SERVER-WEBAPP MsmMask.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2059
static inline int check_rule_2059(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /MsmMask.exe
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2059
static inline void alert_rule_2059(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2059;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MsmMask.exe access", 
                     sizeof("SERVER-WEBAPP MsmMask.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2060
// Rule Name: SERVER-WEBAPP DB4Web access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2060
static inline int check_rule_2060(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /DB4Web/
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2060
static inline void alert_rule_2060(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2060;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DB4Web access", 
                     sizeof("SERVER-WEBAPP DB4Web access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2061
// Rule Name: SERVER-APACHE Apache Tomcat null byte directory listing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2061
static inline int check_rule_2061(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |00|.jsp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2061
static inline void alert_rule_2061(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2061;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat null byte directory listing attempt", 
                     sizeof("SERVER-APACHE Apache Tomcat null byte directory listing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2062
// Rule Name: SERVER-WEBAPP iPlanet .perf access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2062
static inline int check_rule_2062(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.perf
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2062
static inline void alert_rule_2062(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2062;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP iPlanet .perf access", 
                     sizeof("SERVER-WEBAPP iPlanet .perf access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2063
// Rule Name: SERVER-WEBAPP Demarc SQL injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2063
static inline int check_rule_2063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dm/demarc', 's_key=', '\'",distance 0', '\'",distance 1', '\'",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2063
static inline void alert_rule_2063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Demarc SQL injection attempt", 
                     sizeof("SERVER-WEBAPP Demarc SQL injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2065
// Rule Name: SERVER-WEBAPP Lotus Notes .csp script source download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2065
static inline int check_rule_2065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .csp.
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2065
static inline void alert_rule_2065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus Notes .csp script source download attempt", 
                     sizeof("SERVER-WEBAPP Lotus Notes .csp script source download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2066
// Rule Name: SERVER-WEBAPP Lotus Notes .pl script source download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2066
static inline int check_rule_2066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.pl', '.pl', '.",within 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2066
static inline void alert_rule_2066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus Notes .pl script source download attempt", 
                     sizeof("SERVER-WEBAPP Lotus Notes .pl script source download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2067
// Rule Name: SERVER-WEBAPP Lotus Notes .exe script source download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2067
static inline int check_rule_2067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe', '.exe', '.",within 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2067
static inline void alert_rule_2067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lotus Notes .exe script source download attempt", 
                     sizeof("SERVER-WEBAPP Lotus Notes .exe script source download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2068
// Rule Name: SERVER-WEBAPP BitKeeper arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2068
static inline int check_rule_2068(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/diffs/', "'", '|3B|",distance 0', '\'",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2068
static inline void alert_rule_2068(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2068;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BitKeeper arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP BitKeeper arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2069
// Rule Name: SERVER-WEBAPP chip.ini access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2069
static inline int check_rule_2069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /chip.ini
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2069
static inline void alert_rule_2069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP chip.ini access", 
                     sizeof("SERVER-WEBAPP chip.ini access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2070
// Rule Name: SERVER-WEBAPP post32.exe arbitrary command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2070
static inline int check_rule_2070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /post32.exe|7C|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2070
static inline void alert_rule_2070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP post32.exe arbitrary command attempt", 
                     sizeof("SERVER-WEBAPP post32.exe arbitrary command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2071
// Rule Name: SERVER-WEBAPP post32.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2071
static inline int check_rule_2071(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /post32.exe
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2071
static inline void alert_rule_2071(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2071;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP post32.exe access", 
                     sizeof("SERVER-WEBAPP post32.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2072
// Rule Name: SERVER-WEBAPP lyris.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2072
static inline int check_rule_2072(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /lyris.pl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2072
static inline void alert_rule_2072(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2072;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP lyris.pl access", 
                     sizeof("SERVER-WEBAPP lyris.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2073
// Rule Name: SERVER-WEBAPP globals.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2073
static inline int check_rule_2073(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /globals.pl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2073
static inline void alert_rule_2073(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2073;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP globals.pl access", 
                     sizeof("SERVER-WEBAPP globals.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2074
// Rule Name: SERVER-WEBAPP Mambo uploadimage.php upload php file attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2074
static inline int check_rule_2074(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/uploadimage.php', 'userfile_name=', '.php",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2074
static inline void alert_rule_2074(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2074;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Mambo uploadimage.php upload php file attempt", 
                     sizeof("SERVER-WEBAPP Mambo uploadimage.php upload php file attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2075
// Rule Name: SERVER-WEBAPP Mambo upload.php upload php file attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2075
static inline int check_rule_2075(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/upload.php', 'userfile_name=', '.php",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2075
static inline void alert_rule_2075(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2075;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Mambo upload.php upload php file attempt", 
                     sizeof("SERVER-WEBAPP Mambo upload.php upload php file attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2076
// Rule Name: SERVER-WEBAPP Mambo uploadimage.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2076
static inline int check_rule_2076(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /uploadimage.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2076
static inline void alert_rule_2076(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2076;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Mambo uploadimage.php access", 
                     sizeof("SERVER-WEBAPP Mambo uploadimage.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2077
// Rule Name: SERVER-WEBAPP Mambo upload.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2077
static inline int check_rule_2077(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /upload.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2077
static inline void alert_rule_2077(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2077;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Mambo upload.php access", 
                     sizeof("SERVER-WEBAPP Mambo upload.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2078
// Rule Name: SERVER-WEBAPP phpBB privmsg.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2078
static inline int check_rule_2078(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /privmsg.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2078
static inline void alert_rule_2078(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2078;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phpBB privmsg.php access", 
                     sizeof("SERVER-WEBAPP phpBB privmsg.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2080
// Rule Name: PROTOCOL-RPC portmap nlockmgr request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2080
static inline int check_rule_2080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 01 86 B5|",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2080
static inline void alert_rule_2080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap nlockmgr request TCP", 
                     sizeof("PROTOCOL-RPC portmap nlockmgr request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2082
// Rule Name: PROTOCOL-RPC portmap rpc.xfsmd request TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2082
static inline int check_rule_2082(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0|",depth 4,offset 16', '|00 00 00 03|",within 4,distance 4', '|00 05 F7|h",within 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2082
static inline void alert_rule_2082(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2082;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap rpc.xfsmd request TCP", 
                     sizeof("PROTOCOL-RPC portmap rpc.xfsmd request TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2084
// Rule Name: PROTOCOL-RPC rpc.xfsmd xfs_export attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2084
static inline int check_rule_2084(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 05 F7|h",depth 4,offset 16', '|00 00 00 0D|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2084
static inline void alert_rule_2084(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2084;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC rpc.xfsmd xfs_export attempt TCP", 
                     sizeof("PROTOCOL-RPC rpc.xfsmd xfs_export attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2085
// Rule Name: SERVER-WEBAPP parse_xml.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2085
static inline int check_rule_2085(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /parse_xml.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2085
static inline void alert_rule_2085(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2085;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP parse_xml.cgi access", 
                     sizeof("SERVER-WEBAPP parse_xml.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2086
// Rule Name: SERVER-WEBAPP streaming server parse_xml.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2086
static inline int check_rule_2086(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /parse_xml.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2086
static inline void alert_rule_2086(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2086;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP streaming server parse_xml.cgi access", 
                     sizeof("SERVER-WEBAPP streaming server parse_xml.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2087
// Rule Name: SERVER-MAIL From comment overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2087
static inline int check_rule_2087(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['From|3A|",nocase', '<><><><><><><><><><><><><><><><><><><><><><>",distance 0', '|28|",distance 1', '|29|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2087
static inline void alert_rule_2087(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2087;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL From comment overflow attempt", 
                     sizeof("SERVER-MAIL From comment overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2089
// Rule Name: PROTOCOL-RPC ypupdated arbitrary command attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2089
static inline int check_rule_2089(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 BC|",depth 4,offset 16', '|00 00 00 01|",within 4,distance 4', '|7C|",distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2089
static inline void alert_rule_2089(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2089;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC ypupdated arbitrary command attempt TCP", 
                     sizeof("PROTOCOL-RPC ypupdated arbitrary command attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2090
// Rule Name: SERVER-IIS WEBDAV exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2090
static inline int check_rule_2090(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HTTP/1.1|0A|Content-type|3A| text/xml|0A|HOST|3A|', 'Accept|3A| */*|0A|Translate|3A| f|0A|Content-length|3A|5276|0A 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2090
static inline void alert_rule_2090(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2090;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS WEBDAV exploit attempt", 
                     sizeof("SERVER-IIS WEBDAV exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2091
// Rule Name: SERVER-IIS WEBDAV nessus safe scan attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2091
static inline int check_rule_2091(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['SEARCH / HTTP/1.1|0D 0A|Host|3A|', '|0D 0A 0D 0A|",within 255']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2091
static inline void alert_rule_2091(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2091;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS WEBDAV nessus safe scan attempt", 
                     sizeof("SERVER-IIS WEBDAV nessus safe scan attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2093
// Rule Name: PROTOCOL-RPC portmap proxy integer overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: rpc-portmap-decode
// Generated from Snort rule parser output

// Check function for rule 2093
static inline int check_rule_2093(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 111
    if (tcph->dest != htons(111))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A0 00|",depth 5,offset 16', '|00 00 00 05|",within 4,distance 3', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2093
static inline void alert_rule_2093(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2093;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC portmap proxy integer overflow attempt TCP", 
                     sizeof("PROTOCOL-RPC portmap proxy integer overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2095
// Rule Name: PROTOCOL-RPC CMSD TCP CMSD_CREATE array buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2095
static inline int check_rule_2095(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 E4|",depth 4,offset 16', '|00 00 00 15|",within 4,distance 4', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2095
static inline void alert_rule_2095(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2095;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC CMSD TCP CMSD_CREATE array buffer overflow attempt", 
                     sizeof("PROTOCOL-RPC CMSD TCP CMSD_CREATE array buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2100
// Rule Name: MALWARE-BACKDOOR SubSeven 2.1 Gold server connection response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 2100
static inline int check_rule_2100(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['connected. time/date|3A| ",depth 22', 'version|3A| GOLD 2.1",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2100
static inline void alert_rule_2100(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2100;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR SubSeven 2.1 Gold server connection response", 
                     sizeof("MALWARE-BACKDOOR SubSeven 2.1 Gold server connection response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2101
// Rule Name: OS-WINDOWS Microsoft Windows SMB Trans Max Param/Count OS-WINDOWS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2101
static inline int check_rule_2101(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB%",within 5,distance 3', '|00 00 00 00|",within 4,distance 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2101
static inline void alert_rule_2101(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2101;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB Trans Max Param/Count OS-WINDOWS attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB Trans Max Param/Count OS-WINDOWS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2103
// Rule Name: NETBIOS SMB Trans2 OPEN2 unicode maximum param count overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2103
static inline int check_rule_2103(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3', '|00 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2103
static inline void alert_rule_2103(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2103;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 OPEN2 unicode maximum param count overflow attempt", 
                     sizeof("NETBIOS SMB Trans2 OPEN2 unicode maximum param count overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2104
// Rule Name: INDICATOR-COMPROMISE rexec username too long response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 2104
static inline int check_rule_2104(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 512
    if (tcph->source != htons(512))
        return 0;
    // Content matching (simplified)
    // Full pattern: username too long",depth 17
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2104
static inline void alert_rule_2104(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2104;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE rexec username too long response", 
                     sizeof("INDICATOR-COMPROMISE rexec username too long response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2105
// Rule Name: PROTOCOL-IMAP authenticate literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2105
static inline int check_rule_2105(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: AUTHENTICATE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2105
static inline void alert_rule_2105(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2105;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP authenticate literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP authenticate literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2106
// Rule Name: PROTOCOL-IMAP lsub overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2106
static inline int check_rule_2106(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LSUB
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2106
static inline void alert_rule_2106(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2106;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP lsub overflow attempt", 
                     sizeof("PROTOCOL-IMAP lsub overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2107
// Rule Name: PROTOCOL-IMAP create buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2107
static inline int check_rule_2107(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: CREATE
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2107
static inline void alert_rule_2107(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2107;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP create buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP create buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2108
// Rule Name: PROTOCOL-POP CAPA overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2108
static inline int check_rule_2108(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: CAPA",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2108
static inline void alert_rule_2108(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2108;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP CAPA overflow attempt", 
                     sizeof("PROTOCOL-POP CAPA overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2109
// Rule Name: PROTOCOL-POP TOP overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2109
static inline int check_rule_2109(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: TOP",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2109
static inline void alert_rule_2109(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2109;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP TOP overflow attempt", 
                     sizeof("PROTOCOL-POP TOP overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2110
// Rule Name: PROTOCOL-POP STAT overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2110
static inline int check_rule_2110(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: STAT",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2110
static inline void alert_rule_2110(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2110;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP STAT overflow attempt", 
                     sizeof("PROTOCOL-POP STAT overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2111
// Rule Name: PROTOCOL-POP DELE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2111
static inline int check_rule_2111(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: DELE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2111
static inline void alert_rule_2111(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2111;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP DELE overflow attempt", 
                     sizeof("PROTOCOL-POP DELE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2112
// Rule Name: PROTOCOL-POP RSET overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2112
static inline int check_rule_2112(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: RSET",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2112
static inline void alert_rule_2112(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2112;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP RSET overflow attempt", 
                     sizeof("PROTOCOL-POP RSET overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2113
// Rule Name: PROTOCOL-SERVICES rexec username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2113
static inline int check_rule_2113(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 512
    if (tcph->dest != htons(512))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",offset 9', '|00|",distance 0', '|00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2113
static inline void alert_rule_2113(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2113;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rexec username overflow attempt", 
                     sizeof("PROTOCOL-SERVICES rexec username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2114
// Rule Name: PROTOCOL-SERVICES rexec password overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2114
static inline int check_rule_2114(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 512
    if (tcph->dest != htons(512))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|', '|00|",distance 33', '|00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2114
static inline void alert_rule_2114(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2114;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SERVICES rexec password overflow attempt", 
                     sizeof("PROTOCOL-SERVICES rexec password overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2115
// Rule Name: SERVER-WEBAPP album.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2115
static inline int check_rule_2115(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /album.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2115
static inline void alert_rule_2115(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2115;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP album.pl access", 
                     sizeof("SERVER-WEBAPP album.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2116
// Rule Name: SERVER-WEBAPP chipcfg.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2116
static inline int check_rule_2116(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /chipcfg.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2116
static inline void alert_rule_2116(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2116;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP chipcfg.cgi access", 
                     sizeof("SERVER-WEBAPP chipcfg.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2117
// Rule Name: SERVER-IIS Battleaxe Forum login.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2117
static inline int check_rule_2117(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: myaccount/login.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2117
static inline void alert_rule_2117(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2117;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Battleaxe Forum login.asp access", 
                     sizeof("SERVER-IIS Battleaxe Forum login.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2118
// Rule Name: PROTOCOL-IMAP list overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2118
static inline int check_rule_2118(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LIST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2118
static inline void alert_rule_2118(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2118;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP list overflow attempt", 
                     sizeof("PROTOCOL-IMAP list overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2119
// Rule Name: PROTOCOL-IMAP rename literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2119
static inline int check_rule_2119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: RENAME",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2119
static inline void alert_rule_2119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP rename literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP rename literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2120
// Rule Name: PROTOCOL-IMAP create literal buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2120
static inline int check_rule_2120(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: CREATE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2120
static inline void alert_rule_2120(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2120;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP create literal buffer overflow attempt", 
                     sizeof("PROTOCOL-IMAP create literal buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2121
// Rule Name: PROTOCOL-POP DELE negative argument attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2121
static inline int check_rule_2121(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: DELE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2121
static inline void alert_rule_2121(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2121;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP DELE negative argument attempt", 
                     sizeof("PROTOCOL-POP DELE negative argument attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2122
// Rule Name: PROTOCOL-POP UIDL negative argument attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2122
static inline int check_rule_2122(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: UIDL",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2122
static inline void alert_rule_2122(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2122;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP UIDL negative argument attempt", 
                     sizeof("PROTOCOL-POP UIDL negative argument attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2123
// Rule Name: INDICATOR-COMPROMISE Microsoft cmd.exe banner
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 2123
static inline int check_rule_2123(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Microsoft Windows",depth 18', '|28|C|29| Copyright 1985-",distance 0', 'Microsoft Corp.",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2123
static inline void alert_rule_2123(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2123;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Microsoft cmd.exe banner", 
                     sizeof("INDICATOR-COMPROMISE Microsoft cmd.exe banner"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2124
// Rule Name: MALWARE-BACKDOOR Remote PC Access connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 2124
static inline int check_rule_2124(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 34012
    if (tcph->dest != htons(34012))
        return 0;
    // Content matching (simplified)
    // Full pattern: |28 00 01 00 04 00 00 00 00 00 00 00|",depth 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2124
static inline void alert_rule_2124(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2124;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Remote PC Access connection", 
                     sizeof("MALWARE-BACKDOOR Remote PC Access connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2125
// Rule Name: PROTOCOL-FTP CWD Root directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2125
static inline int check_rule_2125(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['CWD",nocase', 'C|3A 5C|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2125
static inline void alert_rule_2125(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2125;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP CWD Root directory traversal attempt", 
                     sizeof("PROTOCOL-FTP CWD Root directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2126
// Rule Name: OS-WINDOWS Microsoft Windows PPTP Start Control Request buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2126
static inline int check_rule_2126(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1723
    if (tcph->dest != htons(1723))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01|",depth 2,offset 2', '|00 01|",depth 2,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2126
static inline void alert_rule_2126(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2126;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows PPTP Start Control Request buffer overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows PPTP Start Control Request buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2127
// Rule Name: SERVER-WEBAPP ikonboard.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2127
static inline int check_rule_2127(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ikonboard.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2127
static inline void alert_rule_2127(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2127;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ikonboard.cgi access", 
                     sizeof("SERVER-WEBAPP ikonboard.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2128
// Rule Name: SERVER-WEBAPP swsrv.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2128
static inline int check_rule_2128(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /swsrv.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2128
static inline void alert_rule_2128(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2128;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP swsrv.cgi access", 
                     sizeof("SERVER-WEBAPP swsrv.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2129
// Rule Name: SERVER-IIS nsiislog.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2129
static inline int check_rule_2129(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nsiislog.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2129
static inline void alert_rule_2129(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2129;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS nsiislog.dll access", 
                     sizeof("SERVER-IIS nsiislog.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2130
// Rule Name: SERVER-IIS IISProtect siteadmin.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2130
static inline int check_rule_2130(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisprotect/admin/SiteAdmin.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2130
static inline void alert_rule_2130(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2130;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS IISProtect siteadmin.asp access", 
                     sizeof("SERVER-IIS IISProtect siteadmin.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2131
// Rule Name: SERVER-IIS IISProtect access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2131
static inline int check_rule_2131(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisprotect/admin/",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2131
static inline void alert_rule_2131(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2131;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS IISProtect access", 
                     sizeof("SERVER-IIS IISProtect access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2132
// Rule Name: SERVER-IIS Synchrologic Email Accelerator userid list access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2132
static inline int check_rule_2132(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /en/admin/aggregate.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2132
static inline void alert_rule_2132(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2132;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS Synchrologic Email Accelerator userid list access attempt", 
                     sizeof("SERVER-IIS Synchrologic Email Accelerator userid list access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2133
// Rule Name: SERVER-IIS MS BizTalk server access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2133
static inline int check_rule_2133(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /biztalkhttpreceive.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2133
static inline void alert_rule_2133(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2133;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS MS BizTalk server access", 
                     sizeof("SERVER-IIS MS BizTalk server access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2134
// Rule Name: SERVER-IIS register.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2134
static inline int check_rule_2134(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /register.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2134
static inline void alert_rule_2134(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2134;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS register.asp access", 
                     sizeof("SERVER-IIS register.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2135
// Rule Name: SERVER-WEBAPP philboard.mdb access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2135
static inline int check_rule_2135(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /philboard.mdb
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2135
static inline void alert_rule_2135(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2135;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP philboard.mdb access", 
                     sizeof("SERVER-WEBAPP philboard.mdb access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2136
// Rule Name: SERVER-WEBAPP philboard_admin.asp authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2136
static inline int check_rule_2136(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/philboard_admin.asp', 'Cookie",nocase', 'philboard_admin=True",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2136
static inline void alert_rule_2136(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2136;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP philboard_admin.asp authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP philboard_admin.asp authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2137
// Rule Name: SERVER-WEBAPP philboard_admin.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2137
static inline int check_rule_2137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /philboard_admin.asp
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2137
static inline void alert_rule_2137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP philboard_admin.asp access", 
                     sizeof("SERVER-WEBAPP philboard_admin.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2138
// Rule Name: SERVER-WEBAPP logicworks.ini access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2138
static inline int check_rule_2138(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /logicworks.ini
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2138
static inline void alert_rule_2138(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2138;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP logicworks.ini access", 
                     sizeof("SERVER-WEBAPP logicworks.ini access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2139
// Rule Name: SERVER-WEBAPP /*.shtml access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2139
static inline int check_rule_2139(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /*.shtml
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2139
static inline void alert_rule_2139(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2139;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /*.shtml access", 
                     sizeof("SERVER-WEBAPP /*.shtml access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2140
// Rule Name: SERVER-WEBAPP p-news.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2140
static inline int check_rule_2140(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /p-news.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2140
static inline void alert_rule_2140(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2140;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP p-news.php access", 
                     sizeof("SERVER-WEBAPP p-news.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2141
// Rule Name: SERVER-WEBAPP shoutbox.php directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2141
static inline int check_rule_2141(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/shoutbox.php', 'conf=', '../",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2141
static inline void alert_rule_2141(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2141;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP shoutbox.php directory traversal attempt", 
                     sizeof("SERVER-WEBAPP shoutbox.php directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2142
// Rule Name: SERVER-WEBAPP shoutbox.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2142
static inline int check_rule_2142(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/shoutbox.php",fast_pattern,nocase', 'conf=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2142
static inline void alert_rule_2142(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2142;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP shoutbox.php access", 
                     sizeof("SERVER-WEBAPP shoutbox.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2143
// Rule Name: SERVER-WEBAPP b2 cafelog gm-2-b2.php remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2143
static inline int check_rule_2143(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gm-2-b2.php",fast_pattern,nocase', 'b2inc=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2143
static inline void alert_rule_2143(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2143;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP b2 cafelog gm-2-b2.php remote file include attempt", 
                     sizeof("SERVER-WEBAPP b2 cafelog gm-2-b2.php remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2144
// Rule Name: SERVER-WEBAPP b2 cafelog gm-2-b2.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2144
static inline int check_rule_2144(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /gm-2-b2.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2144
static inline void alert_rule_2144(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2144;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP b2 cafelog gm-2-b2.php access", 
                     sizeof("SERVER-WEBAPP b2 cafelog gm-2-b2.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2145
// Rule Name: SERVER-WEBAPP TextPortal admin.php default password admin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2145
static inline int check_rule_2145(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/admin.php', 'op=admin_enter', 'password=admin",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2145
static inline void alert_rule_2145(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2145;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TextPortal admin.php default password admin attempt", 
                     sizeof("SERVER-WEBAPP TextPortal admin.php default password admin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2146
// Rule Name: SERVER-WEBAPP TextPortal admin.php default password 12345 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2146
static inline int check_rule_2146(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/admin.php', 'op=admin_enter', 'password=12345",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2146
static inline void alert_rule_2146(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2146;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TextPortal admin.php default password 12345 attempt", 
                     sizeof("SERVER-WEBAPP TextPortal admin.php default password 12345 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2147
// Rule Name: SERVER-WEBAPP BLNews objects.inc.php4 remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2147
static inline int check_rule_2147(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/objects.inc.php4', 'Server[path]=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2147
static inline void alert_rule_2147(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2147;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BLNews objects.inc.php4 remote file include attempt", 
                     sizeof("SERVER-WEBAPP BLNews objects.inc.php4 remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2148
// Rule Name: SERVER-WEBAPP BLNews objects.inc.php4 access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2148
static inline int check_rule_2148(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /objects.inc.php4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2148
static inline void alert_rule_2148(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2148;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BLNews objects.inc.php4 access", 
                     sizeof("SERVER-WEBAPP BLNews objects.inc.php4 access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2149
// Rule Name: SERVER-WEBAPP Turba status.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2149
static inline int check_rule_2149(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /turba/status.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2149
static inline void alert_rule_2149(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2149;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Turba status.php access", 
                     sizeof("SERVER-WEBAPP Turba status.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2150
// Rule Name: SERVER-WEBAPP ttCMS header.php remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2150
static inline int check_rule_2150(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/admin/templates/header.php",fast_pattern,nocase', 'admin_root=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2150
static inline void alert_rule_2150(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2150;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ttCMS header.php remote file include attempt", 
                     sizeof("SERVER-WEBAPP ttCMS header.php remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2151
// Rule Name: SERVER-WEBAPP ttCMS header.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2151
static inline int check_rule_2151(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin/templates/header.php
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2151
static inline void alert_rule_2151(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2151;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ttCMS header.php access", 
                     sizeof("SERVER-WEBAPP ttCMS header.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2152
// Rule Name: SERVER-WEBAPP test.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2152
static inline int check_rule_2152(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /test.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2152
static inline void alert_rule_2152(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2152;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP test.php access", 
                     sizeof("SERVER-WEBAPP test.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2153
// Rule Name: SERVER-WEBAPP autohtml.php directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2153
static inline int check_rule_2153(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/autohtml.php",fast_pattern,nocase', 'name=', '../../",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2153
static inline void alert_rule_2153(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2153;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP autohtml.php directory traversal attempt", 
                     sizeof("SERVER-WEBAPP autohtml.php directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2154
// Rule Name: SERVER-WEBAPP autohtml.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2154
static inline int check_rule_2154(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /autohtml.php
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2154
static inline void alert_rule_2154(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2154;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP autohtml.php access", 
                     sizeof("SERVER-WEBAPP autohtml.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2155
// Rule Name: SERVER-WEBAPP ttforum remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2155
static inline int check_rule_2155(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['forum/index.php', 'template=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2155
static inline void alert_rule_2155(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2155;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ttforum remote file include attempt", 
                     sizeof("SERVER-WEBAPP ttforum remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2156
// Rule Name: SERVER-WEBAPP mod_gzip_status access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2156
static inline int check_rule_2156(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mod_gzip_status
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2156
static inline void alert_rule_2156(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2156;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mod_gzip_status access", 
                     sizeof("SERVER-WEBAPP mod_gzip_status access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2157
// Rule Name: SERVER-IIS IISProtect globaladmin.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2157
static inline int check_rule_2157(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /iisprotect/admin/GlobalAdmin.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2157
static inline void alert_rule_2157(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2157;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS IISProtect globaladmin.asp access", 
                     sizeof("SERVER-IIS IISProtect globaladmin.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2158
// Rule Name: SERVER-OTHER BGP invalid length
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 2158
static inline int check_rule_2158(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Check dst port: 179
    if (tcph->dest != htons(179))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2158
static inline void alert_rule_2158(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2158;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER BGP invalid length", 
                     sizeof("SERVER-OTHER BGP invalid length"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2159
// Rule Name: SERVER-OTHER BGP invalid type 0
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 2159
static inline int check_rule_2159(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 179
    if (tcph->dest != htons(179))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF|",depth 16', '|00|",within 1,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2159
static inline void alert_rule_2159(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2159;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER BGP invalid type 0", 
                     sizeof("SERVER-OTHER BGP invalid type 0"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2176
// Rule Name: OS-WINDOWS Microsoft Windows SMB startup folder access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2176
static inline int check_rule_2176(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",depth 5,offset 4', 'Documents and Settings|5C|All Users|5C|Start Menu|5C|Programs|5C|Startup|00|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2176
static inline void alert_rule_2176(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2176;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB startup folder access", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB startup folder access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2177
// Rule Name: OS-WINDOWS Microsoft Windows SMB startup folder unicode access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2177
static inline int check_rule_2177(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",depth 5,offset 4', '|5C 00|S|00|t|00|a|00|r|00|t|00| |00|M|00|e|00|n|00|u|00 5C 00|P|00|r|00|o|00|g|00|r|00|a|00|m|00|s|00 5C 00|S|00|t|00|a|00|r|00|t|00|u|00|p",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2177
static inline void alert_rule_2177(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2177;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB startup folder unicode access", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB startup folder unicode access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2178
// Rule Name: PROTOCOL-FTP USER format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2178
static inline int check_rule_2178(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2178
static inline void alert_rule_2178(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2178;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP USER format string attempt", 
                     sizeof("PROTOCOL-FTP USER format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2179
// Rule Name: PROTOCOL-FTP PASS format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2179
static inline int check_rule_2179(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2179
static inline void alert_rule_2179(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2179;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP PASS format string attempt", 
                     sizeof("PROTOCOL-FTP PASS format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2180
// Rule Name: PUA-P2P BitTorrent announce request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2180
static inline int check_rule_2180(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/announce', 'info_hash=', 'peer_id=', 'event=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2180
static inline void alert_rule_2180(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2180;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P BitTorrent announce request", 
                     sizeof("PUA-P2P BitTorrent announce request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2181
// Rule Name: PUA-P2P BitTorrent transfer
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2181
static inline int check_rule_2181(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |13|BitTorrent protocol",depth 20
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2181
static inline void alert_rule_2181(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2181;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P BitTorrent transfer", 
                     sizeof("PUA-P2P BitTorrent transfer"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2183
// Rule Name: SERVER-MAIL Sendmail Content-Transfer-Encoding overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2183
static inline int check_rule_2183(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Transfer-Encoding",nocase', '|3A|",distance 0', '!"|0A|",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2183
static inline void alert_rule_2183(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2183;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail Content-Transfer-Encoding overflow attempt", 
                     sizeof("SERVER-MAIL Sendmail Content-Transfer-Encoding overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2184
// Rule Name: PROTOCOL-RPC mountd TCP mount path overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2184
static inline int check_rule_2184(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 01 86 A5 00|",depth 5,offset 16', '|00 00 00 01|",within 4,distance 3', '|00 00 00 00|",depth 4,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2184
static inline void alert_rule_2184(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2184;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC mountd TCP mount path overflow attempt", 
                     sizeof("PROTOCOL-RPC mountd TCP mount path overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2190
// Rule Name: NETBIOS DCERPC invalid bind attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2190
static inline int check_rule_2190(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 135
    if (tcph->dest != htons(135))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|05|",depth 1', '|0B|",within 1,distance 1', '|00|",within 1,distance 21']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2190
static inline void alert_rule_2190(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2190;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS DCERPC invalid bind attempt", 
                     sizeof("NETBIOS DCERPC invalid bind attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2191
// Rule Name: NETBIOS SMB DCERPC invalid bind attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2191
static inline int check_rule_2191(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB%",depth 5,offset 4,nocase', '&|00|",within 2,distance 56', '|5C 00|P|00|I|00|P|00|E|00 5C 00|",within 12,distance 5,nocase', '|05|",within 1,distance 2', '|0B|",within 1,distance 1', '|00|",within 1,distance 21']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2191
static inline void alert_rule_2191(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2191;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB DCERPC invalid bind attempt", 
                     sizeof("NETBIOS SMB DCERPC invalid bind attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2194
// Rule Name: SERVER-WEBAPP CSMailto.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2194
static inline int check_rule_2194(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /CSMailto.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2194
static inline void alert_rule_2194(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2194;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CSMailto.cgi access", 
                     sizeof("SERVER-WEBAPP CSMailto.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2195
// Rule Name: SERVER-WEBAPP alert.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2195
static inline int check_rule_2195(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /alert.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2195
static inline void alert_rule_2195(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2195;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP alert.cgi access", 
                     sizeof("SERVER-WEBAPP alert.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2196
// Rule Name: SERVER-WEBAPP catgy.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2196
static inline int check_rule_2196(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /catgy.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2196
static inline void alert_rule_2196(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2196;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP catgy.cgi access", 
                     sizeof("SERVER-WEBAPP catgy.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2197
// Rule Name: SERVER-WEBAPP cvsview2.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2197
static inline int check_rule_2197(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cvsview2.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2197
static inline void alert_rule_2197(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2197;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cvsview2.cgi access", 
                     sizeof("SERVER-WEBAPP cvsview2.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2198
// Rule Name: SERVER-WEBAPP cvslog.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2198
static inline int check_rule_2198(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cvslog.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2198
static inline void alert_rule_2198(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2198;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cvslog.cgi access", 
                     sizeof("SERVER-WEBAPP cvslog.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2199
// Rule Name: SERVER-WEBAPP multidiff.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2199
static inline int check_rule_2199(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /multidiff.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2199
static inline void alert_rule_2199(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2199;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP multidiff.cgi access", 
                     sizeof("SERVER-WEBAPP multidiff.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2200
// Rule Name: SERVER-WEBAPP dnewsweb.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2200
static inline int check_rule_2200(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dnewsweb.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2200
static inline void alert_rule_2200(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2200;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP dnewsweb.cgi access", 
                     sizeof("SERVER-WEBAPP dnewsweb.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2201
// Rule Name: SERVER-WEBAPP Matt Wright download.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2201
static inline int check_rule_2201(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /download.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2201
static inline void alert_rule_2201(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2201;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Matt Wright download.cgi access", 
                     sizeof("SERVER-WEBAPP Matt Wright download.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2202
// Rule Name: SERVER-WEBAPP Webmin Directory edit_action.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2202
static inline int check_rule_2202(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /edit_action.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2202
static inline void alert_rule_2202(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2202;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Webmin Directory edit_action.cgi access", 
                     sizeof("SERVER-WEBAPP Webmin Directory edit_action.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2203
// Rule Name: SERVER-WEBAPP Leif M. Wright everythingform.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2203
static inline int check_rule_2203(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /everythingform.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2203
static inline void alert_rule_2203(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2203;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Leif M. Wright everythingform.cgi access", 
                     sizeof("SERVER-WEBAPP Leif M. Wright everythingform.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2204
// Rule Name: SERVER-WEBAPP EasyBoard 2000 ezadmin.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2204
static inline int check_rule_2204(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ezadmin.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2204
static inline void alert_rule_2204(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2204;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP EasyBoard 2000 ezadmin.cgi access", 
                     sizeof("SERVER-WEBAPP EasyBoard 2000 ezadmin.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2205
// Rule Name: SERVER-WEBAPP EasyBoard 2000 ezboard.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2205
static inline int check_rule_2205(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ezboard.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2205
static inline void alert_rule_2205(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2205;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP EasyBoard 2000 ezboard.cgi access", 
                     sizeof("SERVER-WEBAPP EasyBoard 2000 ezboard.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2206
// Rule Name: SERVER-WEBAPP EasyBoard 2000 ezman.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2206
static inline int check_rule_2206(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ezman.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2206
static inline void alert_rule_2206(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2206;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP EasyBoard 2000 ezman.cgi access", 
                     sizeof("SERVER-WEBAPP EasyBoard 2000 ezman.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2207
// Rule Name: SERVER-WEBAPP FileSeek fileseek.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2207
static inline int check_rule_2207(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fileseek.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2207
static inline void alert_rule_2207(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2207;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP FileSeek fileseek.cgi access", 
                     sizeof("SERVER-WEBAPP FileSeek fileseek.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2208
// Rule Name: SERVER-WEBAPP Faq-O-Matic fom.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2208
static inline int check_rule_2208(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /fom.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2208
static inline void alert_rule_2208(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2208;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Faq-O-Matic fom.cgi access", 
                     sizeof("SERVER-WEBAPP Faq-O-Matic fom.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2209
// Rule Name: SERVER-WEBAPP Infonautics getdoc.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2209
static inline int check_rule_2209(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /getdoc.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2209
static inline void alert_rule_2209(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2209;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Infonautics getdoc.cgi access", 
                     sizeof("SERVER-WEBAPP Infonautics getdoc.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2210
// Rule Name: SERVER-WEBAPP Multiple Vendors global.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2210
static inline int check_rule_2210(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /global.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2210
static inline void alert_rule_2210(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2210;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Multiple Vendors global.cgi access", 
                     sizeof("SERVER-WEBAPP Multiple Vendors global.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2211
// Rule Name: SERVER-WEBAPP Lars Ellingsen guestserver.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2211
static inline int check_rule_2211(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /guestserver.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2211
static inline void alert_rule_2211(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2211;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Lars Ellingsen guestserver.cgi access", 
                     sizeof("SERVER-WEBAPP Lars Ellingsen guestserver.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2212
// Rule Name: SERVER-WEBAPP cgiCentral WebStore imageFolio.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2212
static inline int check_rule_2212(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /imageFolio.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2212
static inline void alert_rule_2212(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2212;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiCentral WebStore imageFolio.cgi access", 
                     sizeof("SERVER-WEBAPP cgiCentral WebStore imageFolio.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2213
// Rule Name: SERVER-WEBAPP Oatmeal Studios Mail File mailfile.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2213
static inline int check_rule_2213(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mailfile.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2213
static inline void alert_rule_2213(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2213;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oatmeal Studios Mail File mailfile.cgi access", 
                     sizeof("SERVER-WEBAPP Oatmeal Studios Mail File mailfile.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2214
// Rule Name: SERVER-WEBAPP 3R Soft MailStudio 2000 mailview.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2214
static inline int check_rule_2214(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /mailview.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2214
static inline void alert_rule_2214(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2214;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP 3R Soft MailStudio 2000 mailview.cgi access", 
                     sizeof("SERVER-WEBAPP 3R Soft MailStudio 2000 mailview.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2215
// Rule Name: SERVER-WEBAPP Alabanza Control Panel nsManager.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2215
static inline int check_rule_2215(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nsManager.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2215
static inline void alert_rule_2215(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2215;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Alabanza Control Panel nsManager.cgi access", 
                     sizeof("SERVER-WEBAPP Alabanza Control Panel nsManager.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2216
// Rule Name: SERVER-WEBAPP Ipswitch IMail readmail.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2216
static inline int check_rule_2216(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /readmail.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2216
static inline void alert_rule_2216(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2216;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ipswitch IMail readmail.cgi access", 
                     sizeof("SERVER-WEBAPP Ipswitch IMail readmail.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2217
// Rule Name: SERVER-WEBAPP Ipswitch IMail printmail.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2217
static inline int check_rule_2217(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /printmail.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2217
static inline void alert_rule_2217(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2217;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ipswitch IMail printmail.cgi access", 
                     sizeof("SERVER-WEBAPP Ipswitch IMail printmail.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2218
// Rule Name: SERVER-WEBAPP Oracle Cobalt RaQ service.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2218
static inline int check_rule_2218(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /service.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2218
static inline void alert_rule_2218(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2218;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle Cobalt RaQ service.cgi access", 
                     sizeof("SERVER-WEBAPP Oracle Cobalt RaQ service.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2219
// Rule Name: SERVER-WEBAPP Trend Micro Interscan VirusWall setpasswd.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2219
static inline int check_rule_2219(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /setpasswd.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2219
static inline void alert_rule_2219(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2219;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Trend Micro Interscan VirusWall setpasswd.cgi access", 
                     sizeof("SERVER-WEBAPP Trend Micro Interscan VirusWall setpasswd.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2220
// Rule Name: SERVER-WEBAPP Leif M. Wright simplestmail.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2220
static inline int check_rule_2220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /simplestmail.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2220
static inline void alert_rule_2220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Leif M. Wright simplestmail.cgi access", 
                     sizeof("SERVER-WEBAPP Leif M. Wright simplestmail.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2221
// Rule Name: SERVER-WEBAPP cgiCentral WebStore ws_mail.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2221
static inline int check_rule_2221(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ws_mail.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2221
static inline void alert_rule_2221(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2221;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiCentral WebStore ws_mail.cgi access", 
                     sizeof("SERVER-WEBAPP cgiCentral WebStore ws_mail.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2222
// Rule Name: SERVER-WEBAPP Infinity CGI exploit scanner nph-exploitscanget.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2222
static inline int check_rule_2222(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nph-exploitscanget.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2222
static inline void alert_rule_2222(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2222;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Infinity CGI exploit scanner nph-exploitscanget.cgi access", 
                     sizeof("SERVER-WEBAPP Infinity CGI exploit scanner nph-exploitscanget.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2223
// Rule Name: SERVER-WEBAPP CGIScript.net csNews.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2223
static inline int check_rule_2223(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /csNews.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2223
static inline void alert_rule_2223(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2223;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CGIScript.net csNews.cgi access", 
                     sizeof("SERVER-WEBAPP CGIScript.net csNews.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2224
// Rule Name: SERVER-WEBAPP Psunami Bulletin Board psunami.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2224
static inline int check_rule_2224(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /psunami.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2224
static inline void alert_rule_2224(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2224;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Psunami Bulletin Board psunami.cgi access", 
                     sizeof("SERVER-WEBAPP Psunami Bulletin Board psunami.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2225
// Rule Name: SERVER-WEBAPP Linksys BEFSR41 gozila.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2225
static inline int check_rule_2225(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /gozila.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2225
static inline void alert_rule_2225(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2225;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys BEFSR41 gozila.cgi access", 
                     sizeof("SERVER-WEBAPP Linksys BEFSR41 gozila.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2226
// Rule Name: SERVER-WEBAPP pmachine remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2226
static inline int check_rule_2226(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['lib.inc.php",fast_pattern,nocase', 'pm_path=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2226
static inline void alert_rule_2226(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2226;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pmachine remote file include attempt", 
                     sizeof("SERVER-WEBAPP pmachine remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2227
// Rule Name: SERVER-WEBAPP forum_details.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2227
static inline int check_rule_2227(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: forum_details.php
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2227
static inline void alert_rule_2227(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2227;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP forum_details.php access", 
                     sizeof("SERVER-WEBAPP forum_details.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2228
// Rule Name: SERVER-WEBAPP phpMyAdmin db_details_importdocsql.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2228
static inline int check_rule_2228(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: db_details_importdocsql.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2228
static inline void alert_rule_2228(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2228;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phpMyAdmin db_details_importdocsql.php access", 
                     sizeof("SERVER-WEBAPP phpMyAdmin db_details_importdocsql.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2229
// Rule Name: SERVER-WEBAPP viewtopic.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2229
static inline int check_rule_2229(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/viewtopic.php",fast_pattern,nocase', 'days=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2229
static inline void alert_rule_2229(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2229;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP viewtopic.php access", 
                     sizeof("SERVER-WEBAPP viewtopic.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2230
// Rule Name: SERVER-WEBAPP NetGear router default password login attempt admin/password
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 2230
static inline int check_rule_2230(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Authorization|3A|",nocase', 'YWRtaW46cGFzc3dvcmQ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2230
static inline void alert_rule_2230(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2230;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP NetGear router default password login attempt admin/password", 
                     sizeof("SERVER-WEBAPP NetGear router default password login attempt admin/password"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2231
// Rule Name: SERVER-WEBAPP register.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2231
static inline int check_rule_2231(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /register.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2231
static inline void alert_rule_2231(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2231;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP register.dll access", 
                     sizeof("SERVER-WEBAPP register.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2232
// Rule Name: SERVER-WEBAPP ContentFilter.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2232
static inline int check_rule_2232(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ContentFilter.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2232
static inline void alert_rule_2232(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2232;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ContentFilter.dll access", 
                     sizeof("SERVER-WEBAPP ContentFilter.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2233
// Rule Name: SERVER-WEBAPP SFNofitication.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2233
static inline int check_rule_2233(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /SFNofitication.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2233
static inline void alert_rule_2233(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2233;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SFNofitication.dll access", 
                     sizeof("SERVER-WEBAPP SFNofitication.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2234
// Rule Name: SERVER-WEBAPP TOP10.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2234
static inline int check_rule_2234(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /TOP10.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2234
static inline void alert_rule_2234(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2234;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TOP10.dll access", 
                     sizeof("SERVER-WEBAPP TOP10.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2235
// Rule Name: SERVER-WEBAPP SpamExcp.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2235
static inline int check_rule_2235(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /SpamExcp.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2235
static inline void alert_rule_2235(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2235;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SpamExcp.dll access", 
                     sizeof("SERVER-WEBAPP SpamExcp.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2236
// Rule Name: SERVER-WEBAPP spamrule.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2236
static inline int check_rule_2236(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /spamrule.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2236
static inline void alert_rule_2236(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2236;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP spamrule.dll access", 
                     sizeof("SERVER-WEBAPP spamrule.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2237
// Rule Name: SERVER-WEBAPP cgiWebupdate.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2237
static inline int check_rule_2237(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgiWebupdate.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2237
static inline void alert_rule_2237(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2237;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cgiWebupdate.exe access", 
                     sizeof("SERVER-WEBAPP cgiWebupdate.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2238
// Rule Name: SERVER-WEBAPP WebLogic ConsoleHelp view source attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2238
static inline int check_rule_2238(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ConsoleHelp/",nocase', '.jsp",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2238
static inline void alert_rule_2238(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2238;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WebLogic ConsoleHelp view source attempt", 
                     sizeof("SERVER-WEBAPP WebLogic ConsoleHelp view source attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2239
// Rule Name: SERVER-WEBAPP redirect.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2239
static inline int check_rule_2239(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /redirect.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2239
static inline void alert_rule_2239(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2239;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP redirect.exe access", 
                     sizeof("SERVER-WEBAPP redirect.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2240
// Rule Name: SERVER-WEBAPP changepw.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2240
static inline int check_rule_2240(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /changepw.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2240
static inline void alert_rule_2240(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2240;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP changepw.exe access", 
                     sizeof("SERVER-WEBAPP changepw.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2241
// Rule Name: SERVER-WEBAPP cwmail.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2241
static inline int check_rule_2241(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cwmail.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2241
static inline void alert_rule_2241(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2241;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cwmail.exe access", 
                     sizeof("SERVER-WEBAPP cwmail.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2242
// Rule Name: SERVER-WEBAPP ddicgi.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2242
static inline int check_rule_2242(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ddicgi.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2242
static inline void alert_rule_2242(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2242;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ddicgi.exe access", 
                     sizeof("SERVER-WEBAPP ddicgi.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2243
// Rule Name: SERVER-WEBAPP ndcgi.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2243
static inline int check_rule_2243(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ndcgi.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2243
static inline void alert_rule_2243(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2243;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ndcgi.exe access", 
                     sizeof("SERVER-WEBAPP ndcgi.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2244
// Rule Name: SERVER-WEBAPP VsSetCookie.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2244
static inline int check_rule_2244(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /VsSetCookie.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2244
static inline void alert_rule_2244(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2244;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP VsSetCookie.exe access", 
                     sizeof("SERVER-WEBAPP VsSetCookie.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2245
// Rule Name: SERVER-WEBAPP Webnews.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2245
static inline int check_rule_2245(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Webnews.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2245
static inline void alert_rule_2245(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2245;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Webnews.exe access", 
                     sizeof("SERVER-WEBAPP Webnews.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2246
// Rule Name: SERVER-WEBAPP webadmin.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2246
static inline int check_rule_2246(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webadmin.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2246
static inline void alert_rule_2246(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2246;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP webadmin.dll access", 
                     sizeof("SERVER-WEBAPP webadmin.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2247
// Rule Name: SERVER-IIS UploadScript11.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2247
static inline int check_rule_2247(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /UploadScript11.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2247
static inline void alert_rule_2247(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2247;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS UploadScript11.asp access", 
                     sizeof("SERVER-IIS UploadScript11.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2248
// Rule Name: SERVER-IIS DirectoryListing.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2248
static inline int check_rule_2248(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /DirectoryListing.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2248
static inline void alert_rule_2248(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2248;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS DirectoryListing.asp access", 
                     sizeof("SERVER-IIS DirectoryListing.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2249
// Rule Name: SERVER-IIS /pcadmin/login.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2249
static inline int check_rule_2249(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pcadmin/login.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2249
static inline void alert_rule_2249(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2249;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS /pcadmin/login.asp access", 
                     sizeof("SERVER-IIS /pcadmin/login.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2250
// Rule Name: PROTOCOL-POP USER format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2250
static inline int check_rule_2250(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2250
static inline void alert_rule_2250(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2250;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP USER format string attempt", 
                     sizeof("PROTOCOL-POP USER format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2252
// Rule Name: OS-WINDOWS Microsoft Windows SMB-DS DCERPC Remote Activation bind attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2252
static inline int check_rule_2252(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB%",depth 5,offset 4,nocase', '&|00|",within 2,distance 56', '|5C 00|P|00|I|00|P|00|E|00 5C 00|",within 12,distance 5,nocase', '|05|",within 1', '|0B|",within 1,distance 1', '|B8|J|9F|M|1C|}|CF 11 86 1E 00| |AF|n|7C|W",within 16,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2252
static inline void alert_rule_2252(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2252;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB-DS DCERPC Remote Activation bind attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB-DS DCERPC Remote Activation bind attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2253
// Rule Name: SERVER-MAIL XEXCH50 overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2253
static inline int check_rule_2253(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: XEXCH50",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2253
static inline void alert_rule_2253(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2253;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL XEXCH50 overflow attempt", 
                     sizeof("SERVER-MAIL XEXCH50 overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2255
// Rule Name: PROTOCOL-RPC sadmind query with root credentials attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2255
static inline int check_rule_2255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 01 87 88|",depth 4,offset 16', '|00 00 00 01 00 00 00 01|",within 8,distance 4', '|00 00 00 00|",within 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2255
static inline void alert_rule_2255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-RPC sadmind query with root credentials attempt TCP", 
                     sizeof("PROTOCOL-RPC sadmind query with root credentials attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2258
// Rule Name: OS-WINDOWS Microsoft Windows SMB-DS DCERPC Messenger Service buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2258
static inline int check_rule_2258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB%",depth 5,offset 4,nocase', '&|00|",within 2,distance 56', '|5C 00|P|00|I|00|P|00|E|00 5C 00|",within 12,distance 5,nocase', '|04 00|",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2258
static inline void alert_rule_2258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB-DS DCERPC Messenger Service buffer overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB-DS DCERPC Messenger Service buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2259
// Rule Name: SERVER-MAIL EXPN overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2259
static inline int check_rule_2259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: EXPN",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2259
static inline void alert_rule_2259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL EXPN overflow attempt", 
                     sizeof("SERVER-MAIL EXPN overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2260
// Rule Name: SERVER-MAIL VRFY overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2260
static inline int check_rule_2260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: VRFY",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2260
static inline void alert_rule_2260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL VRFY overflow attempt", 
                     sizeof("SERVER-MAIL VRFY overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2261
// Rule Name: SERVER-MAIL Sendmail SEND FROM prescan too many addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2261
static inline int check_rule_2261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SEND FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2261
static inline void alert_rule_2261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SEND FROM prescan too many addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SEND FROM prescan too many addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2262
// Rule Name: SERVER-MAIL Sendmail SEND FROM prescan too long addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2262
static inline int check_rule_2262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SEND FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2262
static inline void alert_rule_2262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SEND FROM prescan too long addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SEND FROM prescan too long addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2263
// Rule Name: SERVER-MAIL Sendmail SAML FROM prescan too many addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2263
static inline int check_rule_2263(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SAML FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2263
static inline void alert_rule_2263(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2263;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SAML FROM prescan too many addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SAML FROM prescan too many addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2264
// Rule Name: SERVER-MAIL Sendmail SAML FROM prescan too long addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2264
static inline int check_rule_2264(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SAML FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2264
static inline void alert_rule_2264(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2264;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SAML FROM prescan too long addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SAML FROM prescan too long addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2265
// Rule Name: SERVER-MAIL Sendmail SOML FROM prescan too many addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2265
static inline int check_rule_2265(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SOML FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2265
static inline void alert_rule_2265(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2265;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SOML FROM prescan too many addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SOML FROM prescan too many addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2266
// Rule Name: SERVER-MAIL Sendmail SOML FROM prescan too long addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2266
static inline int check_rule_2266(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: SOML FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2266
static inline void alert_rule_2266(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2266;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail SOML FROM prescan too long addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail SOML FROM prescan too long addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2267
// Rule Name: SERVER-MAIL Sendmail MAIL FROM prescan too many addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2267
static inline int check_rule_2267(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: MAIL FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2267
static inline void alert_rule_2267(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2267;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail MAIL FROM prescan too many addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail MAIL FROM prescan too many addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2268
// Rule Name: SERVER-MAIL Sendmail MAIL FROM prescan too long addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2268
static inline int check_rule_2268(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: MAIL FROM|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2268
static inline void alert_rule_2268(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2268;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail MAIL FROM prescan too long addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail MAIL FROM prescan too long addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2269
// Rule Name: SERVER-MAIL Sendmail RCPT TO prescan too many addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2269
static inline int check_rule_2269(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: RCPT TO|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2269
static inline void alert_rule_2269(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2269;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail RCPT TO prescan too many addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail RCPT TO prescan too many addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2270
// Rule Name: SERVER-MAIL Sendmail RCPT TO prescan too long addresses overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2270
static inline int check_rule_2270(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: RCPT TO|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2270
static inline void alert_rule_2270(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2270;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Sendmail RCPT TO prescan too long addresses overflow", 
                     sizeof("SERVER-MAIL Sendmail RCPT TO prescan too long addresses overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2271
// Rule Name: MALWARE-BACKDOOR FsSniffer connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 2271
static inline int check_rule_2271(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: RemoteNC Control Password|3A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2271
static inline void alert_rule_2271(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2271;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR FsSniffer connection attempt", 
                     sizeof("MALWARE-BACKDOOR FsSniffer connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2272
// Rule Name: PROTOCOL-FTP LIST integer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2272
static inline int check_rule_2272(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: LIST",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2272
static inline void alert_rule_2272(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2272;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP LIST integer overflow attempt", 
                     sizeof("PROTOCOL-FTP LIST integer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2273
// Rule Name: PROTOCOL-IMAP login brute force attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 2273
static inline int check_rule_2273(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2273
static inline void alert_rule_2273(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2273;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP login brute force attempt", 
                     sizeof("PROTOCOL-IMAP login brute force attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2274
// Rule Name: PROTOCOL-POP login brute force attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 2274
static inline int check_rule_2274(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2274
static inline void alert_rule_2274(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2274;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP login brute force attempt", 
                     sizeof("PROTOCOL-POP login brute force attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2275
// Rule Name: SERVER-MAIL AUTH LOGON brute force attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 2275
static inline int check_rule_2275(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SMTP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 25
    if (tcph->source != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Authentication unsuccessful",offset 54,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2275
static inline void alert_rule_2275(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2275;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL AUTH LOGON brute force attempt", 
                     sizeof("SERVER-MAIL AUTH LOGON brute force attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2276
// Rule Name: SERVER-WEBAPP oracle portal demo access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2276
static inline int check_rule_2276(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pls/portal/PORTAL_DEMO",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2276
static inline void alert_rule_2276(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2276;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP oracle portal demo access", 
                     sizeof("SERVER-WEBAPP oracle portal demo access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2277
// Rule Name: SERVER-WEBAPP PeopleSoft PeopleBooks psdoccgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2277
static inline int check_rule_2277(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /psdoccgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2277
static inline void alert_rule_2277(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2277;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PeopleSoft PeopleBooks psdoccgi access", 
                     sizeof("SERVER-WEBAPP PeopleSoft PeopleBooks psdoccgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2278
// Rule Name: SERVER-WEBAPP HTTP request with negative Content-Length attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2278
static inline int check_rule_2278(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length|3A|",nocase', '!"|0D|",within 10', '!"|0A|",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2278
static inline void alert_rule_2278(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2278;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP HTTP request with negative Content-Length attempt", 
                     sizeof("SERVER-WEBAPP HTTP request with negative Content-Length attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2279
// Rule Name: SERVER-WEBAPP UpdateClasses.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2279
static inline int check_rule_2279(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /UpdateClasses.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2279
static inline void alert_rule_2279(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2279;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP UpdateClasses.php access", 
                     sizeof("SERVER-WEBAPP UpdateClasses.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2280
// Rule Name: SERVER-WEBAPP Title.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2280
static inline int check_rule_2280(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Title.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2280
static inline void alert_rule_2280(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2280;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Title.php access", 
                     sizeof("SERVER-WEBAPP Title.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2281
// Rule Name: SERVER-WEBAPP Setup.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2281
static inline int check_rule_2281(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Setup.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2281
static inline void alert_rule_2281(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2281;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Setup.php access", 
                     sizeof("SERVER-WEBAPP Setup.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2282
// Rule Name: SERVER-WEBAPP GlobalFunctions.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2282
static inline int check_rule_2282(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /GlobalFunctions.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2282
static inline void alert_rule_2282(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2282;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP GlobalFunctions.php access", 
                     sizeof("SERVER-WEBAPP GlobalFunctions.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2283
// Rule Name: SERVER-WEBAPP DatabaseFunctions.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2283
static inline int check_rule_2283(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /DatabaseFunctions.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2283
static inline void alert_rule_2283(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2283;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DatabaseFunctions.php access", 
                     sizeof("SERVER-WEBAPP DatabaseFunctions.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2284
// Rule Name: SERVER-WEBAPP rolis guestbook remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2284
static inline int check_rule_2284(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/insert.inc.php",fast_pattern,nocase', 'path=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2284
static inline void alert_rule_2284(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2284;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rolis guestbook remote file include attempt", 
                     sizeof("SERVER-WEBAPP rolis guestbook remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2285
// Rule Name: SERVER-WEBAPP rolis guestbook access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2285
static inline int check_rule_2285(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /insert.inc.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2285
static inline void alert_rule_2285(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2285;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP rolis guestbook access", 
                     sizeof("SERVER-WEBAPP rolis guestbook access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2286
// Rule Name: SERVER-WEBAPP friends.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2286
static inline int check_rule_2286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /friends.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2286
static inline void alert_rule_2286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP friends.php access", 
                     sizeof("SERVER-WEBAPP friends.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2287
// Rule Name: SERVER-WEBAPP Advanced Poll admin_comment.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2287
static inline int check_rule_2287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_comment.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2287
static inline void alert_rule_2287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_comment.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_comment.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2288
// Rule Name: SERVER-WEBAPP Advanced Poll admin_edit.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2288
static inline int check_rule_2288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_edit.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2288
static inline void alert_rule_2288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_edit.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_edit.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2289
// Rule Name: SERVER-WEBAPP Advanced Poll admin_embed.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2289
static inline int check_rule_2289(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_embed.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2289
static inline void alert_rule_2289(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2289;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_embed.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_embed.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2290
// Rule Name: SERVER-WEBAPP Advanced Poll admin_help.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2290
static inline int check_rule_2290(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_help.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2290
static inline void alert_rule_2290(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2290;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_help.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_help.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2291
// Rule Name: SERVER-WEBAPP Advanced Poll admin_license.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2291
static inline int check_rule_2291(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_license.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2291
static inline void alert_rule_2291(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2291;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_license.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_license.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2292
// Rule Name: SERVER-WEBAPP Advanced Poll admin_logout.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2292
static inline int check_rule_2292(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_logout.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2292
static inline void alert_rule_2292(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2292;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_logout.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_logout.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2293
// Rule Name: SERVER-WEBAPP Advanced Poll admin_password.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2293
static inline int check_rule_2293(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_password.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2293
static inline void alert_rule_2293(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2293;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_password.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_password.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2294
// Rule Name: SERVER-WEBAPP Advanced Poll admin_preview.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2294
static inline int check_rule_2294(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_preview.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2294
static inline void alert_rule_2294(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2294;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_preview.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_preview.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2295
// Rule Name: SERVER-WEBAPP Advanced Poll admin_settings.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2295
static inline int check_rule_2295(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_settings.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2295
static inline void alert_rule_2295(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2295;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_settings.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_settings.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2296
// Rule Name: SERVER-WEBAPP Advanced Poll admin_stats.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2296
static inline int check_rule_2296(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_stats.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2296
static inline void alert_rule_2296(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2296;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_stats.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_stats.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2297
// Rule Name: SERVER-WEBAPP Advanced Poll admin_templates_misc.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2297
static inline int check_rule_2297(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_templates_misc.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2297
static inline void alert_rule_2297(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2297;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_templates_misc.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_templates_misc.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2298
// Rule Name: SERVER-WEBAPP Advanced Poll admin_templates.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2298
static inline int check_rule_2298(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_templates.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2298
static inline void alert_rule_2298(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2298;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_templates.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_templates.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2299
// Rule Name: SERVER-WEBAPP Advanced Poll admin_tpl_misc_new.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2299
static inline int check_rule_2299(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_tpl_misc_new.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2299
static inline void alert_rule_2299(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2299;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_tpl_misc_new.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_tpl_misc_new.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2300
// Rule Name: SERVER-WEBAPP Advanced Poll admin_tpl_new.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2300
static inline int check_rule_2300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /admin_tpl_new.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2300
static inline void alert_rule_2300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll admin_tpl_new.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll admin_tpl_new.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2301
// Rule Name: SERVER-WEBAPP Advanced Poll booth.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2301
static inline int check_rule_2301(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /booth.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2301
static inline void alert_rule_2301(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2301;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll booth.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll booth.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2302
// Rule Name: SERVER-WEBAPP Advanced Poll poll_ssi.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2302
static inline int check_rule_2302(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /poll_ssi.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2302
static inline void alert_rule_2302(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2302;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll poll_ssi.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll poll_ssi.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2303
// Rule Name: SERVER-WEBAPP Advanced Poll popup.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2303
static inline int check_rule_2303(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/popup.php",fast_pattern,nocase', 'include_path=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2303
static inline void alert_rule_2303(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2303;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Advanced Poll popup.php access", 
                     sizeof("SERVER-WEBAPP Advanced Poll popup.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2304
// Rule Name: SERVER-WEBAPP files.inc.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2304
static inline int check_rule_2304(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /files.inc.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2304
static inline void alert_rule_2304(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2304;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP files.inc.php access", 
                     sizeof("SERVER-WEBAPP files.inc.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2305
// Rule Name: SERVER-WEBAPP chatbox.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2305
static inline int check_rule_2305(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /chatbox.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2305
static inline void alert_rule_2305(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2305;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP chatbox.php access", 
                     sizeof("SERVER-WEBAPP chatbox.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2306
// Rule Name: SERVER-WEBAPP gallery remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2306
static inline int check_rule_2306(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/setup/', 'GALLERY_BASEDIR=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2306
static inline void alert_rule_2306(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2306;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP gallery remote file include attempt", 
                     sizeof("SERVER-WEBAPP gallery remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2307
// Rule Name: SERVER-WEBAPP PayPal Storefront remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2307
static inline int check_rule_2307(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['do=ext', 'page=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2307
static inline void alert_rule_2307(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2307;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PayPal Storefront remote file include attempt", 
                     sizeof("SERVER-WEBAPP PayPal Storefront remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2317
// Rule Name: INDICATOR-COMPROMISE CVS non-relative path error response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2317
static inline int check_rule_2317(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2401
    if (tcph->source != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: E cvs server|3A| warning|3A| cannot make directory CVS in /",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2317
static inline void alert_rule_2317(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2317;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE CVS non-relative path error response", 
                     sizeof("INDICATOR-COMPROMISE CVS non-relative path error response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2318
// Rule Name: SERVER-OTHER CVS non-relative path access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2318
static inline int check_rule_2318(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2401
    if (tcph->dest != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: Argument
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2318
static inline void alert_rule_2318(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2318;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER CVS non-relative path access attempt", 
                     sizeof("SERVER-OTHER CVS non-relative path access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2319
// Rule Name: SERVER-OTHER ebola PASS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2319
static inline int check_rule_2319(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1655
    if (tcph->dest != htons(1655))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2319
static inline void alert_rule_2319(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2319;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER ebola PASS overflow attempt", 
                     sizeof("SERVER-OTHER ebola PASS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2320
// Rule Name: SERVER-OTHER ebola USER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2320
static inline int check_rule_2320(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1655
    if (tcph->dest != htons(1655))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2320
static inline void alert_rule_2320(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2320;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER ebola USER overflow attempt", 
                     sizeof("SERVER-OTHER ebola USER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2321
// Rule Name: SERVER-IIS foxweb.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2321
static inline int check_rule_2321(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /foxweb.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2321
static inline void alert_rule_2321(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2321;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS foxweb.exe access", 
                     sizeof("SERVER-IIS foxweb.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2322
// Rule Name: SERVER-IIS foxweb.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2322
static inline int check_rule_2322(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /foxweb.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2322
static inline void alert_rule_2322(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2322;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS foxweb.dll access", 
                     sizeof("SERVER-IIS foxweb.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2323
// Rule Name: SERVER-WEBAPP iSoft-Solutions QuickStore shopping cart quickstore.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2323
static inline int check_rule_2323(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /quickstore.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2323
static inline void alert_rule_2323(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2323;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP iSoft-Solutions QuickStore shopping cart quickstore.cgi access", 
                     sizeof("SERVER-WEBAPP iSoft-Solutions QuickStore shopping cart quickstore.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2324
// Rule Name: SERVER-IIS VP-ASP shopsearch.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2324
static inline int check_rule_2324(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /shopsearch.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2324
static inline void alert_rule_2324(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2324;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS VP-ASP shopsearch.asp access", 
                     sizeof("SERVER-IIS VP-ASP shopsearch.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2325
// Rule Name: SERVER-IIS VP-ASP ShopDisplayProducts.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2325
static inline int check_rule_2325(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ShopDisplayProducts.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2325
static inline void alert_rule_2325(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2325;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS VP-ASP ShopDisplayProducts.asp access", 
                     sizeof("SERVER-IIS VP-ASP ShopDisplayProducts.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2326
// Rule Name: SERVER-IIS sgdynamo.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2326
static inline int check_rule_2326(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sgdynamo.exe",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2326
static inline void alert_rule_2326(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2326;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS sgdynamo.exe access", 
                     sizeof("SERVER-IIS sgdynamo.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2327
// Rule Name: SERVER-WEBAPP bsml.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2327
static inline int check_rule_2327(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /bsml.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2327
static inline void alert_rule_2327(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2327;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP bsml.pl access", 
                     sizeof("SERVER-WEBAPP bsml.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2328
// Rule Name: SERVER-WEBAPP authentication_index.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2328
static inline int check_rule_2328(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /authentication_index.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2328
static inline void alert_rule_2328(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2328;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP authentication_index.php access", 
                     sizeof("SERVER-WEBAPP authentication_index.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2330
// Rule Name: PROTOCOL-IMAP auth overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2330
static inline int check_rule_2330(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['AUTH', '!"|0A|",within 368']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2330
static inline void alert_rule_2330(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2330;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP auth overflow attempt", 
                     sizeof("PROTOCOL-IMAP auth overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2331
// Rule Name: SERVER-WEBAPP MatrikzGB privilege escalation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2331
static inline int check_rule_2331(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: new_rights=admin",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2331
static inline void alert_rule_2331(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2331;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MatrikzGB privilege escalation attempt", 
                     sizeof("SERVER-WEBAPP MatrikzGB privilege escalation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2332
// Rule Name: PROTOCOL-FTP MKD format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2332
static inline int check_rule_2332(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MKD",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2332
static inline void alert_rule_2332(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2332;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP MKD format string attempt", 
                     sizeof("PROTOCOL-FTP MKD format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2333
// Rule Name: PROTOCOL-FTP RENAME format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2333
static inline int check_rule_2333(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RENAME",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2333
static inline void alert_rule_2333(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2333;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RENAME format string attempt", 
                     sizeof("PROTOCOL-FTP RENAME format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2334
// Rule Name: PROTOCOL-FTP Yak! FTP server default account login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 2334
static inline int check_rule_2334(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3535
    if (tcph->dest != htons(3535))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['USER",nocase', 'y049575046",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2334
static inline void alert_rule_2334(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2334;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP Yak! FTP server default account login attempt", 
                     sizeof("PROTOCOL-FTP Yak! FTP server default account login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2335
// Rule Name: PROTOCOL-FTP RMD / attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2335
static inline int check_rule_2335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3535
    if (tcph->dest != htons(3535))
        return 0;
    // Content matching (simplified)
    // Full pattern: RMD",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2335
static inline void alert_rule_2335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RMD / attempt", 
                     sizeof("PROTOCOL-FTP RMD / attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2338
// Rule Name: PROTOCOL-FTP LIST buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2338
static inline int check_rule_2338(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: LIST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2338
static inline void alert_rule_2338(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2338;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP LIST buffer overflow attempt", 
                     sizeof("PROTOCOL-FTP LIST buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2340
// Rule Name: PROTOCOL-FTP SITE CHMOD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2340
static inline int check_rule_2340(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['SITE",nocase', 'CHMOD",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2340
static inline void alert_rule_2340(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2340;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP SITE CHMOD overflow attempt", 
                     sizeof("PROTOCOL-FTP SITE CHMOD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2341
// Rule Name: SERVER-WEBAPP DCP-Portal remote file include editor script attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2341
static inline int check_rule_2341(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/library/editor/editor.php",fast_pattern,nocase', 'root=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2341
static inline void alert_rule_2341(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2341;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DCP-Portal remote file include editor script attempt", 
                     sizeof("SERVER-WEBAPP DCP-Portal remote file include editor script attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2342
// Rule Name: SERVER-WEBAPP DCP-Portal remote file include lib script attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2342
static inline int check_rule_2342(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/library/lib.php",fast_pattern,nocase', 'root=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2342
static inline void alert_rule_2342(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2342;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DCP-Portal remote file include lib script attempt", 
                     sizeof("SERVER-WEBAPP DCP-Portal remote file include lib script attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2343
// Rule Name: PROTOCOL-FTP STOR overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2343
static inline int check_rule_2343(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['STOR",nocase', '!"|0D|",within 200', '!"|0A|",within 200', '!"|00|",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2343
static inline void alert_rule_2343(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2343;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP STOR overflow attempt", 
                     sizeof("PROTOCOL-FTP STOR overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2344
// Rule Name: PROTOCOL-FTP XCWD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2344
static inline int check_rule_2344(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: XCWD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2344
static inline void alert_rule_2344(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2344;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP XCWD overflow attempt", 
                     sizeof("PROTOCOL-FTP XCWD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2345
// Rule Name: SERVER-WEBAPP PhpGedView search.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2345
static inline int check_rule_2345(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.php",nocase', 'action=soundex",fast_pattern,nocase', 'firstname=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2345
static inline void alert_rule_2345(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2345;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PhpGedView search.php access", 
                     sizeof("SERVER-WEBAPP PhpGedView search.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2346
// Rule Name: SERVER-WEBAPP myPHPNuke chatheader.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2346
static inline int check_rule_2346(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /chatheader.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2346
static inline void alert_rule_2346(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2346;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP myPHPNuke chatheader.php access", 
                     sizeof("SERVER-WEBAPP myPHPNuke chatheader.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2347
// Rule Name: SERVER-WEBAPP myPHPNuke partner.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2347
static inline int check_rule_2347(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /partner.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2347
static inline void alert_rule_2347(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2347;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP myPHPNuke partner.php access", 
                     sizeof("SERVER-WEBAPP myPHPNuke partner.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2353
// Rule Name: SERVER-WEBAPP IdeaBox cord.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2353
static inline int check_rule_2353(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php",nocase', 'ideaDir=",fast_pattern,nocase', 'cord.php",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2353
static inline void alert_rule_2353(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2353;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP IdeaBox cord.php file include", 
                     sizeof("SERVER-WEBAPP IdeaBox cord.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2354
// Rule Name: SERVER-WEBAPP IdeaBox notification.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2354
static inline int check_rule_2354(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php",nocase', 'gorumDir=",fast_pattern,nocase', 'notification.php",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2354
static inline void alert_rule_2354(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2354;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP IdeaBox notification.php file include", 
                     sizeof("SERVER-WEBAPP IdeaBox notification.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2355
// Rule Name: SERVER-WEBAPP Invision Board emailer.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2355
static inline int check_rule_2355(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ad_member.php",fast_pattern,nocase', 'emailer.php",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2355
static inline void alert_rule_2355(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2355;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Invision Board emailer.php file include", 
                     sizeof("SERVER-WEBAPP Invision Board emailer.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2356
// Rule Name: SERVER-WEBAPP WebChat db_mysql.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2356
static inline int check_rule_2356(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/defines.php",nocase', 'WEBCHATPATH=",nocase', 'db_mysql.php",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2356
static inline void alert_rule_2356(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2356;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WebChat db_mysql.php file include", 
                     sizeof("SERVER-WEBAPP WebChat db_mysql.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2357
// Rule Name: SERVER-WEBAPP WebChat english.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2357
static inline int check_rule_2357(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/defines.php",nocase', 'WEBCHATPATH=",nocase', 'english.php",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2357
static inline void alert_rule_2357(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2357;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WebChat english.php file include", 
                     sizeof("SERVER-WEBAPP WebChat english.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2358
// Rule Name: SERVER-WEBAPP Typo3 translations.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2358
static inline int check_rule_2358(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/translations.php",fast_pattern,nocase', 'ONLY=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2358
static inline void alert_rule_2358(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2358;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Typo3 translations.php file include", 
                     sizeof("SERVER-WEBAPP Typo3 translations.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2359
// Rule Name: SERVER-WEBAPP Invision Board ipchat.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2359
static inline int check_rule_2359(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ipchat.php",nocase', 'root_path=', 'conf_global.php",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2359
static inline void alert_rule_2359(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2359;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Invision Board ipchat.php file include", 
                     sizeof("SERVER-WEBAPP Invision Board ipchat.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2360
// Rule Name: SERVER-WEBAPP myphpPagetool pt_config.inc file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2360
static inline int check_rule_2360(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/doc/admin",nocase', 'ptinclude=",nocase', 'pt_config.inc",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2360
static inline void alert_rule_2360(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2360;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP myphpPagetool pt_config.inc file include", 
                     sizeof("SERVER-WEBAPP myphpPagetool pt_config.inc file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2361
// Rule Name: SERVER-WEBAPP news.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2361
static inline int check_rule_2361(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/news.php",fast_pattern,nocase', 'template=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2361
static inline void alert_rule_2361(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2361;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP news.php file include", 
                     sizeof("SERVER-WEBAPP news.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2362
// Rule Name: SERVER-WEBAPP YaBB SE packages.php file include
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2362
static inline int check_rule_2362(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/packages.php",fast_pattern,nocase', 'packer.php",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2362
static inline void alert_rule_2362(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2362;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP YaBB SE packages.php file include", 
                     sizeof("SERVER-WEBAPP YaBB SE packages.php file include"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2363
// Rule Name: SERVER-WEBAPP Cyboards default_header.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2363
static inline int check_rule_2363(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /default_header.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2363
static inline void alert_rule_2363(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2363;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cyboards default_header.php access", 
                     sizeof("SERVER-WEBAPP Cyboards default_header.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2364
// Rule Name: SERVER-WEBAPP Cyboards options_form.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2364
static inline int check_rule_2364(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /options_form.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2364
static inline void alert_rule_2364(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2364;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cyboards options_form.php access", 
                     sizeof("SERVER-WEBAPP Cyboards options_form.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2365
// Rule Name: SERVER-WEBAPP newsPHP Language file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2365
static inline int check_rule_2365(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nphpd.php",fast_pattern,nocase', 'LangFile",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2365
static inline void alert_rule_2365(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2365;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP newsPHP Language file include attempt", 
                     sizeof("SERVER-WEBAPP newsPHP Language file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2366
// Rule Name: SERVER-WEBAPP PhpGedView PGV authentication_index.php base directory manipulation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2366
static inline int check_rule_2366(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/authentication_index.php",nocase', 'PGV_BASE_DIRECTORY",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2366
static inline void alert_rule_2366(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2366;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PhpGedView PGV authentication_index.php base directory manipulation attempt", 
                     sizeof("SERVER-WEBAPP PhpGedView PGV authentication_index.php base directory manipulation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2367
// Rule Name: SERVER-WEBAPP PhpGedView PGV functions.php base directory manipulation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2367
static inline int check_rule_2367(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/functions.php",nocase', 'PGV_BASE_DIRECTORY",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2367
static inline void alert_rule_2367(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2367;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PhpGedView PGV functions.php base directory manipulation attempt", 
                     sizeof("SERVER-WEBAPP PhpGedView PGV functions.php base directory manipulation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2368
// Rule Name: SERVER-WEBAPP PhpGedView PGV config_gedcom.php base directory manipulation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2368
static inline int check_rule_2368(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/config_gedcom.php",nocase', 'PGV_BASE_DIRECTORY",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2368
static inline void alert_rule_2368(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2368;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PhpGedView PGV config_gedcom.php base directory manipulation attempt", 
                     sizeof("SERVER-WEBAPP PhpGedView PGV config_gedcom.php base directory manipulation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2369
// Rule Name: SERVER-WEBAPP ISAPISkeleton.dll access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2369
static inline int check_rule_2369(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ISAPISkeleton.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2369
static inline void alert_rule_2369(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2369;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ISAPISkeleton.dll access", 
                     sizeof("SERVER-WEBAPP ISAPISkeleton.dll access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2370
// Rule Name: SERVER-WEBAPP BugPort config.conf file access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2370
static inline int check_rule_2370(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /config.conf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2370
static inline void alert_rule_2370(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2370;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BugPort config.conf file access", 
                     sizeof("SERVER-WEBAPP BugPort config.conf file access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2371
// Rule Name: SERVER-WEBAPP Sample_showcode.html access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2371
static inline int check_rule_2371(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Sample_showcode.html",nocase', 'fname']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2371
static inline void alert_rule_2371(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2371;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Sample_showcode.html access", 
                     sizeof("SERVER-WEBAPP Sample_showcode.html access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2372
// Rule Name: SERVER-WEBAPP Photopost PHP Pro showphoto.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2372
static inline int check_rule_2372(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /showphoto.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2372
static inline void alert_rule_2372(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2372;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Photopost PHP Pro showphoto.php access", 
                     sizeof("SERVER-WEBAPP Photopost PHP Pro showphoto.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2373
// Rule Name: PROTOCOL-FTP XMKD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2373
static inline int check_rule_2373(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: XMKD",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2373
static inline void alert_rule_2373(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2373;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP XMKD overflow attempt", 
                     sizeof("PROTOCOL-FTP XMKD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2374
// Rule Name: PROTOCOL-FTP NLST overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2374
static inline int check_rule_2374(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: NLST",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2374
static inline void alert_rule_2374(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2374;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP NLST overflow attempt", 
                     sizeof("PROTOCOL-FTP NLST overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2375
// Rule Name: MALWARE-CNC DoomJuice/mydoom.a backdoor upload/execute
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 2375
static inline int check_rule_2375(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 3127-3199
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 3127 || dst_port > 3199)
        return 0;
    // Content matching (simplified)
    // Full pattern: |85 13|<|9E A2|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2375
static inline void alert_rule_2375(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2375;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC DoomJuice/mydoom.a backdoor upload/execute", 
                     sizeof("MALWARE-CNC DoomJuice/mydoom.a backdoor upload/execute"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2386
// Rule Name: SERVER-IIS NTLM ASN1 vulnerability scan attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2386
static inline int check_rule_2386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Authorization|3A|",nocase', 'Negotiate",within 20,nocase', 'YIQAAABiBoMAAAYrBgEFBQKgggBTMIFQoA4wDAYKKwYBBAGCNwICCqM",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2386
static inline void alert_rule_2386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS NTLM ASN1 vulnerability scan attempt", 
                     sizeof("SERVER-IIS NTLM ASN1 vulnerability scan attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2388
// Rule Name: SERVER-WEBAPP Apple QuickTime streaming server view_broadcast.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2388
static inline int check_rule_2388(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /view_broadcast.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2388
static inline void alert_rule_2388(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2388;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Apple QuickTime streaming server view_broadcast.cgi access", 
                     sizeof("SERVER-WEBAPP Apple QuickTime streaming server view_broadcast.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2389
// Rule Name: PROTOCOL-FTP RNTO overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2389
static inline int check_rule_2389(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RNTO",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2389
static inline void alert_rule_2389(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2389;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RNTO overflow attempt", 
                     sizeof("PROTOCOL-FTP RNTO overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2390
// Rule Name: PROTOCOL-FTP STOU overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2390
static inline int check_rule_2390(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: STOU",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2390
static inline void alert_rule_2390(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2390;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP STOU overflow attempt", 
                     sizeof("PROTOCOL-FTP STOU overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2391
// Rule Name: PROTOCOL-FTP APPE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2391
static inline int check_rule_2391(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: APPE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2391
static inline void alert_rule_2391(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2391;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP APPE overflow attempt", 
                     sizeof("PROTOCOL-FTP APPE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2392
// Rule Name: PROTOCOL-FTP RETR overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2392
static inline int check_rule_2392(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RETR",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2392
static inline void alert_rule_2392(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2392;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RETR overflow attempt", 
                     sizeof("PROTOCOL-FTP RETR overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2393
// Rule Name: SERVER-WEBAPP /_admin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2393
static inline int check_rule_2393(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /_admin/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2393
static inline void alert_rule_2393(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2393;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /_admin access", 
                     sizeof("SERVER-WEBAPP /_admin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2394
// Rule Name: SERVER-WEBAPP Compaq web-based management agent denial of service attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2394
static inline int check_rule_2394(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2301
    if (tcph->dest != htons(2301))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['<!",depth 75', '>",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2394
static inline void alert_rule_2394(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2394;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Compaq web-based management agent denial of service attempt", 
                     sizeof("SERVER-WEBAPP Compaq web-based management agent denial of service attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2395
// Rule Name: SERVER-WEBAPP InteractiveQuery.jsp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2395
static inline int check_rule_2395(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /InteractiveQuery.jsp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2395
static inline void alert_rule_2395(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2395;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP InteractiveQuery.jsp access", 
                     sizeof("SERVER-WEBAPP InteractiveQuery.jsp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2396
// Rule Name: SERVER-WEBAPP CCBill whereami.cgi arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2396
static inline int check_rule_2396(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/whereami.cgi?",nocase', 'g=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2396
static inline void alert_rule_2396(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2396;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CCBill whereami.cgi arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP CCBill whereami.cgi arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2397
// Rule Name: SERVER-WEBAPP CCBill whereami.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2397
static inline int check_rule_2397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /whereami.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2397
static inline void alert_rule_2397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP CCBill whereami.cgi access", 
                     sizeof("SERVER-WEBAPP CCBill whereami.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2398
// Rule Name: SERVER-WEBAPP WAnewsletter newsletter.php file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2398
static inline int check_rule_2398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['newsletter.php",nocase', 'waroot",fast_pattern,nocase', 'start.php",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2398
static inline void alert_rule_2398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WAnewsletter newsletter.php file include attempt", 
                     sizeof("SERVER-WEBAPP WAnewsletter newsletter.php file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2399
// Rule Name: SERVER-WEBAPP WAnewsletter db_type.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2399
static inline int check_rule_2399(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sql/db_type.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2399
static inline void alert_rule_2399(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2399;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WAnewsletter db_type.php access", 
                     sizeof("SERVER-WEBAPP WAnewsletter db_type.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2400
// Rule Name: SERVER-WEBAPP edittag.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2400
static inline int check_rule_2400(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /edittag.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2400
static inline void alert_rule_2400(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2400;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP edittag.pl access", 
                     sizeof("SERVER-WEBAPP edittag.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2401
// Rule Name: NETBIOS SMB Session Setup andx username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2401
static inline int check_rule_2401(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', 's",depth 1,offset 39', '!"|00|",within 255,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2401
static inline void alert_rule_2401(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2401;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Session Setup andx username overflow attempt", 
                     sizeof("NETBIOS SMB Session Setup andx username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2402
// Rule Name: NETBIOS SMB-DS Session Setup andx username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2402
static inline int check_rule_2402(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', 's",depth 1,offset 39', '!"|00|",within 255,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2402
static inline void alert_rule_2402(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2402;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Session Setup andx username overflow attempt", 
                     sizeof("NETBIOS SMB-DS Session Setup andx username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2403
// Rule Name: NETBIOS SMB Session Setup unicode username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2403
static inline int check_rule_2403(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMBs",within 5,distance 3', '!"|00 00|",within 510,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2403
static inline void alert_rule_2403(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2403;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Session Setup unicode username overflow attempt", 
                     sizeof("NETBIOS SMB Session Setup unicode username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2404
// Rule Name: NETBIOS SMB-DS Session Setup unicode andx username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2404
static inline int check_rule_2404(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', 's",depth 1,offset 39', '!"|00 00|",within 510,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2404
static inline void alert_rule_2404(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2404;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Session Setup unicode andx username overflow attempt", 
                     sizeof("NETBIOS SMB-DS Session Setup unicode andx username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2405
// Rule Name: SERVER-WEBAPP phptest.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2405
static inline int check_rule_2405(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /phptest.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2405
static inline void alert_rule_2405(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2405;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP phptest.php access", 
                     sizeof("SERVER-WEBAPP phptest.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2406
// Rule Name: PROTOCOL-TELNET APC SmartSlot default admin account attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: suspicious-login
// Generated from Snort rule parser output

// Check function for rule 2406
static inline int check_rule_2406(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: TENmanUFactOryPOWER",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2406
static inline void alert_rule_2406(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2406;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET APC SmartSlot default admin account attempt", 
                     sizeof("PROTOCOL-TELNET APC SmartSlot default admin account attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2407
// Rule Name: SERVER-WEBAPP util.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2407
static inline int check_rule_2407(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /util.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2407
static inline void alert_rule_2407(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2407;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP util.pl access", 
                     sizeof("SERVER-WEBAPP util.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2408
// Rule Name: SERVER-WEBAPP Invision Power Board search.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2408
static inline int check_rule_2408(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.pl', 'st=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2408
static inline void alert_rule_2408(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2408;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Invision Power Board search.pl access", 
                     sizeof("SERVER-WEBAPP Invision Power Board search.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2409
// Rule Name: PROTOCOL-POP APOP USER overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2409
static inline int check_rule_2409(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: APOP",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2409
static inline void alert_rule_2409(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2409;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP APOP USER overflow attempt", 
                     sizeof("PROTOCOL-POP APOP USER overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2410
// Rule Name: SERVER-WEBAPP IGeneric Free Shopping Cart page.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2410
static inline int check_rule_2410(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /page.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2410
static inline void alert_rule_2410(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2410;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP IGeneric Free Shopping Cart page.php access", 
                     sizeof("SERVER-WEBAPP IGeneric Free Shopping Cart page.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2411
// Rule Name: SERVER-WEBAPP RealNetworks RealSystem Server DESCRIBE buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2411
static inline int check_rule_2411(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 554
    if (tcph->dest != htons(554))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['DESCRIBE",nocase', '../",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2411
static inline void alert_rule_2411(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2411;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP RealNetworks RealSystem Server DESCRIBE buffer overflow attempt", 
                     sizeof("SERVER-WEBAPP RealNetworks RealSystem Server DESCRIBE buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2412
// Rule Name: INDICATOR-COMPROMISE successful cross site scripting forced download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-user
// Generated from Snort rule parser output

// Check function for rule 2412
static inline int check_rule_2412(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |0A|Referer|3A| res|3A|/C|3A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2412
static inline void alert_rule_2412(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2412;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE successful cross site scripting forced download attempt", 
                     sizeof("INDICATOR-COMPROMISE successful cross site scripting forced download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2416
// Rule Name: PROTOCOL-FTP invalid MDTM command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2416
static inline int check_rule_2416(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MDTM",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2416
static inline void alert_rule_2416(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2416;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP invalid MDTM command attempt", 
                     sizeof("PROTOCOL-FTP invalid MDTM command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2417
// Rule Name: PROTOCOL-FTP format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: string-detect
// Generated from Snort rule parser output

// Check function for rule 2417
static inline int check_rule_2417(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: %",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2417
static inline void alert_rule_2417(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2417;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP format string attempt", 
                     sizeof("PROTOCOL-FTP format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2418
// Rule Name: POLICY-OTHER Microsoft Windows Terminal Server no encryption session initiation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2418
static inline int check_rule_2418(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|03 00 01|",depth 3', '|00|",depth 1,offset 288']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2418
static inline void alert_rule_2418(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2418;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Microsoft Windows Terminal Server no encryption session initiation attempt", 
                     sizeof("POLICY-OTHER Microsoft Windows Terminal Server no encryption session initiation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2419
// Rule Name: FILE-IDENTIFY RealNetworks Realplayer .ram playlist file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2419
static inline int check_rule_2419(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .ra",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2419
static inline void alert_rule_2419(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2419;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RealNetworks Realplayer .ram playlist file download request", 
                     sizeof("FILE-IDENTIFY RealNetworks Realplayer .ram playlist file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2420
// Rule Name: FILE-IDENTIFY RealNetworks Realplayer .rmp playlist file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2420
static inline int check_rule_2420(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .rmp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2420
static inline void alert_rule_2420(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2420;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RealNetworks Realplayer .rmp playlist file download request", 
                     sizeof("FILE-IDENTIFY RealNetworks Realplayer .rmp playlist file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2422
// Rule Name: FILE-IDENTIFY RealNetworks Realplayer .rt playlist file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2422
static inline int check_rule_2422(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .rt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2422
static inline void alert_rule_2422(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2422;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RealNetworks Realplayer .rt playlist file download request", 
                     sizeof("FILE-IDENTIFY RealNetworks Realplayer .rt playlist file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2423
// Rule Name: FILE-IDENTIFY RealNetworks Realplayer .rp playlist file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2423
static inline int check_rule_2423(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .rp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2423
static inline void alert_rule_2423(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2423;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RealNetworks Realplayer .rp playlist file download request", 
                     sizeof("FILE-IDENTIFY RealNetworks Realplayer .rp playlist file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2424
// Rule Name: PROTOCOL-NNTP sendsys overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2424
static inline int check_rule_2424(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: sendsys",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2424
static inline void alert_rule_2424(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2424;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP sendsys overflow attempt", 
                     sizeof("PROTOCOL-NNTP sendsys overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2425
// Rule Name: PROTOCOL-NNTP senduuname overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2425
static inline int check_rule_2425(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: senduuname",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2425
static inline void alert_rule_2425(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2425;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP senduuname overflow attempt", 
                     sizeof("PROTOCOL-NNTP senduuname overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2426
// Rule Name: PROTOCOL-NNTP version overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2426
static inline int check_rule_2426(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: version",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2426
static inline void alert_rule_2426(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2426;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP version overflow attempt", 
                     sizeof("PROTOCOL-NNTP version overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2427
// Rule Name: PROTOCOL-NNTP checkgroups overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2427
static inline int check_rule_2427(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: checkgroups",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2427
static inline void alert_rule_2427(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2427;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP checkgroups overflow attempt", 
                     sizeof("PROTOCOL-NNTP checkgroups overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2428
// Rule Name: PROTOCOL-NNTP ihave overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2428
static inline int check_rule_2428(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: ihave",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2428
static inline void alert_rule_2428(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2428;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP ihave overflow attempt", 
                     sizeof("PROTOCOL-NNTP ihave overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2429
// Rule Name: PROTOCOL-NNTP sendme overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2429
static inline int check_rule_2429(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: sendme",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2429
static inline void alert_rule_2429(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2429;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP sendme overflow attempt", 
                     sizeof("PROTOCOL-NNTP sendme overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2430
// Rule Name: PROTOCOL-NNTP newgroup overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2430
static inline int check_rule_2430(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: newgroup",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2430
static inline void alert_rule_2430(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2430;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP newgroup overflow attempt", 
                     sizeof("PROTOCOL-NNTP newgroup overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2431
// Rule Name: PROTOCOL-NNTP rmgroup overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2431
static inline int check_rule_2431(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: rmgroup",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2431
static inline void alert_rule_2431(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2431;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP rmgroup overflow attempt", 
                     sizeof("PROTOCOL-NNTP rmgroup overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2432
// Rule Name: PROTOCOL-NNTP article post without path attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2432
static inline int check_rule_2432(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: takethis",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2432
static inline void alert_rule_2432(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2432;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP article post without path attempt", 
                     sizeof("PROTOCOL-NNTP article post without path attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2433
// Rule Name: SERVER-WEBAPP MDaemon form2raw.cgi overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2433
static inline int check_rule_2433(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3000
    if (tcph->dest != htons(3000))
        return 0;
    // Content matching (simplified)
    // Full pattern: /form2raw.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2433
static inline void alert_rule_2433(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2433;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MDaemon form2raw.cgi overflow attempt", 
                     sizeof("SERVER-WEBAPP MDaemon form2raw.cgi overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2434
// Rule Name: SERVER-WEBAPP MDaemon form2raw.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2434
static inline int check_rule_2434(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /form2raw.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2434
static inline void alert_rule_2434(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2434;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MDaemon form2raw.cgi access", 
                     sizeof("SERVER-WEBAPP MDaemon form2raw.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2435
// Rule Name: FILE-IDENTIFY Microsoft emf file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2435
static inline int check_rule_2435(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .emf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2435
static inline void alert_rule_2435(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2435;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft emf file download request", 
                     sizeof("FILE-IDENTIFY Microsoft emf file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2436
// Rule Name: FILE-IDENTIFY Microsoft Windows Audio wmf file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 2436
static inline int check_rule_2436(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .wmf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2436
static inline void alert_rule_2436(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2436;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Windows Audio wmf file download request", 
                     sizeof("FILE-IDENTIFY Microsoft Windows Audio wmf file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2438
// Rule Name: FILE-MULTIMEDIA RealNetworks RealPlayer playlist file URL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2438
static inline int check_rule_2438(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: file|3A|//",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2438
static inline void alert_rule_2438(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2438;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-MULTIMEDIA RealNetworks RealPlayer playlist file URL overflow attempt", 
                     sizeof("FILE-MULTIMEDIA RealNetworks RealPlayer playlist file URL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2439
// Rule Name: FILE-MULTIMEDIA RealNetworks RealPlayer playlist http URL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2439
static inline int check_rule_2439(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: http|3A|//",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2439
static inline void alert_rule_2439(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2439;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-MULTIMEDIA RealNetworks RealPlayer playlist http URL overflow attempt", 
                     sizeof("FILE-MULTIMEDIA RealNetworks RealPlayer playlist http URL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2440
// Rule Name: FILE-MULTIMEDIA RealNetworks RealPlayer playlist rtsp URL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2440
static inline int check_rule_2440(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: rtsp|3A|//",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2440
static inline void alert_rule_2440(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2440;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-MULTIMEDIA RealNetworks RealPlayer playlist rtsp URL overflow attempt", 
                     sizeof("FILE-MULTIMEDIA RealNetworks RealPlayer playlist rtsp URL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2441
// Rule Name: SERVER-WEBAPP NetObserve authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2441
static inline int check_rule_2441(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['login=0",nocase', 'login=0",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2441
static inline void alert_rule_2441(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2441;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP NetObserve authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP NetObserve authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2447
// Rule Name: SERVER-WEBAPP ServletManager access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2447
static inline int check_rule_2447(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /servlet/ServletManager",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2447
static inline void alert_rule_2447(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2447;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ServletManager access", 
                     sizeof("SERVER-WEBAPP ServletManager access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2448
// Rule Name: SERVER-WEBAPP setinfo.hts access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2448
static inline int check_rule_2448(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /setinfo.hts",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2448
static inline void alert_rule_2448(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2448;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP setinfo.hts access", 
                     sizeof("SERVER-WEBAPP setinfo.hts access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2449
// Rule Name: PROTOCOL-FTP ALLO overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2449
static inline int check_rule_2449(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ALLO",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2449
static inline void alert_rule_2449(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2449;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP ALLO overflow attempt", 
                     sizeof("PROTOCOL-FTP ALLO overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2450
// Rule Name: POLICY-SOCIAL Yahoo IM successful logon
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2450
static inline int check_rule_2450(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 01|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2450
static inline void alert_rule_2450(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2450;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM successful logon", 
                     sizeof("POLICY-SOCIAL Yahoo IM successful logon"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2451
// Rule Name: POLICY-SOCIAL Yahoo IM voicechat
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2451
static inline int check_rule_2451(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00|J",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2451
static inline void alert_rule_2451(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2451;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM voicechat", 
                     sizeof("POLICY-SOCIAL Yahoo IM voicechat"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2452
// Rule Name: POLICY-SOCIAL Yahoo IM ping
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2452
static inline int check_rule_2452(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 5050
    if (tcph->dest != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 12|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2452
static inline void alert_rule_2452(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2452;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM ping", 
                     sizeof("POLICY-SOCIAL Yahoo IM ping"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2453
// Rule Name: POLICY-SOCIAL Yahoo IM conference invitation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2453
static inline int check_rule_2453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 18|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2453
static inline void alert_rule_2453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference invitation", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference invitation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2454
// Rule Name: POLICY-SOCIAL Yahoo IM conference logon success
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2454
static inline int check_rule_2454(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 19|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2454
static inline void alert_rule_2454(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2454;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference logon success", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference logon success"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2455
// Rule Name: POLICY-SOCIAL Yahoo IM conference message
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2455
static inline int check_rule_2455(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 5050
    if (tcph->dest != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 1D|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2455
static inline void alert_rule_2455(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2455;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference message", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference message"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2456
// Rule Name: POLICY-SOCIAL Yahoo Messenger File Transfer Receive Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2456
static inline int check_rule_2456(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4', '|00|M",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2456
static inline void alert_rule_2456(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2456;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo Messenger File Transfer Receive Request", 
                     sizeof("POLICY-SOCIAL Yahoo Messenger File Transfer Receive Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2457
// Rule Name: POLICY-SOCIAL Yahoo IM message
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2457
static inline int check_rule_2457(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Check dst port: 5101
    if (tcph->dest != htons(5101))
        return 0;
    // Content matching (simplified)
    // Full pattern: YMSG",depth 4,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2457
static inline void alert_rule_2457(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2457;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM message", 
                     sizeof("POLICY-SOCIAL Yahoo IM message"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2458
// Rule Name: POLICY-SOCIAL Yahoo IM successful chat join
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2458
static inline int check_rule_2458(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5050
    if (tcph->source != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00 98|",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2458
static inline void alert_rule_2458(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2458;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM successful chat join", 
                     sizeof("POLICY-SOCIAL Yahoo IM successful chat join"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2459
// Rule Name: POLICY-SOCIAL Yahoo IM conference offer invitation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2459
static inline int check_rule_2459(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 5050
    if (tcph->dest != htons(5050))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['YMSG",depth 4,nocase', '|00|P",depth 2,offset 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2459
static inline void alert_rule_2459(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2459;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference offer invitation", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference offer invitation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2460
// Rule Name: POLICY-SOCIAL Yahoo IM conference request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2460
static inline int check_rule_2460(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 5100
    if (tcph->dest != htons(5100))
        return 0;
    // Content matching (simplified)
    // Full pattern: <R",depth 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2460
static inline void alert_rule_2460(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2460;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference request", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2461
// Rule Name: POLICY-SOCIAL Yahoo IM conference watch
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2461
static inline int check_rule_2461(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 5100
    if (tcph->source != htons(5100))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0D 00 05 00|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2461
static inline void alert_rule_2461(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2461;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SOCIAL Yahoo IM conference watch", 
                     sizeof("POLICY-SOCIAL Yahoo IM conference watch"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2474
// Rule Name: NETBIOS SMB-DS ADMIN$ share access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2474
static inline int check_rule_2474(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMBu",within 5,distance 3', 'ADMIN|24 00|",distance 2,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2474
static inline void alert_rule_2474(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2474;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS ADMIN$ share access", 
                     sizeof("NETBIOS SMB-DS ADMIN$ share access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2484
// Rule Name: SERVER-WEBAPP source.jsp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2484
static inline int check_rule_2484(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /source.jsp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2484
static inline void alert_rule_2484(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2484;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP source.jsp access", 
                     sizeof("SERVER-WEBAPP source.jsp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2485
// Rule Name: BROWSER-PLUGINS Symantec Norton Internet Security 2004 ActiveX clsid access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2485
static inline int check_rule_2485(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: 0534CF61-83C5-4765-B19B-45F7A4E135D0",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2485
static inline void alert_rule_2485(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2485;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-PLUGINS Symantec Norton Internet Security 2004 ActiveX clsid access", 
                     sizeof("BROWSER-PLUGINS Symantec Norton Internet Security 2004 ActiveX clsid access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2487
// Rule Name: SERVER-MAIL WinZip MIME content-type buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2487
static inline int check_rule_2487(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Content-Type|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2487
static inline void alert_rule_2487(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2487;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL WinZip MIME content-type buffer overflow", 
                     sizeof("SERVER-MAIL WinZip MIME content-type buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2488
// Rule Name: SERVER-MAIL WinZip MIME content-disposition buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2488
static inline int check_rule_2488(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A|",fast_pattern,nocase', 'Content-Disposition|3A|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2488
static inline void alert_rule_2488(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2488;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL WinZip MIME content-disposition buffer overflow", 
                     sizeof("SERVER-MAIL WinZip MIME content-disposition buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2489
// Rule Name: SERVER-OTHER esignal STREAMQUOTE buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2489
static inline int check_rule_2489(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['<STREAMQUOTE>",nocase', '!"</STREAMQUOTE>",within 1040,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2489
static inline void alert_rule_2489(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2489;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER esignal STREAMQUOTE buffer overflow attempt", 
                     sizeof("SERVER-OTHER esignal STREAMQUOTE buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2490
// Rule Name: SERVER-OTHER esignal SNAPQUOTE buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2490
static inline int check_rule_2490(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['<SNAPQUOTE>",nocase', '!"</SNAPQUOTE>",within 1052,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2490
static inline void alert_rule_2490(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2490;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER esignal SNAPQUOTE buffer overflow attempt", 
                     sizeof("SERVER-OTHER esignal SNAPQUOTE buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2508
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP lsass DsRolerUpgradeDownlevelServer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2508
static inline int check_rule_2508(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    return 1;  // All checks passed
}

// Alert function for rule 2508
static inline void alert_rule_2508(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2508;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP lsass DsRolerUpgradeDownlevelServer overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP lsass DsRolerUpgradeDownlevelServer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2523
// Rule Name: SERVER-OTHER BGP spoofed connection reset attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 2523
static inline int check_rule_2523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 179
    if (tcph->dest != htons(179))
        return 0;
    // Check TCP flags
    if (!((tcph->syn == 1) && (tcph->fin == 1) && (tcph->rst == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 2523
static inline void alert_rule_2523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER BGP spoofed connection reset attempt", 
                     sizeof("SERVER-OTHER BGP spoofed connection reset attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2545
// Rule Name: SERVER-OTHER AFP FPLoginExt username buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2545
static inline int check_rule_2545(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 548
    if (tcph->dest != htons(548))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 02|",depth 2', '?",within 1,distance 14', 'cleartxt passwrd",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2545
static inline void alert_rule_2545(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2545;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER AFP FPLoginExt username buffer overflow attempt", 
                     sizeof("SERVER-OTHER AFP FPLoginExt username buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2546
// Rule Name: PROTOCOL-FTP MDTM overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2546
static inline int check_rule_2546(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: MDTM",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2546
static inline void alert_rule_2546(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2546;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP MDTM overflow attempt", 
                     sizeof("PROTOCOL-FTP MDTM overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2547
// Rule Name: SERVER-OTHER HP Web JetAdmin remote file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2547
static inline int check_rule_2547(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /plugins/hpjwja/script/devices_update_printer_fw_upload.hts",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2547
static inline void alert_rule_2547(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2547;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER HP Web JetAdmin remote file upload attempt", 
                     sizeof("SERVER-OTHER HP Web JetAdmin remote file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2548
// Rule Name: SERVER-OTHER HP Web JetAdmin setinfo access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2548
static inline int check_rule_2548(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /plugins/hpjdwm/script/test/setinfo.hts",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2548
static inline void alert_rule_2548(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2548;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER HP Web JetAdmin setinfo access attempt", 
                     sizeof("SERVER-OTHER HP Web JetAdmin setinfo access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2549
// Rule Name: SERVER-OTHER HP Web JetAdmin file write attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2549
static inline int check_rule_2549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/plugins/framework/script/tree.xms",fast_pattern,nocase', 'WriteToFile",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2549
static inline void alert_rule_2549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER HP Web JetAdmin file write attempt", 
                     sizeof("SERVER-OTHER HP Web JetAdmin file write attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2550
// Rule Name: FILE-OTHER Nullsoft Winamp XM file buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2550
static inline int check_rule_2550(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Extended Module|3A 20|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2550
static inline void alert_rule_2550(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2550;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER Nullsoft Winamp XM file buffer overflow attempt", 
                     sizeof("FILE-OTHER Nullsoft Winamp XM file buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2551
// Rule Name: SERVER-OTHER Oracle Web Cache GET overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2551
static inline int check_rule_2551(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: GET
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2551
static inline void alert_rule_2551(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2551;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache GET overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache GET overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2552
// Rule Name: SERVER-OTHER Oracle Web Cache HEAD overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2552
static inline int check_rule_2552(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: HEAD
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2552
static inline void alert_rule_2552(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2552;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache HEAD overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache HEAD overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2553
// Rule Name: SERVER-OTHER Oracle Web Cache PUT overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2553
static inline int check_rule_2553(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: PUT
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2553
static inline void alert_rule_2553(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2553;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache PUT overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache PUT overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2554
// Rule Name: SERVER-OTHER Oracle Web Cache POST overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2554
static inline int check_rule_2554(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: POST
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2554
static inline void alert_rule_2554(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2554;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache POST overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache POST overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2555
// Rule Name: SERVER-OTHER Oracle Web Cache TRACE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2555
static inline int check_rule_2555(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: TRACE
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2555
static inline void alert_rule_2555(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2555;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache TRACE overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache TRACE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2556
// Rule Name: SERVER-OTHER Oracle Web Cache DELETE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2556
static inline int check_rule_2556(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: DELETE
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2556
static inline void alert_rule_2556(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2556;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache DELETE overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache DELETE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2557
// Rule Name: SERVER-OTHER Oracle Web Cache LOCK overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2557
static inline int check_rule_2557(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: LOCK
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2557
static inline void alert_rule_2557(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2557;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache LOCK overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache LOCK overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2558
// Rule Name: SERVER-OTHER Oracle Web Cache MKCOL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2558
static inline int check_rule_2558(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: MKCOL
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2558
static inline void alert_rule_2558(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2558;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache MKCOL overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache MKCOL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2559
// Rule Name: SERVER-OTHER Oracle Web Cache COPY overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2559
static inline int check_rule_2559(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: COPY
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2559
static inline void alert_rule_2559(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2559;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache COPY overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache COPY overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2560
// Rule Name: SERVER-OTHER Oracle Web Cache MOVE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2560
static inline int check_rule_2560(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port range: 7777-7778
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 7777 || dst_port > 7778)
        return 0;
    // Content matching (simplified)
    // Full pattern: MOVE
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2560
static inline void alert_rule_2560(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2560;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Oracle Web Cache MOVE overflow attempt", 
                     sizeof("SERVER-OTHER Oracle Web Cache MOVE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2561
// Rule Name: SERVER-OTHER rsync backup-dir directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: string-detect
// Generated from Snort rule parser output

// Check function for rule 2561
static inline int check_rule_2561(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 873
    if (tcph->dest != htons(873))
        return 0;
    // Content matching (simplified)
    // Full pattern: --backup-dir",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2561
static inline void alert_rule_2561(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2561;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER rsync backup-dir directory traversal attempt", 
                     sizeof("SERVER-OTHER rsync backup-dir directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2562
// Rule Name: SERVER-WEBAPP McAfee ePO file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2562
static inline int check_rule_2562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 81
    if (tcph->dest != htons(81))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/spipe/repl_file",nocase', 'Command=BEGIN",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2562
static inline void alert_rule_2562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP McAfee ePO file upload attempt", 
                     sizeof("SERVER-WEBAPP McAfee ePO file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2565
// Rule Name: SERVER-WEBAPP modules.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2565
static inline int check_rule_2565(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /modules.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2565
static inline void alert_rule_2565(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2565;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP modules.php access", 
                     sizeof("SERVER-WEBAPP modules.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2566
// Rule Name: SERVER-WEBAPP PHPBB viewforum.php access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2566
static inline int check_rule_2566(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/viewforum.php",nocase', 'topic_id=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2566
static inline void alert_rule_2566(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2566;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHPBB viewforum.php access", 
                     sizeof("SERVER-WEBAPP PHPBB viewforum.php access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2567
// Rule Name: SERVER-WEBAPP Emumail init.emu access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2567
static inline int check_rule_2567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /init.emu",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2567
static inline void alert_rule_2567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Emumail init.emu access", 
                     sizeof("SERVER-WEBAPP Emumail init.emu access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2568
// Rule Name: SERVER-WEBAPP Emumail emumail.fcgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2568
static inline int check_rule_2568(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /emumail.fcgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2568
static inline void alert_rule_2568(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2568;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Emumail emumail.fcgi access", 
                     sizeof("SERVER-WEBAPP Emumail emumail.fcgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2569
// Rule Name: SERVER-WEBAPP cPanel resetpass access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2569
static inline int check_rule_2569(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /resetpass",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2569
static inline void alert_rule_2569(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2569;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP cPanel resetpass access", 
                     sizeof("SERVER-WEBAPP cPanel resetpass access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2570
// Rule Name: SERVER-WEBAPP invalid HTTP version string
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: non-standard-protocol
// Generated from Snort rule parser output

// Check function for rule 2570
static inline int check_rule_2570(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' HTTP/",depth 300,nocase', '!"0.9",within 3', '!"1.0",within 3', '!"1.1",within 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2570
static inline void alert_rule_2570(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2570;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP invalid HTTP version string", 
                     sizeof("SERVER-WEBAPP invalid HTTP version string"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2571
// Rule Name: SERVER-IIS SmarterTools SmarterMail frmGetAttachment.aspx access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2571
static inline int check_rule_2571(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /frmGetAttachment.aspx",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2571
static inline void alert_rule_2571(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2571;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS SmarterTools SmarterMail frmGetAttachment.aspx access", 
                     sizeof("SERVER-IIS SmarterTools SmarterMail frmGetAttachment.aspx access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2572
// Rule Name: SERVER-IIS SmarterTools SmarterMail login.aspx buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2572
static inline int check_rule_2572(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/login.aspx",nocase', 'txtusername=', '!"|0A|",within 980,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2572
static inline void alert_rule_2572(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2572;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS SmarterTools SmarterMail login.aspx buffer overflow attempt", 
                     sizeof("SERVER-IIS SmarterTools SmarterMail login.aspx buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2573
// Rule Name: SERVER-IIS SmarterTools SmarterMail frmCompose.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2573
static inline int check_rule_2573(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /frmCompose.aspx",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2573
static inline void alert_rule_2573(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2573;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS SmarterTools SmarterMail frmCompose.asp access", 
                     sizeof("SERVER-IIS SmarterTools SmarterMail frmCompose.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2574
// Rule Name: PROTOCOL-FTP RETR format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2574
static inline int check_rule_2574(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RETR",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2574
static inline void alert_rule_2574(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2574;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RETR format string attempt", 
                     sizeof("PROTOCOL-FTP RETR format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2575
// Rule Name: SERVER-WEBAPP Opt-X header.php remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2575
static inline int check_rule_2575(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/header.php",nocase', 'systempath=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2575
static inline void alert_rule_2575(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2575;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Opt-X header.php remote file include attempt", 
                     sizeof("SERVER-WEBAPP Opt-X header.php remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2576
// Rule Name: SERVER-ORACLE dbms_repcat.generate_replication_support buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2576
static inline int check_rule_2576(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.generate_replication_support",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2576
static inline void alert_rule_2576(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2576;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.generate_replication_support buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.generate_replication_support buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2577
// Rule Name: FILE-OTHER local resource redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2577
static inline int check_rule_2577(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Location|3A|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2577
static inline void alert_rule_2577(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2577;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER local resource redirection attempt", 
                     sizeof("FILE-OTHER local resource redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2580
// Rule Name: SERVER-WEBAPP server negative Content-Length attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2580
static inline int check_rule_2580(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Content-Length",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2580
static inline void alert_rule_2580(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2580;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP server negative Content-Length attempt", 
                     sizeof("SERVER-WEBAPP server negative Content-Length attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2581
// Rule Name: SERVER-WEBAPP SAP Crystal Reports crystalimagehandler.aspx access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2581
static inline int check_rule_2581(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /crystalimagehandler.aspx",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2581
static inline void alert_rule_2581(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2581;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SAP Crystal Reports crystalimagehandler.aspx access", 
                     sizeof("SERVER-WEBAPP SAP Crystal Reports crystalimagehandler.aspx access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2582
// Rule Name: OS-WINDOWS SAP Crystal Reports crystalImageHandler.asp directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2582
static inline int check_rule_2582(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/crystalimagehandler",fast_pattern,nocase', 'dynamicimage=../",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2582
static inline void alert_rule_2582(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2582;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS SAP Crystal Reports crystalImageHandler.asp directory traversal attempt", 
                     sizeof("OS-WINDOWS SAP Crystal Reports crystalImageHandler.asp directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2583
// Rule Name: SERVER-OTHER CVS Max-dotdot integer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 2583
static inline int check_rule_2583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 2401
    if (tcph->dest != htons(2401))
        return 0;
    // Content matching (simplified)
    // Full pattern: Max-dotdot",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2583
static inline void alert_rule_2583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER CVS Max-dotdot integer overflow attempt", 
                     sizeof("SERVER-OTHER CVS Max-dotdot integer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2584
// Rule Name: SERVER-OTHER eMule buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2584
static inline int check_rule_2584(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 6666-6669
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 6666 || src_port > 6669)
        return 0;
    // Content matching (simplified)
    // Full pattern: PRIVMSG",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2584
static inline void alert_rule_2584(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2584;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER eMule buffer overflow attempt", 
                     sizeof("SERVER-OTHER eMule buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2585
// Rule Name: SERVER-WEBAPP nessus 2.x 404 probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2585
static inline int check_rule_2585(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /NessusTest",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2585
static inline void alert_rule_2585(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2585;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP nessus 2.x 404 probe", 
                     sizeof("SERVER-WEBAPP nessus 2.x 404 probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2587
// Rule Name: PUA-P2P eDonkey server response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 2587
static inline int check_rule_2587(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 4711
    if (tcph->source != htons(4711))
        return 0;
    // Content matching (simplified)
    // Full pattern: Server|3A| eMule",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2587
static inline void alert_rule_2587(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2587;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-P2P eDonkey server response", 
                     sizeof("PUA-P2P eDonkey server response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2588
// Rule Name: SERVER-WEBAPP TUTOS path disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2588
static inline int check_rule_2588(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/note_overview.php', 'id=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2588
static inline void alert_rule_2588(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2588;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TUTOS path disclosure attempt", 
                     sizeof("SERVER-WEBAPP TUTOS path disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2597
// Rule Name: SERVER-WEBAPP Samba SWAT Authorization overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2597
static inline int check_rule_2597(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Authorization|3A|",nocase', 'Basic",within 50,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2597
static inline void alert_rule_2597(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2597;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Samba SWAT Authorization overflow attempt", 
                     sizeof("SERVER-WEBAPP Samba SWAT Authorization overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2598
// Rule Name: SERVER-WEBAPP Samba SWAT Authorization port 901 overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2598
static inline int check_rule_2598(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Authorization|3A| Basic",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2598
static inline void alert_rule_2598(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2598;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Samba SWAT Authorization port 901 overflow attempt", 
                     sizeof("SERVER-WEBAPP Samba SWAT Authorization port 901 overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2599
// Rule Name: SERVER-ORACLE dbms_repcat.add_grouped_column buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2599
static inline int check_rule_2599(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_grouped_column",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2599
static inline void alert_rule_2599(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2599;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_grouped_column buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_grouped_column buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2601
// Rule Name: SERVER-ORACLE dbms_repcat.drop_master_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2601
static inline int check_rule_2601(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_master_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2601
static inline void alert_rule_2601(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2601;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_master_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_master_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2603
// Rule Name: SERVER-ORACLE dbms_repcat.create_mview_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2603
static inline int check_rule_2603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_mview_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2603
static inline void alert_rule_2603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_mview_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_mview_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2605
// Rule Name: SERVER-ORACLE dbms_repcat.compare_old_values buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2605
static inline int check_rule_2605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.compare_old_values",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2605
static inline void alert_rule_2605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.compare_old_values buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.compare_old_values buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2606
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2606
static inline int check_rule_2606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2606
static inline void alert_rule_2606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2608
// Rule Name: SERVER-ORACLE sysdbms_repcat_rgt.check_ddl_text buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2608
static inline int check_rule_2608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sysdbms_repcat_rgt.check_ddl_text",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2608
static inline void alert_rule_2608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sysdbms_repcat_rgt.check_ddl_text buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sysdbms_repcat_rgt.check_ddl_text buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2609
// Rule Name: SERVER-ORACLE dbms_repcat.cancel_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2609
static inline int check_rule_2609(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.cancel_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2609
static inline void alert_rule_2609(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2609;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.cancel_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.cancel_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2611
// Rule Name: SERVER-ORACLE LINK metadata buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2611
static inline int check_rule_2611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['CREATE",nocase', 'DATABASE",nocase', 'LINK",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2611
static inline void alert_rule_2611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE LINK metadata buffer overflow attempt", 
                     sizeof("SERVER-ORACLE LINK metadata buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2612
// Rule Name: SERVER-ORACLE sys.dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2612
static inline int check_rule_2612(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_auth.revoke_surrogate_repcat",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2612
static inline void alert_rule_2612(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2612;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2614
// Rule Name: SERVER-ORACLE time_zone buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2614
static inline int check_rule_2614(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: TIME_ZONE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2614
static inline void alert_rule_2614(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2614;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE time_zone buffer overflow attempt", 
                     sizeof("SERVER-ORACLE time_zone buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2615
// Rule Name: SERVER-ORACLE sys.dbms_repcat_auth.grant_surrogate_repcat buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2615
static inline int check_rule_2615(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_auth.grant_surrogate_repcat",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2615
static inline void alert_rule_2615(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2615;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_auth.grant_surrogate_repcat buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_auth.grant_surrogate_repcat buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2617
// Rule Name: SERVER-ORACLE sys.dbms_repcat.alter_mview_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2617
static inline int check_rule_2617(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat.alter_mview_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2617
static inline void alert_rule_2617(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2617;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat.alter_mview_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat.alter_mview_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2619
// Rule Name: SERVER-ORACLE dbms_repcat.alter_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2619
static inline int check_rule_2619(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2619
static inline void alert_rule_2619(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2619;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2621
// Rule Name: SERVER-ORACLE dbms_repcat_sna_utl.register_flavor_change buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2621
static inline int check_rule_2621(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_sna_utl.register_flavor_change",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2621
static inline void alert_rule_2621(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2621;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_sna_utl.register_flavor_change buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_sna_utl.register_flavor_change buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2624
// Rule Name: SERVER-ORACLE dbms_repcat_admin.unregister_user_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2624
static inline int check_rule_2624(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_admin.unregister_user_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2624
static inline void alert_rule_2624(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2624;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_admin.unregister_user_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_admin.unregister_user_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2626
// Rule Name: SERVER-ORACLE dbms_repcat.send_old_values buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2626
static inline int check_rule_2626(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.send_old_values",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2626
static inline void alert_rule_2626(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2626;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.send_old_values buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.send_old_values buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2627
// Rule Name: SERVER-ORACLE dbms_repcat.repcat_import_check buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2627
static inline int check_rule_2627(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.repcat_import_check",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2627
static inline void alert_rule_2627(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2627;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.repcat_import_check buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.repcat_import_check buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2629
// Rule Name: SERVER-ORACLE dbms_repcat_admin.register_user_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2629
static inline int check_rule_2629(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_admin.register_user_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2629
static inline void alert_rule_2629(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2629;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_admin.register_user_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_admin.register_user_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2633
// Rule Name: SERVER-ORACLE sys.dbms_rectifier_diff.rectify buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2633
static inline int check_rule_2633(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_rectifier_diff.rectify",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2633
static inline void alert_rule_2633(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2633;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_rectifier_diff.rectify buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_rectifier_diff.rectify buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2637
// Rule Name: SERVER-ORACLE dbms_repcat.drop_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2637
static inline int check_rule_2637(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2637
static inline void alert_rule_2637(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2637;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2639
// Rule Name: SERVER-ORACLE dbms_repcat.drop_mview_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2639
static inline int check_rule_2639(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_mview_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2639
static inline void alert_rule_2639(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2639;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_mview_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_mview_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2641
// Rule Name: SERVER-ORACLE dbms_repcat_instantiate.drop_site_instantiation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2641
static inline int check_rule_2641(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_instantiate.drop_site_instantiation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2641
static inline void alert_rule_2641(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2641;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_instantiate.drop_site_instantiation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_instantiate.drop_site_instantiation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2643
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.ensure_not_published buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2643
static inline int check_rule_2643(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.ensure_not_published",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2643
static inline void alert_rule_2643(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2643;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.ensure_not_published buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.ensure_not_published buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2644
// Rule Name: SERVER-ORACLE from_tz buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2644
static inline int check_rule_2644(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: FROM_TZ",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2644
static inline void alert_rule_2644(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2644;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE from_tz buffer overflow attempt", 
                     sizeof("SERVER-ORACLE from_tz buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2645
// Rule Name: SERVER-ORACLE dbms_repcat_instantiate.instantiate_offline buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2645
static inline int check_rule_2645(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_instantiate.instantiate_offline",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2645
static inline void alert_rule_2645(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2645;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_instantiate.instantiate_offline buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_instantiate.instantiate_offline buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2649
// Rule Name: SERVER-ORACLE Oracle 9i TNS Listener SERVICE_NAME Remote Buffer Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2649
static inline int check_rule_2649(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['connect_data",nocase', '|28|service_name=",nocase', '!"|29|",within 1000']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2649
static inline void alert_rule_2649(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2649;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE Oracle 9i TNS Listener SERVICE_NAME Remote Buffer Overflow attempt", 
                     sizeof("SERVER-ORACLE Oracle 9i TNS Listener SERVICE_NAME Remote Buffer Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2650
// Rule Name: SERVER-ORACLE user name buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2650
static inline int check_rule_2650(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['connect_data",nocase', '|28|user=",nocase', '!"|29|",within 1000']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2650
static inline void alert_rule_2650(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2650;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE user name buffer overflow attempt", 
                     sizeof("SERVER-ORACLE user name buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2651
// Rule Name: SERVER-ORACLE NUMTODSINTERVAL/NUMTOYMINTERVAL buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2651
static inline int check_rule_2651(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['NUMTO",nocase', 'INTERVAL",distance 2,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2651
static inline void alert_rule_2651(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2651;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE NUMTODSINTERVAL/NUMTOYMINTERVAL buffer overflow attempt", 
                     sizeof("SERVER-ORACLE NUMTODSINTERVAL/NUMTOYMINTERVAL buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2652
// Rule Name: SERVER-ORACLE dbms_offline_og.begin_load buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2652
static inline int check_rule_2652(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.begin_load",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2652
static inline void alert_rule_2652(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2652;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.begin_load buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.begin_load buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2654
// Rule Name: SERVER-WEBAPP PHPNuke Forum viewtopic SQL insertion attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2654
static inline int check_rule_2654(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/modules.php",nocase', 'name=Forums', 'file=viewtopic",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2654
static inline void alert_rule_2654(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2654;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHPNuke Forum viewtopic SQL insertion attempt", 
                     sizeof("SERVER-WEBAPP PHPNuke Forum viewtopic SQL insertion attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2655
// Rule Name: SERVER-OTHER HP Web JetAdmin ExecuteFile admin access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2655
static inline int check_rule_2655(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/plugins/framework/script/content.hts",fast_pattern,nocase', 'ExecuteFile",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2655
static inline void alert_rule_2655(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2655;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER HP Web JetAdmin ExecuteFile admin access", 
                     sizeof("SERVER-OTHER HP Web JetAdmin ExecuteFile admin access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2656
// Rule Name: SERVER-WEBAPP SSLv2 Client_Hello Challenge Length overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2656
static inline int check_rule_2656(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: |01 00 02|",depth 3,offset 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2656
static inline void alert_rule_2656(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2656;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SSLv2 Client_Hello Challenge Length overflow attempt", 
                     sizeof("SERVER-WEBAPP SSLv2 Client_Hello Challenge Length overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2657
// Rule Name: SERVER-WEBAPP SSLv2 Client_Hello with pad Challenge Length overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2657
static inline int check_rule_2657(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: |01 00 02|",depth 3,offset 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2657
static inline void alert_rule_2657(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2657;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SSLv2 Client_Hello with pad Challenge Length overflow attempt", 
                     sizeof("SERVER-WEBAPP SSLv2 Client_Hello with pad Challenge Length overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2663
// Rule Name: SERVER-WEBAPP Ipswitch WhatsUpGold instancename overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2663
static inline int check_rule_2663(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/_maincfgret.cgi",fast_pattern,nocase', 'instancename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2663
static inline void alert_rule_2663(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2663;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ipswitch WhatsUpGold instancename overflow attempt", 
                     sizeof("SERVER-WEBAPP Ipswitch WhatsUpGold instancename overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2664
// Rule Name: PROTOCOL-IMAP login format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2664
static inline int check_rule_2664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2664
static inline void alert_rule_2664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP login format string attempt", 
                     sizeof("PROTOCOL-IMAP login format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2665
// Rule Name: PROTOCOL-IMAP login literal format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2665
static inline int check_rule_2665(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2665
static inline void alert_rule_2665(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2665;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP login literal format string attempt", 
                     sizeof("PROTOCOL-IMAP login literal format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2666
// Rule Name: PROTOCOL-POP PASS format string attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2666
static inline int check_rule_2666(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 110
    if (tcph->dest != htons(110))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2666
static inline void alert_rule_2666(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2666;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-POP PASS format string attempt", 
                     sizeof("PROTOCOL-POP PASS format string attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2667
// Rule Name: SERVER-IIS ping.asp access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2667
static inline int check_rule_2667(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ping.asp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2667
static inline void alert_rule_2667(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2667;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS ping.asp access", 
                     sizeof("SERVER-IIS ping.asp access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2668
// Rule Name: SERVER-WEBAPP processit access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2668
static inline int check_rule_2668(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /processit.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2668
static inline void alert_rule_2668(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2668;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP processit access", 
                     sizeof("SERVER-WEBAPP processit access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2669
// Rule Name: SERVER-WEBAPP ibillpm.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2669
static inline int check_rule_2669(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ibillpm.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2669
static inline void alert_rule_2669(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2669;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP ibillpm.pl access", 
                     sizeof("SERVER-WEBAPP ibillpm.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2670
// Rule Name: SERVER-WEBAPP pgpmail.pl access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2670
static inline int check_rule_2670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pgpmail.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2670
static inline void alert_rule_2670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP pgpmail.pl access", 
                     sizeof("SERVER-WEBAPP pgpmail.pl access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2671
// Rule Name: BROWSER-IE Microsoft Internet Explorer bitmap BitmapOffset integer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2671
static inline int check_rule_2671(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: BM
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2671
static inline void alert_rule_2671(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2671;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-IE Microsoft Internet Explorer bitmap BitmapOffset integer overflow attempt", 
                     sizeof("BROWSER-IE Microsoft Internet Explorer bitmap BitmapOffset integer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2672
// Rule Name: SERVER-WEBAPP sresult.exe access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 2672
static inline int check_rule_2672(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /sresult.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2672
static inline void alert_rule_2672(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2672;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP sresult.exe access", 
                     sizeof("SERVER-WEBAPP sresult.exe access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2673
// Rule Name: FILE-IMAGE libpng tRNS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2673
static inline int check_rule_2673(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|89|PNG|0D 0A 1A 0A|', 'IHDR",within 4,distance 4', 'tRNS",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2673
static inline void alert_rule_2673(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2673;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE libpng tRNS overflow attempt", 
                     sizeof("FILE-IMAGE libpng tRNS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2674
// Rule Name: SERVER-ORACLE dbms_repcat.add_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2674
static inline int check_rule_2674(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2674
static inline void alert_rule_2674(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2674;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2675
// Rule Name: SERVER-ORACLE dbms_repcat_rgt.instantiate_offline buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2675
static inline int check_rule_2675(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_rgt.instantiate_offline",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2675
static inline void alert_rule_2675(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2675;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_rgt.instantiate_offline buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_rgt.instantiate_offline buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2677
// Rule Name: SERVER-ORACLE dbms_repcat_rgt.instantiate_online buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2677
static inline int check_rule_2677(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_rgt.instantiate_online",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2677
static inline void alert_rule_2677(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2677;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_rgt.instantiate_online buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_rgt.instantiate_online buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2678
// Rule Name: SERVER-ORACLE ctx_output.start_log buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2678
static inline int check_rule_2678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ctx_output.start_log",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2678
static inline void alert_rule_2678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE ctx_output.start_log buffer overflow attempt", 
                     sizeof("SERVER-ORACLE ctx_output.start_log buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2679
// Rule Name: SERVER-ORACLE sys.dbms_system.ksdwrt buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2679
static inline int check_rule_2679(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_system.ksdwrt",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2679
static inline void alert_rule_2679(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2679;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_system.ksdwrt buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_system.ksdwrt buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2680
// Rule Name: SERVER-ORACLE ctxsys.driddlr.subindexpopulate buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2680
static inline int check_rule_2680(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ctxsys.driddlr.subindexpopulate",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2680
static inline void alert_rule_2680(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2680;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE ctxsys.driddlr.subindexpopulate buffer overflow attempt", 
                     sizeof("SERVER-ORACLE ctxsys.driddlr.subindexpopulate buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2681
// Rule Name: SERVER-ORACLE mdsys.sdo_admin.sdo_code_size buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2681
static inline int check_rule_2681(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: mdsys.sdo_admin.sdo_code_size",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2681
static inline void alert_rule_2681(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2681;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE mdsys.sdo_admin.sdo_code_size buffer overflow attempt", 
                     sizeof("SERVER-ORACLE mdsys.sdo_admin.sdo_code_size buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2682
// Rule Name: SERVER-ORACLE mdsys.md2.validate_geom buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2682
static inline int check_rule_2682(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: mdsys.md2.validate_geom",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2682
static inline void alert_rule_2682(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2682;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE mdsys.md2.validate_geom buffer overflow attempt", 
                     sizeof("SERVER-ORACLE mdsys.md2.validate_geom buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2683
// Rule Name: SERVER-ORACLE mdsys.md2.sdo_code_size buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2683
static inline int check_rule_2683(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: mdsys.md2.sdo_code_size",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2683
static inline void alert_rule_2683(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2683;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE mdsys.md2.sdo_code_size buffer overflow attempt", 
                     sizeof("SERVER-ORACLE mdsys.md2.sdo_code_size buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2684
// Rule Name: SERVER-ORACLE sys.ltutil.pushdeferredtxns buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2684
static inline int check_rule_2684(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.ltutil.pushdeferredtxns",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2684
static inline void alert_rule_2684(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2684;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.ltutil.pushdeferredtxns buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.ltutil.pushdeferredtxns buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2685
// Rule Name: SERVER-ORACLE sys.dbms_repcat_rq.add_column buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2685
static inline int check_rule_2685(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_rq.add_column",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2685
static inline void alert_rule_2685(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2685;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_rq.add_column buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_rq.add_column buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2686
// Rule Name: SERVER-ORACLE sys.dbms_rectifier_diff.differences buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2686
static inline int check_rule_2686(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_rectifier_diff.differences",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2686
static inline void alert_rule_2686(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2686;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_rectifier_diff.differences buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_rectifier_diff.differences buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2687
// Rule Name: SERVER-ORACLE sys.dbms_internal_repcat.validate buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2687
static inline int check_rule_2687(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_internal_repcat.validate",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2687
static inline void alert_rule_2687(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2687;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_internal_repcat.validate buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_internal_repcat.validate buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2688
// Rule Name: SERVER-ORACLE sys.dbms_internal_repcat.enable_receiver_trace buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2688
static inline int check_rule_2688(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_internal_repcat.enable_receiver_trace",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2688
static inline void alert_rule_2688(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2688;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_internal_repcat.enable_receiver_trace buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_internal_repcat.enable_receiver_trace buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2689
// Rule Name: SERVER-ORACLE sys.dbms_internal_repcat.disable_receiver_trace buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2689
static inline int check_rule_2689(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_internal_repcat.disable_receiver_trace",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2689
static inline void alert_rule_2689(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2689;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_internal_repcat.disable_receiver_trace buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_internal_repcat.disable_receiver_trace buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2690
// Rule Name: SERVER-ORACLE sys.dbms_defer_repcat.enable_propagation_to_dblink buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2690
static inline int check_rule_2690(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_defer_repcat.enable_propagation_to_dblink",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2690
static inline void alert_rule_2690(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2690;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_defer_repcat.enable_propagation_to_dblink buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_defer_repcat.enable_propagation_to_dblink buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2691
// Rule Name: SERVER-ORACLE sys.dbms_defer_internal_sys.parallel_push_recovery buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2691
static inline int check_rule_2691(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_defer_internal_sys.parallel_push_recovery",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2691
static inline void alert_rule_2691(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2691;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_defer_internal_sys.parallel_push_recovery buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_defer_internal_sys.parallel_push_recovery buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2692
// Rule Name: SERVER-ORACLE sys.dbms_aqadm_sys.verify_queue_types buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2692
static inline int check_rule_2692(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_aqadm_sys.verify_queue_types",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2692
static inline void alert_rule_2692(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2692;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_aqadm_sys.verify_queue_types buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_aqadm_sys.verify_queue_types buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2693
// Rule Name: SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_no_queue buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2693
static inline int check_rule_2693(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_aqadm.verify_queue_types_no_queue",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2693
static inline void alert_rule_2693(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2693;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_no_queue buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_no_queue buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2694
// Rule Name: SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_get_nrp buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2694
static inline int check_rule_2694(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_aqadm.verify_queue_types_get_nrp",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2694
static inline void alert_rule_2694(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2694;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_get_nrp buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_aqadm.verify_queue_types_get_nrp buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2695
// Rule Name: SERVER-ORACLE sys.dbms_aq_import_internal.aq_table_defn_update buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2695
static inline int check_rule_2695(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_aq_import_internal.aq_table_defn_update",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2695
static inline void alert_rule_2695(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2695;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_aq_import_internal.aq_table_defn_update buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_aq_import_internal.aq_table_defn_update buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2696
// Rule Name: SERVER-ORACLE sys.dbms_repcat_utl.is_master buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2696
static inline int check_rule_2696(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_utl.is_master",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2696
static inline void alert_rule_2696(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2696;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_utl.is_master buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_utl.is_master buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2697
// Rule Name: SERVER-ORACLE alter file buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2697
static inline int check_rule_2697(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: alter",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2697
static inline void alert_rule_2697(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2697;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE alter file buffer overflow attempt", 
                     sizeof("SERVER-ORACLE alter file buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2698
// Rule Name: SERVER-ORACLE create file buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2698
static inline int check_rule_2698(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: create",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2698
static inline void alert_rule_2698(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2698;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE create file buffer overflow attempt", 
                     sizeof("SERVER-ORACLE create file buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2699
// Rule Name: SERVER-ORACLE TO_CHAR buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2699
static inline int check_rule_2699(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: TO_CHAR",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2699
static inline void alert_rule_2699(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2699;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE TO_CHAR buffer overflow attempt", 
                     sizeof("SERVER-ORACLE TO_CHAR buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2701
// Rule Name: SERVER-WEBAPP Oracle iSQLPlus sid overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2701
static inline int check_rule_2701(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /isqlplus",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2701
static inline void alert_rule_2701(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2701;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle iSQLPlus sid overflow attempt", 
                     sizeof("SERVER-WEBAPP Oracle iSQLPlus sid overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2702
// Rule Name: SERVER-WEBAPP Oracle iSQLPlus username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2702
static inline int check_rule_2702(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /isqlplus",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2702
static inline void alert_rule_2702(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2702;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle iSQLPlus username overflow attempt", 
                     sizeof("SERVER-WEBAPP Oracle iSQLPlus username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2703
// Rule Name: SERVER-WEBAPP Oracle iSQLPlus login.uix username overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2703
static inline int check_rule_2703(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /login.uix",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2703
static inline void alert_rule_2703(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2703;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle iSQLPlus login.uix username overflow attempt", 
                     sizeof("SERVER-WEBAPP Oracle iSQLPlus login.uix username overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2704
// Rule Name: SERVER-WEBAPP Oracle 10g iSQLPlus login.unix connectID overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2704
static inline int check_rule_2704(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/login.uix",nocase', 'connectID=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2704
static inline void alert_rule_2704(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2704;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle 10g iSQLPlus login.unix connectID overflow attempt", 
                     sizeof("SERVER-WEBAPP Oracle 10g iSQLPlus login.unix connectID overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2705
// Rule Name: FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2705
static inline int check_rule_2705(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type",nocase', 'image/",nocase', '|FF D8|",within 2,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2705
static inline void alert_rule_2705(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2705;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt", 
                     sizeof("FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2707
// Rule Name: FILE-IMAGE JPEG parser multipacket heap overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2707
static inline int check_rule_2707(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |00 48 00 00 FF|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2707
static inline void alert_rule_2707(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2707;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE JPEG parser multipacket heap overflow attempt", 
                     sizeof("FILE-IMAGE JPEG parser multipacket heap overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2708
// Rule Name: SERVER-ORACLE dbms_offline_og.begin_flavor_change buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2708
static inline int check_rule_2708(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.begin_flavor_change",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2708
static inline void alert_rule_2708(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2708;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.begin_flavor_change buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.begin_flavor_change buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2709
// Rule Name: SERVER-ORACLE dbms_offline_og.begin_instantiation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2709
static inline int check_rule_2709(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.begin_instantiation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2709
static inline void alert_rule_2709(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2709;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.begin_instantiation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.begin_instantiation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2711
// Rule Name: SERVER-ORACLE dbms_offline_og.end_flavor_change buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2711
static inline int check_rule_2711(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.end_flavor_change",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2711
static inline void alert_rule_2711(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2711;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.end_flavor_change buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.end_flavor_change buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2712
// Rule Name: SERVER-ORACLE dbms_offline_og.end_instantiation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2712
static inline int check_rule_2712(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.end_instantiation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2712
static inline void alert_rule_2712(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2712;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.end_instantiation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.end_instantiation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2713
// Rule Name: SERVER-ORACLE dbms_offline_og.end_load buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2713
static inline int check_rule_2713(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.end_load",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2713
static inline void alert_rule_2713(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2713;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.end_load buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.end_load buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2714
// Rule Name: SERVER-ORACLE dbms_offline_og.resume_subset_of_masters buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2714
static inline int check_rule_2714(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_og.resume_subset_of_masters",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2714
static inline void alert_rule_2714(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2714;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_og.resume_subset_of_masters buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_og.resume_subset_of_masters buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2715
// Rule Name: SERVER-ORACLE dbms_offline_snapshot.begin_load buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2715
static inline int check_rule_2715(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_snapshot.begin_load",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2715
static inline void alert_rule_2715(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2715;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_snapshot.begin_load buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_snapshot.begin_load buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2716
// Rule Name: SERVER-ORACLE dbms_offline_snapshot.end_load buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2716
static inline int check_rule_2716(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_offline_snapshot.end_load",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2716
static inline void alert_rule_2716(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2716;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_offline_snapshot.end_load buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_offline_snapshot.end_load buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2717
// Rule Name: SERVER-ORACLE dbms_rectifier_diff.differences buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2717
static inline int check_rule_2717(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_rectifier_diff.differences",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2717
static inline void alert_rule_2717(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2717;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_rectifier_diff.differences buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_rectifier_diff.differences buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2718
// Rule Name: SERVER-ORACLE dbms_rectifier_diff.rectify buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2718
static inline int check_rule_2718(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_rectifier_diff.rectify",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2718
static inline void alert_rule_2718(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2718;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_rectifier_diff.rectify buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_rectifier_diff.rectify buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2719
// Rule Name: SERVER-ORACLE dbms_repcat.abort_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2719
static inline int check_rule_2719(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.abort_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2719
static inline void alert_rule_2719(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2719;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.abort_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.abort_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2720
// Rule Name: SERVER-ORACLE dbms_repcat.add_column_group_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2720
static inline int check_rule_2720(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_column_group_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2720
static inline void alert_rule_2720(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2720;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_column_group_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_column_group_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2721
// Rule Name: SERVER-ORACLE dbms_repcat.add_columns_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2721
static inline int check_rule_2721(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_columns_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2721
static inline void alert_rule_2721(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2721;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_columns_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_columns_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2722
// Rule Name: SERVER-ORACLE dbms_repcat.add_object_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2722
static inline int check_rule_2722(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_object_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2722
static inline void alert_rule_2722(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2722;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_object_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_object_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2723
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2723
static inline int check_rule_2723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2723
static inline void alert_rule_2723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2724
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2724
static inline int check_rule_2724(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2724
static inline void alert_rule_2724(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2724;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2725
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2725
static inline int check_rule_2725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2725
static inline void alert_rule_2725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2726
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2726
static inline int check_rule_2726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2726
static inline void alert_rule_2726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2727
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2727
static inline int check_rule_2727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2727
static inline void alert_rule_2727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2728
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2728
static inline int check_rule_2728(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2728
static inline void alert_rule_2728(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2728;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2729
// Rule Name: SERVER-ORACLE dbms_repcat.add_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2729
static inline int check_rule_2729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2729
static inline void alert_rule_2729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2730
// Rule Name: SERVER-ORACLE dbms_repcat.add_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2730
static inline int check_rule_2730(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2730
static inline void alert_rule_2730(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2730;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2731
// Rule Name: SERVER-ORACLE dbms_repcat.add_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2731
static inline int check_rule_2731(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2731
static inline void alert_rule_2731(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2731;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2732
// Rule Name: SERVER-ORACLE dbms_repcat.add_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2732
static inline int check_rule_2732(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.add_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2732
static inline void alert_rule_2732(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2732;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.add_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.add_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2733
// Rule Name: SERVER-ORACLE dbms_repcat.alter_master_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2733
static inline int check_rule_2733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_master_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2733
static inline void alert_rule_2733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_master_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_master_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2734
// Rule Name: SERVER-ORACLE dbms_repcat.alter_mview_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2734
static inline int check_rule_2734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_mview_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2734
static inline void alert_rule_2734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_mview_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_mview_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2735
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2735
static inline int check_rule_2735(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2735
static inline void alert_rule_2735(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2735;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2736
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2736
static inline int check_rule_2736(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2736
static inline void alert_rule_2736(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2736;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2737
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2737
static inline int check_rule_2737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2737
static inline void alert_rule_2737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2738
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2738
static inline int check_rule_2738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2738
static inline void alert_rule_2738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2739
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2739
static inline int check_rule_2739(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2739
static inline void alert_rule_2739(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2739;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2740
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2740
static inline int check_rule_2740(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2740
static inline void alert_rule_2740(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2740;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2741
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2741
static inline int check_rule_2741(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2741
static inline void alert_rule_2741(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2741;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2742
// Rule Name: SERVER-ORACLE dbms_repcat.alter_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2742
static inline int check_rule_2742(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2742
static inline void alert_rule_2742(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2742;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2743
// Rule Name: SERVER-ORACLE dbms_repcat.alter_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2743
static inline int check_rule_2743(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2743
static inline void alert_rule_2743(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2743;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2744
// Rule Name: SERVER-ORACLE dbms_repcat.alter_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2744
static inline int check_rule_2744(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2744
static inline void alert_rule_2744(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2744;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2745
// Rule Name: SERVER-ORACLE dbms_repcat.alter_snapshot_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2745
static inline int check_rule_2745(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.alter_snapshot_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2745
static inline void alert_rule_2745(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2745;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.alter_snapshot_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.alter_snapshot_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2746
// Rule Name: SERVER-ORACLE dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2746
static inline int check_rule_2746(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_auth.revoke_surrogate_repcat",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2746
static inline void alert_rule_2746(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2746;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_auth.revoke_surrogate_repcat buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2747
// Rule Name: SERVER-ORACLE dbms_repcat.begin_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2747
static inline int check_rule_2747(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.begin_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2747
static inline void alert_rule_2747(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2747;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.begin_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.begin_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2748
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2748
static inline int check_rule_2748(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2748
static inline void alert_rule_2748(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2748;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2749
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2749
static inline int check_rule_2749(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2749
static inline void alert_rule_2749(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2749;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2750
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_mview_repsites buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2750
static inline int check_rule_2750(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_mview_repsites",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2750
static inline void alert_rule_2750(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2750;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_mview_repsites buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_mview_repsites buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2751
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_priority_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2751
static inline int check_rule_2751(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_priority_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2751
static inline void alert_rule_2751(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2751;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_priority_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_priority_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2752
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2752
static inline int check_rule_2752(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2752
static inline void alert_rule_2752(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2752;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2753
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_repsites buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2753
static inline int check_rule_2753(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_repsites",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2753
static inline void alert_rule_2753(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2753;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_repsites buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_repsites buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2754
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2754
static inline int check_rule_2754(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2754
static inline void alert_rule_2754(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2754;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2755
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2755
static inline int check_rule_2755(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2755
static inline void alert_rule_2755(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2755;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2756
// Rule Name: SERVER-ORACLE dbms_repcat.comment_on_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2756
static inline int check_rule_2756(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.comment_on_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2756
static inline void alert_rule_2756(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2756;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.comment_on_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.comment_on_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2757
// Rule Name: SERVER-ORACLE dbms_repcat.create_master_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2757
static inline int check_rule_2757(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_master_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2757
static inline void alert_rule_2757(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2757;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_master_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_master_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2758
// Rule Name: SERVER-ORACLE dbms_repcat.create_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2758
static inline int check_rule_2758(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2758
static inline void alert_rule_2758(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2758;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2759
// Rule Name: SERVER-ORACLE dbms_repcat.create_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2759
static inline int check_rule_2759(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2759
static inline void alert_rule_2759(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2759;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2760
// Rule Name: SERVER-ORACLE dbms_repcat.define_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2760
static inline int check_rule_2760(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.define_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2760
static inline void alert_rule_2760(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2760;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.define_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.define_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2761
// Rule Name: SERVER-ORACLE dbms_repcat.define_priority_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2761
static inline int check_rule_2761(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.define_priority_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2761
static inline void alert_rule_2761(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2761;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.define_priority_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.define_priority_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2762
// Rule Name: SERVER-ORACLE dbms_repcat.define_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2762
static inline int check_rule_2762(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.define_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2762
static inline void alert_rule_2762(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2762;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.define_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.define_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2763
// Rule Name: SERVER-ORACLE dbms_repcat.do_deferred_repcat_admin buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2763
static inline int check_rule_2763(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.do_deferred_repcat_admin",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2763
static inline void alert_rule_2763(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2763;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.do_deferred_repcat_admin buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.do_deferred_repcat_admin buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2764
// Rule Name: SERVER-ORACLE dbms_repcat.drop_column_group_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2764
static inline int check_rule_2764(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_column_group_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2764
static inline void alert_rule_2764(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2764;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_column_group_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_column_group_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2765
// Rule Name: SERVER-ORACLE dbms_repcat.drop_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2765
static inline int check_rule_2765(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2765
static inline void alert_rule_2765(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2765;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2766
// Rule Name: SERVER-ORACLE dbms_repcat.drop_columns_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2766
static inline int check_rule_2766(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_columns_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2766
static inline void alert_rule_2766(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2766;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_columns_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_columns_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2767
// Rule Name: SERVER-ORACLE dbms_repcat.drop_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2767
static inline int check_rule_2767(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2767
static inline void alert_rule_2767(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2767;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2768
// Rule Name: SERVER-ORACLE dbms_repcat.drop_grouped_column buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2768
static inline int check_rule_2768(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_grouped_column",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2768
static inline void alert_rule_2768(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2768;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_grouped_column buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_grouped_column buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2769
// Rule Name: SERVER-ORACLE dbms_repcat.drop_mview_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2769
static inline int check_rule_2769(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_mview_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2769
static inline void alert_rule_2769(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2769;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_mview_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_mview_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2770
// Rule Name: SERVER-ORACLE dbms_repcat.drop_object_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2770
static inline int check_rule_2770(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_object_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2770
static inline void alert_rule_2770(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2770;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_object_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_object_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2771
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2771
static inline int check_rule_2771(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2771
static inline void alert_rule_2771(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2771;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2772
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2772
static inline int check_rule_2772(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2772
static inline void alert_rule_2772(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2772;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2773
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2773
static inline int check_rule_2773(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2773
static inline void alert_rule_2773(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2773;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2774
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2774
static inline int check_rule_2774(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2774
static inline void alert_rule_2774(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2774;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2775
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2775
static inline int check_rule_2775(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2775
static inline void alert_rule_2775(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2775;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2776
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2776
static inline int check_rule_2776(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2776
static inline void alert_rule_2776(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2776;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2777
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2777
static inline int check_rule_2777(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2777
static inline void alert_rule_2777(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2777;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2778
// Rule Name: SERVER-ORACLE dbms_repcat.drop_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2778
static inline int check_rule_2778(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2778
static inline void alert_rule_2778(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2778;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2779
// Rule Name: SERVER-ORACLE dbms_repcat.drop_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2779
static inline int check_rule_2779(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2779
static inline void alert_rule_2779(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2779;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2780
// Rule Name: SERVER-ORACLE dbms_repcat.drop_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2780
static inline int check_rule_2780(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2780
static inline void alert_rule_2780(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2780;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2781
// Rule Name: SERVER-ORACLE dbms_repcat.drop_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2781
static inline int check_rule_2781(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2781
static inline void alert_rule_2781(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2781;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2782
// Rule Name: SERVER-ORACLE dbms_repcat.drop_snapshot_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2782
static inline int check_rule_2782(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_snapshot_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2782
static inline void alert_rule_2782(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2782;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_snapshot_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_snapshot_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2783
// Rule Name: SERVER-ORACLE dbms_repcat.drop_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2783
static inline int check_rule_2783(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2783
static inline void alert_rule_2783(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2783;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2784
// Rule Name: SERVER-ORACLE dbms_repcat.drop_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2784
static inline int check_rule_2784(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.drop_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2784
static inline void alert_rule_2784(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2784;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.drop_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.drop_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2785
// Rule Name: SERVER-ORACLE dbms_repcat.execute_ddl buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2785
static inline int check_rule_2785(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.execute_ddl",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2785
static inline void alert_rule_2785(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2785;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.execute_ddl buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.execute_ddl buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2786
// Rule Name: SERVER-ORACLE dbms_repcat.generate_replication_package buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2786
static inline int check_rule_2786(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.generate_replication_package",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2786
static inline void alert_rule_2786(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2786;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.generate_replication_package buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.generate_replication_package buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2787
// Rule Name: SERVER-ORACLE dbms_repcat_instantiate.instantiate_online buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2787
static inline int check_rule_2787(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_instantiate.instantiate_online",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2787
static inline void alert_rule_2787(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2787;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_instantiate.instantiate_online buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_instantiate.instantiate_online buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2788
// Rule Name: SERVER-ORACLE dbms_repcat.make_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2788
static inline int check_rule_2788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.make_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2788
static inline void alert_rule_2788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.make_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.make_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2789
// Rule Name: SERVER-ORACLE dbms_repcat.obsolete_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2789
static inline int check_rule_2789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.obsolete_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2789
static inline void alert_rule_2789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.obsolete_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.obsolete_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2790
// Rule Name: SERVER-ORACLE dbms_repcat.publish_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2790
static inline int check_rule_2790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.publish_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2790
static inline void alert_rule_2790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.publish_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.publish_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2791
// Rule Name: SERVER-ORACLE dbms_repcat.purge_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2791
static inline int check_rule_2791(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.purge_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2791
static inline void alert_rule_2791(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2791;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.purge_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.purge_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2792
// Rule Name: SERVER-ORACLE dbms_repcat.purge_master_log buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2792
static inline int check_rule_2792(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.purge_master_log",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2792
static inline void alert_rule_2792(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2792;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.purge_master_log buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.purge_master_log buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2793
// Rule Name: SERVER-ORACLE dbms_repcat.purge_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2793
static inline int check_rule_2793(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.purge_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2793
static inline void alert_rule_2793(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2793;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.purge_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.purge_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2794
// Rule Name: SERVER-ORACLE dbms_repcat.refresh_mview_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2794
static inline int check_rule_2794(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.refresh_mview_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2794
static inline void alert_rule_2794(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2794;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.refresh_mview_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.refresh_mview_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2795
// Rule Name: SERVER-ORACLE dbms_repcat.refresh_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2795
static inline int check_rule_2795(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.refresh_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2795
static inline void alert_rule_2795(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2795;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.refresh_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.refresh_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2796
// Rule Name: SERVER-ORACLE dbms_repcat.register_mview_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2796
static inline int check_rule_2796(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.register_mview_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2796
static inline void alert_rule_2796(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2796;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.register_mview_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.register_mview_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2797
// Rule Name: SERVER-ORACLE dbms_repcat.register_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2797
static inline int check_rule_2797(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.register_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2797
static inline void alert_rule_2797(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2797;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.register_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.register_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2798
// Rule Name: SERVER-ORACLE dbms_repcat.register_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2798
static inline int check_rule_2798(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.register_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2798
static inline void alert_rule_2798(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2798;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.register_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.register_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2799
// Rule Name: SERVER-ORACLE dbms_repcat.relocate_masterdef buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2799
static inline int check_rule_2799(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.relocate_masterdef",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2799
static inline void alert_rule_2799(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2799;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.relocate_masterdef buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.relocate_masterdef buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2800
// Rule Name: SERVER-ORACLE dbms_repcat.rename_shadow_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2800
static inline int check_rule_2800(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.rename_shadow_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2800
static inline void alert_rule_2800(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2800;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.rename_shadow_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.rename_shadow_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2801
// Rule Name: SERVER-ORACLE dbms_repcat.resume_master_activity buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2801
static inline int check_rule_2801(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.resume_master_activity",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2801
static inline void alert_rule_2801(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2801;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.resume_master_activity buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.resume_master_activity buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2802
// Rule Name: SERVER-ORACLE dbms_repcat_rgt.check_ddl_text buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2802
static inline int check_rule_2802(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_rgt.check_ddl_text",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2802
static inline void alert_rule_2802(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2802;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_rgt.check_ddl_text buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_rgt.check_ddl_text buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2803
// Rule Name: SERVER-ORACLE dbms_repcat_rgt.drop_site_instantiation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2803
static inline int check_rule_2803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat_rgt.drop_site_instantiation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2803
static inline void alert_rule_2803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat_rgt.drop_site_instantiation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat_rgt.drop_site_instantiation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2804
// Rule Name: SERVER-ORACLE dbms_repcat.send_and_compare_old_values buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2804
static inline int check_rule_2804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.send_and_compare_old_values",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2804
static inline void alert_rule_2804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.send_and_compare_old_values buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.send_and_compare_old_values buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2805
// Rule Name: SERVER-ORACLE dbms_repcat.set_columns buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2805
static inline int check_rule_2805(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.set_columns",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2805
static inline void alert_rule_2805(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2805;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.set_columns buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.set_columns buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2806
// Rule Name: SERVER-ORACLE dbms_repcat.set_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2806
static inline int check_rule_2806(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.set_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2806
static inline void alert_rule_2806(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2806;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.set_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.set_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2807
// Rule Name: SERVER-ORACLE dbms_repcat.specify_new_masters buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2807
static inline int check_rule_2807(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.specify_new_masters",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2807
static inline void alert_rule_2807(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2807;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.specify_new_masters buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.specify_new_masters buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2808
// Rule Name: SERVER-ORACLE dbms_repcat.suspend_master_activity buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2808
static inline int check_rule_2808(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.suspend_master_activity",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2808
static inline void alert_rule_2808(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2808;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.suspend_master_activity buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.suspend_master_activity buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2809
// Rule Name: SERVER-ORACLE dbms_repcat.unregister_mview_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2809
static inline int check_rule_2809(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.unregister_mview_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2809
static inline void alert_rule_2809(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2809;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.unregister_mview_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.unregister_mview_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2810
// Rule Name: SERVER-ORACLE dbms_repcat.unregister_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2810
static inline int check_rule_2810(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.unregister_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2810
static inline void alert_rule_2810(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2810;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.unregister_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.unregister_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2811
// Rule Name: SERVER-ORACLE dbms_repcat.validate_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2811
static inline int check_rule_2811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.validate_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2811
static inline void alert_rule_2811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.validate_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.validate_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2812
// Rule Name: SERVER-ORACLE dbms_repcat.validate_for_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2812
static inline int check_rule_2812(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.validate_for_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2812
static inline void alert_rule_2812(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2812;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.validate_for_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.validate_for_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2813
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.abort_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2813
static inline int check_rule_2813(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.abort_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2813
static inline void alert_rule_2813(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2813;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.abort_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.abort_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2814
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.add_object_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2814
static inline int check_rule_2814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.add_object_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2814
static inline void alert_rule_2814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.add_object_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.add_object_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2815
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.begin_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2815
static inline int check_rule_2815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.begin_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2815
static inline void alert_rule_2815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.begin_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.begin_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2816
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.drop_object_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2816
static inline int check_rule_2816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.drop_object_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2816
static inline void alert_rule_2816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.drop_object_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.drop_object_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2817
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.add_column_group_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2817
static inline int check_rule_2817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.add_column_group_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2817
static inline void alert_rule_2817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.add_column_group_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.add_column_group_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2818
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.add_columns_to_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2818
static inline int check_rule_2818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.add_columns_to_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2818
static inline void alert_rule_2818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.add_columns_to_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.add_columns_to_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2819
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_column_group_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2819
static inline int check_rule_2819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.drop_column_group_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2819
static inline void alert_rule_2819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_column_group_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_column_group_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2820
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_columns_from_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2820
static inline int check_rule_2820(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.drop_columns_from_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2820
static inline void alert_rule_2820(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2820;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_columns_from_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.drop_columns_from_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2821
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.obsolete_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2821
static inline int check_rule_2821(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.obsolete_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2821
static inline void alert_rule_2821(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2821;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.obsolete_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.obsolete_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2822
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.publish_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2822
static inline int check_rule_2822(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.publish_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2822
static inline void alert_rule_2822(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2822;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.publish_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.publish_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2823
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla_mas.purge_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2823
static inline int check_rule_2823(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla_mas.purge_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2823
static inline void alert_rule_2823(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2823;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla_mas.purge_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla_mas.purge_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2824
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.set_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2824
static inline int check_rule_2824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.set_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2824
static inline void alert_rule_2824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.set_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.set_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2825
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.validate_flavor_definition buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2825
static inline int check_rule_2825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.validate_flavor_definition",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2825
static inline void alert_rule_2825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.validate_flavor_definition buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.validate_flavor_definition buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2826
// Rule Name: SERVER-ORACLE sys.dbms_repcat_fla.validate_for_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2826
static inline int check_rule_2826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_fla.validate_for_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2826
static inline void alert_rule_2826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_fla.validate_for_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_fla.validate_for_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2827
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.alter_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2827
static inline int check_rule_2827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.alter_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2827
static inline void alert_rule_2827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.alter_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.alter_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2828
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2828
static inline int check_rule_2828(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.comment_on_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2828
static inline void alert_rule_2828(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2828;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2829
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2829
static inline int check_rule_2829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.comment_on_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2829
static inline void alert_rule_2829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.comment_on_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2830
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.create_master_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2830
static inline int check_rule_2830(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.create_master_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2830
static inline void alert_rule_2830(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2830;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.create_master_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.create_master_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2831
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.create_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2831
static inline int check_rule_2831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.create_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2831
static inline void alert_rule_2831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.create_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.create_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2832
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.do_deferred_repcat_admin buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2832
static inline int check_rule_2832(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.do_deferred_repcat_admin",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2832
static inline void alert_rule_2832(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2832;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.do_deferred_repcat_admin buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.do_deferred_repcat_admin buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2833
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.drop_master_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2833
static inline int check_rule_2833(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.drop_master_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2833
static inline void alert_rule_2833(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2833;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.drop_master_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.drop_master_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2834
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.generate_replication_package buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2834
static inline int check_rule_2834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.generate_replication_package",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2834
static inline void alert_rule_2834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.generate_replication_package buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.generate_replication_package buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2835
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.purge_master_log buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2835
static inline int check_rule_2835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.purge_master_log",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2835
static inline void alert_rule_2835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.purge_master_log buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.purge_master_log buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2836
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.relocate_masterdef buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2836
static inline int check_rule_2836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.relocate_masterdef",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2836
static inline void alert_rule_2836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.relocate_masterdef buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.relocate_masterdef buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2837
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.rename_shadow_column_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2837
static inline int check_rule_2837(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.rename_shadow_column_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2837
static inline void alert_rule_2837(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2837;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.rename_shadow_column_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.rename_shadow_column_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2838
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.resume_master_activity buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2838
static inline int check_rule_2838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.resume_master_activity",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2838
static inline void alert_rule_2838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.resume_master_activity buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.resume_master_activity buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2839
// Rule Name: SERVER-ORACLE sys.dbms_repcat_mas.suspend_master_activity buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2839
static inline int check_rule_2839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_mas.suspend_master_activity",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2839
static inline void alert_rule_2839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_mas.suspend_master_activity buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_mas.suspend_master_activity buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2840
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.alter_snapshot_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2840
static inline int check_rule_2840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.alter_snapshot_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2840
static inline void alert_rule_2840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.alter_snapshot_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.alter_snapshot_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2841
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.create_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2841
static inline int check_rule_2841(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.create_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2841
static inline void alert_rule_2841(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2841;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.create_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.create_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2842
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2842
static inline int check_rule_2842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.drop_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2842
static inline void alert_rule_2842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2843
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2843
static inline int check_rule_2843(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.drop_snapshot_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2843
static inline void alert_rule_2843(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2843;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.drop_snapshot_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2844
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.refresh_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2844
static inline int check_rule_2844(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.refresh_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2844
static inline void alert_rule_2844(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2844;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.refresh_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.refresh_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2845
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.register_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2845
static inline int check_rule_2845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.register_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2845
static inline void alert_rule_2845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.register_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.register_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2846
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.repcat_import_check buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2846
static inline int check_rule_2846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.repcat_import_check",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2846
static inline void alert_rule_2846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.repcat_import_check buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.repcat_import_check buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2847
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.unregister_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2847
static inline int check_rule_2847(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.unregister_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2847
static inline void alert_rule_2847(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2847;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.unregister_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.unregister_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2848
// Rule Name: SERVER-ORACLE sys.dbms_repcat_utl4.drop_master_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2848
static inline int check_rule_2848(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_utl4.drop_master_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2848
static inline void alert_rule_2848(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2848;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_utl4.drop_master_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_utl4.drop_master_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2849
// Rule Name: SERVER-ORACLE sys.dbms_repcat_utl.drop_an_object buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2849
static inline int check_rule_2849(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_utl.drop_an_object",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2849
static inline void alert_rule_2849(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2849;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_utl.drop_an_object buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_utl.drop_an_object buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2850
// Rule Name: SERVER-ORACLE dbms_repcat.create_mview_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2850
static inline int check_rule_2850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_mview_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2850
static inline void alert_rule_2850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_mview_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_mview_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2851
// Rule Name: SERVER-ORACLE dbms_repcat.create_snapshot_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2851
static inline int check_rule_2851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.create_snapshot_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2851
static inline void alert_rule_2851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.create_snapshot_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.create_snapshot_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2852
// Rule Name: SERVER-ORACLE dbms_repcat.generate_mview_support buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2852
static inline int check_rule_2852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.generate_mview_support",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2852
static inline void alert_rule_2852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.generate_mview_support buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.generate_mview_support buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2853
// Rule Name: SERVER-ORACLE dbms_repcat.generate_replication_trigger buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2853
static inline int check_rule_2853(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.generate_replication_trigger",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2853
static inline void alert_rule_2853(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2853;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.generate_replication_trigger buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.generate_replication_trigger buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2854
// Rule Name: SERVER-ORACLE dbms_repcat.generate_snapshot_support buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2854
static inline int check_rule_2854(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.generate_snapshot_support",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2854
static inline void alert_rule_2854(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2854;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.generate_snapshot_support buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.generate_snapshot_support buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2855
// Rule Name: SERVER-ORACLE dbms_repcat.remove_master_databases buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2855
static inline int check_rule_2855(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.remove_master_databases",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2855
static inline void alert_rule_2855(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2855;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.remove_master_databases buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.remove_master_databases buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2856
// Rule Name: SERVER-ORACLE dbms_repcat.switch_mview_master buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2856
static inline int check_rule_2856(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.switch_mview_master",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2856
static inline void alert_rule_2856(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2856;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.switch_mview_master buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.switch_mview_master buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2857
// Rule Name: SERVER-ORACLE dbms_repcat.switch_snapshot_master buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2857
static inline int check_rule_2857(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dbms_repcat.switch_snapshot_master",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2857
static inline void alert_rule_2857(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2857;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE dbms_repcat.switch_snapshot_master buffer overflow attempt", 
                     sizeof("SERVER-ORACLE dbms_repcat.switch_snapshot_master buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2858
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2858
static inline int check_rule_2858(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2858
static inline void alert_rule_2858(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2858;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2859
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2859
static inline int check_rule_2859(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2859
static inline void alert_rule_2859(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2859;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2860
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2860
static inline int check_rule_2860(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2860
static inline void alert_rule_2860(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2860;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2861
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2861
static inline int check_rule_2861(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2861
static inline void alert_rule_2861(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2861;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2862
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2862
static inline int check_rule_2862(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2862
static inline void alert_rule_2862(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2862;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2863
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2863
static inline int check_rule_2863(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2863
static inline void alert_rule_2863(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2863;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2864
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2864
static inline int check_rule_2864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2864
static inline void alert_rule_2864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2865
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2865
static inline int check_rule_2865(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2865
static inline void alert_rule_2865(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2865;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2866
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2866
static inline int check_rule_2866(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2866
static inline void alert_rule_2866(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2866;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2867
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2867
static inline int check_rule_2867(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2867
static inline void alert_rule_2867(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2867;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2868
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.add_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2868
static inline int check_rule_2868(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.add_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2868
static inline void alert_rule_2868(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2868;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.add_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.add_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2869
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2869
static inline int check_rule_2869(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2869
static inline void alert_rule_2869(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2869;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2870
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2870
static inline int check_rule_2870(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2870
static inline void alert_rule_2870(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2870;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2871
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2871
static inline int check_rule_2871(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2871
static inline void alert_rule_2871(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2871;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2872
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2872
static inline int check_rule_2872(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2872
static inline void alert_rule_2872(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2872;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2873
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2873
static inline int check_rule_2873(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2873
static inline void alert_rule_2873(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2873;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2874
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2874
static inline int check_rule_2874(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2874
static inline void alert_rule_2874(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2874;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2875
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2875
static inline int check_rule_2875(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2875
static inline void alert_rule_2875(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2875;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2876
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2876
static inline int check_rule_2876(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2876
static inline void alert_rule_2876(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2876;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2877
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2877
static inline int check_rule_2877(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2877
static inline void alert_rule_2877(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2877;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2878
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2878
static inline int check_rule_2878(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.alter_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2878
static inline void alert_rule_2878(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2878;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.alter_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2879
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.cancel_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2879
static inline int check_rule_2879(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.cancel_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2879
static inline void alert_rule_2879(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2879;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.cancel_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.cancel_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2880
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.comment_on_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2880
static inline int check_rule_2880(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.comment_on_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2880
static inline void alert_rule_2880(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2880;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.comment_on_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.comment_on_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2881
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.comment_on_priority_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2881
static inline int check_rule_2881(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.comment_on_priority_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2881
static inline void alert_rule_2881(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2881;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.comment_on_priority_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.comment_on_priority_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2882
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.comment_on_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2882
static inline int check_rule_2882(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.comment_on_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2882
static inline void alert_rule_2882(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2882;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.comment_on_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.comment_on_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2883
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.comment_on_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2883
static inline int check_rule_2883(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.comment_on_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2883
static inline void alert_rule_2883(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2883;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.comment_on_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.comment_on_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2884
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.comment_on_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2884
static inline int check_rule_2884(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.comment_on_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2884
static inline void alert_rule_2884(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2884;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.comment_on_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.comment_on_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2885
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.define_priority_group buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2885
static inline int check_rule_2885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.define_priority_group",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2885
static inline void alert_rule_2885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.define_priority_group buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.define_priority_group buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2886
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.define_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2886
static inline int check_rule_2886(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.define_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2886
static inline void alert_rule_2886(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2886;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.define_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.define_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2887
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_delete_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2887
static inline int check_rule_2887(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_delete_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2887
static inline void alert_rule_2887(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2887;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_delete_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_delete_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2888
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_char buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2888
static inline int check_rule_2888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_char",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2888
static inline void alert_rule_2888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_char buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_char buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2889
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_date buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2889
static inline int check_rule_2889(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_date",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2889
static inline void alert_rule_2889(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2889;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_date buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_date buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2890
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nchar buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2890
static inline int check_rule_2890(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_nchar",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2890
static inline void alert_rule_2890(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2890;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nchar buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nchar buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2891
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_number buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2891
static inline int check_rule_2891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_number",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2891
static inline void alert_rule_2891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_number buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_number buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2892
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nvarchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2892
static inline int check_rule_2892(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_nvarchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2892
static inline void alert_rule_2892(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2892;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nvarchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_nvarchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2893
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_raw buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2893
static inline int check_rule_2893(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_raw",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2893
static inline void alert_rule_2893(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2893;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_raw buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_raw buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2894
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2894
static inline int check_rule_2894(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2894
static inline void alert_rule_2894(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2894;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2895
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_varchar2 buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2895
static inline int check_rule_2895(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_priority_varchar2",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2895
static inline void alert_rule_2895(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2895;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_varchar2 buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_priority_varchar2 buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2896
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority_site buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2896
static inline int check_rule_2896(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_site_priority_site",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2896
static inline void alert_rule_2896(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2896;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority_site buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority_site buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2897
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2897
static inline int check_rule_2897(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_site_priority",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2897
static inline void alert_rule_2897(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2897;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_site_priority buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2898
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_unique_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2898
static inline int check_rule_2898(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_unique_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2898
static inline void alert_rule_2898(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2898;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_unique_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_unique_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2899
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.drop_update_resolution buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2899
static inline int check_rule_2899(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.drop_update_resolution",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2899
static inline void alert_rule_2899(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2899;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.drop_update_resolution buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.drop_update_resolution buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2900
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.purge_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2900
static inline int check_rule_2900(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.purge_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2900
static inline void alert_rule_2900(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2900;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.purge_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.purge_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2901
// Rule Name: SERVER-ORACLE sys.dbms_repcat_conf.register_statistics buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2901
static inline int check_rule_2901(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_conf.register_statistics",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2901
static inline void alert_rule_2901(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2901;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_conf.register_statistics buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_conf.register_statistics buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2902
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.alter_snapshot_propagation buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2902
static inline int check_rule_2902(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.alter_snapshot_propagation",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2902
static inline void alert_rule_2902(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2902;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.alter_snapshot_propagation buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.alter_snapshot_propagation buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2903
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2903
static inline int check_rule_2903(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.create_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2903
static inline void alert_rule_2903(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2903;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2904
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2904
static inline int check_rule_2904(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.create_snapshot_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2904
static inline void alert_rule_2904(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2904;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2905
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repschema buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2905
static inline int check_rule_2905(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.create_snapshot_repschema",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2905
static inline void alert_rule_2905(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2905;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repschema buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.create_snapshot_repschema buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2906
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2906
static inline int check_rule_2906(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.drop_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2906
static inline void alert_rule_2906(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2906;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2907
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repobject buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2907
static inline int check_rule_2907(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.drop_snapshot_repobject",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2907
static inline void alert_rule_2907(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2907;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repobject buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repobject buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2908
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repschema buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2908
static inline int check_rule_2908(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.drop_snapshot_repschema",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2908
static inline void alert_rule_2908(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2908;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repschema buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.drop_snapshot_repschema buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2909
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.generate_snapshot_support buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2909
static inline int check_rule_2909(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.generate_snapshot_support",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2909
static inline void alert_rule_2909(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2909;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.generate_snapshot_support buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.generate_snapshot_support buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2910
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2910
static inline int check_rule_2910(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.refresh_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2910
static inline void alert_rule_2910(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2910;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2911
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repschema buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2911
static inline int check_rule_2911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.refresh_snapshot_repschema",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2911
static inline void alert_rule_2911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repschema buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.refresh_snapshot_repschema buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2912
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.register_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2912
static inline int check_rule_2912(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.register_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2912
static inline void alert_rule_2912(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2912;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.register_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.register_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2913
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.repcat_import_check buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2913
static inline int check_rule_2913(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.repcat_import_check",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2913
static inline void alert_rule_2913(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2913;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.repcat_import_check buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.repcat_import_check buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2914
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.set_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2914
static inline int check_rule_2914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.set_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2914
static inline void alert_rule_2914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.set_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.set_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2915
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.switch_snapshot_master buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2915
static inline int check_rule_2915(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.switch_snapshot_master",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2915
static inline void alert_rule_2915(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2915;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.switch_snapshot_master buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.switch_snapshot_master buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2916
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.unregister_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2916
static inline int check_rule_2916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.unregister_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2916
static inline void alert_rule_2916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.unregister_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.unregister_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2917
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna_utl.switch_snapshot_master buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2917
static inline int check_rule_2917(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna_utl.switch_snapshot_master",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2917
static inline void alert_rule_2917(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2917;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna_utl.switch_snapshot_master buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna_utl.switch_snapshot_master buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2918
// Rule Name: SERVER-ORACLE sys.dbms_repcat_sna.validate_for_local_flavor buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2918
static inline int check_rule_2918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_sna.validate_for_local_flavor",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2918
static inline void alert_rule_2918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_sna.validate_for_local_flavor buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_sna.validate_for_local_flavor buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2919
// Rule Name: SERVER-ORACLE sys.dbms_repcat_untrusted.register_snapshot_repgroup buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 2919
static inline int check_rule_2919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Variable port check: $ORACLE_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sys.dbms_repcat_untrusted.register_snapshot_repgroup",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2919
static inline void alert_rule_2919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE sys.dbms_repcat_untrusted.register_snapshot_repgroup buffer overflow attempt", 
                     sizeof("SERVER-ORACLE sys.dbms_repcat_untrusted.register_snapshot_repgroup buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2922
// Rule Name: PROTOCOL-DNS TCP inverse query
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 2922
static inline int check_rule_2922(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 2922
static inline void alert_rule_2922(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2922;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-DNS TCP inverse query", 
                     sizeof("PROTOCOL-DNS TCP inverse query"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2923
// Rule Name: NETBIOS SMB repeated logon failure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 2923
static inline int check_rule_2923(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 139
    if (tcph->source != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMBs",depth 5,offset 4', 'm|00 00 C0|",within 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2923
static inline void alert_rule_2923(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2923;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB repeated logon failure", 
                     sizeof("NETBIOS SMB repeated logon failure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2924
// Rule Name: NETBIOS SMB-DS repeated logon failure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 2924
static inline int check_rule_2924(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMBs",depth 5,offset 4', 'm|00 00 C0|",within 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2924
static inline void alert_rule_2924(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2924;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS repeated logon failure", 
                     sizeof("NETBIOS SMB-DS repeated logon failure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2926
// Rule Name: SERVER-WEBAPP PhpGedView PGV base directory manipulation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 2926
static inline int check_rule_2926(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_conf.php",nocase', 'PGV_BASE_DIRECTORY",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2926
static inline void alert_rule_2926(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2926;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PhpGedView PGV base directory manipulation", 
                     sizeof("SERVER-WEBAPP PhpGedView PGV base directory manipulation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2927
// Rule Name: OS-WINDOWS Microsoft Windows XPAT pattern overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2927
static inline int check_rule_2927(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: PAT|20|",depth 5,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2927
static inline void alert_rule_2927(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2927;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows XPAT pattern overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows XPAT pattern overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2936
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP nddeapi NDdeSetTrustedShareW overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 2936
static inline int check_rule_2936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: !"|00|",depth 256,offset 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 2936
static inline void alert_rule_2936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP nddeapi NDdeSetTrustedShareW overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP nddeapi NDdeSetTrustedShareW overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 2942
// Rule Name: NETBIOS DCERPC NCACN-IP-TCP winreg InitiateSystemShutdown attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 2942
static inline int check_rule_2942(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 2942
static inline void alert_rule_2942(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 2942;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS DCERPC NCACN-IP-TCP winreg InitiateSystemShutdown attempt", 
                     sizeof("NETBIOS DCERPC NCACN-IP-TCP winreg InitiateSystemShutdown attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3007
// Rule Name: PROTOCOL-IMAP command overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3007
static inline int check_rule_3007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: LOGIN
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3007
static inline void alert_rule_3007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP command overflow attempt", 
                     sizeof("PROTOCOL-IMAP command overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3008
// Rule Name: PROTOCOL-IMAP delete literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3008
static inline int check_rule_3008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: DELETE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3008
static inline void alert_rule_3008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP delete literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP delete literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3009
// Rule Name: MALWARE-BACKDOOR NetBus Pro 2.0 connection request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3009
static inline int check_rule_3009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20034
    if (tcph->dest != htons(20034))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['BN |00 02 00|",depth 6', '|05 00|",depth 2,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3009
static inline void alert_rule_3009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR NetBus Pro 2.0 connection request", 
                     sizeof("MALWARE-BACKDOOR NetBus Pro 2.0 connection request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3010
// Rule Name: MALWARE-CNC RUX the Tick get windows directory
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3010
static inline int check_rule_3010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 22222
    if (tcph->dest != htons(22222))
        return 0;
    // Content matching (simplified)
    // Full pattern: WINDIR",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3010
static inline void alert_rule_3010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RUX the Tick get windows directory", 
                     sizeof("MALWARE-CNC RUX the Tick get windows directory"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3011
// Rule Name: MALWARE-CNC RUX the Tick get system directory
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3011
static inline int check_rule_3011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 22222
    if (tcph->dest != htons(22222))
        return 0;
    // Content matching (simplified)
    // Full pattern: SYSDIR",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3011
static inline void alert_rule_3011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RUX the Tick get system directory", 
                     sizeof("MALWARE-CNC RUX the Tick get system directory"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3012
// Rule Name: MALWARE-CNC RUX the Tick upload/execute arbitrary file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3012
static inline int check_rule_3012(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 22222
    if (tcph->dest != htons(22222))
        return 0;
    // Content matching (simplified)
    // Full pattern: ABCJZDATEIV",depth 11
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3012
static inline void alert_rule_3012(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3012;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RUX the Tick upload/execute arbitrary file", 
                     sizeof("MALWARE-CNC RUX the Tick upload/execute arbitrary file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3013
// Rule Name: MALWARE-CNC Asylum 0.1 connection request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3013
static inline int check_rule_3013(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 23432
    if (tcph->dest != htons(23432))
        return 0;
    // Content matching (simplified)
    // Full pattern: RQS",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3013
static inline void alert_rule_3013(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3013;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Asylum 0.1 connection request", 
                     sizeof("MALWARE-CNC Asylum 0.1 connection request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3014
// Rule Name: MALWARE-CNC Asylum 0.1 connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3014
static inline int check_rule_3014(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 23432
    if (tcph->source != htons(23432))
        return 0;
    // Content matching (simplified)
    // Full pattern: GNT",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3014
static inline void alert_rule_3014(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3014;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Asylum 0.1 connection", 
                     sizeof("MALWARE-CNC Asylum 0.1 connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3015
// Rule Name: MALWARE-CNC Insane Network 4.0 connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3015
static inline int check_rule_3015(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 2000
    if (tcph->source != htons(2000))
        return 0;
    // Content matching (simplified)
    // Full pattern: Insane Network vs 4.0 by Suid Flow|0A 0D|www.blackcode.com|0A 0D|[r00t]|23|",depth 62
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3015
static inline void alert_rule_3015(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3015;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Insane Network 4.0 connection", 
                     sizeof("MALWARE-CNC Insane Network 4.0 connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3016
// Rule Name: MALWARE-CNC Insane Network 4.0 connection port 63536
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3016
static inline int check_rule_3016(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 63536
    if (tcph->source != htons(63536))
        return 0;
    // Content matching (simplified)
    // Full pattern: Insane Network vs 4.0 by Suid Flow|0A 0D|www.blackcode.com|0A 0D|[r00t]|23|",depth 62
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3016
static inline void alert_rule_3016(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3016;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Insane Network 4.0 connection port 63536", 
                     sizeof("MALWARE-CNC Insane Network 4.0 connection port 63536"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3017
// Rule Name: OS-WINDOWS Microsoft Windows WINS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3017
static inline int check_rule_3017(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 42
    if (tcph->dest != htons(42))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 3017
static inline void alert_rule_3017(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3017;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows WINS overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows WINS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3018
// Rule Name: NETBIOS SMB NT Trans NT CREATE oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3018
static inline int check_rule_3018(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3018
static inline void alert_rule_3018(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3018;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3019
// Rule Name: NETBIOS SMB NT Trans NT CREATE andx oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3019
static inline int check_rule_3019(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3019
static inline void alert_rule_3019(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3019;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE andx oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE andx oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3020
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3020
static inline int check_rule_3020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3020
static inline void alert_rule_3020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3021
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode andx oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3021
static inline int check_rule_3021(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3021
static inline void alert_rule_3021(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3021;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode andx oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode andx oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3022
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3022
static inline int check_rule_3022(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3022
static inline void alert_rule_3022(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3022;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3023
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE andx oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3023
static inline int check_rule_3023(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3023
static inline void alert_rule_3023(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3023;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE andx oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE andx oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3024
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3024
static inline int check_rule_3024(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3024
static inline void alert_rule_3024(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3024;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3025
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode andx oversized Security Descriptor attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3025
static inline int check_rule_3025(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3025
static inline void alert_rule_3025(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3025;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode andx oversized Security Descriptor attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode andx oversized Security Descriptor attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3026
// Rule Name: NETBIOS SMB NT Trans NT CREATE SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3026
static inline int check_rule_3026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3026
static inline void alert_rule_3026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE SACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3027
// Rule Name: NETBIOS SMB NT Trans NT CREATE andx SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3027
static inline int check_rule_3027(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3027
static inline void alert_rule_3027(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3027;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE andx SACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE andx SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3028
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3028
static inline int check_rule_3028(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3028
static inline void alert_rule_3028(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3028;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode SACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3029
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode andx SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3029
static inline int check_rule_3029(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3029
static inline void alert_rule_3029(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3029;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode andx SACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode andx SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3030
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3030
static inline int check_rule_3030(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3030
static inline void alert_rule_3030(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3030;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE SACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3031
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE andx SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3031
static inline int check_rule_3031(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3031
static inline void alert_rule_3031(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3031;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE andx SACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE andx SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3032
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3032
static inline int check_rule_3032(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3032
static inline void alert_rule_3032(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3032;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode SACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3033
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode andx SACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3033
static inline int check_rule_3033(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3033
static inline void alert_rule_3033(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3033;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode andx SACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode andx SACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3034
// Rule Name: NETBIOS SMB NT Trans NT CREATE DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3034
static inline int check_rule_3034(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3034
static inline void alert_rule_3034(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3034;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE DACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3035
// Rule Name: NETBIOS SMB NT Trans NT CREATE andx DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3035
static inline int check_rule_3035(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3035
static inline void alert_rule_3035(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3035;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE andx DACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE andx DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3036
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3036
static inline int check_rule_3036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3036
static inline void alert_rule_3036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode DACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3037
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode andx DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3037
static inline int check_rule_3037(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3037
static inline void alert_rule_3037(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3037;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode andx DACL overflow attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode andx DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3038
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3038
static inline int check_rule_3038(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3038
static inline void alert_rule_3038(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3038;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE DACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3039
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE andx DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3039
static inline int check_rule_3039(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3039
static inline void alert_rule_3039(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3039;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE andx DACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE andx DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3040
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3040
static inline int check_rule_3040(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3040
static inline void alert_rule_3040(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3040;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode DACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3041
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode andx DACL overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3041
static inline int check_rule_3041(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3041
static inline void alert_rule_3041(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3041;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode andx DACL overflow attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode andx DACL overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3042
// Rule Name: NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3042
static inline int check_rule_3042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3042
static inline void alert_rule_3042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3043
// Rule Name: NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3043
static inline int check_rule_3043(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3043
static inline void alert_rule_3043(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3043;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3044
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3044
static inline int check_rule_3044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3044
static inline void alert_rule_3044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3045
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3045
static inline int check_rule_3045(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3045
static inline void alert_rule_3045(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3045;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3046
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3046
static inline int check_rule_3046(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3046
static inline void alert_rule_3046(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3046;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3047
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3047
static inline int check_rule_3047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3047
static inline void alert_rule_3047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3048
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3048
static inline int check_rule_3048(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3048
static inline void alert_rule_3048(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3048;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3049
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3049
static inline int check_rule_3049(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 12', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3049
static inline void alert_rule_3049(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3049;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3050
// Rule Name: NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3050
static inline int check_rule_3050(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3050
static inline void alert_rule_3050(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3050;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3051
// Rule Name: NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3051
static inline int check_rule_3051(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3051
static inline void alert_rule_3051(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3051;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3052
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3052
static inline int check_rule_3052(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3052
static inline void alert_rule_3052(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3052;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3053
// Rule Name: NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3053
static inline int check_rule_3053(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3053
static inline void alert_rule_3053(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3053;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3054
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3054
static inline int check_rule_3054(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3054
static inline void alert_rule_3054(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3054;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3055
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3055
static inline int check_rule_3055(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3055
static inline void alert_rule_3055(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3055;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3056
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3056
static inline int check_rule_3056(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB|A0|",within 5,distance 3', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3056
static inline void alert_rule_3056(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3056;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3057
// Rule Name: NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3057
static inline int check_rule_3057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '|A0|",depth 1,offset 39', '|01 00|",within 2,distance 37', '!"|00 00 00 00|",within 4,distance 16', '|00 00|",within 2,distance -10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3057
static inline void alert_rule_3057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt", 
                     sizeof("NETBIOS SMB-DS NT Trans NT CREATE unicode andx invalid SACL ace size dos attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3058
// Rule Name: PROTOCOL-IMAP copy literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3058
static inline int check_rule_3058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: COPY",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3058
static inline void alert_rule_3058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP copy literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP copy literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3061
// Rule Name: APP-DETECT distccd remote command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 3061
static inline int check_rule_3061(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3632
    if (tcph->dest != htons(3632))
        return 0;
    // Content matching (simplified)
    // Full pattern: DIST00000001",depth 12,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3061
static inline void alert_rule_3061(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3061;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT distccd remote command execution attempt", 
                     sizeof("APP-DETECT distccd remote command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3062
// Rule Name: SERVER-WEBAPP NetScreen SA 5000 delhomepage.cgi access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 3062
static inline int check_rule_3062(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /delhomepage.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3062
static inline void alert_rule_3062(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3062;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP NetScreen SA 5000 delhomepage.cgi access", 
                     sizeof("SERVER-WEBAPP NetScreen SA 5000 delhomepage.cgi access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3063
// Rule Name: MALWARE-BACKDOOR Vampire 1.2 connection request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3063
static inline int check_rule_3063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1020
    if (tcph->dest != htons(1020))
        return 0;
    // Content matching (simplified)
    // Full pattern: Hello...",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3063
static inline void alert_rule_3063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Vampire 1.2 connection request", 
                     sizeof("MALWARE-BACKDOOR Vampire 1.2 connection request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3064
// Rule Name: MALWARE-BACKDOOR Vampire 1.2 connection confirmation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3064
static inline int check_rule_3064(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 1020
    if (tcph->source != htons(1020))
        return 0;
    // Content matching (simplified)
    // Full pattern: Vampire v1.2 Server On-Line.....",depth 32
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3064
static inline void alert_rule_3064(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3064;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Vampire 1.2 connection confirmation", 
                     sizeof("MALWARE-BACKDOOR Vampire 1.2 connection confirmation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3065
// Rule Name: PROTOCOL-IMAP append literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3065
static inline int check_rule_3065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: APPEND",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3065
static inline void alert_rule_3065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP append literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP append literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3066
// Rule Name: PROTOCOL-IMAP APPEND overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3066
static inline int check_rule_3066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['APPEND",nocase', '!"|0D 0A|",within 256']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3066
static inline void alert_rule_3066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP APPEND overflow attempt", 
                     sizeof("PROTOCOL-IMAP APPEND overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3067
// Rule Name: PROTOCOL-IMAP examine literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3067
static inline int check_rule_3067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: EXAMINE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3067
static inline void alert_rule_3067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP examine literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP examine literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3069
// Rule Name: PROTOCOL-IMAP fetch literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3069
static inline int check_rule_3069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: FETCH",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3069
static inline void alert_rule_3069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP fetch literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP fetch literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3070
// Rule Name: PROTOCOL-IMAP fetch overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3070
static inline int check_rule_3070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: FETCH",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3070
static inline void alert_rule_3070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP fetch overflow attempt", 
                     sizeof("PROTOCOL-IMAP fetch overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3071
// Rule Name: PROTOCOL-IMAP status literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3071
static inline int check_rule_3071(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: STATUS",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3071
static inline void alert_rule_3071(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3071;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP status literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP status literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3072
// Rule Name: PROTOCOL-IMAP STATUS overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3072
static inline int check_rule_3072(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['STATUS",nocase', '!"|0D 0A|",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3072
static inline void alert_rule_3072(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3072;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP STATUS overflow attempt", 
                     sizeof("PROTOCOL-IMAP STATUS overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3073
// Rule Name: PROTOCOL-IMAP SUBSCRIBE literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3073
static inline int check_rule_3073(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: SUBSCRIBE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3073
static inline void alert_rule_3073(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3073;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP SUBSCRIBE literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP SUBSCRIBE literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3074
// Rule Name: PROTOCOL-IMAP SUBSCRIBE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3074
static inline int check_rule_3074(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: SUBSCRIBE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3074
static inline void alert_rule_3074(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3074;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP SUBSCRIBE overflow attempt", 
                     sizeof("PROTOCOL-IMAP SUBSCRIBE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3075
// Rule Name: PROTOCOL-IMAP unsubscribe literal overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3075
static inline int check_rule_3075(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: UNSUBSCRIBE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3075
static inline void alert_rule_3075(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3075;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP unsubscribe literal overflow attempt", 
                     sizeof("PROTOCOL-IMAP unsubscribe literal overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3076
// Rule Name: PROTOCOL-IMAP UNSUBSCRIBE overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3076
static inline int check_rule_3076(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 143
    if (tcph->dest != htons(143))
        return 0;
    // Content matching (simplified)
    // Full pattern: UNSUBSCRIBE",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3076
static inline void alert_rule_3076(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3076;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-IMAP UNSUBSCRIBE overflow attempt", 
                     sizeof("PROTOCOL-IMAP UNSUBSCRIBE overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3077
// Rule Name: PROTOCOL-FTP RNFR overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3077
static inline int check_rule_3077(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: RNFR",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3077
static inline void alert_rule_3077(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3077;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP RNFR overflow attempt", 
                     sizeof("PROTOCOL-FTP RNFR overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3078
// Rule Name: PROTOCOL-NNTP Microsoft Windows SEARCH pattern overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3078
static inline int check_rule_3078(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 119
    if (tcph->dest != htons(119))
        return 0;
    // Content matching (simplified)
    // Full pattern: SEARCH|20|",depth 7,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3078
static inline void alert_rule_3078(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3078;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-NNTP Microsoft Windows SEARCH pattern overflow attempt", 
                     sizeof("PROTOCOL-NNTP Microsoft Windows SEARCH pattern overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3079
// Rule Name: BROWSER-IE Microsoft Internet Explorer ANI file parsing buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3079
static inline int check_rule_3079(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['RIFF",depth 4', 'ACON",within 4,distance 4', 'anih",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3079
static inline void alert_rule_3079(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3079;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-IE Microsoft Internet Explorer ANI file parsing buffer overflow attempt", 
                     sizeof("BROWSER-IE Microsoft Internet Explorer ANI file parsing buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3081
// Rule Name: MALWARE-BACKDOOR Y3KRAT 1.5 Connect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3081
static inline int check_rule_3081(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 5880
    if (tcph->source != htons(5880))
        return 0;
    // Content matching (simplified)
    // Full pattern: connected",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3081
static inline void alert_rule_3081(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3081;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Y3KRAT 1.5 Connect", 
                     sizeof("MALWARE-BACKDOOR Y3KRAT 1.5 Connect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3082
// Rule Name: MALWARE-BACKDOOR Y3KRAT 1.5 Connect Client Response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3082
static inline int check_rule_3082(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 5880
    if (tcph->dest != htons(5880))
        return 0;
    // Content matching (simplified)
    // Full pattern: getclient",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3082
static inline void alert_rule_3082(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3082;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Y3KRAT 1.5 Connect Client Response", 
                     sizeof("MALWARE-BACKDOOR Y3KRAT 1.5 Connect Client Response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3083
// Rule Name: MALWARE-BACKDOOR Y3KRAT 1.5 Connection confirmation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 3083
static inline int check_rule_3083(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 5880
    if (tcph->source != htons(5880))
        return 0;
    // Content matching (simplified)
    // Full pattern: client",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3083
static inline void alert_rule_3083(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3083;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Y3KRAT 1.5 Connection confirmation", 
                     sizeof("MALWARE-BACKDOOR Y3KRAT 1.5 Connection confirmation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3084
// Rule Name: SERVER-OTHER Veritas backup overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3084
static inline int check_rule_3084(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 6101
    if (tcph->dest != htons(6101))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|02 00|",depth 2', '|00|",within 1,distance 1', '!"|00|",depth 66,offset 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3084
static inline void alert_rule_3084(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3084;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Veritas backup overflow attempt", 
                     sizeof("SERVER-OTHER Veritas backup overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3085
// Rule Name: SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3085
static inline int check_rule_3085(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: aim|3A|goaway?message=",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3085
static inline void alert_rule_3085(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3085;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt", 
                     sizeof("SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3086
// Rule Name: SERVER-WEBAPP 3Com 3CRADSL72 ADSL 11g Wireless Router app_sta.stm access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 3086
static inline int check_rule_3086(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /app_sta.stm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3086
static inline void alert_rule_3086(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3086;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP 3Com 3CRADSL72 ADSL 11g Wireless Router app_sta.stm access attempt", 
                     sizeof("SERVER-WEBAPP 3Com 3CRADSL72 ADSL 11g Wireless Router app_sta.stm access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3087
// Rule Name: SERVER-IIS w3who.dll buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3087
static inline int check_rule_3087(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /w3who.dll?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3087
static inline void alert_rule_3087(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3087;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS w3who.dll buffer overflow attempt", 
                     sizeof("SERVER-IIS w3who.dll buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3088
// Rule Name: FILE-MULTIMEDIA Nullsoft Winamp cda file name overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3088
static inline int check_rule_3088(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .cda",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3088
static inline void alert_rule_3088(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3088;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-MULTIMEDIA Nullsoft Winamp cda file name overflow attempt", 
                     sizeof("FILE-MULTIMEDIA Nullsoft Winamp cda file name overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3114
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP llsrpc LlsrConnect overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3114
static inline int check_rule_3114(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 3114
static inline void alert_rule_3114(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3114;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP llsrpc LlsrConnect overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP llsrpc LlsrConnect overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3130
// Rule Name: PUA-OTHER Microsoft MSN Messenger png overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3130
static inline int check_rule_3130(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 1863
    if (tcph->source != htons(1863))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['application/x-msnmsgrp2p",nocase', '|89|PNG|0D 0A 1A 0A|",distance 0', 'IHDR",within 4,distance 4', '|03|",within 1,distance 9', 'tRNS",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3130
static inline void alert_rule_3130(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3130;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER Microsoft MSN Messenger png overflow", 
                     sizeof("PUA-OTHER Microsoft MSN Messenger png overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3131
// Rule Name: SERVER-WEBAPP mailman directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 3131
static inline int check_rule_3131(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mailman/', '.../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3131
static inline void alert_rule_3131(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3131;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP mailman directory traversal attempt", 
                     sizeof("SERVER-WEBAPP mailman directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3132
// Rule Name: FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3132
static inline int check_rule_3132(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|89|PNG|0D 0A 1A 0A|', 'IHDR",within 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3132
static inline void alert_rule_3132(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3132;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt", 
                     sizeof("FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3133
// Rule Name: FILE-IMAGE Microsoft Multiple Products PNG large image height download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3133
static inline int check_rule_3133(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|89|PNG|0D 0A 1A 0A|', 'IHDR",within 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3133
static inline void alert_rule_3133(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3133;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft Multiple Products PNG large image height download attempt", 
                     sizeof("FILE-IMAGE Microsoft Multiple Products PNG large image height download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3134
// Rule Name: FILE-IMAGE Microsoft PNG large colour depth download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3134
static inline int check_rule_3134(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|89|PNG|0D 0A 1A 0A|', 'IHDR",within 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3134
static inline void alert_rule_3134(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3134;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft PNG large colour depth download attempt", 
                     sizeof("FILE-IMAGE Microsoft PNG large colour depth download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3135
// Rule Name: NETBIOS SMB Trans2 QUERY_FILE_INFO attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3135
static inline int check_rule_3135(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3', '|07 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3135
static inline void alert_rule_3135(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3135;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 QUERY_FILE_INFO attempt", 
                     sizeof("NETBIOS SMB Trans2 QUERY_FILE_INFO attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3136
// Rule Name: NETBIOS SMB Trans2 QUERY_FILE_INFO andx attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3136
static inline int check_rule_3136(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39', '|07 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3136
static inline void alert_rule_3136(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3136;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 QUERY_FILE_INFO andx attempt", 
                     sizeof("NETBIOS SMB Trans2 QUERY_FILE_INFO andx attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3137
// Rule Name: NETBIOS SMB-DS Trans2 QUERY_FILE_INFO attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3137
static inline int check_rule_3137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3', '|07 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3137
static inline void alert_rule_3137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Trans2 QUERY_FILE_INFO attempt", 
                     sizeof("NETBIOS SMB-DS Trans2 QUERY_FILE_INFO attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3138
// Rule Name: NETBIOS SMB-DS Trans2 QUERY_FILE_INFO andx attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3138
static inline int check_rule_3138(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39', '|07 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3138
static inline void alert_rule_3138(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3138;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Trans2 QUERY_FILE_INFO andx attempt", 
                     sizeof("NETBIOS SMB-DS Trans2 QUERY_FILE_INFO andx attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3139
// Rule Name: NETBIOS SMB Trans2 FIND_FIRST2 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3139
static inline int check_rule_3139(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3', '|01 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3139
static inline void alert_rule_3139(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3139;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 FIND_FIRST2 attempt", 
                     sizeof("NETBIOS SMB Trans2 FIND_FIRST2 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3140
// Rule Name: NETBIOS SMB Trans2 FIND_FIRST2 andx attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3140
static inline int check_rule_3140(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 139
    if (tcph->dest != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39', '|01 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3140
static inline void alert_rule_3140(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3140;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 FIND_FIRST2 andx attempt", 
                     sizeof("NETBIOS SMB Trans2 FIND_FIRST2 andx attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3141
// Rule Name: NETBIOS SMB-DS Trans2 FIND_FIRST2 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3141
static inline int check_rule_3141(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3', '|01 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3141
static inline void alert_rule_3141(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3141;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Trans2 FIND_FIRST2 attempt", 
                     sizeof("NETBIOS SMB-DS Trans2 FIND_FIRST2 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3142
// Rule Name: NETBIOS SMB-DS Trans2 FIND_FIRST2 andx attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3142
static inline int check_rule_3142(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39', '|01 00|",within 2,distance 29']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3142
static inline void alert_rule_3142(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3142;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Trans2 FIND_FIRST2 andx attempt", 
                     sizeof("NETBIOS SMB-DS Trans2 FIND_FIRST2 andx attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3143
// Rule Name: OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 command response overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3143
static inline int check_rule_3143(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 139
    if (tcph->source != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3143
static inline void alert_rule_3143(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3143;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 command response overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 command response overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3144
// Rule Name: OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 response andx overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3144
static inline int check_rule_3144(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 139
    if (tcph->source != htons(139))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3144
static inline void alert_rule_3144(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3144;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 response andx overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB Trans2 FIND_FIRST2 response andx overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3145
// Rule Name: OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3145
static inline int check_rule_3145(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB2",within 5,distance 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3145
static inline void alert_rule_3145(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3145;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3146
// Rule Name: OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response andx overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3146
static inline int check_rule_3146(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",depth 1', '|FF|SMB",within 4,distance 3', '2",depth 1,offset 39']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3146
static inline void alert_rule_3146(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3146;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response andx overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB-DS Trans2 FIND_FIRST2 response andx overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3147
// Rule Name: PROTOCOL-TELNET login buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3147
static inline int check_rule_3147(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FA|'|00 00|TTYPROMPT|01|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3147
static inline void alert_rule_3147(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3147;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET login buffer overflow attempt", 
                     sizeof("PROTOCOL-TELNET login buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3148
// Rule Name: OS-WINDOWS Microsoft Windows HTML Help hhctrl.ocx clsid access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3148
static inline int check_rule_3148(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: clsid:adb880a6-d8ff-11cf-9377-00aa003b7a11",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3148
static inline void alert_rule_3148(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3148;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows HTML Help hhctrl.ocx clsid access attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows HTML Help hhctrl.ocx clsid access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3149
// Rule Name: BROWSER-IE Microsoft Internet Explorer malformed object type overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3149
static inline int check_rule_3149(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['object",nocase', 'type",within 200,nocase', '////////////////////////////////",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3149
static inline void alert_rule_3149(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3149;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-IE Microsoft Internet Explorer malformed object type overflow attempt", 
                     sizeof("BROWSER-IE Microsoft Internet Explorer malformed object type overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3150
// Rule Name: SERVER-IIS SQLXML content type overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3150
static inline int check_rule_3150(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: contenttype=
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3150
static inline void alert_rule_3150(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3150;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS SQLXML content type overflow", 
                     sizeof("SERVER-IIS SQLXML content type overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3151
// Rule Name: PROTOCOL-FINGER / execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 3151
static inline int check_rule_3151(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 79
    if (tcph->dest != htons(79))
        return 0;
    // Content matching (simplified)
    // Full pattern: /
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3151
static inline void alert_rule_3151(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3151;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FINGER / execution attempt", 
                     sizeof("PROTOCOL-FINGER / execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3152
// Rule Name: SQL sa brute force failed login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 3152
static inline int check_rule_3152(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SQL_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 1433
    if (tcph->source != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: Login failed for user 'sa'",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3152
static inline void alert_rule_3152(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3152;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sa brute force failed login attempt", 
                     sizeof("SQL sa brute force failed login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3153
// Rule Name: PROTOCOL-DNS TCP inverse query overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3153
static inline int check_rule_3153(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 53
    if (tcph->dest != htons(53))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 3153
static inline void alert_rule_3153(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3153;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-DNS TCP inverse query overflow", 
                     sizeof("PROTOCOL-DNS TCP inverse query overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3155
// Rule Name: MALWARE-BACKDOOR BackOrifice 2000 Inbound Traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 3155
static inline int check_rule_3155(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 31337
    if (tcph->dest != htons(31337))
        return 0;
    // Content matching (simplified)
    // Full pattern: 1j|D0 D9|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3155
static inline void alert_rule_3155(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3155;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR BackOrifice 2000 Inbound Traffic", 
                     sizeof("MALWARE-BACKDOOR BackOrifice 2000 Inbound Traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3158
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator CoGetInstanceFromFile attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3158
static inline int check_rule_3158(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|01 10 08 00 CC CC CC CC|', '|5C 00 5C 00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3158
static inline void alert_rule_3158(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3158;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator CoGetInstanceFromFile attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator CoGetInstanceFromFile attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3192
// Rule Name: OS-WINDOWS Microsoft Windows Media Player directory traversal via Content-Disposition attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3192
static inline int check_rule_3192(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.wmz",fast_pattern,nocase', 'Content-Disposition|3A|",nocase', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3192
static inline void alert_rule_3192(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3192;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows Media Player directory traversal via Content-Disposition attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows Media Player directory traversal via Content-Disposition attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3193
// Rule Name: SERVER-IIS .cmd executable file parsing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 3193
static inline int check_rule_3193(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .cmd|22|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3193
static inline void alert_rule_3193(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3193;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .cmd executable file parsing attack", 
                     sizeof("SERVER-IIS .cmd executable file parsing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3194
// Rule Name: SERVER-IIS .bat executable file parsing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 3194
static inline int check_rule_3194(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .bat|22|",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3194
static inline void alert_rule_3194(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3194;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS .bat executable file parsing attack", 
                     sizeof("SERVER-IIS .bat executable file parsing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3195
// Rule Name: OS-WINDOWS name query overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3195
static inline int check_rule_3195(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 137
    if (tcph->dest != htons(137))
        return 0;
    // Content matching (simplified)
    // Full pattern:  ",offset 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3195
static inline void alert_rule_3195(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3195;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS name query overflow attempt TCP", 
                     sizeof("OS-WINDOWS name query overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3199
// Rule Name: OS-WINDOWS Microsoft Windows WINS name query overflow attempt TCP
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3199
static inline int check_rule_3199(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 42
    if (tcph->dest != htons(42))
        return 0;
    // Content matching (simplified)
    // Full pattern:  ",offset 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3199
static inline void alert_rule_3199(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3199;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows WINS name query overflow attempt TCP", 
                     sizeof("OS-WINDOWS Microsoft Windows WINS name query overflow attempt TCP"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3201
// Rule Name: SERVER-IIS httpodbc.dll access - nimda
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 3201
static inline int check_rule_3201(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /httpodbc.dll",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3201
static inline void alert_rule_3201(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3201;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-IIS httpodbc.dll access - nimda", 
                     sizeof("SERVER-IIS httpodbc.dll access - nimda"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3218
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP winreg OpenKey overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3218
static inline int check_rule_3218(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 3218
static inline void alert_rule_3218(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3218;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP winreg OpenKey overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP winreg OpenKey overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3238
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP irot IrotIsRunning/Revoke overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3238
static inline int check_rule_3238(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    return 1;  // All checks passed
}

// Alert function for rule 3238
static inline void alert_rule_3238(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3238;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP irot IrotIsRunning/Revoke overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP irot IrotIsRunning/Revoke overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3273
// Rule Name: SQL sa brute force failed login unicode attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: unsuccessful-user
// Generated from Snort rule parser output

// Check function for rule 3273
static inline int check_rule_3273(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SQL_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 1433
    if (tcph->source != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: L|00|o|00|g|00|i|00|n|00| |00|f|00|a|00|i|00|l|00|e|00|d|00| |00|f|00|o|00|r|00| |00|u|00|s|00|e|00|r|00| |00|'|00|s|00|a|00|'|00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3273
static inline void alert_rule_3273(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3273;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL sa brute force failed login unicode attempt", 
                     sizeof("SQL sa brute force failed login unicode attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3274
// Rule Name: PROTOCOL-TELNET login buffer non-evasive overflow attempt 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3274
static inline int check_rule_3274(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $TELNET_SERVERS (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FA|'|00 00|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3274
static inline void alert_rule_3274(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3274;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-TELNET login buffer non-evasive overflow attempt ", 
                     sizeof("PROTOCOL-TELNET login buffer non-evasive overflow attempt "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3397
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator RemoteCreateInstance attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3397
static inline int check_rule_3397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|01 10 08 00 CC CC CC CC|', '|5C 00 5C 00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3397
static inline void alert_rule_3397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator RemoteCreateInstance attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP ISystemActivator RemoteCreateInstance attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3409
// Rule Name: OS-WINDOWS DCERPC NCACN-IP-TCP IActivation remoteactivation overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3409
static inline int check_rule_3409(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    return 1;  // All checks passed
}

// Alert function for rule 3409
static inline void alert_rule_3409(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3409;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS DCERPC NCACN-IP-TCP IActivation remoteactivation overflow attempt", 
                     sizeof("OS-WINDOWS DCERPC NCACN-IP-TCP IActivation remoteactivation overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3441
// Rule Name: PROTOCOL-FTP PORT bounce attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 3441
static inline int check_rule_3441(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PORT",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3441
static inline void alert_rule_3441(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3441;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP PORT bounce attempt", 
                     sizeof("PROTOCOL-FTP PORT bounce attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3442
// Rule Name: OS-WINDOWS Microsoft Windows TCP print service overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 3442
static inline int check_rule_3442(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 515
    if (tcph->dest != htons(515))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",within 497', '|0A|",within 497']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3442
static inline void alert_rule_3442(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3442;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows TCP print service overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows TCP print service overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3453
// Rule Name: SERVER-OTHER Arkeia client backup system info probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 3453
static inline int check_rule_3453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 617
    if (tcph->dest != htons(617))
        return 0;
    // Content matching (simplified)
    // Full pattern: ARKADMIN_GET_
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3453
static inline void alert_rule_3453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Arkeia client backup system info probe", 
                     sizeof("SERVER-OTHER Arkeia client backup system info probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3454
// Rule Name: SERVER-OTHER Arkeia client backup generic info probe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 3454
static inline int check_rule_3454(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 617
    if (tcph->dest != htons(617))
        return 0;
    // Content matching (simplified)
    // Full pattern: ARKFS|00|root|00|root",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3454
static inline void alert_rule_3454(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3454;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Arkeia client backup generic info probe", 
                     sizeof("SERVER-OTHER Arkeia client backup generic info probe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3455
// Rule Name: SERVER-OTHER Bontago Game Server Nickname buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3455
static inline int check_rule_3455(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 5001
    if (tcph->dest != htons(5001))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF 01 00 00 00 00 01|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3455
static inline void alert_rule_3455(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3455;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Bontago Game Server Nickname buffer overflow", 
                     sizeof("SERVER-OTHER Bontago Game Server Nickname buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3456
// Rule Name: SERVER-MYSQL 4.0 root login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 3456
static inline int check_rule_3456(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SQL_SERVERS (destination)
    // Check dst port: 3306
    if (tcph->dest != htons(3306))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|01|",depth 1,offset 3', 'root|00|",within 5,distance 5,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3456
static inline void alert_rule_3456(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3456;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MYSQL 4.0 root login attempt", 
                     sizeof("SERVER-MYSQL 4.0 root login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3457
// Rule Name: SERVER-OTHER Arkeia backup client type 77 overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3457
static inline int check_rule_3457(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 617
    if (tcph->dest != htons(617))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00|M",depth 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3457
static inline void alert_rule_3457(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3457;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Arkeia backup client type 77 overflow attempt", 
                     sizeof("SERVER-OTHER Arkeia backup client type 77 overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3458
// Rule Name: SERVER-OTHER Arkeia backup client type 84 overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 3458
static inline int check_rule_3458(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 617
    if (tcph->dest != htons(617))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|T",depth 2', '!"|00|",depth 255,offset 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3458
static inline void alert_rule_3458(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3458;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Arkeia backup client type 84 overflow attempt", 
                     sizeof("SERVER-OTHER Arkeia backup client type 84 overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3460
// Rule Name: PROTOCOL-FTP REST with numeric argument
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 3460
static inline int check_rule_3460(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: REST",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3460
static inline void alert_rule_3460(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3460;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP REST with numeric argument", 
                     sizeof("PROTOCOL-FTP REST with numeric argument"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3461
// Rule Name: SERVER-MAIL Content-Type overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3461
static inline int check_rule_3461(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Type",nocase', '|3A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3461
static inline void alert_rule_3461(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3461;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Content-Type overflow attempt", 
                     sizeof("SERVER-MAIL Content-Type overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3462
// Rule Name: BROWSER-IE Microsoft Internet Explorer Content-Encoding overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 3462
static inline int check_rule_3462(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Encoding",nocase', '|3A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3462
static inline void alert_rule_3462(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3462;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-IE Microsoft Internet Explorer Content-Encoding overflow attempt", 
                     sizeof("BROWSER-IE Microsoft Internet Explorer Content-Encoding overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3463
// Rule Name: SERVER-WEBAPP awstats access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 3463
static inline int check_rule_3463(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /awstats.pl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3463
static inline void alert_rule_3463(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3463;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP awstats access", 
                     sizeof("SERVER-WEBAPP awstats access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 3464
// Rule Name: SERVER-WEBAPP awstats.pl command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 3464
static inline int check_rule_3464(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/awstats.pl?",fast_pattern,nocase', 'update=', 'logfile=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 3464
static inline void alert_rule_3464(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 3464;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP awstats.pl command execution attempt", 
                     sizeof("SERVER-WEBAPP awstats.pl command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 8445
// Rule Name: FILE-OFFICE Microsoft Windows RTF file with embedded object package download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 8445
static inline int check_rule_8445(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['{|5C|rt",nocase', '{|5C|object|5C|objemb{|5C|*|5C|objclass Package}",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 8445
static inline void alert_rule_8445(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 8445;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE Microsoft Windows RTF file with embedded object package download attempt", 
                     sizeof("FILE-OFFICE Microsoft Windows RTF file with embedded object package download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 13801
// Rule Name: FILE-IDENTIFY RTF file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 13801
static inline int check_rule_13801(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .rtf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 13801
static inline void alert_rule_13801(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 13801;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RTF file download request", 
                     sizeof("FILE-IDENTIFY RTF file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 15013
// Rule Name: FILE-IDENTIFY PDF file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 15013
static inline int check_rule_15013(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .pdf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 15013
static inline void alert_rule_15013(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 15013;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PDF file download request", 
                     sizeof("FILE-IDENTIFY PDF file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 15587
// Rule Name: FILE-IDENTIFY Microsoft Office Word file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 15587
static inline int check_rule_15587(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .doc",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 15587
static inline void alert_rule_15587(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 15587;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Office Word file download request", 
                     sizeof("FILE-IDENTIFY Microsoft Office Word file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16205
// Rule Name: FILE-IDENTIFY BMP file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16205
static inline int check_rule_16205(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .bmp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16205
static inline void alert_rule_16205(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16205;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY BMP file download request", 
                     sizeof("FILE-IDENTIFY BMP file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16368
// Rule Name: MALWARE-CNC Win.Trojan.Hydraq variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 16368
static inline int check_rule_16368(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF FF FF FF FF FF 00 00 FE FF FF FF FF FF FF FF FF FF 88 FF|",depth 20
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16368
static inline void alert_rule_16368(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16368;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Hydraq variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Hydraq variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16406
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16406
static inline int check_rule_16406(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jpg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16406
static inline void alert_rule_16406(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16406;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16407
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16407
static inline int check_rule_16407(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jpeg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16407
static inline void alert_rule_16407(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16407;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16425
// Rule Name: FILE-IDENTIFY Portable Executable binary file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16425
static inline int check_rule_16425(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16425
static inline void alert_rule_16425(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16425;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable binary file download request", 
                     sizeof("FILE-IDENTIFY Portable Executable binary file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16474
// Rule Name: FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16474
static inline int check_rule_16474(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|D0 CF 11 E0 A1 B1 1A E1|', '>|00 03 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16474
static inline void alert_rule_16474(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16474;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected", 
                     sizeof("FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16475
// Rule Name: FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16475
static inline int check_rule_16475(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|D0 CF 11 E0 A1 B1 1A E1|', '>|00 04 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16475
static inline void alert_rule_16475(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16475;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected", 
                     sizeof("FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 16529
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 16529
static inline int check_rule_16529(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .pjpeg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 16529
static inline void alert_rule_16529(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 16529;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 17314
// Rule Name: FILE-IDENTIFY OLE document file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 17314
static inline int check_rule_17314(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |D0 CF 11 E0 A1 B1 1A E1|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 17314
static inline void alert_rule_17314(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 17314;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY OLE document file magic detected", 
                     sizeof("FILE-IDENTIFY OLE document file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 17380
// Rule Name: FILE-IDENTIFY PNG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 17380
static inline int check_rule_17380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .png",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 17380
static inline void alert_rule_17380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 17380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PNG file download request", 
                     sizeof("FILE-IDENTIFY PNG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 17733
// Rule Name: FILE-IDENTIFY XML file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 17733
static inline int check_rule_17733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .xml",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 17733
static inline void alert_rule_17733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 17733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file download request", 
                     sizeof("FILE-IDENTIFY XML file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 17745
// Rule Name: NETBIOS SMB TRANS2 Find_First2 request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 17745
static inline int check_rule_17745(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB2|00 00 00 00|",depth 9,offset 4', '|00 00|",within 2,distance 13', '|00|",within 1,distance 18', '|00 00|",within 2,distance 6', '|01 00|",within 2,distance 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 17745
static inline void alert_rule_17745(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 17745;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB TRANS2 Find_First2 request attempt", 
                     sizeof("NETBIOS SMB TRANS2 Find_First2 request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 19190
// Rule Name: NETBIOS SMB-DS Trans2 Distributed File System GET_DFS_REFERRAL request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 19190
static inline int check_rule_19190(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00|",offset 1', '|FF|SMB2",depth 5,offset 4', '|00 00 00 00|",within 4', '|10 00|",depth 2,offset 65']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 19190
static inline void alert_rule_19190(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 19190;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB-DS Trans2 Distributed File System GET_DFS_REFERRAL request", 
                     sizeof("NETBIOS SMB-DS Trans2 Distributed File System GET_DFS_REFERRAL request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 19211
// Rule Name: FILE-IDENTIFY ZIP archive file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 19211
static inline int check_rule_19211(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .zip",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 19211
static inline void alert_rule_19211(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 19211;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ZIP archive file download request", 
                     sizeof("FILE-IDENTIFY ZIP archive file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20080
// Rule Name: MALWARE-CNC Win.Trojan.Derusbi.A variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 20080
static inline int check_rule_20080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 12080
    if (tcph->dest != htons(12080))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 01 00 00 00|",depth 7,offset 1', '|01 00 00 00 68 01 00 00|",within 8,distance 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20080
static inline void alert_rule_20080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Derusbi.A variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Derusbi.A variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20221
// Rule Name: MALWARE-CNC Win.Trojan.Injector variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 20221
static inline int check_rule_20221(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| Opera|5C|9.64|0A|",fast_pattern,nocase', 'bb.php?v=', 'id=",distance 0', 'b=",distance 0', 'tm=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20221
static inline void alert_rule_20221(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20221;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20223
// Rule Name: FILE-IDENTIFY SMI file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20223
static inline int check_rule_20223(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .smi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20223
static inline void alert_rule_20223(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20223;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SMI file download request", 
                     sizeof("FILE-IDENTIFY SMI file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20260
// Rule Name: FILE-IDENTIFY Microsoft Client Agent Helper JAR file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20260
static inline int check_rule_20260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: _helper.jar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20260
static inline void alert_rule_20260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Client Agent Helper JAR file download request", 
                     sizeof("FILE-IDENTIFY Microsoft Client Agent Helper JAR file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20463
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20463
static inline int check_rule_20463(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['PK|03 04|', '!"|14 00 06 00|",within 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20463
static inline void alert_rule_20463(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20463;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20464
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20464
static inline int check_rule_20464(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK00PK|03 04|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20464
static inline void alert_rule_20464(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20464;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20465
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20465
static inline int check_rule_20465(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|01 02|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20465
static inline void alert_rule_20465(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20465;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20466
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20466
static inline int check_rule_20466(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|05 06|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20466
static inline void alert_rule_20466(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20466;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20467
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20467
static inline int check_rule_20467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|06 08|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20467
static inline void alert_rule_20467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20468
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20468
static inline int check_rule_20468(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|06 07|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20468
static inline void alert_rule_20468(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20468;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20469
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20469
static inline int check_rule_20469(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|06 06|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20469
static inline void alert_rule_20469(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20469;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20478
// Rule Name: FILE-IDENTIFY PNG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20478
static inline int check_rule_20478(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |89|PNG|0D 0A 1A 0A|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20478
static inline void alert_rule_20478(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20478;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PNG file magic detected", 
                     sizeof("FILE-IDENTIFY PNG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20480
// Rule Name: FILE-IDENTIFY JPEG file magic detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20480
static inline int check_rule_20480(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |FF D8 FF|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20480
static inline void alert_rule_20480(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20480;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detection", 
                     sizeof("FILE-IDENTIFY JPEG file magic detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20483
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20483
static inline int check_rule_20483(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |FF D8 FF E0|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20483
static inline void alert_rule_20483(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20483;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20486
// Rule Name: FILE-IDENTIFY RTF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20486
static inline int check_rule_20486(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|5C|rt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20486
static inline void alert_rule_20486(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20486;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RTF file magic detected", 
                     sizeof("FILE-IDENTIFY RTF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20494
// Rule Name: FILE-IDENTIFY PDF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20494
static inline int check_rule_20494(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PDF-",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20494
static inline void alert_rule_20494(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20494;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PDF file magic detected", 
                     sizeof("FILE-IDENTIFY PDF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20621
// Rule Name: FILE-IDENTIFY JAR file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20621
static inline int check_rule_20621(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20621
static inline void alert_rule_20621(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20621;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR file download request", 
                     sizeof("FILE-IDENTIFY JAR file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20850
// Rule Name: FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20850
static inline int check_rule_20850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.emf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20850
static inline void alert_rule_20850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected", 
                     sizeof("FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20851
// Rule Name: FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20851
static inline int check_rule_20851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.emf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20851
static inline void alert_rule_20851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected", 
                     sizeof("FILE-IDENTIFY Microsoft Windows EMF metafile file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20963
// Rule Name: FILE-IDENTIFY DIB file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20963
static inline int check_rule_20963(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .dib",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20963
static inline void alert_rule_20963(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20963;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY DIB file download request", 
                     sizeof("FILE-IDENTIFY DIB file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20964
// Rule Name: FILE-IDENTIFY SAMI file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20964
static inline int check_rule_20964(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .sami",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20964
static inline void alert_rule_20964(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20964;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SAMI file download request", 
                     sizeof("FILE-IDENTIFY SAMI file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20965
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20965
static inline int check_rule_20965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jpe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20965
static inline void alert_rule_20965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20966
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20966
static inline int check_rule_20966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jif",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20966
static inline void alert_rule_20966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 20967
// Rule Name: FILE-IDENTIFY JPEG file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 20967
static inline int check_rule_20967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .jfi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 20967
static inline void alert_rule_20967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 20967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file download request", 
                     sizeof("FILE-IDENTIFY JPEG file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21035
// Rule Name: FILE-IDENTIFY PDF file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21035
static inline int check_rule_21035(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.pdf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21035
static inline void alert_rule_21035(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21035;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PDF file attachment detected", 
                     sizeof("FILE-IDENTIFY PDF file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21036
// Rule Name: FILE-IDENTIFY PDF file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21036
static inline int check_rule_21036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.pdf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21036
static inline void alert_rule_21036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PDF file attachment detected", 
                     sizeof("FILE-IDENTIFY PDF file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21230
// Rule Name: MALWARE-CNC Win.Trojan.Betad variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21230
static inline int check_rule_21230(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/login.php",nocase', '|C9 97 A2 F3 7E 37 CB 7E 27|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21230
static inline void alert_rule_21230(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21230;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Betad variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Betad variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21246
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string DataCha0s
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: network-scan
// Generated from Snort rule parser output

// Check function for rule 21246
static inline int check_rule_21246(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|DataCha0s",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21246
static inline void alert_rule_21246(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21246;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string DataCha0s", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string DataCha0s"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21255
// Rule Name: MALWARE-OTHER known malicious FTP login banner - 0wns j0
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21255
static inline int check_rule_21255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 21
    if (tcph->source != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['220|20|",depth 4', '0wns j0",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21255
static inline void alert_rule_21255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER known malicious FTP login banner - 0wns j0", 
                     sizeof("MALWARE-OTHER known malicious FTP login banner - 0wns j0"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21256
// Rule Name: MALWARE-OTHER known malicious FTP quit banner - Goodbye happy r00ting
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21256
static inline int check_rule_21256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 21
    if (tcph->source != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: 221 Goodbye happy r00ting",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21256
static inline void alert_rule_21256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER known malicious FTP quit banner - Goodbye happy r00ting", 
                     sizeof("MALWARE-OTHER known malicious FTP quit banner - Goodbye happy r00ting"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21257
// Rule Name: MALWARE-CNC URI - known scanner tool muieblackcat
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: network-scan
// Generated from Snort rule parser output

// Check function for rule 21257
static inline int check_rule_21257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /muieblackcat",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21257
static inline void alert_rule_21257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC URI - known scanner tool muieblackcat", 
                     sizeof("MALWARE-CNC URI - known scanner tool muieblackcat"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21266
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string Morfeus Scanner
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: network-scan
// Generated from Snort rule parser output

// Check function for rule 21266
static inline int check_rule_21266(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User|2D|Agent|3A 20|Morfeus|20|Fucking|20|Scanner",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21266
static inline void alert_rule_21266(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21266;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string Morfeus Scanner", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string Morfeus Scanner"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21267
// Rule Name: POLICY-OTHER TRENDnet IP Camera anonymous access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 21267
static inline int check_rule_21267(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /anony/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21267
static inline void alert_rule_21267(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21267;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER TRENDnet IP Camera anonymous access attempt", 
                     sizeof("POLICY-OTHER TRENDnet IP Camera anonymous access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21282
// Rule Name: FILE-IDENTIFY XSL file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21282
static inline int check_rule_21282(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .xsl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21282
static inline void alert_rule_21282(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21282;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSL file download request", 
                     sizeof("FILE-IDENTIFY XSL file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21283
// Rule Name: FILE-IDENTIFY XSL file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21283
static inline int check_rule_21283(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xsl",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21283
static inline void alert_rule_21283(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21283;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSL file attachment detected", 
                     sizeof("FILE-IDENTIFY XSL file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21284
// Rule Name: FILE-IDENTIFY XSL file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21284
static inline int check_rule_21284(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xsl",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21284
static inline void alert_rule_21284(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21284;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSL file attachment detected", 
                     sizeof("FILE-IDENTIFY XSL file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21285
// Rule Name: FILE-IDENTIFY XSLT file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21285
static inline int check_rule_21285(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .xslt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21285
static inline void alert_rule_21285(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21285;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSLT file download request", 
                     sizeof("FILE-IDENTIFY XSLT file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21286
// Rule Name: FILE-IDENTIFY XSLT file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21286
static inline int check_rule_21286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xslt",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21286
static inline void alert_rule_21286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSLT file attachment detected", 
                     sizeof("FILE-IDENTIFY XSLT file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21287
// Rule Name: FILE-IDENTIFY XSLT file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21287
static inline int check_rule_21287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xslt",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21287
static inline void alert_rule_21287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XSLT file attachment detected", 
                     sizeof("FILE-IDENTIFY XSLT file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21288
// Rule Name: FILE-IDENTIFY XML download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21288
static inline int check_rule_21288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A|",nocase', 'text/xml",within 20,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21288
static inline void alert_rule_21288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML download detected", 
                     sizeof("FILE-IDENTIFY XML download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21327
// Rule Name: MALWARE-CNC User-Agent ASafaWeb Scan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: network-scan
// Generated from Snort rule parser output

// Check function for rule 21327
static inline int check_rule_21327(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| asafaweb.com",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21327
static inline void alert_rule_21327(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21327;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent ASafaWeb Scan", 
                     sizeof("MALWARE-CNC User-Agent ASafaWeb Scan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21375
// Rule Name: SERVER-WEBAPP Remote Execution Backdoor Attempt Against Horde
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 21375
static inline int check_rule_21375(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/services/javascript.php",fast_pattern,nocase', 'href=', 'file=open_calendar.js']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21375
static inline void alert_rule_21375(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21375;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Remote Execution Backdoor Attempt Against Horde", 
                     sizeof("SERVER-WEBAPP Remote Execution Backdoor Attempt Against Horde"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21410
// Rule Name: FILE-IDENTIFY paq8o file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21410
static inline int check_rule_21410(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .paq8o",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21410
static inline void alert_rule_21410(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21410;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY paq8o file download request", 
                     sizeof("FILE-IDENTIFY paq8o file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21411
// Rule Name: FILE-IDENTIFY paq8o file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21411
static inline int check_rule_21411(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.paq8o",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21411
static inline void alert_rule_21411(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21411;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY paq8o file attachment detected", 
                     sizeof("FILE-IDENTIFY paq8o file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21412
// Rule Name: FILE-IDENTIFY paq8o file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21412
static inline int check_rule_21412(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.paq8o",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21412
static inline void alert_rule_21412(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21412;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY paq8o file attachment detected", 
                     sizeof("FILE-IDENTIFY paq8o file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21417
// Rule Name: FILE-PDF hostile PDF associated with Laik exploit kit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21417
static inline int check_rule_21417(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['%PDF-1.', ') /CreationDate (D:20110405234628)>>",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21417
static inline void alert_rule_21417(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21417;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-PDF hostile PDF associated with Laik exploit kit", 
                     sizeof("FILE-PDF hostile PDF associated with Laik exploit kit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21438
// Rule Name: EXPLOIT-KIT Blackhole exploit kit JavaScript carat string splitting with hostile applet
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21438
static inline int check_rule_21438(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<html><body><applet|20|code=",nocase', '|20|archive=",distance 0,nocase', 'display|3A|none|3B|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21438
static inline void alert_rule_21438(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21438;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackhole exploit kit JavaScript carat string splitting with hostile applet", 
                     sizeof("EXPLOIT-KIT Blackhole exploit kit JavaScript carat string splitting with hostile applet"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21442
// Rule Name: MALWARE-CNC URI request for known malicious URI - base64 encoded
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21442
static inline int check_rule_21442(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET http|3A 2F 2F|",depth 11', 'clk=', '&bid=",distance 0', '&aid=",within 5,distance 40', '&sid=",distance 0', '&rd=",distance 0', '&x86=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21442
static inline void alert_rule_21442(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21442;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC URI request for known malicious URI - base64 encoded", 
                     sizeof("MALWARE-CNC URI request for known malicious URI - base64 encoded"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21444
// Rule Name: MALWARE-CNC Win.Trojan.TDSS variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21444
static inline int check_rule_21444(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| Mozilla/4.0 (compatible|3B 20|)",fast_pattern,nocase', 'HOST|3A|', '!"X-BlueCoat-Via",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21444
static inline void alert_rule_21444(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21444;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TDSS variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TDSS variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21475
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string core-project
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21475
static inline int check_rule_21475(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|core-project",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21475
static inline void alert_rule_21475(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21475;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string core-project", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string core-project"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21480
// Rule Name: FILE-IDENTIFY XML file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21480
static inline int check_rule_21480(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <xml>",depth 50,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21480
static inline void alert_rule_21480(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21480;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file magic detected", 
                     sizeof("FILE-IDENTIFY XML file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21498
// Rule Name: FILE-IDENTIFY XML file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21498
static inline int check_rule_21498(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <?xml",depth 50,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21498
static inline void alert_rule_21498(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21498;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file magic detected", 
                     sizeof("FILE-IDENTIFY XML file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21499
// Rule Name: FILE-IDENTIFY XML file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21499
static inline int check_rule_21499(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xml",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21499
static inline void alert_rule_21499(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21499;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file attachment detected", 
                     sizeof("FILE-IDENTIFY XML file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21500
// Rule Name: FILE-IDENTIFY XML file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21500
static inline int check_rule_21500(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xml",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21500
static inline void alert_rule_21500(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21500;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file attachment detected", 
                     sizeof("FILE-IDENTIFY XML file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21562
// Rule Name: MALWARE-CNC Win.Trojan.Bredolab variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21562
static inline int check_rule_21562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'User-Agent|3A 20|Mozilla/4.0|0D 0A|', 'smk=",depth 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21562
static inline void alert_rule_21562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bredolab variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bredolab variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21613
// Rule Name: FILE-IDENTIFY PNG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21613
static inline int check_rule_21613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.png",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21613
static inline void alert_rule_21613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PNG file attachment detected", 
                     sizeof("FILE-IDENTIFY PNG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21614
// Rule Name: FILE-IDENTIFY PNG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21614
static inline int check_rule_21614(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.png",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21614
static inline void alert_rule_21614(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21614;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PNG file attachment detected", 
                     sizeof("FILE-IDENTIFY PNG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21695
// Rule Name: FILE-IDENTIFY SMI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21695
static inline int check_rule_21695(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.smi",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21695
static inline void alert_rule_21695(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21695;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SMI file attachment detected", 
                     sizeof("FILE-IDENTIFY SMI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21696
// Rule Name: FILE-IDENTIFY SMI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21696
static inline int check_rule_21696(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.smi",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21696
static inline void alert_rule_21696(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21696;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SMI file attachment detected", 
                     sizeof("FILE-IDENTIFY SMI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21697
// Rule Name: FILE-IDENTIFY SAMI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21697
static inline int check_rule_21697(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.sami",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21697
static inline void alert_rule_21697(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21697;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SAMI file attachment detected", 
                     sizeof("FILE-IDENTIFY SAMI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21698
// Rule Name: FILE-IDENTIFY SAMI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21698
static inline int check_rule_21698(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.sami",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21698
static inline void alert_rule_21698(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21698;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY SAMI file attachment detected", 
                     sizeof("FILE-IDENTIFY SAMI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21724
// Rule Name: FILE-IDENTIFY ANI file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21724
static inline int check_rule_21724(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .ani",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21724
static inline void alert_rule_21724(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21724;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ANI file download request", 
                     sizeof("FILE-IDENTIFY ANI file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21725
// Rule Name: FILE-IDENTIFY ANI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21725
static inline int check_rule_21725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.ani",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21725
static inline void alert_rule_21725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ANI file attachment detected", 
                     sizeof("FILE-IDENTIFY ANI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21726
// Rule Name: FILE-IDENTIFY ANI file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21726
static inline int check_rule_21726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.ani",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21726
static inline void alert_rule_21726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ANI file attachment detected", 
                     sizeof("FILE-IDENTIFY ANI file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21727
// Rule Name: FILE-IDENTIFY ANI file magic detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21727
static inline int check_rule_21727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['RIFF",depth 4', 'ACON",within 4,distance 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21727
static inline void alert_rule_21727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ANI file magic detection", 
                     sizeof("FILE-IDENTIFY ANI file magic detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21728
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21728
static inline int check_rule_21728(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21728
static inline void alert_rule_21728(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21728;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21729
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21729
static inline int check_rule_21729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21729
static inline void alert_rule_21729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21730
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21730
static inline int check_rule_21730(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpeg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21730
static inline void alert_rule_21730(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21730;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21731
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21731
static inline int check_rule_21731(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpeg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21731
static inline void alert_rule_21731(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21731;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21732
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21732
static inline int check_rule_21732(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.pjpeg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21732
static inline void alert_rule_21732(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21732;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21733
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21733
static inline int check_rule_21733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.pjpeg",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21733
static inline void alert_rule_21733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21734
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21734
static inline int check_rule_21734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpe",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21734
static inline void alert_rule_21734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21735
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21735
static inline int check_rule_21735(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jpe",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21735
static inline void alert_rule_21735(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21735;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21736
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21736
static inline int check_rule_21736(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jif",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21736
static inline void alert_rule_21736(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21736;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21737
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21737
static inline int check_rule_21737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jif",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21737
static inline void alert_rule_21737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21738
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21738
static inline int check_rule_21738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jfi",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21738
static inline void alert_rule_21738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21739
// Rule Name: FILE-IDENTIFY JPG file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21739
static inline int check_rule_21739(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.jfi",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21739
static inline void alert_rule_21739(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21739;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPG file attachment detected", 
                     sizeof("FILE-IDENTIFY JPG file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21746
// Rule Name: FILE-IDENTIFY RTF file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21746
static inline int check_rule_21746(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.rtf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21746
static inline void alert_rule_21746(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21746;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RTF file attachment detected", 
                     sizeof("FILE-IDENTIFY RTF file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21747
// Rule Name: FILE-IDENTIFY RTF file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21747
static inline int check_rule_21747(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.rtf",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21747
static inline void alert_rule_21747(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21747;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RTF file attachment detected", 
                     sizeof("FILE-IDENTIFY RTF file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21818
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %ALLUSERSPROFILE%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21818
static inline int check_rule_21818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %ALLUSERSPROFILE%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21818
static inline void alert_rule_21818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %ALLUSERSPROFILE%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %ALLUSERSPROFILE%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21819
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMDATA%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21819
static inline int check_rule_21819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PROGRAMDATA%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21819
static inline void alert_rule_21819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMDATA%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMDATA%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21820
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %APPDATA%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21820
static inline int check_rule_21820(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %APPDATA%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21820
static inline void alert_rule_21820(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21820;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %APPDATA%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %APPDATA%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21821
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21821
static inline int check_rule_21821(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %COMMONPROGRAMFILES%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21821
static inline void alert_rule_21821(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21821;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21822
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES - x86%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21822
static inline int check_rule_21822(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %COMMONPROGRAMFILES|40|x86|41|%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21822
static inline void alert_rule_21822(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21822;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES - x86%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %COMMONPROGRAMFILES - x86%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21823
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %COMSPEC%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21823
static inline int check_rule_21823(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %COMSPEC%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21823
static inline void alert_rule_21823(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21823;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %COMSPEC%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %COMSPEC%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21824
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %HOMEDRIVE%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21824
static inline int check_rule_21824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %HOMEDRIVE%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21824
static inline void alert_rule_21824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %HOMEDRIVE%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %HOMEDRIVE%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21825
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %HOMEPATH%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21825
static inline int check_rule_21825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %HOMEPATH%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21825
static inline void alert_rule_21825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %HOMEPATH%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %HOMEPATH%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21826
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %LOCALAPPDATA%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21826
static inline int check_rule_21826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %LOCALAPPDATA%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21826
static inline void alert_rule_21826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %LOCALAPPDATA%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %LOCALAPPDATA%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21827
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21827
static inline int check_rule_21827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PROGRAMFILES%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21827
static inline void alert_rule_21827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21828
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES - X86%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21828
static inline int check_rule_21828(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PROGRAMFILES|40|X86|41|%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21828
static inline void alert_rule_21828(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21828;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES - X86%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %PROGRAMFILES - X86%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21829
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %SystemDrive%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21829
static inline int check_rule_21829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %SystemDrive%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21829
static inline void alert_rule_21829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %SystemDrive%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %SystemDrive%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21830
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %SystemRoot%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21830
static inline int check_rule_21830(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %SystemRoot%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21830
static inline void alert_rule_21830(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21830;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %SystemRoot%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %SystemRoot%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21831
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %TEMP%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21831
static inline int check_rule_21831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %TEMP%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21831
static inline void alert_rule_21831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %TEMP%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %TEMP%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21832
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %TMP%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21832
static inline int check_rule_21832(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %TMP%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21832
static inline void alert_rule_21832(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21832;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %TMP%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %TMP%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21833
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %USERDATA%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21833
static inline int check_rule_21833(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %USERDATA%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21833
static inline void alert_rule_21833(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21833;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %USERDATA%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %USERDATA%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21834
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %USERNAME%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21834
static inline int check_rule_21834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %USERNAME%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21834
static inline void alert_rule_21834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %USERNAME%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %USERNAME%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21835
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %USERPROFILE%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21835
static inline int check_rule_21835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %USERPROFILE%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21835
static inline void alert_rule_21835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %USERPROFILE%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %USERPROFILE%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21836
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %WINDIR%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21836
static inline int check_rule_21836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %WINDIR%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21836
static inline void alert_rule_21836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %WINDIR%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %WINDIR%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21837
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %PUBLIC%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21837
static inline int check_rule_21837(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PUBLIC%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21837
static inline void alert_rule_21837(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21837;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %PUBLIC%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %PUBLIC%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21838
// Rule Name: SERVER-WEBAPP System variable directory traversal attempt - %PSModulePath%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21838
static inline int check_rule_21838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PSModulePath%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21838
static inline void alert_rule_21838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable directory traversal attempt - %PSModulePath%", 
                     sizeof("SERVER-WEBAPP System variable directory traversal attempt - %PSModulePath%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21839
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %COMPUTERNAME%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21839
static inline int check_rule_21839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %COMPUTERNAME%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21839
static inline void alert_rule_21839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %COMPUTERNAME%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %COMPUTERNAME%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21840
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %LOGONSERVER%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21840
static inline int check_rule_21840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %LOGONSERVER%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21840
static inline void alert_rule_21840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %LOGONSERVER%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %LOGONSERVER%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21841
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %PATH%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21841
static inline int check_rule_21841(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PATH%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21841
static inline void alert_rule_21841(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21841;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %PATH%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %PATH%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21842
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %PATHEXT%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21842
static inline int check_rule_21842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PATHEXT%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21842
static inline void alert_rule_21842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %PATHEXT%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %PATHEXT%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21843
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %PROMPT%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21843
static inline int check_rule_21843(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %PROMPT%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21843
static inline void alert_rule_21843(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21843;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %PROMPT%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %PROMPT%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21844
// Rule Name: SERVER-WEBAPP System variable in URI attempt - %USERDOMAIN%
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 21844
static inline int check_rule_21844(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %USERDOMAIN%",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21844
static inline void alert_rule_21844(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21844;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP System variable in URI attempt - %USERDOMAIN%", 
                     sizeof("SERVER-WEBAPP System variable in URI attempt - %USERDOMAIN%"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21845
// Rule Name: MALWARE-OTHER TDS Sutra - redirect received
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21845
static inline int check_rule_21845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_0000=",fast_pattern', 'SL_', '_0000=",within 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21845
static inline void alert_rule_21845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER TDS Sutra - redirect received", 
                     sizeof("MALWARE-OTHER TDS Sutra - redirect received"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21846
// Rule Name: MALWARE-CNC TDS Sutra - request in.cgi
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21846
static inline int check_rule_21846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /in.cgi?
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21846
static inline void alert_rule_21846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC TDS Sutra - request in.cgi", 
                     sizeof("MALWARE-CNC TDS Sutra - request in.cgi"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21848
// Rule Name: MALWARE-OTHER TDS Sutra - page redirecting to a SutraTDS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21848
static inline int check_rule_21848(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/in.cgi?', '!"id=",within 3,nocase', '!"&",within 6', '!"=",within 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21848
static inline void alert_rule_21848(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21848;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER TDS Sutra - page redirecting to a SutraTDS", 
                     sizeof("MALWARE-OTHER TDS Sutra - page redirecting to a SutraTDS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21849
// Rule Name: MALWARE-OTHER TDS Sutra - HTTP header redirecting to a SutraTDS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21849
static inline int check_rule_21849(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /in.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21849
static inline void alert_rule_21849(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21849;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER TDS Sutra - HTTP header redirecting to a SutraTDS", 
                     sizeof("MALWARE-OTHER TDS Sutra - HTTP header redirecting to a SutraTDS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21850
// Rule Name: MALWARE-OTHER TDS Sutra - request hi.cgi
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21850
static inline int check_rule_21850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /hi.cgi
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21850
static inline void alert_rule_21850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER TDS Sutra - request hi.cgi", 
                     sizeof("MALWARE-OTHER TDS Sutra - request hi.cgi"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21851
// Rule Name: MALWARE-OTHER TDS Sutra - redirect received
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 21851
static inline int check_rule_21851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['302', '=_', '_|5C 3B| domain=",within 11,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21851
static inline void alert_rule_21851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER TDS Sutra - redirect received", 
                     sizeof("MALWARE-OTHER TDS Sutra - redirect received"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21856
// Rule Name: FILE-IDENTIFY ZIP file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21856
static inline int check_rule_21856(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.zip",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21856
static inline void alert_rule_21856(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21856;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ZIP file attachment detected", 
                     sizeof("FILE-IDENTIFY ZIP file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21857
// Rule Name: FILE-IDENTIFY ZIP file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21857
static inline int check_rule_21857(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.zip",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21857
static inline void alert_rule_21857(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21857;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ZIP file attachment detected", 
                     sizeof("FILE-IDENTIFY ZIP file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21908
// Rule Name: FILE-IDENTIFY Portable Executable file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21908
static inline int check_rule_21908(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.exe",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21908
static inline void alert_rule_21908(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21908;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable file attachment detected", 
                     sizeof("FILE-IDENTIFY Portable Executable file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21909
// Rule Name: FILE-IDENTIFY Portable Executable file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21909
static inline int check_rule_21909(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.exe",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21909
static inline void alert_rule_21909(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21909;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable file attachment detected", 
                     sizeof("FILE-IDENTIFY Portable Executable file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 21940
// Rule Name: FILE-IDENTIFY EMF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 21940
static inline int check_rule_21940(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|01 00 00 00|",depth 4', '|20|EMF",within 4,distance 36,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 21940
static inline void alert_rule_21940(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 21940;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY EMF file magic detected", 
                     sizeof("FILE-IDENTIFY EMF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22043
// Rule Name: FILE-IDENTIFY XM file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 22043
static inline int check_rule_22043(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .xm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22043
static inline void alert_rule_22043(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22043;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XM file download request", 
                     sizeof("FILE-IDENTIFY XM file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22044
// Rule Name: FILE-IDENTIFY XM file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 22044
static inline int check_rule_22044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xm",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22044
static inline void alert_rule_22044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XM file attachment detected", 
                     sizeof("FILE-IDENTIFY XM file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22045
// Rule Name: FILE-IDENTIFY XM file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 22045
static inline int check_rule_22045(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.xm",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22045
static inline void alert_rule_22045(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22045;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XM file attachment detected", 
                     sizeof("FILE-IDENTIFY XM file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22046
// Rule Name: FILE-IDENTIFY XM file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 22046
static inline int check_rule_22046(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Extended Module:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22046
static inline void alert_rule_22046(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22046;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XM file magic detected", 
                     sizeof("FILE-IDENTIFY XM file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22061
// Rule Name: MALWARE-OTHER Alureon - Malicious IFRAME load attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 22061
static inline int check_rule_22061(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: name=|5C 22|Twitter|5C 22| scrolling=|5C 22|auto|5C 22| frameborder=|5C 22|no|5C 22| align=|5C 22|center|5C 22| height = |5C 22|1px|5C 22| width = |5C 22|1px|5C 22|>",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22061
static inline void alert_rule_22061(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22061;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Alureon - Malicious IFRAME load attempt", 
                     sizeof("MALWARE-OTHER Alureon - Malicious IFRAME load attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 22063
// Rule Name: SERVER-WEBAPP PHP-CGI remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 22063
static inline int check_rule_22063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: auto_prepend_file
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 22063
static inline void alert_rule_22063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 22063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP PHP-CGI remote file include attempt", 
                     sizeof("SERVER-WEBAPP PHP-CGI remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23055
// Rule Name: PROTOCOL-FTP Multiple Products FTP MKD buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 23055
static inline int check_rule_23055(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MKD ",depth 4', '!"|0A|",within 75']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23055
static inline void alert_rule_23055(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23055;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-FTP Multiple Products FTP MKD buffer overflow attempt", 
                     sizeof("PROTOCOL-FTP Multiple Products FTP MKD buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23179
// Rule Name: INDICATOR-COMPROMISE script before DOCTYPE possible malicious redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 23179
static inline int check_rule_23179(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: </script><!DOCTYPE",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23179
static inline void alert_rule_23179(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23179;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE script before DOCTYPE possible malicious redirect attempt", 
                     sizeof("INDICATOR-COMPROMISE script before DOCTYPE possible malicious redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23481
// Rule Name: INDICATOR-OBFUSCATION hex escaped characters in setTimeout call
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 23481
static inline int check_rule_23481(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['setTimeout|28|",nocase', '|5C|x",within 10,nocase', '|5C|x",within 10,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23481
static inline void alert_rule_23481(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23481;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION hex escaped characters in setTimeout call", 
                     sizeof("INDICATOR-OBFUSCATION hex escaped characters in setTimeout call"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23482
// Rule Name: INDICATOR-OBFUSCATION hex escaped characters in addEventListener call
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 23482
static inline int check_rule_23482(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['addEventListener|28|",nocase', '|5C|x",within 10,nocase', '|5C|x",within 10,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23482
static inline void alert_rule_23482(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23482;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION hex escaped characters in addEventListener call", 
                     sizeof("INDICATOR-OBFUSCATION hex escaped characters in addEventListener call"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23621
// Rule Name: INDICATOR-OBFUSCATION known packer routine with secondary obfuscation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23621
static inline int check_rule_23621(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: eval(function(p,a,c,k,e,r)",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23621
static inline void alert_rule_23621(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23621;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION known packer routine with secondary obfuscation", 
                     sizeof("INDICATOR-OBFUSCATION known packer routine with secondary obfuscation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23636
// Rule Name: INDICATOR-OBFUSCATION JavaScript built-in function parseInt appears obfuscated - likely packer or encoder
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 23636
static inline int check_rule_23636(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |5B 27|parse|27 2B 27|Int|27 5D 28|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23636
static inline void alert_rule_23636(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23636;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION JavaScript built-in function parseInt appears obfuscated - likely packer or encoder", 
                     sizeof("INDICATOR-OBFUSCATION JavaScript built-in function parseInt appears obfuscated - likely packer or encoder"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23651
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23651
static inline int check_rule_23651(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PK|03 04|",depth 4', '!"|14 00 06 00|",within 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23651
static inline void alert_rule_23651(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23651;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23652
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23652
static inline int check_rule_23652(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK00PK|03 04|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23652
static inline void alert_rule_23652(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23652;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23653
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23653
static inline int check_rule_23653(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|01 02|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23653
static inline void alert_rule_23653(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23653;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23654
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23654
static inline int check_rule_23654(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|05 06|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23654
static inline void alert_rule_23654(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23654;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23655
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23655
static inline int check_rule_23655(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|06 08|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23655
static inline void alert_rule_23655(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23655;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23656
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23656
static inline int check_rule_23656(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|06 07|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23656
static inline void alert_rule_23656(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23656;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23657
// Rule Name: FILE-IDENTIFY JAR/ZIP file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23657
static inline int check_rule_23657(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|06 06|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23657
static inline void alert_rule_23657(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23657;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JAR/ZIP file magic detected", 
                     sizeof("FILE-IDENTIFY JAR/ZIP file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23664
// Rule Name: FILE-IDENTIFY PNG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23664
static inline int check_rule_23664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |89|PNG|0D 0A 1A 0A|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23664
static inline void alert_rule_23664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PNG file magic detected", 
                     sizeof("FILE-IDENTIFY PNG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23667
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23667
static inline int check_rule_23667(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF D8 FF E0|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23667
static inline void alert_rule_23667(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23667;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23670
// Rule Name: FILE-IDENTIFY RTF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23670
static inline int check_rule_23670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: {|5C|rt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23670
static inline void alert_rule_23670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY RTF file magic detected", 
                     sizeof("FILE-IDENTIFY RTF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23678
// Rule Name: FILE-IDENTIFY PDF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23678
static inline int check_rule_23678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: %PDF-",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23678
static inline void alert_rule_23678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY PDF file magic detected", 
                     sizeof("FILE-IDENTIFY PDF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23707
// Rule Name: FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23707
static inline int check_rule_23707(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|D0 CF 11 E0 A1 B1 1A E1|', '>|00 03 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23707
static inline void alert_rule_23707(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23707;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected", 
                     sizeof("FILE-IDENTIFY Microsoft Compound File Binary v3 file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23708
// Rule Name: FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23708
static inline int check_rule_23708(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|D0 CF 11 E0 A1 B1 1A E1|', '>|00 04 00|",within 4,distance 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23708
static inline void alert_rule_23708(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23708;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected", 
                     sizeof("FILE-IDENTIFY Microsoft Compound File Binary v4 file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23711
// Rule Name: FILE-IDENTIFY OLE Document file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23711
static inline int check_rule_23711(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |D0 CF 11 E0 A1 B1 1A E1|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23711
static inline void alert_rule_23711(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23711;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY OLE Document file magic detected", 
                     sizeof("FILE-IDENTIFY OLE Document file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23725
// Rule Name: FILE-IDENTIFY Portable Executable binary file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23725
static inline int check_rule_23725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['MZ', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23725
static inline void alert_rule_23725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable binary file magic detected", 
                     sizeof("FILE-IDENTIFY Portable Executable binary file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23758
// Rule Name: FILE-IDENTIFY XML file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23758
static inline int check_rule_23758(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: <xml>",depth 50,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23758
static inline void alert_rule_23758(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23758;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file magic detected", 
                     sizeof("FILE-IDENTIFY XML file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23759
// Rule Name: FILE-IDENTIFY XML file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23759
static inline int check_rule_23759(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: <?xml",depth 50,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23759
static inline void alert_rule_23759(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23759;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XML file magic detected", 
                     sizeof("FILE-IDENTIFY XML file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23766
// Rule Name: FILE-IDENTIFY EMF file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23766
static inline int check_rule_23766(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|01 00 00 00|",depth 4', '|20|EMF",within 4,distance 36,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23766
static inline void alert_rule_23766(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23766;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY EMF file magic detected", 
                     sizeof("FILE-IDENTIFY EMF file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 23773
// Rule Name: FILE-IDENTIFY XM file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 23773
static inline int check_rule_23773(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Extended Module:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 23773
static inline void alert_rule_23773(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 23773;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XM file magic detected", 
                     sizeof("FILE-IDENTIFY XM file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24015
// Rule Name: MALWARE-CNC Win.Trojan.Magania variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 24015
static inline int check_rule_24015(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['User-Agent: Google page|0D 0A|",fast_pattern,nocase', '.asp?', 'mac=",within 4', '&ver=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24015
static inline void alert_rule_24015(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24015;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Magania variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Magania variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24017
// Rule Name: MALWARE-OTHER Possible malicious redirect - rebots.php
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 24017
static inline int check_rule_24017(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /rebots.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24017
static inline void alert_rule_24017(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24017;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Possible malicious redirect - rebots.php", 
                     sizeof("MALWARE-OTHER Possible malicious redirect - rebots.php"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24225
// Rule Name: MALWARE-OTHER malicious redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 24225
static inline int check_rule_24225(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: a=YWZmaWQ9MDUyODg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24225
static inline void alert_rule_24225(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24225;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER malicious redirection attempt", 
                     sizeof("MALWARE-OTHER malicious redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24251
// Rule Name: OS-MOBILE Android/Fakelash.A!tr.spy trojan command and control channel traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 24251
static inline int check_rule_24251(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/data.php?action=",nocase', '&m=",distance 0,nocase', '&p=",distance 0,nocase', '&n=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24251
static inline void alert_rule_24251(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24251;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Android/Fakelash.A!tr.spy trojan command and control channel traffic", 
                     sizeof("OS-MOBILE Android/Fakelash.A!tr.spy trojan command and control channel traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24253
// Rule Name: INDICATOR-COMPROMISE IP only webpage redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 24253
static inline int check_rule_24253(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <html><head><meta http-equiv=|22|refresh
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24253
static inline void alert_rule_24253(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24253;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE IP only webpage redirect attempt", 
                     sizeof("INDICATOR-COMPROMISE IP only webpage redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24254
// Rule Name: INDICATOR-COMPROMISE IP only webpage redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 24254
static inline int check_rule_24254(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: document.location=
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24254
static inline void alert_rule_24254(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24254;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE IP only webpage redirect attempt", 
                     sizeof("INDICATOR-COMPROMISE IP only webpage redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24265
// Rule Name: MALWARE-OTHER Malicious UA detected on non-standard port
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 24265
static inline int check_rule_24265(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 84
    if (tcph->dest != htons(84))
        return 0;
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Mozilla/5.0 |28|Windows|3B| U|3B| MSIE 9.0|3B| Windows NT 9.0|3B| en-US|29|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24265
static inline void alert_rule_24265(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24265;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Malicious UA detected on non-standard port", 
                     sizeof("MALWARE-OTHER Malicious UA detected on non-standard port"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24359
// Rule Name: OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 24359
static inline int check_rule_24359(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|73 00 00 00 00|",depth 9,offset 4', '|00 00|",within 2,distance 13', '|FF|",within 1,distance 9', 'NTLMSSP|00 03 00 00 00|",within 100', '|00 00 00 00 48 00 00 00|",within 8,distance 24,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24359
static inline void alert_rule_24359(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24359;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24455
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 24455
static inline int check_rule_24455(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |FF D8 FF E1|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24455
static inline void alert_rule_24455(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24455;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24456
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 24456
static inline int check_rule_24456(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |FF D8 FF EE|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24456
static inline void alert_rule_24456(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24456;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24457
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 24457
static inline int check_rule_24457(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF D8 FF E1|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24457
static inline void alert_rule_24457(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24457;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24458
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 24458
static inline int check_rule_24458(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF D8 FF EE|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24458
static inline void alert_rule_24458(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24458;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24598
// Rule Name: POLICY-SPAM 1.usa.gov URL in email, possible spam redirect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 24598
static inline int check_rule_24598(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: http|3A 2F 2F|1.usa.gov
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24598
static inline void alert_rule_24598(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24598;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SPAM 1.usa.gov URL in email, possible spam redirect", 
                     sizeof("POLICY-SPAM 1.usa.gov URL in email, possible spam redirect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24885
// Rule Name: MALWARE-CNC Potential Banking Trojan Config File Download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 24885
static inline int check_rule_24885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|2F|Config|2E|txt",fast_pattern,nocase', 'Mozilla|2F|3|2E|0|20 28|compatible|3B 20|Indy|20|Library|29 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24885
static inline void alert_rule_24885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential Banking Trojan Config File Download", 
                     sizeof("MALWARE-CNC Potential Banking Trojan Config File Download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24886
// Rule Name: MALWARE-CNC Win.Trojan.Dorkbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 24886
static inline int check_rule_24886(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?ip=', '&os=",distance 0', '&name=",distance 0', '&id=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24886
static inline void alert_rule_24886(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24886;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dorkbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dorkbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 24972
// Rule Name: NETBIOS SMB Trans2 FIND_FIRST2 find file and directory info request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: protocol-command-decode
// Generated from Snort rule parser output

// Check function for rule 24972
static inline int check_rule_24972(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB2|00 00 00 00|",depth 9,offset 4', '|01 00|",within 2,distance 52', '|04 01|",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 24972
static inline void alert_rule_24972(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 24972;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS SMB Trans2 FIND_FIRST2 find file and directory info request", 
                     sizeof("NETBIOS SMB Trans2 FIND_FIRST2 find file and directory info request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25050
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25050
static inline int check_rule_25050(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php', '|3B 20|MSIE|20|', '!"|0D 0A|Accept|2D|Language|3A|', '!"|0D 0A|Referer|3A|', '!"|0D 0A|Cookie|3A|', '!"Content-Disposition', 'Content-Length: ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25050
static inline void alert_rule_25050(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25050;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25054
// Rule Name: MALWARE-CNC ZeroAccess Clickserver callback
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25054
static inline int check_rule_25054(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern:  HTTP/1.0|0D 0A|Host:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25054
static inline void alert_rule_25054(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25054;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC ZeroAccess Clickserver callback", 
                     sizeof("MALWARE-CNC ZeroAccess Clickserver callback"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25119
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - NewBrandTest
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25119
static inline int check_rule_25119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|NewBrandTest|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25119
static inline void alert_rule_25119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - NewBrandTest", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - NewBrandTest"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25224
// Rule Name: MALWARE-CNC Win.Trojan.ZeroAccess URI and Referer
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25224
static inline int check_rule_25224(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /s/?k=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25224
static inline void alert_rule_25224(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25224;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ZeroAccess URI and Referer", 
                     sizeof("MALWARE-CNC Win.Trojan.ZeroAccess URI and Referer"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25256
// Rule Name: MALWARE-CNC Win.Worm.Gamarue variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25256
static inline int check_rule_25256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/a/image.php",fast_pattern,nocase', 'User-Agent|3A 20|Mozilla/4.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25256
static inline void alert_rule_25256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Gamarue variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Gamarue variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25257
// Rule Name: MALWARE-CNC Win.Trojan.Skintrim variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25257
static inline int check_rule_25257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/bin/check.php?cv=', 'ThIs_Is_tHe_bouNdaRY_$",fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25257
static inline void alert_rule_25257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Skintrim variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Skintrim variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25258
// Rule Name: MALWARE-CNC Win.Trojan.Rombrast variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25258
static inline int check_rule_25258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/file.aspx?file=",fast_pattern,nocase', 'ksp/WS']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25258
static inline void alert_rule_25258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rombrast variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Rombrast variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25259
// Rule Name: MALWARE-CNC Win.Trojan.BancosBanload variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25259
static inline int check_rule_25259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.gif', '|0D 0A|Accept|2D|Encoding|3A 20|gzip|2C|deflateidentity|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25259
static inline void alert_rule_25259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BancosBanload variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BancosBanload variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25269
// Rule Name: MALWARE-CNC Win.Trojan.Buterat variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25269
static inline int check_rule_25269(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['From|3A|', 'Via|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25269
static inline void alert_rule_25269(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25269;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Buterat variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Buterat variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25271
// Rule Name: MALWARE-CNC Win.Trojan.Buzus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25271
static inline int check_rule_25271(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/default.aspx?ver=', '&uid=",distance 0', '|3B 20|MRA|20|5.10|20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25271
static inline void alert_rule_25271(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25271;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Buzus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Buzus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25277
// Rule Name: MALWARE-OTHER Request for a non-legit postal receipt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 25277
static inline int check_rule_25277(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .php?php=receipt",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25277
static inline void alert_rule_25277(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25277;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Request for a non-legit postal receipt", 
                     sizeof("MALWARE-OTHER Request for a non-legit postal receipt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25358
// Rule Name: APP-DETECT Acunetix web vulnerability scan attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25358
static inline int check_rule_25358(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Acunetix-",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25358
static inline void alert_rule_25358(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25358;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scan attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scan attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25359
// Rule Name: APP-DETECT Acunetix web vulnerability scanner probe attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25359
static inline int check_rule_25359(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /acunetix-wvs-test-for-some-inexistent-file",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25359
static inline void alert_rule_25359(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25359;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner probe attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner probe attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25360
// Rule Name: APP-DETECT Acunetix web vulnerability scanner authentication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25360
static inline int check_rule_25360(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: password=g00dPa$$w0rD",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25360
static inline void alert_rule_25360(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25360;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner authentication attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner authentication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25361
// Rule Name: APP-DETECT Acunetix web vulnerability scanner RFI attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25361
static inline int check_rule_25361(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: src=/testasp.vulnweb.com/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25361
static inline void alert_rule_25361(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25361;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner RFI attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner RFI attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25362
// Rule Name: APP-DETECT Acunetix web vulnerability scanner base64 XSS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25362
static inline int check_rule_25362(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PHNjcmlwdD",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25362
static inline void alert_rule_25362(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25362;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner base64 XSS attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner base64 XSS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25363
// Rule Name: APP-DETECT Acunetix web vulnerability scanner URI injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25363
static inline int check_rule_25363(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['http:/www.acunetix.com",fast_pattern,nocase', 'Acunetix-",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25363
static inline void alert_rule_25363(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25363;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner URI injection attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner URI injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25364
// Rule Name: APP-DETECT Acunetix web vulnerability scanner prompt XSS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25364
static inline int check_rule_25364(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <ScRiPt>prompt(",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25364
static inline void alert_rule_25364(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25364;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner prompt XSS attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner prompt XSS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25365
// Rule Name: APP-DETECT Acunetix web vulnerability scanner XSS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 25365
static inline int check_rule_25365(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: >=|5C|xa2",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25365
static inline void alert_rule_25365(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25365;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Acunetix web vulnerability scanner XSS attempt", 
                     sizeof("APP-DETECT Acunetix web vulnerability scanner XSS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25471
// Rule Name: MALWARE-CNC Pushdo Spiral Traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25471
static inline int check_rule_25471(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/?ptrxcz_",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25471
static inline void alert_rule_25471(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25471;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Pushdo Spiral Traffic", 
                     sizeof("MALWARE-CNC Pushdo Spiral Traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25503
// Rule Name: MALWARE-CNC Necurs Rootkit sba.cgi
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25503
static inline int check_rule_25503(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/cgi-bin/sba.cgi",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25503
static inline void alert_rule_25503(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25503;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Necurs Rootkit sba.cgi", 
                     sizeof("MALWARE-CNC Necurs Rootkit sba.cgi"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25504
// Rule Name: MALWARE-CNC Necurs Rootkit op.cgi
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25504
static inline int check_rule_25504(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/cgi-bin/op.cgi",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25504
static inline void alert_rule_25504(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25504;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Necurs Rootkit op.cgi", 
                     sizeof("MALWARE-CNC Necurs Rootkit op.cgi"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25511
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25511
static inline int check_rule_25511(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: lfstream|26|",depth 9,offset 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25511
static inline void alert_rule_25511(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25511;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25513
// Rule Name: FILE-IDENTIFY Portable Executable download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 25513
static inline int check_rule_25513(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['application/octet-stream",fast_pattern,nocase', 'MZ",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25513
static inline void alert_rule_25513(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25513;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable download detected", 
                     sizeof("FILE-IDENTIFY Portable Executable download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25514
// Rule Name: FILE-IDENTIFY Portable Executable download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 25514
static inline int check_rule_25514(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['application/x-msdos-program",fast_pattern,nocase', 'MZ",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25514
static inline void alert_rule_25514(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25514;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable download detected", 
                     sizeof("FILE-IDENTIFY Portable Executable download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25515
// Rule Name: FILE-IDENTIFY Portable Executable binary file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 25515
static inline int check_rule_25515(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['MZ', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25515
static inline void alert_rule_25515(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25515;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable binary file magic detected", 
                     sizeof("FILE-IDENTIFY Portable Executable binary file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25518
// Rule Name: OS-MOBILE Apple iPod User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25518
static inline int check_rule_25518(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'iPod",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25518
static inline void alert_rule_25518(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25518;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Apple iPod User-Agent detected", 
                     sizeof("OS-MOBILE Apple iPod User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25519
// Rule Name: OS-MOBILE Apple iPad User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25519
static inline int check_rule_25519(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'iPad",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25519
static inline void alert_rule_25519(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25519;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Apple iPad User-Agent detected", 
                     sizeof("OS-MOBILE Apple iPad User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25520
// Rule Name: OS-MOBILE Apple iPhone User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25520
static inline int check_rule_25520(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'iPhone",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25520
static inline void alert_rule_25520(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25520;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Apple iPhone User-Agent detected", 
                     sizeof("OS-MOBILE Apple iPhone User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25521
// Rule Name: OS-MOBILE Android User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25521
static inline int check_rule_25521(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'android",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25521
static inline void alert_rule_25521(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25521;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Android User-Agent detected", 
                     sizeof("OS-MOBILE Android User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25522
// Rule Name: OS-MOBILE Nokia User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25522
static inline int check_rule_25522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'nokia",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25522
static inline void alert_rule_25522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Nokia User-Agent detected", 
                     sizeof("OS-MOBILE Nokia User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25523
// Rule Name: OS-MOBILE Samsung User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25523
static inline int check_rule_25523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'Samsung",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25523
static inline void alert_rule_25523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Samsung User-Agent detected", 
                     sizeof("OS-MOBILE Samsung User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25524
// Rule Name: OS-MOBILE Kindle User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25524
static inline int check_rule_25524(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'kindle",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25524
static inline void alert_rule_25524(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25524;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-MOBILE Kindle User-Agent detected", 
                     sizeof("OS-MOBILE Kindle User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25525
// Rule Name: OS-OTHER Nintendo User-Agent detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25525
static inline int check_rule_25525(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'nintendo",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25525
static inline void alert_rule_25525(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25525;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Nintendo User-Agent detected", 
                     sizeof("OS-OTHER Nintendo User-Agent detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25577
// Rule Name: MALWARE-CNC Win.Rootkit.Necurs possible URI with encrypted POST
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25577
static inline int check_rule_25577(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/admin/host.php",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25577
static inline void alert_rule_25577(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25577;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Rootkit.Necurs possible URI with encrypted POST", 
                     sizeof("MALWARE-CNC Win.Rootkit.Necurs possible URI with encrypted POST"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25578
// Rule Name: MALWARE-OTHER Fake postal receipt HTTP Response phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25578
static inline int check_rule_25578(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|filename=PostalReceipt.zip|0D 0A|",fast_pattern,nocase', 'PostalReceipt.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25578
static inline void alert_rule_25578(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25578;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake postal receipt HTTP Response phishing attack", 
                     sizeof("MALWARE-OTHER Fake postal receipt HTTP Response phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25579
// Rule Name: MALWARE-OTHER Fake bookinginfo HTTP Response phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25579
static inline int check_rule_25579(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|filename=BookingInfo.zip|0D 0A|",fast_pattern,nocase', 'BookingInfo.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25579
static inline void alert_rule_25579(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25579;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake bookinginfo HTTP Response phishing attack", 
                     sizeof("MALWARE-OTHER Fake bookinginfo HTTP Response phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25580
// Rule Name: MALWARE-OTHER Fake bookingdetails HTTP Response phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25580
static inline int check_rule_25580(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|filename=BookingDetails.zip|0D 0A|",fast_pattern,nocase', 'BookingDetails.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25580
static inline void alert_rule_25580(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25580;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake bookingdetails HTTP Response phishing attack", 
                     sizeof("MALWARE-OTHER Fake bookingdetails HTTP Response phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25627
// Rule Name: MALWARE-CNC Win.Trojan.Reventon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25627
static inline int check_rule_25627(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |9A 02 00 00|",depth 4,fast_pattern
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25627
static inline void alert_rule_25627(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25627;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Reventon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Reventon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25652
// Rule Name: MALWARE-CNC Win.Trojan.Kryptic variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25652
static inline int check_rule_25652(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept-Language: en-us|3B 0D 0A|', 'wok5VLG.6",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25652
static inline void alert_rule_25652(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25652;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kryptic variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kryptic variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25660
// Rule Name: MALWARE-CNC Win.Trojan.Medfos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25660
static inline int check_rule_25660(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/js/disable.js?type=",fast_pattern,nocase', 'Accept|3A 20|application/javascript|2C 20 2A 2F 2A 3B|q=0.8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25660
static inline void alert_rule_25660(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25660;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Medfos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Medfos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25675
// Rule Name: MALWARE-CNC Win.Trojan.Fakeavlock variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25675
static inline int check_rule_25675(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Mozilla/5.0 (Windows|3B| U|3B| MSIE 9.0|3B| Windows NT 9.0|3B| en-US)|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25675
static inline void alert_rule_25675(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25675;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fakeavlock variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fakeavlock variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25765
// Rule Name: MALWARE-CNC Trojan Agent YEH variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25765
static inline int check_rule_25765(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |29 3B 28|b|3A|3790|3B|c|3A|INT|2D|6760|3B|l|3A|09|29 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25765
static inline void alert_rule_25765(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25765;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Trojan Agent YEH variant outbound connection", 
                     sizeof("MALWARE-CNC Trojan Agent YEH variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25766
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25766
static inline int check_rule_25766(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cmd.php?cmd=', 'arq=",distance 0', 'cmd2=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25766
static inline void alert_rule_25766(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25766;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25807
// Rule Name: MALWARE-CNC Win.Trojan.Urausy Botnet variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25807
static inline int check_rule_25807(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|29 20|Chrome|2F|', '!"|0A|Accept-Encoding|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25807
static inline void alert_rule_25807(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25807;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Urausy Botnet variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Urausy Botnet variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25829
// Rule Name: MALWARE-CNC Trojan Banker FTC variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25829
static inline int check_rule_25829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/listas/out/si.php",fast_pattern,nocase', 'HTTP/1.0|0D 0A|",depth 10,offset 24']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25829
static inline void alert_rule_25829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Trojan Banker FTC variant outbound connection", 
                     sizeof("MALWARE-CNC Trojan Banker FTC variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25854
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25854
static inline int check_rule_25854(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|2F|', '!"|0A|Referer|3A|', '!"|0A|Cookie|3A|', '|3B 20|MSIE|20|7.0|3B 20|', '|2E|info|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25854
static inline void alert_rule_25854(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25854;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25947
// Rule Name: APP-DETECT Ammyy remote access tool
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 25947
static inline int check_rule_25947(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['POST', '|0A|Host|3A 20|rl.ammyy.com|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25947
static inline void alert_rule_25947(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25947;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Ammyy remote access tool", 
                     sizeof("APP-DETECT Ammyy remote access tool"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25948
// Rule Name: EXPLOIT-KIT redirection to driveby download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25948
static inline int check_rule_25948(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Home/index.php|22| width=1 height=1 scrolling=no></iframe>",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25948
static inline void alert_rule_25948(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25948;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT redirection to driveby download", 
                     sizeof("EXPLOIT-KIT redirection to driveby download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 25949
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy outbound data connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 25949
static inline int check_rule_25949(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php', 'User|2D|Agent|3A 20|Mozilla|2F|3.0|20 28|compatible|3B 20|Indy Library|29 0D 0A|', 'form-data|3B| name=|22|userfile|22 3B| filename=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 25949
static inline void alert_rule_25949(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 25949;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy outbound data connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy outbound data connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26020
// Rule Name: EXPLOIT-KIT Sibhost exploit kit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26020
static inline int check_rule_26020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: yoO4TAbn2tpl5DltCfASJIZ2spEJPLSn",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26020
static inline void alert_rule_26020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Sibhost exploit kit", 
                     sizeof("EXPLOIT-KIT Sibhost exploit kit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26023
// Rule Name: MALWARE-CNC Win.Trojan.Zbot variant in.php outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26023
static inline int check_rule_26023(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/in.php', '.ru|0D 0A|User-Agent|3A 20|Mozilla/4.0|0D 0A|",fast_pattern,nocase', '|0A|Content-Length|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26023
static inline void alert_rule_26023(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26023;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot variant in.php outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot variant in.php outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26024
// Rule Name: MALWARE-CNC Win.Trojan.Wecod variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26024
static inline int check_rule_26024(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /b/n/winrar/tudo.rar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26024
static inline void alert_rule_26024(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26024;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Wecod variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Wecod variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26057
// Rule Name: FILE-IDENTIFY ZIP file download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26057
static inline int check_rule_26057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|03 04 14 00 06 00|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26057
static inline void alert_rule_26057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ZIP file download detected", 
                     sizeof("FILE-IDENTIFY ZIP file download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26058
// Rule Name: FILE-IDENTIFY ZIP file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26058
static inline int check_rule_26058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|03 04 14 00 06 00|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26058
static inline void alert_rule_26058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY ZIP file attachment detected", 
                     sizeof("FILE-IDENTIFY ZIP file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26075
// Rule Name: MALWARE-CNC Bancos variant outbound connection SQL query POST data
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26075
static inline int check_rule_26075(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: a=select CAMPO from PAGINA where CODIGO = ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26075
static inline void alert_rule_26075(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26075;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Bancos variant outbound connection SQL query POST data", 
                     sizeof("MALWARE-CNC Bancos variant outbound connection SQL query POST data"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26106
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26106
static inline int check_rule_26106(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php', '|3B 20|MSIE|20|', '|0D 0A|Accept|2D|Encoding|3A 20|identity|0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26106
static inline void alert_rule_26106(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26106;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26203
// Rule Name: MALWARE-CNC Win.Trojan.Gupd variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26203
static inline int check_rule_26203(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['cstype=",depth 7', '&authname=",within 48,distance 1', '&authpass=",within 48,distance 1', '&hostname=",within 48,distance 1', '&ostype=",within 256,distance 1', '&macaddr=",within 64,distance 16', '&owner=",within 48,distance 17']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26203
static inline void alert_rule_26203(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26203;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gupd variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gupd variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26211
// Rule Name: MALWARE-CNC Win.Trojan.Eldorado variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26211
static inline int check_rule_26211(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/pid/pid.txt",fast_pattern,nocase', '(compatible|3B 20|Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26211
static inline void alert_rule_26211(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26211;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Eldorado variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Eldorado variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26212
// Rule Name: MALWARE-CNC Win.Trojan.Proxyier variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26212
static inline int check_rule_26212(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET /?",depth 6', 'HTTP/1.1|0D 0A|Host|3A 20|update|2E|",distance 0', '0b8pre|0D 0A|",fast_pattern,nocase', '!"|0A|Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26212
static inline void alert_rule_26212(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26212;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Proxyier variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Proxyier variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26251
// Rule Name: FILE-IDENTIFY JPEG file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26251
static inline int check_rule_26251(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |00 10|JFIF
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26251
static inline void alert_rule_26251(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26251;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detected", 
                     sizeof("FILE-IDENTIFY JPEG file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26261
// Rule Name: MALWARE-OTHER Fake postal receipt HTTP Response phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26261
static inline int check_rule_26261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|filename=Postal-Receipt.zip|0D 0A|",fast_pattern,nocase', 'Postal-Receipt.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26261
static inline void alert_rule_26261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake postal receipt HTTP Response phishing attack", 
                     sizeof("MALWARE-OTHER Fake postal receipt HTTP Response phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26264
// Rule Name: MALWARE-CNC Dapato banking Trojan variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26264
static inline int check_rule_26264(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pics/_vti_cnf/00.inf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26264
static inline void alert_rule_26264(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26264;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Dapato banking Trojan variant outbound connection", 
                     sizeof("MALWARE-CNC Dapato banking Trojan variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26275
// Rule Name: SERVER-WEBAPP DD-WRT httpd cgi-bin remote command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 26275
static inline int check_rule_26275(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/",depth 10,nocase', '${IFS}",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26275
static inline void alert_rule_26275(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26275;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DD-WRT httpd cgi-bin remote command execution attempt", 
                     sizeof("SERVER-WEBAPP DD-WRT httpd cgi-bin remote command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26276
// Rule Name: SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 26276
static inline int check_rule_26276(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/apply.cgi",fast_pattern,nocase', 'submit_button', '%0",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26276
static inline void alert_rule_26276(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26276;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt", 
                     sizeof("SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26277
// Rule Name: SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 26277
static inline int check_rule_26277(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/apply.cgi",fast_pattern,nocase', 'submit_button', '%0",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26277
static inline void alert_rule_26277(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26277;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt", 
                     sizeof("SERVER-WEBAPP Linksys E1500/E2500 apply.cgi submit_button page redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26278
// Rule Name: SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 26278
static inline int check_rule_26278(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/apply.cgi",fast_pattern,nocase', '!"Authorization:",nocase', 'action=Apply",nocase', 'PasswdModify=1",nocase', 'http_passwd=",nocase', 'http_passwdConfirm=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26278
static inline void alert_rule_26278(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26278;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt", 
                     sizeof("SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26279
// Rule Name: SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 26279
static inline int check_rule_26279(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/apply.cgi",fast_pattern,nocase', '!"Authorization:",nocase', 'action=Apply",nocase', 'PasswdModify=1",nocase', 'http_passwd=",nocase', 'http_passwdConfirm=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26279
static inline void alert_rule_26279(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26279;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt", 
                     sizeof("SERVER-WEBAPP Linksys E1500/E2500 apply.cgi unauthenticated password reset attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26286
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - search.dnssearch.org
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26286
static inline int check_rule_26286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| search.dnssearch.org|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26286
static inline void alert_rule_26286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - search.dnssearch.org", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - search.dnssearch.org"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26287
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - search.namequery.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26287
static inline int check_rule_26287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| search.namequery.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26287
static inline void alert_rule_26287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - search.namequery.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - search.namequery.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26288
// Rule Name: MALWARE-CNC Brontok Worm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26288
static inline int check_rule_26288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Brontok.A8 Browser|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26288
static inline void alert_rule_26288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Brontok Worm variant outbound connection", 
                     sizeof("MALWARE-CNC Brontok Worm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26289
// Rule Name: MALWARE-CNC Daws Trojan Outbound Plaintext over SSL Port
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26289
static inline int check_rule_26289(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['POST",depth 4', '|0D 0A|Content|2D|Disposition|3A 20|form|2D|data|3B 20|name|3D 22|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26289
static inline void alert_rule_26289(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26289;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Daws Trojan Outbound Plaintext over SSL Port", 
                     sizeof("MALWARE-CNC Daws Trojan Outbound Plaintext over SSL Port"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26319
// Rule Name: MALWARE-CNC file path used as User-Agent - potential Trojan
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26319
static inline int check_rule_26319(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|C:|5C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26319
static inline void alert_rule_26319(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26319;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC file path used as User-Agent - potential Trojan", 
                     sizeof("MALWARE-CNC file path used as User-Agent - potential Trojan"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26325
// Rule Name: MALWARE-CNC Win.Trojan.Scar variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26325
static inline int check_rule_26325(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?mac=",fast_pattern,nocase', '|0D 0A|Accept-Language|3A 20|ko|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26325
static inline void alert_rule_26325(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26325;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Scar variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Scar variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26327
// Rule Name: MALWARE-CNC OSX.Trojan.Flashfake variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26327
static inline int check_rule_26327(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|sv|3A|', '|3B 20|id|3A|",within 5,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26327
static inline void alert_rule_26327(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26327;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC OSX.Trojan.Flashfake variant outbound connection", 
                     sizeof("MALWARE-CNC OSX.Trojan.Flashfake variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26335
// Rule Name: MALWARE-CNC FBI Ransom Trojan variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26335
static inline int check_rule_26335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /nosignal.jpg?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26335
static inline void alert_rule_26335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC FBI Ransom Trojan variant outbound connection", 
                     sizeof("MALWARE-CNC FBI Ransom Trojan variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26353
// Rule Name: INDICATOR-COMPROMISE IP address check to dyndns.org detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26353
static inline int check_rule_26353(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Host|3A 20|checkip.dyndns.org",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26353
static inline void alert_rule_26353(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26353;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE IP address check to dyndns.org detected", 
                     sizeof("INDICATOR-COMPROMISE IP address check to dyndns.org detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26370
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection - ksa.txt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26370
static inline int check_rule_26370(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ksa.txt",fast_pattern,nocase', 'User-Agent|3A 20|Mozilla/3.0 (compatible|3B| Indy Library)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26370
static inline void alert_rule_26370(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26370;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection - ksa.txt", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection - ksa.txt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26371
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection - op POST
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26371
static inline int check_rule_26371(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['op=",depth 3', '&nmpc=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26371
static inline void alert_rule_26371(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26371;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection - op POST", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection - op POST"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26380
// Rule Name: MALWARE-OTHER UTF-8 BOM in zip file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26380
static inline int check_rule_26380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |EF BB BF 50 4B 03 04|",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26380
static inline void alert_rule_26380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER UTF-8 BOM in zip file attachment detected", 
                     sizeof("MALWARE-OTHER UTF-8 BOM in zip file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26381
// Rule Name: MALWARE-OTHER UTF-8 BOM in zip file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26381
static inline int check_rule_26381(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |EF BB BF 50 4B 03 04|",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26381
static inline void alert_rule_26381(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26381;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER UTF-8 BOM in zip file attachment detected", 
                     sizeof("MALWARE-OTHER UTF-8 BOM in zip file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26382
// Rule Name: MALWARE-OTHER UTF-8 BOM in zip file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26382
static inline int check_rule_26382(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |EF BB BF 50 4B 03 04|",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26382
static inline void alert_rule_26382(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26382;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER UTF-8 BOM in zip file attachment detected", 
                     sizeof("MALWARE-OTHER UTF-8 BOM in zip file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26395
// Rule Name: APP-DETECT Ufasoft bitcoin miner possible data upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 26395
static inline int check_rule_26395(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Ufasoft",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26395
static inline void alert_rule_26395(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26395;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Ufasoft bitcoin miner possible data upload", 
                     sizeof("APP-DETECT Ufasoft bitcoin miner possible data upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26398
// Rule Name: MALWARE-CNC Win.Trojan.Gamarue variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26398
static inline int check_rule_26398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'panel1/gate.php', ' HTTP/1.1|0D 0A|Cache-Control: no-cache|0D 0A|Connection|3A|",fast_pattern,nocase', '+",depth 15']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26398
static inline void alert_rule_26398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gamarue variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gamarue variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26410
// Rule Name: INDICATOR-COMPROMISE IP address check to j.maxmind.com detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26410
static inline int check_rule_26410(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/app/geoip.js', 'Host|3A 20|j.maxmind.com",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26410
static inline void alert_rule_26410(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26410;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE IP address check to j.maxmind.com detected", 
                     sizeof("INDICATOR-COMPROMISE IP address check to j.maxmind.com detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26411
// Rule Name: MALWARE-OTHER Win.Worm.Dorkbot folder snkb0ptz creation attempt SMB
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26411
static inline int check_rule_26411(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: |73 00 6E 00 6B 00 62 00 30 00 70 00 74 00 7A 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26411
static inline void alert_rule_26411(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26411;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Worm.Dorkbot folder snkb0ptz creation attempt SMB", 
                     sizeof("MALWARE-OTHER Win.Worm.Dorkbot folder snkb0ptz creation attempt SMB"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26412
// Rule Name: MALWARE-OTHER Win.Worm.Dorkbot executable snkb0ptz.exe creation attempt SMB
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26412
static inline int check_rule_26412(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|73 00 6E 00 6B 00 62 00 30 00 70 00 74 00 7A 00|",fast_pattern,nocase', '.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26412
static inline void alert_rule_26412(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26412;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Worm.Dorkbot executable snkb0ptz.exe creation attempt SMB", 
                     sizeof("MALWARE-OTHER Win.Worm.Dorkbot executable snkb0ptz.exe creation attempt SMB"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26413
// Rule Name: MALWARE-OTHER Win.Worm.Dorkbot Desktop.ini snkb0ptz.exe creation attempt SMB
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26413
static inline int check_rule_26413(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|73 00 6E 00 6B 00 62 00 30 00 70 00 74 00 7A 00|', '|5C|",within 1', '|00 44 00 65 00 73 00 6B 00 74 00 6F 00 70 00 2E 00 69 00 6E 00 69 00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26413
static inline void alert_rule_26413(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26413;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Worm.Dorkbot Desktop.ini snkb0ptz.exe creation attempt SMB", 
                     sizeof("MALWARE-OTHER Win.Worm.Dorkbot Desktop.ini snkb0ptz.exe creation attempt SMB"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26467
// Rule Name: MALWARE-CNC Win.Trojan.Magic variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26467
static inline int check_rule_26467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: some_magic_code1",depth 36
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26467
static inline void alert_rule_26467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Magic variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Magic variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26468
// Rule Name: SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 26468
static inline int check_rule_26468(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['blobheadername2=Location",fast_pattern,nocase', 'blobheadervalue2=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26468
static inline void alert_rule_26468(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26468;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt", 
                     sizeof("SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26469
// Rule Name: SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 26469
static inline int check_rule_26469(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['blobheadername2=Refresh",fast_pattern,nocase', 'blobheadervalue2=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26469
static inline void alert_rule_26469(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26469;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt", 
                     sizeof("SERVER-ORACLE Oracle WebCenter FatWire Satellite Server header injection on blobheadername2 attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26470
// Rule Name: MALWARE-OTHER Win.Trojan.Zeus Spam 2013 dated zip/exe HTTP Response - potential malware download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26470
static inline int check_rule_26470(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['-2013.zip|0D 0A|",fast_pattern,nocase', '-2013.zip|0D 0A|', '-",within 1,distance -14', '-2013.exe', '-",within 1,distance -14']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26470
static inline void alert_rule_26470(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26470;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Zeus Spam 2013 dated zip/exe HTTP Response - potential malware download", 
                     sizeof("MALWARE-OTHER Win.Trojan.Zeus Spam 2013 dated zip/exe HTTP Response - potential malware download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26480
// Rule Name: MALWARE-CNC Win.Trojan.Zbot fake PNG config file download without User-Agent
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26480
static inline int check_rule_26480(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept: application/xml,application/xhtml+xml,text/html|3B|q=0.9,text/plain|3B|q=0.8,image/png,*/*|3B|q=0.5|0D 0A|",fast_pattern,nocase', '!"User-Agent:",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26480
static inline void alert_rule_26480(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26480;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot fake PNG config file download without User-Agent", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot fake PNG config file download without User-Agent"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26482
// Rule Name: MALWARE-CNC Unknown Thinner Encrypted POST botnet C&C
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26482
static inline int check_rule_26482(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /thinner/thumb?img=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26482
static inline void alert_rule_26482(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26482;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unknown Thinner Encrypted POST botnet C&C", 
                     sizeof("MALWARE-CNC Unknown Thinner Encrypted POST botnet C&C"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26483
// Rule Name: SERVER-WEBAPP JavaScript tag in User-Agent field possible XSS attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 26483
static inline int check_rule_26483(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| <SCRIPT>",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26483
static inline void alert_rule_26483(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26483;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP JavaScript tag in User-Agent field possible XSS attempt", 
                     sizeof("SERVER-WEBAPP JavaScript tag in User-Agent field possible XSS attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26522
// Rule Name: MALWARE-CNC User-Agent known malicious user agent NOKIAN95/WEB
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26522
static inline int check_rule_26522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| NOKIAN95|2F|WEB",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26522
static inline void alert_rule_26522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent NOKIAN95/WEB", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent NOKIAN95/WEB"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26526
// Rule Name: EXPLOIT-KIT Portable Executable downloaded with bad DOS stub
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26526
static inline int check_rule_26526(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['MZ",depth 2', '|2F 2A 14 20|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26526
static inline void alert_rule_26526(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26526;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Portable Executable downloaded with bad DOS stub", 
                     sizeof("EXPLOIT-KIT Portable Executable downloaded with bad DOS stub"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26528
// Rule Name: INDICATOR-COMPROMISE Unix.Backdoor.Cdorked redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26528
static inline int check_rule_26528(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['0aW1lP",fast_pattern', '/index.php?",distance -50', 'time=', '&src=",distance 0', '&surl=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26528
static inline void alert_rule_26528(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26528;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Unix.Backdoor.Cdorked redirect attempt", 
                     sizeof("INDICATOR-COMPROMISE Unix.Backdoor.Cdorked redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26533
// Rule Name: MALWARE-CNC Unknown malware - Incorrect headers - Referer HTTP/1.0
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26533
static inline int check_rule_26533(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Referer: HTTP/1.0|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26533
static inline void alert_rule_26533(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26533;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unknown malware - Incorrect headers - Referer HTTP/1.0", 
                     sizeof("MALWARE-CNC Unknown malware - Incorrect headers - Referer HTTP/1.0"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26534
// Rule Name: EXPLOIT-KIT Stamp exploit kit portable executable download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26534
static inline int check_rule_26534(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/elections.php?",fast_pattern,nocase', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26534
static inline void alert_rule_26534(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26534;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Stamp exploit kit portable executable download", 
                     sizeof("EXPLOIT-KIT Stamp exploit kit portable executable download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26553
// Rule Name: PUA-ADWARE Win.Adware.BProtector browser hijacker dll list download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26553
static inline int check_rule_26553(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['GET', '/builds/",nocase', 'fflists.txt",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26553
static inline void alert_rule_26553(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26553;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.BProtector browser hijacker dll list download attempt", 
                     sizeof("PUA-ADWARE Win.Adware.BProtector browser hijacker dll list download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26558
// Rule Name: MALWARE-CNC User-Agent known Malicious user agent Brutus AET
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26558
static inline int check_rule_26558(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Mozilla|2F|3.0 |28|Compatible|29 3B|Brutus|2F|AET",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26558
static inline void alert_rule_26558(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26558;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known Malicious user agent Brutus AET", 
                     sizeof("MALWARE-CNC User-Agent known Malicious user agent Brutus AET"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26560
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection - getcomando POST data
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26560
static inline int check_rule_26560(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: tipo=getcomando&",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26560
static inline void alert_rule_26560(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26560;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection - getcomando POST data", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection - getcomando POST data"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26561
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26561
static inline int check_rule_26561(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&sk1=",fast_pattern,nocase', 'bn1=",depth 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26561
static inline void alert_rule_26561(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26561;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26562
// Rule Name: EXPLOIT-KIT Nuclear exploit kit Spoofed Host Header .com- requests
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26562
static inline int check_rule_26562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.com-', '|0D 0A|Accept|3A 20|text/html, image/gif, image/jpeg, *|3B| q=.2, */*|3B| q=.2|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26562
static inline void alert_rule_26562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Nuclear exploit kit Spoofed Host Header .com- requests", 
                     sizeof("EXPLOIT-KIT Nuclear exploit kit Spoofed Host Header .com- requests"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26563
// Rule Name: MALWARE-CNC Harakit botnet traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26563
static inline int check_rule_26563(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['sousi.extasix.com|0D 0A|",fast_pattern,nocase', '/genst.htm']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26563
static inline void alert_rule_26563(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26563;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Harakit botnet traffic", 
                     sizeof("MALWARE-CNC Harakit botnet traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26576
// Rule Name: MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26576
static inline int check_rule_26576(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wp-content",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26576
static inline void alert_rule_26576(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26576;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt", 
                     sizeof("MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26577
// Rule Name: MALWARE-CNC User-Agent known malicious user agent Opera 10
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26577
static inline int check_rule_26577(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Opera/10|20|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26577
static inline void alert_rule_26577(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26577;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent Opera 10", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent Opera 10"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26578
// Rule Name: MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26578
static inline int check_rule_26578(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/images/m.php?id=",fast_pattern,nocase', '|3B 20|MSIE 6.0|3B 20|', '!"Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26578
static inline void alert_rule_26578(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26578;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent", 
                     sizeof("MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26579
// Rule Name: MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26579
static inline int check_rule_26579(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ccbill/m.php?id=",fast_pattern,nocase', '|3B 20|MSIE 6.0|3B 20|', '!"Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26579
static inline void alert_rule_26579(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26579;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent", 
                     sizeof("MALWARE-CNC Win.Trojan.Kazy/FakeAV Checkin with IE6 User-Agent"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26585
// Rule Name: INDICATOR-COMPROMISE config.inc.php in iframe
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26585
static inline int check_rule_26585(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<iframe', 'config.inc.php",within 100', '</iframe>",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26585
static inline void alert_rule_26585(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26585;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE config.inc.php in iframe", 
                     sizeof("INDICATOR-COMPROMISE config.inc.php in iframe"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26613
// Rule Name: MALWARE-CNC Medfos Trojan variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26613
static inline int check_rule_26613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/feed?req=http",fast_pattern,nocase', '|3B| MSIE ', '!"|0D 0A|Accept-Language:', '!"|0D 0A|Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26613
static inline void alert_rule_26613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Medfos Trojan variant outbound connection", 
                     sizeof("MALWARE-CNC Medfos Trojan variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26655
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.PCRat data upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 26655
static inline int check_rule_26655(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: PCRatd",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26655
static inline void alert_rule_26655(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26655;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.PCRat data upload", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.PCRat data upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26656
// Rule Name: MALWARE-CNC Win.Trojan.Travnet Botnet data upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26656
static inline int check_rule_26656(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['hostid=', '|26|hostname=', '|26|hostip=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26656
static inline void alert_rule_26656(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26656;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Travnet Botnet data upload", 
                     sizeof("MALWARE-CNC Win.Trojan.Travnet Botnet data upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26657
// Rule Name: MALWARE-CNC Win.Trojan.Shiz variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26657
static inline int check_rule_26657(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/login.php",depth 10', 'Referer|3A| http://www.google.com', 'User-Agent|3A| Mozilla/4.0 |28|compatible|3B| MSIE 2.0|3B|",fast_pattern,nocase', 'HTTP/1.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26657
static inline void alert_rule_26657(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26657;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Shiz variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Shiz variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26658
// Rule Name: BROWSER-WEBKIT Possible Google Chrome Plugin install from non-trusted source
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 26658
static inline int check_rule_26658(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['!"googleusercontent', '!"google.com', '|2F|crx|2F|blobs', '!"gvt1.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26658
static inline void alert_rule_26658(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26658;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-WEBKIT Possible Google Chrome Plugin install from non-trusted source", 
                     sizeof("BROWSER-WEBKIT Possible Google Chrome Plugin install from non-trusted source"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26659
// Rule Name: BROWSER-FIREFOX Possible Mozilla Firefox Plugin install from non-Mozilla source
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 26659
static inline int check_rule_26659(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['!"mozilla', '.xpi",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26659
static inline void alert_rule_26659(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26659;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Possible Mozilla Firefox Plugin install from non-Mozilla source", 
                     sizeof("BROWSER-FIREFOX Possible Mozilla Firefox Plugin install from non-Mozilla source"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26660
// Rule Name: MALWARE-OTHER Fake delivery information phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26660
static inline int check_rule_26660(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B| filename=', 'Delivery_Information_ID-",fast_pattern,nocase', 'Delivery_Information_ID-', '.exe",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26660
static inline void alert_rule_26660(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26660;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake delivery information phishing attack", 
                     sizeof("MALWARE-OTHER Fake delivery information phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26695
// Rule Name: MALWARE-CNC Win.Trojan.Namihno variant outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26695
static inline int check_rule_26695(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/windows/update/search?hl=', '&q=",distance 0', '&meta=",distance 0', '&id=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26695
static inline void alert_rule_26695(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26695;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Namihno variant outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Namihno variant outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26696
// Rule Name: MALWARE-CNC Cbeplay Ransomware variant outbound connection - Abnormal HTTP Headers
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26696
static inline int check_rule_26696(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST /index.php HTTP/1.1|0D 0A|Content-Type: multipart/form-data|3B| boundary=",depth 70', '|0D 0A|Connection: close|0D 0A|Cache-Control: no-cache|0D 0A|Content-Length: ', '|3B| name=|22|data|22 3B| filename=|22|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26696
static inline void alert_rule_26696(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26696;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Cbeplay Ransomware variant outbound connection - Abnormal HTTP Headers", 
                     sizeof("MALWARE-CNC Cbeplay Ransomware variant outbound connection - Abnormal HTTP Headers"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26697
// Rule Name: MALWARE-CNC Cbeplay Ransomware variant outbound connection - POST Body
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26697
static inline int check_rule_26697(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['index.php', '|3B| name=|22|data|22 3B| filename=|22|",fast_pattern,nocase', '--",depth 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26697
static inline void alert_rule_26697(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26697;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Cbeplay Ransomware variant outbound connection - POST Body", 
                     sizeof("MALWARE-CNC Cbeplay Ransomware variant outbound connection - POST Body"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26698
// Rule Name: MALWARE-OTHER Compromised Website response - leads to Exploit Kit
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26698
static inline int check_rule_26698(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<!--ded509-->', '<!--/ded509-->",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26698
static inline void alert_rule_26698(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26698;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Compromised Website response - leads to Exploit Kit", 
                     sizeof("MALWARE-OTHER Compromised Website response - leads to Exploit Kit"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26712
// Rule Name: MALWARE-CNC Kazy Trojan check-in
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26712
static inline int check_rule_26712(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Opera/11 |28|Windows NT 5.1|3B 20 3B| x86|29|",fast_pattern,nocase', '/count.php?page=",depth 16']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26712
static inline void alert_rule_26712(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26712;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Kazy Trojan check-in", 
                     sizeof("MALWARE-CNC Kazy Trojan check-in"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26713
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev rev 1 outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26713
static inline int check_rule_26713(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gate.php|3F|reg=', 'User-Agent|3A| Mozilla/4.0 (compatible|3B| Synapse)|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26713
static inline void alert_rule_26713(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26713;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev rev 1 outbound traffic", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev rev 1 outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26714
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev rev 2 outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26714
static inline int check_rule_26714(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gate.php|3F|reg=', 'User-Agent|3A| Mozilla/4.0 (SEObot)|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26714
static inline void alert_rule_26714(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26714;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev rev 2 outbound traffic", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev rev 2 outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26715
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev rev 3 outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26715
static inline int check_rule_26715(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gate.php|3F|id=', 'User-Agent|3A| Mozilla/4.0 (compatible|3B| SEObot)|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26715
static inline void alert_rule_26715(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26715;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev rev 3 outbound traffic", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev rev 3 outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26719
// Rule Name: MALWARE-CNC Win.Trojan.Kbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26719
static inline int check_rule_26719(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: s_alive.php?id=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26719
static inline void alert_rule_26719(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26719;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26720
// Rule Name: MALWARE-CNC Win.Trojan.Kbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26720
static inline int check_rule_26720(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: s_task.php?id=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26720
static inline void alert_rule_26720(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26720;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26722
// Rule Name: MALWARE-CNC Bancos fake JPG encrypted config file download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26722
static inline int check_rule_26722(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.com.br|0D 0A 0D 0A|",fast_pattern,nocase', '/imagens/",depth 9', '.jpg",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26722
static inline void alert_rule_26722(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26722;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Bancos fake JPG encrypted config file download", 
                     sizeof("MALWARE-CNC Bancos fake JPG encrypted config file download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26723
// Rule Name: MALWARE-CNC Trojan Downloader7
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26723
static inline int check_rule_26723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.lavaibrasilok.com|0D 0A 0D 0A|",fast_pattern,nocase', '|3B| MSIE ', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26723
static inline void alert_rule_26723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Trojan Downloader7", 
                     sizeof("MALWARE-CNC Trojan Downloader7"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26725
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc http command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26725
static inline int check_rule_26725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: http|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26725
static inline void alert_rule_26725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc http command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc http command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26726
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc stop command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26726
static inline int check_rule_26726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: stop|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26726
static inline void alert_rule_26726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc stop command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc stop command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26727
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc die command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26727
static inline int check_rule_26727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: die|7C|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26727
static inline void alert_rule_26727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc die command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc die command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26728
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc sleep command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26728
static inline int check_rule_26728(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: sleep|7C|",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26728
static inline void alert_rule_26728(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26728;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc sleep command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc sleep command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26729
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc simple command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26729
static inline int check_rule_26729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: simple|7C|",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26729
static inline void alert_rule_26729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc simple command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc simple command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26730
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc loginpost command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26730
static inline int check_rule_26730(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: loginpost|7C|",depth 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26730
static inline void alert_rule_26730(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26730;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc loginpost command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc loginpost command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26731
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc datapost command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26731
static inline int check_rule_26731(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: datapost|7C|",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26731
static inline void alert_rule_26731(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26731;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc datapost command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc datapost command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26732
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc syn command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26732
static inline int check_rule_26732(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: syn|7C|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26732
static inline void alert_rule_26732(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26732;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc syn command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc syn command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26733
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc udp command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26733
static inline int check_rule_26733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: udp|7C|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26733
static inline void alert_rule_26733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc udp command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc udp command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26734
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc udpdata command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26734
static inline int check_rule_26734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: udpdata|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26734
static inline void alert_rule_26734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc udpdata command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc udpdata command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26735
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc data command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26735
static inline int check_rule_26735(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: data|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26735
static inline void alert_rule_26735(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26735;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc data command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc data command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26736
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc icmp command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26736
static inline int check_rule_26736(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: icmp|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26736
static inline void alert_rule_26736(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26736;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc icmp command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc icmp command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26737
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc tcpdata command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26737
static inline int check_rule_26737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: tcpdata|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26737
static inline void alert_rule_26737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc tcpdata command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc tcpdata command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26738
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc dataget command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26738
static inline int check_rule_26738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dataget|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26738
static inline void alert_rule_26738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc dataget command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc dataget command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26739
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc connect command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26739
static inline int check_rule_26739(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: connect|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26739
static inline void alert_rule_26739(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26739;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc connect command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc connect command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26740
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc dns command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26740
static inline int check_rule_26740(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: dns|7C|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26740
static inline void alert_rule_26740(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26740;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc dns command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc dns command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26741
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc exec command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26741
static inline int check_rule_26741(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: exec|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26741
static inline void alert_rule_26741(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26741;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc exec command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc exec command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26742
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc resolve command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26742
static inline int check_rule_26742(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: resolve|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26742
static inline void alert_rule_26742(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26742;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc resolve command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc resolve command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26743
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc antiddos command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26743
static inline int check_rule_26743(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: antiddos|7C|",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26743
static inline void alert_rule_26743(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26743;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc antiddos command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc antiddos command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26744
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc range command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26744
static inline int check_rule_26744(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: range|7C|",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26744
static inline void alert_rule_26744(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26744;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc range command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc range command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26745
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc ftp command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26745
static inline int check_rule_26745(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ftp|7C|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26745
static inline void alert_rule_26745(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26745;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc ftp command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc ftp command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26746
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc download command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26746
static inline int check_rule_26746(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: download|7C|",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26746
static inline void alert_rule_26746(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26746;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc download command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc download command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26747
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc fastddos command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26747
static inline int check_rule_26747(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: fastddos|7C|",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26747
static inline void alert_rule_26747(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26747;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc fastddos command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc fastddos command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26748
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc slowhttp command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26748
static inline int check_rule_26748(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: slowhttp|7C|",depth 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26748
static inline void alert_rule_26748(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26748;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc slowhttp command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc slowhttp command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26749
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc allhttp command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26749
static inline int check_rule_26749(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: allhttp|7C|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26749
static inline void alert_rule_26749(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26749;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc allhttp command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc allhttp command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26750
// Rule Name: MALWARE-CNC Win.Trojan.BlackRev cnc full command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26750
static inline int check_rule_26750(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: full|7C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26750
static inline void alert_rule_26750(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26750;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRev cnc full command", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRev cnc full command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26774
// Rule Name: MALWARE-CNC Win.Worm.Luder variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26774
static inline int check_rule_26774(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/loader.cpl",fast_pattern,nocase', '|3B 20|MSIE|20|', '!"|0D 0A|Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26774
static inline void alert_rule_26774(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26774;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Luder variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Luder variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26775
// Rule Name: MALWARE-CNC Win.Trojan.Blocker variant outbound connection HTTP Header Structure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26775
static inline int check_rule_26775(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/index.html', '.info|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26775
static inline void alert_rule_26775(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26775;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Blocker variant outbound connection HTTP Header Structure", 
                     sizeof("MALWARE-CNC Win.Trojan.Blocker variant outbound connection HTTP Header Structure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26776
// Rule Name: MALWARE-CNC Win.Trojan.Blocker variant outbound connection POST
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26776
static inline int check_rule_26776(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'cmd=gravar&dados=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26776
static inline void alert_rule_26776(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26776;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Blocker variant outbound connection POST", 
                     sizeof("MALWARE-CNC Win.Trojan.Blocker variant outbound connection POST"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26779
// Rule Name: MALWARE-CNC Win.Trojan.Cridex encrypted POST check-in
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26779
static inline int check_rule_26779(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cos3q/in",fast_pattern,nocase', '.exe",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26779
static inline void alert_rule_26779(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26779;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Cridex encrypted POST check-in", 
                     sizeof("MALWARE-CNC Win.Trojan.Cridex encrypted POST check-in"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26780
// Rule Name: MALWARE-CNC cridex HTTP Response - default0.js
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26780
static inline int check_rule_26780(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |00|<script type=|22|text/javascript|22| src=|22|/scripts/default0.js|22|></script>|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26780
static inline void alert_rule_26780(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26780;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC cridex HTTP Response - default0.js", 
                     sizeof("MALWARE-CNC cridex HTTP Response - default0.js"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26811
// Rule Name: MALWARE-CNC XP Fake Antivirus Payment Page Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26811
static inline int check_rule_26811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /content/img/awards.jpg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26811
static inline void alert_rule_26811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC XP Fake Antivirus Payment Page Request", 
                     sizeof("MALWARE-CNC XP Fake Antivirus Payment Page Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26812
// Rule Name: MALWARE-CNC XP Fake Antivirus Check-in
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26812
static inline int check_rule_26812(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |3B| MSIE 6.0|3B| Windows NT 5.1)|0D 0A|Accept: */*|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26812
static inline void alert_rule_26812(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26812;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC XP Fake Antivirus Check-in", 
                     sizeof("MALWARE-CNC XP Fake Antivirus Check-in"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26814
// Rule Name: EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from Linked-In Mailing Campaign
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26814
static inline int check_rule_26814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /linkendorse.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26814
static inline void alert_rule_26814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from Linked-In Mailing Campaign", 
                     sizeof("EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from Linked-In Mailing Campaign"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26834
// Rule Name: EXPLOIT-KIT Sweet Orange exploit kit landing page in.php base64 uri
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26834
static inline int check_rule_26834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/in.php', '&q=",distance 0', '==",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26834
static inline void alert_rule_26834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Sweet Orange exploit kit landing page in.php base64 uri", 
                     sizeof("EXPLOIT-KIT Sweet Orange exploit kit landing page in.php base64 uri"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26835
// Rule Name: MALWARE-CNC RDN Banker POST variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26835
static inline int check_rule_26835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'op=IncluirAvisos&",fast_pattern,nocase', 'HostBD=",depth 7,offset 17']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26835
static inline void alert_rule_26835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RDN Banker POST variant outbound connection", 
                     sizeof("MALWARE-CNC RDN Banker POST variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26836
// Rule Name: MALWARE-CNC RDN Banker Strange Google Traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26836
static inline int check_rule_26836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/4.0 (compatible|3B| Win32|3B| WinHttp.WinHttpRequest.5)",fast_pattern,nocase', 'Host: www.google.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26836
static inline void alert_rule_26836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RDN Banker Strange Google Traffic", 
                     sizeof("MALWARE-CNC RDN Banker Strange Google Traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26837
// Rule Name: MALWARE-CNC BitBot Idle C2 response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26837
static inline int check_rule_26837(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <|5C 5C 5C|>IDLE<|5C 5C 5C|>",depth 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26837
static inline void alert_rule_26837(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26837;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BitBot Idle C2 response", 
                     sizeof("MALWARE-CNC BitBot Idle C2 response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26838
// Rule Name: EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from NatPay Mailing Campaign
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26838
static inline int check_rule_26838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /natpay.html?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26838
static inline void alert_rule_26838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from NatPay Mailing Campaign", 
                     sizeof("EXPLOIT-KIT Blackholev2 exploit kit Initial Gate from NatPay Mailing Campaign"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26842
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Boda Malware Checkin
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26842
static inline int check_rule_26842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['macName=",depth 60', '&macOS=",within 100', '&macMac=",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26842
static inline void alert_rule_26842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Boda Malware Checkin", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Boda Malware Checkin"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26910
// Rule Name: MALWARE-CNC ZeroAccess Encrypted 128-byte POST No Accept Headers
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26910
static inline int check_rule_26910(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'Content-Length: 128|0D 0A|",fast_pattern,nocase', ' HTTP/1.', '|0D 0A|User-Agent: ",within 14,distance 1', '!"|0D 0A|Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26910
static inline void alert_rule_26910(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26910;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC ZeroAccess Encrypted 128-byte POST No Accept Headers", 
                     sizeof("MALWARE-CNC ZeroAccess Encrypted 128-byte POST No Accept Headers"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26911
// Rule Name: MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26911
static inline int check_rule_26911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/info.php?act=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26911
static inline void alert_rule_26911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26912
// Rule Name: MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26912
static inline int check_rule_26912(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '<|7C|>",fast_pattern,nocase', 'data=",depth 5', '<|7C|>",within 3,distance 31', '<|7C|>",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26912
static inline void alert_rule_26912(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26912;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Rombrast Trojan outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26923
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26923
static inline int check_rule_26923(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/images/', '.php?id=",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26923
static inline void alert_rule_26923(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26923;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26924
// Rule Name: MALWARE-CNC Potential Gozi Trojan HTTP Header Structure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26924
static inline int check_rule_26924(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['= HTTP/1.",fast_pattern,nocase', '.php?', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26924
static inline void alert_rule_26924(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26924;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential Gozi Trojan HTTP Header Structure", 
                     sizeof("MALWARE-CNC Potential Gozi Trojan HTTP Header Structure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26925
// Rule Name: SQL generic convert injection attempt - GET parameter
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 26925
static inline int check_rule_26925(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: convert|28|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26925
static inline void alert_rule_26925(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26925;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL generic convert injection attempt - GET parameter", 
                     sizeof("SQL generic convert injection attempt - GET parameter"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26947
// Rule Name: EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26947
static inline int check_rule_26947(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: filename=atom.jar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26947
static inline void alert_rule_26947(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26947;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download", 
                     sizeof("EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26948
// Rule Name: EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26948
static inline int check_rule_26948(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: filename=site.jar",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26948
static inline void alert_rule_26948(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26948;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download", 
                     sizeof("EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit inbound java exploit download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26949
// Rule Name: EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit landing page
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26949
static inline int check_rule_26949(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<applet width=', '0",within 1,distance 1', ' height=",within 8,distance 1', '0",within 1,distance 1', ' code=",within 6,distance 1', 'site.avi",within 8,distance 1,nocase', ' archive=",within 9,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26949
static inline void alert_rule_26949(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26949;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit landing page", 
                     sizeof("EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit landing page"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26951
// Rule Name: EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Malvertising Campaign URI request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26951
static inline int check_rule_26951(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/.cache/?f=",fast_pattern', '.jar']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26951
static inline void alert_rule_26951(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26951;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Malvertising Campaign URI request", 
                     sizeof("EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Malvertising Campaign URI request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26965
// Rule Name: MALWARE-CNC Win.Trojan.Win32 Facebook Secure Cryptor C2
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26965
static inline int check_rule_26965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/forum/search.php?email=', '&method=",distance 0', '!"Referer', '!"Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26965
static inline void alert_rule_26965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Win32 Facebook Secure Cryptor C2", 
                     sizeof("MALWARE-CNC Win.Trojan.Win32 Facebook Secure Cryptor C2"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26966
// Rule Name: MALWARE-CNC Win32/Autorun.JN variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26966
static inline int check_rule_26966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/u5.htm",fast_pattern,nocase', '//u5.htm']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26966
static inline void alert_rule_26966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win32/Autorun.JN variant outbound connection", 
                     sizeof("MALWARE-CNC Win32/Autorun.JN variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26968
// Rule Name: MALWARE-CNC Win.Trojan.Gozi Data Theft POST Data
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26968
static inline int check_rule_26968(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'data.php', '|0D 0A|URL: ",fast_pattern,nocase', 'Content-Disposition: form-data|3B| name=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26968
static inline void alert_rule_26968(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26968;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gozi Data Theft POST Data", 
                     sizeof("MALWARE-CNC Win.Trojan.Gozi Data Theft POST Data"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26969
// Rule Name: MALWARE-CNC Win.Trojan.Gozi Trojan Data Theft POST URL
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26969
static inline int check_rule_26969(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php?version=', '&user=",distance 0', '&server=",distance 0', '&name=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26969
static inline void alert_rule_26969(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26969;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gozi Trojan Data Theft POST URL", 
                     sizeof("MALWARE-CNC Win.Trojan.Gozi Trojan Data Theft POST URL"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26970
// Rule Name: MALWARE-CNC Win.Trojan.Pirminay variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26970
static inline int check_rule_26970(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Cookie: cache=cc2=",fast_pattern,nocase', 'cache=cc2=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26970
static inline void alert_rule_26970(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26970;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Pirminay variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Pirminay variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26984
// Rule Name: MALWARE-CNC Win.Trojan.Injector Info Stealer Trojan variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26984
static inline int check_rule_26984(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/xgi-bin/",depth 9', '.php?",within 5,distance 1', '|3B| MSIE ', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26984
static inline void alert_rule_26984(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26984;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector Info Stealer Trojan variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector Info Stealer Trojan variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 26985
// Rule Name: EXPLOIT-KIT Rawin exploit kit outbound java retrieval
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 26985
static inline int check_rule_26985(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['rawin.php?b=', '&v=1.",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 26985
static inline void alert_rule_26985(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 26985;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Rawin exploit kit outbound java retrieval", 
                     sizeof("EXPLOIT-KIT Rawin exploit kit outbound java retrieval"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27017
// Rule Name: MALWARE-CNC Win.Trojan.Dapato variant inbound response connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27017
static inline int check_rule_27017(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 150|0D 0A|",fast_pattern,nocase', '|0D 0A|",depth 2,offset 4', '|0D 0A|",within 2,distance 4', '|0D 0A|",within 2,distance 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27017
static inline void alert_rule_27017(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27017;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dapato variant inbound response connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dapato variant inbound response connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27039
// Rule Name: MALWARE-CNC Win.Trojan.OnlineGameHack variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27039
static inline int check_rule_27039(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get.asp?mac=', '&os=",within 36']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27039
static inline void alert_rule_27039(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27039;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.OnlineGameHack variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.OnlineGameHack variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27040
// Rule Name: EXPLOIT-KIT Styx exploit kit plugin detection connection jorg
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27040
static inline int check_rule_27040(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /jorg.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27040
static inline void alert_rule_27040(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27040;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Styx exploit kit plugin detection connection jorg", 
                     sizeof("EXPLOIT-KIT Styx exploit kit plugin detection connection jorg"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27041
// Rule Name: EXPLOIT-KIT Styx exploit kit plugin detection connection jlnp
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27041
static inline int check_rule_27041(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /jlnp.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27041
static inline void alert_rule_27041(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27041;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Styx exploit kit plugin detection connection jlnp", 
                     sizeof("EXPLOIT-KIT Styx exploit kit plugin detection connection jlnp"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27042
// Rule Name: EXPLOIT-KIT Styx exploit kit plugin detection connection jovf
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27042
static inline int check_rule_27042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /jovf.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27042
static inline void alert_rule_27042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Styx exploit kit plugin detection connection jovf", 
                     sizeof("EXPLOIT-KIT Styx exploit kit plugin detection connection jovf"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27044
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string pb - Htbot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27044
static inline int check_rule_27044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: pb|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27044
static inline void alert_rule_27044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string pb - Htbot", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string pb - Htbot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27045
// Rule Name: MALWARE-CNC Win.Trojan.Blocker Download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27045
static inline int check_rule_27045(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['filename=', 'security_cleaner.exe",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27045
static inline void alert_rule_27045(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27045;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Blocker Download", 
                     sizeof("MALWARE-CNC Win.Trojan.Blocker Download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27047
// Rule Name: INDICATOR-COMPROMISE Unknown ?1 redirect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: bad-unknown
// Generated from Snort rule parser output

// Check function for rule 27047
static inline int check_rule_27047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GET /?1 HTTP/1.1",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27047
static inline void alert_rule_27047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Unknown ?1 redirect", 
                     sizeof("INDICATOR-COMPROMISE Unknown ?1 redirect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27085
// Rule Name: EXPLOIT-KIT Unknown Malvertising exploit kit Hostile Jar pipe.class
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27085
static inline int check_rule_27085(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['PK', '|00|pipe.class",distance 0', '|00|inc.class",distance 0', '|00|fdp.class",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27085
static inline void alert_rule_27085(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27085;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Unknown Malvertising exploit kit Hostile Jar pipe.class", 
                     sizeof("EXPLOIT-KIT Unknown Malvertising exploit kit Hostile Jar pipe.class"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27086
// Rule Name: EXPLOIT-KIT Unknown Malvertising exploit kit stage-1 redirect
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27086
static inline int check_rule_27086(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<html><body><script>|0A|var ",fast_pattern', 'document.createElement(",within 80', '.setAttribute(|22|archive|22|, ",within 65', '.setAttribute(|22|codebase|22|, ",within 65', '.setAttribute(|22|id|22|, ",within 65', '.setAttribute(|22|code|22|, ",within 65', '|22|)|3B 0A|document.body.appendChild(",within 65', '</script>|0A|</body>|0A|</html>|0A 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27086
static inline void alert_rule_27086(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27086;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Unknown Malvertising exploit kit stage-1 redirect", 
                     sizeof("EXPLOIT-KIT Unknown Malvertising exploit kit stage-1 redirect"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27110
// Rule Name: EXPLOIT-KIT Blackholev2/Cool exploit kit outbound portable executable request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27110
static inline int check_rule_27110(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['php?sf=', '&Ze=",distance 0', '&m=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27110
static inline void alert_rule_27110(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27110;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Cool exploit kit outbound portable executable request", 
                     sizeof("EXPLOIT-KIT Blackholev2/Cool exploit kit outbound portable executable request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27113
// Rule Name: EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Zeroaccess download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27113
static inline int check_rule_27113(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?f=a', '&k=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27113
static inline void alert_rule_27113(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27113;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Zeroaccess download attempt", 
                     sizeof("EXPLOIT-KIT DotkaChef/Rmayana/DotCache exploit kit Zeroaccess download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27144
// Rule Name: EXPLOIT-KIT Private exploit kit outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27144
static inline int check_rule_27144(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?', 'content-type: application/', ' Java/1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27144
static inline void alert_rule_27144(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27144;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Private exploit kit outbound traffic", 
                     sizeof("EXPLOIT-KIT Private exploit kit outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27199
// Rule Name: MALWARE-CNC Win.Trojan.Meredrop variant outbound connection GET Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27199
static inline int check_rule_27199(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?",depth 2', 'h=NT",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27199
static inline void alert_rule_27199(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27199;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Meredrop variant outbound connection GET Request", 
                     sizeof("MALWARE-CNC Win.Trojan.Meredrop variant outbound connection GET Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27200
// Rule Name: MALWARE-CNC Win.Trojan.Meredrop variant outbound connection POST Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27200
static inline int check_rule_27200(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '|3B 20|MSIE 28|3B 20|",fast_pattern,nocase', 'User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27200
static inline void alert_rule_27200(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27200;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Meredrop variant outbound connection POST Request", 
                     sizeof("MALWARE-CNC Win.Trojan.Meredrop variant outbound connection POST Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27201
// Rule Name: MALWARE-CNC Win.Trojan.Neurevt variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27201
static inline int check_rule_27201(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['ps0=",depth 4', 'ps1=",distance 0', 'cs1=",distance 0', 'cs2=",distance 0', 'cs3=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27201
static inline void alert_rule_27201(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27201;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Neurevt variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Neurevt variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27203
// Rule Name: INDICATOR-COMPROMISE Apache auto_prepend_file a.control.bin C2 traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27203
static inline int check_rule_27203(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| SEX|2F|1",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27203
static inline void alert_rule_27203(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27203;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Apache auto_prepend_file a.control.bin C2 traffic", 
                     sizeof("INDICATOR-COMPROMISE Apache auto_prepend_file a.control.bin C2 traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27204
// Rule Name: MALWARE-CNC Potential Bancos Brazilian Banking Trojan Browser Proxy Autoconfig File
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27204
static inline int check_rule_27204(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['return |22|DIRECT|22|",fast_pattern,nocase', '.com.br",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27204
static inline void alert_rule_27204(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27204;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential Bancos Brazilian Banking Trojan Browser Proxy Autoconfig File", 
                     sizeof("MALWARE-CNC Potential Bancos Brazilian Banking Trojan Browser Proxy Autoconfig File"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27246
// Rule Name: MALWARE-OTHER Mac OSX FBI ransomware
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27246
static inline int check_rule_27246(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <iframe src=|22|YOUR|25|20BROWSER|25|20HAS|25|20BEEN|25|20LOCKED",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27246
static inline void alert_rule_27246(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27246;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Mac OSX FBI ransomware", 
                     sizeof("MALWARE-OTHER Mac OSX FBI ransomware"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27248
// Rule Name: MALWARE-CNC Win.Trojan.Gamarue - Mozi1la User-Agent
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27248
static inline int check_rule_27248(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Mozi1la/4.0|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27248
static inline void alert_rule_27248(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27248;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gamarue - Mozi1la User-Agent", 
                     sizeof("MALWARE-CNC Win.Trojan.Gamarue - Mozi1la User-Agent"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27252
// Rule Name: MALWARE-CNC Win.Trojan.ZeroAccess 111-byte URL variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27252
static inline int check_rule_27252(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['==",depth 2,offset 103', ' HTTP/1.0|0D 0A|Host:",within 16,distance 10', '!"Accept:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27252
static inline void alert_rule_27252(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27252;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ZeroAccess 111-byte URL variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ZeroAccess 111-byte URL variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27253
// Rule Name: MALWARE-CNC Win.Trojan.Cridex Encrypted POST w/ URL Pattern
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27253
static inline int check_rule_27253(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'U|3B| MSIE ', '|0D 0A|Connection|3A| Keep-Alive|0D 0A|Cache-Control|3A| no-cache",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27253
static inline void alert_rule_27253(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27253;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Cridex Encrypted POST w/ URL Pattern", 
                     sizeof("MALWARE-CNC Win.Trojan.Cridex Encrypted POST w/ URL Pattern"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27254
// Rule Name: MALWARE-CNC Yakes Trojan HTTP Header Structure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27254
static inline int check_rule_27254(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php HTTP/1.1|0D 0A|Cache-Control: ",fast_pattern,nocase', '.php HTTP/1.1",nocase', '|0D 0A|Cache-Control: no-cache|0D 0A|Connection: close|0D 0A|Pragma: no-cache|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|",within 113']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27254
static inline void alert_rule_27254(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27254;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Yakes Trojan HTTP Header Structure", 
                     sizeof("MALWARE-CNC Yakes Trojan HTTP Header Structure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27255
// Rule Name: INDICATOR-COMPROMISE All Numbers .EXE file name from abnormally ordered HTTP headers - Potential Yakes Trojan Download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27255
static inline int check_rule_27255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '.exe HTTP/1.1|0D 0A|Cache-Control: ",fast_pattern,nocase', '.exe HTTP/1.1",nocase', '|0D 0A|Cache-Control: no-cache|0D 0A|Connection: close|0D 0A|Pragma: no-cache|0D 0A|User-Agent: ",within 76', '|3A 20|",distance 0', '!"|3A 20|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27255
static inline void alert_rule_27255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE All Numbers .EXE file name from abnormally ordered HTTP headers - Potential Yakes Trojan Download", 
                     sizeof("INDICATOR-COMPROMISE All Numbers .EXE file name from abnormally ordered HTTP headers - Potential Yakes Trojan Download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27256
// Rule Name: MALWARE-CNC Win.Trojan.Kryptik Drive-by Download Malware
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27256
static inline int check_rule_27256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '.php?id=",offset 6,fast_pattern', ' HTTP/1.",within 11,distance 1', '|0D 0A|User-Agent: Mozilla/",within 22,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27256
static inline void alert_rule_27256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kryptik Drive-by Download Malware", 
                     sizeof("MALWARE-CNC Win.Trojan.Kryptik Drive-by Download Malware"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27257
// Rule Name: MALWARE-CNC Win.Trojan.Kryptic 7-byte URI Invalid Firefox Headers - no Accept-Language
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27257
static inline int check_rule_27257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', 'Firefox/3.",fast_pattern,nocase', '!"Accept-Language:', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27257
static inline void alert_rule_27257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kryptic 7-byte URI Invalid Firefox Headers - no Accept-Language", 
                     sizeof("MALWARE-CNC Win.Trojan.Kryptic 7-byte URI Invalid Firefox Headers - no Accept-Language"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27533
// Rule Name: MALWARE-CNC Potential Win.Trojan.Kraziomel Download - 000.jpg
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27533
static inline int check_rule_27533(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/000.jpg",fast_pattern,nocase', 'HTTP/1.0|0D 0A|Host: ', '!"|3A 20|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27533
static inline void alert_rule_27533(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27533;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential Win.Trojan.Kraziomel Download - 000.jpg", 
                     sizeof("MALWARE-CNC Potential Win.Trojan.Kraziomel Download - 000.jpg"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27538
// Rule Name: MALWARE-OTHER self-signed SSL certificate with default MyCompany Ltd organization name
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 27538
static inline int check_rule_27538(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|55 04 0A|', '|0E|MyCompany Ltd",within 14,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27538
static inline void alert_rule_27538(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27538;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER self-signed SSL certificate with default MyCompany Ltd organization name", 
                     sizeof("MALWARE-OTHER self-signed SSL certificate with default MyCompany Ltd organization name"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27565
// Rule Name: MALWARE-OTHER HideMeBetter spam injection variant
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27565
static inline int check_rule_27565(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<div id=|22|HideMeBetter|22|>",fast_pattern,nocase', 'if(document|2E|getElementById(|22|HideMeBetter|22|)|20 21 3D 20|null)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27565
static inline void alert_rule_27565(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27565;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER HideMeBetter spam injection variant", 
                     sizeof("MALWARE-OTHER HideMeBetter spam injection variant"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27567
// Rule Name: MALWARE-CNC Win.Trojan.Rovnix malicious download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27567
static inline int check_rule_27567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ld.aspx",nocase', 'User-Agent|3A 20|FWVersionTestAgent|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27567
static inline void alert_rule_27567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rovnix malicious download request", 
                     sizeof("MALWARE-CNC Win.Trojan.Rovnix malicious download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27596
// Rule Name: MALWARE-CNC Win.Trojan.Redyms variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27596
static inline int check_rule_27596(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&intip=",fast_pattern,nocase', '?id=', '&port=",distance 0', '&bid=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27596
static inline void alert_rule_27596(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27596;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Redyms variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Redyms variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27599
// Rule Name: MALWARE-CNC Fort Disco Registration variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27599
static inline int check_rule_27599(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cmd.php', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| Synapse)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27599
static inline void alert_rule_27599(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27599;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Fort Disco Registration variant outbound connection", 
                     sizeof("MALWARE-CNC Fort Disco Registration variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27629
// Rule Name: MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27629
static inline int check_rule_27629(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tomcat-docs/index.jsp?/', 'User-Agent|3A| Mozilla/4.0 |28|compatible|3B| MSIE 5.01|3B| Windows NT 5.0|29|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27629
static inline void alert_rule_27629(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27629;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27630
// Rule Name: MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27630
static inline int check_rule_27630(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/bbs/search.asp', 'User-Agent|3A| Mozilla/4.0 |28|compatible|3B| MSIE 5.0|3B| Windows NT 5.0|29|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27630
static inline void alert_rule_27630(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27630;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27631
// Rule Name: MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27631
static inline int check_rule_27631(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/buy-sell/search.asp?newsid=', 'User-Agent|3A| Mozilla/4.0 |28|compatible|3B| MSIE 5.0|3B| Windows NT 5.0|29|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27631
static inline void alert_rule_27631(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27631;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Aumlib variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27633
// Rule Name: MALWARE-CNC Worm.Silly variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27633
static inline int check_rule_27633(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ul.htm",fast_pattern,nocase', '|3B| MSIE 6.0|3B 20|', '!"Accept-Language: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27633
static inline void alert_rule_27633(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27633;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Worm.Silly variant outbound connection", 
                     sizeof("MALWARE-CNC Worm.Silly variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27648
// Rule Name: MALWARE-CNC Win.Trojan.SpyBanker.ZSL variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27648
static inline int check_rule_27648(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'valor=",depth 6', ']branco[",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27648
static inline void alert_rule_27648(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27648;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpyBanker.ZSL variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpyBanker.ZSL variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27649
// Rule Name: MALWARE-CNC Brazilian Banking Trojan data theft
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27649
static inline int check_rule_27649(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'remetente=",depth 10', '&destinatario=",distance 0', '&assunto=",distance 0', '&mensagem=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27649
static inline void alert_rule_27649(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27649;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Brazilian Banking Trojan data theft", 
                     sizeof("MALWARE-CNC Brazilian Banking Trojan data theft"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27680
// Rule Name: MALWARE-CNC Win.Trojan.ZeroAccess variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27680
static inline int check_rule_27680(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .php HTTP/1.1|0D 0A|User-Agent: Opera/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27680
static inline void alert_rule_27680(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27680;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ZeroAccess variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ZeroAccess variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27708
// Rule Name: MALWARE-CNC Win.Ransomware.Urausy outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27708
static inline int check_rule_27708(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.html', '|0D 0A|User-Agent|3A| Mozilla/5.0 |28|compatible|3B| MSIE 9.0|3B| Windows NT 6.1|3B| Trident/5.0",fast_pattern,nocase', '!"Cookie:', '!"X-BlueCoat-Via:', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27708
static inline void alert_rule_27708(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27708;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Urausy outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.Urausy outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27726
// Rule Name: MALWARE-CNC Orbit Downloader denial of service update
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27726
static inline int check_rule_27726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /update/ido.ipl",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27726
static inline void alert_rule_27726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Orbit Downloader denial of service update", 
                     sizeof("MALWARE-CNC Orbit Downloader denial of service update"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27727
// Rule Name: MALWARE-CNC Orbit Downloader denial of service update
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27727
static inline int check_rule_27727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /update/myinfo.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27727
static inline void alert_rule_27727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Orbit Downloader denial of service update", 
                     sizeof("MALWARE-CNC Orbit Downloader denial of service update"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27728
// Rule Name: MALWARE-CNC Orbit Downloader denial of service update
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27728
static inline int check_rule_27728(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /update/param.php?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27728
static inline void alert_rule_27728(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27728;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Orbit Downloader denial of service update", 
                     sizeof("MALWARE-CNC Orbit Downloader denial of service update"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27774
// Rule Name: MALWARE-CNC RDN Banker Data Exfiltration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27774
static inline int check_rule_27774(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '|3B| name=|22|arquivo|22 3B| filename=|22|C:|5C|",fast_pattern,nocase', '_.log|22 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27774
static inline void alert_rule_27774(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27774;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC RDN Banker Data Exfiltration", 
                     sizeof("MALWARE-CNC RDN Banker Data Exfiltration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27775
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27775
static inline int check_rule_27775(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '.htm', '!"Accept', '|0A|Content-Length: 164|0D 0A|User-Agent: ",fast_pattern,nocase', 'host|3A|",nocase', '|2E|",within 5', '|2E|",within 4', '|2E|",within 4', '|6C 55 55 45|",depth 4,offset 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27775
static inline void alert_rule_27775(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27775;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27802
// Rule Name: MALWARE-CNC Win.Trojan.PRISM variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27802
static inline int check_rule_27802(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/page/index_htm_files2/",nocase', '.png",within 4,distance 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27802
static inline void alert_rule_27802(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27802;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PRISM variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.PRISM variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27803
// Rule Name: MALWARE-CNC Win.Trojan.PRISM variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27803
static inline int check_rule_27803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/form.php",depth 9', 'RcpTfdsvoD9KB9O",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27803
static inline void alert_rule_27803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PRISM variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.PRISM variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27804
// Rule Name: MALWARE-CNC Win.Trojan.PRISM variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27804
static inline int check_rule_27804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/page/index.php",nocase', 'foo=', 'data=RcpTfdssoD9KB9O",depth 20,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27804
static inline void alert_rule_27804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PRISM variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.PRISM variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27805
// Rule Name: MALWARE-CNC Win.Trojan.Bisonha variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27805
static inline int check_rule_27805(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['GET /3001",fast_pattern', '0000000000000000000000000']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27805
static inline void alert_rule_27805(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27805;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bisonha variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bisonha variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27865
// Rule Name: EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27865
static inline int check_rule_27865(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php",fast_pattern,nocase', 'GET', '!"PacketShaper', '!"siteadvisor.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27865
static inline void alert_rule_27865(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27865;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page request", 
                     sizeof("EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27866
// Rule Name: EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27866
static inline int check_rule_27866(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: <body><b></b><style>div{overflow|3A|hidden|3B|width|3A|1px|3B|height|3A|1px}</style><div>",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27866
static inline void alert_rule_27866(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27866;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page", 
                     sizeof("EXPLOIT-KIT Blackholev2/Darkleech exploit kit landing page"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27902
// Rule Name: PROTOCOL-VOIP Possible SIP OPTIONS service information gathering attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 27902
static inline int check_rule_27902(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SIP_SERVERS (destination)
    // Variable port check: $SIP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: SIP/2.0",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27902
static inline void alert_rule_27902(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27902;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-VOIP Possible SIP OPTIONS service information gathering attempt", 
                     sizeof("PROTOCOL-VOIP Possible SIP OPTIONS service information gathering attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27903
// Rule Name: PROTOCOL-VOIP Ghost call attack attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 27903
static inline int check_rule_27903(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SIP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $SIP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: SIP/2.0",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27903
static inline void alert_rule_27903(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27903;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-VOIP Ghost call attack attempt", 
                     sizeof("PROTOCOL-VOIP Ghost call attack attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27904
// Rule Name: PROTOCOL-VOIP Excessive number of SIP 4xx responses potential user or password guessing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 27904
static inline int check_rule_27904(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $SIP_SERVERS (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $SIP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: SIP/2.0",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27904
static inline void alert_rule_27904(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27904;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-VOIP Excessive number of SIP 4xx responses potential user or password guessing attempt", 
                     sizeof("PROTOCOL-VOIP Excessive number of SIP 4xx responses potential user or password guessing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27907
// Rule Name: EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27907
static inline int check_rule_27907(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', ' Java/1.",fast_pattern,nocase', '.php?']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27907
static inline void alert_rule_27907(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27907;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27913
// Rule Name: PUA-ADWARE Vittalia adware - get ads
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27913
static inline int check_rule_27913(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/afr.php?zoneid=', '/ads/ox.html']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27913
static inline void alert_rule_27913(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27913;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Vittalia adware - get ads", 
                     sizeof("PUA-ADWARE Vittalia adware - get ads"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27914
// Rule Name: PUA-ADWARE Vittalia adware - post install
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27914
static inline int check_rule_27914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/report.php?key=', 'User-Agent|3A| NSIS_ToolkitOffers (Mozilla)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27914
static inline void alert_rule_27914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Vittalia adware - post install", 
                     sizeof("PUA-ADWARE Vittalia adware - post install"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27915
// Rule Name: PUA-ADWARE Vittalia adware outbound connection - pre install
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27915
static inline int check_rule_27915(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/instapi.php?idMk=', '&state=",distance 0', '&idTime=",distance 0', '&idA2=",distance 0', '&xVal=",distance 0', 'User-Agent|3A| NSIS_ToolkitOffers (Mozilla)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27915
static inline void alert_rule_27915(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27915;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Vittalia adware outbound connection - pre install", 
                     sizeof("PUA-ADWARE Vittalia adware outbound connection - pre install"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27916
// Rule Name: PUA-TOOLBARS Vittalia adware outbound connection - Eazel toolbar install
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27916
static inline int check_rule_27916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/utilsbar/EazelBar.exe', 'User-Agent|3A| NSIS_ToolkitOffers (Mozilla)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27916
static inline void alert_rule_27916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-TOOLBARS Vittalia adware outbound connection - Eazel toolbar install", 
                     sizeof("PUA-TOOLBARS Vittalia adware outbound connection - Eazel toolbar install"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27917
// Rule Name: PUA-TOOLBARS Vittalia adware outbound connection - offers
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27917
static inline int check_rule_27917(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/listener.php', 'User-Agent|3A| NSIS_ToolkitOffers (Mozilla)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27917
static inline void alert_rule_27917(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27917;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-TOOLBARS Vittalia adware outbound connection - offers", 
                     sizeof("PUA-TOOLBARS Vittalia adware outbound connection - offers"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27918
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27918
static inline int check_rule_27918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe HTTP/1.0|0D 0A|Host:",fast_pattern,nocase', 'Accept-Encoding: identity, *|3B|q=0|0D 0A|', '|3B| MSIE ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27918
static inline void alert_rule_27918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27919
// Rule Name: MALWARE-CNC Win.Trojan.Zeus encrypted POST Data exfiltration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27919
static inline int check_rule_27919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept-Encoding|3A| identity, *|3B|q=0|0D 0A|",fast_pattern,nocase', '|3B| MSIE ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27919
static inline void alert_rule_27919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus encrypted POST Data exfiltration", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus encrypted POST Data exfiltration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27964
// Rule Name: MALWARE-CNC Win.Trojan.Gh0st variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27964
static inline int check_rule_27964(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Gh0st",depth 5', '|00 00 00|",within 3,distance 1', '|00 00 78 9C|",within 4,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27964
static inline void alert_rule_27964(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27964;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gh0st variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gh0st variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27965
// Rule Name: MALWARE-CNC Win.Trojan.Eupuds variant connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27965
static inline int check_rule_27965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: insert into avs (id, pc,data,ref,country , id_user, mostrar)values(",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27965
static inline void alert_rule_27965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Eupuds variant connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Eupuds variant connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27966
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27966
static inline int check_rule_27966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', '=Response",nocase', 'FromBase64String",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27966
static inline void alert_rule_27966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27967
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27967
static inline int check_rule_27967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', 'caidao=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27967
static inline void alert_rule_27967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 27968
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 27968
static inline int check_rule_27968(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', '=Execute",nocase', 'On+Error+Resume+Next:",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 27968
static inline void alert_rule_27968(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 27968;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28005
// Rule Name: MALWARE-CNC Win.Trojan.Kuluoz outbound command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28005
static inline int check_rule_28005(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?', '-dsafe_mode",distance 0', '-ddisable_functions",distance 0', '-dallow_url_fopen",distance 0', '-dallow_url_include",distance 0', '-dauto_prepend_file",distance 0', 'echo.txt']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28005
static inline void alert_rule_28005(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28005;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluoz outbound command", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluoz outbound command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28006
// Rule Name: MALWARE-OTHER Win.Trojan.Kuluoz outbound download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28006
static inline int check_rule_28006(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ?message=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28006
static inline void alert_rule_28006(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28006;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Kuluoz outbound download request", 
                     sizeof("MALWARE-OTHER Win.Trojan.Kuluoz outbound download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28007
// Rule Name: MALWARE-CNC BLYPT installer startupkey outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28007
static inline int check_rule_28007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.aspx?info=startupkey_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28007
static inline void alert_rule_28007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BLYPT installer startupkey outbound traffic", 
                     sizeof("MALWARE-CNC BLYPT installer startupkey outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28008
// Rule Name: MALWARE-CNC BLYPT installer reuse outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28008
static inline int check_rule_28008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.aspx?info=reuse",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28008
static inline void alert_rule_28008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BLYPT installer reuse outbound traffic", 
                     sizeof("MALWARE-CNC BLYPT installer reuse outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28009
// Rule Name: MALWARE-CNC BLYPT installer configkey outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28009
static inline int check_rule_28009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.aspx?info=configkey",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28009
static inline void alert_rule_28009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BLYPT installer configkey outbound traffic", 
                     sizeof("MALWARE-CNC BLYPT installer configkey outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28010
// Rule Name: MALWARE-CNC BLYPT installer tserror outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28010
static inline int check_rule_28010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.aspx?info=tserror_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28010
static inline void alert_rule_28010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BLYPT installer tserror outbound traffic", 
                     sizeof("MALWARE-CNC BLYPT installer tserror outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28011
// Rule Name: MALWARE-CNC BLYPT installer createproc outbound traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28011
static inline int check_rule_28011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.aspx?info=createproc_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28011
static inline void alert_rule_28011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC BLYPT installer createproc outbound traffic", 
                     sizeof("MALWARE-CNC BLYPT installer createproc outbound traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28012
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28012
static inline int check_rule_28012(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: from=%20Nome..:",depth 15
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28012
static inline void alert_rule_28012(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28012;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28026
// Rule Name: EXPLOIT-KIT Blackholev2 exploit kit landing page
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28026
static inline int check_rule_28026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: </div><i></i><style>div{overflow|3A|hidden|3B|width|3A|1px|3B|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28026
static inline void alert_rule_28026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2 exploit kit landing page", 
                     sizeof("EXPLOIT-KIT Blackholev2 exploit kit landing page"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28028
// Rule Name: EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28028
static inline int check_rule_28028(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', ' Java/1.",fast_pattern,nocase', '.php?']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28028
static inline void alert_rule_28028(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28028;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt", 
                     sizeof("EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28033
// Rule Name: MALWARE-CNC Win.Ransomware.Urausy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28033
static inline int check_rule_28033(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Opera/10.80 |28|Windows NT 5.1|3B| U|3B| Edition Yx|3B| en|29| Presto/2.9.168 Version/11.52|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28033
static inline void alert_rule_28033(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28033;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Urausy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.Urausy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28042
// Rule Name: MALWARE-CNC Win.Trojan.Caphaw variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28042
static inline int check_rule_28042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ping.html?r=",fast_pattern,nocase', '!"/utils/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28042
static inline void alert_rule_28042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Caphaw variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Caphaw variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28044
// Rule Name: MALWARE-CNC Win.Trojan.CryptoLocker variant connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28044
static inline int check_rule_28044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /crypt_1_sell",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28044
static inline void alert_rule_28044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CryptoLocker variant connection", 
                     sizeof("MALWARE-CNC Win.Trojan.CryptoLocker variant connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28079
// Rule Name: MALWARE-CNC Win.Trojan.Napolar variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28079
static inline int check_rule_28079(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'v=', '|26|u=",within 3,distance 3', '|26|c=",distance 0', '|26|s={",distance 0', '}|26|w=",within 4,distance 36']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28079
static inline void alert_rule_28079(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28079;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Napolar variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Napolar variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28080
// Rule Name: MALWARE-CNC Win.Trojan.Napolar data theft
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28080
static inline int check_rule_28080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe&h=",fast_pattern,nocase', 'p=",depth 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28080
static inline void alert_rule_28080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Napolar data theft", 
                     sizeof("MALWARE-CNC Win.Trojan.Napolar data theft"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28105
// Rule Name: MALWARE-CNC Win.Trojan.Banload variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28105
static inline int check_rule_28105(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /v22/mutabixa/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28105
static inline void alert_rule_28105(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28105;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28106
// Rule Name: MALWARE-CNC Win.Trojan.Banload information upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28106
static inline int check_rule_28106(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/v22/mutabixa/1nf3ct/', 'chave=",distance 0', '&url=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28106
static inline void alert_rule_28106(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28106;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload information upload", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload information upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28107
// Rule Name: MALWARE-CNC Win.Trojan.Banload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28107
static inline int check_rule_28107(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.jpg', 'User-Agent|3A| runddll32.exe",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28107
static inline void alert_rule_28107(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28107;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload download", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28114
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /default.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28114
static inline int check_rule_28114(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/default.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28114
static inline void alert_rule_28114(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28114;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /default.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /default.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28115
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /file.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28115
static inline int check_rule_28115(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/file.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28115
static inline void alert_rule_28115(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28115;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /file.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /file.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28116
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /home.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28116
static inline int check_rule_28116(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/home.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28116
static inline void alert_rule_28116(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28116;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /home.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /home.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28117
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /install.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28117
static inline int check_rule_28117(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/install.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28117
static inline void alert_rule_28117(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28117;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /install.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /install.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28118
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /login.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28118
static inline int check_rule_28118(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/login.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28118
static inline void alert_rule_28118(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28118;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /login.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /login.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28119
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /search.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28119
static inline int check_rule_28119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/search.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28119
static inline void alert_rule_28119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /search.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /search.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28120
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /start.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28120
static inline int check_rule_28120(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/start.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28120
static inline void alert_rule_28120(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28120;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /start.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /start.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28121
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /welcome.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28121
static inline int check_rule_28121(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/welcome.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28121
static inline void alert_rule_28121(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28121;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /welcome.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /welcome.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28122
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /index.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28122
static inline int check_rule_28122(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/index.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28122
static inline void alert_rule_28122(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28122;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /index.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /index.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28123
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /setup.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28123
static inline int check_rule_28123(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/setup.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28123
static inline void alert_rule_28123(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28123;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /setup.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /setup.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28147
// Rule Name: MALWARE-CNC Win.Trojan.Conficker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28147
static inline int check_rule_28147(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search?q=",fast_pattern,nocase', 'User-Agent: Mozilla/4.0 (compatible|3B| MSIE ', ': no-cache|0D 0A 0D 0A|', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28147
static inline void alert_rule_28147(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28147;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Conficker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Conficker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28148
// Rule Name: MALWARE-CNC Win.Trojan.Mevade variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28148
static inline int check_rule_28148(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|uuid: ",fast_pattern,nocase', '!"User-Agent:', 'Content-Type|3A| binary/octet-stream|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28148
static inline void alert_rule_28148(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28148;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Mevade variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Mevade variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28153
// Rule Name: MALWARE-CNC Win.Trojan.Foreign variant outbound connection - /html2/
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28153
static inline int check_rule_28153(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/html2/",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28153
static inline void alert_rule_28153(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28153;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Foreign variant outbound connection - /html2/", 
                     sizeof("MALWARE-CNC Win.Trojan.Foreign variant outbound connection - /html2/"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28154
// Rule Name: MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.1
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28154
static inline int check_rule_28154(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '|3B| MSIE 7.1|3B 20|",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28154
static inline void alert_rule_28154(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28154;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.1", 
                     sizeof("MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.1"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28155
// Rule Name: MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.2
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28155
static inline int check_rule_28155(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '|3B| MSIE 7.2|3B 20|",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28155
static inline void alert_rule_28155(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28155;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.2", 
                     sizeof("MALWARE-CNC Win.Trojan.Foreign variant outbound connection - MSIE 7.2"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28156
// Rule Name: PUA-ADWARE Linkury outbound time check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28156
static inline int check_rule_28156(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /utc/now HTTP/1.1|0D 0A|Host: www.timeapi.org|0D 0A|Connection: Keep-Alive|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28156
static inline void alert_rule_28156(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28156;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Linkury outbound time check", 
                     sizeof("PUA-ADWARE Linkury outbound time check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28192
// Rule Name: MALWARE-CNC Win.Trojan.Kuluoz Potential Phishing URL
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28192
static inline int check_rule_28192(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/info.php?message=",fast_pattern,nocase', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28192
static inline void alert_rule_28192(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28192;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluoz Potential Phishing URL", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluoz Potential Phishing URL"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28215
// Rule Name: SERVER-WEBAPP vBulletin upgrade.php exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 28215
static inline int check_rule_28215(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['install/upgrade.php",fast_pattern,nocase', 'firstrun=false', '&customerid=', 'username%5d=', 'password%5d=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28215
static inline void alert_rule_28215(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28215;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP vBulletin upgrade.php exploit attempt", 
                     sizeof("SERVER-WEBAPP vBulletin upgrade.php exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28233
// Rule Name: EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28233
static inline int check_rule_28233(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' Java/1.",fast_pattern,nocase', '.php?']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28233
static inline void alert_rule_28233(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28233;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT Blackholev2/Cool exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28242
// Rule Name: MALWARE-CNC Win.Trojan.KanKan variant connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28242
static inline int check_rule_28242(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?u=",depth 4', '&u2=', '&u5=inststart', 'NSIS_Inetc (Mozilla)",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28242
static inline void alert_rule_28242(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28242;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.KanKan variant connection", 
                     sizeof("MALWARE-CNC Win.Trojan.KanKan variant connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28255
// Rule Name: MALWARE-CNC Win.Trojan.Kuluoz Potential phishing URL
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28255
static inline int check_rule_28255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get.php?invite=",fast_pattern,nocase', 'Accept-Encoding: gzip', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28255
static inline void alert_rule_28255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluoz Potential phishing URL", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluoz Potential phishing URL"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28285
// Rule Name: MALWARE-CNC Win.Trojan.hdog connectivity check-in version 2
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28285
static inline int check_rule_28285(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?gws_rd=cr",fast_pattern,nocase', '|0D 0A|Connection: Close|0D 0A 0D 0A|', '|3B 20|MSIE|20|', '!"Accept-Encoding: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28285
static inline void alert_rule_28285(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28285;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.hdog connectivity check-in version 2", 
                     sizeof("MALWARE-CNC Win.Trojan.hdog connectivity check-in version 2"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28291
// Rule Name: EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28291
static inline int check_rule_28291(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' Java/1.",fast_pattern,nocase', '.php?']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28291
static inline void alert_rule_28291(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28291;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt", 
                     sizeof("EXPLOIT-KIT Blackholev2/Cool exploit kit exploit download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28300
// Rule Name: MALWARE-CNC Win.Trojan.Agent variant connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28300
static inline int check_rule_28300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/status/?&cmp=",fast_pattern', '&src=",distance 0', '&status=start",distance 0', '!"User-Agent: ', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28300
static inline void alert_rule_28300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent variant connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent variant connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28323
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28323
static inline int check_rule_28323(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', 'FromBase64String', 'z",within 200,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28323
static inline void alert_rule_28323(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28323;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28324
// Rule Name: PUA-ADWARE FakeAV runtime detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28324
static inline int check_rule_28324(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&affid=",fast_pattern,nocase', '/api/",nocase', '?ts=",nocase', '&token=",nocase', '&group=",nocase', '&nid=",nocase', '&lid=",nocase', '&ver=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28324
static inline void alert_rule_28324(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28324;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE FakeAV runtime detection", 
                     sizeof("PUA-ADWARE FakeAV runtime detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28344
// Rule Name: INDICATOR-OBFUSCATION large number of calls to chr function - possible sql injection obfuscation
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 28344
static inline int check_rule_28344(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', 'CHR(",nocase', 'CHR(",distance 0,nocase', 'CHR(",distance 0,nocase', 'CHR(",distance 0,nocase', 'CHR(",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28344
static inline void alert_rule_28344(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28344;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION large number of calls to chr function - possible sql injection obfuscation", 
                     sizeof("INDICATOR-OBFUSCATION large number of calls to chr function - possible sql injection obfuscation"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28345
// Rule Name: INDICATOR-OBFUSCATION Javascript obfuscation - split - seen in IFRAMEr Tool attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28345
static inline int check_rule_28345(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ps=|22|split|22 3B|asd=function()",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28345
static inline void alert_rule_28345(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28345;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION Javascript obfuscation - split - seen in IFRAMEr Tool attack", 
                     sizeof("INDICATOR-OBFUSCATION Javascript obfuscation - split - seen in IFRAMEr Tool attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28346
// Rule Name: INDICATOR-OBFUSCATION Javascript obfuscation - seen in IFRAMEr Tool attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28346
static inline int check_rule_28346(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: aq=|22|0x|22 3B|ff=String|3B|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28346
static inline void alert_rule_28346(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28346;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION Javascript obfuscation - seen in IFRAMEr Tool attack", 
                     sizeof("INDICATOR-OBFUSCATION Javascript obfuscation - seen in IFRAMEr Tool attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28405
// Rule Name: MALWARE-CNC Win.Trojan.Kazy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28405
static inline int check_rule_28405(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/p.ashx?prd=",fast_pattern', '&pixGuid=",distance 0', '&ver=",distance 0', '&rnd=",distance 0', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28405
static inline void alert_rule_28405(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28405;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kazy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kazy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28406
// Rule Name: MALWARE-CNC Win.Trojan.Kazy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28406
static inline int check_rule_28406(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe HTTP/1.1|0D 0A|User-Agent: Mozilla/",fast_pattern,nocase', '|3B| MSIE ', '!"Accept', '|29 0D 0A|Host: ",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28406
static inline void alert_rule_28406(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28406;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kazy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kazy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28420
// Rule Name: INDICATOR-OBFUSCATION Javascript obfuscation - createElement - seen in IFRAMEr Tool attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28420
static inline int check_rule_28420(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |22|c|22|+|22|r|22 3A|2+|22|e|22|+|22|a|22|+|22|t|22|+|22|e|22|+|22|E|22|+|22|l|22|+|22|e|22|+|22|m|22|+((f)?|22|e|22|+|22|n|22|+|22|t|22 3A 22 22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28420
static inline void alert_rule_28420(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28420;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION Javascript obfuscation - createElement - seen in IFRAMEr Tool attack", 
                     sizeof("INDICATOR-OBFUSCATION Javascript obfuscation - createElement - seen in IFRAMEr Tool attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28421
// Rule Name: INDICATOR-OBFUSCATION Javascript obfuscation - fromCharCode - seen in IFRAMEr Tool attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28421
static inline int check_rule_28421(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |22|fr|22|+|22|omCh|22|+|22|arCo|22|+|22|de|22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28421
static inline void alert_rule_28421(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28421;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-OBFUSCATION Javascript obfuscation - fromCharCode - seen in IFRAMEr Tool attack", 
                     sizeof("INDICATOR-OBFUSCATION Javascript obfuscation - fromCharCode - seen in IFRAMEr Tool attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28428
// Rule Name: EXPLOIT-KIT Glazunov exploit kit landing page
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28428
static inline int check_rule_28428(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['= |22|applet|22 3B 20|', '= |22|object|22 3B 20|",within 50', '=|27|param|27 3B 20|",within 50', '.zip|27 3B| </script>",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28428
static inline void alert_rule_28428(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28428;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Glazunov exploit kit landing page", 
                     sizeof("EXPLOIT-KIT Glazunov exploit kit landing page"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28429
// Rule Name: EXPLOIT-KIT Glazunov exploit kit outbound jnlp download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28429
static inline int check_rule_28429(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.jnlp",fast_pattern', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28429
static inline void alert_rule_28429(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28429;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Glazunov exploit kit outbound jnlp download attempt", 
                     sizeof("EXPLOIT-KIT Glazunov exploit kit outbound jnlp download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28430
// Rule Name: EXPLOIT-KIT Glazunov exploit kit zip file download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28430
static inline int check_rule_28430(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.zip",fast_pattern', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28430
static inline void alert_rule_28430(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28430;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Glazunov exploit kit zip file download", 
                     sizeof("EXPLOIT-KIT Glazunov exploit kit zip file download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28446
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant SQL check-in
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28446
static inline int check_rule_28446(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 1039
    if (tcph->dest != htons(1039))
        return 0;
    // Content matching (simplified)
    // Full pattern: s|00|e|00|l|00|e|00|c|00|t|00| |00|v|00|e|00|r|00|i|00|f|00|i|00|c|00|a|00|n|00|d|00|o|00| |00|f|00|r|00|o|00|m|00| |00|v|00|e|00|r|00|i|00|f|00|i|00|c|00|a|00|n|00|d|00|o|00| |00|w|00|h|00|e|00|r|00|e|00| |00|i|00|d|00|_|00|p|00|c|00|=|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28446
static inline void alert_rule_28446(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28446;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant SQL check-in", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant SQL check-in"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28450
// Rule Name: EXPLOIT-KIT Sakura exploit kit exploit payload retrieve attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28450
static inline int check_rule_28450(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.ld",fast_pattern,nocase', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28450
static inline void alert_rule_28450(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28450;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Sakura exploit kit exploit payload retrieve attempt", 
                     sizeof("EXPLOIT-KIT Sakura exploit kit exploit payload retrieve attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28493
// Rule Name: MALWARE-CNC DeputyDog diskless method outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28493
static inline int check_rule_28493(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: lynx|0D 0A|",fast_pattern,nocase', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28493
static inline void alert_rule_28493(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28493;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC DeputyDog diskless method outbound connection", 
                     sizeof("MALWARE-CNC DeputyDog diskless method outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28538
// Rule Name: MALWARE-CNC Win.Trojan.Asprox/Kuluoz variant connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28538
static inline int check_rule_28538(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| WOW64|3B| rv:23.0) Gecko/20100101 Firefox/23.0', 'Content-Disposition: form-data|3B| name=|22|key|22 3B| filename=|22|key.bin|22|",fast_pattern,nocase', 'Content-Disposition: form-data|3B| name=|22|data|22 3B| filename=|22|data.bin|22|', 'Content-Type: multipart/form-data|3B| boundary=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28538
static inline void alert_rule_28538(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28538;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Asprox/Kuluoz variant connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Asprox/Kuluoz variant connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28541
// Rule Name: MALWARE-CNC Win.Trojan.ZeroAccess Download Headers
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28541
static inline int check_rule_28541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|Accept: */*|0D 0A|Accept-Encoding: identity, *|3B|q=0|0D 0A|Connection: close|0D 0A|User-Agent: ",fast_pattern,nocase', '.exe HTTP/1.0|0D 0A|Host: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28541
static inline void alert_rule_28541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ZeroAccess Download Headers", 
                     sizeof("MALWARE-CNC Win.Trojan.ZeroAccess Download Headers"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28542
// Rule Name: MALWARE-CNC Win.Trojan.Conficker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28542
static inline int check_rule_28542(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1|0D 0A|User-Agent: Mozilla/4.0 (compatible|3B| MSIE 7.0|3B| Windows NT 5.1|3B| Trident/4.0)|0D 0A|Host: checkip.dyndns.org|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28542
static inline void alert_rule_28542(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28542;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Conficker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Conficker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28543
// Rule Name: MALWARE-CNC Win.Trojan.Conficker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28543
static inline int check_rule_28543(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1|0D 0A|User-Agent: Mozilla/4.0 (compatible|3B| MSIE 7.0|3B| Windows NT 5.1|3B| Trident/4.0)|0D 0A|Host: www.ask.com|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28543
static inline void alert_rule_28543(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28543;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Conficker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Conficker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28553
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /main.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28553
static inline int check_rule_28553(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/main.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28553
static inline void alert_rule_28553(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28553;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /main.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /main.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28554
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /online.htm GET Encrypted Payload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28554
static inline int check_rule_28554(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/online.htm",fast_pattern,nocase', '!"Referer', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28554
static inline void alert_rule_28554(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28554;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /online.htm GET Encrypted Payload", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection - /online.htm GET Encrypted Payload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28596
// Rule Name: EXPLOIT-KIT Nuclear exploit kit payload request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28596
static inline int check_rule_28596(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /f/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28596
static inline void alert_rule_28596(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28596;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Nuclear exploit kit payload request", 
                     sizeof("EXPLOIT-KIT Nuclear exploit kit payload request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28795
// Rule Name: EXPLOIT-KIT Goon/Infinity exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28795
static inline int check_rule_28795(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.mp3",fast_pattern,nocase', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28795
static inline void alert_rule_28795(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28795;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Goon/Infinity exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT Goon/Infinity exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28800
// Rule Name: MALWARE-CNC Win.Trojan.Zeus outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28800
static inline int check_rule_28800(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['GET / HTTP/1.1|0D 0A|Accept: */*|0D 0A|Accept-Language:",depth 45', '|0D 0A|Connection: Close|0D 0A 0D 0A|",fast_pattern', 'google.com|0D 0A|', '|3B 20|MSIE|20|', '!"Accept-Encoding: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28800
static inline void alert_rule_28800(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28800;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28802
// Rule Name: MALWARE-CNC Win.Trojan.Bancos outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28802
static inline int check_rule_28802(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['ip-who-is.com|0D 0A|",fast_pattern,nocase', '/locate-ip/",depth 11', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28802
static inline void alert_rule_28802(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28802;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28803
// Rule Name: MALWARE-CNC Win.Trojan.Injector inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28803
static inline int check_rule_28803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: UPDATE|7C|",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28803
static inline void alert_rule_28803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28804
// Rule Name: MALWARE-CNC Win.Trojan.Injector outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28804
static inline int check_rule_28804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A 0D 0A|&nome=",fast_pattern,nocase', 'conteudo=",depth 9']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28804
static inline void alert_rule_28804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28806
// Rule Name: INDICATOR-COMPROMISE potential malware download - single digit .exe file download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28806
static inline int check_rule_28806(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28806
static inline void alert_rule_28806(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28806;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE potential malware download - single digit .exe file download", 
                     sizeof("INDICATOR-COMPROMISE potential malware download - single digit .exe file download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28807
// Rule Name: MALWARE-CNC Win.Trojan.Injector variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28807
static inline int check_rule_28807(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/load.exe HTTP/1.1|0D 0A|User-Agent: Mozilla/",fast_pattern,nocase', '|3B 20|MSIE|20|', ')|0D 0A|Host: ",distance 0', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28807
static inline void alert_rule_28807(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28807;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28809
// Rule Name: MALWARE-CNC Win.Trojan.Dofoil inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28809
static inline int check_rule_28809(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |3B 20|filename=exe.exe|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28809
static inline void alert_rule_28809(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28809;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dofoil inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dofoil inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28810
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28810
static inline int check_rule_28810(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|2F|', '!"|0A|Referer|3A|', '!"|0A|Cookie|3A|', '|3B 20|MSIE|20|7.0|3B 20|', '|2E|biz|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28810
static inline void alert_rule_28810(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28810;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection - MSIE7 No Referer No Cookie"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28814
// Rule Name: MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28814
static inline int check_rule_28814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/post.aspx?forumID=",fast_pattern,nocase', '|0D 0A|URL: http",depth 11,offset 17', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28814
static inline void alert_rule_28814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28815
// Rule Name: MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28815
static inline int check_rule_28815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['forumdisplay.php?fid=",fast_pattern,nocase', 'id=",depth 3', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28815
static inline void alert_rule_28815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gozi/Neverquest variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28817
// Rule Name: MALWARE-CNC Win.Backdoor.Iniduoh variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28817
static inline int check_rule_28817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/is-ready",fast_pattern,nocase', 'User|2D|Agent|3A 20|', '|3C 7C 3E|",within 3,distance 8', '|3C 7C 3E|",within 18', '|3C 7C 3E|Microsoft Windows",within 84']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28817
static inline void alert_rule_28817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Iniduoh variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Iniduoh variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28852
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Linux.Trojan.Zollard
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28852
static inline int check_rule_28852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Zollard|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28852
static inline void alert_rule_28852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Linux.Trojan.Zollard", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Linux.Trojan.Zollard"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28859
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent z00sAgent - Win.Trojan.Zbot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28859
static inline int check_rule_28859(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| z00sAgent",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28859
static inline void alert_rule_28859(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28859;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent z00sAgent - Win.Trojan.Zbot", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent z00sAgent - Win.Trojan.Zbot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28913
// Rule Name: MALWARE-BACKDOOR Zollard variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28913
static inline int check_rule_28913(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 58455
    if (tcph->dest != htons(58455))
        return 0;
    // Content matching (simplified)
    // Full pattern: .zollard/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28913
static inline void alert_rule_28913(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28913;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Zollard variant outbound connection attempt", 
                     sizeof("MALWARE-BACKDOOR Zollard variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28918
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant network connectivity check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28918
static inline int check_rule_28918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: Host: bit.ly|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28918
static inline void alert_rule_28918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant network connectivity check", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant network connectivity check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28919
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant network connectivity check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28919
static inline int check_rule_28919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: Host: bitly.com|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28919
static inline void alert_rule_28919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant network connectivity check", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant network connectivity check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28930
// Rule Name: MALWARE-CNC Win.Trojan.Fakeav variant outbound data connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28930
static inline int check_rule_28930(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?",depth 2', 'Firefox/4.0b8pre|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28930
static inline void alert_rule_28930(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28930;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fakeav variant outbound data connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fakeav variant outbound data connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28940
// Rule Name: MALWARE-CNC Win.Trojan.Rovnix malicious download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28940
static inline int check_rule_28940(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/config.php?",fast_pattern,nocase', 'version=', 'user=', 'server=', 'id=', 'crc=', 'id=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28940
static inline void alert_rule_28940(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28940;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rovnix malicious download", 
                     sizeof("MALWARE-CNC Win.Trojan.Rovnix malicious download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28945
// Rule Name: INDICATOR-COMPROMISE exe.exe download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28945
static inline int check_rule_28945(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /exe.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28945
static inline void alert_rule_28945(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28945;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE exe.exe download", 
                     sizeof("INDICATOR-COMPROMISE exe.exe download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28960
// Rule Name: MALWARE-CNC Win.Trojan.Alurewo outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28960
static inline int check_rule_28960(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cmd?version=",fast_pattern,nocase', '&aid=', '&id=",distance 0', '&os=",within 4,distance 36']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28960
static inline void alert_rule_28960(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28960;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Alurewo outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Alurewo outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28969
// Rule Name: EXPLOIT-KIT HiMan exploit kit outbound payload retreival - specific string
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28969
static inline int check_rule_28969(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /tx.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28969
static inline void alert_rule_28969(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28969;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT HiMan exploit kit outbound payload retreival - specific string", 
                     sizeof("EXPLOIT-KIT HiMan exploit kit outbound payload retreival - specific string"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28976
// Rule Name: MALWARE-CNC Win.Trojan.Agent.DF - Data Exfiltration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28976
static inline int check_rule_28976(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B| name=|22|arquivo|22 3B| filename=|22|C:|5C|",fast_pattern,nocase', '|0D 0A|TP=', '|0D 0A|LGSN=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28976
static inline void alert_rule_28976(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28976;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent.DF - Data Exfiltration", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent.DF - Data Exfiltration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28977
// Rule Name: MALWARE-CNC Win.Trojan.Agent.DF - User-Agent Missing Bracket
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28977
static inline int check_rule_28977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|Windows NT 5.0|0D 0A|Host:",fast_pattern,nocase', ' HTTP/1.1|0D 0A|Connection: Keep-Alive|0D 0A|Accept: */*|0D 0A|User-Agent: Mozilla/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28977
static inline void alert_rule_28977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent.DF - User-Agent Missing Bracket", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent.DF - User-Agent Missing Bracket"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28982
// Rule Name: MALWARE-CNC Win.Worm.Steckt IRCbot requesting URL through IRC
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28982
static inline int check_rule_28982(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['JOIN |3A|#', '!dl http://",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28982
static inline void alert_rule_28982(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28982;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Steckt IRCbot requesting URL through IRC", 
                     sizeof("MALWARE-CNC Win.Worm.Steckt IRCbot requesting URL through IRC"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28983
// Rule Name: MALWARE-CNC Win.Trojan.Steckt IRCbot executable download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28983
static inline int check_rule_28983(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| Mozilla/4.0 |28|compatible|29 0D 0A|",fast_pattern,nocase', '/launch.php', '?f=', '&s=",distance 0', '&is_direct=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28983
static inline void alert_rule_28983(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28983;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Steckt IRCbot executable download", 
                     sizeof("MALWARE-CNC Win.Trojan.Steckt IRCbot executable download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28984
// Rule Name: MALWARE-CNC Win.Worm.Steckt IRCbot executable download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28984
static inline int check_rule_28984(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| Mozilla/4.0 |28|compatible|29 0D 0A|",fast_pattern,nocase', '/direct.php', '?f=', '&s=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28984
static inline void alert_rule_28984(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28984;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Steckt IRCbot executable download", 
                     sizeof("MALWARE-CNC Win.Worm.Steckt IRCbot executable download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28985
// Rule Name: MALWARE-CNC Win.Worm.Steckt IRCbot executable download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28985
static inline int check_rule_28985(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/site2/', '!"Referer|3A| ', '60gp=', '60gpBAK=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28985
static inline void alert_rule_28985(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28985;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Steckt IRCbot executable download", 
                     sizeof("MALWARE-CNC Win.Worm.Steckt IRCbot executable download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28986
// Rule Name: MALWARE-CNC Win.Worm.Neeris IRCbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28986
static inline int check_rule_28986(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: JOIN #biz abc|0D 0A|",depth 15
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28986
static inline void alert_rule_28986(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28986;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Neeris IRCbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Neeris IRCbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28987
// Rule Name: MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28987
static inline int check_rule_28987(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: JOIN #n jobs|0D 0A|",depth 14
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28987
static inline void alert_rule_28987(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28987;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 28988
// Rule Name: MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 28988
static inline int check_rule_28988(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: JOIN #test1|20 7C 0D 0A|JOIN #test2|20 7C 0D 0A|JOIN #test3 (null)|0D 0A|",depth 50
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 28988
static inline void alert_rule_28988(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 28988;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Steckt IRCbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29031
// Rule Name: MALWARE-CNC Win.Trojan.Banload variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29031
static inline int check_rule_29031(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/avcheck.exe|0D 0A 0D 0A|",fast_pattern,nocase', '|0D 0A|Location: https://dl.dropboxusercontent.com/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29031
static inline void alert_rule_29031(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29031;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29127
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29127
static inline int check_rule_29127(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webstat/?i=",depth 12,fast_pattern', 'User-Agent: Mozilla/7', '|3B 20|MSIE|20|",distance 0', '!"Accept-Encoding:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29127
static inline void alert_rule_29127(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29127;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29166
// Rule Name: EXPLOIT-KIT CritX exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29166
static inline int check_rule_29166(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /loadmsie.php?id=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29166
static inline void alert_rule_29166(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29166;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT CritX exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT CritX exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29167
// Rule Name: EXPLOIT-KIT CritX exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29167
static inline int check_rule_29167(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe|0D 0A|",fast_pattern,nocase', 'filename=', '.exe|0D 0A|",within 6,distance 24']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29167
static inline void alert_rule_29167(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29167;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT CritX exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT CritX exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29174
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string fortis
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29174
static inline int check_rule_29174(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: fortis|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29174
static inline void alert_rule_29174(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29174;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string fortis", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string fortis"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29189
// Rule Name: EXPLOIT-KIT Magnitude exploit kit Microsoft Internet Explorer Payload request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29189
static inline int check_rule_29189(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?",depth 2,fast_pattern', ' MSIE ', '!"Referer|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29189
static inline void alert_rule_29189(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29189;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Magnitude exploit kit Microsoft Internet Explorer Payload request", 
                     sizeof("EXPLOIT-KIT Magnitude exploit kit Microsoft Internet Explorer Payload request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29216
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29216
static inline int check_rule_29216(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/se/gate.php', 'HTTP/1.1|0D 0A|Cache-Control: no-cache|0D 0A|Connection: close|0D 0A|Pragma: no-cache|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|User-Agent: Mozilla/4.0|0D 0A|Content-Length: ",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29216
static inline void alert_rule_29216(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29216;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29220
// Rule Name: MALWARE-CNC Win.Trojan.Strictor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29220
static inline int check_rule_29220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mod/lookfashon.jpg",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29220
static inline void alert_rule_29220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Strictor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Strictor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29259
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29259
static inline int check_rule_29259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /chamjavanv.inf?aapf/login.jsp?=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29259
static inline void alert_rule_29259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29260
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29260
static inline int check_rule_29260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /novredir_inf.php?apt/login.jsp?=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29260
static inline void alert_rule_29260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29261
// Rule Name: MALWARE-CNC Win.Trojan.Dropper variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29261
static inline int check_rule_29261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/FileToDownload.exe",fast_pattern,nocase', 'Host: dl.dropbox.com|0D 0A|', '!"Accept', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29261
static inline void alert_rule_29261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29300
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29300
static inline int check_rule_29300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|3B 20|filename=CostcoForm.zip|0D 0A|",fast_pattern,nocase', 'CostcoForm.exe']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29300
static inline void alert_rule_29300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29349
// Rule Name: MALWARE-CNC Win.Trojan.Zusy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29349
static inline int check_rule_29349(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['rotina=UPDATE&tip=stat&nome=",depth 28,fast_pattern', '&tmp=",distance 0', '&stat=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29349
static inline void alert_rule_29349(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29349;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zusy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zusy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29378
// Rule Name: MALWARE-CNC Win.Trojan.Dropper inbound encrypted traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29378
static inline int check_rule_29378(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |05 29 00 00 00 05 29 00 00 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29378
static inline void alert_rule_29378(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29378;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper inbound encrypted traffic", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper inbound encrypted traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29379
// Rule Name: MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic - potential exfiltration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29379
static inline int check_rule_29379(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |03 2B 82 86 02 A0 05|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29379
static inline void alert_rule_29379(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29379;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic - potential exfiltration", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic - potential exfiltration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29380
// Rule Name: MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29380
static inline int check_rule_29380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |05 29 00 00 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29380
static inline void alert_rule_29380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper outbound encrypted traffic"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29384
// Rule Name: FILE-IDENTIFY Adobe AIR file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 29384
static inline int check_rule_29384(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .air",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29384
static inline void alert_rule_29384(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29384;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Adobe AIR file download request", 
                     sizeof("FILE-IDENTIFY Adobe AIR file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29385
// Rule Name: FILE-IDENTIFY Adobe AIR file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 29385
static inline int check_rule_29385(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.air",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29385
static inline void alert_rule_29385(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29385;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Adobe AIR file attachment detected", 
                     sizeof("FILE-IDENTIFY Adobe AIR file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29386
// Rule Name: FILE-IDENTIFY Adobe AIR file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 29386
static inline int check_rule_29386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.air",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29386
static inline void alert_rule_29386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Adobe AIR file attachment detected", 
                     sizeof("FILE-IDENTIFY Adobe AIR file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29395
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29395
static inline int check_rule_29395(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/webhp HTTP/1.1|0D 0A|Accept: */*|0D 0A|Connection: Close|0D 0A|User-Agent: Mozilla/4.0 (",fast_pattern,nocase', '|3B| MSIE ', 'google.', '!"Accept-', 'NID=",depth 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29395
static inline void alert_rule_29395(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29395;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29396
// Rule Name: POLICY-SPAM Potential phishing attack - .zip receipt filename download with .exe name within .zip the same 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29396
static inline int check_rule_29396(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Receipt",fast_pattern,nocase', '.zip', 'PK",depth 2', '.exe",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29396
static inline void alert_rule_29396(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29396;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SPAM Potential phishing attack - .zip receipt filename download with .exe name within .zip the same ", 
                     sizeof("POLICY-SPAM Potential phishing attack - .zip receipt filename download with .exe name within .zip the same "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29397
// Rule Name: POLICY-SPAM Potential phishing attack - .zip shipping filename download with .exe name within .zip the same 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29397
static inline int check_rule_29397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Shipping",fast_pattern,nocase', '.zip', 'PK",depth 2', '.exe",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29397
static inline void alert_rule_29397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SPAM Potential phishing attack - .zip shipping filename download with .exe name within .zip the same ", 
                     sizeof("POLICY-SPAM Potential phishing attack - .zip shipping filename download with .exe name within .zip the same "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29398
// Rule Name: POLICY-SPAM Potential phishing attack - .zip voicemail filename download with .exe name within .zip the same 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29398
static inline int check_rule_29398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['voicemail",fast_pattern,nocase', '.zip', 'PK",depth 2', '.exe",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29398
static inline void alert_rule_29398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SPAM Potential phishing attack - .zip voicemail filename download with .exe name within .zip the same ", 
                     sizeof("POLICY-SPAM Potential phishing attack - .zip voicemail filename download with .exe name within .zip the same "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29399
// Rule Name: POLICY-SPAM Potential phishing attack - .zip statement filename download with .exe name within .zip the same 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29399
static inline int check_rule_29399(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['statement",fast_pattern,nocase', '.zip', 'PK",depth 2', '.exe",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29399
static inline void alert_rule_29399(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29399;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-SPAM Potential phishing attack - .zip statement filename download with .exe name within .zip the same ", 
                     sizeof("POLICY-SPAM Potential phishing attack - .zip statement filename download with .exe name within .zip the same "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29459
// Rule Name: MALWARE-CNC Win.Trojan.Fexel variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29459
static inline int check_rule_29459(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|0A|Agtid|3A 20|', '08x|0D 0A|",within 5,distance 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29459
static inline void alert_rule_29459(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29459;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fexel variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fexel variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29569
// Rule Name: MALWARE-CNC Linux.Backdoor.Shellbot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29569
static inline int check_rule_29569(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['JOIN|20|#vnc|0A|",depth 10', 'PRIVMSG|20|#vnc|20 3A|",within 14', 'status checking program online",within 30,distance 7,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29569
static inline void alert_rule_29569(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29569;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Backdoor.Shellbot outbound connection", 
                     sizeof("MALWARE-CNC Linux.Backdoor.Shellbot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29664
// Rule Name: MALWARE-CNC Win.Trojan.DomaIQ variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29664
static inline int check_rule_29664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/trace/Start HTTP/1.1|0D 0A|Host: ",fast_pattern,nocase', '/debug/Version/",depth 15', '!"Accept', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29664
static inline void alert_rule_29664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DomaIQ variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DomaIQ variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29665
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29665
static inline int check_rule_29665(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&bolausado",fast_pattern,nocase', 'rotina=",depth 7', '&casa=",distance 0', '&idcliente",distance 0', '&outro=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29665
static inline void alert_rule_29665(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29665;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29666
// Rule Name: MALWARE-CNC Win.Trojan.Linkup outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29666
static inline int check_rule_29666(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/uplink.php?logo.jpg",fast_pattern,nocase', 'User-Agent: Mozilla/5.0', 'token=",depth 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29666
static inline void alert_rule_29666(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29666;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Linkup outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Linkup outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29760
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string MSIE 4.01 - Win.Trojan.Careto
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29760
static inline int check_rule_29760(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Mozilla/4.0 |28|compatible|3B| MSIE 4.01|3B| Windows NT|29 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29760
static inline void alert_rule_29760(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29760;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string MSIE 4.01 - Win.Trojan.Careto", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string MSIE 4.01 - Win.Trojan.Careto"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29788
// Rule Name: MALWARE-CNC Win.Trojan.Careto outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29788
static inline int check_rule_29788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Group|3D|', 'Install|3D|', 'Ver|3D|', 'Ask|3D|', 'Bn|3D|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29788
static inline void alert_rule_29788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Careto outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Careto outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29789
// Rule Name: MALWARE-CNC Win.Trojan.Careto plugin download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29789
static inline int check_rule_29789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ag/plugin.crx",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29789
static inline void alert_rule_29789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Careto plugin download", 
                     sizeof("MALWARE-CNC Win.Trojan.Careto plugin download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29790
// Rule Name: MALWARE-CNC Win.Trojan.Careto plugin download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29790
static inline int check_rule_29790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /l/af_l_addon.xpi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29790
static inline void alert_rule_29790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Careto plugin download", 
                     sizeof("MALWARE-CNC Win.Trojan.Careto plugin download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29791
// Rule Name: MALWARE-CNC Win.Trojan.Careto plugin download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29791
static inline int check_rule_29791(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /m/f_l_addon.xpi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29791
static inline void alert_rule_29791(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29791;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Careto plugin download", 
                     sizeof("MALWARE-CNC Win.Trojan.Careto plugin download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29816
// Rule Name: MALWARE-CNC Win.Trojan.Jackpos outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29816
static inline int check_rule_29816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/post', 'User-Agent: something",fast_pattern,nocase', 'mac=', '&t1=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29816
static inline void alert_rule_29816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Jackpos outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Jackpos outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29817
// Rule Name: MALWARE-CNC Win.Trojan.Jackpos outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29817
static inline int check_rule_29817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/post/echo",fast_pattern,nocase', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29817
static inline void alert_rule_29817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Jackpos outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Jackpos outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29824
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - TixDll - Win.Trojan.Adload.dyhq
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29824
static inline int check_rule_29824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: TixDll|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29824
static inline void alert_rule_29824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - TixDll - Win.Trojan.Adload.dyhq", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - TixDll - Win.Trojan.Adload.dyhq"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29828
// Rule Name: MALWARE-CNC Win.Trojan.Adload.dyhq variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29828
static inline int check_rule_29828(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get/?ver=",depth 10', '&aid=",distance 0', '&hid=",distance 0', '&rid=",distance 0', '&data=",distance 0', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29828
static inline void alert_rule_29828(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29828;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Adload.dyhq variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Adload.dyhq variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29829
// Rule Name: SERVER-WEBAPP HNAP remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 29829
static inline int check_rule_29829(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/HNAP1",fast_pattern,nocase', 'Authorization: Basic YWRtaW46']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29829
static inline void alert_rule_29829(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29829;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP HNAP remote code execution attempt", 
                     sizeof("SERVER-WEBAPP HNAP remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29830
// Rule Name: SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 29830
static inline int check_rule_29830(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tmUnblock.cgi",fast_pattern,nocase', '%74%74%63%70%5f%69%70']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29830
static inline void alert_rule_29830(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29830;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29831
// Rule Name: SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 29831
static inline int check_rule_29831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tmUnblock.cgi",fast_pattern,nocase', 'ttcp_ip']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29831
static inline void alert_rule_29831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-series HNAP TheMoon remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29862
// Rule Name: MALWARE-CNC Win.Trojan.Pirminay variant outbout connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29862
static inline int check_rule_29862(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['filename=|22|full__setup.zip|22 0D 0A|",fast_pattern,nocase', 'full__setup.exe",depth 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29862
static inline void alert_rule_29862(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29862;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Pirminay variant outbout connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Pirminay variant outbout connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29863
// Rule Name: MALWARE-CNC Win.Trojan.Pirminay variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29863
static inline int check_rule_29863(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/read/swf/searchProductResult.jsp",fast_pattern,nocase', 'cache=cc2=",depth 10', '|3B| core=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29863
static inline void alert_rule_29863(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29863;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Pirminay variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Pirminay variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29864
// Rule Name: EXPLOIT-KIT Redkit exploit kit payload request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29864
static inline int check_rule_29864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/download.asp?p=",nocase', ' Java/1.",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29864
static inline void alert_rule_29864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Redkit exploit kit payload request", 
                     sizeof("EXPLOIT-KIT Redkit exploit kit payload request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29865
// Rule Name: MALWARE-CNC Win.Trojan.Kuluoz outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29865
static inline int check_rule_29865(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: [' HTTP/1.1|0D 0A|Accept: */*|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| WOW64|3B| rv:25.0) Gecko/20100101 Firefox/25.0|0D 0A|Host: ",fast_pattern,nocase', 'POST /",depth 6', ' HTTP/1.1",within 9,distance 42']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29865
static inline void alert_rule_29865(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29865;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluoz outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluoz outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29869
// Rule Name: MALWARE-CNC Win.Trojan.Napolar phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29869
static inline int check_rule_29869(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: facebook.com.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29869
static inline void alert_rule_29869(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29869;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Napolar phishing attack", 
                     sizeof("MALWARE-CNC Win.Trojan.Napolar phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29870
// Rule Name: MALWARE-CNC Win.Trojan.Pony HTTP response connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29870
static inline int check_rule_29870(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 16', 'STATUS-IMPORT-OK",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29870
static inline void alert_rule_29870(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29870;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Pony HTTP response connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Pony HTTP response connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29882
// Rule Name: MALWARE-CNC Win.Trojan.WEC variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29882
static inline int check_rule_29882(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1|0D 0A|User-Agent: Mozilla/4.0|0D 0A|Host: checkip.dyndns.org|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29882
static inline void alert_rule_29882(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29882;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.WEC variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.WEC variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29884
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29884
static inline int check_rule_29884(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/gate.php",fast_pattern,nocase', '|3B 20|MSIE|20|', '!"Accept-Language:', '!"Referer:', '!"Accept-Encoding:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29884
static inline void alert_rule_29884(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29884;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29887
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string Updates downloader - Win.Trojan.Upatre
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29887
static inline int check_rule_29887(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Updates downloader|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29887
static inline void alert_rule_29887(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29887;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string Updates downloader - Win.Trojan.Upatre", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string Updates downloader - Win.Trojan.Upatre"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29891
// Rule Name: MALWARE-CNC Win.Trojan.Pushdo variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29891
static inline int check_rule_29891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '!"Referer|3A 20|', 'Accept|3A| */*|0D 0A|Accept-Language|3A| en-us|0D 0A|Content-Type|3A| application/octet-stream|0D 0A|Content-Length|3A| ",depth 93', 'Connection|3A| Keep-Alive|0D 0A|Cache-Control|3A| no-cache|0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29891
static inline void alert_rule_29891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Pushdo variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Pushdo variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29895
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29895
static inline int check_rule_29895(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 166', '.php HTTP/1.1|0D 0A|Accept: */*|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| Trident/7.0|3B| rv:11.0) like Gecko|0D 0A|Host: ",fast_pattern,nocase', 'v=",depth 2', '&c=",within 7']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29895
static inline void alert_rule_29895(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29895;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29897
// Rule Name: MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29897
static inline int check_rule_29897(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/prl/el.html",fast_pattern,nocase', 'Accept: text/html, */*|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29897
static inline void alert_rule_29897(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29897;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 29981
// Rule Name: MALWARE-CNC Win.Trojan.Tiny variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 29981
static inline int check_rule_29981(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ie-error.gif?action=utility",fast_pattern,nocase', '&os=', '&error=",distance 0', '&rnd=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 29981
static inline void alert_rule_29981(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 29981;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Tiny variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Tiny variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30003
// Rule Name: EXPLOIT-KIT Hello/LightsOut exploit kit payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30003
static inline int check_rule_30003(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?a=dw",fast_pattern,nocase', ' Java/1.']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30003
static inline void alert_rule_30003(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30003;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Hello/LightsOut exploit kit payload download attempt", 
                     sizeof("EXPLOIT-KIT Hello/LightsOut exploit kit payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30068
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30068
static inline int check_rule_30068(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/and/image.php",fast_pattern,nocase', 'User-Agent: Mozilla/4.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30068
static inline void alert_rule_30068(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30068;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30070
// Rule Name: MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30070
static inline int check_rule_30070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/android/sms/sync.php",fast_pattern,nocase', 'User-Agent|3A 20|Apache-HttpClient|2F|', 'bot_id=', '&imei=",distance 0', '&iscallhack=",distance 0', '&issmshack=",distance 0', '&isrecordhack=",distance 0', '&isadmin=",distance 0', '&control_number=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30070
static inline void alert_rule_30070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt", 
                     sizeof("MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30071
// Rule Name: MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30071
static inline int check_rule_30071(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/android/sms/ping.php",fast_pattern,nocase', 'User-Agent|3A 20|Apache-HttpClient|2F|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30071
static inline void alert_rule_30071(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30071;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt", 
                     sizeof("MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30072
// Rule Name: MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30072
static inline int check_rule_30072(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/android/sms/index.php",fast_pattern,nocase', 'User-Agent|3A 20|Apache-HttpClient|2F|', 'bot_id=', '&number=&iccid=&model=",distance 0', '&imei=",distance 0', '&os=",distance 0', '&control_number=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30072
static inline void alert_rule_30072(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30072;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt", 
                     sizeof("MALWARE-OTHER ANDR.Trojan.iBanking outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30087
// Rule Name: MALWARE-CNC Win.Trojan.Gamut configuration download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30087
static inline int check_rule_30087(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |26|file=SenderClient.conf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30087
static inline void alert_rule_30087(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30087;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gamut configuration download", 
                     sizeof("MALWARE-CNC Win.Trojan.Gamut configuration download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30091
// Rule Name: MALWARE-CNC Win.Trojan.Necurs variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30091
static inline int check_rule_30091(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/forum/db.php HTTP/1.1|0D 0A|Content-Type: application/octet-stream|0D 0A|Host: ",fast_pattern,nocase', '!"User-Agent:', '!"Referer:', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30091
static inline void alert_rule_30091(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30091;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Necurs variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Necurs variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30191
// Rule Name: MALWARE-CNC Win.Trojan.Uroburos usermode-centric client request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30191
static inline int check_rule_30191(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/1/6b-558694705129b01c0",fast_pattern,nocase', 'Connection: Keep-Alive|0D 0A|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30191
static inline void alert_rule_30191(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30191;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Uroburos usermode-centric client request", 
                     sizeof("MALWARE-CNC Win.Trojan.Uroburos usermode-centric client request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30196
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30196
static inline int check_rule_30196(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tmp/image.php",fast_pattern,nocase', 'User-Agent: Mozilla/4.0|0D 0A|', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30196
static inline void alert_rule_30196(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30196;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30198
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30198
static inline int check_rule_30198(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .xpg.com.br|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30198
static inline void alert_rule_30198(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30198;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30220
// Rule Name: EXPLOIT-KIT Nuclear exploit kit outbound payload request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30220
static inline int check_rule_30220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /f/",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30220
static inline void alert_rule_30220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Nuclear exploit kit outbound payload request", 
                     sizeof("EXPLOIT-KIT Nuclear exploit kit outbound payload request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30234
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30234
static inline int check_rule_30234(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|User-Agent: Mozilla/5.0 (Windows|3B| U|3B| Windows NT 6.1|3B| pt-BR|3B| rv:1.9.2b5) Gecko/20091204 Firefox/3.6b5|0D 0A 0D 0A|",fast_pattern,nocase', '|0D 0A|Accept-Encoding: gzip,deflate, identity|0D 0A|', ' HTTP/1.1|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30234
static inline void alert_rule_30234(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30234;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30255
// Rule Name: MALWARE-CNC Win.Trojan.Strictor HTTP Response - Brazil Geolocated Infected User
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30255
static inline int check_rule_30255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 6|0D 0A|', 'BRASIL",depth 6,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30255
static inline void alert_rule_30255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Strictor HTTP Response - Brazil Geolocated Infected User", 
                     sizeof("MALWARE-CNC Win.Trojan.Strictor HTTP Response - Brazil Geolocated Infected User"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30256
// Rule Name: MALWARE-CNC Win.Trojan.Strictor HTTP Response - Non-Brazil Geolocated Infected User
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30256
static inline int check_rule_30256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 13|0D 0A|', 'INTERNACIONAL",depth 13,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30256
static inline void alert_rule_30256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Strictor HTTP Response - Non-Brazil Geolocated Infected User", 
                     sizeof("MALWARE-CNC Win.Trojan.Strictor HTTP Response - Non-Brazil Geolocated Infected User"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30257
// Rule Name: MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30257
static inline int check_rule_30257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/eh.html HTTP/1.1|0D 0A|Content-Type: text/html|0D 0A|Host: ",fast_pattern,nocase', '|0D 0A|Accept: text/html, */*|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30257
static inline void alert_rule_30257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ExplorerHijack variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30258
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30258
static inline int check_rule_30258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/forumdisplay.php?fid=",fast_pattern,nocase', 'id=",depth 3', '&iv=",within 4,distance 36', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30258
static inline void alert_rule_30258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30259
// Rule Name: MALWARE-CNC Win.Trojan.Strictor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30259
static inline int check_rule_30259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/20",depth 3', '|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|",fast_pattern,nocase', '.inf",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30259
static inline void alert_rule_30259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Strictor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Strictor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30260
// Rule Name: PUA-ADWARE Lucky Leap Adware outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30260
static inline int check_rule_30260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gcs?alpha=",fast_pattern,nocase', '|0D 0A|Cache-Control: no-store,no-cache|0D 0A|Pragma: no-cache|0D 0A|Connection: Keep-Alive|0D 0A 0D 0A|', '!"Accept', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30260
static inline void alert_rule_30260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Lucky Leap Adware outbound connection", 
                     sizeof("PUA-ADWARE Lucky Leap Adware outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30261
// Rule Name: PUA-ADWARE Lucky Leap Adware outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30261
static inline int check_rule_30261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gdi?alpha=",fast_pattern,nocase', '|0D 0A|Cache-Control: no-store,no-cache|0D 0A|Pragma: no-cache|0D 0A|Connection: Keep-Alive|0D 0A 0D 0A|', '!"Accept', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30261
static inline void alert_rule_30261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Lucky Leap Adware outbound connection", 
                     sizeof("PUA-ADWARE Lucky Leap Adware outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30262
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30262
static inline int check_rule_30262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['lista', '|3B| name=|22|arquivo|22 3B| filename=|22|C:|5C|",fast_pattern,nocase', '.log|22 0D 0A|",nocase', '!"Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30262
static inline void alert_rule_30262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30288
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba.M initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30288
static inline int check_rule_30288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/stat?', 'uptime=', '&downlink=",distance 0', '&uplink=",distance 0', '&id=",distance 0', '&statpass=bpass",distance 0,fast_pattern', '&version=",distance 0', '&features=",distance 0', '&guid=",distance 0', '&comment=",distance 0', '&p=",distance 0', '&s=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30288
static inline void alert_rule_30288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba.M initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba.M initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30319
// Rule Name: EXPLOIT-KIT Goon/Infinity exploit kit malicious portable executable file request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30319
static inline int check_rule_30319(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .mp3?rnd=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30319
static inline void alert_rule_30319(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30319;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Goon/Infinity exploit kit malicious portable executable file request", 
                     sizeof("EXPLOIT-KIT Goon/Infinity exploit kit malicious portable executable file request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30336
// Rule Name: MALWARE-CNC Linux.Trojan.Calfbot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30336
static inline int check_rule_30336(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/b/index.php?id=",fast_pattern,nocase', '&sent=', '&notsent=",distance 0', '&stat=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30336
static inline void alert_rule_30336(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30336;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.Calfbot outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.Calfbot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30482
// Rule Name: MALWARE-CNC Win.Trojan.Zbot/Bublik inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30482
static inline int check_rule_30482(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 1600-1604
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1600 || src_port > 1604)
        return 0;
    // Content matching (simplified)
    // Full pattern: E|00|N|00|D|00|S|00|E|00|R|00|V|00|E|00|R|00|B|00|U|00|F|00|F|00|E|00|R|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30482
static inline void alert_rule_30482(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30482;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot/Bublik inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot/Bublik inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30483
// Rule Name: MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30483
static inline int check_rule_30483(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1600-1604
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1600 || dst_port > 1604)
        return 0;
    // Content matching (simplified)
    // Full pattern: GET /123456789.functionss",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30483
static inline void alert_rule_30483(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30483;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30484
// Rule Name: MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30484
static inline int check_rule_30484(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1600-1604
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1600 || dst_port > 1604)
        return 0;
    // Content matching (simplified)
    // Full pattern: myversion|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30484
static inline void alert_rule_30484(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30484;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot/Bublik outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30510
// Rule Name: SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30510
static inline int check_rule_30510(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 00|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30510
static inline void alert_rule_30510(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30510;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30511
// Rule Name: SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30511
static inline int check_rule_30511(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 01|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30511
static inline void alert_rule_30511(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30511;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30512
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30512
static inline int check_rule_30512(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 02|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30512
static inline void alert_rule_30512(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30512;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30513
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30513
static inline int check_rule_30513(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 03|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30513
static inline void alert_rule_30513(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30513;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30514
// Rule Name: SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30514
static inline int check_rule_30514(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 00|",depth 3', '|02|",within 1,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30514
static inline void alert_rule_30514(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30514;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30515
// Rule Name: SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30515
static inline int check_rule_30515(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 01|",depth 3', '|02|",within 1,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30515
static inline void alert_rule_30515(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30515;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30516
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30516
static inline int check_rule_30516(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 02|",depth 3', '|02|",within 1,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30516
static inline void alert_rule_30516(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30516;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30517
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30517
static inline int check_rule_30517(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 03|",depth 3', '|02|",within 1,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30517
static inline void alert_rule_30517(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30517;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30520
// Rule Name: SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt - vulnerable client response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30520
static inline int check_rule_30520(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 00|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30520
static inline void alert_rule_30520(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30520;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt - vulnerable client response", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 heartbeat read overrun attempt - vulnerable client response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30521
// Rule Name: SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt - vulnerable client response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30521
static inline int check_rule_30521(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 01|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30521
static inline void alert_rule_30521(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30521;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt - vulnerable client response", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 heartbeat read overrun attempt - vulnerable client response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30522
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt - vulnerable client response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30522
static inline int check_rule_30522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 02|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30522
static inline void alert_rule_30522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt - vulnerable client response", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt - vulnerable client response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30523
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt - vulnerable client response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30523
static inline int check_rule_30523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 03|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30523
static inline void alert_rule_30523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt - vulnerable client response", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt - vulnerable client response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30524
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30524
static inline int check_rule_30524(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 02 00 03 01 40 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30524
static inline void alert_rule_30524(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30524;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30525
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30525
static inline int check_rule_30525(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->dest) != 25 && ntohs(tcph->dest) != 443 && ntohs(tcph->dest) != 465 && ntohs(tcph->dest) != 636 && ntohs(tcph->dest) != 992 && ntohs(tcph->dest) != 993 && ntohs(tcph->dest) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: |18 03 03 00 40|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30525
static inline void alert_rule_30525(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30525;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 heartbeat read overrun attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30547
// Rule Name: MALWARE-CNC Win.Trojan.Ramdo variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30547
static inline int check_rule_30547(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.org|0D 0A|Content-Length|3A| 128|0D 0A|Cache-Control|3A| no-cache|0D 0A 0D 0A|",fast_pattern,nocase', '!"User-Agent|3A|', '!"Accept|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30547
static inline void alert_rule_30547(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30547;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ramdo variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Ramdo variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30548
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30548
static inline int check_rule_30548(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/write', 'Host: default|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30548
static inline void alert_rule_30548(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30548;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30549
// Rule Name: SERVER-OTHER OpenSSL Heartbleed masscan access exploitation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30549
static inline int check_rule_30549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: [masscan/1.0]
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30549
static inline void alert_rule_30549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL Heartbleed masscan access exploitation attempt", 
                     sizeof("SERVER-OTHER OpenSSL Heartbleed masscan access exploitation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30551
// Rule Name: MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Minerd
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30551
static inline int check_rule_30551(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /minerd.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30551
static inline void alert_rule_30551(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30551;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Minerd", 
                     sizeof("MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Minerd"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30552
// Rule Name: MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Systema
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30552
static inline int check_rule_30552(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /aviatic/systema.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30552
static inline void alert_rule_30552(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30552;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Systema", 
                     sizeof("MALWARE-CNC Malicious BitCoiner Miner download - Win.Trojan.Systema"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30566
// Rule Name: MALWARE-CNC Linux.Trojan.Elknot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30566
static inline int check_rule_30566(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 10991
    if (tcph->dest != htons(10991))
        return 0;
    // Content matching (simplified)
    // Full pattern: Linux|20|",depth 6,offset 17
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30566
static inline void alert_rule_30566(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30566;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.Elknot outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.Elknot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30567
// Rule Name: MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30567
static inline int check_rule_30567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['pdf_efax_",fast_pattern,nocase', 'PK",depth 2', '.pif",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30567
static inline void alert_rule_30567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30568
// Rule Name: MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30568
static inline int check_rule_30568(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cache/pdf_efax_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30568
static inline void alert_rule_30568(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30568;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Agent E-FAX phishing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30569
// Rule Name: MALWARE-OTHER Win.Trojan.Agent Funeral ceremony phishing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30569
static inline int check_rule_30569(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['filename=FuneralCeremony_",fast_pattern,nocase', '.zip",nocase', 'FuneralCeremony_', '.exe",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30569
static inline void alert_rule_30569(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30569;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Agent Funeral ceremony phishing attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Agent Funeral ceremony phishing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30570
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30570
static inline int check_rule_30570(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 80
    if (tcph->dest != htons(80))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/webhp HTTP/1.1|0D 0A|Accept: */*|0D 0A|Connection: Close|0D 0A|User-Agent: Mozilla/4.0 (",fast_pattern,nocase', '|3B| MSIE ', 'google.', '!"Accept-', 'PREF=",depth 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30570
static inline void alert_rule_30570(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30570;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30777
// Rule Name: SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30777
static inline int check_rule_30777(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 00|', '|18 03 00|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30777
static inline void alert_rule_30777(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30777;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30778
// Rule Name: SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30778
static inline int check_rule_30778(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|17 03 00|', '|18 03 00|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30778
static inline void alert_rule_30778(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30778;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30779
// Rule Name: SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30779
static inline int check_rule_30779(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 01|', '|18 03 01|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30779
static inline void alert_rule_30779(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30779;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30780
// Rule Name: SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30780
static inline int check_rule_30780(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|17 03 01|', '|18 03 01|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30780
static inline void alert_rule_30780(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30780;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30781
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30781
static inline int check_rule_30781(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 02|', '|18 03 02|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30781
static inline void alert_rule_30781(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30781;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30782
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30782
static inline int check_rule_30782(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|17 03 02|', '|18 03 02|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30782
static inline void alert_rule_30782(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30782;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30783
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30783
static inline int check_rule_30783(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 03|', '|18 03 03|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30783
static inline void alert_rule_30783(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30783;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30784
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30784
static inline int check_rule_30784(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|17 03 03|', '|18 03 03|",within 3,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30784
static inline void alert_rule_30784(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30784;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30785
// Rule Name: SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30785
static inline int check_rule_30785(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 00|', '|18 03 00|",within 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30785
static inline void alert_rule_30785(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30785;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL SSLv3 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30786
// Rule Name: SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30786
static inline int check_rule_30786(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 01|', '|18 03 01|",within 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30786
static inline void alert_rule_30786(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30786;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30787
// Rule Name: SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30787
static inline int check_rule_30787(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 02|', '|18 03 02|",within 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30787
static inline void alert_rule_30787(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30787;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.1 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30788
// Rule Name: SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 30788
static inline int check_rule_30788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port list: [25, 443, 465, 636, 992, 993, 995]
    if (ntohs(tcph->source) != 25 && ntohs(tcph->source) != 443 && ntohs(tcph->source) != 465 && ntohs(tcph->source) != 636 && ntohs(tcph->source) != 992 && ntohs(tcph->source) != 993 && ntohs(tcph->source) != 995)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|18 03 03|', '|18 03 03|",within 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30788
static inline void alert_rule_30788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt", 
                     sizeof("SERVER-OTHER OpenSSL TLSv1.2 large heartbeat response - possible ssl heartbleed attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30906
// Rule Name: FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 30906
static inline int check_rule_30906(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|50 4B 03 04|",depth 4', '|00 00|",within 2,distance 24', '.exe",within 64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30906
static inline void alert_rule_30906(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30906;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt", 
                     sizeof("FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30909
// Rule Name: FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 30909
static inline int check_rule_30909(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|50 4B 03 04|",depth 4', '|00 00|",within 2,distance 24', '.exe",within 64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30909
static inline void alert_rule_30909(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30909;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt", 
                     sizeof("FILE-OTHER RARLAB WinRAR ZIP format filename spoof attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30914
// Rule Name: MALWARE-CNC Win.Trojan.SpySmall variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30914
static inline int check_rule_30914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/4.0 (compatible|3B| MSIE 10.0|3B| Windows NT 6.2|3B| Trident/4.0|0D 0A|",fast_pattern,nocase', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30914
static inline void alert_rule_30914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpySmall variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpySmall variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30915
// Rule Name: MALWARE-CNC Win.Trojan.SpySmall variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30915
static inline int check_rule_30915(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |3E 00|e|00|c|00|h|00|o|00 20 00|c|00|m|00|d|00 5F 00|b|00|e|00|g|00|i|00|n|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30915
static inline void alert_rule_30915(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30915;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpySmall variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpySmall variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30918
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - User-Agent User-Agent Mozilla
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30918
static inline int check_rule_30918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: User-Agent: Mozilla/",fast_pattern,nocase', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30918
static inline void alert_rule_30918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - User-Agent User-Agent Mozilla", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - User-Agent User-Agent Mozilla"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30919
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30919
static inline int check_rule_30919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/js/prototype/order.php",fast_pattern,nocase', ' HTTP/1.1|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|User-Agent: Mozilla/', '|3B 20|MSIE|20|",distance 0', '|29 0D 0A|Host:",distance 0', '!"Accept', '!"|0D 0A|Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30919
static inline void alert_rule_30919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30920
// Rule Name: EXPLOIT-KIT Multiple exploit kit redirection gate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30920
static inline int check_rule_30920(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php?q=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30920
static inline void alert_rule_30920(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30920;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Multiple exploit kit redirection gate", 
                     sizeof("EXPLOIT-KIT Multiple exploit kit redirection gate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30948
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Hikit outbound banner response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30948
static inline int check_rule_30948(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |5D 00 20 00|h|00|i|00|k|00|i|00|t|00|>|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30948
static inline void alert_rule_30948(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30948;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Hikit outbound banner response", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Hikit outbound banner response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30973
// Rule Name: EXPLOIT-KIT CritX exploit kit payload request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30973
static inline int check_rule_30973(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/load', '.php",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30973
static inline void alert_rule_30973(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30973;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT CritX exploit kit payload request", 
                     sizeof("EXPLOIT-KIT CritX exploit kit payload request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30997
// Rule Name: INDICATOR-COMPROMISE Potential malware download - .doc.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30997
static inline int check_rule_30997(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.doc.exe",fast_pattern,nocase', 'Content-Length:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30997
static inline void alert_rule_30997(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30997;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - .doc.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - .doc.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30998
// Rule Name: INDICATOR-COMPROMISE Potential malware download - .gif.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30998
static inline int check_rule_30998(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.gif.exe",fast_pattern,nocase', 'Content-Length:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30998
static inline void alert_rule_30998(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30998;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - .gif.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - .gif.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 30999
// Rule Name: INDICATOR-COMPROMISE Potential malware download - .jpeg.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 30999
static inline int check_rule_30999(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.jpeg.exe",fast_pattern,nocase', 'Content-Length:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 30999
static inline void alert_rule_30999(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 30999;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - .jpeg.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - .jpeg.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31000
// Rule Name: INDICATOR-COMPROMISE Potential malware download - .jpg.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31000
static inline int check_rule_31000(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.jpg.exe",fast_pattern,nocase', 'Content-Length:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31000
static inline void alert_rule_31000(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31000;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - .jpg.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - .jpg.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31001
// Rule Name: INDICATOR-COMPROMISE Potential malware download - .pdf.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31001
static inline int check_rule_31001(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.pdf.exe",fast_pattern,nocase', 'Content-Length:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31001
static inline void alert_rule_31001(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31001;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - .pdf.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - .pdf.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31020
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31020
static inline int check_rule_31020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/hunter/123/order.php",fast_pattern,nocase', '!"Accept', '!"|0D 0A|Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31020
static inline void alert_rule_31020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31036
// Rule Name: MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31036
static inline int check_rule_31036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '.php HTTP/1.0|0D 0A|Connection: keep-alive|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|Content-Length: 0|0D 0A|Host: ', '|0D 0A|Accept: text/html,application/xhtml+xml,application/xml|3B|q=0.9,*/*|3B|q=0.8|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31036
static inline void alert_rule_31036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31053
// Rule Name: MALWARE-CNC Win.Trojan.MadnessPro outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31053
static inline int check_rule_31053(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?', 'uid=', '&mk=",fast_pattern', '&os=', '&rs=', '&c=', '&rq=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31053
static inline void alert_rule_31053(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31053;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MadnessPro outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MadnessPro outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31070
// Rule Name: MALWARE-CNC Win.Rootkit.Necurs outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31070
static inline int check_rule_31070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/docs/index.php",fast_pattern,nocase', 'Content-Type|3A 20|application/octet-stream', '!"User-Agent|3A 20|', '!"Accept|3A 20|', '!"Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31070
static inline void alert_rule_31070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Rootkit.Necurs outbound connection", 
                     sizeof("MALWARE-CNC Win.Rootkit.Necurs outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31084
// Rule Name: MALWARE-CNC Win.Trojan.Zbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31084
static inline int check_rule_31084(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/srt/ge.php",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31084
static inline void alert_rule_31084(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31084;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31090
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - User-Agent hello crazyk
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31090
static inline int check_rule_31090(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: hello crazyk|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31090
static inline void alert_rule_31090(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31090;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - User-Agent hello crazyk", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - User-Agent hello crazyk"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31112
// Rule Name: MALWARE-CNC Win.Trojan.Bancos password stealing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31112
static inline int check_rule_31112(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['rotina=plogin&login=",fast_pattern,nocase', '&senha=', '&casa=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31112
static inline void alert_rule_31112(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31112;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos password stealing attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos password stealing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31113
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31113
static inline int check_rule_31113(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [')&dt=",fast_pattern,nocase', 'pc=",depth 3', '&av=",distance 0', '&wd=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31113
static inline void alert_rule_31113(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31113;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31221
// Rule Name: MALWARE-CNC Win.Trojan.Banker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31221
static inline int check_rule_31221(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/notify.php HTTP/1.0|0D 0A|",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A|', 'Content-Length: 0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31221
static inline void alert_rule_31221(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31221;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31222
// Rule Name: MALWARE-CNC Win.Trojan.Banker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31222
static inline int check_rule_31222(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/second/game1.inf",fast_pattern,nocase', '|3B 20|MSIE|20|', '!"Accept-Language:', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31222
static inline void alert_rule_31222(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31222;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31243
// Rule Name: MALWARE-CNC Win.Trojan.Necurs variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31243
static inline int check_rule_31243(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/news/index.php HTTP/1.1|0D 0A|Content-Type: application/octet-stream|0D 0A|Host: ",fast_pattern,nocase', '!"User-Agent:', '!"Referer:', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31243
static inline void alert_rule_31243(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31243;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Necurs variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Necurs variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31244
// Rule Name: MALWARE-CNC Win.Trojan.Kuluoz outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31244
static inline int check_rule_31244(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['POST /",depth 6', ' HTTP/1.1",within 9,distance 42', 'Firefox/",distance 0', '!"|0D 0A|Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31244
static inline void alert_rule_31244(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31244;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluoz outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluoz outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31260
// Rule Name: MALWARE-CNC Win.Trojan.Andromeda HTTP proxy response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31260
static inline int check_rule_31260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['function FindProxyForURL(url, host)",depth 35', 'yx0=0|3B|yx1=1|3B|yx2=2|3B|yx3=3|3B|yx4=4|3B|yx5=5|3B|yx6=6|3B|yx7=7|3B|yx8=8|3B|yx9=9|3B|lit=|22 22|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31260
static inline void alert_rule_31260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Andromeda HTTP proxy response attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Andromeda HTTP proxy response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31261
// Rule Name: MALWARE-CNC Win.Trojan.Symmi outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31261
static inline int check_rule_31261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.inf HTTP/1.1|0D 0A|Accept: */*|0D 0A|Accept-Encoding: gzip, deflate|0D 0A|User-Agent: Mozilla/",fast_pattern,nocase', '|3B 20|MSIE|20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31261
static inline void alert_rule_31261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31262
// Rule Name: MALWARE-CNC Win.Worm.VBNA variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31262
static inline int check_rule_31262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/0.gif?",depth 7', ' HTTP/1.1|0D 0A|Host: sstatic1.histats.com|0D 0A 0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31262
static inline void alert_rule_31262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.VBNA variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.VBNA variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31293
// Rule Name: MALWARE-CNC Win.Trojan.Dyre publickey outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31293
static inline int check_rule_31293(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /publickey/ HTTP/1.1|0D 0A|User-Agent: Wget/1.9|0D 0A|Host: ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31293
static inline void alert_rule_31293(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31293;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dyre publickey outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dyre publickey outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31295
// Rule Name: MALWARE-CNC Win.Trojan.Zusy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31295
static inline int check_rule_31295(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/workers.php?mac=",fast_pattern,nocase', '&gpu=', '!"|0D 0A|User-Agent:', '!"|0D 0A|Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31295
static inline void alert_rule_31295(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31295;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zusy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zusy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31315
// Rule Name: MALWARE-CNC Win.Trojan.MSIL variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31315
static inline int check_rule_31315(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/srv2.php?param=1 HTTP/1.1|0D 0A|Host: ",fast_pattern,nocase', 'Connection: Keep-Alive|0D 0A 0D 0A|', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31315
static inline void alert_rule_31315(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31315;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MSIL variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MSIL variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31405
// Rule Name: SERVER-APACHE Apache Chunked-Encoding worm attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 31405
static inline int check_rule_31405(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Transfer-Encoding: Chunked",fast_pattern,nocase', '|0D 0A|",distance 0', '|20|",within 9']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31405
static inline void alert_rule_31405(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31405;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Chunked-Encoding worm attempt", 
                     sizeof("SERVER-APACHE Apache Chunked-Encoding worm attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31442
// Rule Name: MALWARE-CNC Win.Trojan.Injector variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31442
static inline int check_rule_31442(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/re/",fast_pattern,nocase', 'User-Agent: Mozilla/5.0 (compatible|3B| MSIE 9.0|3B| Windows NT 6.1|3B| Trident/5.0)|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|Host: ', '|0D 0A|Connection: Close|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31442
static inline void alert_rule_31442(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31442;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Injector variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Injector variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31449
// Rule Name: MALWARE-CNC Win.Trojan.CryptoWall downloader attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31449
static inline int check_rule_31449(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|macrotest|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31449
static inline void alert_rule_31449(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31449;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CryptoWall downloader attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.CryptoWall downloader attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31450
// Rule Name: MALWARE-CNC Win.Trojan.CryptoWall outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31450
static inline int check_rule_31450(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'HTTP/1.1|0D 0A|Accept: */*|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|Connection: Close|0D 0A|Content-Length: 100|0D 0A|User-Agent: ",fast_pattern,nocase', '=",depth 1,offset 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31450
static inline void alert_rule_31450(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31450;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CryptoWall outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.CryptoWall outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31452
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31452
static inline int check_rule_31452(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .php?chave=xchave&url|3D 20 3D 7C 3D 20|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31452
static inline void alert_rule_31452(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31452;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31453
// Rule Name: MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31453
static inline int check_rule_31453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' HTTP/1.1|0D 0A|User-Agent: Mozilla/5.0|0D 0A|', 'Service Pack ",fast_pattern,nocase', 'Cache-Control: no-cache|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31453
static inline void alert_rule_31453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31454
// Rule Name: MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31454
static inline int check_rule_31454(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.rar HTTP/1.1|0D 0A|Accept: text/*, application/*|0D 0A|User-Agent: Mozilla/5.0|0D 0A|Host: ",fast_pattern,nocase', '|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31454
static inline void alert_rule_31454(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31454;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ChoHeap variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31455
// Rule Name: EXPLOIT-KIT Rig Exploit Kit Outbound DGA Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31455
static inline int check_rule_31455(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .html?0.",depth 11,offset 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31455
static inline void alert_rule_31455(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31455;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Rig Exploit Kit Outbound DGA Request", 
                     sizeof("EXPLOIT-KIT Rig Exploit Kit Outbound DGA Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31458
// Rule Name: MALWARE-CNC Win.Trojan.SDBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31458
static inline int check_rule_31458(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/install', 'argc=",depth 5', '&name=",distance 0', '&previous=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31458
static inline void alert_rule_31458(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31458;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SDBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SDBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31465
// Rule Name: MALWARE-CNC Win.Trojan.Androm Click Fraud Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31465
static inline int check_rule_31465(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/query?version=",fast_pattern,nocase', '&sid=', '&builddate=",distance 0', '&q=",distance 0', '&ref=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31465
static inline void alert_rule_31465(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31465;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm Click Fraud Request", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm Click Fraud Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31466
// Rule Name: MALWARE-CNC Win.Trojan.Androm Click Fraud Request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31466
static inline int check_rule_31466(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|builddate:",fast_pattern,nocase', '|0D 0A|aid: ', '|0D 0A|redirect: http://']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31466
static inline void alert_rule_31466(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31466;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm Click Fraud Request", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm Click Fraud Request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31467
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31467
static inline int check_rule_31467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php",fast_pattern,nocase', 'User-Agent: Mozilla/4.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31467
static inline void alert_rule_31467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31468
// Rule Name: MALWARE-CNC Win.Trojan.Papras variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31468
static inline int check_rule_31468(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/viewforum.php?f=",fast_pattern,nocase', '&sid=', '!"Referer:', '!"Cookie:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31468
static inline void alert_rule_31468(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31468;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Papras variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Papras variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31507
// Rule Name: MALWARE-CNC Win.Trojan.HW32 variant spam attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31507
static inline int check_rule_31507(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: MAIL FROM: <Reademal.com>|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31507
static inline void alert_rule_31507(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31507;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HW32 variant spam attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.HW32 variant spam attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31530
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31530
static inline int check_rule_31530(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?email=libpurple_XMPP",fast_pattern,nocase', '&method=post', ' HTTP/1.0|0D 0A|Accept: */*|0D 0A|Connection: close|0D 0A|Host: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31530
static inline void alert_rule_31530(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31530;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31531
// Rule Name: INDICATOR-COMPROMISE MinerDeploy monitor request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31531
static inline int check_rule_31531(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/monitor.php?",fast_pattern', 'myid=",distance 0', '&ip=",distance 0', '&cgminer=",distance 0', '&operatingsystem=",distance 0', '!"Content-Length|3A 20|', '!"Content-Type|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31531
static inline void alert_rule_31531(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31531;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE MinerDeploy monitor request attempt", 
                     sizeof("INDICATOR-COMPROMISE MinerDeploy monitor request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31593
// Rule Name: MALWARE-CNC Andr.Trojan.SMSSend outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31593
static inline int check_rule_31593(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['sms', '.ashx?t=",fast_pattern,nocase', '!"User-Agent|3A 20|', '!"Accept|3A 20|', '!"Content-Type|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31593
static inline void alert_rule_31593(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31593;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.SMSSend outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.SMSSend outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31603
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba C&C server HELLO request to client
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31603
static inline int check_rule_31603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: HELLO|0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31603
static inline void alert_rule_31603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba C&C server HELLO request to client", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba C&C server HELLO request to client"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31604
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba C&C server READD command to client
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31604
static inline int check_rule_31604(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: READD|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31604
static inline void alert_rule_31604(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31604;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba C&C server READD command to client", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba C&C server READD command to client"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31605
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba C&C server READY command to client
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31605
static inline int check_rule_31605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: READY|0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31605
static inline void alert_rule_31605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba C&C server READY command to client", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba C&C server READY command to client"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31606
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba payload download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31606
static inline int check_rule_31606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/software.php?",fast_pattern,nocase', 'Accept|3A| */*', 'User-Agent|3A| Mozilla/4.0 |28|compatible|3B| MSIE 7.0|3B| Windows NT 6.1|3B|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31606
static inline void alert_rule_31606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba payload download request", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba payload download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31607
// Rule Name: MALWARE-CNC Win.Trojan.Glupteba client response/authenticate to C&C server
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31607
static inline int check_rule_31607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |3A|bpass|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31607
static inline void alert_rule_31607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Glupteba client response/authenticate to C&C server", 
                     sizeof("MALWARE-CNC Win.Trojan.Glupteba client response/authenticate to C&C server"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31641
// Rule Name: MALWARE-CNC Win.Tinybanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31641
static inline int check_rule_31641(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/5.0 (compatible|3B| MSIE 9.0|3B| Windows NT 6.1|3B| Trident/5.0)|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|Host: ",fast_pattern,nocase', '|0D 0A|Content-Length: 13|0D 0A|Connection: Close|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31641
static inline void alert_rule_31641(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31641;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Tinybanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Tinybanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31642
// Rule Name: MALWARE-CNC Win.Tinybanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31642
static inline int check_rule_31642(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/de/",fast_pattern,nocase', 'User-Agent: Mozilla/5.0 (compatible|3B| MSIE 9.0|3B| Windows NT 6.1|3B| Trident/5.0)|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|Host: ', 'Content-Length: 13|0D 0A|Connection: Close|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31642
static inline void alert_rule_31642(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31642;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Tinybanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Tinybanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31644
// Rule Name: MALWARE-CNC Andr.Trojan.Scarelocker outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31644
static inline int check_rule_31644(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/api.php",fast_pattern,nocase', 'User-Agent|3A 20|Apache-HttpClient|2F|UNAVAILABLE', 'method=', '&app_key=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31644
static inline void alert_rule_31644(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31644;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Scarelocker outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Scarelocker outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31649
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31649
static inline int check_rule_31649(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/boydn/boye.html",fast_pattern,nocase', 'User-Agent|3A 20|Mozilla/3.0 (compatible|3B| Indy Library)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31649
static inline void alert_rule_31649(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31649;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31680
// Rule Name: MALWARE-CNC Win.Trojan.Tirabot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31680
static inline int check_rule_31680(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&string=",fast_pattern,nocase', 'key=",depth 4', 'Content-Type: application/x-www-Form-urlencoded|0D 0A|', '.php']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31680
static inline void alert_rule_31680(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31680;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Tirabot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Tirabot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31681
// Rule Name: MALWARE-CNC Win.Trojan.Badur download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31681
static inline int check_rule_31681(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/support.exe",fast_pattern,nocase', '.exe HTTP/1.1|0D 0A|Accept: */*|0D 0A|Accept-Encoding: gzip,deflate,sdch|0D 0A|Host: ', ') Chrome/",distance 0', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31681
static inline void alert_rule_31681(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31681;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Badur download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Badur download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31682
// Rule Name: MALWARE-CNC Win.Trojan.Badur download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31682
static inline int check_rule_31682(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tmps.exe",fast_pattern,nocase', 'Proxy-Authorization: Basic |0D 0A|', '__cfduid=",depth 9', ') Chrome/', '!"Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31682
static inline void alert_rule_31682(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31682;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Badur download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Badur download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31683
// Rule Name: MALWARE-CNC Win.Trojan.Badur variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31683
static inline int check_rule_31683(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get/?data=",depth 11', 'User-Agent: win32|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31683
static inline void alert_rule_31683(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31683;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Badur variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Badur variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31719
// Rule Name: FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 31719
static inline int check_rule_31719(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 10|JFIF",depth 6,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31719
static inline void alert_rule_31719(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31719;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt", 
                     sizeof("FILE-IMAGE Microsoft Multiple Products JPEG parser heap overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31820
// Rule Name: MALWARE-CNC Win.Banker.Delf variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31820
static inline int check_rule_31820(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/notify.php', 'Content-Length: 0|0D 0A|', ' HTTP/1.0|0D 0A|', 'Content-Type: application/x-www-form-urlencoded|0D 0A|', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MyApp)|0D 0A 0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31820
static inline void alert_rule_31820(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31820;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Banker.Delf variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Banker.Delf variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31824
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31824
static inline int check_rule_31824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['dados=",depth 6', '&ct=",distance 0', '/",within 1,distance 2', '/201",within 4,distance 2', '=",within 1,distance 1', '&windows=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31824
static inline void alert_rule_31824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31826
// Rule Name: MALWARE-CNC Win.Trojan.Delf variant HTTP Response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31826
static inline int check_rule_31826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 201|0D 0A|', '<meta name=|22|token|22| content=|22 A4|",depth 29', '|A4 22|/>",within 4,distance 168']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31826
static inline void alert_rule_31826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Delf variant HTTP Response", 
                     sizeof("MALWARE-CNC Win.Trojan.Delf variant HTTP Response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31827
// Rule Name: MALWARE-CNC Win.Trojan.Delf variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31827
static inline int check_rule_31827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/token/token.html HTTP/1.1|0D 0A|User-Agent: ",fast_pattern,nocase', '!"Accept', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31827
static inline void alert_rule_31827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Delf variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Delf variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31830
// Rule Name: POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 31830
static inline int check_rule_31830(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PASS|20|images|0D 0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31830
static inline void alert_rule_31830(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31830;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt", 
                     sizeof("POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31831
// Rule Name: POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 31831
static inline int check_rule_31831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: USER|20|images|0D 0A|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31831
static inline void alert_rule_31831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt", 
                     sizeof("POLICY-OTHER QLogic Switch 5600/5800 default ftp login attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31871
// Rule Name: FILE-IDENTIFY JPEG file magic detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 31871
static inline int check_rule_31871(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF D8 FF|",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31871
static inline void alert_rule_31871(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31871;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file magic detection", 
                     sizeof("FILE-IDENTIFY JPEG file magic detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31916
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31916
static inline int check_rule_31916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/trdpr/trde.html",fast_pattern,nocase', 'Accept: text/html, */*|0D 0A|User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31916
static inline void alert_rule_31916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31923
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant HTTP response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31923
static inline int check_rule_31923(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['%set_intercepts%",fast_pattern,nocase', '%ban_contact%', '%ebaylive%', '%dep_host%', '%relay_soxid%']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31923
static inline void alert_rule_31923(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31923;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant HTTP response attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant HTTP response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31924
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31924
static inline int check_rule_31924(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?method=', '&mode=sox&v=",fast_pattern,nocase', ' HTTP/1.0|0D 0A|Accept: */*|0D 0A|Connection: close|0D 0A|Host: ', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31924
static inline void alert_rule_31924(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31924;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31964
// Rule Name: MALWARE-CNC Win.Trojan.Banker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31964
static inline int check_rule_31964(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/notify.php",fast_pattern,nocase', 'Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31964
static inline void alert_rule_31964(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31964;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31965
// Rule Name: EXPLOIT-KIT Astrum exploit kit landing page
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31965
static inline int check_rule_31965(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['{(new Image).src=|22|/', '%72%6f%72%72%65%6e%6f",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31965
static inline void alert_rule_31965(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31965;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit landing page", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit landing page"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31966
// Rule Name: EXPLOIT-KIT Astrum exploit kit payload delivery
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31966
static inline int check_rule_31966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |D5 B1 F8 24 89 28 15 47|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31966
static inline void alert_rule_31966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit payload delivery", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit payload delivery"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31967
// Rule Name: EXPLOIT-KIT Astrum exploit kit payload delivery
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31967
static inline int check_rule_31967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |F2 F7 94 75 16 7E 8E 15|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31967
static inline void alert_rule_31967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit payload delivery", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit payload delivery"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31970
// Rule Name: EXPLOIT-KIT Astrum exploit kit redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31970
static inline int check_rule_31970(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'Referer|3A 20|', 'x-req|3A 20|",fast_pattern', 'Connection|3A 20|Keep-Alive|0D 0A|', 'Pragma|3A 20|no-cache|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31970
static inline void alert_rule_31970(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31970;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit redirection attempt", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31971
// Rule Name: EXPLOIT-KIT Astrum exploit kit multiple exploit download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31971
static inline int check_rule_31971(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '.. HTTP/1.",fast_pattern,nocase', 'Connection|3A 20|Keep-Alive|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31971
static inline void alert_rule_31971(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31971;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit multiple exploit download request", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit multiple exploit download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31972
// Rule Name: EXPLOIT-KIT Astrum exploit kit payload delivery
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31972
static inline int check_rule_31972(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |DC C7 5E 47 A0 DB D2 51|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31972
static inline void alert_rule_31972(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31972;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Astrum exploit kit payload delivery", 
                     sizeof("EXPLOIT-KIT Astrum exploit kit payload delivery"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31973
// Rule Name: MALWARE-CNC Win.Trojan.Chebri variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31973
static inline int check_rule_31973(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php HTTP/1.0|0D 0A|Host: google.com|0D 0A|User-Agent: ",fast_pattern,nocase', '0=",depth 2', 'Accept-Encoding: none|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31973
static inline void alert_rule_31973(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31973;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Chebri variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Chebri variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31975
// Rule Name: OS-OTHER Bash CGI environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 31975
static inline int check_rule_31975(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %3D%28%29+%7B",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31975
static inline void alert_rule_31975(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31975;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash CGI environment variable injection attempt", 
                     sizeof("OS-OTHER Bash CGI environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31976
// Rule Name: OS-OTHER Bash CGI environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 31976
static inline int check_rule_31976(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: () {",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31976
static inline void alert_rule_31976(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31976;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash CGI environment variable injection attempt", 
                     sizeof("OS-OTHER Bash CGI environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31977
// Rule Name: OS-OTHER Bash CGI environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 31977
static inline int check_rule_31977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: () {",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31977
static inline void alert_rule_31977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash CGI environment variable injection attempt", 
                     sizeof("OS-OTHER Bash CGI environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31978
// Rule Name: OS-OTHER Bash CGI environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 31978
static inline int check_rule_31978(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: () {",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31978
static inline void alert_rule_31978(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31978;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash CGI environment variable injection attempt", 
                     sizeof("OS-OTHER Bash CGI environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31990
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Install - Win.Backdoor.Upatre
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31990
static inline int check_rule_31990(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/0/ HTTP/1.', 'User-Agent: Install|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31990
static inline void alert_rule_31990(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31990;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Install - Win.Backdoor.Upatre", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Install - Win.Backdoor.Upatre"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 31991
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Treck - Win.Backdoor.Upatre
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 31991
static inline int check_rule_31991(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/0/ HTTP/1.', 'User-Agent: Treck|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 31991
static inline void alert_rule_31991(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 31991;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Treck - Win.Backdoor.Upatre", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Treck - Win.Backdoor.Upatre"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32008
// Rule Name: MALWARE-OTHER Fake Delta Ticket HTTP Response phishing attack
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32008
static inline int check_rule_32008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['PK",depth 2', 'DeltaTicket_ET-RM-",distance 0,nocase', '.exe",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32008
static inline void alert_rule_32008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Fake Delta Ticket HTTP Response phishing attack", 
                     sizeof("MALWARE-OTHER Fake Delta Ticket HTTP Response phishing attack"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32009
// Rule Name: MALWARE-CNC Linux.Backdoor.Flooder inbound connection attempt - command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32009
static inline int check_rule_32009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |21 2A 20|SCANNER ON",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32009
static inline void alert_rule_32009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Backdoor.Flooder inbound connection attempt - command", 
                     sizeof("MALWARE-CNC Linux.Backdoor.Flooder inbound connection attempt - command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32010
// Rule Name: MALWARE-CNC Linux.Backdoor.Flooder outbound telnet connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32010
static inline int check_rule_32010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: /bin/busybox|3B|echo -e |27 5C|147|5C|141|5C|171|5C|146|5C|147|5C|164|27 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32010
static inline void alert_rule_32010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Backdoor.Flooder outbound telnet connection attempt", 
                     sizeof("MALWARE-CNC Linux.Backdoor.Flooder outbound telnet connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32011
// Rule Name: MALWARE-CNC Linux.Backdoor.Flooder outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32011
static inline int check_rule_32011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: BUILD X86|0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32011
static inline void alert_rule_32011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Backdoor.Flooder outbound connection", 
                     sizeof("MALWARE-CNC Linux.Backdoor.Flooder outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32038
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32038
static inline int check_rule_32038(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['() {",fast_pattern,nocase', 'MAIL",nocase', 'FROM|3A|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32038
static inline void alert_rule_32038(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32038;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32039
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32039
static inline int check_rule_32039(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['() {",fast_pattern,nocase', 'RCPT",nocase', 'TO|3A|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32039
static inline void alert_rule_32039(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32039;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32042
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32042
static inline int check_rule_32042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SIP_SERVERS (destination)
    // Variable port check: $SIP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: () {
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32042
static inline void alert_rule_32042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32043
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32043
static inline int check_rule_32043(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['USER ",depth 5', '() {",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32043
static inline void alert_rule_32043(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32043;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32065
// Rule Name: MALWARE-CNC Win.Trojan.Asprox inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32065
static inline int check_rule_32065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 30', '|3C|html|3E 3C|body|3E|hi!|3C 2F|body|3E 3C 2F|html|3E|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32065
static inline void alert_rule_32065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Asprox inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Asprox inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32066
// Rule Name: MALWARE-CNC Win.Trojan.Asprox outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32066
static inline int check_rule_32066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/b/pkg/T202",depth 11,fast_pattern', 'UA-CPU: ', 'Connection: Keep-Alive|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32066
static inline void alert_rule_32066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Asprox outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Asprox outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32067
// Rule Name: MALWARE-CNC Win.Trojan.Asprox outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32067
static inline int check_rule_32067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/x/",depth 3,fast_pattern', 'UA-CPU: ', 'Connection: Keep-Alive|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32067
static inline void alert_rule_32067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Asprox outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Asprox outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32069
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32069
static inline int check_rule_32069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['PASS ",depth 5', '() {",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32069
static inline void alert_rule_32069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32072
// Rule Name: MALWARE-CNC Win.Trojan.Zemot configuration download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32072
static inline int check_rule_32072(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mod_', '/soft', '.dll",fast_pattern,nocase', 'Connection|3A 20|Close|0D 0A|', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32072
static inline void alert_rule_32072(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32072;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zemot configuration download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Zemot configuration download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32073
// Rule Name: MALWARE-CNC Win.Trojan.Zemot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32073
static inline int check_rule_32073(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/b/shoe/",fast_pattern,nocase', 'Connection|3A 20|Close|0D 0A|', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32073
static inline void alert_rule_32073(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32073;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zemot outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zemot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32074
// Rule Name: MALWARE-CNC Win.Trojan.Zemot payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32074
static inline int check_rule_32074(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mod_articles-auth-",depth 19,fast_pattern', '/jquery/",within 8,distance 7', 'Accept: */*|0D 0A|Connection|3A 20|Close|0D 0A|', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32074
static inline void alert_rule_32074(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32074;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zemot payload download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Zemot payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32130
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32130
static inline int check_rule_32130(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/beta/order.php",fast_pattern,nocase', ' HTTP/1.1|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|User-Agent: Mozilla/', '|3B 20|MSIE|20|",distance 0', '|29 0D 0A|Host:",distance 0', '!"Accept', '!"|0D 0A|Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32130
static inline void alert_rule_32130(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32130;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32179
// Rule Name: MALWARE-CNC WIN.Trojan.Plugx variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32179
static inline int check_rule_32179(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [80, 443]
    if (ntohs(tcph->dest) != 80 && ntohs(tcph->dest) != 443)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['HHV1:', 'HHV2:",within 20', 'HHV3: 61456",within 20,fast_pattern', 'HHV4:",within 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32179
static inline void alert_rule_32179(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32179;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC WIN.Trojan.Plugx variant outbound connection", 
                     sizeof("MALWARE-CNC WIN.Trojan.Plugx variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32180
// Rule Name: MALWARE-CNC Win.Backdoor.ZxShell connection incoming attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32180
static inline int check_rule_32180(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |85 19 00 00 25 04 00 00|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32180
static inline void alert_rule_32180(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32180;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.ZxShell connection incoming attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.ZxShell connection incoming attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32181
// Rule Name: MALWARE-CNC Win.Backdoor.ZxShell connection outgoing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32181
static inline int check_rule_32181(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |86 19 00 00 04 01 00 00|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32181
static inline void alert_rule_32181(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32181;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.ZxShell connection outgoing attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.ZxShell connection outgoing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32192
// Rule Name: MALWARE-CNC Win.Trojan.Zxshell variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32192
static inline int check_rule_32192(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|20|OS|3A 20|', '|20|CPU|3A|",distance 0', 'Hz,RAM|3A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32192
static inline void alert_rule_32192(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32192;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zxshell variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zxshell variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32196
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32196
static inline int check_rule_32196(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['form-data|3B| name=|22|PLUG|22 0D 0A|",fast_pattern,nocase', 'form-data|3B| name=|22|PC|22 0D 0A|', 'form-data|3B| name=|22|SEG|22 0D 0A|",distance 0', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32196
static inline void alert_rule_32196(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32196;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32225
// Rule Name: MALWARE-CNC Win.Trojan.Cryptowall variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32225
static inline int check_rule_32225(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /blog-trabajos/n65dj17i1836",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32225
static inline void alert_rule_32225(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32225;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Cryptowall variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Cryptowall variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32244
// Rule Name: BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 32244
static inline int check_rule_32244(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['javascript|3A|//",fast_pattern,nocase', 'document.cookie",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32244
static inline void alert_rule_32244(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32244;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt", 
                     sizeof("BROWSER-FIREFOX Mozilla 1.0 Javascript arbitrary cookie access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32250
// Rule Name: MALWARE-CNC Win.Trojan.Hydraq.variant outbound detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32250
static inline int check_rule_32250(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/info.xml', 'Host:', 'update-adobe.com",within 30']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32250
static inline void alert_rule_32250(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32250;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Hydraq.variant outbound detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Hydraq.variant outbound detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32260
// Rule Name: MALWARE-OTHER Sinkhole reply - irc-sinkhole.cert.pl
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32260
static inline int check_rule_32260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|3A|irc|2D|sinkhole|2E|cert|2E|pl",fast_pattern,nocase', '|3A|End of MOTD command|2E|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32260
static inline void alert_rule_32260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Sinkhole reply - irc-sinkhole.cert.pl", 
                     sizeof("MALWARE-OTHER Sinkhole reply - irc-sinkhole.cert.pl"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32335
// Rule Name: OS-OTHER Bash CGI environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32335
static inline int check_rule_32335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [' () {",depth 50', '!"HTTP/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32335
static inline void alert_rule_32335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash CGI environment variable injection attempt", 
                     sizeof("OS-OTHER Bash CGI environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32366
// Rule Name: OS-OTHER Bash environment variable injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 32366
static inline int check_rule_32366(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['() {', '}",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32366
static inline void alert_rule_32366(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32366;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-OTHER Bash environment variable injection attempt", 
                     sizeof("OS-OTHER Bash environment variable injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32367
// Rule Name: MALWARE-CNC Win.Trojan.GameOverZeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32367
static inline int check_rule_32367(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/update', 'POST', '|0D 0A|Accept-Encoding:|0D 0A|Connection: close|0D 0A|Content-Length: ",fast_pattern,nocase', '!"User-Agent:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32367
static inline void alert_rule_32367(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32367;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GameOverZeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.GameOverZeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32370
// Rule Name: SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 32370
static inline int check_rule_32370(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: aim|3A|goaway?message=",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32370
static inline void alert_rule_32370(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32370;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt", 
                     sizeof("SERVER-OTHER AOL Instant Messenger goaway message buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32374
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32374
static inline int check_rule_32374(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/and/gate.php",fast_pattern,nocase', 'User-Agent: Mozilla/4.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32374
static inline void alert_rule_32374(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32374;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32378
// Rule Name: FILE-IDENTIFY bmp file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32378
static inline int check_rule_32378(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.bmp",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32378
static inline void alert_rule_32378(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32378;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY bmp file attachment detected", 
                     sizeof("FILE-IDENTIFY bmp file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32380
// Rule Name: FILE-IDENTIFY dib file attachment detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32380
static inline int check_rule_32380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['.dib",fast_pattern,nocase', 'Content-Disposition: attachment|3B|', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32380
static inline void alert_rule_32380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY dib file attachment detected", 
                     sizeof("FILE-IDENTIFY dib file attachment detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32386
// Rule Name: EXPLOIT-KIT Nuclear exploit kit outbound structure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32386
static inline int check_rule_32386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /f/",depth 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32386
static inline void alert_rule_32386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Nuclear exploit kit outbound structure", 
                     sizeof("EXPLOIT-KIT Nuclear exploit kit outbound structure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32583
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32583
static inline int check_rule_32583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cbrry/cbre.html",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32583
static inline void alert_rule_32583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32584
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32584
static inline int check_rule_32584(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['plug=NAO",fast_pattern,nocase', '.php HTTP/1.0|0D 0A|', 'Content-Length: 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32584
static inline void alert_rule_32584(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32584;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32604
// Rule Name: MALWARE-CNC Win.Trojan.Geodo variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32604
static inline int check_rule_32604(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/4.0 (compatible|3B|MSIE 7.0|3B|Windows NT 6.0)|0D 0A|",fast_pattern,nocase', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32604
static inline void alert_rule_32604(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32604;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Geodo variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Geodo variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32605
// Rule Name: MALWARE-CNC Win.Worm.Jenxcus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32605
static inline int check_rule_32605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /seo.php?username=MAREYOLE&format=ptp",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32605
static inline void alert_rule_32605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Worm.Jenxcus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Worm.Jenxcus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32606
// Rule Name: MALWARE-CNC Win.Trojan.Sodebral variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32606
static inline int check_rule_32606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/verifica/index.php?id=",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32606
static inline void alert_rule_32606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sodebral variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Sodebral variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32607
// Rule Name: MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32607
static inline int check_rule_32607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['INTERNACIONAL",depth 13', '!"Content-Length', 'Transfer-Encoding: chunked']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32607
static inline void alert_rule_32607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32608
// Rule Name: MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32608
static inline int check_rule_32608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['BRASIL",depth 6', '!"Content-Length', 'Transfer-Encoding: chunked']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32608
static inline void alert_rule_32608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Sodebral HTTP Response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32645
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string RUpdate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32645
static inline int check_rule_32645(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: User-Agent: RUpdate|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32645
static inline void alert_rule_32645(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32645;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string RUpdate", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string RUpdate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32646
// Rule Name: INDICATOR-COMPROMISE Potential malware download - _pdf.exe within .zip file
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32646
static inline int check_rule_32646(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: _pdf.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32646
static inline void alert_rule_32646(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32646;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential malware download - _pdf.exe within .zip file", 
                     sizeof("INDICATOR-COMPROMISE Potential malware download - _pdf.exe within .zip file"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32665
// Rule Name: MALWARE-CNC Win.Trojan.Chopstick variant outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32665
static inline int check_rule_32665(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search?btnG=', 'utm=",distance 0', 'ai=",distance 0', '!".",depth 20', '!"|22|",depth 20', '!"|3A|",depth 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32665
static inline void alert_rule_32665(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32665;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Chopstick variant outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Chopstick variant outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32667
// Rule Name: MALWARE-CNC Win.Trojan.Chopstick variant outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32667
static inline int check_rule_32667(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webhp?rel=', 'hl=",distance 0', 'ai=",distance 0', '!".",depth 20', '!"|22|",depth 20', '!"|3A|",depth 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32667
static inline void alert_rule_32667(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32667;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Chopstick variant outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Chopstick variant outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32670
// Rule Name: MALWARE-CNC Win.Dropper.Ch variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32670
static inline int check_rule_32670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/tasks.php",fast_pattern,nocase', 'Content-length:', 'Content-type:', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32670
static inline void alert_rule_32670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Dropper.Ch variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Dropper.Ch variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32674
// Rule Name: MALWARE-CNC Win.Trojan.Wiper variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32674
static inline int check_rule_32674(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['(|00|",depth 2', '|04 00 00 00|",within 4,distance 36']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32674
static inline void alert_rule_32674(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32674;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Wiper variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Wiper variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32776
// Rule Name: MALWARE-CNC FIN4 VBA Macro credentials upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32776
static inline int check_rule_32776(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/report.php?msg=",fast_pattern,nocase', '&uname=', '&pword=', 'Content-Length|3A 20|0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32776
static inline void alert_rule_32776(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32776;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC FIN4 VBA Macro credentials upload attempt", 
                     sizeof("MALWARE-CNC FIN4 VBA Macro credentials upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32823
// Rule Name: MALWARE-CNC Win.Trojan.Darkhotel outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32823
static inline int check_rule_32823(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/images/view.php",fast_pattern,nocase', 'User-Agent|3A 20|', 'Media Center PC 6.0",within 175', '!"Accept|3A 20|', '!"Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32823
static inline void alert_rule_32823(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32823;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Darkhotel outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Darkhotel outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32824
// Rule Name: MALWARE-CNC Win.Trojan.Darkhotel variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32824
static inline int check_rule_32824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/txt/read.php",fast_pattern,nocase', 'User-Agent|3A 20|', 'Media Center PC 6.0",within 175', '!"Accept|3A 20|', '!"Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32824
static inline void alert_rule_32824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Darkhotel variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Darkhotel variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32825
// Rule Name: MALWARE-CNC Win.Trojan.Darkhotel outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32825
static inline int check_rule_32825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bin/read_i.php?', 'a1=', '&a2=step2-down",fast_pattern,nocase', '&a3=', '&a4=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32825
static inline void alert_rule_32825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Darkhotel outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Darkhotel outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32826
// Rule Name: MALWARE-CNC Win.Trojan.Darkhotel data upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32826
static inline int check_rule_32826(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/html/docu.php', 'User-Agent|3A 20|', 'Media Center PC 6.0",within 175']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32826
static inline void alert_rule_32826(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32826;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Darkhotel data upload attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Darkhotel data upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32827
// Rule Name: MALWARE-CNC Win.Trojan.Darkhotel response connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32827
static inline int check_rule_32827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: DEXT87
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32827
static inline void alert_rule_32827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Darkhotel response connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Darkhotel response connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32845
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - 209.53.113.223
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32845
static inline int check_rule_32845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| 209.53.113.223|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32845
static inline void alert_rule_32845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - 209.53.113.223", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - 209.53.113.223"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32846
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - absolute.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32846
static inline int check_rule_32846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.absolute.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32846
static inline void alert_rule_32846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - absolute.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - absolute.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32847
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - bh.namequery.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32847
static inline int check_rule_32847(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| bh.namequery.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32847
static inline void alert_rule_32847(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32847;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - bh.namequery.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - bh.namequery.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32848
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - namequery.nettrace.co.za
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32848
static inline int check_rule_32848(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| namequery.nettrace.co.za|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32848
static inline void alert_rule_32848(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32848;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - namequery.nettrace.co.za", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - namequery.nettrace.co.za"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32849
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - search.us.namequery.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32849
static inline int check_rule_32849(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| search.us.namequery.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32849
static inline void alert_rule_32849(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32849;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - search.us.namequery.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - search.us.namequery.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32850
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - search2.namequery.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32850
static inline int check_rule_32850(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| search2.namequery.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32850
static inline void alert_rule_32850(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32850;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - search2.namequery.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - search2.namequery.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32851
// Rule Name: APP-DETECT Absolute Software Computrace outbound connection - search64.namequery.com
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 32851
static inline int check_rule_32851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host|3A| search64.namequery.com|0D 0A|",fast_pattern,nocase', 'TagId: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32851
static inline void alert_rule_32851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Absolute Software Computrace outbound connection - search64.namequery.com", 
                     sizeof("APP-DETECT Absolute Software Computrace outbound connection - search64.namequery.com"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32852
// Rule Name: MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32852
static inline int check_rule_32852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/11/form.php",fast_pattern,nocase', 'POST', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32852
static inline void alert_rule_32852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32853
// Rule Name: MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32853
static inline int check_rule_32853(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/11/feed.php",fast_pattern,nocase', 'POST', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32853
static inline void alert_rule_32853(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32853;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Poolfiend variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32888
// Rule Name: INDICATOR-COMPROMISE Potential Redirect from Compromised WordPress site to Fedex - Spammed Malware Download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32888
static inline int check_rule_32888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/wp-admin/",fast_pattern,nocase', 'Host: www.fedex.com|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32888
static inline void alert_rule_32888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Potential Redirect from Compromised WordPress site to Fedex - Spammed Malware Download attempt", 
                     sizeof("INDICATOR-COMPROMISE Potential Redirect from Compromised WordPress site to Fedex - Spammed Malware Download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32889
// Rule Name: FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 32889
static inline int check_rule_32889(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|89|PNG|0D 0A 1A 0A|', 'IHDR",within 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32889
static inline void alert_rule_32889(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32889;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt", 
                     sizeof("FILE-IMAGE Microsoft and libpng multiple products PNG large image width overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32911
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32911
static inline int check_rule_32911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 488
    if (tcph->source != htons(488))
        return 0;
    // Content matching (simplified)
    // Full pattern: |60 DB 37 37 37 37 37 37|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32911
static inline void alert_rule_32911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32912
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32912
static inline int check_rule_32912(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 488
    if (tcph->dest != htons(488))
        return 0;
    // Content matching (simplified)
    // Full pattern: |60 DB 37 37 37 37 37 37|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32912
static inline void alert_rule_32912(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32912;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32913
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32913
static inline int check_rule_32913(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|4C 4C|",depth 2,offset 16', '|75 14 2A 2A|",within 4,distance 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32913
static inline void alert_rule_32913(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32913;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32914
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32914
static inline int check_rule_32914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |8A 10 80 C2 67 80 F2 24 88 10|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32914
static inline void alert_rule_32914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32915
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32915
static inline int check_rule_32915(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 488
    if (tcph->source != htons(488))
        return 0;
    // Content matching (simplified)
    // Full pattern: |65 DB 37 37 37 37 37 37|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32915
static inline void alert_rule_32915(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32915;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32916
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32916
static inline int check_rule_32916(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 488
    if (tcph->dest != htons(488))
        return 0;
    // Content matching (simplified)
    // Full pattern: |65 DB 37 37 37 37 37 37|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32916
static inline void alert_rule_32916(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32916;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper outbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32917
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32917
static inline int check_rule_32917(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: [8080, 133, 117, 189]
    if (ntohs(tcph->source) != 8080 && ntohs(tcph->source) != 133 && ntohs(tcph->source) != 117 && ntohs(tcph->source) != 189)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|7B 08 2A 2A|",offset 17', '|08 2A 2A 01 00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32917
static inline void alert_rule_32917(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32917;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper inbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32918
// Rule Name: MALWARE-BACKDOOR Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32918
static inline int check_rule_32918(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Sleepy!@#qaz13402scvsde890",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32918
static inline void alert_rule_32918(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32918;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32919
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32919
static inline int check_rule_32919(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |C9 06 D9 96 FC 37 23 5A FE F9 40 BA 4C 94 14 98|",depth 16
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32919
static inline void alert_rule_32919(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32919;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32920
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32920
static inline int check_rule_32920(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |AA 64 BA F2 56|",depth 50
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32920
static inline void alert_rule_32920(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32920;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32921
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32921
static inline int check_rule_32921(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |AA 74 BA F2 B9 75|",depth 74
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32921
static inline void alert_rule_32921(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32921;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32922
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32922
static inline int check_rule_32922(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |0C 1F 1F 1F 4D 5A 4C 4F 50 51 4C 5A 3F 2D 2F 2F 3F 50 54 3E 3E 3E|",depth 22
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32922
static inline void alert_rule_32922(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32922;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32923
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32923
static inline int check_rule_32923(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |D3 C4 D2 D1 CE CF D2 C4 A1 B3 B1 B1 A1 CE CA A0 A0 A0|",depth 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32923
static inline void alert_rule_32923(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32923;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32924
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32924
static inline int check_rule_32924(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |17 08 14 13 67 0F 13 13 17 67 15 02 16 12 02 14 13 78 47 47|",depth 24
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32924
static inline void alert_rule_32924(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32924;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32925
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32925
static inline int check_rule_32925(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |4F 50 4C 4B 3F 57 4B 4B 4F 3F 4D 5A 4E 4A 5A 4C 4B 20 1F|",depth 23
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32925
static inline void alert_rule_32925(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32925;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32926
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32926
static inline int check_rule_32926(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |15 02 14 17 08 09 14 02 67 75 77 77 67 08 0C 66 66 66|",depth 22
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32926
static inline void alert_rule_32926(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32926;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32927
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32927
static inline int check_rule_32927(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |09 22 33 30 28 35 2C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32927
static inline void alert_rule_32927(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32927;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32928
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32928
static inline int check_rule_32928(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |13 2F 22 35 22 67 26 35 22 29 27 33 67 28 37 22 29 67 37 28 35 33 34 69|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32928
static inline void alert_rule_32928(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32928;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32929
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32929
static inline int check_rule_32929(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |43 47 47 47 45 67 47 47 43 47 47 47 44 67 47 47|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32929
static inline void alert_rule_32929(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32929;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32930
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32930
static inline int check_rule_32930(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |43 47 47 47 42 67 47 47 43 47 47 47 4F 67 47 47 43 47 47 47 43 67 47 47 43 47 47 47 4E 67 47 47|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32930
static inline void alert_rule_32930(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32930;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32931
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32931
static inline int check_rule_32931(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |D1 CE D2 D5 A1 C9 D5 D5 D1 A1 D3 C4 D0 D4 C4 D2 D5 BE|",depth 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32931
static inline void alert_rule_32931(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32931;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32932
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32932
static inline int check_rule_32932(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |17 08 14 13 67 0F 13 13 17 67 15 02 16 12 02 14 13 78|",depth 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32932
static inline void alert_rule_32932(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32932;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32933
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper listener download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32933
static inline int check_rule_32933(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |0C 1F 1F 1F 4F 50 4C 4B 3F 57 4B 4B 4F 3F 4D 5A 4E 4A 5A 4C 4B 20|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32933
static inline void alert_rule_32933(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32933;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper listener download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper listener download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32934
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32934
static inline int check_rule_32934(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |8A 10 80 EA 62 80 F2 B4 88 10|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32934
static inline void alert_rule_32934(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32934;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32935
// Rule Name: MALWARE-OTHER Win.Trojan.Wiper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32935
static inline int check_rule_32935(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |8A 10 80 C2 4E 80 F2 79 88 10|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32935
static inline void alert_rule_32935(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32935;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Trojan.Wiper download attempt", 
                     sizeof("MALWARE-OTHER Win.Trojan.Wiper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32936
// Rule Name: MALWARE-TOOLS Win.Trojan.Wiper proxy tools download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32936
static inline int check_rule_32936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |8A 10 80 C2 3A 80 F2 73 88 10|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32936
static inline void alert_rule_32936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-TOOLS Win.Trojan.Wiper proxy tools download attempt", 
                     sizeof("MALWARE-TOOLS Win.Trojan.Wiper proxy tools download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32937
// Rule Name: MALWARE-TOOLS Win.Trojan.Wiper proxy communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32937
static inline int check_rule_32937(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;



    // Content matching (simplified)
    // Full pattern: ['!"HTTP/1', '|E2 1D 49 49|",depth 4,fast_pattern', '|49 49 49 49|",within 4,distance 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32937
static inline void alert_rule_32937(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32937;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-TOOLS Win.Trojan.Wiper proxy communication attempt", 
                     sizeof("MALWARE-TOOLS Win.Trojan.Wiper proxy communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32938
// Rule Name: MALWARE-TOOLS Win.Trojan.Wiper proxy tool download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32938
static inline int check_rule_32938(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |82 F4 DE D4 D3 C2 CA F5 C8 C8 D3 82 FB F4 DE D4 D3 C2 CA 94 95 FB D4 D1 C4 CF C8 D4 D3 89 C2 DF C2 87 8A CC 87 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32938
static inline void alert_rule_32938(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32938;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-TOOLS Win.Trojan.Wiper proxy tool download attempt", 
                     sizeof("MALWARE-TOOLS Win.Trojan.Wiper proxy tool download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32956
// Rule Name: MALWARE-CNC Android.CoolReaper.Trojan outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32956
static inline int check_rule_32956(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/dmp/api/",fast_pattern,nocase', 'User-Agent|3A 20|UAC/1.0.0 (Android ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32956
static inline void alert_rule_32956(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32956;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Android.CoolReaper.Trojan outbound connection", 
                     sizeof("MALWARE-CNC Android.CoolReaper.Trojan outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32957
// Rule Name: MALWARE-CNC Win.Trojan.TinyZBot outbound SOAP connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32957
static inline int check_rule_32957(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/checkupdate.asmx",fast_pattern,nocase', 'SOAPAction|3A 20|', 'User-Agent|3A 20|Mozilla/4.0|20 28|compatible|3B 20|MSIE 6.0|3B 20|MS Web Services Client Protocol']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32957
static inline void alert_rule_32957(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32957;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TinyZBot outbound SOAP connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.TinyZBot outbound SOAP connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32958
// Rule Name: MALWARE-CNC Win.Trojan.TinyZBot response connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32958
static inline int check_rule_32958(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<?xml', '<soap:Body><GetFileListResponse xmlns=|22|http|3A 2F 2F|",within 70,distance 200', '<GetFileListResult><string>[ALL]__",within 75,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32958
static inline void alert_rule_32958(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32958;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TinyZBot response connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.TinyZBot response connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32976
// Rule Name: MALWARE-CNC Win.Trojan.Kuluos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32976
static inline int check_rule_32976(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/w1/feed.php",fast_pattern,nocase', '!"Connection|3A 20|', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32976
static inline void alert_rule_32976(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32976;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 32977
// Rule Name: MALWARE-CNC Win.Trojan.Kuluos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 32977
static inline int check_rule_32977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/w1/form.php",fast_pattern,nocase', '!"Connection|3A 20|', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 32977
static inline void alert_rule_32977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 32977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kuluos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kuluos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33047
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - realupdate - Win.Backdoor.Upatre
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33047
static inline int check_rule_33047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['/0/ HTTP/1.', 'User-Agent: realupdate|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33047
static inline void alert_rule_33047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - realupdate - Win.Backdoor.Upatre", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - realupdate - Win.Backdoor.Upatre"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33058
// Rule Name: MALWARE-CNC Win.Backdoor.Medusa variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33058
static inline int check_rule_33058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00|U|00|n|00|d|00|e|00|r|00 20 00|C|00|o|00|n|00|s|00|t|00|r|00|u|00|c|00|t|00|i|00|o|00|n|00|<|00|/', '|00 22 00 3E 00|w|00|w|00|w|00|.|00|m|00|i|00|c|00|r|00|o|00|s|00|o|00|f|00|t|00 2E 00|c|00|o|00|m|00 3C|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33058
static inline void alert_rule_33058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Medusa variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Medusa variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33059
// Rule Name: MALWARE-CNC Win.Backdoor.Medusa variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33059
static inline int check_rule_33059(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/bbc_mirror/', 'search?id=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33059
static inline void alert_rule_33059(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33059;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Medusa variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Medusa variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33060
// Rule Name: MALWARE-CNC Win.Backdoor.Medusa variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33060
static inline int check_rule_33060(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'CNN_Mirror/EN', 'search?id=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33060
static inline void alert_rule_33060(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33060;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Medusa variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Medusa variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33153
// Rule Name: MALWARE-CNC Win.Trojan.Heur variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33153
static inline int check_rule_33153(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '/01/WindowsUpdate",fast_pattern,nocase', '!"User-Agent:', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33153
static inline void alert_rule_33153(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33153;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Heur variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Heur variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33207
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Mazilla/5.0 - Win.Backdoor.Upatre
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33207
static inline int check_rule_33207(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: User-Agent: Mazilla/5.0|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33207
static inline void alert_rule_33207(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33207;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Mazilla/5.0 - Win.Backdoor.Upatre", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Mazilla/5.0 - Win.Backdoor.Upatre"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33212
// Rule Name: PUA-ADWARE SoftPulse variant HTTP response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33212
static inline int check_rule_33212(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: [',|22|installerBehavior|22|:{|22|hideOnInstall|22|:",fast_pattern,nocase', '{|22|time|22|:', '|22|country|22|",within 30', ',|22|countryId|22|:",within 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33212
static inline void alert_rule_33212(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33212;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE SoftPulse variant HTTP response attempt", 
                     sizeof("PUA-ADWARE SoftPulse variant HTTP response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33219
// Rule Name: MALWARE-CNC Win.Trojan.Gamarue variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33219
static inline int check_rule_33219(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/2ldr.php",fast_pattern,nocase', 'User-Agent: Mozilla/4.0|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33219
static inline void alert_rule_33219(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33219;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gamarue variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gamarue variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33220
// Rule Name: MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33220
static inline int check_rule_33220(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['HawkEye Keylogger",fast_pattern,nocase', 'Subject: =?utf-8?B']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33220
static inline void alert_rule_33220(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33220;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33221
// Rule Name: MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33221
static inline int check_rule_33221(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Subject|3A 20|=?utf-8?B?",fast_pattern', '=?=|0D 0A|",within 150']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33221
static inline void alert_rule_33221(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33221;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot", 
                     sizeof("MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33222
// Rule Name: MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33222
static inline int check_rule_33222(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['=0D=0AClipboard",fast_pattern,nocase', '=0D=0AKeyboard",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33222
static inline void alert_rule_33222(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33222;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot", 
                     sizeof("MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33223
// Rule Name: MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33223
static inline int check_rule_33223(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: name=screenshot",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33223
static inline void alert_rule_33223(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33223;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot", 
                     sizeof("MALWARE-CNC Win.Trojan.HawkEye Keylogger exfiltration attempt - clipboard and screenshot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33224
// Rule Name: INDICATOR-COMPROMISE Win.Trojan.Blocker variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 33224
static inline int check_rule_33224(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/5.0 (Windows NT 6.3|3B| WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/35.0.1916.114 Safari/537.36|0D 0A|Host: checkip.dyndns.org|0D 0A|",fast_pattern,nocase', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33224
static inline void alert_rule_33224(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33224;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Win.Trojan.Blocker variant outbound connection attempt", 
                     sizeof("INDICATOR-COMPROMISE Win.Trojan.Blocker variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33227
// Rule Name: MALWARE-CNC Win.Agent.BHHK variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33227
static inline int check_rule_33227(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET / HTTP/1.1|0D 0A|User-Agent: Mozilla/4.0 (compatible|3B| MSIE 7.0|3B| Windows NT 6.0)|0D 0A|Host: windowsupdate.microsoft.com|0D 0A|Connection: Close|0D 0A 0D 0A|",fast_pattern,nocase', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33227
static inline void alert_rule_33227(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33227;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Agent.BHHK variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Agent.BHHK variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33228
// Rule Name: MALWARE-CNC Win.Trojan.Kovter variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33228
static inline int check_rule_33228(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/form2.php",fast_pattern,nocase', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33228
static inline void alert_rule_33228(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33228;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kovter variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kovter variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33282
// Rule Name: MALWARE-CNC Win.Trojan.Upatre variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33282
static inline int check_rule_33282(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/js/jquery-",fast_pattern', '.js?",within 15,distance 1', 'Referer|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33282
static inline void alert_rule_33282(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33282;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Upatre variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Upatre variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33443
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33443
static inline int check_rule_33443(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/r1xpr/r1xe.html",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33443
static inline void alert_rule_33443(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33443;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33444
// Rule Name: MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33444
static inline int check_rule_33444(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/m343ff4ufbnmm4uu4nf34m443frr/",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33444
static inline void alert_rule_33444(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33444;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33449
// Rule Name: MALWARE-CNC Win.Trojan.FileEncoder IP geolocation checkin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33449
static inline int check_rule_33449(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: GET / HTTP/1.1|0D 0A|User-Agent: Mozilla/4.0 (compatible|3B| MSIE 6.0|3B| Windows NT 5.1|3B| SV1|3B| .NET4.0C|3B| .NET4.0E|3B| .NET CLR 2.0.50727|3B| .NET CLR 3.0.4506.2152|3B| .NET CLR 3.5.30729)|0D 0A|Host: ip-addr.es|0D 0A|Cache-Control: no-cache|0D 0A 0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33449
static inline void alert_rule_33449(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33449;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FileEncoder IP geolocation checkin attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.FileEncoder IP geolocation checkin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33450
// Rule Name: MALWARE-CNC Win.Trojan.FileEncoder variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33450
static inline int check_rule_33450(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '=",depth 2', 'Content-Length: 128|0D 0A|",fast_pattern,nocase', 'Content-Type: application/x-www-form-urlencoded|0D 0A|', '|3B 20|MSIE|20|', '!"Accept-Language:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33450
static inline void alert_rule_33450(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33450;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FileEncoder variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FileEncoder variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33452
// Rule Name: PUA-TOOLBARS Win.Toolbar.Crossrider variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33452
static inline int check_rule_33452(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.gif?action=', '&browser=",distance 0', '&osbuild=",distance 0', '&osprod=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33452
static inline void alert_rule_33452(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33452;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-TOOLBARS Win.Toolbar.Crossrider variant outbound connection", 
                     sizeof("PUA-TOOLBARS Win.Toolbar.Crossrider variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33453
// Rule Name: MALWARE-CNC Win.Trojan.Kovter variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33453
static inline int check_rule_33453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/12/index.php",fast_pattern,nocase', 'User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| Trident/7.0|3B| rv:11.0) like Gecko|0D 0A|', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33453
static inline void alert_rule_33453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kovter variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kovter variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33457
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33457
static inline int check_rule_33457(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', 'User-Agent: http://www.pershop.com.br/",fast_pattern,nocase', '.php', '!"Referer:', '!"Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33457
static inline void alert_rule_33457(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33457;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33519
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - ALIZER
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33519
static inline int check_rule_33519(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|ALIZER|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33519
static inline void alert_rule_33519(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33519;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - ALIZER", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - ALIZER"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33520
// Rule Name: MALWARE-CNC Win.Trojan.Zusy inbound CNC response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33520
static inline int check_rule_33520(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0A|Array|0A 28 0A 20 20 20 20 5B|",fast_pattern', '] => ",within 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33520
static inline void alert_rule_33520(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33520;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zusy inbound CNC response", 
                     sizeof("MALWARE-CNC Win.Trojan.Zusy inbound CNC response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33521
// Rule Name: MALWARE-CNC Win.Trojan.Zusy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33521
static inline int check_rule_33521(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['&pcname=",fast_pattern,nocase', 'hwid=",depth 5', '&mode=",within 50', '&system=",within 32', '&version=",within 60']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33521
static inline void alert_rule_33521(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33521;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zusy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zusy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33522
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent - DNS Changer
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33522
static inline int check_rule_33522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|DNS Check|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33522
static inline void alert_rule_33522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent - DNS Changer", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent - DNS Changer"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33523
// Rule Name: MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33523
static inline int check_rule_33523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|",fast_pattern,nocase', '/postinstall.php?', 'src=",within 5', '&medium=",within 15']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33523
static inline void alert_rule_33523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33524
// Rule Name: MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33524
static inline int check_rule_33524(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/updateb.xml?",fast_pattern,nocase', 'rnd=', '&spfail=",within 20', '&guid=",within 15']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33524
static inline void alert_rule_33524(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33524;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DNSChanger variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33547
// Rule Name: MALWARE-CNC Win.Trojan.Turla outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33547
static inline int check_rule_33547(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '?uid=', '&context=",distance 0', '&mode=text",distance 0,fast_pattern', '&data=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33547
static inline void alert_rule_33547(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33547;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Turla outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Turla outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33646
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33646
static inline int check_rule_33646(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/check.action?iid=', '&kernel=",within 8,distance 32']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33646
static inline void alert_rule_33646(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33646;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33647
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33647
static inline int check_rule_33647(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/submit.action?username=', '&password=",within 30', '.tgz",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33647
static inline void alert_rule_33647(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33647;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33648
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33648
static inline int check_rule_33648(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/compiler.action?iid=', '&username=",within 10,distance 32', '&password=",within 30,distance 1', '&kernel=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33648
static inline void alert_rule_33648(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33648;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33649
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - Google Omaha - Win.Trojan.ExtenBro
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33649
static inline int check_rule_33649(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: Google Omaha|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33649
static inline void alert_rule_33649(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33649;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - Google Omaha - Win.Trojan.ExtenBro", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - Google Omaha - Win.Trojan.ExtenBro"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33650
// Rule Name: MALWARE-CNC Win.Trojan.Tinba outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33650
static inline int check_rule_33650(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/preview/', 'Content-Length: 157|0D 0A|', '!"User-Agent|3A 20|', '|00 80 00 00 00|",depth 5,offset 24']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33650
static inline void alert_rule_33650(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33650;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Tinba outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Tinba outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33677
// Rule Name: MALWARE-CNC Win.Trojan.Babar outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33677
static inline int check_rule_33677(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bb/index.php', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSI 6.0|3B|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33677
static inline void alert_rule_33677(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33677;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Babar outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Babar outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33678
// Rule Name: MALWARE-CNC Win.Trojan.FannyWorm outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33678
static inline int check_rule_33678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0 (compatible|3B|)|0D 0A|",fast_pattern,nocase', '/ads/QueryRecord', '.html",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33678
static inline void alert_rule_33678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FannyWorm outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FannyWorm outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33740
// Rule Name: FILE-IMAGE Microsoft emf file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 33740
static inline int check_rule_33740(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern:  EMF",depth 4,offset 40
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33740
static inline void alert_rule_33740(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33740;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IMAGE Microsoft emf file download request", 
                     sizeof("FILE-IMAGE Microsoft emf file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33815
// Rule Name: PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 33815
static inline int check_rule_33815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/install.ashx?id=",fast_pattern,nocase', 'User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33815
static inline void alert_rule_33815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection", 
                     sizeof("PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33816
// Rule Name: PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 33816
static inline int check_rule_33816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ping.ashx?action=",fast_pattern,nocase', '&usid=', '&aff=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33816
static inline void alert_rule_33816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection", 
                     sizeof("PUA-ADWARE Adware Goobzo/CrossRider variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33822
// Rule Name: MALWARE-CNC Win.Trojan.Egamipload variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33822
static inline int check_rule_33822(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/service/related?sector=",fast_pattern,nocase', 'Mozilla|2F|4.0 (compatible|3B| MSIE 8.0|3B| Windows NT 5.1|3B| Trident/4.0)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33822
static inline void alert_rule_33822(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33822;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Egamipload variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Egamipload variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33825
// Rule Name: OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 33825
static inline int check_rule_33825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|73 00 00 00 00|",depth 9,offset 4', '|00 00|",within 2,distance 13', '|FF|",within 1,distance 9', 'NTLMSSP|00 03 00 00 00|",within 100', '|00 00 00 00 40 00 00 00|",within 8,distance 24']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33825
static inline void alert_rule_33825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB NTLM NULL session attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33833
// Rule Name: PUA-ADWARE User-Agent adware OutBrowse/Amonitize
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33833
static inline int check_rule_33833(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla', ' Loader|0D 0A|",within 150,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33833
static inline void alert_rule_33833(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33833;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE User-Agent adware OutBrowse/Amonitize", 
                     sizeof("PUA-ADWARE User-Agent adware OutBrowse/Amonitize"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33834
// Rule Name: PUA-ADWARE User-Agent adware OutBrowse/Amonitize
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33834
static inline int check_rule_33834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|', ' Pi/3.1415926|0D 0A|",within 150,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33834
static inline void alert_rule_33834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE User-Agent adware OutBrowse/Amonitize", 
                     sizeof("PUA-ADWARE User-Agent adware OutBrowse/Amonitize"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33835
// Rule Name: PUA-ADWARE User-Agent adware OutBrowse/Amonitize
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33835
static inline int check_rule_33835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|', ' in my heart of heart.|0D 0A|",within 150,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33835
static inline void alert_rule_33835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE User-Agent adware OutBrowse/Amonitize", 
                     sizeof("PUA-ADWARE User-Agent adware OutBrowse/Amonitize"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33851
// Rule Name: MALWARE-CNC Win.Trojan.Poseidon outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33851
static inline int check_rule_33851(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Mozilla/4.0 (compatible|3B| MSIE 8.0|3B| Windows NT 6.1|3B| Trident/4.0|3B| SLCC2|3B| .NET CLR 2.0.50727|3B| .NET CLR 3.5.30729|3B| .NET CLR 3.0.30729|3B| Media Center PC 6.0)",fast_pattern,nocase', 'uid=",depth 4', '&uinfo=",within 26', '&win=",distance 0', '&bits=",within 6,distance 3', '&build=",within 20,distance 8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33851
static inline void alert_rule_33851(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33851;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Poseidon outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Poseidon outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33852
// Rule Name: MALWARE-CNC Win.Trojan.Poseidon outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33852
static inline int check_rule_33852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Mozilla/4.0 (compatible|3B| MSIE 8.0|3B| Windows NT 6.1|3B| Trident/4.0|3B| SLCC2|3B| .NET CLR 2.0.50727|3B| .NET CLR 3.5.30729|3B| .NET CLR 3.0.30729|3B| Media Center PC 6.0)', 'oprat=",depth 6', '&uinfo=",within 10,distance 23', '&win=",distance 0', '&vers=",within 6,distance 3']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33852
static inline void alert_rule_33852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Poseidon outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Poseidon outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 33885
// Rule Name: MALWARE-CNC Win.Trojan.Gh0st variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 33885
static inline int check_rule_33885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['KrisR",depth 5', '|00 00 00|",within 3,distance 1', '|00 00 78 9C|",within 4,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 33885
static inline void alert_rule_33885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 33885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gh0st variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gh0st variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34047
// Rule Name: MALWARE-CNC Win.Trojan.VBPasswordStealer variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34047
static inline int check_rule_34047(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?', 'action=add",fast_pattern', '&username=",distance 0', '&password=",distance 0', '&app=",distance 0', '&pcname=",distance 0', '&sitename=",distance 0', '!"Accept', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34047
static inline void alert_rule_34047(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34047;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.VBPasswordStealer variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.VBPasswordStealer variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34119
// Rule Name: PUA-ADWARE InstallMetrix precheck stage outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34119
static inline int check_rule_34119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/installer_gate_client.php?",fast_pattern,nocase', 'download_id=', '&mode=prechecking",distance 0', '!"Accept', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34119
static inline void alert_rule_34119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE InstallMetrix precheck stage outbound connection", 
                     sizeof("PUA-ADWARE InstallMetrix precheck stage outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34120
// Rule Name: PUA-ADWARE InstallMetrix fetch offers stage outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34120
static inline int check_rule_34120(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/installer_gate_client.php?",fast_pattern,nocase', 'download_id=', '&mode=getcombo",distance 0', '&offers=",distance 0', '!"Accept', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34120
static inline void alert_rule_34120(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34120;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE InstallMetrix fetch offers stage outbound connection", 
                     sizeof("PUA-ADWARE InstallMetrix fetch offers stage outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34121
// Rule Name: PUA-ADWARE InstallMetrix reporting binary installation stage status
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34121
static inline int check_rule_34121(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|",fast_pattern,nocase', '|22|event_type|22|",offset 1', '|22|environment|22|",distance 0', '|22|machine_ID|22|",distance 0', '|22|result|22|",distance 0', '|22|failure_reason|22|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34121
static inline void alert_rule_34121(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34121;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE InstallMetrix reporting binary installation stage status", 
                     sizeof("PUA-ADWARE InstallMetrix reporting binary installation stage status"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34122
// Rule Name: PUA-ADWARE InstallMetrix reporting fetch offers stage status
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34122
static inline int check_rule_34122(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/report.php?', 'download_id=",distance 0', '&mode=",distance 0', '&combo_id=",distance 0', '&os_name=",distance 0', '&os_add=",distance 0', '&os_build=",distance 0', '&proj_id=",distance 0', '&offer_id=",distance 0', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34122
static inline void alert_rule_34122(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34122;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE InstallMetrix reporting fetch offers stage status", 
                     sizeof("PUA-ADWARE InstallMetrix reporting fetch offers stage status"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34125
// Rule Name: PUA-ADWARE User-Agent Vitruvian
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34125
static inline int check_rule_34125(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|Vitruvian",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34125
static inline void alert_rule_34125(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34125;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE User-Agent Vitruvian", 
                     sizeof("PUA-ADWARE User-Agent Vitruvian"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34126
// Rule Name: PUA-ADWARE Vitruvian outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34126
static inline int check_rule_34126(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/inst?', 'hid=', '&sid=",distance 0', '&tr=",distance 0', '&a=",distance 0', '&adm=",distance 0', '&os=",distance 0', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| Win32|3B| WinHttp.WinHttpRequest.",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34126
static inline void alert_rule_34126(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34126;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Vitruvian outbound connection", 
                     sizeof("PUA-ADWARE Vitruvian outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34127
// Rule Name: PUA-ADWARE Vitruvian outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34127
static inline int check_rule_34127(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/inst?', 'sid=', '&st=",distance 0', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| Win32|3B| WinHttp.WinHttpRequest.",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34127
static inline void alert_rule_34127(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34127;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Vitruvian outbound connection", 
                     sizeof("PUA-ADWARE Vitruvian outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34136
// Rule Name: MALWARE-CNC Win.Trojan.Banload variant MSSQL response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34136
static inline int check_rule_34136(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 1433
    if (tcph->source != htons(1433))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|0B|m|00|a|00|c|00|a|00|v|00|e|00|r|00|d|00|e|00|m|00|2|00 06|m|00|a|00|s|00|t|00|e|00|r|00|",fast_pattern,nocase', '|08|D|00|B|00|S|00|Q|00|0|00|0|00|1|00|7|00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34136
static inline void alert_rule_34136(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34136;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload variant MSSQL response", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload variant MSSQL response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34137
// Rule Name: PUA-ADWARE SearchProtect user-agent detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34137
static inline int check_rule_34137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|SearchProtect|3B|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34137
static inline void alert_rule_34137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE SearchProtect user-agent detection", 
                     sizeof("PUA-ADWARE SearchProtect user-agent detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34140
// Rule Name: MALWARE-CNC Win.Trojan.Dyre publickey outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34140
static inline int check_rule_34140(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 DE C5 45 99 14 1E F5 7E 56 78 DF 23 CE 8A 12|",fast_pattern,nocase', 'LvtfOWStYYHNbdiE15aNsOyg']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34140
static inline void alert_rule_34140(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34140;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dyre publickey outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dyre publickey outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34144
// Rule Name: PUA-ADWARE SuperOptimizer installation status
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34144
static inline int check_rule_34144(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|",fast_pattern,nocase', '|22|event_type|22|",depth 15,offset 1', '|22|installation_session_id|22|",within 100', '|22|environment|22|",distance 0', '|22|command_line|22|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34144
static inline void alert_rule_34144(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34144;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE SuperOptimizer installation status", 
                     sizeof("PUA-ADWARE SuperOptimizer installation status"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34145
// Rule Name: PUA-ADWARE SuperOptimizer encrypted data transmission
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34145
static inline int check_rule_34145(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|",fast_pattern,nocase', '|22|encryptedKey|22|",depth 20,offset 1', '|22|encryptedData|22|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34145
static inline void alert_rule_34145(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34145;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE SuperOptimizer encrypted data transmission", 
                     sizeof("PUA-ADWARE SuperOptimizer encrypted data transmission"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34146
// Rule Name: PUA-ADWARE SuperOptimizer geolocation request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34146
static inline int check_rule_34146(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ip/?client=sp",fast_pattern,nocase', 'User-Agent|3A 20|NSIS_Inetc (Mozilla)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34146
static inline void alert_rule_34146(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34146;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE SuperOptimizer geolocation request", 
                     sizeof("PUA-ADWARE SuperOptimizer geolocation request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34236
// Rule Name: PUA-ADWARE Eorezo outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34236
static inline int check_rule_34236(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/atJs/v",fast_pattern', '/Client/",within 8,distance 1', '!"Accept', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34236
static inline void alert_rule_34236(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34236;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Eorezo outbound connection", 
                     sizeof("PUA-ADWARE Eorezo outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34237
// Rule Name: PUA-ADWARE Eorezo get advertisement
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34237
static inline int check_rule_34237(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/advert/getads.cgi?', 'did=",distance 0', 'User-Agent|3A 20|mpck_",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34237
static inline void alert_rule_34237(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34237;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Eorezo get advertisement", 
                     sizeof("PUA-ADWARE Eorezo get advertisement"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34261
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34261
static inline int check_rule_34261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/check?iid=', '&kernel=",within 8,distance 32']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34261
static inline void alert_rule_34261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34262
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34262
static inline int check_rule_34262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/compiler?iid=', '&username=",within 10,distance 32', '&password=",within 30,distance 1', '&kernel=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34262
static inline void alert_rule_34262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34263
// Rule Name: MALWARE-CNC Linux.Trojan.XORDDoS outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34263
static inline int check_rule_34263(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/upload/module', 'build.tgz",within 9,distance 32']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34263
static inline void alert_rule_34263(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34263;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.XORDDoS outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.XORDDoS outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34287
// Rule Name: SERVER-WEBAPP vBulletin XSS redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 34287
static inline int check_rule_34287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/misc.php?v=', '&js=js",within 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34287
static inline void alert_rule_34287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP vBulletin XSS redirect attempt", 
                     sizeof("SERVER-WEBAPP vBulletin XSS redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34292
// Rule Name: MALWARE-CNC Win.Trojan.Kraken outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34292
static inline int check_rule_34292(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/idcontact.php?', '&steam=",within 35', '&origin=",within 10', '&webnavig=",within 12']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34292
static inline void alert_rule_34292(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34292;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kraken outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kraken outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34307
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34307
static inline int check_rule_34307(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /get_status.php?name=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34307
static inline void alert_rule_34307(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34307;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34308
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34308
static inline int check_rule_34308(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/products/fupdates.php?', 'account=",distance 0', '&name=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34308
static inline void alert_rule_34308(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34308;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34309
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34309
static inline int check_rule_34309(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/products/file_order', '.php?",within 8', 'name=",distance 0', '&path=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34309
static inline void alert_rule_34309(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34309;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34310
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34310
static inline int check_rule_34310(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/add_user.php?name=', '&user=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34310
static inline void alert_rule_34310(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34310;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34311
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34311
static inline int check_rule_34311(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/new/', '_flash",within 12', '.php?",within 15', 'name=",distance 0', '&serial=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34311
static inline void alert_rule_34311(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34311;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34312
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34312
static inline int check_rule_34312(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/new/get_tree.php?', 'name=",distance 0', '&date=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34312
static inline void alert_rule_34312(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34312;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34313
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34313
static inline int check_rule_34313(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/new/add_tree.php?', 'name=",distance 0', '&date=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34313
static inline void alert_rule_34313(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34313;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34314
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34314
static inline int check_rule_34314(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/new/all_file_info1.php?', 'name=",distance 0', '&user=",distance 0', '&file=",distance 0', '&type=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34314
static inline void alert_rule_34314(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34314;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34315
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34315
static inline int check_rule_34315(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/flupdate/', '.html",within 7']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34315
static inline void alert_rule_34315(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34315;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34316
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34316
static inline int check_rule_34316(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gget_rtemp.php?n=",fast_pattern,nocase', 'User-Agent|3A 20|SK",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34316
static inline void alert_rule_34316(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34316;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34317
// Rule Name: MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34317
static inline int check_rule_34317(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/aadd_rtemp.php?n=",fast_pattern,nocase', 'User-Agent|3A 20|SK",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34317
static inline void alert_rule_34317(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34317;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.DesertFalcon variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34318
// Rule Name: MALWARE-CNC Win.Trojan.CryptoWall variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34318
static inline int check_rule_34318(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?",nocase', '|3D|",within 1,distance 1', 'Cache-Control: no-cache|0D 0A 0D 0A|",nocase', '!"|0D 0A|Accept-', 'Content-Type: application/x-www-form-urlencoded|0D 0A|",fast_pattern,nocase', '|3D|",depth 2,offset 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34318
static inline void alert_rule_34318(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34318;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CryptoWall variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.CryptoWall variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34365
// Rule Name: SERVER-WEBAPP Magento remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 34365
static inline int check_rule_34365(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Adminhtml_', 'forwarded=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34365
static inline void alert_rule_34365(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34365;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Magento remote code execution attempt", 
                     sizeof("SERVER-WEBAPP Magento remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34366
// Rule Name: MALWARE-CNC Win.Trojan.Beebone outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34366
static inline int check_rule_34366(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSIE 7.0|3B| Windows NT 5.1|3B| SV1)|0D 0A|",fast_pattern,nocase', 'GET']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34366
static inline void alert_rule_34366(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34366;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Beebone outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Beebone outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34367
// Rule Name: MALWARE-CNC Win.Trojan.Banload variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34367
static inline int check_rule_34367(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/arquivo/vrs.txt",fast_pattern,nocase', 'Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34367
static inline void alert_rule_34367(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34367;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34368
// Rule Name: MALWARE-CNC Win.Trojan.Banload variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34368
static inline int check_rule_34368(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/arquivo/cookie.txt",fast_pattern,nocase', 'Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34368
static inline void alert_rule_34368(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34368;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34452
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34452
static inline int check_rule_34452(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/poppxr/popi.html",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34452
static inline void alert_rule_34452(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34452;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34453
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34453
static inline int check_rule_34453(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['sname=",depth 6', '.php HTTP/1.0|0D 0A|', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34453
static inline void alert_rule_34453(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34453;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34461
// Rule Name: MALWARE-CNC Linux.Trojan.Mumblehard variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34461
static inline int check_rule_34461(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['POST / HTTP/1.0|0D 0A|Host: ",depth 28', 'Content-type: application/x-www-form-urlencoded|0D 0A|Content-Length: ",within 100', '|0D 0A 0D 0A 0F 0F 09|",within 25,fast_pattern', '!"User-Agent: ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34461
static inline void alert_rule_34461(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34461;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.Mumblehard variant outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.Mumblehard variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34462
// Rule Name: MALWARE-CNC Linux.Downloader.Mumblehard variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34462
static inline int check_rule_34462(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| rv:7.0.1) Gecko/20100101 Firefox/7.0.1|0D 0A|",fast_pattern,nocase', 'Accept: text/html,application/xhtml+xml,application/xml|3B|q=0.8,*/*|3B|q=0.9|0D 0A|', 'Accept-Language: en-us,en|3B|q=0.5|0D 0A|",distance 0', 'Accept-Encoding: gzip, deflate|0D 0A|",distance 0', 'Accept-Charset: ISO-8859-1,utf-8|3B|q=0.7,*|3B|q=0.7|0D 0A|",distance 0', 'Connection: close|0D 0A 0D 0A|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34462
static inline void alert_rule_34462(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34462;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Downloader.Mumblehard variant outbound connection", 
                     sizeof("MALWARE-CNC Linux.Downloader.Mumblehard variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34500
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Wekby Torn variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34500
static inline int check_rule_34500(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 00 00 11 C8 00 00 00 00 00 00 00 00 00 00 00|",depth 16,fast_pattern,fast_pattern_offset 0,fast_pattern_length 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34500
static inline void alert_rule_34500(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34500;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Wekby Torn variant outbound connection", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Wekby Torn variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34501
// Rule Name: MALWARE-CNC Win.Backdoor.Wekby Torn variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34501
static inline int check_rule_34501(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 00 00 11 D0 00 00 00|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34501
static inline void alert_rule_34501(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34501;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Wekby Torn variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Wekby Torn variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34622
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34622
static inline int check_rule_34622(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/popkx3/popi.html",fast_pattern,nocase', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34622
static inline void alert_rule_34622(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34622;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34843
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - EMERY - Win.Trojan.W97M
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34843
static inline int check_rule_34843(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|EMERY|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34843
static inline void alert_rule_34843(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34843;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - EMERY - Win.Trojan.W97M", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - EMERY - Win.Trojan.W97M"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34864
// Rule Name: INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34864
static inline int check_rule_34864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03 03|', '|0B|",within 1,distance 2', '|30 82|",within 2,distance 9', '|30 82|",within 2,distance 2', '|A0 03 02 01 02 02|",within 6,distance 2', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01|",within 22', '|31|",within 1,distance 5', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30|",within 10,distance 3', '|17 0D|",within 2,distance 1', 'Z|17 0D|",within 3,distance 12', 'Z|30|",within 2,distance 12', '|31|",within 1,distance 1', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30 82|",within 9,distance 2', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01 01 05 00 03 82|",within 17,distance 2', '|30 82|",within 2,distance 3', '|02 82|",within 2,distance 2', '|02 03 01 00 01 A3 0D 30 0B 30 09 06 03 55 1D 13 04 02 30 00 30 0D 06 09 2A 86 48 86 F7 0D 01 01 0B 05 00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34864
static inline void alert_rule_34864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate", 
                     sizeof("INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34868
// Rule Name: MALWARE-CNC Win.Trojan.Rovnix variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34868
static inline int check_rule_34868(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/vbulletin/post.php?qu=",fast_pattern,nocase', '!"User-Agent:', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34868
static inline void alert_rule_34868(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34868;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rovnix variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Rovnix variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34917
// Rule Name: MALWARE-CNC Win.Trojan.Critroni certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34917
static inline int check_rule_34917(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 21
    if (tcph->source != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 D3 62 47 DA 62 4A A1 34|', '|3B 02 49 86 4B DF D7 D7 6C E2 2F 36 81 01 24 3F|",within 400']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34917
static inline void alert_rule_34917(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34917;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Critroni certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Critroni certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34931
// Rule Name: MALWARE-CNC Win.Trojan.Bancos variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34931
static inline int check_rule_34931(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['ID_MAQUINA=",fast_pattern,nocase', '&VERSAO=",nocase', '&WIN=",within 50,nocase', '&NAVEGADOR=",within 200,nocase', '&PLUGIN=",within 50,nocase', '&AV=",within 50,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34931
static inline void alert_rule_34931(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34931;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bancos variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bancos variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34950
// Rule Name: MALWARE-CNC Win.Trojan.Prok variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34950
static inline int check_rule_34950(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/prok/', 'Content-Type: multipart/form-data, boundary=7DF051D",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34950
static inline void alert_rule_34950(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34950;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Prok variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Prok variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34958
// Rule Name: MALWARE-CNC Win.Trojan.Androm variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34958
static inline int check_rule_34958(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/forum/image.php",fast_pattern,nocase', '|0D 0A|User-Agent: Mozilla/4.0|0D 0A|', '|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34958
static inline void alert_rule_34958(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34958;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Androm variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Androm variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34959
// Rule Name: MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34959
static inline int check_rule_34959(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['texto=%0D%0A",depth 12', '/consulta', 'User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)|0D 0A 0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34959
static inline void alert_rule_34959(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34959;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SpyBanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34964
// Rule Name: PUA-ADWARE Win.Adware.Sendori user-agent detection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 34964
static inline int check_rule_34964(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|Sendori-Client-Win32",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34964
static inline void alert_rule_34964(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34964;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.Sendori user-agent detection", 
                     sizeof("PUA-ADWARE Win.Adware.Sendori user-agent detection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34994
// Rule Name: MALWARE-CNC Win.Trojan.Banbra variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34994
static inline int check_rule_34994(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/imagens/nacional/new/1/2/3/br/contador.php",fast_pattern,nocase', 'User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| WOW64|3B| rv:12.0) Gecko/20100101 Firefox/12.0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34994
static inline void alert_rule_34994(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34994;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banbra variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banbra variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34995
// Rule Name: MALWARE-CNC Win.Trojan.Banbra HTTP Header Structure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34995
static inline int check_rule_34995(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| WOW64|3B| rv:12.0) Gecko/20100101 Firefox/12.0|0D 0A 0D 0A|",fast_pattern,nocase', '.php HTTP/1.1|0D 0A|Content-Type: text/html|0D 0A|Host: ', '.php']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34995
static inline void alert_rule_34995(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34995;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banbra HTTP Header Structure", 
                     sizeof("MALWARE-CNC Win.Trojan.Banbra HTTP Header Structure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34996
// Rule Name: MALWARE-CNC Win.Trojan.Agent-ALPW variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34996
static inline int check_rule_34996(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|0D 0A|Accept: text/html, */*|0D 0A|Accept-Encoding: identity|0D 0A|User-Agent: Mozilla/5.0 (Windows NT 6.1|3B| WOW64|3B| rv:12.0) Gecko/20100101 Firefox/12.0|0D 0A 0D 0A|",fast_pattern,nocase', 'A=",depth 2', '.php']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34996
static inline void alert_rule_34996(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34996;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent-ALPW variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent-ALPW variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 34997
// Rule Name: MALWARE-CNC Win.Trojan.Graftor variant HTTP Response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 34997
static inline int check_rule_34997(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['HTTP/1.1 200 OK|0D 0A|Content-Length: ', '|0D 0A 0D 0A|session:",within 15,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 34997
static inline void alert_rule_34997(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 34997;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Graftor variant HTTP Response", 
                     sizeof("MALWARE-CNC Win.Trojan.Graftor variant HTTP Response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35030
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35030
static inline int check_rule_35030(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/diff.php",fast_pattern,nocase', '|0D 0A|User-Agent: Mozilla/4.0|0D 0A|', '|0D 0A|Content-Type: application/octet-stream|0D 0A|', '|A0 CD 37 A4 5B|",depth 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35030
static inline void alert_rule_35030(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35030;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35076
// Rule Name: MALWARE-CNC Win.Zusy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35076
static inline int check_rule_35076(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/siganofi/rounder.php",fast_pattern,nocase', 'Cache-Control: no-cache', 'Pragma|3A| no-cache|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35076
static inline void alert_rule_35076(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35076;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Zusy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Zusy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35312
// Rule Name: MALWARE-CNC Win.Trojan.Ursnif outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35312
static inline int check_rule_35312(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/photoLibrary/?user=', '&ver=', '&os2=",fast_pattern,nocase', '&type=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35312
static inline void alert_rule_35312(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35312;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ursnif outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Ursnif outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35344
// Rule Name: MALWARE-CNC Win.Trojan.Cryptowall click fraud response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35344
static inline int check_rule_35344(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['2|7C|http://",depth 9', '/search.php|7C|http://",within 60', '|7C|Mozilla/4.0 ",within 100', '/r.php?key=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35344
static inline void alert_rule_35344(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35344;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Cryptowall click fraud response", 
                     sizeof("MALWARE-CNC Win.Trojan.Cryptowall click fraud response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35353
// Rule Name: MALWARE-CNC Win.Trojan.Elise.B variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35353
static inline int check_rule_35353(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSIE 8.0)",fast_pattern,nocase', '/page_",depth 6,offset 9,nocase', '.html",within 5,distance 8,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35353
static inline void alert_rule_35353(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35353;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Elise.B variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Elise.B variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35386
// Rule Name: MALWARE-CNC Win.Trojan.Bedep initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35386
static inline int check_rule_35386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['protocolVersion|22|",offset 2', '|22|rev|22|",within 10', '|22|buildId|22|",within 15', '|22|tags|22 3A|",distance 0', '|22|type|22 3A 22|",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35386
static inline void alert_rule_35386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bedep initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bedep initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35387
// Rule Name: MALWARE-CNC Win.Trojan.Andromeda initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35387
static inline int check_rule_35387(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/forum.php",depth 10', 'User-Agent|3A 20|Mozilla/4.0|0D 0A|",fast_pattern,nocase', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35387
static inline void alert_rule_35387(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35387;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Andromeda initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Andromeda initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35388
// Rule Name: MALWARE-CNC Win.Trojan.Andromeda download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35388
static inline int check_rule_35388(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.mod', 'User-Agent|3A 20|Mozilla/4.0|0D 0A|",fast_pattern,nocase', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35388
static inline void alert_rule_35388(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35388;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Andromeda download request", 
                     sizeof("MALWARE-CNC Win.Trojan.Andromeda download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35393
// Rule Name: MALWARE-CNC Win.Trojan.TorrentLocker/Teerac self-signed certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35393
static inline int check_rule_35393(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 01 00 51 02|', '|55 04 06 13 02|XX",fast_pattern,nocase', '|55 04 07 0C 0C|Default City', '|55 04 0A 0C 13|Default Company Ltd",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35393
static inline void alert_rule_35393(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35393;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TorrentLocker/Teerac self-signed certificate", 
                     sizeof("MALWARE-CNC Win.Trojan.TorrentLocker/Teerac self-signed certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35394
// Rule Name: MALWARE-CNC Win.Trojan.TorrentLocker/Teerac payment page request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35394
static inline int check_rule_35394(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?user_code=', '&user_pass=",fast_pattern,nocase', 'Referer|3A|', 'tor",within 30,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35394
static inline void alert_rule_35394(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35394;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TorrentLocker/Teerac payment page request", 
                     sizeof("MALWARE-CNC Win.Trojan.TorrentLocker/Teerac payment page request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35549
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35549
static inline int check_rule_35549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/order.php', '|0D 0A|User-Agent: Mozilla/4.0|0D 0A|', '|0D 0A|Content-Type: application/octet-stream|0D 0A|', '|A0 CD 37 A4 5B|",depth 5,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35549
static inline void alert_rule_35549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35733
// Rule Name: MALWARE-CNC Win.Trojan.Potao outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35733
static inline int check_rule_35733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |3C|methodName|3E|10a7d030-1a61-11e3-beea-001c42e2a08b|3C 2F|methodName|3E|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35733
static inline void alert_rule_35733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Potao outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Potao outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35734
// Rule Name: SERVER-WEBAPP Netgear WNDR4700 and R6200 admin interface authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 35734
static inline int check_rule_35734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /BRS_03B_haveBackupFile_fileRestore.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35734
static inline void alert_rule_35734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNDR4700 and R6200 admin interface authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNDR4700 and R6200 admin interface authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35745
// Rule Name: INDICATOR-COMPROMISE Wild Neutron potential exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35745
static inline int check_rule_35745(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.swf?', 'styleid=",distance 0', '&langid=",distance 0', '&sid=",distance 0', '&d=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35745
static inline void alert_rule_35745(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35745;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Wild Neutron potential exploit attempt", 
                     sizeof("INDICATOR-COMPROMISE Wild Neutron potential exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35746
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35746
static inline int check_rule_35746(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/atomic.php",fast_pattern,nocase', '|0D 0A|User-Agent: Mozilla/4.0|0D 0A|', '|A0 CD 37 A4 5B|",depth 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35746
static inline void alert_rule_35746(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35746;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35749
// Rule Name: MALWARE-CNC Win.Backdoor.IsSpace outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35749
static inline int check_rule_35749(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/SNews.asp?HostID=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35749
static inline void alert_rule_35749(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35749;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.IsSpace outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.IsSpace outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35750
// Rule Name: MALWARE-CNC Win.Backdoor.IsSpace initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 35750
static inline int check_rule_35750(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/STTip.asp",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35750
static inline void alert_rule_35750(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35750;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.IsSpace initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.IsSpace initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 35852
// Rule Name: FILE-IDENTIFY JPEG file upload detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 35852
static inline int check_rule_35852(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |FF D8 FF E1|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 35852
static inline void alert_rule_35852(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 35852;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY JPEG file upload detected", 
                     sizeof("FILE-IDENTIFY JPEG file upload detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36058
// Rule Name: FILE-IDENTIFY OLE Document upload detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 36058
static inline int check_rule_36058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Disposition|3A|",nocase', 'Form-data|3B|",within 20,nocase', '|D0 CF 11 E0 A1 B1 1A E1|",within 200,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36058
static inline void alert_rule_36058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY OLE Document upload detected", 
                     sizeof("FILE-IDENTIFY OLE Document upload detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36064
// Rule Name: MALWARE-CNC Win.Trojan.Bagsu variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36064
static inline int check_rule_36064(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/rp?v=",fast_pattern,nocase', '!"User-Agent:', '&u=', '&c=",within 3,distance 32', '&f=",distance 0', '&a=",distance 0', '&d=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36064
static inline void alert_rule_36064(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36064;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bagsu variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bagsu variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36065
// Rule Name: MALWARE-CNC Win.Trojan.Bagsu variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36065
static inline int check_rule_36065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/offers_new?v=",fast_pattern,nocase', '!"User-Agent', '&a=', '&i=",distance 0', '&f=",distance 0', '&u=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36065
static inline void alert_rule_36065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bagsu variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bagsu variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36066
// Rule Name: MALWARE-CNC Win.Trojan.Bagsu variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36066
static inline int check_rule_36066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/4.0 (compatible|3B| MyApp)|0D 0A 0D 0A|",fast_pattern,nocase', 'windows=",depth 8', '&av=",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36066
static inline void alert_rule_36066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bagsu variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bagsu variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36107
// Rule Name: MALWARE-CNC Win.Trojan.FakeAV variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36107
static inline int check_rule_36107(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/purchase.php?a=",fast_pattern,nocase', '&v=', '&u=",distance 0', '&bgload=",within 8,distance 32']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36107
static inline void alert_rule_36107(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36107;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FakeAV variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FakeAV variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36108
// Rule Name: MALWARE-CNC Win.Trojan.Nimisi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36108
static inline int check_rule_36108(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['!"User-Agent', '/logs.php?&prog=",fast_pattern,nocase', '&url=', '&user=",distance 0', '&pass=",distance 0', '&comp=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36108
static inline void alert_rule_36108(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36108;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Nimisi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Nimisi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36202
// Rule Name: MALWARE-CNC Win.Trojan.Yakes variant dropper
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36202
static inline int check_rule_36202(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/document.php?rnd=",fast_pattern,nocase', '&id=",depth 4,offset 22']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36202
static inline void alert_rule_36202(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36202;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Yakes variant dropper", 
                     sizeof("MALWARE-CNC Win.Trojan.Yakes variant dropper"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36611
// Rule Name: INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 36611
static inline int check_rule_36611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03 01|', '|0B|",within 1,distance 2', '|30 82|",within 2,distance 9', '|30 82|",within 2,distance 2', '|A0 03 02 01 02 02|",within 6,distance 2', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01|",within 22', '|31|",within 1,distance 5', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30|",within 1,distance string_size', '|17 0D|",within 2,distance 1', 'Z|17 0D|",within 3,distance 12', 'Z|30|",within 2,distance 12', '|31|",within 1,distance 1', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30 82|",within 2,distance string_size', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01 01 05 00 03 82|",within 17,distance 2', '|30 82|",within 2,distance 3', '|02 82|",within 2,distance 2', '|02 03 01 00 01 A3 0D 30 0B 30 09 06 03 55 1D 13 04 02 30 00 30 0D 06 09 2A 86 48 86 F7 0D 01 01 0B 05 00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36611
static inline void alert_rule_36611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate", 
                     sizeof("INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36612
// Rule Name: INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 36612
static inline int check_rule_36612(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03 02|', '|0B|",within 1,distance 2', '|30 82|",within 2,distance 9', '|30 82|",within 2,distance 2', '|A0 03 02 01 02 02|",within 6,distance 2', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01|",within 22', '|31|",within 1,distance 5', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30|",within 10,distance 3', '|17 0D|",within 2,distance 1', 'Z|17 0D|",within 3,distance 12', 'Z|30|",within 2,distance 12', '|31|",within 1,distance 1', '|30|",within 1,distance 1', '|06 03 55 04 03 0C|",within 6,distance 1', '|30 82|",within 9,distance 2', '|30 0D 06 09 2A 86 48 86 F7 0D 01 01 01 05 00 03 82|",within 17,distance 2', '|30 82|",within 2,distance 3', '|02 82|",within 2,distance 2', '|02 03 01 00 01 A3 0D 30 0B 30 09 06 03 55 1D 13 04 02 30 00 30 0D 06 09 2A 86 48 86 F7 0D 01 01 0B 05 00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36612
static inline void alert_rule_36612(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36612;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate", 
                     sizeof("INDICATOR-COMPROMISE Metasploit Meterpreter reverse HTTPS certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 36914
// Rule Name: MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 36914
static inline int check_rule_36914(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wp-admin/', '.exe|20|HTTP/1.",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 36914
static inline void alert_rule_36914(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 36914;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site", 
                     sizeof("MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37045
// Rule Name: MALWARE-CNC Win.Trojan.Kovter outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37045
static inline int check_rule_37045(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/counter/?",fast_pattern,nocase', 'UA-CPU', 'MSIE 7.0|3B|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37045
static inline void alert_rule_37045(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37045;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kovter outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kovter outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37245
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37245
static inline int check_rule_37245(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: =@eval(base64_decode($_POST",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37245
static inline void alert_rule_37245(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37245;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37467
// Rule Name: MALWARE-CNC Win.Trojan.Vawtrak variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37467
static inline int check_rule_37467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/rss/feed/stream",fast_pattern,nocase', '|3F|",depth 1,offset 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37467
static inline void alert_rule_37467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Vawtrak variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Vawtrak variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37521
// Rule Name: MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37521
static inline int check_rule_37521(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Recoveries/Browser.txt",fast_pattern,nocase', '!"User-Agent', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37521
static inline void alert_rule_37521(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37521;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37522
// Rule Name: MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37522
static inline int check_rule_37522(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Recoveries/Mail.txt",fast_pattern,nocase', '!"User-Agent', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37522
static inline void alert_rule_37522(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37522;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37523
// Rule Name: MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37523
static inline int check_rule_37523(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Recoveries/OSKey.txt",fast_pattern,nocase', '!"User-Agent', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37523
static inline void alert_rule_37523(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37523;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpySoft variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37552
// Rule Name: MALWARE-CNC Win.Trojan.Engr variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37552
static inline int check_rule_37552(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php', 'boundary=Xu02=$",fast_pattern,nocase', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37552
static inline void alert_rule_37552(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37552;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Engr variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Engr variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37646
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant dropper download connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37646
static inline int check_rule_37646(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |A6 4D AA E1 65 52 A5 E1 E3 58 76 E1 81 4D A5 E1 CE 48 9C E1 BB 4D A5 E1 CE 48 A9 E1 A1 4D A5 E1|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37646
static inline void alert_rule_37646(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37646;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant dropper download connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant dropper download connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37647
// Rule Name: MALWARE-CNC Win.Trojan.Symmi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37647
static inline int check_rule_37647(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/vip.jpg",fast_pattern,nocase', 'User-Agent: Mozilla/4.0 (compatible)|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37647
static inline void alert_rule_37647(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37647;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Symmi variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Symmi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37733
// Rule Name: MALWARE-CNC Win.Trojan.Dridex dropper variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37733
static inline int check_rule_37733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gt.jpg?",fast_pattern', '=",within 1,distance 15', 'bytes=6433-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37733
static inline void alert_rule_37733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex dropper variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex dropper variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37814
// Rule Name: POLICY-OTHER Polycom Botnet inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37814
static inline int check_rule_37814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1720
    if (tcph->dest != htons(1720))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|03 00|",depth 2', '|08|",distance 2', '|05|",distance 4', 'MERA RTU",within 100,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37814
static inline void alert_rule_37814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Polycom Botnet inbound connection attempt", 
                     sizeof("POLICY-OTHER Polycom Botnet inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37815
// Rule Name: POLICY-OTHER Polycom Botnet inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37815
static inline int check_rule_37815(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 1720
    if (tcph->dest != htons(1720))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|03 00|",depth 2', '|08 02|",within 2,distance 2', 'EE|A8 C6|3",within 80', 'ooh323",distance 6,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37815
static inline void alert_rule_37815(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37815;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Polycom Botnet inbound connection attempt", 
                     sizeof("POLICY-OTHER Polycom Botnet inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37816
// Rule Name: MALWARE-CNC Win.Trojan.Kazy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37816
static inline int check_rule_37816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['post=",depth 5,fast_pattern', '/index.php', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37816
static inline void alert_rule_37816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kazy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kazy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37834
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37834
static inline int check_rule_37834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/lockycrypt.rar",fast_pattern,nocase', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37834
static inline void alert_rule_37834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37835
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 37835
static inline int check_rule_37835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /34gf5y/r34f3345g",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37835
static inline void alert_rule_37835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 37963
// Rule Name: INDICATOR-COMPROMISE malicious file download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 37963
static inline int check_rule_37963(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |2F 70 6F 63|
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 37963
static inline void alert_rule_37963(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 37963;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE malicious file download attempt", 
                     sizeof("INDICATOR-COMPROMISE malicious file download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38255
// Rule Name: MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38255
static inline int check_rule_38255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0|0D 0A|",fast_pattern,nocase', 'Pragma|3A 20|no-cache', 'Proxy-Connection|3A 20|Keep-Alive|0D 0A|', 'POST', '/photos/photo.asp', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38255
static inline void alert_rule_38255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection", 
                     sizeof("MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38256
// Rule Name: MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38256
static inline int check_rule_38256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['CONNECT', 'User-Agent|3A 20|Mozilla/4.0|0D 0A|",fast_pattern,nocase', 'Pragma|3A 20|no-cache', 'Proxy-Connection|3A 20|Keep-Alive|0D 0A|', 'Accept: */*', 'Accept-Encoding|3A| identity', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38256
static inline void alert_rule_38256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection", 
                     sizeof("MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38257
// Rule Name: MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38257
static inline int check_rule_38257(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSIE 6.0|3B| Windows NT 5.1)|0D 0A|",fast_pattern,nocase', 'Pragma|3A 20|no-cache', 'Cache-Control|3A 20|no-cache', 'GET', '/Query.asp?loginid=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38257
static inline void alert_rule_38257(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38257;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection", 
                     sizeof("MALWARE-CNC Win-Linux.Trojan.Derusbi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38258
// Rule Name: MALWARE-CNC Win/Linux.Trojan.Derusbi variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38258
static inline int check_rule_38258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSIE 6.0|3B| Windows NT 5.1)|0D 0A|",fast_pattern,nocase', 'Pragma|3A 20|no-cache', 'Cache-Control|3A 20|no-cache', 'POST', '/login1.asp']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38258
static inline void alert_rule_38258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win/Linux.Trojan.Derusbi variant outbound connection", 
                     sizeof("MALWARE-CNC Win/Linux.Trojan.Derusbi variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38331
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38331
static inline int check_rule_38331(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/main.php",fast_pattern,nocase', '!"|0D 0A|Accept|2D|Language|3A|', '!"|0D 0A|Referer|3A|', '!"|0D 0A|Cookie|3A|', 'Content-Length|3A 20|', 'Connection|3A 20|Keep-Alive|0D 0A|Cache-Control|3A 20|no-cache']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38331
static inline void alert_rule_38331(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38331;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38333
// Rule Name: MALWARE-CNC Linux.Trojan.Bifrose outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38333
static inline int check_rule_38333(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|9B 4F B0 75 E2 76 96 04 5A F1 F9 43 D4 A2 6B|",depth 15,offset 4', '|76 13 85 45 17 1B|",within 6,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38333
static inline void alert_rule_38333(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38333;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.Trojan.Bifrose outbound connection", 
                     sizeof("MALWARE-CNC Linux.Trojan.Bifrose outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38353
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38353
static inline int check_rule_38353(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |43 00 00 00 05|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38353
static inline void alert_rule_38353(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38353;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38354
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant failed read logs
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38354
static inline int check_rule_38354(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |01 00 00 00 3C|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38354
static inline void alert_rule_38354(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38354;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant failed read logs", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant failed read logs"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38355
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38355
static inline int check_rule_38355(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |01 00 00 00 01|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38355
static inline void alert_rule_38355(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38355;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38357
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant send credentials
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38357
static inline int check_rule_38357(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |01 00 00 00 3D|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38357
static inline void alert_rule_38357(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38357;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant send credentials", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant send credentials"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38359
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant send mail credentials
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38359
static inline int check_rule_38359(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |01 00 00 00 41|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38359
static inline void alert_rule_38359(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38359;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant send mail credentials", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant send mail credentials"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38378
// Rule Name: MALWARE-CNC Win.Trojan.Dridex certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38378
static inline int check_rule_38378(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 4043
    if (tcph->source != htons(4043))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|0B|",distance 3', '|55 04 07 0C 06|Lisbon', '|55 04 0A 0C 10|Souppi Otiop SEM",distance 6', '|55 04 03 0C 0E|wthcethesmw.ph']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38378
static inline void alert_rule_38378(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38378;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38379
// Rule Name: MALWARE-CNC Win.Trojan.Dridex file download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38379
static inline int check_rule_38379(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['FeintedEscalator",fast_pattern,nocase', 'InkingGrange']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38379
static inline void alert_rule_38379(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38379;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex file download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex file download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38380
// Rule Name: MALWARE-CNC Win.Trojan.Dridex file download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38380
static inline int check_rule_38380(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['FeintedEscalator",fast_pattern,nocase', 'InkingGrange']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38380
static inline void alert_rule_38380(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38380;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex file download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex file download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38385
// Rule Name: MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38385
static inline int check_rule_38385(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: USER obitex@benfoods.tk|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38385
static inline void alert_rule_38385(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38385;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38386
// Rule Name: MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38386
static inline int check_rule_38386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: PASS Goodman1986|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38386
static inline void alert_rule_38386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38387
// Rule Name: MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38387
static inline int check_rule_38387(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['STOR Screenshot from|3A 20|",fast_pattern', '|29|.png",within 80']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38387
static inline void alert_rule_38387(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38387;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FTPKeyLogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38388
// Rule Name: MALWARE-CNC Win.Trojan.FTPKeyLogger geolocation check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38388
static inline int check_rule_38388(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/geoip/geoip.php",fast_pattern,nocase', '!"Accept', '!"User-Agent', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38388
static inline void alert_rule_38388(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38388;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FTPKeyLogger geolocation check", 
                     sizeof("MALWARE-CNC Win.Trojan.FTPKeyLogger geolocation check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38509
// Rule Name: MALWARE-CNC Win.Trojan.Boaxxe variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38509
static inline int check_rule_38509(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |7C 7C|CM01|7C|CM02|7C|CM03|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38509
static inline void alert_rule_38509(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38509;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Boaxxe variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Boaxxe variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38510
// Rule Name: MALWARE-CNC Win.Trojan.iSpySoft variant exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38510
static inline int check_rule_38510(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '/api?upload",fast_pattern,nocase', 'Expect|3A 20|', '!"User-Agent', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38510
static inline void alert_rule_38510(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38510;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpySoft variant exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpySoft variant exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38517
// Rule Name: MALWARE-CNC binary download while video expected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38517
static inline int check_rule_38517(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A 20|video/quicktime|0D 0A 0D 0A|', 'MZ",within 2', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38517
static inline void alert_rule_38517(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38517;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC binary download while video expected", 
                     sizeof("MALWARE-CNC binary download while video expected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38557
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38557
static inline int check_rule_38557(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/News/gate.php",fast_pattern,nocase', 'Connection|3A 20|Keep-Alive', '!"Accept', '!"Content-Type', 'User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38557
static inline void alert_rule_38557(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38557;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38558
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38558
static inline int check_rule_38558(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/News/gate.php",fast_pattern,nocase', '=",depth 4', 'User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38558
static inline void alert_rule_38558(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38558;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38559
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - keystorkes
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38559
static inline int check_rule_38559(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/News/gate.php?",fast_pattern,nocase', '<br><br><b><big>']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38559
static inline void alert_rule_38559(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38559;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - keystorkes", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - keystorkes"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38560
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - screenshot
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38560
static inline int check_rule_38560(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/News/gate.php?",fast_pattern,nocase', 'JFIF']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38560
static inline void alert_rule_38560(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38560;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - screenshot", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger outbound connection - screenshot"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38561
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger plugins download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38561
static inline int check_rule_38561(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.p HTTP/1.1",fast_pattern,nocase', '/plugins/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38561
static inline void alert_rule_38561(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38561;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger plugins download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger plugins download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38562
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger initial exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38562
static inline int check_rule_38562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php",fast_pattern,nocase', 'pc=",nocase', '&admin=",distance 0,nocase', '&os=",distance 0,nocase', '&hid=",distance 0,nocase', '&arc=",distance 0,nocase', 'User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38562
static inline void alert_rule_38562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger initial exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger initial exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38563
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger fake 404 response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38563
static inline int check_rule_38563(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['200', 'OK', '>404 Not Found<",fast_pattern,nocase', ' requested URL / was not found ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38563
static inline void alert_rule_38563(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38563;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger fake 404 response", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger fake 404 response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38564
// Rule Name: MALWARE-CNC Win.Trojan.GateKeylogger keylog exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38564
static inline int check_rule_38564(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/post.php?",fast_pattern,nocase', 'pl=', '&education=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38564
static inline void alert_rule_38564(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38564;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.GateKeylogger keylog exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.GateKeylogger keylog exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38565
// Rule Name: MALWARE-CNC Win.Trojan.Sweeper variant dropper initial download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38565
static inline int check_rule_38565(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HEAD', '.bin",fast_pattern,nocase', 'User-Agent|3A 20|Microsoft BITS', 'Accept-Encoding|3A 20|identity|0D 0A|', '!"Content-Length']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38565
static inline void alert_rule_38565(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38565;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sweeper variant dropper initial download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Sweeper variant dropper initial download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38566
// Rule Name: MALWARE-CNC Win.Trojan.Sweeper variant dropper download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38566
static inline int check_rule_38566(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.bin",fast_pattern,nocase', 'User-Agent|3A 20|Microsoft BITS', 'Accept-Encoding|3A 20|identity|0D 0A|', 'If-Unmodified-Since', 'Range']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38566
static inline void alert_rule_38566(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38566;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sweeper variant dropper download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Sweeper variant dropper download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38580
// Rule Name: FILE-OFFICE RFT document malformed header
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 38580
static inline int check_rule_38580(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |7B 5C|rtvpn",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38580
static inline void alert_rule_38580(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38580;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE RFT document malformed header", 
                     sizeof("FILE-OFFICE RFT document malformed header"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38581
// Rule Name: FILE-OFFICE RFT document malformed header
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 38581
static inline int check_rule_38581(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |7B 5C|rtvpn",depth 7
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38581
static inline void alert_rule_38581(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38581;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE RFT document malformed header", 
                     sizeof("FILE-OFFICE RFT document malformed header"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38584
// Rule Name: MALWARE-CNC Win.Backdoor.DFSCook variant JS dropper outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38584
static inline int check_rule_38584(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/img/script.php?",fast_pattern,nocase', 'Accept|3A 20|*/*|0D 0A|', 'UA-CPU|3A 20|', '!"Referer', '!"Accept-Language']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38584
static inline void alert_rule_38584(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38584;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.DFSCook variant JS dropper outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.DFSCook variant JS dropper outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38585
// Rule Name: MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38585
static inline int check_rule_38585(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wp-includes.php?d=",fast_pattern,nocase', 'Accept|3A 20|*/*|0D 0A|', 'Connection|3A 20|close|0D 0A|', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38585
static inline void alert_rule_38585(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38585;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38586
// Rule Name: MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38586
static inline int check_rule_38586(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/api.php?d=",fast_pattern,nocase', 'Accept|3A 20|*/*|0D 0A|', 'Connection|3A 20|close|0D 0A|', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38586
static inline void alert_rule_38586(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38586;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38587
// Rule Name: MALWARE-CNC Win.Backdoor.DFSCook variant temporary redirect attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38587
static inline int check_rule_38587(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['307', 'Temporary Redirect', 'Set-Cookie|3A 20|DFSCOOK=",fast_pattern,nocase', 'Location: ', '/api.php?d=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38587
static inline void alert_rule_38587(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38587;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.DFSCook variant temporary redirect attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.DFSCook variant temporary redirect attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38588
// Rule Name: MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38588
static inline int check_rule_38588(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?d=",fast_pattern,nocase', 'Accept|3A 20|*/*', '!"User-Agent', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38588
static inline void alert_rule_38588(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38588;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.DFSCook variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38594
// Rule Name: APP-DETECT Bloomberg web crawler outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 38594
static inline int check_rule_38594(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: BLP_bbot",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38594
static inline void alert_rule_38594(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38594;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "APP-DETECT Bloomberg web crawler outbound connection", 
                     sizeof("APP-DETECT Bloomberg web crawler outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38603
// Rule Name: MALWARE-CNC Win.Trojan.UP007 variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38603
static inline int check_rule_38603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.asp",fast_pattern,nocase', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| MSIE 7.0|3B| Windows NT 5.1|3B|)', 'Accept-Language|3A 20|en-us|0D 0A|', 'UP007']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38603
static inline void alert_rule_38603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.UP007 variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.UP007 variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38606
// Rule Name: MALWARE-CNC Win.Trojan.Qakbot variant network speed test
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38606
static inline int check_rule_38606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/random750x750.jpg?x=",fast_pattern,nocase', '&y=', 'Accept|3A 20|application/x-shockwave-flash, image/gif, image/jpeg, image/pjpeg, */*|0D 0A|', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"Accept-', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38606
static inline void alert_rule_38606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Qakbot variant network speed test", 
                     sizeof("MALWARE-CNC Win.Trojan.Qakbot variant network speed test"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38607
// Rule Name: MALWARE-CNC Win.Trojan.Qakbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38607
static inline int check_rule_38607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['btst=', 'snkz=', 'Accept|3A 20|application/x-shockwave-flash, image/gif, image/jpeg, image/pjpeg, */*|0D 0A|",fast_pattern,nocase', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"Connection', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38607
static inline void alert_rule_38607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Qakbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Qakbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38608
// Rule Name: MALWARE-CNC Win.Trojan.RockLoader variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38608
static inline int check_rule_38608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/api/",fast_pattern,nocase', 'Content-Type|3A 20|octet-stream', 'Cache-Control|3A 20|no-cache|0D 0A|', '!"User-Agent', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38608
static inline void alert_rule_38608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.RockLoader variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.RockLoader variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38610
// Rule Name: MALWARE-CNC Win.Trojan.Godzilla downloader successful base64 binary download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38610
static inline int check_rule_38610(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GODZILLA=",fast_pattern,nocase', 'GODZILLA=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38610
static inline void alert_rule_38610(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38610;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Godzilla downloader successful base64 binary download", 
                     sizeof("MALWARE-CNC Win.Trojan.Godzilla downloader successful base64 binary download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38619
// Rule Name: INDICATOR-COMPROMISE Content-Type text/plain containing Portable Executable data
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38619
static inline int check_rule_38619(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A 20|text/plain', 'MZ",depth 2', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38619
static inline void alert_rule_38619(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38619;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Content-Type text/plain containing Portable Executable data", 
                     sizeof("INDICATOR-COMPROMISE Content-Type text/plain containing Portable Executable data"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38620
// Rule Name: MALWARE-CNC Win.Trojan.Dridex certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38620
static inline int check_rule_38620(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|0B|",distance 3', '|55 04 07 0C 0B|Ouagadougou', '|55 04 0A 0C 16|Tiongon Wledb A.M.B.A.",distance 6', '|55 04 03 0C 10|ina.themanyag.zm",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38620
static inline void alert_rule_38620(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38620;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38621
// Rule Name: MALWARE-CNC Win.Trojan.Dridex certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38621
static inline int check_rule_38621(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|0B|",distance 3', '|55 04 07 0C 09|Bujumbura', '|55 04 0A 0C 10|Wiqur Hitin ehf.",distance 6', '|55 04 03 0C 11|puppeitursilth.cz",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38621
static inline void alert_rule_38621(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38621;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38886
// Rule Name: MALWARE-CNC Win.Trojan.Bayrob variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38886
static inline int check_rule_38886(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |4C 48 42 80 71 C2 A5 DF|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38886
static inline void alert_rule_38886(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38886;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bayrob variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Bayrob variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38887
// Rule Name: MALWARE-CNC Win.Trojan.Locky JS dropper outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38887
static inline int check_rule_38887(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/log.php?",fast_pattern,nocase', 'UA-CPU', 'Accept|3A 20|*/*', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38887
static inline void alert_rule_38887(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38887;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky JS dropper outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky JS dropper outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38888
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38888
static inline int check_rule_38888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/userinfo.php",fast_pattern,nocase', 'Cache-Control|3A 20|no-cache|0D 0A|', 'Content-Type|3A 20|application/x-www-form-urlencoded|0D 0A|', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38888
static inline void alert_rule_38888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38890
// Rule Name: MALWARE-CNC Win.Trojan.Kirts exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38890
static inline int check_rule_38890(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?fname=Hawkeye_Keylogger",fast_pattern,nocase', '&data=', '!"User-Agent', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38890
static inline void alert_rule_38890(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38890;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kirts exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Kirts exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38891
// Rule Name: MALWARE-CNC Win.Trojan.Kirts initial registration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38891
static inline int check_rule_38891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: Subject|3A 20|=?utf-8?B?SGF3a0V5ZSBMb2dnZXIg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38891
static inline void alert_rule_38891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kirts initial registration", 
                     sizeof("MALWARE-CNC Win.Trojan.Kirts initial registration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38950
// Rule Name: MALWARE-CNC Win.Trojan.PassStealer passwords exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38950
static inline int check_rule_38950(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Passwords Recorded On ",fast_pattern', 'Time of Recording:",within 20,distance 22', 'IP Address",within 12,distance 15']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38950
static inline void alert_rule_38950(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38950;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PassStealer passwords exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.PassStealer passwords exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38951
// Rule Name: PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38951
static inline int check_rule_38951(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/installer.php?', 'CODE=",fast_pattern,nocase', 'UID=', 'action=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38951
static inline void alert_rule_38951(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38951;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt", 
                     sizeof("PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38952
// Rule Name: PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38952
static inline int check_rule_38952(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/optin.php?",fast_pattern,nocase', 'f=', 'quant=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38952
static inline void alert_rule_38952(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38952;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt", 
                     sizeof("PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38953
// Rule Name: PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 38953
static inline int check_rule_38953(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/info.php?', 'quant=",fast_pattern,nocase', 'f=', 'h=', 'size=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38953
static inline void alert_rule_38953(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38953;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt", 
                     sizeof("PUA-ADWARE Win.Adware.OpenSoftwareUpdater variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 38993
// Rule Name: SQL use of sleep function in HTTP header - likely SQL injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 38993
static inline int check_rule_38993(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| ', 'sleep(",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 38993
static inline void alert_rule_38993(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 38993;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SQL use of sleep function in HTTP header - likely SQL injection attempt", 
                     sizeof("SQL use of sleep function in HTTP header - likely SQL injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39064
// Rule Name: MALWARE-CNC Win.Trojan.Sinrin initial JS dropper outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39064
static inline int check_rule_39064(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept|3A 20|*/*|0D 0A|UA-CPU|3A 20|",fast_pattern,nocase', 'Connection|3A 20|Keep-Alive|0D 0A|', 'Accept-Encoding|3A 20|gzip, deflate|0D 0A|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39064
static inline void alert_rule_39064(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39064;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Sinrin initial JS dropper outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Sinrin initial JS dropper outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39080
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39080
static inline int check_rule_39080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |3B 00 00 00 05|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39080
static inline void alert_rule_39080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant connection setup"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39106
// Rule Name: MALWARE-CNC Win.Trojan.LuminosityLink RAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39106
static inline int check_rule_39106(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['=P4CK3T=",depth 32', '8_=_8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39106
static inline void alert_rule_39106(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39106;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.LuminosityLink RAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.LuminosityLink RAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39107
// Rule Name: MALWARE-CNC Win.Trojan.LuminosityLink RAT variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39107
static inline int check_rule_39107(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['=P4CK3T=",depth 32', '8_=_8']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39107
static inline void alert_rule_39107(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39107;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.LuminosityLink RAT variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.LuminosityLink RAT variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39159
// Rule Name: MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39159
static inline int check_rule_39159(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03 01|', '|02|",distance 2', '|03 01|",distance 3', '|55 04 06 13 02|FR', '|55 04 0A 13 0C|assylias.Inc",distance 6', '|55 04 03 13 08|assylias",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39159
static inline void alert_rule_39159(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39159;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate", 
                     sizeof("MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39160
// Rule Name: MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39160
static inline int check_rule_39160(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03 01|', '|02|",distance 2', '|03 01|",distance 3', '|55 04 06 13 02|US', '|55 04 08 13 0A|California",distance 6', '|55 04 07 13 0E|Redwood Shores",distance 6', '|55 04 0A 13 14|Oracle America, Inc.",distance 6', '|55 04 0B 13 13|Code Signing Bureau",distance 6', '|55 04 03 13 14|Oracle America, Inc.",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39160
static inline void alert_rule_39160(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39160;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate", 
                     sizeof("MALWARE-CNC Win.Backdoor.JRat inbound self-signed SSL certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39163
// Rule Name: MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39163
static inline int check_rule_39163(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|59|",distance 3', '|55 04 06 13 02|BN', '|55 04 07 0C 13|Bandar Seri Begawan",distance 6', '|55 04 0A 0C 12|Cowchi Aromep LTD.",distance 6', '|55 04 03 0C 17|tsre131.eollaieefi.jprs",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39163
static inline void alert_rule_39163(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39163;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39164
// Rule Name: MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39164
static inline int check_rule_39164(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|59|",distance 3', '|55 04 06 13 02|PW', '|55 04 07 0C 08|Melekeok",distance 6', '|55 04 0A 0C 0E|Merwh Whena NL",distance 6', '|55 04 03 0C 16|pepa634.omeewengreq.mz",distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39164
static inline void alert_rule_39164(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39164;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Dridex self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39409
// Rule Name: MALWARE-CNC Win.Trojan.iSpy variant initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39409
static inline int check_rule_39409(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['=0D=0A=0D=0A",fast_pattern,nocase', 'iSpy Keylogger', 'Computer Information', 'Username:",within 30', 'Installed",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39409
static inline void alert_rule_39409(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39409;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpy variant initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpy variant initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39410
// Rule Name: MALWARE-CNC Win.Trojan.iSpy variant exfiltration outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39410
static inline int check_rule_39410(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['=0D=0A",fast_pattern,nocase', 'iSpy Keylogger', '=0D=0ABrowser', '=0D=0AWebsite",within 70', '=0D=0AUsername",within 70', '=0D=0APassword",within 70']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39410
static inline void alert_rule_39410(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39410;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpy variant exfiltration outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpy variant exfiltration outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39411
// Rule Name: MALWARE-CNC Win.Trojan.Qbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39411
static inline int check_rule_39411(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['zwlviewforumogaf.php",fast_pattern,nocase', 'Host|3A| a.topgunnphoto.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39411
static inline void alert_rule_39411(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39411;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Qbot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Qbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39526
// Rule Name: FILE-OFFICE RTF document incorrect file magic attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 39526
static inline int check_rule_39526(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |7B 5C|rt|0D 3C|",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39526
static inline void alert_rule_39526(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39526;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE RTF document incorrect file magic attempt", 
                     sizeof("FILE-OFFICE RTF document incorrect file magic attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39527
// Rule Name: FILE-OFFICE RTF document incorrect file magic attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 39527
static inline int check_rule_39527(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |7B 5C|rt|0D 3C|",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39527
static inline void alert_rule_39527(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39527;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE RTF document incorrect file magic attempt", 
                     sizeof("FILE-OFFICE RTF document incorrect file magic attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39528
// Rule Name: FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 39528
static inline int check_rule_39528(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: WRAssembly",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39528
static inline void alert_rule_39528(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39528;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt", 
                     sizeof("FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39529
// Rule Name: FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 39529
static inline int check_rule_39529(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: WRAssembly",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39529
static inline void alert_rule_39529(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39529;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt", 
                     sizeof("FILE-OFFICE Microsoft Office RTF WRAssembly ASLR bypass download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39573
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39573
static inline int check_rule_39573(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |08 00 00 00 27 C7 CC 6B C2 FD 13 0E|",depth 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39573
static inline void alert_rule_39573(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39573;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39574
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39574
static inline int check_rule_39574(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |08 00 00 00 D7 75 FF F7 C7 62 B9 82|",depth 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39574
static inline void alert_rule_39574(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39574;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39575
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39575
static inline int check_rule_39575(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|40 00 00 00|",depth 4', '!"|0A|",within 1,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39575
static inline void alert_rule_39575(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39575;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39576
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39576
static inline int check_rule_39576(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |20 00 00 00 AD|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39576
static inline void alert_rule_39576(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39576;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39577
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39577
static inline int check_rule_39577(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |08 00 00 00 86 CC 02 89 8F F7 A6 67|",depth 12
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39577
static inline void alert_rule_39577(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39577;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39578
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39578
static inline int check_rule_39578(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |20 00 00 00 FE A5 0D 55 BB 10 A4 09 7A D9 86 FF 6C 81 E6 97 7C 91 BC DA EE 89 08 2A|",depth 28
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39578
static inline void alert_rule_39578(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39578;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39579
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39579
static inline int check_rule_39579(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |38 00 00 00 FE A5 0D 55 BB 10 A4 09 7A D9 86 FF 6C 81 E6 97 7C 91 BC DA EE 89 08 2A|",depth 28
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39579
static inline void alert_rule_39579(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39579;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39580
// Rule Name: MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39580
static inline int check_rule_39580(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |40 00 00 00 FE A5 0D 55 BB 10 A4 09 7A D9 86 FF 6C 81 E6 97 7C 91 BC DA EE 89 08 2A|",depth 28
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39580
static inline void alert_rule_39580(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39580;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.NanoBot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39581
// Rule Name: MALWARE-CNC Win.Trojan.NanoBot/Perseus initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39581
static inline int check_rule_39581(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |38 00 00 00 F5 13 89 53|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39581
static inline void alert_rule_39581(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39581;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NanoBot/Perseus initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.NanoBot/Perseus initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39582
// Rule Name: MALWARE-CNC Win.Trojan.NanoBot/Perseus server heartbeat request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39582
static inline int check_rule_39582(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |20 00 00 00 2B FF 4B F4|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39582
static inline void alert_rule_39582(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39582;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NanoBot/Perseus server heartbeat request attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.NanoBot/Perseus server heartbeat request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39583
// Rule Name: MALWARE-CNC Win.Trojan.NanoBot/Perseus client heartbeat response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39583
static inline int check_rule_39583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |30 00 00 00 2B FF 4B F4|",depth 8
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39583
static inline void alert_rule_39583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NanoBot/Perseus client heartbeat response attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.NanoBot/Perseus client heartbeat response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39705
// Rule Name: MALWARE-CNC Win.Trojan.Zeus variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39705
static inline int check_rule_39705(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['attachment|3B|', 'filename=', '/us.xml",within 20,fast_pattern', 'Content-Type|3A 20|application/octet-stream|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39705
static inline void alert_rule_39705(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39705;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zeus variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zeus variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39729
// Rule Name: INDICATOR-COMPROMISE Content-Type image containing Portable Executable data
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39729
static inline int check_rule_39729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type|3A 20|image/",fast_pattern,nocase', 'MZ",depth 2', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39729
static inline void alert_rule_39729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Content-Type image containing Portable Executable data", 
                     sizeof("INDICATOR-COMPROMISE Content-Type image containing Portable Executable data"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39737
// Rule Name: SERVER-WEBAPP HttpOxy CGI application vulnerability potential man-in-the-middle attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 39737
static inline int check_rule_39737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |0A|Proxy|3A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39737
static inline void alert_rule_39737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP HttpOxy CGI application vulnerability potential man-in-the-middle attempt", 
                     sizeof("SERVER-WEBAPP HttpOxy CGI application vulnerability potential man-in-the-middle attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39738
// Rule Name: MALWARE-CNC Win.Trojan.Trans variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39738
static inline int check_rule_39738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /site/images/banners/casecor21.gif",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39738
static inline void alert_rule_39738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Trans variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Trans variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39800
// Rule Name: MALWARE-CNC Win.Trojan.Hancitor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39800
static inline int check_rule_39800(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php",fast_pattern,nocase', 'GUID=",depth 122', 'BUILD=",depth 122', 'INFO=",depth 122', 'IP=",depth 122', 'TYPE=",depth 122']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39800
static inline void alert_rule_39800(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39800;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Hancitor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Hancitor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39801
// Rule Name: MALWARE-CNC Win.Trojan.Spyrat variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39801
static inline int check_rule_39801(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 900
    if (tcph->dest != htons(900))
        return 0;
    // Content matching (simplified)
    // Full pattern: myversion|7C|2.5.2.",depth 19
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39801
static inline void alert_rule_39801(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39801;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Spyrat variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Spyrat variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39903
// Rule Name: FILE-OFFICE Microsoft Windows RTF file with embedded object package SMTP upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 39903
static inline int check_rule_39903(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['{|5C|rt",nocase', '{|5C|object|5C|objemb{|5C|*|5C|objclass Package}",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39903
static inline void alert_rule_39903(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39903;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-OFFICE Microsoft Windows RTF file with embedded object package SMTP upload attempt", 
                     sizeof("FILE-OFFICE Microsoft Windows RTF file with embedded object package SMTP upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 39911
// Rule Name: MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 39911
static inline int check_rule_39911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: Subject: HawkEye Keylogger |7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 39911
static inline void alert_rule_39911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 39911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.HawkEye keylogger exfiltration attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40011
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40011
static inline int check_rule_40011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/data/info.php",fast_pattern,nocase', 'x-requested-with: XMLHttpRequest', 'Referer|3A| http|3A|', '/data",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40011
static inline void alert_rule_40011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40015
// Rule Name: BROWSER-FIREFOX Mozilla Firefox about field spoofing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 40015
static inline int check_rule_40015(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['about:",fast_pattern,nocase', '?",within 15', '<",within 100', 'location",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40015
static inline void alert_rule_40015(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40015;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Mozilla Firefox about field spoofing attempt", 
                     sizeof("BROWSER-FIREFOX Mozilla Firefox about field spoofing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40035
// Rule Name: FILE-IDENTIFY XLSB file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 40035
static inline int check_rule_40035(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PK|03 04|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40035
static inline void alert_rule_40035(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40035;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XLSB file magic detected", 
                     sizeof("FILE-IDENTIFY XLSB file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40036
// Rule Name: FILE-IDENTIFY XLSB file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 40036
static inline int check_rule_40036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: PK|03 04|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40036
static inline void alert_rule_40036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY XLSB file magic detected", 
                     sizeof("FILE-IDENTIFY XLSB file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40063
// Rule Name: OS-LINUX Linux Kernel Challenge ACK provocation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 40063
static inline int check_rule_40063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Check TCP flags
    if (!((tcph->rst == 1)))
        return 0;
    return 1;  // All checks passed
}

// Alert function for rule 40063
static inline void alert_rule_40063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-LINUX Linux Kernel Challenge ACK provocation attempt", 
                     sizeof("OS-LINUX Linux Kernel Challenge ACK provocation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40184
// Rule Name: EXPLOIT-KIT Phoenix Exploit Kit inbound geoip.php bdr exploit attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 40184
static inline int check_rule_40184(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /geoip.php?bdr=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40184
static inline void alert_rule_40184(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40184;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "EXPLOIT-KIT Phoenix Exploit Kit inbound geoip.php bdr exploit attempt", 
                     sizeof("EXPLOIT-KIT Phoenix Exploit Kit inbound geoip.php bdr exploit attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40251
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Win.Trojan.Perseus
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40251
static inline int check_rule_40251(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| bUQ8QmvUpI57udWFxQHPkuyKDfc3T8u5",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40251
static inline void alert_rule_40251(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40251;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Win.Trojan.Perseus", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Win.Trojan.Perseus"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40252
// Rule Name: MALWARE-CNC Win.Perseus variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40252
static inline int check_rule_40252(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['mashine=",fast_pattern,nocase', 'publickey=', 'user=', 'os=', 'processor=', 'mac=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40252
static inline void alert_rule_40252(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40252;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Perseus variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Perseus variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40260
// Rule Name: MALWARE-CNC Osx.Trojan.Keydnap variant initial backdoor download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40260
static inline int check_rule_40260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/icloudsyncd",fast_pattern,nocase', 'Accept|3A 20|*/*', '!"User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40260
static inline void alert_rule_40260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.Keydnap variant initial backdoor download attempt", 
                     sizeof("MALWARE-CNC Osx.Trojan.Keydnap variant initial backdoor download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40541
// Rule Name: MALWARE-CNC Win.Trojan.Satana ransomware outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40541
static inline int check_rule_40541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/add.php",fast_pattern,nocase', 'id=', 'code=', 'sdata=', 'name=', 'md5=', 'dlen=', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40541
static inline void alert_rule_40541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Satana ransomware outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Satana ransomware outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40549
// Rule Name: MALWARE-CNC Win.Trojan.CryPy ransomware variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40549
static inline int check_rule_40549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/victim.php?info=",fast_pattern,nocase', '&ip=', 'info=', 'User-Agent|3A 20|Python-urllib/', '!"Accept', '!"Connection', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40549
static inline void alert_rule_40549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CryPy ransomware variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.CryPy ransomware variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40550
// Rule Name: MALWARE-CNC Win.Trojan.Dexter Banker variant second stage download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40550
static inline int check_rule_40550(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/images/",fast_pattern,nocase', '.rar', 'User-Agent|3A 20|Mozilla/4.0 (compatible|3B| Synapse)|0D 0A|', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40550
static inline void alert_rule_40550(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40550;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dexter Banker variant second stage download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dexter Banker variant second stage download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40551
// Rule Name: MALWARE-CNC Win.Trojan.Dexter Banker variant successful installation report attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40551
static inline int check_rule_40551(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/LetsGo.php?A=",fast_pattern,nocase', 'Sytem=', 'qual=', '!"Accept', '!"referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40551
static inline void alert_rule_40551(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40551;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dexter Banker variant successful installation report attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dexter Banker variant successful installation report attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40559
// Rule Name: MALWARE-CNC Win.Trojan.iSpy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40559
static inline int check_rule_40559(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['iSpyKelogger",fast_pattern,nocase', 'gate=', 'token=",distance 0', 'name=",distance 0', '!"User-Agent', '!"Connection']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40559
static inline void alert_rule_40559(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40559;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.iSpy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.iSpy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40762
// Rule Name: MALWARE-CNC Android.Trojan.SpyNote RAT variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40762
static inline int check_rule_40762(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: Server Prent <please>|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40762
static inline void alert_rule_40762(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40762;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Android.Trojan.SpyNote RAT variant inbound connection", 
                     sizeof("MALWARE-CNC Android.Trojan.SpyNote RAT variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40763
// Rule Name: MALWARE-CNC Android.Trojan.SpyNote RAT variant getSMS command response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40763
static inline int check_rule_40763(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |7C|ge|7C|t|7C|SM|7C|S|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40763
static inline void alert_rule_40763(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40763;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Android.Trojan.SpyNote RAT variant getSMS command response", 
                     sizeof("MALWARE-CNC Android.Trojan.SpyNote RAT variant getSMS command response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40764
// Rule Name: MALWARE-CNC Android.Trojan.SpyNote RAT variant getContacts command response
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40764
static inline int check_rule_40764(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: send|7C|G|7C 7C|Cont|7C|acts|7C|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40764
static inline void alert_rule_40764(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40764;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Android.Trojan.SpyNote RAT variant getContacts command response", 
                     sizeof("MALWARE-CNC Android.Trojan.SpyNote RAT variant getContacts command response"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40816
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40816
static inline int check_rule_40816(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/message.php",fast_pattern,nocase', 'x-requested-with|3A 20|XMLHttpRequest|0D 0A|', 'Referer|3A 20|', 'Accept|3A 20|*/*|0D 0A|Accept-Language|3A 20|en-us|0D 0A|', 'Cache-Control|3A 20|no-cache|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40816
static inline void alert_rule_40816(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40816;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40827
// Rule Name: PUA-ADWARE MindSpark framework installer attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40827
static inline int check_rule_40827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|Mindspark MIP ",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40827
static inline void alert_rule_40827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE MindSpark framework installer attempt", 
                     sizeof("PUA-ADWARE MindSpark framework installer attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40831
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40831
static inline int check_rule_40831(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: new_houdini|0D 0A|",depth 13,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40831
static inline void alert_rule_40831(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40831;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40832
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant keylogger inbound init command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40832
static inline int check_rule_40832(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: silence_keylogger|0D 0A|",depth 19,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40832
static inline void alert_rule_40832(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40832;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant keylogger inbound init command attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant keylogger inbound init command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40833
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound init command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40833
static inline int check_rule_40833(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: screenshot_init|0D 0A|",depth 17,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40833
static inline void alert_rule_40833(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40833;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound init command attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound init command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40834
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound silence command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40834
static inline int check_rule_40834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: silence_screenshot|0D 0A|",depth 20,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40834
static inline void alert_rule_40834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound silence command attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant screenshot inbound silence command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40835
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant screen_thumb inbound init command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40835
static inline int check_rule_40835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: screen_thumb|0D 0A|",depth 14,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40835
static inline void alert_rule_40835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant screen_thumb inbound init command attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant screen_thumb inbound init command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40836
// Rule Name: MALWARE-CNC Win.Backdoor.Houdini variant file enumeration inbound init/root/faf command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40836
static inline int check_rule_40836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: file_manager_",depth 13,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40836
static inline void alert_rule_40836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Houdini variant file enumeration inbound init/root/faf command attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.Houdini variant file enumeration inbound init/root/faf command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40839
// Rule Name: PUA-ADWARE Sokuxuan outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40839
static inline int check_rule_40839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/UpgSvr/",fast_pattern,nocase', '.xml']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40839
static inline void alert_rule_40839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Sokuxuan outbound connection attempt", 
                     sizeof("PUA-ADWARE Sokuxuan outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40840
// Rule Name: PUA-OTHER Bitcoin Mining subscribe Stratum protocol client request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 40840
static inline int check_rule_40840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 1024-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1024 || src_port > 65535)
        return 0;
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|7B 22|id|22 3A|', '|22|method|22 3A 22|mining.subscribe|22|', '|22|params|22 3A|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40840
static inline void alert_rule_40840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER Bitcoin Mining subscribe Stratum protocol client request attempt", 
                     sizeof("PUA-OTHER Bitcoin Mining subscribe Stratum protocol client request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40841
// Rule Name: PUA-OTHER Bitcoin Mining authorize Stratum protocol client request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 40841
static inline int check_rule_40841(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 1024-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1024 || src_port > 65535)
        return 0;
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|7B 22|id|22 3A|', '|22|method|22 3A 22|mining.authorize|22|', '|22|params|22 3A|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40841
static inline void alert_rule_40841(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40841;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER Bitcoin Mining authorize Stratum protocol client request attempt", 
                     sizeof("PUA-OTHER Bitcoin Mining authorize Stratum protocol client request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40842
// Rule Name: PUA-OTHER Bitcoin Mining extranonce Stratum protocol subscribe client request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 40842
static inline int check_rule_40842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port range: 1024-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1024 || src_port > 65535)
        return 0;
    // Check dst port range: 1024-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1024 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|7B 22|id|22 3A|', '|22|method|22 3A 22|mining.extranonce.subscribe|22|', '|22|params|22 3A|",distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40842
static inline void alert_rule_40842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-OTHER Bitcoin Mining extranonce Stratum protocol subscribe client request attempt", 
                     sizeof("PUA-OTHER Bitcoin Mining extranonce Stratum protocol subscribe client request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40888
// Rule Name: BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 40888
static inline int check_rule_40888(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.createElementNS', 'svg",within 10', '.setAttribute', 'begin",within 15', '.setAttribute",distance 0', 'end",within 10', '.end",within 20', '.setAttribute",distance 0', 'end",within 10', '.end",within 20', '.pauseAnimations",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40888
static inline void alert_rule_40888(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40888;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt", 
                     sizeof("BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40896
// Rule Name: BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 40896
static inline int check_rule_40896(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.pauseAnimations",fast_pattern,nocase', 'svg",nocase', 'animate",nocase', 'begin",within 50,nocase', 'end",within 50,nocase', '.end",within 30,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40896
static inline void alert_rule_40896(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40896;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt", 
                     sizeof("BROWSER-FIREFOX Mozilla Firefox ESR NotifyTimeChange use after free attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40911
// Rule Name: MALWARE-CNC Win.Rootkit.Sednit variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40911
static inline int check_rule_40911(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search.php",fast_pattern,nocase', 'as_ft=', 'as_q=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40911
static inline void alert_rule_40911(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40911;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Rootkit.Sednit variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Rootkit.Sednit variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 40991
// Rule Name: MALWARE-CNC Linux.DDoS.D93 outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 40991
static inline int check_rule_40991(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 31337
    if (tcph->dest != htons(31337))
        return 0;
    // Content matching (simplified)
    // Full pattern: |4E 0F 42 07 27|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 40991
static inline void alert_rule_40991(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 40991;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Linux.DDoS.D93 outbound connection", 
                     sizeof("MALWARE-CNC Linux.DDoS.D93 outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41095
// Rule Name: SERVER-WEBAPP Netgear WNR2000 authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41095
static inline int check_rule_41095(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /apply_noauth.cgi",depth 17,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41095
static inline void alert_rule_41095(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41095;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41096
// Rule Name: SERVER-WEBAPP Netgear WNR2000 hidden_lang_avi stack buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41096
static inline int check_rule_41096(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/lang_check",nocase', 'hidden_lang_avi=",nocase', '!"&",within 36']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41096
static inline void alert_rule_41096(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41096;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 hidden_lang_avi stack buffer overflow attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 hidden_lang_avi stack buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41334
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41334
static inline int check_rule_41334(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/admin.php?f=",fast_pattern,nocase', 'UA-CPU|3A 20|', 'MSIE 7.0|3B|', 'Accept|3A 20|*/*', '!"Accept-Language', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41334
static inline void alert_rule_41334(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41334;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41335
// Rule Name: MALWARE-CNC Win.Trojan.Locky variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41335
static inline int check_rule_41335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/checkupdate",fast_pattern,nocase', 'x-requested-with|3A 20|', 'Referer', '=",depth 15', '%",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41335
static inline void alert_rule_41335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Locky variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Locky variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41336
// Rule Name: MALWARE-CNC Andr.Trojan.Sysch variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41336
static inline int check_rule_41336(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['time|3A 20|",fast_pattern,nocase', 'User-Agent|3A 20|HttpEngine', '.do']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41336
static inline void alert_rule_41336(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41336;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Sysch variant outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Sysch variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41337
// Rule Name: MALWARE-CNC Andr.Trojan.Sysch variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41337
static inline int check_rule_41337(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GZIPOK|3A 20|",fast_pattern,nocase', 'CompGZ|3A 20|', 'ReqType|3A 20|', '.do', '!"Accept', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41337
static inline void alert_rule_41337(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41337;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Sysch variant outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Sysch variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41346
// Rule Name: SERVER-WEBAPP Western Digital MyCloud command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41346
static inline int check_rule_41346(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/web/google_analytics.php",fast_pattern,nocase', 'cmd=set",nocase', 'arg=",nocase', 'isAdmin=1",nocase', 'username=admin",nocase', 'local_login=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41346
static inline void alert_rule_41346(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41346;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Western Digital MyCloud command injection attempt", 
                     sizeof("SERVER-WEBAPP Western Digital MyCloud command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41347
// Rule Name: SERVER-WEBAPP Western Digital MyCloud command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41347
static inline int check_rule_41347(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/web/google_analytics.php",fast_pattern,nocase', 'cmd=set",nocase', 'arg=",nocase', '%26",distance 0', 'isAdmin=1",nocase', 'username=admin",nocase', 'local_login=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41347
static inline void alert_rule_41347(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41347;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Western Digital MyCloud command injection attempt", 
                     sizeof("SERVER-WEBAPP Western Digital MyCloud command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41348
// Rule Name: SERVER-WEBAPP Western Digital MyCloud command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41348
static inline int check_rule_41348(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/web/google_analytics.php",fast_pattern,nocase', 'cmd=set",nocase', 'arg=",nocase', 'isAdmin=1",nocase', 'username=admin",nocase', 'local_login=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41348
static inline void alert_rule_41348(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41348;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Western Digital MyCloud command injection attempt", 
                     sizeof("SERVER-WEBAPP Western Digital MyCloud command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41349
// Rule Name: SERVER-WEBAPP Western Digital MyCloud command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41349
static inline int check_rule_41349(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/web/google_analytics.php",fast_pattern,nocase', 'cmd=set",nocase', 'arg",nocase', 'Content-Disposition",nocase', 'isAdmin=1",nocase', 'username=admin",nocase', 'local_login=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41349
static inline void alert_rule_41349(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41349;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Western Digital MyCloud command injection attempt", 
                     sizeof("SERVER-WEBAPP Western Digital MyCloud command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41374
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant registration message
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41374
static inline int check_rule_41374(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |41 00 00 00 83|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41374
static inline void alert_rule_41374(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41374;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant registration message", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant registration message"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41375
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant check logs
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41375
static inline int check_rule_41375(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |38 00 00 00 85|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41375
static inline void alert_rule_41375(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41375;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant check logs", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant check logs"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41376
// Rule Name: MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41376
static inline int check_rule_41376(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |01 00 00 00 81|",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41376
static inline void alert_rule_41376(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41376;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive", 
                     sizeof("MALWARE-CNC Win.Trojan.NetWiredRC variant keepalive"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41409
// Rule Name: POLICY-OTHER Cisco Webex explicit use of web plugin detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 41409
static inline int check_rule_41409(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: cwcsf-nativemsg-iframe-43c85c0d-d633-af5e-c056-32dc7efc570b.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41409
static inline void alert_rule_41409(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41409;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Cisco Webex explicit use of web plugin detected", 
                     sizeof("POLICY-OTHER Cisco Webex explicit use of web plugin detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41441
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - X-Mas
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41441
static inline int check_rule_41441(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|Useragents",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41441
static inline void alert_rule_41441(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41441;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - X-Mas", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - X-Mas"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41442
// Rule Name: MALWARE-CNC Win.Ransomware.X-Mas outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41442
static inline int check_rule_41442(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['WebKitFormBoundary', '|20|form-data|3B 20|name=|22|uid|22|",fast_pattern', '|20|form-data|3B 20|name=|22|uname|22|",distance 0', '|20|form-data|3B 20|name=|22|cname|22|",distance 0', '|20|form-data|3B 20|name=|22|ltime|22|",distance 0', '|20|form-data|3B 20|name=|22|uright|22|",distance 0', '|20|form-data|3B 20|name=|22|sysinfo|22|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41442
static inline void alert_rule_41442(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41442;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.X-Mas outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.X-Mas outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41443
// Rule Name: MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41443
static inline int check_rule_41443(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php",fast_pattern,nocase', '163=', '&x=",distance 0', '&z=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41443
static inline void alert_rule_41443(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41443;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41444
// Rule Name: MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41444
static inline int check_rule_41444(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php?",fast_pattern,nocase', '|3C|br|3E 3C|br|3E 3C|b|3E 3C|big|3E 3C|font color=|22|', '|22 3E 20 5B|",within 12', '!"Accept-', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41444
static inline void alert_rule_41444(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41444;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.X-Mas variant keylogger outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41445
// Rule Name: SERVER-OTHER QNAP remote buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41445
static inline int check_rule_41445(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgi.cgi",fast_pattern,nocase', 'u=', 'p=', '!"&",within 263', '!"|0D 0A|",within 263']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41445
static inline void alert_rule_41445(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41445;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP remote buffer overflow attempt", 
                     sizeof("SERVER-OTHER QNAP remote buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41495
// Rule Name: SERVER-WEBAPP WordPress get_post authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41495
static inline int check_rule_41495(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wp-json/",fast_pattern,nocase', 'id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41495
static inline void alert_rule_41495(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41495;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WordPress get_post authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP WordPress get_post authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41496
// Rule Name: SERVER-WEBAPP WordPress get_post authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41496
static inline int check_rule_41496(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wp-json/",fast_pattern,nocase', 'id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41496
static inline void alert_rule_41496(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41496;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WordPress get_post authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP WordPress get_post authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41497
// Rule Name: SERVER-WEBAPP WordPress get_post authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41497
static inline int check_rule_41497(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wp-json/",fast_pattern,nocase', '|22|id|22|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41497
static inline void alert_rule_41497(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41497;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP WordPress get_post authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP WordPress get_post authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41498
// Rule Name: MALWARE-CNC Win.Ransomware.CryptoLocker binary download response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41498
static inline int check_rule_41498(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Set-Cookie|3A 20|mediaplanBAK|3D|",fast_pattern,nocase', 'Set-Cookie|3A 20|mediaplan|3D|', 'Content-Type|3A 20|text/plain', 'MZ', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41498
static inline void alert_rule_41498(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41498;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.CryptoLocker binary download response attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.CryptoLocker binary download response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41499
// Rule Name: SERVER-SAMBA Microsoft Windows SMBv2/SMBv3 Buffer Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 41499
static inline int check_rule_41499(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FE|SMB|40 00|",depth 6,offset 4', '|03 00|",within 2,distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41499
static inline void alert_rule_41499(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41499;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-SAMBA Microsoft Windows SMBv2/SMBv3 Buffer Overflow attempt", 
                     sizeof("SERVER-SAMBA Microsoft Windows SMBv2/SMBv3 Buffer Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41504
// Rule Name: SERVER-WEBAPP Netgear passwordrecovered.cgi insecure admin password disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 41504
static inline int check_rule_41504(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /passwordrecovered.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41504
static inline void alert_rule_41504(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41504;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear passwordrecovered.cgi insecure admin password disclosure attempt", 
                     sizeof("SERVER-WEBAPP Netgear passwordrecovered.cgi insecure admin password disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41663
// Rule Name: MALWARE-CNC Osx.Downloader.MacDownloader variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41663
static inline int check_rule_41663(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Servermac.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41663
static inline void alert_rule_41663(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41663;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Downloader.MacDownloader variant outbound connection", 
                     sizeof("MALWARE-CNC Osx.Downloader.MacDownloader variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41698
// Rule Name: SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41698
static inline int check_rule_41698(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ping.cgi",nocase', 'ping_IPAddr=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41698
static inline void alert_rule_41698(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41698;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41699
// Rule Name: SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41699
static inline int check_rule_41699(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ping.cgi",nocase', 'ping_IPAddr=",fast_pattern,nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41699
static inline void alert_rule_41699(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41699;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41700
// Rule Name: SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41700
static inline int check_rule_41700(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ping.cgi",nocase', 'ping_IPAddr=",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41700
static inline void alert_rule_41700(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41700;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 ping.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41710
// Rule Name: INDICATOR-COMPROMISE Binary file download request from internationalized domain name using Microsoft BITS
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41710
static inline int check_rule_41710(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| Microsoft BITS', 'Host|3A 20|xn--",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41710
static inline void alert_rule_41710(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41710;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Binary file download request from internationalized domain name using Microsoft BITS", 
                     sizeof("INDICATOR-COMPROMISE Binary file download request from internationalized domain name using Microsoft BITS"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41711
// Rule Name: MALWARE-CNC Win.Trojan.Houdini variant initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41711
static inline int check_rule_41711(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: new_slave|0D 0A|",depth 11,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41711
static inline void alert_rule_41711(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41711;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Houdini variant initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Houdini variant initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41712
// Rule Name: MALWARE-CNC Win.Trojan.Houdini backdoor file download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 41712
static inline int check_rule_41712(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ChromeSetup.bat",fast_pattern,nocase', 'User-Agent|3A| Microsoft BITS']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41712
static inline void alert_rule_41712(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41712;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Houdini backdoor file download request", 
                     sizeof("MALWARE-CNC Win.Trojan.Houdini backdoor file download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41713
// Rule Name: SERVER-WEBAPP DotNetNuke installation attempt detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41713
static inline int check_rule_41713(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Install/InstallWizard.aspx",fast_pattern,nocase', 'executeinstall']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41713
static inline void alert_rule_41713(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41713;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DotNetNuke installation attempt detected", 
                     sizeof("SERVER-WEBAPP DotNetNuke installation attempt detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41722
// Rule Name: SERVER-OTHER Cisco IOS Smart Install protocol backup config command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41722
static inline int check_rule_41722(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4786
    if (tcph->dest != htons(4786))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 01 00 00 00 01 00 00 00 08|",depth 12', '://']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41722
static inline void alert_rule_41722(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41722;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Cisco IOS Smart Install protocol backup config command attempt", 
                     sizeof("SERVER-OTHER Cisco IOS Smart Install protocol backup config command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41723
// Rule Name: SERVER-OTHER Cisco IOS Smart Install protocol download config command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41723
static inline int check_rule_41723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4786
    if (tcph->dest != htons(4786))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 01 00 00 00 01 00 00 00 03|",depth 12', 'tftp://",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41723
static inline void alert_rule_41723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Cisco IOS Smart Install protocol download config command attempt", 
                     sizeof("SERVER-OTHER Cisco IOS Smart Install protocol download config command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41724
// Rule Name: SERVER-OTHER Cisco IOS Smart Install protocol download image command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41724
static inline int check_rule_41724(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4786
    if (tcph->dest != htons(4786))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 01 00 00 00 01 00 00 00 02|",depth 12', 'tftp://",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41724
static inline void alert_rule_41724(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41724;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Cisco IOS Smart Install protocol download image command attempt", 
                     sizeof("SERVER-OTHER Cisco IOS Smart Install protocol download image command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41725
// Rule Name: SERVER-OTHER Cisco IOS Smart Install protocol version command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41725
static inline int check_rule_41725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 4786
    if (tcph->dest != htons(4786))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|00 00 00 02 00 00 00 01 00 00 00 05|",depth 12', 'tftp://",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41725
static inline void alert_rule_41725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Cisco IOS Smart Install protocol version command attempt", 
                     sizeof("SERVER-OTHER Cisco IOS Smart Install protocol version command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41748
// Rule Name: SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41748
static inline int check_rule_41748(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnslookup.cgi",fast_pattern,nocase', 'host_name",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41748
static inline void alert_rule_41748(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41748;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41749
// Rule Name: SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41749
static inline int check_rule_41749(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnslookup.cgi",fast_pattern,nocase', 'host_name=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41749
static inline void alert_rule_41749(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41749;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41750
// Rule Name: SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41750
static inline int check_rule_41750(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnslookup.cgi",fast_pattern,nocase', 'host_name=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41750
static inline void alert_rule_41750(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41750;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41751
// Rule Name: SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 41751
static inline int check_rule_41751(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dnslookup.cgi",fast_pattern,nocase', 'host_name=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41751
static inline void alert_rule_41751(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41751;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200 dnslookup.cgi command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 41978
// Rule Name: OS-WINDOWS Microsoft Windows SMB remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 41978
static inline int check_rule_41978(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: |FF|SMB3|00 00 00 00|",depth 9,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 41978
static inline void alert_rule_41978(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 41978;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB remote code execution attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42019
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string - Andr.Trojan.Agent
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42019
static inline int check_rule_42019(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Ray-Downer|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42019
static inline void alert_rule_42019(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42019;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string - Andr.Trojan.Agent", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string - Andr.Trojan.Agent"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42021
// Rule Name: MALWARE-CNC Andr.Trojan.Agent variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42021
static inline int check_rule_42021(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wroot/v3",fast_pattern,nocase', '.do', 'uuid=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42021
static inline void alert_rule_42021(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42021;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Agent variant outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Agent variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42059
// Rule Name: MALWARE-CNC Win.Ransomware.Sage variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42059
static inline int check_rule_42059(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Host: mbfce24rgn65bx3g.",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42059
static inline void alert_rule_42059(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42059;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Sage variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.Sage variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42128
// Rule Name: MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42128
static inline int check_rule_42128(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A| WinHttpClient",fast_pattern,nocase', '//Home/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42128
static inline void alert_rule_42128(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42128;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42129
// Rule Name: MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42129
static inline int check_rule_42129(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['commandId=",fast_pattern,nocase', '/Home/",depth 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42129
static inline void alert_rule_42129(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42129;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Ismdoor variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42225
// Rule Name: MALWARE-CNC Win.Trojan.RedLeaves outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42225
static inline int check_rule_42225(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [80, 53]
    if (ntohs(tcph->dest) != 80 && ntohs(tcph->dest) != 53)
        return 0;
    // Content matching (simplified)
    // Full pattern: |7A 8D 9B DC|",depth 4,offset 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42225
static inline void alert_rule_42225(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42225;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.RedLeaves outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.RedLeaves outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42255
// Rule Name: OS-WINDOWS Microsoft Windows empty RDP cookie negotiation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 42255
static inline int check_rule_42255(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|08 E0 00 00 00 00|",depth 6,offset 4', '|0D 0A|",within 2,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42255
static inline void alert_rule_42255(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42255;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows empty RDP cookie negotiation attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows empty RDP cookie negotiation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42256
// Rule Name: OS-WINDOWS Microsoft Windows SMB anonymous user session setup request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 42256
static inline int check_rule_42256(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|73 00 00 00 00|",depth 13,offset 4', '|01 00 00 00 00 00 00 00|",within 8,distance 38', '|00 00 00 00 00|",within 5,distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42256
static inline void alert_rule_42256(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42256;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB anonymous user session setup request detected", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB anonymous user session setup request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42331
// Rule Name: MALWARE-CNC Win.Trojan.Doublepulsar variant process injection command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42331
static inline int check_rule_42331(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|32 00 00 00 00|",depth 9,offset 4', '|42 00|",within 2,distance 21', '|0E 00|",within 2,distance 29', '!"|00 00|",within 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42331
static inline void alert_rule_42331(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42331;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Doublepulsar variant process injection command", 
                     sizeof("MALWARE-CNC Win.Trojan.Doublepulsar variant process injection command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42332
// Rule Name: MALWARE-CNC Win.Trojan.Doublepulsar variant ping command
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42332
static inline int check_rule_42332(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|32 00 00 00 00|",depth 9,offset 4', '|41 00|",within 2,distance 21', '|0E 00 0D 00 00 00 00 00 00 00 00 00 00 00 00 00 00|",within 17,distance 29,fast_pattern,fast_pattern_offset 0,fast_pattern_length 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42332
static inline void alert_rule_42332(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42332;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Doublepulsar variant ping command", 
                     sizeof("MALWARE-CNC Win.Trojan.Doublepulsar variant ping command"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42338
// Rule Name: OS-WINDOWS Microsoft Windows SMB large NT RENAME transaction request memory leak attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 42338
static inline int check_rule_42338(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A0|",depth 5,offset 4', '|05 00|",within 2,distance 64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42338
static inline void alert_rule_42338(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42338;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB large NT RENAME transaction request memory leak attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB large NT RENAME transaction request memory leak attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42339
// Rule Name: OS-WINDOWS Microsoft Windows SMB possible leak of kernel heap memory
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 42339
static inline int check_rule_42339(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Frag",fast_pattern', 'Free', '|FA FF FF|', '|F8 FF FF|",within 3,distance 5', '|F8 FF FF|",within 3,distance 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42339
static inline void alert_rule_42339(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42339;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB possible leak of kernel heap memory", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB possible leak of kernel heap memory"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42340
// Rule Name: OS-WINDOWS Microsoft Windows SMB anonymous session IPC share access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 42340
static inline int check_rule_42340(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|75 00 00 00 00|",depth 9,offset 4', '|00 5C 00|I|00|P|00|C|00|$|00 00 00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42340
static inline void alert_rule_42340(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42340;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB anonymous session IPC share access attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB anonymous session IPC share access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42398
// Rule Name: MALWARE-CNC Win.Trojan.RedLeaves outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42398
static inline int check_rule_42398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: [80, 53]
    if (ntohs(tcph->dest) != 80 && ntohs(tcph->dest) != 53)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['856",depth 3,offset 1', '856|9A F3 EC 89|",within 7,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42398
static inline void alert_rule_42398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.RedLeaves outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.RedLeaves outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42834
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42834
static inline int check_rule_42834(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', 'z9=base64%5fdecode",fast_pattern,nocase', '=%40eval']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42834
static inline void alert_rule_42834(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42834;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42835
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42835
static inline int check_rule_42835(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Forwarded-For",nocase', '=edoced_46esab",fast_pattern,nocase', 'z0=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42835
static inline void alert_rule_42835(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42835;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42836
// Rule Name: MALWARE-CNC Win.Backdoor.Chopper web shell connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42836
static inline int check_rule_42836(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: =@eval(get_magic_quotes_gpc()?stripslashes($_POST[",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42836
static inline void alert_rule_42836(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42836;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.Chopper web shell connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.Chopper web shell connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42857
// Rule Name: SERVER-WEBAPP MVPower DVR Shell arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 42857
static inline int check_rule_42857(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /shell?",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42857
static inline void alert_rule_42857(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42857;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MVPower DVR Shell arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP MVPower DVR Shell arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42880
// Rule Name: MALWARE-CNC Deputy Dog implant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42880
static inline int check_rule_42880(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Connect.php?id=",fast_pattern,nocase', 'SessionID:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42880
static inline void alert_rule_42880(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42880;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Deputy Dog implant outbound connection", 
                     sizeof("MALWARE-CNC Deputy Dog implant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42881
// Rule Name: MALWARE-CNC Deputy Dog implant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42881
static inline int check_rule_42881(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/JP-ja/js?",fast_pattern,nocase', 'SessionID:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42881
static inline void alert_rule_42881(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42881;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Deputy Dog implant outbound connection", 
                     sizeof("MALWARE-CNC Deputy Dog implant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42882
// Rule Name: MALWARE-CNC ZoxPNG initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42882
static inline int check_rule_42882(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search?q=Google&go=&qs=n&form=",fast_pattern,nocase', 'pq=google&sc=8-1&sp=-1&sk=', 'Cookie|3A 20|SESSIONID=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42882
static inline void alert_rule_42882(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42882;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC ZoxPNG initial outbound connection", 
                     sizeof("MALWARE-CNC ZoxPNG initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42883
// Rule Name: MALWARE-CNC Win.Trojan.MadMax implant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42883
static inline int check_rule_42883(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mm.jpg",depth 7,fast_pattern', 'User-Agent|3A 20|Mozilla/5.0 (compatible']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42883
static inline void alert_rule_42883(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42883;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MadMax implant outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.MadMax implant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42884
// Rule Name: MALWARE-CNC Win.Trojan.MadMax implant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42884
static inline int check_rule_42884(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/logon.aspx?Id=",fast_pattern,nocase', 'Cookie|3A 20|SessionData=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42884
static inline void alert_rule_42884(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42884;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MadMax implant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MadMax implant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42885
// Rule Name: MALWARE-CNC WashingTon ssl certificate negotiation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 42885
static inline int check_rule_42885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['WashingTon",fast_pattern,nocase', 'WebMaster@Microsoft.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42885
static inline void alert_rule_42885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC WashingTon ssl certificate negotiation attempt", 
                     sizeof("MALWARE-CNC WashingTon ssl certificate negotiation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 42944
// Rule Name: OS-WINDOWS Microsoft Windows SMB remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 42944
static inline int check_rule_42944(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A0 00 00 00 00|",depth 9,offset 4', '|01 00 00 00 00|",within 5,distance 59']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 42944
static inline void alert_rule_42944(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 42944;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB remote code execution attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43002
// Rule Name: PROTOCOL-OTHER NETBIOS SMB IPC share access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 43002
static inline int check_rule_43002(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|75 00 00 00 00|",depth 9,offset 4', 'IPC$|00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43002
static inline void alert_rule_43002(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43002;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER NETBIOS SMB IPC share access attempt", 
                     sizeof("PROTOCOL-OTHER NETBIOS SMB IPC share access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43003
// Rule Name: PROTOCOL-OTHER NETBIOS SMB IPC share access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 43003
static inline int check_rule_43003(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|75 00 00 00 00|",depth 9,offset 4', 'I|00|P|00|C|00|$|00 00 00|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43003
static inline void alert_rule_43003(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43003;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER NETBIOS SMB IPC share access attempt", 
                     sizeof("PROTOCOL-OTHER NETBIOS SMB IPC share access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43004
// Rule Name: SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 43004
static inline int check_rule_43004(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A2 00 00 00 00|",depth 9,offset 4', '/",within 1', '/",within len,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43004
static inline void alert_rule_43004(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43004;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt", 
                     sizeof("SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43063
// Rule Name: MALWARE-CNC Win.Trojan.Kabob outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43063
static inline int check_rule_43063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: @|E9 03 00 00 00 00 00 00 00 00 64|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43063
static inline void alert_rule_43063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kabob outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Kabob outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43193
// Rule Name: MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43193
static inline int check_rule_43193(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |18 17 E9 E9 E9 E9|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43193
static inline void alert_rule_43193(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43193;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43194
// Rule Name: MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43194
static inline int check_rule_43194(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |1B 17 E9 E9 E9 E9|",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43194
static inline void alert_rule_43194(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43194;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.HiddenCobra variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43285
// Rule Name: SERVER-WEBAPP /.svn/entries file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43285
static inline int check_rule_43285(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /.svn/entries",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43285
static inline void alert_rule_43285(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43285;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /.svn/entries file access attempt", 
                     sizeof("SERVER-WEBAPP /.svn/entries file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43286
// Rule Name: SERVER-WEBAPP /cgi-bin/sh file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43286
static inline int check_rule_43286(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/sh",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43286
static inline void alert_rule_43286(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43286;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /cgi-bin/sh file access attempt", 
                     sizeof("SERVER-WEBAPP /cgi-bin/sh file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43287
// Rule Name: SERVER-WEBAPP /etc/inetd.conf file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43287
static inline int check_rule_43287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /etc/inetd.conf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43287
static inline void alert_rule_43287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /etc/inetd.conf file access attempt", 
                     sizeof("SERVER-WEBAPP /etc/inetd.conf file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43288
// Rule Name: SERVER-WEBAPP /etc/motd file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43288
static inline int check_rule_43288(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /etc/motd",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43288
static inline void alert_rule_43288(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43288;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /etc/motd file access attempt", 
                     sizeof("SERVER-WEBAPP /etc/motd file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43289
// Rule Name: SERVER-WEBAPP /etc/shadow file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43289
static inline int check_rule_43289(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /etc/shadow",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43289
static inline void alert_rule_43289(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43289;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /etc/shadow file access attempt", 
                     sizeof("SERVER-WEBAPP /etc/shadow file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43290
// Rule Name: SERVER-WEBAPP /ws_ftp.log file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43290
static inline int check_rule_43290(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ws_ftp.log",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43290
static inline void alert_rule_43290(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43290;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP /ws_ftp.log file access attempt", 
                     sizeof("SERVER-WEBAPP /ws_ftp.log file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43291
// Rule Name: SERVER-WEBAPP Oracle Application Server 9i unauthenticated application deployment attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43291
static inline int check_rule_43291(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /soap/soaplet/soaprouter",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43291
static inline void alert_rule_43291(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43291;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle Application Server 9i unauthenticated application deployment attempt", 
                     sizeof("SERVER-WEBAPP Oracle Application Server 9i unauthenticated application deployment attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43562
// Rule Name: POLICY-OTHER Teleopti WFM database information request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43562
static inline int check_rule_43562(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /TeleoptiWFM/Administration/GetOneTenant",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43562
static inline void alert_rule_43562(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43562;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Teleopti WFM database information request detected", 
                     sizeof("POLICY-OTHER Teleopti WFM database information request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43563
// Rule Name: POLICY-OTHER Teleopti WFM administrative user credentials request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 43563
static inline int check_rule_43563(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /TeleoptiWFM/Administration/Users",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43563
static inline void alert_rule_43563(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43563;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Teleopti WFM administrative user credentials request detected", 
                     sizeof("POLICY-OTHER Teleopti WFM administrative user credentials request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43564
// Rule Name: POLICY-OTHER Teleopti WFM administrative user creation detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 43564
static inline int check_rule_43564(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /TeleoptiWFM/Administration/AddFirstUser",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43564
static inline void alert_rule_43564(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43564;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Teleopti WFM administrative user creation detected", 
                     sizeof("POLICY-OTHER Teleopti WFM administrative user creation detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43663
// Rule Name: SERVER-OTHER WSFTP IpSwitch custom SITE command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 43663
static inline int check_rule_43663(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 21
    if (tcph->dest != htons(21))
        return 0;
    // Content matching (simplified)
    // Full pattern: SITE SETC",nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43663
static inline void alert_rule_43663(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43663;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER WSFTP IpSwitch custom SITE command execution attempt", 
                     sizeof("SERVER-OTHER WSFTP IpSwitch custom SITE command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43809
// Rule Name: SERVER-WEBAPP Kaspersky Linux File Server WMC cross site request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 43809
static inline int check_rule_43809(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9080
    if (tcph->dest != htons(9080))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgictl?action=setTaskSettings",fast_pattern,nocase', 'settings={|22|",nocase', 'taskId=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43809
static inline void alert_rule_43809(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43809;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Kaspersky Linux File Server WMC cross site request forgery attempt", 
                     sizeof("SERVER-WEBAPP Kaspersky Linux File Server WMC cross site request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43810
// Rule Name: SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 43810
static inline int check_rule_43810(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9080
    if (tcph->dest != htons(9080))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgictl",fast_pattern,nocase', 'reportId=",nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43810
static inline void alert_rule_43810(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43810;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43811
// Rule Name: SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 43811
static inline int check_rule_43811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgictl",fast_pattern,nocase', 'reportId=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43811
static inline void alert_rule_43811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43812
// Rule Name: SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 43812
static inline int check_rule_43812(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgictl",fast_pattern,nocase', 'reportId",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43812
static inline void alert_rule_43812(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43812;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Kaspersky Linux File Server WMC directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43813
// Rule Name: SERVER-WEBAPP Kaspersky Linux File Server WMC cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 43813
static inline int check_rule_43813(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgictl",fast_pattern,nocase', 'scriptName=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43813
static inline void alert_rule_43813(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43813;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Kaspersky Linux File Server WMC cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP Kaspersky Linux File Server WMC cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43825
// Rule Name: MALWARE-CNC Osx.Trojan.XAgent outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43825
static inline int check_rule_43825(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['(unknown version)', 'Darwin/",within 30', 'Accept|3A 20|*/*|0D 0A|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43825
static inline void alert_rule_43825(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43825;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.XAgent outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.XAgent outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43957
// Rule Name: SERVER-WEBAPP Ubiquiti Networks UniFi Cloud Key Firm v0.6.1 Host Remote Command Execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 43957
static inline int check_rule_43957(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /api/status",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43957
static inline void alert_rule_43957(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43957;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Ubiquiti Networks UniFi Cloud Key Firm v0.6.1 Host Remote Command Execution attempt", 
                     sizeof("SERVER-WEBAPP Ubiquiti Networks UniFi Cloud Key Firm v0.6.1 Host Remote Command Execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43972
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43972
static inline int check_rule_43972(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gate.php",fast_pattern,nocase', 'WebKitFormBoundary', 'name=|22|getconfig|22|', 'Referer|3A 20|', 'Connection|3A 20|close|0D 0A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43972
static inline void alert_rule_43972(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43972;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43981
// Rule Name: MALWARE-CNC Andr.Trojan.Femas variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43981
static inline int check_rule_43981(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['did=', '/update/upfolder/updatefun.php",fast_pattern,nocase', 'Dalvik/', 'Android",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43981
static inline void alert_rule_43981(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43981;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Femas variant outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Femas variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 43982
// Rule Name: MALWARE-CNC Andr.Trojan.Femas variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 43982
static inline int check_rule_43982(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['did=', '/pockemon/squirtle/functions.php",fast_pattern,nocase', 'Dalvik/', 'Android",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 43982
static inline void alert_rule_43982(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 43982;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Femas variant outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Femas variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44004
// Rule Name: POLICY-OTHER Cisco DDR2200 ASDL gateway file download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44004
static inline int check_rule_44004(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['download.conf",fast_pattern,nocase', 'filename=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44004
static inline void alert_rule_44004(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44004;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Cisco DDR2200 ASDL gateway file download detected", 
                     sizeof("POLICY-OTHER Cisco DDR2200 ASDL gateway file download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44005
// Rule Name: SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44005
static inline int check_rule_44005(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['waitPingqry",fast_pattern,nocase', 'pingAddr=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44005
static inline void alert_rule_44005(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44005;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt", 
                     sizeof("SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44006
// Rule Name: SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44006
static inline int check_rule_44006(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['waitPingqry",fast_pattern,nocase', 'pingAddr=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44006
static inline void alert_rule_44006(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44006;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt", 
                     sizeof("SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44007
// Rule Name: SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44007
static inline int check_rule_44007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['waitPingqry",fast_pattern,nocase', 'pingAddr=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44007
static inline void alert_rule_44007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt", 
                     sizeof("SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44008
// Rule Name: SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44008
static inline int check_rule_44008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['waitPingqry",fast_pattern,nocase', 'pingAddr",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44008
static inline void alert_rule_44008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt", 
                     sizeof("SERVER-WEBAPP Cisco DDR2200 ADSL gateway command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44399
// Rule Name: MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44399
static inline int check_rule_44399(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: [447]
    if (ntohs(tcph->source) != 447)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",distance 13', '3t2t3rgeg', 'fg2eq34df",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44399
static inline void alert_rule_44399(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44399;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44400
// Rule Name: MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44400
static inline int check_rule_44400(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: [447]
    if (ntohs(tcph->source) != 447)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",distance 13', 'f2tee4', 'rvgvtfdf",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44400
static inline void alert_rule_44400(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44400;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44401
// Rule Name: MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44401
static inline int check_rule_44401(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: [447]
    if (ntohs(tcph->source) != 447)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",distance 13', '|00 92 93 45 3A 42 8B 15 4C|",fast_pattern,nocase', 'London', 'example.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44401
static inline void alert_rule_44401(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44401;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44402
// Rule Name: MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44402
static inline int check_rule_44402(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: [447]
    if (ntohs(tcph->source) != 447)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",distance 13', '|00 DC 5E AE E6 3E EC 78 EC|', 'Alaska', 'John_Alaska@gmail.com",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44402
static inline void alert_rule_44402(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44402;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.Trickbot self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44469
// Rule Name: MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44469
static inline int check_rule_44469(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wp-admin",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44469
static inline void alert_rule_44469(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44469;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt", 
                     sizeof("MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44470
// Rule Name: MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44470
static inline int check_rule_44470(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wp-includes",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44470
static inline void alert_rule_44470(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44470;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt", 
                     sizeof("MALWARE-CNC Potential hostile executable served from compromised or malicious WordPress site attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44591
// Rule Name: MALWARE-CNC Win.Trojan.PandaZeus malicious certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44591
static inline int check_rule_44591(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",within 2,distance 13', "Let's Encrypt", 'gloverkentok.us",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44591
static inline void alert_rule_44591(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44591;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PandaZeus malicious certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.PandaZeus malicious certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44592
// Rule Name: MALWARE-CNC Win.Trojan.PandaZeus self-signed certificate exchange
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44592
static inline int check_rule_44592(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03|', '|30 82|",within 2,distance 13', 'My Company Name LTD.', 'domain.com",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44592
static inline void alert_rule_44592(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44592;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PandaZeus self-signed certificate exchange", 
                     sizeof("MALWARE-CNC Win.Trojan.PandaZeus self-signed certificate exchange"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44643
// Rule Name: SERVER-OTHER Mikrotik RouterOS denial of service attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: denial-of-service
// Generated from Snort rule parser output

// Check function for rule 44643
static inline int check_rule_44643(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8291
    if (tcph->dest != htons(8291))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|12 02|",depth 2', '|FF ED 00 00 00 00|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44643
static inline void alert_rule_44643(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44643;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Mikrotik RouterOS denial of service attempt", 
                     sizeof("SERVER-OTHER Mikrotik RouterOS denial of service attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44652
// Rule Name: MALWARE-CNC Win.Zusy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44652
static inline int check_rule_44652(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /QualityCheck/ni6.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44652
static inline void alert_rule_44652(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44652;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Zusy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Zusy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44687
// Rule Name: SERVER-WEBAPP Netgear DGN1000 series routers authentication bypass attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 44687
static inline int check_rule_44687(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/setup.cgi",nocase', 'currentsetting.htm",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44687
static inline void alert_rule_44687(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44687;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN1000 series routers authentication bypass attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN1000 series routers authentication bypass attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44688
// Rule Name: SERVER-WEBAPP Netgear DGN1000 series routers arbitrary command execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 44688
static inline int check_rule_44688(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/setup.cgi",nocase', 'todo=syscmd",fast_pattern,nocase', 'cmd=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44688
static inline void alert_rule_44688(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44688;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN1000 series routers arbitrary command execution attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN1000 series routers arbitrary command execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44689
// Rule Name: MALWARE-CNC Win.Trojan.Gen variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44689
static inline int check_rule_44689(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /aspnet_client/system_web/4_0_30319/update/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44689
static inline void alert_rule_44689(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44689;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gen variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Gen variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44698
// Rule Name: SERVER-WEBAPP Internal field separator use in HTTP URI attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44698
static inline int check_rule_44698(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: $IFS
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44698
static inline void alert_rule_44698(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44698;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Internal field separator use in HTTP URI attempt", 
                     sizeof("SERVER-WEBAPP Internal field separator use in HTTP URI attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44699
// Rule Name: SERVER-WEBAPP Internal field separator use in HTTP URI attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 44699
static inline int check_rule_44699(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${IFS}
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44699
static inline void alert_rule_44699(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44699;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Internal field separator use in HTTP URI attempt", 
                     sizeof("SERVER-WEBAPP Internal field separator use in HTTP URI attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44762
// Rule Name: MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44762
static inline int check_rule_44762(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent|3A|', 'Mozilla/5.0 (Windows NT 6.1|3B| Win64|3B| x64)|3B| ",distance 0,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44762
static inline void alert_rule_44762(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44762;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44763
// Rule Name: MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 44763
static inline int check_rule_44763(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %D0%8BTl%DC",depth 11
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44763
static inline void alert_rule_44763(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44763;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.KopiLuwak variant outbound request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44790
// Rule Name: SERVER-WEBAPP MikroTik RouterOS cross site request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 44790
static inline int check_rule_44790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cfg",fast_pattern,nocase', 'process=password",nocase', 'password1=",nocase', 'password2=",nocase', 'button=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44790
static inline void alert_rule_44790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MikroTik RouterOS cross site request forgery attempt", 
                     sizeof("SERVER-WEBAPP MikroTik RouterOS cross site request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 44971
// Rule Name: SERVER-OTHER QNAP transcode server command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 44971
static inline int check_rule_44971(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 9251
    if (tcph->dest != htons(9251))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|01 00 00 00|",depth 4', '|7C|",distance 0', '|09|",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 44971
static inline void alert_rule_44971(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 44971;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP transcode server command injection attempt", 
                     sizeof("SERVER-OTHER QNAP transcode server command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45001
// Rule Name: SERVER-WEBAPP Netgear WNR2000 information leak attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 45001
static inline int check_rule_45001(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /BRS_netgear_success.html",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45001
static inline void alert_rule_45001(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45001;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 information leak attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 information leak attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45062
// Rule Name: MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45062
static inline int check_rule_45062(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ews/exchange/",fast_pattern,nocase', 'cadata=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45062
static inline void alert_rule_45062(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45062;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45063
// Rule Name: MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45063
static inline int check_rule_45063(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ews/exchange/",fast_pattern,nocase', 'cadata=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45063
static inline void alert_rule_45063(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45063;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45064
// Rule Name: MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45064
static inline int check_rule_45064(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/W3SVC",fast_pattern,nocase', 'cadata=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45064
static inline void alert_rule_45064(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45064;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45065
// Rule Name: MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45065
static inline int check_rule_45065(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/W3SVC",fast_pattern,nocase', 'cadata=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45065
static inline void alert_rule_45065(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45065;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Neuron variant inbound service request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45090
// Rule Name: MALWARE-CNC Win.Backdoor.StoneDrill server selection outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45090
static inline int check_rule_45090(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: public/Check_Exist.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45090
static inline void alert_rule_45090(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45090;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.StoneDrill server selection outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.StoneDrill server selection outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45091
// Rule Name: MALWARE-CNC Win.Backdoor.StoneDrill login outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45091
static inline int check_rule_45091(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['username=MD5Sum",fast_pattern,nocase', 'password=MD5Sum', 'button=Login']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45091
static inline void alert_rule_45091(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45091;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.StoneDrill login outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.StoneDrill login outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45092
// Rule Name: MALWARE-CNC Win.Backdoor.StoneDrill get commands outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45092
static inline int check_rule_45092(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/insert/index?",fast_pattern,nocase', 'id=', 'hst=', 'ttype=', 'state=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45092
static inline void alert_rule_45092(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45092;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.StoneDrill get commands outbound connection", 
                     sizeof("MALWARE-CNC Win.Backdoor.StoneDrill get commands outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45397
// Rule Name: PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45397
static inline int check_rule_45397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/report/?application=",fast_pattern,nocase', 'guid=', 'details=', 'action=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45397
static inline void alert_rule_45397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected", 
                     sizeof("PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45398
// Rule Name: PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45398
static inline int check_rule_45398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/click?h=",fast_pattern,nocase', 'subid=', 'data_fb=', 'data_rtt=', 'data_proto=', 'data_ic=', 'data_ss=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45398
static inline void alert_rule_45398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected", 
                     sizeof("PUA-ADWARE Osx.Adware.SurfBuyer adware outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45400
// Rule Name: MALWARE-CNC Osx.Trojan.OceanLotus outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45400
static inline int check_rule_45400(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/sigstore.db?",fast_pattern,nocase', 'k=', '?q=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45400
static inline void alert_rule_45400(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45400;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.OceanLotus outbound connection attempt", 
                     sizeof("MALWARE-CNC Osx.Trojan.OceanLotus outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45411
// Rule Name: POLICY-OTHER TrendMicro ServerProtect server configuration file download detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 45411
static inline int check_rule_45411(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /activeupdate/ini_xml.zip",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45411
static inline void alert_rule_45411(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45411;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER TrendMicro ServerProtect server configuration file download detected", 
                     sizeof("POLICY-OTHER TrendMicro ServerProtect server configuration file download detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45412
// Rule Name: SERVER-WEBAPP Asus RT-AC88U deleteOfflineClients memory corruption attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 45412
static inline int check_rule_45412(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/deleteOfflineClient.cgi",fast_pattern,nocase', 'delete_offline_client=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45412
static inline void alert_rule_45412(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45412;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Asus RT-AC88U deleteOfflineClients memory corruption attempt", 
                     sizeof("SERVER-WEBAPP Asus RT-AC88U deleteOfflineClients memory corruption attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45545
// Rule Name: MALWARE-CNC Osx.Trojan.SHLayer variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45545
static inline int check_rule_45545(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/screens/",nocase', '/",within 1,distance 8', '==",within 2,distance 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45545
static inline void alert_rule_45545(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45545;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.SHLayer variant outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.SHLayer variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45555
// Rule Name: SERVER-WEBAPP MikroTik RouterOS jsproxy readPostData memory corruption attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 45555
static inline int check_rule_45555(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/jsproxy",depth 8,fast_pattern,nocase', '|0D 0A|Content-Length: ",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45555
static inline void alert_rule_45555(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45555;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP MikroTik RouterOS jsproxy readPostData memory corruption attempt", 
                     sizeof("SERVER-WEBAPP MikroTik RouterOS jsproxy readPostData memory corruption attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45563
// Rule Name: MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45563
static inline int check_rule_45563(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: Mozilla/6.1 (compatible|3B| MSIE 9.0|3B| Windows NT 5.3|3B| Trident/5.0)|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45563
static inline void alert_rule_45563(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45563;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45564
// Rule Name: MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45564
static inline int check_rule_45564(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/5.0|0D 0A|Host: ",fast_pattern,nocase', 'Accept: */*|0D 0A|Content-Type: application/x-www-form-urlencoded|0D 0A 0D 0A|', '!"Cookie:', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45564
static inline void alert_rule_45564(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45564;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Unix.Trojan.Vpnfilter variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45607
// Rule Name: MALWARE-CNC Win.Trojan.Rokrat variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45607
static inline int check_rule_45607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?id=', 'fp_vs=",fast_pattern,nocase', 'os_vs=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45607
static inline void alert_rule_45607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Rokrat variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Rokrat variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45642
// Rule Name: MALWARE-CNC Vbs.Trojan.Agent outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45642
static inline int check_rule_45642(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1025-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1025 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Content-Length: 0",fast_pattern,nocase', 'User-Agent', '|2D 7C 2D|",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45642
static inline void alert_rule_45642(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45642;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Trojan.Agent outbound connection", 
                     sizeof("MALWARE-CNC Vbs.Trojan.Agent outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45643
// Rule Name: MALWARE-CNC Vbs.Trojan.Agent inbound payload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45643
static inline int check_rule_45643(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 1025-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1025 || src_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['s0|2D 7C 2D|",fast_pattern,nocase', 'Content-Length', 's0|2D 7C 2D|",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45643
static inline void alert_rule_45643(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45643;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Trojan.Agent inbound payload download", 
                     sizeof("MALWARE-CNC Vbs.Trojan.Agent inbound payload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45644
// Rule Name: MALWARE-CNC Vbs.Trojan.Agent inbound payload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45644
static inline int check_rule_45644(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 1025-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1025 || src_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['s1|2D 7C 2D|",fast_pattern,nocase', 'Content-Length', 's1|2D 7C 2D|",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45644
static inline void alert_rule_45644(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45644;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Trojan.Agent inbound payload download", 
                     sizeof("MALWARE-CNC Vbs.Trojan.Agent inbound payload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45645
// Rule Name: MALWARE-CNC Vbs.Trojan.Agent inbound payload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45645
static inline int check_rule_45645(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port range: 1025-65535
    __u16 src_port = ntohs(tcph->source);
    if (src_port < 1025 || src_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['s2|2D 7C 2D|",fast_pattern,nocase', 'Content-Length', 's3|2D 7C 2D|",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45645
static inline void alert_rule_45645(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45645;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Trojan.Agent inbound payload download", 
                     sizeof("MALWARE-CNC Vbs.Trojan.Agent inbound payload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45646
// Rule Name: MALWARE-CNC Vbs.Trojan.Agent outbound system information disclosure
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45646
static inline int check_rule_45646(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port range: 1025-65535
    __u16 dst_port = ntohs(tcph->dest);
    if (dst_port < 1025 || dst_port > 65535)
        return 0;
    // Content matching (simplified)
    // Full pattern: ['POST /is-return ",depth 16,fast_pattern', 'User-Agent', '|2D 7C 2D|",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45646
static inline void alert_rule_45646(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45646;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Trojan.Agent outbound system information disclosure", 
                     sizeof("MALWARE-CNC Vbs.Trojan.Agent outbound system information disclosure"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45960
// Rule Name: MALWARE-CNC Win.Trojan.Silverstar outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45960
static inline int check_rule_45960(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['response=fallback",fast_pattern,nocase', '/api.php?",depth 9', 'gpu=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45960
static inline void alert_rule_45960(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45960;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Silverstar outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Silverstar outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45961
// Rule Name: MALWARE-CNC Win.Trojan.Revenge RAT initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45961
static inline int check_rule_45961(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Information",depth 11', 'false|2A 2D 5D|NK|5B 2D 2A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45961
static inline void alert_rule_45961(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45961;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Revenge RAT initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Revenge RAT initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45962
// Rule Name: MALWARE-CNC Win.Trojan.Revenge RAT inbound heartbeat check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45962
static inline int check_rule_45962(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: PNC|2A 2D 5D|NK|5B 2D 2A|",depth 11
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45962
static inline void alert_rule_45962(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45962;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Revenge RAT inbound heartbeat check", 
                     sizeof("MALWARE-CNC Win.Trojan.Revenge RAT inbound heartbeat check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45963
// Rule Name: MALWARE-CNC Win.Trojan.UDPOS outbound command and control IP address check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45963
static inline int check_rule_45963(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /index.php?udpool=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45963
static inline void alert_rule_45963(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45963;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.UDPOS outbound command and control IP address check", 
                     sizeof("MALWARE-CNC Win.Trojan.UDPOS outbound command and control IP address check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45977
// Rule Name: OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 45977
static inline int check_rule_45977(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A0|",depth 5,offset 4', '|FF FF FF FF|",within 4,distance 123']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45977
static inline void alert_rule_45977(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45977;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45978
// Rule Name: OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 45978
static inline int check_rule_45978(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Check src port: 445
    if (tcph->source != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A0|",depth 5,offset 4', '|FA FF FF|",within 3,distance 108', '|FA FF FF|",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45978
static inline void alert_rule_45978(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45978;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows SMB kernel heap memory leak attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45979
// Rule Name: MALWARE-CNC MultiOS.Trojan.OSCelestial variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45979
static inline int check_rule_45979(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|72 00 17|com.net.LoginDataPacket",fast_pattern,nocase', '|74 00 13|Lcom/net/LoginData",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45979
static inline void alert_rule_45979(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45979;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Trojan.OSCelestial variant outbound connection", 
                     sizeof("MALWARE-CNC MultiOS.Trojan.OSCelestial variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45980
// Rule Name: MALWARE-CNC MultiOS.Trojan.OSCelestial variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 45980
static inline int check_rule_45980(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |74 00 29|net.oscp.client.keylogger.KeystrokeLogger",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45980
static inline void alert_rule_45980(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45980;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Trojan.OSCelestial variant inbound connection", 
                     sizeof("MALWARE-CNC MultiOS.Trojan.OSCelestial variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 45983
// Rule Name: POLICY-OTHER Sandvine PacketLogic http redirection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 45983
static inline int check_rule_45983(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Check TCP flags
    if (!((tcph->ack == 1) && (tcph->fin == 1)))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Temporary Redirect",fast_pattern,nocase', '307",depth 3', 'Temporary Redirect",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 45983
static inline void alert_rule_45983(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 45983;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Sandvine PacketLogic http redirection attempt", 
                     sizeof("POLICY-OTHER Sandvine PacketLogic http redirection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46048
// Rule Name: MALWARE-CNC Win.Trojan.Gen variant outbound communication
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46048
static inline int check_rule_46048(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /A56WY",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46048
static inline void alert_rule_46048(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46048;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Gen variant outbound communication", 
                     sizeof("MALWARE-CNC Win.Trojan.Gen variant outbound communication"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46050
// Rule Name: MALWARE-CNC Win.Trojan.CrossRAT outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46050
static inline int check_rule_46050(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['[^8]&&&",fast_pattern,nocase', '[^8]&&&']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46050
static inline void alert_rule_46050(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46050;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CrossRAT outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.CrossRAT outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46051
// Rule Name: MALWARE-CNC Win.Trojan.Bandook/Anbacas outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46051
static inline int check_rule_46051(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['QDAwMD",depth 6,fast_pattern', '&&&",within 200']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46051
static inline void alert_rule_46051(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46051;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Bandook/Anbacas outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Bandook/Anbacas outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46052
// Rule Name: MALWARE-CNC User-Agent known malicious user-agent string Uploador - Win.Trojan.CrossRAT
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46052
static inline int check_rule_46052(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Uploador|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46052
static inline void alert_rule_46052(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46052;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user-agent string Uploador - Win.Trojan.CrossRAT", 
                     sizeof("MALWARE-CNC User-Agent known malicious user-agent string Uploador - Win.Trojan.CrossRAT"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46066
// Rule Name: MALWARE-CNC Win.Trojan.yty second stage downloader initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46066
static inline int check_rule_46066(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/football/goal",fast_pattern,nocase', 'ball=', 'score=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46066
static inline void alert_rule_46066(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46066;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.yty second stage downloader initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.yty second stage downloader initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46067
// Rule Name: MALWARE-CNC Win.Trojan.yty plugin downloader initial outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46067
static inline int check_rule_46067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Expect: 100-continue",fast_pattern,nocase', 'pc=', 'pc_data=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46067
static inline void alert_rule_46067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.yty plugin downloader initial outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.yty plugin downloader initial outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46068
// Rule Name: MALWARE-CNC Win.Trojan.yty module download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46068
static inline int check_rule_46068(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/football/download/",depth 19', '!"User-Agent|3A|",nocase', '!"Accept|3A|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46068
static inline void alert_rule_46068(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46068;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.yty module download request", 
                     sizeof("MALWARE-CNC Win.Trojan.yty module download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46069
// Rule Name: MALWARE-CNC Win.Trojan.yty module request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46069
static inline int check_rule_46069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Expect: 100-continue",fast_pattern,nocase', 'cnumber=', 'orname=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46069
static inline void alert_rule_46069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.yty module request", 
                     sizeof("MALWARE-CNC Win.Trojan.yty module request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46070
// Rule Name: MALWARE-CNC Win.Trojan.yty file exfiltration outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46070
static inline int check_rule_46070(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Expect: 100-continue",fast_pattern,nocase', 'id=",depth 3', '&pc=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46070
static inline void alert_rule_46070(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46070;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.yty file exfiltration outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.yty file exfiltration outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46076
// Rule Name: NETBIOS MikroTik RouterOS buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46076
static inline int check_rule_46076(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |81 00|",depth 2
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46076
static inline void alert_rule_46076(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46076;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "NETBIOS MikroTik RouterOS buffer overflow attempt", 
                     sizeof("NETBIOS MikroTik RouterOS buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46080
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46080
static inline int check_rule_46080(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi', 'action=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46080
static inline void alert_rule_46080(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46080;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46081
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46081
static inline int check_rule_46081(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'action=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46081
static inline void alert_rule_46081(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46081;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi cross site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46082
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46082
static inline int check_rule_46082(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'ping",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46082
static inline void alert_rule_46082(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46082;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46083
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46083
static inline int check_rule_46083(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'next_page=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46083
static inline void alert_rule_46083(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46083;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46084
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46084
static inline int check_rule_46084(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'next_page=",nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46084
static inline void alert_rule_46084(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46084;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46085
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46085
static inline int check_rule_46085(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'ping_",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46085
static inline void alert_rule_46085(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46085;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46086
// Rule Name: SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46086
static inline int check_rule_46086(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['apply.cgi",fast_pattern,nocase', 'ping_",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46086
static inline void alert_rule_46086(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46086;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt", 
                     sizeof("SERVER-WEBAPP Linksys E-Series apply.cgi ping function command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46121
// Rule Name: PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46121
static inline int check_rule_46121(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 32764
    if (tcph->source != htons(32764))
        return 0;
    // Content matching (simplified)
    // Full pattern: MMcS",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46121
static inline void alert_rule_46121(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46121;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected", 
                     sizeof("PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46122
// Rule Name: PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46122
static inline int check_rule_46122(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 32764
    if (tcph->source != htons(32764))
        return 0;
    // Content matching (simplified)
    // Full pattern: ScMM",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46122
static inline void alert_rule_46122(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46122;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected", 
                     sizeof("PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46123
// Rule Name: PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46123
static inline int check_rule_46123(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 32764
    if (tcph->dest != htons(32764))
        return 0;
    // Content matching (simplified)
    // Full pattern: MMcS",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46123
static inline void alert_rule_46123(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46123;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected", 
                     sizeof("PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46124
// Rule Name: PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46124
static inline int check_rule_46124(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 32764
    if (tcph->dest != htons(32764))
        return 0;
    // Content matching (simplified)
    // Full pattern: ScMM",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46124
static inline void alert_rule_46124(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46124;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected", 
                     sizeof("PROTOCOL-OTHER use of undocumented ScMM test interface in Cisco small business devices detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46129
// Rule Name: MALWARE-CNC Win.Trojan.HW32 variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46129
static inline int check_rule_46129(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Cpa=+EXEC+",depth 10', '%27%2C%27']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46129
static inline void alert_rule_46129(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46129;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.HW32 variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.HW32 variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46136
// Rule Name: MALWARE-CNC Win.Trojan.Banbra variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46136
static inline int check_rule_46136(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/3.0 (compatible|3B| Indy Library)', 'remetente=",depth 10,fast_pattern', '&destinatario",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46136
static inline void alert_rule_46136(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46136;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banbra variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Banbra variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46137
// Rule Name: MALWARE-CNC Win.Trojan.Cidox variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46137
static inline int check_rule_46137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['POST /b/req/",depth 12', ' HTTP/1.1|0D 0A|Accept: */*|0D 0A|Content-Type: application/octet-stream|0D 0A|Connection: Close|0D 0A|User-Agent: Mozilla/",within 103,distance 24', ')|0D 0A|Host: ",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46137
static inline void alert_rule_46137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Cidox variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Cidox variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46287
// Rule Name: SERVER-WEBAPP Linksys E series denial of service attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: denial-of-service
// Generated from Snort rule parser output

// Check function for rule 46287
static inline int check_rule_46287(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: mfgtst.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46287
static inline void alert_rule_46287(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46287;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Linksys E series denial of service attempt", 
                     sizeof("SERVER-WEBAPP Linksys E series denial of service attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46297
// Rule Name: SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46297
static inline int check_rule_46297(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/pingping.cgi",fast_pattern,nocase', 'ping_ip=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46297
static inline void alert_rule_46297(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46297;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46298
// Rule Name: SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46298
static inline int check_rule_46298(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/pingping.cgi",fast_pattern,nocase', 'ping_ip=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46298
static inline void alert_rule_46298(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46298;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46299
// Rule Name: SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46299
static inline int check_rule_46299(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/pingping.cgi",fast_pattern,nocase', 'ping",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46299
static inline void alert_rule_46299(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46299;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46300
// Rule Name: SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46300
static inline int check_rule_46300(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/pingping.cgi",fast_pattern,nocase', 'ping_ip",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46300
static inline void alert_rule_46300(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46300;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP VioStor NVR and QNAP NAS command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46301
// Rule Name: SERVER-OTHER QNAP QTS X-Forwarded-For buffer overflow
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46301
static inline int check_rule_46301(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/filemanager/wfm2Login.cgi",fast_pattern,nocase', 'X-Forwarded-For",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46301
static inline void alert_rule_46301(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46301;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP QTS X-Forwarded-For buffer overflow", 
                     sizeof("SERVER-OTHER QNAP QTS X-Forwarded-For buffer overflow"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46305
// Rule Name: SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46305
static inline int check_rule_46305(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/wizReq.cgi",fast_pattern,nocase', 'SMB_",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46305
static inline void alert_rule_46305(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46305;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46306
// Rule Name: SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46306
static inline int check_rule_46306(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/wizReq.cgi",fast_pattern,nocase', 'SMB_",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46306
static inline void alert_rule_46306(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46306;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46307
// Rule Name: SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46307
static inline int check_rule_46307(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/wizReq.cgi",fast_pattern,nocase', 'SMB",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46307
static inline void alert_rule_46307(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46307;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46308
// Rule Name: SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46308
static inline int check_rule_46308(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/wizReq.cgi",fast_pattern,nocase', 'SMB_",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46308
static inline void alert_rule_46308(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46308;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt", 
                     sizeof("SERVER-WEBAPP QNAP WTS 4.2.1 command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46309
// Rule Name: SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 46309
static inline int check_rule_46309(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgi.cgi",fast_pattern,nocase', 'u=",nocase', 'p=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46309
static inline void alert_rule_46309(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46309;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt", 
                     sizeof("SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46310
// Rule Name: SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 46310
static inline int check_rule_46310(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/cgi.cgi",fast_pattern,nocase', 'u=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46310
static inline void alert_rule_46310(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46310;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt", 
                     sizeof("SERVER-OTHER QNAP NVR/NAS Heap/Stack Overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46312
// Rule Name: SERVER-WEBAPP Netgear WNR2000 information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 46312
static inline int check_rule_46312(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/NETGEAR_WNR2000.cfg",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46312
static inline void alert_rule_46312(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46312;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 information disclosure attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46313
// Rule Name: SERVER-WEBAPP Netgear WNR2000 information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 46313
static inline int check_rule_46313(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cgi-bin/upg_restore.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46313
static inline void alert_rule_46313(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46313;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 information disclosure attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46314
// Rule Name: SERVER-WEBAPP Netgear WNR2000 information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 46314
static inline int check_rule_46314(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /router-info.htm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46314
static inline void alert_rule_46314(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46314;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear WNR2000 information disclosure attempt", 
                     sizeof("SERVER-WEBAPP Netgear WNR2000 information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46315
// Rule Name: SERVER-WEBAPP Joomla restore.php PHP object injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46315
static inline int check_rule_46315(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/administrator/components/com_joomlaupdate/restore.php",fast_pattern,nocase', 'factory=",nocase', 'OjI2OiJraWNrc3RhcnQuc2V0dXAuc291cmNlZmlsZSI7', 'aHR0cDovL']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46315
static inline void alert_rule_46315(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46315;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Joomla restore.php PHP object injection attempt", 
                     sizeof("SERVER-WEBAPP Joomla restore.php PHP object injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46316
// Rule Name: SERVER-WEBAPP Drupal 8 remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 46316
static inline int check_rule_46316(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['element_parents=",fast_pattern,nocase', '#value']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46316
static inline void alert_rule_46316(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46316;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Drupal 8 remote code execution attempt", 
                     sizeof("SERVER-WEBAPP Drupal 8 remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46317
// Rule Name: SERVER-OTHER NETGEAR TelnetEnable attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 46317
static inline int check_rule_46317(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 23
    if (tcph->dest != htons(23))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|C0 F3 AC 2A 40 79 49 0C A3 6E 89 64 73 66 0F 0B|', '|5D FC 67 3A 16 DC 00 56 A3 6E 89 64 73 66 0F 0B|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46317
static inline void alert_rule_46317(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46317;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER NETGEAR TelnetEnable attempt", 
                     sizeof("SERVER-OTHER NETGEAR TelnetEnable attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46322
// Rule Name: SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46322
static inline int check_rule_46322(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/wlg_sec_profile_main.cgi",fast_pattern,nocase', 'ssid=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46322
static inline void alert_rule_46322(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46322;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46323
// Rule Name: SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46323
static inline int check_rule_46323(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/fw_serv_add.cgi",fast_pattern,nocase', 'userdefined=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46323
static inline void alert_rule_46323(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46323;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt", 
                     sizeof("SERVER-WEBAPP Netgear DGN2200B stored cross-site scripting attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46335
// Rule Name: SERVER-OTHER QNAP QTS hard coded credential access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: default-login-attempt
// Generated from Snort rule parser output

// Check function for rule 46335
static inline int check_rule_46335(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: PASS joxu06wj/|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46335
static inline void alert_rule_46335(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46335;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP QTS hard coded credential access attempt", 
                     sizeof("SERVER-OTHER QNAP QTS hard coded credential access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46339
// Rule Name: MALWARE-CNC Win.Ransomware.Matrix outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46339
static inline int check_rule_46339(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['add.php?apikey=', '&compuser=', '&sid=', '&phase=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46339
static inline void alert_rule_46339(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46339;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Matrix outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.Matrix outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46340
// Rule Name: SERVER-WEBAPP Akeeba Kickstart restoration.php reconnaissance attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46340
static inline int check_rule_46340(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: administrator/components/com_joomlaupdate/restoration.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46340
static inline void alert_rule_46340(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46340;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Akeeba Kickstart restoration.php reconnaissance attempt", 
                     sizeof("SERVER-WEBAPP Akeeba Kickstart restoration.php reconnaissance attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46341
// Rule Name: SERVER-WEBAPP Akeeba Kickstart cross site request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46341
static inline int check_rule_46341(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['administrator/index.php",fast_pattern,nocase', 'option=com_joomlaupdate",nocase', 'task=update.install",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46341
static inline void alert_rule_46341(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46341;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Akeeba Kickstart cross site request forgery attempt", 
                     sizeof("SERVER-WEBAPP Akeeba Kickstart cross site request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46342
// Rule Name: SERVER-OTHER QNAP QTS cross site request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 46342
static inline int check_rule_46342(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['cgi-bin/create_user.cgi",fast_pattern,nocase', 'function=",nocase', 'subfun=",nocase', 'NAME=",nocase', 'PASSWD=",nocase', 'VERIFY=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46342
static inline void alert_rule_46342(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46342;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER QNAP QTS cross site request forgery attempt", 
                     sizeof("SERVER-OTHER QNAP QTS cross site request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46376
// Rule Name: SERVER-OTHER libgd heap-overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46376
static inline int check_rule_46376(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gd2|00 00 02|",fast_pattern', '|02|",within 1,distance 7']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46376
static inline void alert_rule_46376(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46376;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER libgd heap-overflow attempt", 
                     sizeof("SERVER-OTHER libgd heap-overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46377
// Rule Name: SERVER-OTHER libgd heap-overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46377
static inline int check_rule_46377(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gd2|00 00 02|",fast_pattern', '|02|",within 1,distance 7']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46377
static inline void alert_rule_46377(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46377;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER libgd heap-overflow attempt", 
                     sizeof("SERVER-OTHER libgd heap-overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46378
// Rule Name: MALWARE-CNC Win.Trojan.Dropper variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46378
static inline int check_rule_46378(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['IHkoeWRrcnkpIikqNy95ZCB5LSl5ZCB5",depth 40,fast_pattern', '!"Referer|3A|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46378
static inline void alert_rule_46378(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46378;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46416
// Rule Name: MALWARE-CNC Win.Spyware.Autoit outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46416
static inline int check_rule_46416(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: win32=FFD8FFE000104A464946",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46416
static inline void alert_rule_46416(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46416;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Spyware.Autoit outbound connection", 
                     sizeof("MALWARE-CNC Win.Spyware.Autoit outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46421
// Rule Name: MALWARE-CNC Win.Trojan.Kraens delivery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46421
static inline int check_rule_46421(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['{|22|i|22|:|22|%s|22|,|22|l|22|:[",fast_pattern,nocase', 'RES_OK']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46421
static inline void alert_rule_46421(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46421;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kraens delivery attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Kraens delivery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46422
// Rule Name: MALWARE-CNC Win.Trojan.Kraens delivery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46422
static inline int check_rule_46422(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['{|22|i|22|:|22|%s|22|,|22|l|22|:[",fast_pattern,nocase', 'RES_OK']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46422
static inline void alert_rule_46422(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46422;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kraens delivery attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Kraens delivery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46423
// Rule Name: MALWARE-CNC Win.Trojan.Kraens initial outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46423
static inline int check_rule_46423(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/up_d.php",fast_pattern,nocase', '{|22|i|22|:",depth 5']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46423
static inline void alert_rule_46423(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46423;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Kraens initial outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Kraens initial outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46433
// Rule Name: MALWARE-CNC Win.Adware.Doyo initial connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46433
static inline int check_rule_46433(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: data=85702b2fccafcb2f",depth 21
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46433
static inline void alert_rule_46433(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46433;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Adware.Doyo initial connection", 
                     sizeof("MALWARE-CNC Win.Adware.Doyo initial connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46434
// Rule Name: MALWARE-CNC Win.Adware.Doyo client outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46434
static inline int check_rule_46434(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |01 00 00 00 01 01 00 00 01 00 00 00 00 00 04 00 03 00 00 00 00 00 00 00 00 00 00 00|",depth 28
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46434
static inline void alert_rule_46434(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46434;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Adware.Doyo client outbound connection", 
                     sizeof("MALWARE-CNC Win.Adware.Doyo client outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46435
// Rule Name: MALWARE-CNC Vbs.Downloader.Kryptik known malicious user-agent string 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46435
static inline int check_rule_46435(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| USR-KL",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46435
static inline void alert_rule_46435(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46435;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Downloader.Kryptik known malicious user-agent string ", 
                     sizeof("MALWARE-CNC Vbs.Downloader.Kryptik known malicious user-agent string "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46436
// Rule Name: MALWARE-CNC Vbs.Downloader.Agent inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46436
static inline int check_rule_46436(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['3832D640-CF90-11CF-8E43-00A0C911005A",fast_pattern,nocase', 'Workbook_Open",nocase', 'Document_Open",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46436
static inline void alert_rule_46436(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46436;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Downloader.Agent inbound connection", 
                     sizeof("MALWARE-CNC Vbs.Downloader.Agent inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46437
// Rule Name: MALWARE-CNC Vbs.Downloader.Agent inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46437
static inline int check_rule_46437(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['3832D640-CF90-11CF-8E43-00A0C911005A",fast_pattern,nocase', 'Workbook_Open",nocase', 'Document_Open",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46437
static inline void alert_rule_46437(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46437;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Downloader.Agent inbound connection", 
                     sizeof("MALWARE-CNC Vbs.Downloader.Agent inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46438
// Rule Name: MALWARE-CNC Vbs.Downloader.Agent inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46438
static inline int check_rule_46438(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Shell",nocase', 'vbHide",within 100,fast_pattern', 'Chr",nocase', 'Asc",within 100,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46438
static inline void alert_rule_46438(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46438;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Downloader.Agent inbound connection", 
                     sizeof("MALWARE-CNC Vbs.Downloader.Agent inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46439
// Rule Name: MALWARE-CNC Vbs.Downloader.Agent inbound delivery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46439
static inline int check_rule_46439(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['Shell",nocase', 'vbHide",within 100,fast_pattern', 'Chr",nocase', 'Asc",within 100,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46439
static inline void alert_rule_46439(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46439;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Vbs.Downloader.Agent inbound delivery attempt", 
                     sizeof("MALWARE-CNC Vbs.Downloader.Agent inbound delivery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46482
// Rule Name: MALWARE-CNC Installation Keylogger Osx.Trojan.Mokes data exfiltration
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46482
static inline int check_rule_46482(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET /v1 HTTP/1.1",depth 16,fast_pattern', 'Connection: ', 'User-Agent: ', 'Accept-Encoding: ', 'Accept-Language: ', 'Host: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46482
static inline void alert_rule_46482(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46482;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Installation Keylogger Osx.Trojan.Mokes data exfiltration", 
                     sizeof("MALWARE-CNC Installation Keylogger Osx.Trojan.Mokes data exfiltration"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46485
// Rule Name: SERVER-WEBAPP TwonkyMedia server directory listing attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46485
static inline int check_rule_46485(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/rpc/dir",fast_pattern,nocase', 'path=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46485
static inline void alert_rule_46485(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46485;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP TwonkyMedia server directory listing attempt", 
                     sizeof("SERVER-WEBAPP TwonkyMedia server directory listing attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46487
// Rule Name: MALWARE-CNC Win.Trojan.Ammy heartbeat
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46487
static inline int check_rule_46487(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['id=",depth 3,offset 5', '&os=",within 4,distance 8', '&priv=",distance 0', '&cred=",distance 0', '&pcname=",distance 0', '&build_time=",distance 0,fast_pattern', '&card=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46487
static inline void alert_rule_46487(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46487;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ammy heartbeat", 
                     sizeof("MALWARE-CNC Win.Trojan.Ammy heartbeat"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46488
// Rule Name: MALWARE-CNC Win.Trojan.Ammy download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46488
static inline int check_rule_46488(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/q2/index.php?",fast_pattern,nocase', 'id=', '&c=', '&mk=', '&il=', '&vr=', '&bt=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46488
static inline void alert_rule_46488(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46488;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Ammy download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Ammy download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46501
// Rule Name: MALWARE-CNC Win.Trojan.Agent outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46501
static inline int check_rule_46501(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?&1001=",fast_pattern,nocase', '99=', 'f1=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46501
static inline void alert_rule_46501(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46501;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46502
// Rule Name: MALWARE-CNC Win.Trojan.Agent outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46502
static inline int check_rule_46502(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?&1001=",fast_pattern,nocase', '1=', '2=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46502
static inline void alert_rule_46502(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46502;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent outbound request", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46510
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46510
static inline int check_rule_46510(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/twonky_cmd.cgi",fast_pattern,nocase', 'path=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46510
static inline void alert_rule_46510(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46510;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46511
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46511
static inline int check_rule_46511(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/twonky_cmd.cgi",fast_pattern,nocase', 'path=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46511
static inline void alert_rule_46511(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46511;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46512
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46512
static inline int check_rule_46512(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/twonky_cmd.cgi",fast_pattern,nocase', 'path=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46512
static inline void alert_rule_46512(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46512;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46513
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46513
static inline int check_rule_46513(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/twonky_cmd.cgi",fast_pattern,nocase', 'path",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46513
static inline void alert_rule_46513(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46513;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46514
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46514
static inline int check_rule_46514(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/proxy.cgi",fast_pattern,nocase', 'url=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46514
static inline void alert_rule_46514(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46514;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46515
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46515
static inline int check_rule_46515(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/proxy.cgi",fast_pattern,nocase', 'url=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46515
static inline void alert_rule_46515(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46515;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46516
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46516
static inline int check_rule_46516(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/proxy.cgi",fast_pattern,nocase', 'url=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46516
static inline void alert_rule_46516(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46516;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46517
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46517
static inline int check_rule_46517(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/proxy.cgi",fast_pattern,nocase', 'url",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46517
static inline void alert_rule_46517(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46517;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46518
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 46518
static inline int check_rule_46518(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/set.cgi",fast_pattern,nocase', 'n=TLNET_EN",nocase', 'v=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46518
static inline void alert_rule_46518(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46518;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46519
// Rule Name: SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 46519
static inline int check_rule_46519(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/set.cgi",fast_pattern,nocase', 'n=TLNET_EN",nocase', 'v=1",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46519
static inline void alert_rule_46519(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46519;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt", 
                     sizeof("SERVER-WEBAPP Belkin N750 F9K1103 wireless router remote telnet enable attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46611
// Rule Name: MALWARE-CNC Win.Trojan.Banload second stage download request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46611
static inline int check_rule_46611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['!"Referer|3A|', '!"Accept', '!"User-Agent|3A 20|http', '.zip HTTP/1.1|0D 0A|Host|3A 20|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46611
static inline void alert_rule_46611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Banload second stage download request", 
                     sizeof("MALWARE-CNC Win.Trojan.Banload second stage download request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46612
// Rule Name: MALWARE-CNC Win.Trojan.Unruy outbound callout
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46612
static inline int check_rule_46612(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?q=",fast_pattern,nocase', 'Accept-Language: en-us', 'Accept-Encoding: gzip, deflate', 'Connection: Keep-Alive', 'Referer: http://www.google.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46612
static inline void alert_rule_46612(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46612;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Unruy outbound callout", 
                     sizeof("MALWARE-CNC Win.Trojan.Unruy outbound callout"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46665
// Rule Name: SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46665
static inline int check_rule_46665(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/DigitalGuardian/Management/ServerSettingsPDFTemplates.aspx",fast_pattern,nocase', 'inputFilePath",nocase', '.asp",distance 0,nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46665
static inline void alert_rule_46665(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46665;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt", 
                     sizeof("SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46666
// Rule Name: SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46666
static inline int check_rule_46666(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/DigitalGuardian/Policies/PromptSkin.aspx",fast_pattern,nocase', 'skinFile",nocase', '.asp",distance 0,nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46666
static inline void alert_rule_46666(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46666;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt", 
                     sizeof("SERVER-WEBAPP Digital Guardian Management Console arbitrary file upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46742
// Rule Name: MALWARE-CNC Win.Trojan.Dropper malicious script download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46742
static inline int check_rule_46742(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<script",nocase', 'ActiveXObject",nocase', 'WScript.Shell",fast_pattern,nocase', 'p o w e r s h e l l",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46742
static inline void alert_rule_46742(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46742;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper malicious script download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper malicious script download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46743
// Rule Name: MALWARE-CNC Win.Trojan.Dropper initial outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46743
static inline int check_rule_46743(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?utma",fast_pattern,nocase', '!"Referer:",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46743
static inline void alert_rule_46743(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46743;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper initial outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper initial outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46744
// Rule Name: MALWARE-CNC Win.Trojan.Dropper malicious executable download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46744
static inline int check_rule_46744(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type:",nocase', 'application/java-vm",within 50,fast_pattern', 'MZ",depth 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46744
static inline void alert_rule_46744(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46744;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper malicious executable download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper malicious executable download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46747
// Rule Name: MALWARE-CNC Win.Trojan.Qarallax outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46747
static inline int check_rule_46747(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 07|nemesis",depth 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46747
static inline void alert_rule_46747(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46747;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Qarallax outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Qarallax outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46748
// Rule Name: MALWARE-CNC Win.Trojan.Qarallax outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46748
static inline int check_rule_46748(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|00 05|child|01 00 16|",depth 11', '|22|magic|22|",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46748
static inline void alert_rule_46748(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46748;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Qarallax outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Qarallax outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46773
// Rule Name: SERVER-WEBAPP Nagios XI SQL injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46773
static inline int check_rule_46773(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosql/admin/helpedit.php",fast_pattern,nocase', 'selInfoKey1=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46773
static inline void alert_rule_46773(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46773;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI SQL injection attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI SQL injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46774
// Rule Name: SERVER-WEBAPP NagiosXI SQL injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46774
static inline int check_rule_46774(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosql/admin/helpedit.php",fast_pattern,nocase', 'selInfoKey1=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46774
static inline void alert_rule_46774(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46774;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP NagiosXI SQL injection attempt", 
                     sizeof("SERVER-WEBAPP NagiosXI SQL injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46775
// Rule Name: SERVER-WEBAPP Nagios XI command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46775
static inline int check_rule_46775(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosxi/backend/index.php",fast_pattern,nocase', 'command_data=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46775
static inline void alert_rule_46775(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46775;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI command injection attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46776
// Rule Name: SERVER-WEBAPP Nagios XI command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46776
static inline int check_rule_46776(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosxi/backend/index.php",fast_pattern,nocase', 'command_data=",nocase', '%26']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46776
static inline void alert_rule_46776(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46776;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI command injection attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46777
// Rule Name: SERVER-WEBAPP Nagios XI command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46777
static inline int check_rule_46777(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosxi/backend/index.php",fast_pattern,nocase', 'command",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46777
static inline void alert_rule_46777(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46777;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI command injection attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46778
// Rule Name: SERVER-WEBAPP Nagios XI command injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46778
static inline int check_rule_46778(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosxi/backend/index.php",fast_pattern,nocase', 'command_data",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46778
static inline void alert_rule_46778(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46778;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI command injection attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI command injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46779
// Rule Name: SERVER-WEBAPP Nagios XI database settings modification attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46779
static inline int check_rule_46779(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/nagiosql/admin/settings.php",fast_pattern,nocase', 'txtDBname=nagiosql",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46779
static inline void alert_rule_46779(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46779;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Nagios XI database settings modification attempt", 
                     sizeof("SERVER-WEBAPP Nagios XI database settings modification attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46782
// Rule Name: MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46782
static inline int check_rule_46782(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |09 4D 69 63 72 6F 73 6F 66 74 31 10 30 0E 06 03 55 04 0B 13 07 53 75 70 70 6F 72 74 31 0B 30 09 06 03 55 04 03 13 02 63 61|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46782
static inline void alert_rule_46782(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46782;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt", 
                     sizeof("MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46783
// Rule Name: MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46783
static inline int check_rule_46783(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: |09 4D 69 63 72 6F 73 6F 66 74 31 10 30 0E 06 03 55 04 0B 13 07 53 75 70 70 6F 72 74 31 0B 30 09 06 03 55 04 03 13 02 63 61|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46783
static inline void alert_rule_46783(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46783;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt", 
                     sizeof("MALWARE-CNC Unix.Trojan.Vpnfilter variant SSL connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46785
// Rule Name: MALWARE-CNC Win.Downloader.Zebrocy known malicious user-agent string
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46785
static inline int check_rule_46785(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| Mozilla v5.1",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46785
static inline void alert_rule_46785(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46785;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.Zebrocy known malicious user-agent string", 
                     sizeof("MALWARE-CNC Win.Downloader.Zebrocy known malicious user-agent string"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46786
// Rule Name: MALWARE-CNC Win.Downloader.Zebrocy initial outbound request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46786
static inline int check_rule_46786(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['?fort=",fast_pattern,nocase', 'pol=",depth 4']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46786
static inline void alert_rule_46786(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46786;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.Zebrocy initial outbound request", 
                     sizeof("MALWARE-CNC Win.Downloader.Zebrocy initial outbound request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46787
// Rule Name: MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46787
static inline int check_rule_46787(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/telg/sv/sv.php",fast_pattern,nocase', 'id', 'data']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46787
static inline void alert_rule_46787(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46787;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt", 
                     sizeof("MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46788
// Rule Name: MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46788
static inline int check_rule_46788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /telg/index.php?set=show",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46788
static inline void alert_rule_46788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt", 
                     sizeof("MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46789
// Rule Name: MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46789
static inline int check_rule_46789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get/index.php', 'id=Z29nbw==",fast_pattern,nocase', 'user=', 'pass=', 'data=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46789
static inline void alert_rule_46789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt", 
                     sizeof("MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46790
// Rule Name: MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46790
static inline int check_rule_46790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/spyMobile/upload.php",fast_pattern,nocase', 'iemi=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46790
static inline void alert_rule_46790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt", 
                     sizeof("MALWARE-CNC Andr.Trojan.ZooPark outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46802
// Rule Name: SERVER-WEBAPP Anti-Web directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46802
static inline int check_rule_46802(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/write.cgi",fast_pattern,nocase', 'template=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46802
static inline void alert_rule_46802(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46802;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Anti-Web directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Anti-Web directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46803
// Rule Name: SERVER-WEBAPP Anti-Web directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46803
static inline int check_rule_46803(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/write.cgi",fast_pattern,nocase', 'template=",nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46803
static inline void alert_rule_46803(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46803;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Anti-Web directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Anti-Web directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46804
// Rule Name: SERVER-WEBAPP Anti-Web directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46804
static inline int check_rule_46804(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cgi-bin/write.cgi",fast_pattern,nocase', 'template",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46804
static inline void alert_rule_46804(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46804;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Anti-Web directory traversal attempt", 
                     sizeof("SERVER-WEBAPP Anti-Web directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46805
// Rule Name: SERVER-WEBAPP BA Systems BAS Web information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46805
static inline int check_rule_46805(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /isc/get_sid.aspx",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46805
static inline void alert_rule_46805(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46805;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BA Systems BAS Web information disclosure attempt", 
                     sizeof("SERVER-WEBAPP BA Systems BAS Web information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46806
// Rule Name: SERVER-WEBAPP BA Systems BAS Web information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46806
static inline int check_rule_46806(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /isc/get_sid_js.aspx",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46806
static inline void alert_rule_46806(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46806;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP BA Systems BAS Web information disclosure attempt", 
                     sizeof("SERVER-WEBAPP BA Systems BAS Web information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46817
// Rule Name: SERVER-WEBAPP FLIR Breakstream 2300 unauthenticated information disclosure attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 46817
static inline int check_rule_46817(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /getConfigExportFile.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46817
static inline void alert_rule_46817(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46817;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP FLIR Breakstream 2300 unauthenticated information disclosure attempt", 
                     sizeof("SERVER-WEBAPP FLIR Breakstream 2300 unauthenticated information disclosure attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46818
// Rule Name: MALWARE-CNC Win.Ransomware.Satan outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46818
static inline int check_rule_46818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/data/token.php",fast_pattern,nocase', 'status=",nocase', 'code=",nocase', 'Winnet Client",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46818
static inline void alert_rule_46818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Satan outbound connection", 
                     sizeof("MALWARE-CNC Win.Ransomware.Satan outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46819
// Rule Name: MALWARE-OTHER Win.Ransomware.Satan payload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46819
static inline int check_rule_46819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /cab/sts.exe",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46819
static inline void alert_rule_46819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Win.Ransomware.Satan payload download", 
                     sizeof("MALWARE-OTHER Win.Ransomware.Satan payload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46824
// Rule Name: SERVER-WEBAPP DotNetNuke DreamSlider arbitrary file download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 46824
static inline int check_rule_46824(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/DesktopModules/DreamSlider/DownloadProvider.aspx",fast_pattern,nocase', 'file=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46824
static inline void alert_rule_46824(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46824;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP DotNetNuke DreamSlider arbitrary file download attempt", 
                     sizeof("SERVER-WEBAPP DotNetNuke DreamSlider arbitrary file download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46827
// Rule Name: MALWARE-CNC Win.Trojan.Dunihi outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46827
static inline int check_rule_46827(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 00 A2 30 81 9F 30 0D 06 09 2A 86 48 86 F7 0D 01 01 01 05 00 03 81 8D 00 30 81 89 02 81 81 00|",depth 32
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46827
static inline void alert_rule_46827(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46827;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dunihi outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dunihi outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46839
// Rule Name: MALWARE-CNC Win.Trojan.RedLeaves variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46839
static inline int check_rule_46839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.NET CLR 3.0.30729|3B| .NET4.0C|3B| .NET4.0E)|0D 0A|Content-Length",fast_pattern,nocase', '/index.php', 'POST', 'Connection: Keep-Alive|0D 0A|Accept: */*|0D 0A|', '!"Content-Type', '!"Referer', '!"Accept-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46839
static inline void alert_rule_46839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.RedLeaves variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.RedLeaves variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46872
// Rule Name: MALWARE-CNC Win.Trojan.CowerSnail command and control response detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46872
static inline int check_rule_46872(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 20480
    if (tcph->source != htons(20480))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['pk",depth 2', 'R|00|e|00|q|00|u|00|e|00|s|00|t|00|",fast_pattern,nocase', '|00|a|00|r|00|g|00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46872
static inline void alert_rule_46872(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46872;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CowerSnail command and control response detected", 
                     sizeof("MALWARE-CNC Win.Trojan.CowerSnail command and control response detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46873
// Rule Name: MALWARE-CNC Win.Trojan.CowerSnail initial outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46873
static inline int check_rule_46873(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 20480
    if (tcph->dest != htons(20480))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['+CHANNEL|0B|",fast_pattern,nocase', 'line-client']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46873
static inline void alert_rule_46873(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46873;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.CowerSnail initial outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.CowerSnail initial outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46885
// Rule Name: MALWARE-CNC Win.Trojan.Joanap variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46885
static inline int check_rule_46885(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['TO: Joana <xiake722@gmail.com>",fast_pattern,nocase', 'SUBJECT: |5B|T|5D|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46885
static inline void alert_rule_46885(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46885;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Joanap variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Joanap variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46895
// Rule Name: MALWARE-CNC Win.Trojan.Nocturnal outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46895
static inline int check_rule_46895(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/server/gate.php",fast_pattern,nocase', 'name=|22|hwid|22|', 'name=|22|platform|22|', 'name=|22|pcount|22|', 'name=|22|cccount|22|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46895
static inline void alert_rule_46895(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46895;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Nocturnal outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Nocturnal outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46922
// Rule Name: MALWARE-CNC Win.Trojan.Fareit variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46922
static inline int check_rule_46922(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/panel/logout.php",depth 17', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46922
static inline void alert_rule_46922(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46922;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fareit variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Fareit variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46936
// Rule Name: MALWARE-CNC Win.Trojan.Dropper outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46936
static inline int check_rule_46936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: HTTPREAD|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46936
static inline void alert_rule_46936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Dropper outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Dropper outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46966
// Rule Name: MALWARE-CNC Win.Trojan.Danabot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46966
static inline int check_rule_46966(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?m=S&",fast_pattern,nocase', '&a=', '&b=', '&d=', '&e=', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46966
static inline void alert_rule_46966(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46966;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Danabot outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Danabot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46967
// Rule Name: MALWARE-CNC Win.Trojan.Danabot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46967
static inline int check_rule_46967(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?m=F&",fast_pattern,nocase', '&a=', '&b=', '&d=', '&e=', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46967
static inline void alert_rule_46967(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46967;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Danabot outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Danabot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46968
// Rule Name: MALWARE-CNC Win.Trojan.Danabot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46968
static inline int check_rule_46968(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/index.php?m=T&",fast_pattern,nocase', '&a=', '&b=', '&d=', '&e=', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46968
static inline void alert_rule_46968(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46968;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Danabot outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Danabot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46969
// Rule Name: MALWARE-CNC Win.Trojan.Autophyte dropper variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46969
static inline int check_rule_46969(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mainls.cs",fast_pattern,nocase', 'Content-Type: application/octet-stream",nocase', '!"User-Agent",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46969
static inline void alert_rule_46969(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46969;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Autophyte dropper variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Autophyte dropper variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46970
// Rule Name: MALWARE-CNC Win.Trojan.Autophyte RAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46970
static inline int check_rule_46970(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Disposition: form-data|3B| name=|22|board_id|22|",fast_pattern,nocase', 'Content-Disposition: form-data|3B| name=|22|user_id|22|', 'Content-Disposition: form-data|3B| name=|22|file1|22|', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46970
static inline void alert_rule_46970(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46970;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Autophyte RAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Autophyte RAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46979
// Rule Name: INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46979
static inline int check_rule_46979(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host: ', '|0D 0A|",within 14', 'OPTIONS', 'User-Agent: Microsoft Office ', 'Discovery|0D 0A|",within 25', '!"Accept', '!"Referer|3A|', '!"Cookie|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46979
static inline void alert_rule_46979(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46979;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service", 
                     sizeof("INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46980
// Rule Name: INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 46980
static inline int check_rule_46980(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Host: ', '|0D 0A|",within 14', 'HEAD', 'User-Agent: Microsoft Office ', 'Discovery|0D 0A|",within 25', '!"Accept', '!"Content-', '!"Referer|3A|', '!"Cookie|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46980
static inline void alert_rule_46980(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46980;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service", 
                     sizeof("INDICATOR-COMPROMISE Microsoft Office Discovery User-Agent to a potential URL shortener service"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46981
// Rule Name: MALWARE-CNC Win.Trojan.Orcus RAT inbound SSL certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 46981
static inline int check_rule_46981(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|16 03|",depth 2', '|02|",within 1,distance 3', '|0C|Orcus Server",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46981
static inline void alert_rule_46981(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46981;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Orcus RAT inbound SSL certificate", 
                     sizeof("MALWARE-CNC Win.Trojan.Orcus RAT inbound SSL certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 46983
// Rule Name: INDICATOR-COMPROMISE Microsoft cmd.exe banner
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: successful-admin
// Generated from Snort rule parser output

// Check function for rule 46983
static inline int check_rule_46983(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Microsoft Windows",depth 18', 'Microsoft Corp",within 250']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 46983
static inline void alert_rule_46983(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 46983;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Microsoft cmd.exe banner", 
                     sizeof("INDICATOR-COMPROMISE Microsoft cmd.exe banner"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47005
// Rule Name: MALWARE-CNC Win.Trojan.SocketPlayer outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47005
static inline int check_rule_47005(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [3000, 5156]
    if (ntohs(tcph->dest) != 3000 && ntohs(tcph->dest) != 5156)
        return 0;
    // Content matching (simplified)
    // Full pattern: POST /cl/uplod/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47005
static inline void alert_rule_47005(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47005;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SocketPlayer outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SocketPlayer outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47006
// Rule Name: MALWARE-CNC Win.Trojan.SocketPlayer outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47006
static inline int check_rule_47006(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port list: [1337]
    if (ntohs(tcph->dest) != 1337)
        return 0;
    // Content matching (simplified)
    // Full pattern: /uploads/excutbls/h/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47006
static inline void alert_rule_47006(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47006;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SocketPlayer outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.SocketPlayer outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47016
// Rule Name: MALWARE-CNC Win.Spyware.Invisimole CnC outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47016
static inline int check_rule_47016(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/www/",depth 5,fast_pattern', '/00",distance 0', '!"Accept|3A|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47016
static inline void alert_rule_47016(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47016;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Spyware.Invisimole CnC outbound connection", 
                     sizeof("MALWARE-CNC Win.Spyware.Invisimole CnC outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47067
// Rule Name: MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47067
static inline int check_rule_47067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/show_new.php?",fast_pattern,nocase', 'code=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47067
static inline void alert_rule_47067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47068
// Rule Name: MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47068
static inline int check_rule_47068(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/register.php?",fast_pattern,nocase', 'p=",nocase', '&code=",nocase', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47068
static inline void alert_rule_47068(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47068;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47069
// Rule Name: MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47069
static inline int check_rule_47069(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/update_new.php?",fast_pattern,nocase', 'code=",nocase', '!"User-Agent']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47069
static inline void alert_rule_47069(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47069;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TechSupportScam installed binary outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47093
// Rule Name: PUA-ADWARE Win.Adware.Pbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 47093
static inline int check_rule_47093(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/installstarted",fast_pattern,nocase', 'de=",nocase', '_v=",nocase', '_s=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47093
static inline void alert_rule_47093(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47093;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.Pbot variant outbound connection", 
                     sizeof("PUA-ADWARE Win.Adware.Pbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47094
// Rule Name: PUA-ADWARE Win.Adware.Pbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 47094
static inline int check_rule_47094(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/collect.php",fast_pattern,nocase', 'pid=', 'cid=', 'sid=', 'act=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47094
static inline void alert_rule_47094(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47094;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.Pbot variant outbound connection", 
                     sizeof("PUA-ADWARE Win.Adware.Pbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47095
// Rule Name: PUA-ADWARE Win.Adware.Pbot variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 47095
static inline int check_rule_47095(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/installended",fast_pattern,nocase', 'de=",nocase', '_v=",nocase', '_s=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47095
static inline void alert_rule_47095(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47095;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Win.Adware.Pbot variant outbound connection", 
                     sizeof("PUA-ADWARE Win.Adware.Pbot variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47177
// Rule Name: MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound communication
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47177
static inline int check_rule_47177(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |B0 00 B0 00 B0 00 B0 00 26 00 26 00 26 00|",depth 15
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47177
static inline void alert_rule_47177(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47177;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound communication", 
                     sizeof("MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound communication"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47178
// Rule Name: MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47178
static inline int check_rule_47178(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['|50 00 00 00|",depth 4', '!"|0A|",within 1,distance 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47178
static inline void alert_rule_47178(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47178;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.NukeSped RAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47320
// Rule Name: MALWARE-CNC Js.Trojan.Agent JS Sniffer beacon connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47320
static inline int check_rule_47320(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?', '=WyJ1cmw",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47320
static inline void alert_rule_47320(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47320;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Js.Trojan.Agent JS Sniffer beacon connection", 
                     sizeof("MALWARE-CNC Js.Trojan.Agent JS Sniffer beacon connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47338
// Rule Name: MALWARE-CNC Win.Trojan.ARS VBS loader outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47338
static inline int check_rule_47338(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['?os=', '&user=', '&av=', '&fw=', '&hwid=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47338
static inline void alert_rule_47338(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47338;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ARS VBS loader outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ARS VBS loader outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47377
// Rule Name: MALWARE-CNC Unix.Trojan.Vpnfilter plugin variant connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47377
static inline int check_rule_47377(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |00 AC D3 62 78 26 76 31 E5 E7 E5 1D C2 3C 15 40 25 2F 90 BD 1F 7F 0E 5E 33 77 EC 0C 1E 6B 61 47|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47377
static inline void alert_rule_47377(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47377;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Trojan.Vpnfilter plugin variant connection attempt", 
                     sizeof("MALWARE-CNC Unix.Trojan.Vpnfilter plugin variant connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47386
// Rule Name: SERVER-WEBAPP Oracle WebLogic Server unauthenticated modified JSP access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 47386
static inline int check_rule_47386(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7001
    if (tcph->dest != htons(7001))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['/ws_utc/css/config/keystore/",fast_pattern,nocase', '.jsp']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47386
static inline void alert_rule_47386(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47386;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle WebLogic Server unauthenticated modified JSP access attempt", 
                     sizeof("SERVER-WEBAPP Oracle WebLogic Server unauthenticated modified JSP access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47387
// Rule Name: SERVER-WEBAPP Oracle WebLogic Server potential unauthenticated reconnaissance attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 47387
static inline int check_rule_47387(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7001
    if (tcph->dest != htons(7001))
        return 0;
    // Content matching (simplified)
    // Full pattern: /ws_utc/resources/setting/options/general",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47387
static inline void alert_rule_47387(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47387;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle WebLogic Server potential unauthenticated reconnaissance attempt", 
                     sizeof("SERVER-WEBAPP Oracle WebLogic Server potential unauthenticated reconnaissance attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47388
// Rule Name: SERVER-WEBAPP Oracle WebLogic Server potential precursor to keystore attack attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 47388
static inline int check_rule_47388(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 7001
    if (tcph->dest != htons(7001))
        return 0;
    // Content matching (simplified)
    // Full pattern: /ws_utc/resources/setting/keystore",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47388
static inline void alert_rule_47388(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47388;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Oracle WebLogic Server potential precursor to keystore attack attempt", 
                     sizeof("SERVER-WEBAPP Oracle WebLogic Server potential precursor to keystore attack attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47414
// Rule Name: MALWARE-CNC Osx.Trojan.Calisto outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47414
static inline int check_rule_47414(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calisto/upload.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47414
static inline void alert_rule_47414(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47414;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.Calisto outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.Calisto outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47415
// Rule Name: MALWARE-CNC Osx.Trojan.Calisto outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47415
static inline int check_rule_47415(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /calisto/listenyee.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47415
static inline void alert_rule_47415(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47415;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.Calisto outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.Calisto outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47427
// Rule Name: MALWARE-CNC Win.Trojan.Mapoyun variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47427
static inline int check_rule_47427(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['Connection:Close|3B|",fast_pattern,nocase', 'X-CA-",nocase', '!"User-Agent|3A|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47427
static inline void alert_rule_47427(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47427;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Mapoyun variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Mapoyun variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47556
// Rule Name: MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47556
static inline int check_rule_47556(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['GET /logo.png HTTP/1.1|0D 0A|",depth 24', 'User-Agent: Mozilla/4.0 (compatible|3B| MSIE 8.0)|0D 0A|",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47556
static inline void alert_rule_47556(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47556;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47557
// Rule Name: MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47557
static inline int check_rule_47557(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['GET /index.php?id=",depth 18', 'HTTP/1.1|0D 0A|",within 10,distance 11,nocase', 'Cookie:', '!"=",within 50', '!"|3B|",within 50']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47557
static inline void alert_rule_47557(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47557;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.PLEAD downloader outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47567
// Rule Name: MALWARE-CNC Win.Trojan.Zegost variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47567
static inline int check_rule_47567(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: |2A 00 00 00|",depth 4
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47567
static inline void alert_rule_47567(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47567;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zegost variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zegost variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47613
// Rule Name: SERVER-WEBAPP Joomla Proclaim biblestudy backup access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 47613
static inline int check_rule_47613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HTTP_SERVERS (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/media/com_biblestudy/backup/",fast_pattern,nocase', '.sql']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47613
static inline void alert_rule_47613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Joomla Proclaim biblestudy backup access attempt", 
                     sizeof("SERVER-WEBAPP Joomla Proclaim biblestudy backup access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47627
// Rule Name: MALWARE-CNC Win.Trojan.KeyPass variant inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47627
static inline int check_rule_47627(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['|7B 22|line1|22 3A 22|",depth 10,fast_pattern', '|22|line2|22 3A 22|",within 30,distance 30']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47627
static inline void alert_rule_47627(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47627;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.KeyPass variant inbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.KeyPass variant inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47640
// Rule Name: SERVER-WEBAPP SSL certificate with null issuer rdnSequence fields detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 47640
static inline int check_rule_47640(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: |30 07 06 03 55 04 06 13 00 31 09 30 07 06 03 55 04 08 13 00 31 09 30 07 06 03 55 04 07 13 00 31 09 30 07 06 03 55 04 0A 13 00 31 09 30 07 06 03 55 04 0B 13 00 31 09 30 07 06 03 55 04 03 13 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47640
static inline void alert_rule_47640(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47640;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP SSL certificate with null issuer rdnSequence fields detected", 
                     sizeof("SERVER-WEBAPP SSL certificate with null issuer rdnSequence fields detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47650
// Rule Name: MALWARE-CNC Win.Trojan.Marap outbound beacon detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47650
static inline int check_rule_47650(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/dot.php",fast_pattern,nocase', 'param=",depth 6']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47650
static inline void alert_rule_47650(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47650;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Marap outbound beacon detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Marap outbound beacon detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47723
// Rule Name: MALWARE-CNC Andr.Trojan.MysteryBot outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47723
static inline int check_rule_47723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /site/gate.php?i=eyAiYWN0aW9uIjog",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47723
static inline void alert_rule_47723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.MysteryBot outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.MysteryBot outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47876
// Rule Name: MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47876
static inline int check_rule_47876(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/private/",fast_pattern', '.php",distance 0', 'p=', 'User-Agent:', 'Android",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47876
static inline void alert_rule_47876(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47876;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected", 
                     sizeof("MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47877
// Rule Name: MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47877
static inline int check_rule_47877(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/private/checkPanel.php",fast_pattern,nocase', 'User-Agent:', 'Android",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47877
static inline void alert_rule_47877(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47877;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected", 
                     sizeof("MALWARE-CNC Andr.Trojan.AnubisCrypt variant outbound post detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47898
// Rule Name: MALWARE-CNC Win.Trojan.OilRig variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 47898
static inline int check_rule_47898(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /tahw?",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47898
static inline void alert_rule_47898(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47898;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.OilRig variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.OilRig variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47899
// Rule Name: MALWARE-CNC Win.Trojan.OilRig variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 47899
static inline int check_rule_47899(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /khc?",depth 5
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47899
static inline void alert_rule_47899(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47899;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.OilRig variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.OilRig variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47900
// Rule Name: MALWARE-CNC Win.Trojan.OilRig variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 47900
static inline int check_rule_47900(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pser?",depth 6
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47900
static inline void alert_rule_47900(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47900;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.OilRig variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.OilRig variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47934
// Rule Name: MALWARE-CNC Win.Trojan.MSDownloader variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47934
static inline int check_rule_47934(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: MS_D0wnl0ad3r",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47934
static inline void alert_rule_47934(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47934;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MSDownloader variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MSDownloader variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47935
// Rule Name: MALWARE-CNC Win.Trojan.MSDownloader variant download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47935
static inline int check_rule_47935(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Content-Type|3A 20|multipart/form-data|3B| boundary=MS_D0wnl0ad3r",fast_pattern,fast_pattern_offset 44,fast_pattern_length 13
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47935
static inline void alert_rule_47935(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47935;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MSDownloader variant download", 
                     sizeof("MALWARE-CNC Win.Trojan.MSDownloader variant download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 47936
// Rule Name: MALWARE-CNC Win.Trojan.MSDownloader variant download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 47936
static inline int check_rule_47936(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: Content-Type|3A 20|multipart/form-data|3B| boundary=MS_D0wnl0ad3r",fast_pattern,fast_pattern_offset 44,fast_pattern_length 13
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 47936
static inline void alert_rule_47936(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 47936;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MSDownloader variant download", 
                     sizeof("MALWARE-CNC Win.Trojan.MSDownloader variant download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48035
// Rule Name: MALWARE-CNC Win.Trojan.AcridRain outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48035
static inline int check_rule_48035(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['browser/Vivaldi.txtPK",fast_pattern,nocase', '/Upload/', '!"User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48035
static inline void alert_rule_48035(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48035;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.AcridRain outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.AcridRain outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48036
// Rule Name: MALWARE-CNC Win.Trojan.AcridRain outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48036
static inline int check_rule_48036(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/Libs.zip",fast_pattern,nocase', '!"User-Agent|3A 20|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48036
static inline void alert_rule_48036(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48036;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.AcridRain outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.AcridRain outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48092
// Rule Name: MALWARE-CNC Win.Trojan.MirageFox variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48092
static inline int check_rule_48092(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /image_download.php?uid=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48092
static inline void alert_rule_48092(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48092;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MirageFox variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MirageFox variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48093
// Rule Name: MALWARE-CNC Win.Trojan.MirageFox variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48093
static inline int check_rule_48093(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/search?gid=",fast_pattern,nocase', 'Accept:*/*', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48093
static inline void alert_rule_48093(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48093;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.MirageFox variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.MirageFox variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48115
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48115
static inline int check_rule_48115(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /dl.itranslator.info/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48115
static inline void alert_rule_48115(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48115;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48116
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48116
static inline int check_rule_48116(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/gl.php?uid=",fast_pattern,nocase', '&v=', '&x=",within 20']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48116
static inline void alert_rule_48116(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48116;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48117
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48117
static inline int check_rule_48117(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ufiles/",fast_pattern,nocase', '.dll', 'UID: ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48117
static inline void alert_rule_48117(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48117;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48118
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48118
static inline int check_rule_48118(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: ITRANSLATOR|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48118
static inline void alert_rule_48118(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48118;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48119
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48119
static inline int check_rule_48119(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: UID: P002|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48119
static inline void alert_rule_48119(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48119;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48120
// Rule Name: MALWARE-CNC Win.Trojan.ITranslator variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48120
static inline int check_rule_48120(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/cfg?cb=",fast_pattern,nocase', '&guid=', '&uid=",distance 0', '&ua=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48120
static inline void alert_rule_48120(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48120;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ITranslator variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.ITranslator variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48140
// Rule Name: MALWARE-CNC Win.Downloader.XAgent variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48140
static inline int check_rule_48140(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: &itwm=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48140
static inline void alert_rule_48140(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48140;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.XAgent variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Downloader.XAgent variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48764
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48764
static inline int check_rule_48764(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /technet-support/library/online-service-description.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48764
static inline void alert_rule_48764(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48764;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48765
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48765
static inline int check_rule_48765(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /advance/portable_version/service.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48765
static inline void alert_rule_48765(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48765;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48766
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48766
static inline int check_rule_48766(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /pkg/image/do.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48766
static inline void alert_rule_48766(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48766;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48767
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48767
static inline int check_rule_48767(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /Templates/NormalOld.dotm",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48767
static inline void alert_rule_48767(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48767;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant payload download attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48818
// Rule Name: MALWARE-CNC Js.Trojan.Agent variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48818
static inline int check_rule_48818(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A| xmsSofts_1.0.0_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48818
static inline void alert_rule_48818(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48818;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Js.Trojan.Agent variant outbound connection", 
                     sizeof("MALWARE-CNC Js.Trojan.Agent variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48819
// Rule Name: MALWARE-CNC Js.Trojan.Agent variant inbound payload download
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48819
static inline int check_rule_48819(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /blogs/enc7.js",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48819
static inline void alert_rule_48819(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48819;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Js.Trojan.Agent variant inbound payload download", 
                     sizeof("MALWARE-CNC Js.Trojan.Agent variant inbound payload download"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48844
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48844
static inline int check_rule_48844(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /company-device-support/values/correlate-sec.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48844
static inline void alert_rule_48844(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48844;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48845
// Rule Name: MALWARE-CNC Osx.Trojan.WindTail outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48845
static inline int check_rule_48845(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.php?very=",fast_pattern,nocase', '&xnvk=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48845
static inline void alert_rule_48845(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48845;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.WindTail outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.WindTail outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48846
// Rule Name: MALWARE-CNC Osx.Trojan.WindTail outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48846
static inline int check_rule_48846(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: usrnode/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48846
static inline void alert_rule_48846(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48846;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.WindTail outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.WindTail outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48847
// Rule Name: MALWARE-CNC Osx.Trojan.WindTail outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48847
static inline int check_rule_48847(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /qgHUDRZiYhOqQiN/kESklNvxsNZQcPl.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48847
static inline void alert_rule_48847(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48847;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.WindTail outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.WindTail outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48872
// Rule Name: MALWARE-CNC Win.Trojan.Agent variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48872
static inline int check_rule_48872(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /foth1018/go.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48872
static inline void alert_rule_48872(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48872;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Agent variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Agent variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48873
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48873
static inline int check_rule_48873(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/js/drv",fast_pattern,nocase', '!"Referer:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48873
static inline void alert_rule_48873(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48873;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48874
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48874
static inline int check_rule_48874(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ourtyaz/dwnack.php?cId=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48874
static inline void alert_rule_48874(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48874;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48875
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48875
static inline int check_rule_48875(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /MarkQuality455/developerbuild.php?b=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48875
static inline void alert_rule_48875(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48875;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48876
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48876
static inline int check_rule_48876(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /purchase61dfdusfdsu/costnbenifit8889.php?p=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48876
static inline void alert_rule_48876(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48876;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48877
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48877
static inline int check_rule_48877(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ourtyaz/qwe.php?TIe=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48877
static inline void alert_rule_48877(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48877;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48878
// Rule Name: MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48878
static inline int check_rule_48878(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /winter/zxd.php?TIe=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48878
static inline void alert_rule_48878(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48878;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BitterRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48904
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 48904
static inline int check_rule_48904(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /software-apptication/help-support-apl/getidpolapl.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48904
static inline void alert_rule_48904(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48904;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48984
// Rule Name: PROTOCOL-SCADA PCOM Identification ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48984
static inline int check_rule_48984(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: ID",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48984
static inline void alert_rule_48984(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48984;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Identification ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Identification ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48985
// Rule Name: PROTOCOL-SCADA PCOM Init Device ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 48985
static inline int check_rule_48985(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: CCI",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48985
static inline void alert_rule_48985(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48985;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Init Device ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Init Device ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48986
// Rule Name: PROTOCOL-SCADA PCOM Set UnitID ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48986
static inline int check_rule_48986(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: US",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48986
static inline void alert_rule_48986(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48986;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Set UnitID ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Set UnitID ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48987
// Rule Name: PROTOCOL-SCADA PCOM Get UnitID ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48987
static inline int check_rule_48987(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: UG",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48987
static inline void alert_rule_48987(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48987;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get UnitID ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Get UnitID ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48988
// Rule Name: PROTOCOL-SCADA PCOM Read Inputs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48988
static inline int check_rule_48988(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RE",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48988
static inline void alert_rule_48988(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48988;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Inputs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read Inputs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48989
// Rule Name: PROTOCOL-SCADA PCOM Set RTC ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48989
static inline int check_rule_48989(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SC",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48989
static inline void alert_rule_48989(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48989;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Set RTC ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Set RTC ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48990
// Rule Name: PROTOCOL-SCADA PCOM Read Ouputs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48990
static inline int check_rule_48990(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RA",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48990
static inline void alert_rule_48990(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48990;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Ouputs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read Ouputs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48991
// Rule Name: PROTOCOL-SCADA PCOM Read System Bits ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48991
static inline int check_rule_48991(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: GS",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48991
static inline void alert_rule_48991(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48991;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read System Bits ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read System Bits ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48992
// Rule Name: PROTOCOL-SCADA PCOM Read Memory Integers ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48992
static inline int check_rule_48992(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RW",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48992
static inline void alert_rule_48992(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48992;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Memory Integers ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read Memory Integers ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48993
// Rule Name: PROTOCOL-SCADA PCOM Read Memory Longs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48993
static inline int check_rule_48993(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RNL",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48993
static inline void alert_rule_48993(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48993;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Memory Longs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read Memory Longs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48994
// Rule Name: PROTOCOL-SCADA PCOM Write System Integers ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48994
static inline int check_rule_48994(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SF",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48994
static inline void alert_rule_48994(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48994;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write System Integers ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write System Integers ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48995
// Rule Name: PROTOCOL-SCADA PCOM Write System Bits ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48995
static inline int check_rule_48995(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SS",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48995
static inline void alert_rule_48995(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48995;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write System Bits ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write System Bits ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48996
// Rule Name: PROTOCOL-SCADA PCOM Read System Longs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48996
static inline int check_rule_48996(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RNH",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48996
static inline void alert_rule_48996(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48996;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read System Longs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read System Longs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48997
// Rule Name: PROTOCOL-SCADA PCOM Read System Integers ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48997
static inline int check_rule_48997(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: GF",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48997
static inline void alert_rule_48997(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48997;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read System Integers ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read System Integers ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48998
// Rule Name: PROTOCOL-SCADA PCOM Read Memory Bits ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48998
static inline int check_rule_48998(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RB",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48998
static inline void alert_rule_48998(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48998;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Memory Bits ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Read Memory Bits ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 48999
// Rule Name: PROTOCOL-SCADA PCOM Write Ouputs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 48999
static inline int check_rule_48999(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SA",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 48999
static inline void alert_rule_48999(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 48999;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Ouputs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write Ouputs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49000
// Rule Name: PROTOCOL-SCADA PCOM Stop Device ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 49000
static inline int check_rule_49000(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: CCS",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49000
static inline void alert_rule_49000(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49000;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Stop Device ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Stop Device ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49001
// Rule Name: PROTOCOL-SCADA PCOM Start Device ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 49001
static inline int check_rule_49001(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: CCR",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49001
static inline void alert_rule_49001(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49001;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Start Device ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Start Device ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49002
// Rule Name: PROTOCOL-SCADA PCOM Write System Longs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49002
static inline int check_rule_49002(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SNH",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49002
static inline void alert_rule_49002(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49002;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write System Longs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write System Longs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49003
// Rule Name: PROTOCOL-SCADA PCOM Get RTC ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49003
static inline int check_rule_49003(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RC",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49003
static inline void alert_rule_49003(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49003;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get RTC ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Get RTC ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49004
// Rule Name: PROTOCOL-SCADA PCOM Write Memory Bits ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49004
static inline int check_rule_49004(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SB",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49004
static inline void alert_rule_49004(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49004;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Memory Bits ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write Memory Bits ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49005
// Rule Name: PROTOCOL-SCADA PCOM Reset Device ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-dos
// Generated from Snort rule parser output

// Check function for rule 49005
static inline int check_rule_49005(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: CCE",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49005
static inline void alert_rule_49005(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49005;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Reset Device ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Reset Device ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49006
// Rule Name: PROTOCOL-SCADA PCOM Write Memory Longs ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49006
static inline int check_rule_49006(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SNL",depth 3,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49006
static inline void alert_rule_49006(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49006;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Memory Longs ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write Memory Longs ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49007
// Rule Name: PROTOCOL-SCADA PCOM Write Memory Integers ASCII request 
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49007
static inline int check_rule_49007(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SW",depth 2,offset 9
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49007
static inline void alert_rule_49007(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49007;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Memory Integers ASCII request ", 
                     sizeof("PROTOCOL-SCADA PCOM Write Memory Integers ASCII request "));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49008
// Rule Name: PROTOCOL-SCADA PCOM Read Operands binary request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49008
static inline int check_rule_49008(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |4D|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49008
static inline void alert_rule_49008(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49008;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Operands binary request", 
                     sizeof("PROTOCOL-SCADA PCOM Read Operands binary request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49009
// Rule Name: PROTOCOL-SCADA PCOM Set UnitID ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49009
static inline int check_rule_49009(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: US",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49009
static inline void alert_rule_49009(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49009;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Set UnitID ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Set UnitID ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49010
// Rule Name: PROTOCOL-SCADA PCOM Get RTC ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49010
static inline int check_rule_49010(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RC",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49010
static inline void alert_rule_49010(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49010;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get RTC ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Get RTC ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49011
// Rule Name: PROTOCOL-SCADA PCOM Identification ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49011
static inline int check_rule_49011(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: ID",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49011
static inline void alert_rule_49011(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49011;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Identification ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Identification ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49012
// Rule Name: PROTOCOL-SCADA PCOM Write Data Table binary request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49012
static inline int check_rule_49012(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |44|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49012
static inline void alert_rule_49012(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49012;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Data Table binary request", 
                     sizeof("PROTOCOL-SCADA PCOM Write Data Table binary request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49013
// Rule Name: PROTOCOL-SCADA PCOM Get UnitID ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49013
static inline int check_rule_49013(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: UG",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49013
static inline void alert_rule_49013(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49013;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get UnitID ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Get UnitID ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49014
// Rule Name: PROTOCOL-SCADA PCOM Read Data Table binary request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49014
static inline int check_rule_49014(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |04|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49014
static inline void alert_rule_49014(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49014;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Data Table binary request", 
                     sizeof("PROTOCOL-SCADA PCOM Read Data Table binary request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49015
// Rule Name: PROTOCOL-SCADA PCOM Get PLC Name binary request
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49015
static inline int check_rule_49015(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 20256
    if (tcph->dest != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |0C|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49015
static inline void alert_rule_49015(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49015;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get PLC Name binary request", 
                     sizeof("PROTOCOL-SCADA PCOM Get PLC Name binary request"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49016
// Rule Name: PROTOCOL-SCADA PCOM Set RTC ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49016
static inline int check_rule_49016(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SC",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49016
static inline void alert_rule_49016(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49016;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Set RTC ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Set RTC ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49017
// Rule Name: PROTOCOL-SCADA PCOM Read Inputs ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49017
static inline int check_rule_49017(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RE",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49017
static inline void alert_rule_49017(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49017;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Inputs ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Inputs ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49018
// Rule Name: PROTOCOL-SCADA PCOM Read System Bits ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49018
static inline int check_rule_49018(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: GS",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49018
static inline void alert_rule_49018(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49018;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read System Bits ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read System Bits ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49019
// Rule Name: PROTOCOL-SCADA PCOM Read Longs ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49019
static inline int check_rule_49019(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RN",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49019
static inline void alert_rule_49019(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49019;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Longs ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Longs ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49020
// Rule Name: PROTOCOL-SCADA PCOM Read System Integers ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49020
static inline int check_rule_49020(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: GF",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49020
static inline void alert_rule_49020(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49020;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read System Integers ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read System Integers ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49021
// Rule Name: PROTOCOL-SCADA PCOM Read Ouputs ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49021
static inline int check_rule_49021(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RA",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49021
static inline void alert_rule_49021(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49021;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Ouputs ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Ouputs ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49022
// Rule Name: PROTOCOL-SCADA PCOM Read Memory Bits ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49022
static inline int check_rule_49022(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RB",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49022
static inline void alert_rule_49022(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49022;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Memory Bits ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Memory Bits ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49023
// Rule Name: PROTOCOL-SCADA PCOM Read Memory Integers ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49023
static inline int check_rule_49023(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: RW",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49023
static inline void alert_rule_49023(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49023;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Memory Integers ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Memory Integers ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49024
// Rule Name: PROTOCOL-SCADA PCOM Write Memory Bits ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49024
static inline int check_rule_49024(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SB",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49024
static inline void alert_rule_49024(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49024;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Memory Bits ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write Memory Bits ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49025
// Rule Name: PROTOCOL-SCADA PCOM Write System Integers ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49025
static inline int check_rule_49025(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SF",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49025
static inline void alert_rule_49025(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49025;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write System Integers ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write System Integers ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49026
// Rule Name: PROTOCOL-SCADA PCOM Write System Bits ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49026
static inline int check_rule_49026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SS",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49026
static inline void alert_rule_49026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write System Bits ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write System Bits ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49027
// Rule Name: PROTOCOL-SCADA PCOM Write Ouputs ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49027
static inline int check_rule_49027(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SA",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49027
static inline void alert_rule_49027(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49027;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Ouputs ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write Ouputs ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49028
// Rule Name: PROTOCOL-SCADA PCOM Write Memory Integers ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49028
static inline int check_rule_49028(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SW",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49028
static inline void alert_rule_49028(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49028;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Memory Integers ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write Memory Integers ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49029
// Rule Name: PROTOCOL-SCADA PCOM Write Longs ASCII reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49029
static inline int check_rule_49029(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: SN",depth 2,offset 10
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49029
static inline void alert_rule_49029(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49029;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Longs ASCII reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write Longs ASCII reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49030
// Rule Name: PROTOCOL-SCADA PCOM Read Operands binary reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49030
static inline int check_rule_49030(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |CD|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49030
static inline void alert_rule_49030(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49030;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Operands binary reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Operands binary reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49031
// Rule Name: PROTOCOL-SCADA PCOM Get PLC Name binary reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49031
static inline int check_rule_49031(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |8C|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49031
static inline void alert_rule_49031(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49031;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Get PLC Name binary reply", 
                     sizeof("PROTOCOL-SCADA PCOM Get PLC Name binary reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49032
// Rule Name: PROTOCOL-SCADA PCOM Write Data Table binary reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49032
static inline int check_rule_49032(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |C4|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49032
static inline void alert_rule_49032(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49032;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Write Data Table binary reply", 
                     sizeof("PROTOCOL-SCADA PCOM Write Data Table binary reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49033
// Rule Name: PROTOCOL-SCADA PCOM Read Data Table binary reply
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49033
static inline int check_rule_49033(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check src port: 20256
    if (tcph->source != htons(20256))
        return 0;
    // Content matching (simplified)
    // Full pattern: |84|",depth 1,offset 18
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49033
static inline void alert_rule_49033(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49033;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PROTOCOL-SCADA PCOM Read Data Table binary reply", 
                     sizeof("PROTOCOL-SCADA PCOM Read Data Table binary reply"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49042
// Rule Name: PUA-ADWARE Osx.Adware.FairyTail variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49042
static inline int check_rule_49042(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: SpellingChecker/22",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49042
static inline void alert_rule_49042(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49042;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Osx.Adware.FairyTail variant outbound connection detected", 
                     sizeof("PUA-ADWARE Osx.Adware.FairyTail variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49043
// Rule Name: PUA-ADWARE Osx.Adware.Genieo variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49043
static inline int check_rule_49043(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: LinqurySearch",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49043
static inline void alert_rule_49043(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49043;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Osx.Adware.Genieo variant outbound connection detected", 
                     sizeof("PUA-ADWARE Osx.Adware.Genieo variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49044
// Rule Name: PUA-ADWARE Osx.Adware.MacSearch variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49044
static inline int check_rule_49044(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent: macsearch/1 CFNetwork/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49044
static inline void alert_rule_49044(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49044;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "PUA-ADWARE Osx.Adware.MacSearch variant outbound connection detected", 
                     sizeof("PUA-ADWARE Osx.Adware.MacSearch variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49090
// Rule Name: SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 49090
static inline int check_rule_49090(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FF|SMB|A2 00 00 00 00|",depth 9,offset 4', '/",within 2', '/",within len,distance 2']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49090
static inline void alert_rule_49090(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49090;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt", 
                     sizeof("SERVER-SAMBA Samba is_known_pipe arbitrary module load code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49282
// Rule Name: SERVER-WEBAPP Magecart inbound scan for vulnerable plugin attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49282
static inline int check_rule_49282(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['?misc=",fast_pattern,nocase', '&dl=', '/index.php/', '!"Referer', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49282
static inline void alert_rule_49282(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49282;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Magecart inbound scan for vulnerable plugin attempt", 
                     sizeof("SERVER-WEBAPP Magecart inbound scan for vulnerable plugin attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49396
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49396
static inline int check_rule_49396(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /help-desk/remote-assistant-service/PostId.php?q=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49396
static inline void alert_rule_49396(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49396;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49397
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49397
static inline int check_rule_49397(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /ministerstvo-energetiki/seminars/2019/06/Seminar.rtf",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49397
static inline void alert_rule_49397(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49397;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49398
// Rule Name: MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49398
static inline int check_rule_49398(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: S|00|e|00|m|00|i|00|n|00|a|00|r|00|_|00|2|00|0|00|1|00|8|00|_|00|1|00|.|00|A|00|O|00|-|00|A|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49398
static inline void alert_rule_49398(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49398;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zebrocy variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49418
// Rule Name: SERVER-WEBAPP Orange LiveBox unauthorized credentials access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-recon
// Generated from Snort rule parser output

// Check function for rule 49418
static inline int check_rule_49418(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /get_getnetworkconf.cgi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49418
static inline void alert_rule_49418(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49418;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Orange LiveBox unauthorized credentials access attempt", 
                     sizeof("SERVER-WEBAPP Orange LiveBox unauthorized credentials access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49571
// Rule Name: MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49571
static inline int check_rule_49571(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.png?ID=",fast_pattern,nocase', '&MAC=', '&OS=', '&BIT=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49571
static inline void alert_rule_49571(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49571;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49572
// Rule Name: MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49572
static inline int check_rule_49572(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exez?ID=",fast_pattern,nocase', '&GUID=', '&_T=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49572
static inline void alert_rule_49572(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49572;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.Fakewmi variant outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49592
// Rule Name: MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49592
static inline int check_rule_49592(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/bbs/data/tmp/ping.php",fast_pattern,nocase', 'word=",nocase', 'note=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49592
static inline void alert_rule_49592(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49592;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49593
// Rule Name: MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49593
static inline int check_rule_49593(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /indox.php?v=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49593
static inline void alert_rule_49593(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49593;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49594
// Rule Name: MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49594
static inline int check_rule_49594(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: .php?file=Cobra_",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49594
static inline void alert_rule_49594(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49594;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49595
// Rule Name: MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49595
static inline int check_rule_49595(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /board.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49595
static inline void alert_rule_49595(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49595;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.SectorA05 outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49664
// Rule Name: MALWARE-CNC Win.Trojan.TSCookie variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49664
static inline int check_rule_49664(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 443
    if (tcph->dest != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['User-Agent: Mozilla/4.0 (compatible|3B| MSIE 8.0|3B| Win32)|0D 0A|",fast_pattern,nocase', '|20|/t",depth 4,offset 3', '.aspx?m=",within 20', '!"Referer']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49664
static inline void alert_rule_49664(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49664;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TSCookie variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TSCookie variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49676
// Rule Name: MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49676
static inline int check_rule_49676(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |00 00 00 00 73 63 61 6E 6E 69 6E 67 2E 2E 2E 00 20 3A 20 00 73 63 61 6E 20 66 69 6E 69 73 65 64 00 00 00 00 63 3A 2F 2E 6C 6F 67 00 77 61 72 6D 69 6E 67 20 75 70 2E 2E 2E 00 00 00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49676
static inline void alert_rule_49676(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49676;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49677
// Rule Name: MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49677
static inline int check_rule_49677(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['AVBlockTransformation",fast_pattern,nocase', 'boost', 'BlockCipher",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49677
static inline void alert_rule_49677(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49677;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49678
// Rule Name: MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49678
static inline int check_rule_49678(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['AVBlockTransformation",fast_pattern,nocase', 'boost', 'BlockCipher",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49678
static inline void alert_rule_49678(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49678;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49679
// Rule Name: MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49679
static inline int check_rule_49679(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: |40 74 6B 85 FF 7C 65 7F 04 85 F6 74 5F 8B 03 8B 40 04 8B 4C 18 38 0F B7 54 18 40 89 55 EC 8B 41|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49679
static inline void alert_rule_49679(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49679;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49680
// Rule Name: MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49680
static inline int check_rule_49680(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |40 74 6B 85 FF 7C 65 7F 04 85 F6 74 5F 8B 03 8B 40 04 8B 4C 18 38 0F B7 54 18 40 89 55 EC 8B 41|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49680
static inline void alert_rule_49680(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49680;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt", 
                     sizeof("MALWARE-CNC Win.Ransomware.Lockergoga binary download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49788
// Rule Name: MALWARE-CNC Win.Trojan.Zacinlo outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49788
static inline int check_rule_49788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/entry/feedbackinfo/production/",fast_pattern,nocase', 'User-Agent: wget', 'Referer:', '/entry/feedbackinfo/production/",within 100', '!"Accept-', '!"Content-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49788
static inline void alert_rule_49788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zacinlo outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zacinlo outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49789
// Rule Name: MALWARE-CNC Win.Trojan.Zacinlo outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49789
static inline int check_rule_49789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/updaterinfo.bin",fast_pattern,nocase', 'User-Agent: wget', 'Referer:', '/updater/",within 50', '!"Accept-Encoding', '!"Accept-', '!"Content-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49789
static inline void alert_rule_49789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zacinlo outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zacinlo outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49790
// Rule Name: MALWARE-CNC Win.Trojan.Zacinlo outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 49790
static inline int check_rule_49790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/entry/exe/runinfo",fast_pattern,nocase', '&mac=', 'User-Agent: wget', 'Referer:', '/entry/",within 50', '!"Accept-', '!"Content-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49790
static inline void alert_rule_49790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Zacinlo outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Zacinlo outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49837
// Rule Name: SERVER-WEBAPP Tpshop remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49837
static inline int check_rule_49837(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/vendor/phpdocumentor/reflection-docblock/tests/phpDocumentor/Reflection/DocBlock/Tag/LinkTagTeet.php",fast_pattern,nocase', 'down_url=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49837
static inline void alert_rule_49837(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49837;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Tpshop remote file include attempt", 
                     sizeof("SERVER-WEBAPP Tpshop remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49838
// Rule Name: SERVER-WEBAPP Tpshop remote file include attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49838
static inline int check_rule_49838(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/vendor/phpdocumentor/reflection-docblock/tests/phpDocumentor/Reflection/DocBlock/Tag/LinkTagTeet.php",fast_pattern,nocase', 'down_url=",nocase', '|3A|/']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49838
static inline void alert_rule_49838(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49838;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Tpshop remote file include attempt", 
                     sizeof("SERVER-WEBAPP Tpshop remote file include attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49839
// Rule Name: SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49839
static inline int check_rule_49839(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ipecs-cm/download",fast_pattern,nocase', 'file",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49839
static inline void alert_rule_49839(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49839;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt", 
                     sizeof("SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49840
// Rule Name: SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49840
static inline int check_rule_49840(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ipecs-cm/download",fast_pattern,nocase', 'file",nocase', '../']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49840
static inline void alert_rule_49840(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49840;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt", 
                     sizeof("SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49841
// Rule Name: SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49841
static inline int check_rule_49841(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ipecs-cm/download",fast_pattern,nocase', 'Content-Disposition",nocase', 'filename",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49841
static inline void alert_rule_49841(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49841;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt", 
                     sizeof("SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 49842
// Rule Name: SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 49842
static inline int check_rule_49842(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/ipecs-cm/download",fast_pattern,nocase', 'file",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 49842
static inline void alert_rule_49842(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 49842;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt", 
                     sizeof("SERVER-WEBAPP LG-Ericsson iPECS NMS 30M directory traversal attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50137
// Rule Name: OS-WINDOWS Microsoft Windows RDP MS_T120 channel bind attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 50137
static inline int check_rule_50137(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: MS_T120|00|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50137
static inline void alert_rule_50137(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50137;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows RDP MS_T120 channel bind attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows RDP MS_T120 channel bind attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50258
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50258
static inline int check_rule_50258(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/7773/index.php",fast_pattern,nocase', '&string=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50258
static inline void alert_rule_50258(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50258;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50259
// Rule Name: MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50259
static inline int check_rule_50259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/php/gate.php",fast_pattern,nocase', 'key=",nocase', '&string=",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50259
static inline void alert_rule_50259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50260
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50260
static inline int check_rule_50260(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/7773/plug/",fast_pattern,nocase', '.ahk']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50260
static inline void alert_rule_50260(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50260;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50261
// Rule Name: MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50261
static inline int check_rule_50261(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/?gate&hwid=",fast_pattern,nocase', '&pwd=', 'pcuser",distance 0', 'admin",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50261
static inline void alert_rule_50261(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50261;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50262
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50262
static inline int check_rule_50262(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /7773/plug/htv/tv.dll",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50262
static inline void alert_rule_50262(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50262;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50263
// Rule Name: MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50263
static inline int check_rule_50263(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['logincreds=",fast_pattern,nocase', 'isAdmin=', 'antivirus=', 'commentary=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50263
static inline void alert_rule_50263(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50263;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Trojan.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50264
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50264
static inline int check_rule_50264(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /7773/uploads/upload.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50264
static inline void alert_rule_50264(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50264;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50276
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50276
static inline int check_rule_50276(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /temporary_listen_addresses/smsservice",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50276
static inline void alert_rule_50276(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50276;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50277
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50277
static inline int check_rule_50277(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/temporary_listen_addresses/wsman",fast_pattern,nocase', '_reguestguid",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50277
static inline void alert_rule_50277(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50277;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Chopper webshell inbound request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50424
// Rule Name: MALWARE-CNC User-Agent known malicious user agent - BURAN - Win.Trojan.Buran
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50424
static inline int check_rule_50424(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|BURAN|0D 0A|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50424
static inline void alert_rule_50424(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50424;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC User-Agent known malicious user agent - BURAN - Win.Trojan.Buran", 
                     sizeof("MALWARE-CNC User-Agent known malicious user agent - BURAN - Win.Trojan.Buran"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50445
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50445
static inline int check_rule_50445(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.exe",fast_pattern,nocase', 'User-Agent: Microsoft Internet Explorer|0D 0A|', '!"Connection', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50445
static inline void alert_rule_50445(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50445;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot additional payload download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50446
// Rule Name: MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50446
static inline int check_rule_50446(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/get.php?pid=",fast_pattern,nocase', 'User-Agent: Microsoft Internet Explorer|0D 0A|', '!"Connection', '!"Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50446
static inline void alert_rule_50446(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50446;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection", 
                     sizeof("MALWARE-CNC Win.Downloader.TeamBot outbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50811
// Rule Name: MALWARE-CNC Unix.Backdoor.Godlua variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50811
static inline int check_rule_50811(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port: 65314
    if (tcph->dest != htons(65314))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|01 00 10|",depth 3', '|00 12 5C|",within 3,distance 9']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50811
static inline void alert_rule_50811(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50811;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Backdoor.Godlua variant outbound connection", 
                     sizeof("MALWARE-CNC Unix.Backdoor.Godlua variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 50989
// Rule Name: MALWARE-CNC Win.Dropper.Clipbanker variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 50989
static inline int check_rule_50989(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /wp-content/plugins/WPSecurity/load.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 50989
static inline void alert_rule_50989(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 50989;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Dropper.Clipbanker variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Dropper.Clipbanker variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51368
// Rule Name: MALWARE-BACKDOOR Win.Backdoor.Agent webshell inbound request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51368
static inline int check_rule_51368(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Response.Write(|22|UAshell|22|)|3B 0D 0A|",fast_pattern,nocase', '/ua.aspx']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51368
static inline void alert_rule_51368(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51368;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Win.Backdoor.Agent webshell inbound request attempt", 
                     sizeof("MALWARE-BACKDOOR Win.Backdoor.Agent webshell inbound request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51484
// Rule Name: MALWARE-OTHER ANDR.Trojan.Agent outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51484
static inline int check_rule_51484(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /recv_android.php",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51484
static inline void alert_rule_51484(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51484;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER ANDR.Trojan.Agent outbound connection attempt", 
                     sizeof("MALWARE-OTHER ANDR.Trojan.Agent outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51532
// Rule Name: MALWARE-CNC Win.Trojan.BlackRAT variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51532
static inline int check_rule_51532(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['ProClient.Data",fast_pattern,nocase', 'Clientx|2C 20|Version=",nocase', 'data|05|bytes",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51532
static inline void alert_rule_51532(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51532;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRAT variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRAT variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51533
// Rule Name: MALWARE-CNC Win.Trojan.BlackRAT variant inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51533
static inline int check_rule_51533(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: ['BlackRAT.Data",fast_pattern,nocase', '|2C 20|Version=",nocase', 'data|05|bytes",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51533
static inline void alert_rule_51533(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51533;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BlackRAT variant inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.BlackRAT variant inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51541
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51541
static inline int check_rule_51541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: User-Agent|3A 20|Installer/23",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51541
static inline void alert_rule_51541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51542
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51542
static inline int check_rule_51542(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /webkit_login_records",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51542
static inline void alert_rule_51542(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51542;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51543
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51543
static inline int check_rule_51543(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['PATCH', '/installers/', 'X-Installer-",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51543
static inline void alert_rule_51543(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51543;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51544
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51544
static inline int check_rule_51544(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/upload_files', 'X-File-Name:",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51544
static inline void alert_rule_51544(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51544;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51545
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51545
static inline int check_rule_51545(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/tasks', 'X-Finder-",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51545
static inline void alert_rule_51545(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51545;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51546
// Rule Name: MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51546
static inline int check_rule_51546(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/webkit_cookies', 'X-Finder-",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51546
static inline void alert_rule_51546(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51546;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.ModularInstaller variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51636
// Rule Name: MALWARE-CNC Win.Trojan.Amadey botnet outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51636
static inline int check_rule_51636(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['bi=",fast_pattern,nocase', '/index.php",nocase', 'id=",nocase', 'sd=",nocase', 'vs=",nocase', 'ar=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51636
static inline void alert_rule_51636(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51636;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Amadey botnet outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Amadey botnet outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51642
// Rule Name: MALWARE-CNC Osx.Trojan.Gmera variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51642
static inline int check_rule_51642(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/link.php?",depth 13,fast_pattern', 'User-Agent: curl/', '!"Referer",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51642
static inline void alert_rule_51642(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51642;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Osx.Trojan.Gmera variant outbound connection", 
                     sizeof("MALWARE-CNC Osx.Trojan.Gmera variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51670
// Rule Name: MALWARE-CNC Win.Trojan.Silence variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51670
static inline int check_rule_51670(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/showthread.php?",fast_pattern,nocase', 'alphayz=']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51670
static inline void alert_rule_51670(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51670;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Silence variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Silence variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51671
// Rule Name: MALWARE-CNC Win.Trojan.Silence variant outbound connection detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51671
static inline int check_rule_51671(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: /showthread.php?yz=",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51671
static inline void alert_rule_51671(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51671;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Silence variant outbound connection detected", 
                     sizeof("MALWARE-CNC Win.Trojan.Silence variant outbound connection detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 51672
// Rule Name: MALWARE-CNC Andr.Trojan.Moonshine outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 51672
static inline int check_rule_51672(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Check dst port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['User-Agent: hots scot",fast_pattern,nocase', '/ws?",nocase', 'whisky_id=",nocase', 'device_id=",nocase', 'Upgrade: websocket",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 51672
static inline void alert_rule_51672(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 51672;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Andr.Trojan.Moonshine outbound connection", 
                     sizeof("MALWARE-CNC Andr.Trojan.Moonshine outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52026
// Rule Name: MALWARE-OTHER Xml.Phishing.Evernote outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 52026
static inline int check_rule_52026(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_spacertoofee=",fast_pattern,nocase', 'hondacbrheavy']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52026
static inline void alert_rule_52026(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52026;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Xml.Phishing.Evernote outbound connection", 
                     sizeof("MALWARE-OTHER Xml.Phishing.Evernote outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52027
// Rule Name: MALWARE-OTHER Xml.Phishing.Evernote outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 52027
static inline int check_rule_52027(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_truthcolor=",fast_pattern,nocase', 'dramafrine']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52027
static inline void alert_rule_52027(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52027;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Xml.Phishing.Evernote outbound connection", 
                     sizeof("MALWARE-OTHER Xml.Phishing.Evernote outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52056
// Rule Name: FILE-IDENTIFY Portable Executable binary file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 52056
static inline int check_rule_52056(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['ZM', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52056
static inline void alert_rule_52056(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52056;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable binary file magic detected", 
                     sizeof("FILE-IDENTIFY Portable Executable binary file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52057
// Rule Name: FILE-IDENTIFY Portable Executable binary file magic detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 52057
static inline int check_rule_52057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['ZM', 'PE|00 00|",within 4,distance -64']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52057
static inline void alert_rule_52057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "FILE-IDENTIFY Portable Executable binary file magic detected", 
                     sizeof("FILE-IDENTIFY Portable Executable binary file magic detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52259
// Rule Name: MALWARE-CNC Js.Trojan.FakeUpdate outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 52259
static inline int check_rule_52259(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: POST /1x1.png HTTP/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52259
static inline void alert_rule_52259(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52259;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Js.Trojan.FakeUpdate outbound connection", 
                     sizeof("MALWARE-CNC Js.Trojan.FakeUpdate outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52548
// Rule Name: MALWARE-CNC Win.Trojan.XpertRAT inbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 52548
static inline int check_rule_52548(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)

    // Content matching (simplified)
    // Full pattern: |7C 30 7C A1 40 23 40 21|",depth 8,offset 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52548
static inline void alert_rule_52548(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52548;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.XpertRAT inbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.XpertRAT inbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52549
// Rule Name: MALWARE-CNC Win.Trojan.XpertRAT outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 52549
static inline int check_rule_52549(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)

    // Content matching (simplified)
    // Full pattern: |7C|root|7C|",depth 6,offset 3
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52549
static inline void alert_rule_52549(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52549;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.XpertRAT outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.XpertRAT outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 52620
// Rule Name: SERVER-WEBAPP Citrix ADC and Gateway arbitrary code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 52620
static inline int check_rule_52620(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/vpns/",fast_pattern,nocase', 'NSC_USER:', '../",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 52620
static inline void alert_rule_52620(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 52620;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Citrix ADC and Gateway arbitrary code execution attempt", 
                     sizeof("SERVER-WEBAPP Citrix ADC and Gateway arbitrary code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 53341
// Rule Name: SERVER-APACHE Apache Tomcat AJP connector arbitrary file access attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 53341
static inline int check_rule_53341(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 8009
    if (tcph->dest != htons(8009))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|12 34|",depth 2', '|02|",within 1,distance 2', 'HTTP",within protocol_len,nocase', 'javax.servlet.include.request_uri|00|",fast_pattern,nocase', 'javax.servlet.include.servlet_path|00|",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 53341
static inline void alert_rule_53341(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 53341;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-APACHE Apache Tomcat AJP connector arbitrary file access attempt", 
                     sizeof("SERVER-APACHE Apache Tomcat AJP connector arbitrary file access attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 53582
// Rule Name: INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 53582
static inline int check_rule_53582(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['0200000002CE020000000000C000000000000046",fast_pattern,nocase', '6269747361646d696e",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 53582
static inline void alert_rule_53582(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 53582;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt", 
                     sizeof("INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 53583
// Rule Name: INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 53583
static inline int check_rule_53583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['0200000002CE020000000000C000000000000046",fast_pattern,nocase', '6269747361646d696e",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 53583
static inline void alert_rule_53583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 53583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt", 
                     sizeof("INDICATOR-COMPROMISE RTF document with Equation and BITSAdmin download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 53584
// Rule Name: MALWARE-CNC Win.Trojan.FormBook variant outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 53584
static inline int check_rule_53584(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type: application/x-www-form-urlencoded|0D 0A|Accept: */*|0D 0A|Referer:",fast_pattern,nocase', 'Connection: close|0D 0A|Content-Length:', 'Cache-Control: no-cache|0D 0A|Origin:', 'POST', '=",depth 10', '!"=",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 53584
static inline void alert_rule_53584(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 53584;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.FormBook variant outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.FormBook variant outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 54053
// Rule Name: MALWARE-CNC Win.Trojan.Copperhedge outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 54053
static inline int check_rule_54053(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: *dJU!*JE&!M@UNQ@",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 54053
static inline void alert_rule_54053(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 54053;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Copperhedge outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Copperhedge outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 54054
// Rule Name: MALWARE-CNC Win.Trojan.Copperhedge outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 54054
static inline int check_rule_54054(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: t34kjfdla45l",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 54054
static inline void alert_rule_54054(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 54054;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Copperhedge outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Copperhedge outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 54055
// Rule Name: MALWARE-CNC Win.Trojan.Copperhedge outbound connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 54055
static inline int check_rule_54055(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_webident_f",fast_pattern,nocase', '_webident_s']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 54055
static inline void alert_rule_54055(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 54055;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.Copperhedge outbound connection", 
                     sizeof("MALWARE-CNC Win.Trojan.Copperhedge outbound connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 54793
// Rule Name: MALWARE-CNC Unix.Malware.Drovorub cnc inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 54793
static inline int check_rule_54793(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;



    // Content matching (simplified)
    // Full pattern: ['{|22|children|22|:[{|22|name|22|:",fast_pattern,nocase', '|81|",depth 1', '|22 2C 22|value|22 3A|",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 54793
static inline void alert_rule_54793(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 54793;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Unix.Malware.Drovorub cnc inbound connection attempt", 
                     sizeof("MALWARE-CNC Unix.Malware.Drovorub cnc inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56583
// Rule Name: MALWARE-TOOLS GhostPack Rubeus kerberos request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56583
static inline int check_rule_56583(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;


    // Check dst port: 88
    if (tcph->dest != htons(88))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|05|",depth 30', '|0A|",within 1,distance 4', 'Z', '|A7 06 02 04 6C 69 6C 00|",within 25']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56583
static inline void alert_rule_56583(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56583;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-TOOLS GhostPack Rubeus kerberos request attempt", 
                     sizeof("MALWARE-TOOLS GhostPack Rubeus kerberos request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56585
// Rule Name: MALWARE-TOOLS GhostPack Rubeus kerberos request attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56585
static inline int check_rule_56585(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;



    // Content matching (simplified)
    // Full pattern: User32LogonProcesss",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56585
static inline void alert_rule_56585(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56585;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-TOOLS GhostPack Rubeus kerberos request attempt", 
                     sizeof("MALWARE-TOOLS GhostPack Rubeus kerberos request attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56587
// Rule Name: MALWARE-CNC Win.Backdoor.SSLBeacon variant certificate exchange attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56587
static inline int check_rule_56587(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['ajax.microsoft.com",fast_pattern,nocase', 'Information Technologies",nocase', 'Microsoft",nocase', 'Seattle",nocase', 'WA",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56587
static inline void alert_rule_56587(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56587;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.SSLBeacon variant certificate exchange attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.SSLBeacon variant certificate exchange attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56594
// Rule Name: MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56594
static inline int check_rule_56594(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $FILE_DATA_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: aqlKZ7wjzg0iKM00E1WB/jq9_RA46w91EKl9A02Dv/nbNdZiLsB1ci8Ph0fb64/9Ks1YxAE86iz9A0dUiDl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56594
static inline void alert_rule_56594(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56594;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt", 
                     sizeof("MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56595
// Rule Name: MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56595
static inline int check_rule_56595(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: aqlKZ7wjzg0iKM00E1WB/jq9_RA46w91EKl9A02Dv/nbNdZiLsB1ci8Ph0fb64/9Ks1YxAE86iz9A0dUiDl
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56595
static inline void alert_rule_56595(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56595;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt", 
                     sizeof("MALWARE-BACKDOOR MultiOS.Malware.GORAT malware download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56596
// Rule Name: MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56596
static inline int check_rule_56596(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['GET', '|0D 0A|Cookie: SID1=",fast_pattern,nocase', '!"|0D 0A|Referer:', '!"|0D 0A|Accept']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56596
static inline void alert_rule_56596(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56596;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt", 
                     sizeof("MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56597
// Rule Name: MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56597
static inline int check_rule_56597(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', 'Connection: upgrade', '|0D 0A|Upgrade: tcp/1|0D 0A|",fast_pattern,nocase', '!"|0D 0A|Referer:', '!"|0D 0A|Accept', '!"|0D 0A|Cookie:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56597
static inline void alert_rule_56597(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56597;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt", 
                     sizeof("MALWARE-CNC MultiOS.Malware.GORAT outbound communications attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56598
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56598
static inline int check_rule_56598(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|22|meta|22|:{},|22|status|22|:|22|OK|22|,|22|saved|22|:|22|1|22|,|22|starttime|22|:17656184060,|22|id|22|:|22 22|,|22|vims|22|:{|22|dtc|22|:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56598
static inline void alert_rule_56598(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56598;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original inbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56599
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original stager outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56599
static inline int check_rule_56599(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept: */*', 'Accept-Language: en-US', 'Accept-Encoding: gzip, deflate', 'Cookie: SIDCC=AN0-TYutOSq-fxZK6e4kagm70VyKACiG1susXcYRuxK08Y-rHysliq0LWklTqjtulAhQOPH8uA', '/api/v1/user']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56599
static inline void alert_rule_56599(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56599;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original stager outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original stager outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56600
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56600
static inline int check_rule_56600(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept: */*|0D 0A|', 'Accept-Language: en-US|0D 0A|', 'Accept-Encoding: gzip, deflate|0D 0A|', '{|22|locale|22|:|22|en|22|,|22|channel|22|:|22|prod|22|,|22|addon|22|:|22|', 'ses-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56600
static inline void alert_rule_56600(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56600;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56601
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original Stager 2 download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56601
static inline int check_rule_56601(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Content-Type: text/json|0D 0A|', 'Server: Microsoft-IIS/10.0|0D 0A|', 'X-Powered-By: ASP.NET|0D 0A|', 'Cache-Control: no-cache, no-store, max-age=0, must-revalidate|0D 0A|', 'Pragma: no-cache|0D 0A|', 'X-Frame-Options: SAMEORIGIN|0D 0A|', 'Content-Type: image/gif', '|01 00 01 00 00 02 01 44 00 3B|', '|FF FF FF 21 F9 04 01 00 00 00 2C 00 00 00 00|', '|47 49 46 38 39 61 01 00 01 00 80 00 00 00 00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56601
static inline void alert_rule_56601(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56601;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original Stager 2 download attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original Stager 2 download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56602
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original Server 3 inbound beacon attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56602
static inline int check_rule_56602(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|22|alias|22|:|22|apx|22|,|22|prefix|22|:|22 22|,|22|suffix|22|:null,|22|suggestions|22|:[],|22|responseId|22|:|22|15QE9JX9CKE2P|22|,|22|addon|22|: |22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56602
static inline void alert_rule_56602(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56602;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original Server 3 inbound beacon attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original Server 3 inbound beacon attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56603
// Rule Name: MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56603
static inline int check_rule_56603(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Accept: */*|0D 0A|', 'Accept-Language: en-US|0D 0A|', 'Accept-Encoding: gzip, deflate|0D 0A|', 'display-culture=en|3B|check=true|3B|lbcs=0|3B|sess-id=', '|3B|SIDCC=AN0-TY21iJHH32j2m|3B|FHBv3=B']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56603
static inline void alert_rule_56603(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56603;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt", 
                     sizeof("MALWARE-CNC Win.Backdoor.CSBundle_Original outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56605
// Rule Name: MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56605
static inline int check_rule_56605(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|22|navgd|22|:|22|<div class=gnt_n_dd_ls_w><div class=gnt_n_dd_nt>ONLY AT USA TODAY:</div><div class=gnt_n_dd_ls><a class=gnt_n_dd_ls_a href=https://supportlocal.usatoday.com/",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56605
static inline void alert_rule_56605(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56605;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt", 
                     sizeof("MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56606
// Rule Name: MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56606
static inline int check_rule_56606(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['X-Timer: S1593010188.776402,VS0,VE1",fast_pattern,nocase', 'Content-Type: application/json|5C 3B| charset=utf-8', 'Strict-Transport-Security: max-age=10890000', 'Cache-Control: public, immutable, max-age=315360000', 'X-Cache: HIT, HIT', 'Vary: X-AbVariant, X-AltUrl, Accept-Encoding']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56606
static inline void alert_rule_56606(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56606;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt", 
                     sizeof("MALWARE-CNC Rat.Tool.CSBundleUSATodayServer variant inbound command attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56607
// Rule Name: MALWARE-CNC potential Rat.Tool.CSBundleUSAToday connectivity check
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56607
static inline int check_rule_56607(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['gnt_ub=86|5C 3B|gnt_sb=18|5C 3B|usprivacy=1YNY|5C 3B|DigiTrust.v1.identity=', 'Host: www.usatoday.com']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56607
static inline void alert_rule_56607(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56607;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC potential Rat.Tool.CSBundleUSAToday connectivity check", 
                     sizeof("MALWARE-CNC potential Rat.Tool.CSBundleUSAToday connectivity check"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56608
// Rule Name: MALWARE-OTHER Cobalt Strike beacon inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56608
static inline int check_rule_56608(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|22|meta|22|:{},|22|status|22|:|22|OK|22|,|22|saved|22|:|22|1|22|,|22|starttime|22|:17656184060,|22|id|22|:|22 22|,|22|vims|22|:{|22|dtc|22|:|22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56608
static inline void alert_rule_56608(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56608;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Cobalt Strike beacon inbound connection attempt", 
                     sizeof("MALWARE-OTHER Cobalt Strike beacon inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56609
// Rule Name: MALWARE-OTHER Cobalt Strike beacon outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56609
static inline int check_rule_56609(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/v1/push', 'Accept: */*', 'Accept-Encoding: gzip, deflate, br', 'Accept-Language: en-US|0D 0A|', '{|22|locale|22|:|22|en|22|,|22|channel|22|:|22|prod|22|,|22|addon|22|:|22|",fast_pattern,nocase', 'cli', 'l-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56609
static inline void alert_rule_56609(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56609;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Cobalt Strike beacon outbound connection attempt", 
                     sizeof("MALWARE-OTHER Cobalt Strike beacon outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56610
// Rule Name: MALWARE-OTHER Cobalt Strike beacon outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56610
static inline int check_rule_56610(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['auth=0|5C 3B|loc=US}",fast_pattern,nocase', 'sess-=', 'Accept-Encoding: gzip, deflate, br', 'Accept-Language: en-US|0D 0A|', 'Accept: */*']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56610
static inline void alert_rule_56610(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56610;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Cobalt Strike beacon outbound connection attempt", 
                     sizeof("MALWARE-OTHER Cobalt Strike beacon outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56611
// Rule Name: MALWARE-OTHER Cobalt Strike beacon outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56611
static inline int check_rule_56611(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/notification', 'Accept: */*', 'Accept-Encoding: gzip, deflate, br', 'Accept-Language: en-US|0D 0A|', '{|22|locale|22|:|22|en|22|,|22|channel|22|:|22|prod|22|,|22|addon|22|:|22|",fast_pattern,nocase', 'nid', 'msg-']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56611
static inline void alert_rule_56611(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56611;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-OTHER Cobalt Strike beacon outbound connection attempt", 
                     sizeof("MALWARE-OTHER Cobalt Strike beacon outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56612
// Rule Name: MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56612
static inline int check_rule_56612(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Sec-Fetch-Dest:",nocase', 'empty",within 10,nocase', 'request_origin=user",fast_pattern,nocase', 'parent_request_id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56612
static inline void alert_rule_56612(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56612;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt", 
                     sizeof("MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56613
// Rule Name: MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56613
static inline int check_rule_56613(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['_gat_global=1",fast_pattern,nocase', '_gat_global=1",nocase', 'recent_locations",nocase', '_gat_www=1",nocase', 'bse=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56613
static inline void alert_rule_56613(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56613;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt", 
                     sizeof("MALWARE-CNC Rat.Tool.FeyeYelp variant outbound beacon attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56614
// Rule Name: MALWARE-BACKDOOR Cobalt Strike beacon connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56614
static inline int check_rule_56614(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['client-=",fast_pattern,nocase', 'Accept-Encoding|3A| gzip, deflate, br', '|5C 3B|auth=1|7D|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56614
static inline void alert_rule_56614(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56614;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Cobalt Strike beacon connection attempt", 
                     sizeof("MALWARE-BACKDOOR Cobalt Strike beacon connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56615
// Rule Name: MALWARE-CNC Cobalt Strike beacon outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 56615
static inline int check_rule_56615(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: nyt-gdpr=0|3B|nyt-purr=cfh|3B|nyt-geo=US}",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56615
static inline void alert_rule_56615(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56615;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Cobalt Strike beacon outbound connection attempt", 
                     sizeof("MALWARE-CNC Cobalt Strike beacon outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56616
// Rule Name: MALWARE-CNC Cobalt Strike beacon outbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 56616
static inline int check_rule_56616(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: {|22|locale|22 3A 22|en|22|,|22|channel|22 3A 22|prod|22|,|22|addon|22 3A 22|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56616
static inline void alert_rule_56616(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56616;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Cobalt Strike beacon outbound connection attempt", 
                     sizeof("MALWARE-CNC Cobalt Strike beacon outbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56617
// Rule Name: MALWARE-CNC Cobalt Strike beacon inbound connection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 56617
static inline int check_rule_56617(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['x-timer: S1580937960.346550,VS0,VE0",fast_pattern,nocase', 'Cache-Control: public,max-age=31536000', 'Server: UploadServer', 'Vary: Accept-Encoding, Fastly-SSL', 'x-api-version: F-X', 'x-Firefox-Spdy: h2', 'x-served-by: cache-mdw17344-MDW']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56617
static inline void alert_rule_56617(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56617;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Cobalt Strike beacon inbound connection attempt", 
                     sizeof("MALWARE-CNC Cobalt Strike beacon inbound connection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56862
// Rule Name: MALWARE-CNC MultiOS.Malware.GORAT outbound communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56862
static inline int check_rule_56862(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['POST', '|0D 0A 0D 0A|murica",fast_pattern,nocase', '!"|0D 0A|Referer:', '!"|0D 0A|Accept', '!"|0D 0A|Cookie:']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56862
static inline void alert_rule_56862(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56862;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Malware.GORAT outbound communication attempt", 
                     sizeof("MALWARE-CNC MultiOS.Malware.GORAT outbound communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56863
// Rule Name: MALWARE-CNC MultiOS.Malware.GORAT command and control response attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56863
static inline int check_rule_56863(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['HTTP/1.",depth 7', '|0D 0A 0D 0A|murica",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56863
static inline void alert_rule_56863(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56863;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Malware.GORAT command and control response attempt", 
                     sizeof("MALWARE-CNC MultiOS.Malware.GORAT command and control response attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56864
// Rule Name: MALWARE-CNC MultiOS.Malware.GORAT command and control SSL certificate
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56864
static inline int check_rule_56864(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port: 443
    if (tcph->source != htons(443))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|16 03 03|",depth 3', 'ACME Shell Co.0', 'Z",distance 0', 'ACME Shell Co.0",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56864
static inline void alert_rule_56864(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56864;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC MultiOS.Malware.GORAT command and control SSL certificate", 
                     sizeof("MALWARE-CNC MultiOS.Malware.GORAT command and control SSL certificate"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56891
// Rule Name: MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56891
static inline int check_rule_56891(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FE|SMB",depth 4,offset 4', '|0B 00|",within 2,distance 8', '|70 00 69 00 70 00 65 00 73 00 68 00 65 00 6C 00 6C 00 2D 00 70 00 69 00 70 00 65 00 6E 00 61 00 6D 00 65 00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56891
static inline void alert_rule_56891(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56891;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 56892
// Rule Name: MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 56892
static inline int check_rule_56892(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 445
    if (tcph->dest != htons(445))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|FE|SMB",depth 4,offset 4', '|05 00|",within 2,distance 8', '|70 00 69 00 70 00 65 00 73 00 68 00 65 00 6C 00 6C 00 2D 00 70 00 69 00 70 00 65 00 6E 00 61 00 6D 00 65 00|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 56892
static inline void alert_rule_56892(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 56892;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt", 
                     sizeof("MALWARE-CNC Win.Trojan.BasicPipeShell variant communication attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57067
// Rule Name: SERVER-OTHER HP Web JetAdmin file write attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-activity
// Generated from Snort rule parser output

// Check function for rule 57067
static inline int check_rule_57067(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/plugins/framework/script/tree.xms",fast_pattern,nocase', 'WriteToFile",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57067
static inline void alert_rule_57067(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57067;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER HP Web JetAdmin file write attempt", 
                     sizeof("SERVER-OTHER HP Web JetAdmin file write attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57252
// Rule Name: SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57252
static inline int check_rule_57252(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/DDIService.svc/SetObject",fast_pattern,nocase', 'VirtualDirectory",nocase', 'ExternalUrl",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57252
static inline void alert_rule_57252(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57252;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt", 
                     sizeof("SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57253
// Rule Name: SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57253
static inline int check_rule_57253(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/DDIService.svc/SetObject",fast_pattern,nocase', 'VirtualDirectory",nocase', 'FilePathName",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57253
static inline void alert_rule_57253(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57253;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt", 
                     sizeof("SERVER-MAIL Microsoft Exchange Server arbitrary file write attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57336
// Rule Name: POLICY-OTHER F5 iControl REST interface tm.util.bash invocation attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 57336
static inline int check_rule_57336(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/mgmt/tm/util/bash",fast_pattern,nocase', 'command",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57336
static inline void alert_rule_57336(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57336;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER F5 iControl REST interface tm.util.bash invocation attempt", 
                     sizeof("POLICY-OTHER F5 iControl REST interface tm.util.bash invocation attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57461
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.PULSECHECK variant cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57461
static inline int check_rule_57461(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/secid_canceltoken.cgi",fast_pattern,nocase', 'X-CMD|3A|",nocase', 'X-KEY|3A|",nocase', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57461
static inline void alert_rule_57461(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57461;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.PULSECHECK variant cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.PULSECHECK variant cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57462
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57462
static inline int check_rule_57462(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Results of |27|', '|27| execution:|0A 0A|",within 260']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57462
static inline void alert_rule_57462(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57462;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57463
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57463
static inline int check_rule_57463(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<form action=|22 22| method=|22|GET|22|>', '<input type=|22|text|22| name=|22|cmd|22|",distance 0', '<input type=|22|text|22| name=|22|serverid|22|",distance 0,fast_pattern', '<input type=|22|submit|22| value=|22|Run|22|>",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57463
static inline void alert_rule_57463(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57463;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE webshell variant access"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57464
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.HARDPULSE variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57464
static inline int check_rule_57464(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/compcheckjava.cgi",fast_pattern,nocase', 'hashid=",nocase', 'checkcode=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57464
static inline void alert_rule_57464(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57464;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.HARDPULSE variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.HARDPULSE variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57465
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57465
static inline int check_rule_57465(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/licenseserverproto.cgi",fast_pattern,nocase', 'id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57465
static inline void alert_rule_57465(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57465;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.STEADYPULSE variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57466
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57466
static inline int check_rule_57466(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/compcheckresult.cgi",fast_pattern,nocase', 'id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57466
static inline void alert_rule_57466(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57466;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57467
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57467
static inline int check_rule_57467(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/meeting_testmsjava.cgi",fast_pattern,nocase', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57467
static inline void alert_rule_57467(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57467;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57468
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57468
static inline int check_rule_57468(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/meeting_testjs.cgi",fast_pattern,nocase', 'POST']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57468
static inline void alert_rule_57468(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57468;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.SLIGHTPULSE variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57541
// Rule Name: MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 57541
static inline int check_rule_57541(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/compcheckresult.cgi",fast_pattern,nocase', 'id=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57541
static inline void alert_rule_57541(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57541;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection", 
                     sizeof("MALWARE-BACKDOOR Perl.Backdoor.ATRIUM variant inbound cnc connection"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57738
// Rule Name: POLICY-OTHER Active Directory Federation Services policy store transfer service request detected
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-activity
// Generated from Snort rule parser output

// Check function for rule 57738
static inline int check_rule_57738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/adfs/services/policystoretransfer",fast_pattern,nocase', 'POST",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57738
static inline void alert_rule_57738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Active Directory Federation Services policy store transfer service request detected", 
                     sizeof("POLICY-OTHER Active Directory Federation Services policy store transfer service request detected"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57879
// Rule Name: INDICATOR-COMPROMISE Revil Kaseya ransomware log clearing http upload
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: web-application-attack
// Generated from Snort rule parser output

// Check function for rule 57879
static inline int check_rule_57879(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Server.CreateObject', 'KComWExec.execCmd",fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57879
static inline void alert_rule_57879(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57879;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Revil Kaseya ransomware log clearing http upload", 
                     sizeof("INDICATOR-COMPROMISE Revil Kaseya ransomware log clearing http upload"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57906
// Rule Name: SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57906
static inline int check_rule_57906(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/autodiscover",fast_pattern,nocase', 'Email=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57906
static inline void alert_rule_57906(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57906;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt", 
                     sizeof("SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57907
// Rule Name: SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57907
static inline int check_rule_57907(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/autodiscover",fast_pattern,nocase', 'Email=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57907
static inline void alert_rule_57907(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57907;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt", 
                     sizeof("SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57908
// Rule Name: SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57908
static inline int check_rule_57908(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/autodiscover",fast_pattern,nocase', 'Email",nocase', 'Content-Disposition",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57908
static inline void alert_rule_57908(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57908;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt", 
                     sizeof("SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 57983
// Rule Name: SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 57983
static inline int check_rule_57983(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/autodiscover",fast_pattern,nocase', 'Email=",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 57983
static inline void alert_rule_57983(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 57983;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt", 
                     sizeof("SERVER-WEBAPP Microsoft Exchange autodiscover server side request forgery attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58722
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58722
static inline int check_rule_58722(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${jndi:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58722
static inline void alert_rule_58722(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58722;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58723
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58723
static inline int check_rule_58723(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${jndi:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58723
static inline void alert_rule_58723(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58723;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58724
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58724
static inline int check_rule_58724(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${lower:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58724
static inline void alert_rule_58724(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58724;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58725
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58725
static inline int check_rule_58725(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${lower:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58725
static inline void alert_rule_58725(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58725;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58726
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58726
static inline int check_rule_58726(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['jndi",fast_pattern,nocase', 'jndi",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58726
static inline void alert_rule_58726(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58726;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58727
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58727
static inline int check_rule_58727(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${jndi:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58727
static inline void alert_rule_58727(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58727;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58728
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58728
static inline int check_rule_58728(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${lower:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58728
static inline void alert_rule_58728(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58728;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58729
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58729
static inline int check_rule_58729(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${upper:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58729
static inline void alert_rule_58729(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58729;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58730
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58730
static inline int check_rule_58730(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['lower",fast_pattern,nocase', 'lower",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58730
static inline void alert_rule_58730(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58730;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58731
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58731
static inline int check_rule_58731(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${upper:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58731
static inline void alert_rule_58731(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58731;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58732
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58732
static inline int check_rule_58732(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['upper",fast_pattern,nocase', 'upper",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58732
static inline void alert_rule_58732(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58732;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58733
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58733
static inline int check_rule_58733(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${upper:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58733
static inline void alert_rule_58733(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58733;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58734
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58734
static inline int check_rule_58734(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: jndi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58734
static inline void alert_rule_58734(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58734;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58735
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58735
static inline int check_rule_58735(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: lower",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58735
static inline void alert_rule_58735(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58735;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58736
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58736
static inline int check_rule_58736(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: upper",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58736
static inline void alert_rule_58736(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58736;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58737
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58737
static inline int check_rule_58737(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: jndi",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58737
static inline void alert_rule_58737(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58737;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58738
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58738
static inline int check_rule_58738(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: upper",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58738
static inline void alert_rule_58738(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58738;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58739
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58739
static inline int check_rule_58739(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: lower",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58739
static inline void alert_rule_58739(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58739;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58740
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58740
static inline int check_rule_58740(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %24%7b",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58740
static inline void alert_rule_58740(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58740;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58741
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58741
static inline int check_rule_58741(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58741
static inline void alert_rule_58741(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58741;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58742
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58742
static inline int check_rule_58742(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['${",fast_pattern,nocase', '|2F|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58742
static inline void alert_rule_58742(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58742;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58744
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58744
static inline int check_rule_58744(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58744
static inline void alert_rule_58744(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58744;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58751
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58751
static inline int check_rule_58751(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $SMTP_SERVERS (destination)
    // Check dst port: 25
    if (tcph->dest != htons(25))
        return 0;
    // Content matching (simplified)
    // Full pattern: |24 7B|",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58751
static inline void alert_rule_58751(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58751;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58784
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58784
static inline int check_rule_58784(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %2524%257B",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58784
static inline void alert_rule_58784(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58784;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58785
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58785
static inline int check_rule_58785(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %24{",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58785
static inline void alert_rule_58785(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58785;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58786
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58786
static inline int check_rule_58786(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %2524{",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58786
static inline void alert_rule_58786(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58786;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58787
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58787
static inline int check_rule_58787(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %24%257b",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58787
static inline void alert_rule_58787(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58787;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58788
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58788
static inline int check_rule_58788(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: $%7b",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58788
static inline void alert_rule_58788(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58788;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58789
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58789
static inline int check_rule_58789(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: $%257b",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58789
static inline void alert_rule_58789(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58789;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58790
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58790
static inline int check_rule_58790(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: %2524%7b",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58790
static inline void alert_rule_58790(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58790;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58795
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 58795
static inline int check_rule_58795(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Authorization: Basic JH",fast_pattern,nocase', 'Authorization: Basic ']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58795
static inline void alert_rule_58795(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58795;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58801
// Rule Name: INDICATOR-COMPROMISE JNDI LDAP searchResEntry dynamic code download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 58801
static inline int check_rule_58801(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check src port list: []
    if ()
        return 0;
    // Content matching (simplified)
    // Full pattern: ['javaClassName",fast_pattern,nocase', 'javaCodeBase', 'objectClass', 'javaFactory']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58801
static inline void alert_rule_58801(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58801;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE JNDI LDAP searchResEntry dynamic code download attempt", 
                     sizeof("INDICATOR-COMPROMISE JNDI LDAP searchResEntry dynamic code download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 58814
// Rule Name: POLICY-OTHER Java User-Agent remote class download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: policy-violation
// Generated from Snort rule parser output

// Check function for rule 58814
static inline int check_rule_58814(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.class",fast_pattern,nocase', '|0D 0A|User-Agent: Java/",nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 58814
static inline void alert_rule_58814(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 58814;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "POLICY-OTHER Java User-Agent remote class download attempt", 
                     sizeof("POLICY-OTHER Java User-Agent remote class download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59502
// Rule Name: OS-WINDOWS Microsoft Windows Server 2003 smart card authentication buffer overflow attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 59502
static inline int check_rule_59502(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Check dst port: 3389
    if (tcph->dest != htons(3389))
        return 0;
    // Content matching (simplified)
    // Full pattern: ['|03 00|', '|02 F0|",distance 0', '|64|",distance 0', '|72 44 43 49|', '|DC 90 01 08|",within msg_size,fast_pattern']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59502
static inline void alert_rule_59502(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59502;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows Server 2003 smart card authentication buffer overflow attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows Server 2003 smart card authentication buffer overflow attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59925
// Rule Name: SERVER-WEBAPP Multiple products OGNL expression injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-admin
// Generated from Snort rule parser output

// Check function for rule 59925
static inline int check_rule_59925(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['.action",nocase', 'getRuntime', 'exec",within 15']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59925
static inline void alert_rule_59925(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59925;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Multiple products OGNL expression injection attempt", 
                     sizeof("SERVER-WEBAPP Multiple products OGNL expression injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59926
// Rule Name: INDICATOR-COMPROMISE Python remote shell spawn attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: misc-attack
// Generated from Snort rule parser output

// Check function for rule 59926
static inline int check_rule_59926(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['pty.spawn(|22|/bin/', 'sh",within 10']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59926
static inline void alert_rule_59926(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59926;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "INDICATOR-COMPROMISE Python remote shell spawn attempt", 
                     sizeof("INDICATOR-COMPROMISE Python remote shell spawn attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59927
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.TinyUploader upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59927
static inline int check_rule_59927(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['java.io.FileOutputStream",fast_pattern,nocase', '<%', 'request",within 100', 'write', 'getParameter",within 100']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59927
static inline void alert_rule_59927(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59927;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.TinyUploader upload attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.TinyUploader upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59928
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Chopper webshell download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59928
static inline int check_rule_59928(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['DriverManager.getConnection', 'ServletOutputStream', 'ResultSetMetaData', 'request.getParameter']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59928
static inline void alert_rule_59928(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59928;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Chopper webshell download attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Chopper webshell download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59929
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Behinder download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59929
static inline int check_rule_59929(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<%', 'java.util.*', 'extends ClassLoader",fast_pattern,nocase', 'defineClass', 'getInstance(|22|AES|22|)', 'decodeBuffer",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59929
static inline void alert_rule_59929(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59929;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Behinder download attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Behinder download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59930
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Noop download attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59930
static inline int check_rule_59930(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (src)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['java.util.*', 'java.io.FileOutputStream",distance 0', 'request.getParameter(|22|name|22|)",within 200,fast_pattern', 'request.getParameter(|22|contentString|22|)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59930
static inline void alert_rule_59930(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59930;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Noop download attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Noop download attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59931
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Chopper upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59931
static inline int check_rule_59931(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['DriverManager.getConnection', 'ServletOutputStream', 'ResultSetMetaData', 'request.getParameter']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59931
static inline void alert_rule_59931(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59931;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Chopper upload attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Chopper upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59932
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Behinder upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59932
static inline int check_rule_59932(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['<%', 'java.util.*', 'extends ClassLoader",fast_pattern,nocase', 'defineClass', 'getInstance(|22|AES|22|)', 'decodeBuffer",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59932
static inline void alert_rule_59932(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59932;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Behinder upload attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Behinder upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59933
// Rule Name: MALWARE-BACKDOOR Jsp.Webshell.Noop upload attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 59933
static inline int check_rule_59933(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['java.util.*', 'java.io.FileOutputStream",distance 0', 'request.getParameter(|22|name|22|)",within 200,fast_pattern', 'confluence",nocase', 'request.getParameter(|22|contentString|22|)']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59933
static inline void alert_rule_59933(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59933;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-BACKDOOR Jsp.Webshell.Noop upload attempt", 
                     sizeof("MALWARE-BACKDOOR Jsp.Webshell.Noop upload attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 59934
// Rule Name: SERVER-WEBAPP Atlassian Confluence OGNL expression injection attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 59934
static inline int check_rule_59934(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['${",fast_pattern', 'java",distance 0', '|28|",distance 0', '}",distance 0']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 59934
static inline void alert_rule_59934(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 59934;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Atlassian Confluence OGNL expression injection attempt", 
                     sizeof("SERVER-WEBAPP Atlassian Confluence OGNL expression injection attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 60642
// Rule Name: SERVER-WEBAPP Microsoft Exchange Server remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 60642
static inline int check_rule_60642(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['autodiscover.json",fast_pattern,nocase', 'Powershell",distance 0,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 60642
static inline void alert_rule_60642(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 60642;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP Microsoft Exchange Server remote code execution attempt", 
                     sizeof("SERVER-WEBAPP Microsoft Exchange Server remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 62362
// Rule Name: MALWARE-CNC Win.Dropper.Gamaredon command and control beacon attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: trojan-activity
// Generated from Snort rule parser output

// Check function for rule 62362
static inline int check_rule_62362(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $HOME_NET (source)
    // Variable IP check: $EXTERNAL_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['/log.php', 'Expect: 100-continue', 'i",depth 1']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 62362
static inline void alert_rule_62362(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 62362;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "MALWARE-CNC Win.Dropper.Gamaredon command and control beacon attempt", 
                     sizeof("MALWARE-CNC Win.Dropper.Gamaredon command and control beacon attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300052
// Rule Name: OS-WINDOWS Microsoft Windows HTTP protocol stack remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300052
static inline int check_rule_300052(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: Accept-Encoding:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300052
static inline void alert_rule_300052(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300052;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "OS-WINDOWS Microsoft Windows HTTP protocol stack remote code execution attempt", 
                     sizeof("OS-WINDOWS Microsoft Windows HTTP protocol stack remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300055
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300055
static inline int check_rule_300055(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${lower:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300055
static inline void alert_rule_300055(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300055;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300056
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300056
static inline int check_rule_300056(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${upper:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300056
static inline void alert_rule_300056(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300056;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300057
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300057
static inline int check_rule_300057(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${jndi:",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300057
static inline void alert_rule_300057(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300057;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300058
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300058
static inline int check_rule_300058(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ${",fast_pattern,nocase
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300058
static inline void alert_rule_300058(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300058;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300061
// Rule Name: SERVER-OTHER Apache Log4j logging remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300061
static inline int check_rule_300061(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['${",fast_pattern,nocase', '|2F|']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300061
static inline void alert_rule_300061(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300061;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-OTHER Apache Log4j logging remote code execution attempt", 
                     sizeof("SERVER-OTHER Apache Log4j logging remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}

// eBPF Rule Implementation: SID 300131
// Rule Name: SERVER-WEBAPP F5 BIG-IP iControl remote code execution attempt
// Protocol: TCP (6)
// Action: ALERT
// Priority: 3
// Classification: attempted-user
// Generated from Snort rule parser output

// Check function for rule 300131
static inline int check_rule_300131(struct __sk_buff *skb, void *data, void *data_end) {
    // Parse Ethernet header
    struct ethhdr *eth = data;
    if ((void *)(eth + 1) > data_end)
        return 0;

    // Parse IP header
    struct iphdr *iph = (struct iphdr *)(eth + 1);
    if ((void *)(iph + 1) > data_end)
        return 0;

    // Verify TCP protocol
    if (iph->protocol != IPPROTO_TCP)
        return 0;

    // Parse TCP header
    struct tcphdr *tcph = (struct tcphdr *)(iph + 1);
    if ((void *)(tcph + 1) > data_end)
        return 0;

    // Variable IP check: $EXTERNAL_NET (source)
    // Variable IP check: $HOME_NET (destination)
    // Variable port check: $HTTP_PORTS (dst)
    // Port variables need runtime resolution
    // Content matching (simplified)
    // Full pattern: ['Connection|3A|",nocase', 'X-F5-Auth",distance 0,fast_pattern,nocase']
    // Note: Complex content matching requires BPF string matching library
    return 1;  // All checks passed
}

// Alert function for rule 300131
static inline void alert_rule_300131(struct __sk_buff *skb, 
                                          __u32 src_ip, __u32 dst_ip,
                                          __u16 src_port, __u16 dst_port) {
    // Create alert event
    struct alert_event {
        __u32 rule_id;
        __u32 priority;
        __u32 src_ip;
        __u32 dst_ip;
        __u16 src_port;
        __u16 dst_port;
        char msg[256];
    } alert = {};
    
    alert.rule_id = 300131;
    alert.priority = 3;
    alert.src_ip = src_ip;
    alert.dst_ip = dst_ip;
    alert.src_port = src_port;
    alert.dst_port = dst_port;
    
    // Copy alert message (limited to 255 chars in eBPF)
    __builtin_memcpy(&alert.msg, "SERVER-WEBAPP F5 BIG-IP iControl remote code execution attempt", 
                     sizeof("SERVER-WEBAPP F5 BIG-IP iControl remote code execution attempt"));
    
    // Submit alert to userspace via perf buffer
    // events.perf_submit(ctx, &alert, sizeof(alert));
}
